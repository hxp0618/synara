apiVersion: v1
kind: ServiceAccount
metadata:
  name: synara-control-plane
  # Production overlays bind this exact ServiceAccount to a dedicated cloud
  # workload identity scoped to the Artifact bucket's tenants/* prefix.
  labels:
    app.kubernetes.io/name: synara-control-plane
    app.kubernetes.io/part-of: synara
