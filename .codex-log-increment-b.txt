warning: `--full-auto` is deprecated; use `--sandbox workspace-write` instead.
Reading additional input from stdin...
OpenAI Codex v0.145.0
--------
workdir: /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
model: gpt-5.6-sol
provider: custom
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: xhigh
reasoning summaries: none
session id: 019f9ec3-8e3c-7f20-9659-b5cc0f08fc42
--------
user
# Task: Implement guaranteed warm-pool min-idle (Increment B) in the Synara Go control plane

Work in THIS directory (a git worktree on branch `fast-cloud-agents`). Do not touch other checkouts.
Read `docs/plans/fast-cloud-agents-implementation.md` first — §2 has exact file:line anchors and the
frozen design decisions D1–D6. Implement Increment B exactly per those decisions.

## Goal

Upgrade Kubernetes warm pools from best-effort to a hard minimum-idle guarantee so interactive agents
start in seconds: a new `min_idle_units` floor per worker pool, replenished with priority BEFORE cold
execution Pods, exempt from demand eviction, with a deficit metric and alert.

## Scope (all in services/control-plane unless noted)

1. Migration `000086_worker_pool_min_idle.sql` (PostgreSQL, forward-only, follow the style of
   `migrations/000058_worker_pools_placement.sql` and `000069_worker_pool_warm_capacity.sql`):
   - `ALTER TABLE worker_pools ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0`;
   - `CHECK (min_idle_units >= 0 AND min_idle_units <= desired_idle_units)` (named constraint);
   - `ALTER TABLE worker_pool_warm_capacity ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0`;
   - extend `assert_worker_pool_warm_capacity_scope()` (defined in `000069:49-98`) via CREATE OR
     REPLACE so the authority row's `min_idle_units` must equal the pool's, same as desired/max.
2. SQLite parity: the Personal profile uses GORM AutoMigrate + trigger/safety mirrors under
   `internal/database` (see existing warm-capacity sqlite tests for the pattern). Mirror the same
   scope assertion the way `desired_idle_units` is mirrored today.
3. Models: `internal/persistence/placement_models.go` (pool struct) and
   `internal/persistence/warm_capacity_models.go` add `MinIdleUnits int` with matching gorm column.
4. Placement service API: `internal/placement/service.go` — add `MinIdleUnits *int` to
   `CreatePoolInput`/update input (`:81-92`, `:236`, `:292`), normalize + validate
   (0 <= min <= desiredIdleUnits) in `normalizePoolInput` (`:803`); surface the field in the HTTP
   handlers `internal/httpapi/placement_api.go:35,61` responses/requests (follow existing field
   plumbing style exactly).
5. Reconciler (the core change), `internal/executiontargets/kubernetes_reconciler.go` and
   `kubernetes_warm_pool.go`:
   - split `kubernetesWarmPodPlans` (`kubernetes_warm_pool.go:316-353`) output into guaranteed
     plans (slot index < pool.MinIdleUnits) and best-effort plans (the rest); keep the existing
     `desiredTotal = DesiredIdleUnits + claimed` clamp arithmetic unchanged;
   - create guaranteed warm plans BEFORE the cold execution Pod loop (`kubernetes_reconciler.go:826-899`),
     still respecting the target-wide `MaxActivePods` budget; best-effort plans stay where the current
     warm loop is (`:900-923`). When the budget truncates guaranteed plans, do not fail the pass —
     count the shortfall into the deficit (design D4, fail-visible);
   - exempt guaranteed slots (index < MinIdleUnits) from `warm-pool-demand-fallback` eviction
     (`:748-807`, design D3);
   - both plan computations (`:619-621` validation pass and `:808-810` post-eviction) must use the
     same split so hashes/pod names stay stable.
6. Warm capacity authority + deficit metric:
   - carry `MinIdleUnits` through `ManagedKubernetesWarmCapacityObservation`
     (`internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go:15-28`), the
     observation builder (`kubernetes_warm_pool.go:86-136`), and the writer
     `internal/warmcapacity/service.go` `Observe` (CAS update set);
   - project `synara_worker_pool_warm_deficit{capacity_class}` in
     `internal/observability/distributed_routing_billing_metrics.go` next to the existing
     warm-capacity gauges (`:507-625`): per fresh authority row deficit =
     max(0, min_idle_units - ready_idle_units), summed by bounded capacity class (reuse the existing
     class bounding/`other` collapse). Only fresh (unexpired) observations contribute, matching the
     existing `units` gauge freshness rule. Also export the already-stored `desired_idle_units` and
     new `min_idle_units` as `kind="desired_idle"` / `kind="min_idle"` series on the existing
     `synara_worker_pool_warm_capacity_units` gauge (bounded kinds only).
7. Alert rule: append to `deploy/kubernetes/monitoring/prometheus-rules.yaml` in the existing
   `synara-control-plane.fast-provision-slo` group:
   `SynaraWarmPoolMinIdleDeficit`: `sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0`
   for 10m, severity warning, description mentioning guaranteed warm floor unmet.
8. Contract doc: update `docs/contracts/worker-pool-placement-v1.md` — in the Worker Pools and
   Live warm-capacity authority sections, document `minIdleUnits` (guaranteed floor within
   desired_idle_units, priority replenish before cold Pods, eviction exemption for guaranteed slots,
   budget-truncation counts as visible deficit, deficit metric name). Keep the existing tone:
   precise, states what is and is not guaranteed.

## Constraints

- Migration numbering: `000086` exactly; never edit existing migrations except the explicit
  CREATE OR REPLACE of the scope-assertion function inside the NEW migration file.
- PostgreSQL + SQLite behavioral parity, following how desired/max are asserted today.
- Do not touch anything under `docs/reports/` (immutable evidence).
- Follow surrounding code style; no new dependencies; keep bounded-label discipline (no pool/target
  IDs in metric labels).
- Do not run `bun fmt`/`bun lint`/`bun typecheck` or any bun command; Go verification only.

## Verification (run all; fix failures)

```
cd services/control-plane
go build ./...
go test ./internal/placement/... ./internal/executiontargets/... ./internal/warmcapacity/... \
  ./internal/observability/... ./internal/persistence/... ./internal/database/... ./internal/httpapi/...
```

Add focused tests: pool input validation (min>desired rejected), plan split (guaranteed vs
best-effort slots, budget truncation), eviction exemption, deficit metric projection (fresh vs
expired), and the sqlite scope-assertion mirror. Follow the existing table-driven test patterns in
each package.

## Deliverable

When done, write a report to `.codex-report-increment-b.md` in the worktree root: what changed
(file list), design decisions applied, test results (exact go test output tail), and any remaining
risks or intentionally-unimplemented edges. Leave the worktree with all changes uncommitted.
warning: Under-development features enabled: chronicle. Under-development features are incomplete and may behave unpredictably. To suppress this warning, set `suppress_unstable_features_warning = true` in /Users/huang/.codex/config.toml.
codex
I’ll implement Increment B only in this worktree, beginning with the frozen D1–D6 plan and current Git state. I’ll preserve existing changes, add the focused Go/SQLite/metrics tests, run exactly the requested Go verification, and leave everything uncommitted with the requested report.
  → Read frozen plan, relevant memory, Git state, and existing implementation/tests
  • Implement migration, SQLite parity, models, placement API, reconciler, capacity writer, metrics, alert, and contract docs
  • Add focused tests for validation, split/budget, eviction, metrics freshness, and SQLite scope parity
  • Run requested Go build/tests, fix failures, and write .codex-report-increment-b.md
exec
/bin/zsh -lc "sed -n '1,260p' docs/plans/fast-cloud-agents-implementation.md" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
# Fast Cloud Agents 实现计划（worktree: fast-cloud-agents）

目标：在现有 Stage 4 逻辑之上，把"拉起一个远程 agent"的交互路径压到秒级。参照物：E2B（秒级
sandbox 分配）与 kubernetes-sigs/agent-sandbox（Sandbox/SandboxTemplate/SandboxClaim/
SandboxWarmPool CRD + 控制器模式）。设计蓝本：
[`fast-provision-runtime-proposal-v0`](fast-provision-runtime-proposal-v0.md)。

基线：分支 `fast-cloud-agents`，基于 `codex/saas-tenancy-user` @ `4e30436c`。

## 0. 概念对齐（agent-sandbox ↔ Synara 现有抽象）

| agent-sandbox        | Synara 现有物                                             | 差距                     |
| -------------------- | --------------------------------------------------------- | ------------------------ |
| Sandbox（稳定身份）  | Worker incarnation + 逻辑 Worker + Session Workspace      | 无——已有更强的 fencing  |
| SandboxTemplate      | Worker Release Revision + immutable Manifest              | 无                       |
| SandboxWarmPool      | `worker_pools.mode=warm` + Migration `000069` 容量权威    | **软偏好 → 硬保证**      |
| SandboxClaim         | Execution Claim（fairqueue + release-aware 匹配）         | **claim 后仍有长尾启动** |
| hibernation/resume   | suspend/resume（checkpoint + receipt）                    | 恢复介质慢（对象存储）   |
| gVisor/Kata 隔离     | Pod SecurityContext + 受保护 cgroup supervisor            | 后续可选运行时层         |

结论：不需要新领域模型；实现 = 把 warm 从"软偏好"升级为"硬保证"，并把 claim 后启动链路中
可以前置的部分全部前置。

## 1. 增量顺序（每个增量独立可合并、可验证）

### 增量 A：秒级 SLO 门禁（已完成，比原计划更便宜）

- 勘察结论：`provision_tier` 不需要新迁移——Migration `000059`/`000081` 已把
  `warm_pool_mode` + `warm_pool_result`（`pending | not-requested | hit | fallback`，常量在
  `internal/executions/generation_facts.go:18-21`）固化进 Generation fact，且
  `synara_execution_cold_start_duration_seconds_30d` 分位数 gauge 已携带这两个标签
  （`internal/observability/generation_fact_metrics.go:193-195`）。tier 语义即
  `hit`=warm 命中、`fallback`=请求 warm 但冷启动、`not-requested`=cold。
- 交付（零迁移、零 Go 代码）：`deploy/kubernetes/monitoring/prometheus-rules.yaml` 新增
  `synara-control-plane.fast-provision-slo` 规则组：
  - `SynaraWarmHitColdStartSLOBreach`：warm 命中的 dispatch→Provider-ready P95 > 2.5s 持续
    15 分钟即告警——交互式秒级 SLO 的第一道硬门禁；
  - `SynaraWarmPoolFallbackSurge`：30 天窗口内 warm 请求的 fallback 占比 > 20% 持续 30 分钟
    告警——暖池容量不足信号。
- 后续把该 SLO 写入 Stage 4 完成条件属主分支文档改动，合并时一并提。

### 增量 B：`guaranteed-warm`——min-idle 硬补齐

- Pool 配置新增 `minIdleUnits`（0 = 现状软语义，>0 = 硬保证）；Kubernetes Reconciler 每轮把
  ready-idle 缺口视为必须立即补齐的目标，补齐失败/持续缺口产生 bounded 告警指标
  `synara_worker_pool_warm_deficit{capacity_class}`。
- Placement 对 interactive 请求：warm miss 时不再静默冷启动，而是（可配置的）短暂等待补齐窗口
  （bounded，默认 0 保持兼容）。
- 交付：Pool schema/配置扩展 + Reconciler 补齐逻辑 + deficit 指标 + Prometheus 告警规则。

### 增量 C：Provider Host 预启动（warm worker 在 claim 前完成 Describe）

- 现状假设（待勘察确认）：agentd 在 claim 后才启动 Provider Host 进程并 Describe。
- 目标：warm-pool 模式的 agentd 在注册后、claim 前就启动 Host 进程到 `Describe` 完成并缓存
  capability 描述；claim 后仅注入 Grant 凭证（FD 3 路径不变）+ StartSession。
- 安全边界不变：凭证仍在 claim 后 resolve；预启动的 Host 无凭证、无 Workspace、无 Session 状态。
- 交付：agentd warm 模式改造 + Host 生命周期管理（claim 超时/release 轮换时回收预启动进程）。

### 增量 D：Workspace cache-first（Grant 放行 + freshness 标注）

- Claim 放行条件从"网络 fetch 成功"改为"Grant resolve 成功"；网络 fetch 转后台，Session Event
  携带 `workspaceFreshness`。按 proposal §3。

### 增量 E（后续/spike）：snapshot-restore 层

- microVM 选型 spike（Firecracker 自管 / Kata on K8s / agent-sandbox 复用）→ 独立提案后实施。
- agent-sandbox 的 SandboxWarmPool + Kata 路线是候选之一：如果采用，Synara 的 Kubernetes Target
  可以把 warm Pod 换成 Sandbox CR，复用其控制器做预热，Synara 保留 Claim/fencing/Grant 权威。

## 2. 代码锚点（勘察完成）

> 迁移链尾为 `000085_agent_executions_claim_indexes.sql`；新迁移从 **`000086`** 起。

- **Pool 模型**：`migrations/000058_worker_pools_placement.sql:6-12`（mode/capacity/desired_idle/
  max_active）；`internal/persistence/placement_models.go:19`；warm 容量权威表
  `migrations/000069_worker_pool_warm_capacity.sql`，其 `:49-98` scope 触发器**断言 authority 行与
  pool 行的 desired/max 一致**——新增列必须同步扩展。
- **Reconciler warm 路径**：计划函数 `kubernetes_warm_pool.go:316-353`（`desiredTotal =
  DesiredIdleUnits + claimed`，clamp Max）；创建循环 `kubernetes_reconciler.go:900-923` **位于冷
  执行 Pod 循环（`:826-899`）之后**且受 `MaxActivePods` 截断——软语义根因。warm 槽驱逐
  `:748-807`（`warm-pool-demand-fallback`）。授权发布仅在全 pass 成功后（`:308-328`）。
- **Placement**：`internal/placement/service.go:445-520` `selectExecutionFromPolicy`；fallback 序
  `:461-478`；warm 偏好测试 `:499-514`；**静默冷回退 `:516-518`（无信号）**。注意该函数在
  launch 事务内持锁——不能睡眠等补齐。
- **Claim**：warm 匹配 `internal/executions/lifecycle.go:207-224`；fairqueue warm tiebreak
  `claim_fair_queue.go:23-26`；release 过滤 `workerreleases/scheduling.go:141-151`。
- **agentd**：Host 严格在 claim 后启动（`daemon.go:160` Claim → `:238` runExecution →
  `provider_host_v2.go:236` start）。凭证经 FD-3 **惰性写入**（`:982-989`）——预启动持 FD 不写
  即可行；无凭证 Describe 路径已存在（`:193-226`）。预 claim Host 的 cgroup 命名需占位方案
  （execution ID 未知）。
- **Generation facts**：`deriveWarmPoolResult` `generation_facts.go:80-88`；claim 时写结果 `:209`；
  写一次触发器 `migrations/000059:188-191`；rollup 复合主键含双 warm 标签
  （`metric_rollup_models.go:43-52`、`000081:16,25-52`）。
- **Config**：pool 尺寸仅 API（`placement_api.go:35,61`）；`maxActivePods` 来自 Target 加密配置
  （默认 50，`kubernetes_reconciler.go:1634`）；`SYNARA_AGENTD_WORKER_MODE`（agentd
  `config.go:264-283`）。

### 增量 B 设计决策（已定）

- **D1**：`worker_pools.min_idle_units INTEGER NOT NULL DEFAULT 0`，
  `CHECK (0 <= min_idle_units AND min_idle_units <= desired_idle_units)`——min-idle 是 desired 内的
  "保证下限"，不是独立目标；plans 算术不变，只改优先级/豁免/亏空核算。镜像进 warm 容量权威行并
  扩展 `000069` scope 触发器与 SQLite 对应约束。
- **D2**：Reconciler 重排——每个 warm pool 的**前 `min_idle_units` 个槽（slot 0..min-1）在冷执行
  Pod 循环之前创建**（保证槽），其余 warm 槽保持现状（冷循环后、best-effort）。槽名稳定，保证
  身份稳定。
- **D3**：`warm-pool-demand-fallback` 驱逐只允许命中 slot index ≥ min_idle_units 的槽。
- **D4**：保证需求超出 `MaxActivePods` 时不 fail closed：按预算截断并计入亏空（fail-visible）；
  API 层只保留 D1 的列级 CHECK（maxActivePods 在加密 Target 配置内，API 不解密）。
- **D5**：新指标 `synara_worker_pool_warm_deficit{capacity_class}` =
  Σ max(0, min_idle − ready_idle)，从权威行投影（`desired_idle_units` 已在权威行但未导出，一并
  导出 min）；Prometheus 告警：deficit > 0 持续 10 分钟 warning。
- **D6**：placement 不在事务内等待（勘察确认持锁）；interactive 等待补齐留给未来在事务外的
  重试形态，本增量不做。

## 3. 验证策略

- 每增量：Go focused test + 现有 PostgreSQL/SQLite 双栈测试模式；增量 B/C 需要 OrbStack 真实
  Kubernetes 验收 lane（复用 stage-4 已有 runner 模式）。
- SLO 证明：增量 A 的 tier 分位数在增量 B/C 落地前后对比，目标 warm 命中 P95 ≤ 2.5s。
- 遵守 TODO Roadmap-wide rules：迁移号跨分支唯一（新迁移从当前链尾 `000084` 起，合并前复核）；
  docs/reports 证据不可变；sweep 唯一权威。

## 4. 进度

- [x] worktree 创建（`fast-cloud-agents` @ `4e30436c`）
- [x] agent-sandbox 概念对齐
- [ ] 代码勘察（进行中；A 所需锚点已自查完成）
- [x] 增量 A（SLO 告警门禁，`prometheus-rules.yaml`）
- [ ] 增量 B（**实施中**：codex gpt-5.6-sol 后台执行，brief 见 `.codex-brief-increment-b.md`，
      报告将写入 `.codex-report-increment-b.md`；完成后由本会话验收 + 复核）
- [ ] 增量 C
- [ ] 增量 D
- [ ] 增量 E spike

exec
/bin/zsh -lc "rg -n -C 3 'Stage 4 scheduling|warm.capacity|000069|worker_pool_warm_capacity|min_idle' /Users/huang/.codex/memories/MEMORY.md" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
1253-
1254-- Stage 3, automatic Worker Release rollback, migration 000042, auto_rollback.go, policy-version-scoped observation window, Audit, Outbox, e944a2e7, 6f808f35, 9aefbfc8, synara-stage3-worm-data, all-dot-synara-hyphen-entries-absent
1255-
1256:## Task 15: Continue Stage 4 scheduling-decision, claim-release, fair-share, and warm-capacity work in OrbStack, outcome observed in progress
1257-
1258-### rollout_summary_files
1259-
1260-- extensions/chronicle/resources/2026-07-25T15-15-00-DdAd-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T15-15-00-DdAd-10min-memory-summary.md, updated_at=2026-07-25T15:15:00+00:00, thread_id=None, Kubernetes/ACK product and cost-research context [chronicle memory])
1261:- extensions/chronicle/resources/2026-07-25T14-55-00-YhbE-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T14-55-00-YhbE-10min-memory-summary.md, updated_at=2026-07-25T14:55:00+00:00, thread_id=None, Phase-Three resource optimization, warm-capacity and merge context [chronicle memory])
1262-- extensions/chronicle/resources/2026-07-25T09-26-00-Fkxv-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T09-26-00-Fkxv-10min-memory-summary.md, updated_at=2026-07-25T09:26:00+00:00, thread_id=None, merge/TODO/resource-optimization observations [chronicle memory])
1263-- extensions/chronicle/resources/2026-07-25T08-54-00-tquZ-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T08-54-00-tquZ-10min-memory-summary.md, updated_at=2026-07-25T08:54:00+00:00, thread_id=None, Kubernetes-resilience review context [chronicle memory])
1264-- extensions/chronicle/resources/2026-07-25T08-53-00-mQpu-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T08-53-00-mQpu-10min-memory-summary.md, updated_at=2026-07-25T08:53:00+00:00, thread_id=None, JetBrains diff and Codex checklist context [chronicle memory])
--
1271-- extensions/chronicle/resources/2026-07-26T03-08-00-RMyP-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-26T03-08-00-RMyP-10min-memory-summary.md, updated_at=2026-07-26T03:08:00+00:00, thread_id=None, immutable scheduling-decision and claim-release observations [chronicle memory])
1272-- extensions/chronicle/resources/2026-07-26T02-58-00-lYTB-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-26T02-58-00-lYTB-10min-memory-summary.md, updated_at=2026-07-26T02:58:00+00:00, thread_id=None, claim/fair-share and temporary PostgreSQL namespace context [chronicle memory])
1273-- extensions/chronicle/resources/2026-07-25T15-25-00-NMDN-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T15-25-00-NMDN-10min-memory-summary.md, updated_at=2026-07-25T15:25:00+00:00, thread_id=None, Stage 4 task/evidence-gap context [chronicle memory])
1274:- extensions/chronicle/resources/2026-07-25T15-05-00-xIiC-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T15-05-00-xIiC-10min-memory-summary.md, updated_at=2026-07-25T15:05:00+00:00, thread_id=None, warm-capacity work and parallel workbench context [chronicle memory])
1275-
1276-### keywords
1277-
1278:- 评估第三阶段资源优化, 000076_execution_scheduling_decisions.sql, ReleaseFact, worker_claim_facts, claim fair-share, quota fairness, warm capacity, execution_launch_target_test.go, recovery_bundle_test.go, OrbStack PostgreSQL, OrbStack Kubernetes, immutable decision record, shared allocation
1279-
1280-## User preferences
1281-

exec
/bin/zsh -lc 'git status --short --branch' in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 291ms:
## fast-cloud-agents
 M deploy/kubernetes/monitoring/prometheus-rules.yaml
?? .codex-brief-increment-b.md
?? .codex-log-increment-b.txt
?? docs/plans/fast-cloud-agents-implementation.md

codex
The worktree is already dirty from Increment A and an existing Increment B brief/log. I’m checking whether any background implementation is still writing here before editing, so I don’t collide with or overwrite concurrent work.
exec
/bin/zsh -lc "sed -n '1,260p' .codex-brief-increment-b.md" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
# Task: Implement guaranteed warm-pool min-idle (Increment B) in the Synara Go control plane

Work in THIS directory (a git worktree on branch `fast-cloud-agents`). Do not touch other checkouts.
Read `docs/plans/fast-cloud-agents-implementation.md` first — §2 has exact file:line anchors and the
frozen design decisions D1–D6. Implement Increment B exactly per those decisions.

## Goal

Upgrade Kubernetes warm pools from best-effort to a hard minimum-idle guarantee so interactive agents
start in seconds: a new `min_idle_units` floor per worker pool, replenished with priority BEFORE cold
execution Pods, exempt from demand eviction, with a deficit metric and alert.

## Scope (all in services/control-plane unless noted)

1. Migration `000086_worker_pool_min_idle.sql` (PostgreSQL, forward-only, follow the style of
   `migrations/000058_worker_pools_placement.sql` and `000069_worker_pool_warm_capacity.sql`):
   - `ALTER TABLE worker_pools ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0`;
   - `CHECK (min_idle_units >= 0 AND min_idle_units <= desired_idle_units)` (named constraint);
   - `ALTER TABLE worker_pool_warm_capacity ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0`;
   - extend `assert_worker_pool_warm_capacity_scope()` (defined in `000069:49-98`) via CREATE OR
     REPLACE so the authority row's `min_idle_units` must equal the pool's, same as desired/max.
2. SQLite parity: the Personal profile uses GORM AutoMigrate + trigger/safety mirrors under
   `internal/database` (see existing warm-capacity sqlite tests for the pattern). Mirror the same
   scope assertion the way `desired_idle_units` is mirrored today.
3. Models: `internal/persistence/placement_models.go` (pool struct) and
   `internal/persistence/warm_capacity_models.go` add `MinIdleUnits int` with matching gorm column.
4. Placement service API: `internal/placement/service.go` — add `MinIdleUnits *int` to
   `CreatePoolInput`/update input (`:81-92`, `:236`, `:292`), normalize + validate
   (0 <= min <= desiredIdleUnits) in `normalizePoolInput` (`:803`); surface the field in the HTTP
   handlers `internal/httpapi/placement_api.go:35,61` responses/requests (follow existing field
   plumbing style exactly).
5. Reconciler (the core change), `internal/executiontargets/kubernetes_reconciler.go` and
   `kubernetes_warm_pool.go`:
   - split `kubernetesWarmPodPlans` (`kubernetes_warm_pool.go:316-353`) output into guaranteed
     plans (slot index < pool.MinIdleUnits) and best-effort plans (the rest); keep the existing
     `desiredTotal = DesiredIdleUnits + claimed` clamp arithmetic unchanged;
   - create guaranteed warm plans BEFORE the cold execution Pod loop (`kubernetes_reconciler.go:826-899`),
     still respecting the target-wide `MaxActivePods` budget; best-effort plans stay where the current
     warm loop is (`:900-923`). When the budget truncates guaranteed plans, do not fail the pass —
     count the shortfall into the deficit (design D4, fail-visible);
   - exempt guaranteed slots (index < MinIdleUnits) from `warm-pool-demand-fallback` eviction
     (`:748-807`, design D3);
   - both plan computations (`:619-621` validation pass and `:808-810` post-eviction) must use the
     same split so hashes/pod names stay stable.
6. Warm capacity authority + deficit metric:
   - carry `MinIdleUnits` through `ManagedKubernetesWarmCapacityObservation`
     (`internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go:15-28`), the
     observation builder (`kubernetes_warm_pool.go:86-136`), and the writer
     `internal/warmcapacity/service.go` `Observe` (CAS update set);
   - project `synara_worker_pool_warm_deficit{capacity_class}` in
     `internal/observability/distributed_routing_billing_metrics.go` next to the existing
     warm-capacity gauges (`:507-625`): per fresh authority row deficit =
     max(0, min_idle_units - ready_idle_units), summed by bounded capacity class (reuse the existing
     class bounding/`other` collapse). Only fresh (unexpired) observations contribute, matching the
     existing `units` gauge freshness rule. Also export the already-stored `desired_idle_units` and
     new `min_idle_units` as `kind="desired_idle"` / `kind="min_idle"` series on the existing
     `synara_worker_pool_warm_capacity_units` gauge (bounded kinds only).
7. Alert rule: append to `deploy/kubernetes/monitoring/prometheus-rules.yaml` in the existing
   `synara-control-plane.fast-provision-slo` group:
   `SynaraWarmPoolMinIdleDeficit`: `sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0`
   for 10m, severity warning, description mentioning guaranteed warm floor unmet.
8. Contract doc: update `docs/contracts/worker-pool-placement-v1.md` — in the Worker Pools and
   Live warm-capacity authority sections, document `minIdleUnits` (guaranteed floor within
   desired_idle_units, priority replenish before cold Pods, eviction exemption for guaranteed slots,
   budget-truncation counts as visible deficit, deficit metric name). Keep the existing tone:
   precise, states what is and is not guaranteed.

## Constraints

- Migration numbering: `000086` exactly; never edit existing migrations except the explicit
  CREATE OR REPLACE of the scope-assertion function inside the NEW migration file.
- PostgreSQL + SQLite behavioral parity, following how desired/max are asserted today.
- Do not touch anything under `docs/reports/` (immutable evidence).
- Follow surrounding code style; no new dependencies; keep bounded-label discipline (no pool/target
  IDs in metric labels).
- Do not run `bun fmt`/`bun lint`/`bun typecheck` or any bun command; Go verification only.

## Verification (run all; fix failures)

```
cd services/control-plane
go build ./...
go test ./internal/placement/... ./internal/executiontargets/... ./internal/warmcapacity/... \
  ./internal/observability/... ./internal/persistence/... ./internal/database/... ./internal/httpapi/...
```

Add focused tests: pool input validation (min>desired rejected), plan split (guaranteed vs
best-effort slots, budget truncation), eviction exemption, deficit metric projection (fresh vs
expired), and the sqlite scope-assertion mirror. Follow the existing table-driven test patterns in
each package.

## Deliverable

When done, write a report to `.codex-report-increment-b.md` in the worktree root: what changed
(file list), design decisions applied, test results (exact go test output tail), and any remaining
risks or intentionally-unimplemented edges. Leave the worktree with all changes uncommitted.

exec
/bin/zsh -lc 'tail -n 80 .codex-log-increment-b.txt' in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
exec
/bin/zsh -lc 'git diff -- deploy/kubernetes/monitoring/prometheus-rules.yaml' in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 036ae9bc..28897ab5 100644
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -141,3 +141,28 @@ spec:
           annotations:
             summary: Synara has expired SSE connection leases
             description: Expired SSE leases remain and should be removed by reconnect or retention cleanup.
+    - name: synara-control-plane.fast-provision-slo
+      rules:
+        - alert: SynaraWarmHitColdStartSLOBreach
+          expr: |
+            max by (target_kind, warm_pool_mode) (
+              synara_execution_cold_start_duration_seconds_30d{warm_pool_result="hit", quantile="p95"}
+            ) > 2.5
+          for: 15m
+          labels:
+            severity: warning
+          annotations:
+            summary: Warm-hit agent start P95 exceeds the interactive SLO
+            description: Trailing-30-day dispatch-to-Provider-ready P95 for warm-pool hits has exceeded 2.5 seconds for 15 minutes; the fast-provision interactive SLO is breached even when warm capacity is available.
+        - alert: SynaraWarmPoolFallbackSurge
+          expr: |
+            sum(synara_warm_pool_acquisitions_30d{result="fallback"})
+              /
+            clamp_min(sum(synara_warm_pool_acquisitions_30d{result=~"hit|fallback"}), 1)
+            > 0.2
+          for: 30m
+          labels:
+            severity: warning
+          annotations:
+            summary: Warm Pool requests are frequently falling back to cold starts
+            description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.

 succeeded in 0ms:
  （默认 50，`kubernetes_reconciler.go:1634`）；`SYNARA_AGENTD_WORKER_MODE`（agentd
  `config.go:264-283`）。

### 增量 B 设计决策（已定）

- **D1**：`worker_pools.min_idle_units INTEGER NOT NULL DEFAULT 0`，
  `CHECK (0 <= min_idle_units AND min_idle_units <= desired_idle_units)`——min-idle 是 desired 内的
  "保证下限"，不是独立目标；plans 算术不变，只改优先级/豁免/亏空核算。镜像进 warm 容量权威行并
  扩展 `000069` scope 触发器与 SQLite 对应约束。
- **D2**：Reconciler 重排——每个 warm pool 的**前 `min_idle_units` 个槽（slot 0..min-1）在冷执行
  Pod 循环之前创建**（保证槽），其余 warm 槽保持现状（冷循环后、best-effort）。槽名稳定，保证
  身份稳定。
- **D3**：`warm-pool-demand-fallback` 驱逐只允许命中 slot index ≥ min_idle_units 的槽。
- **D4**：保证需求超出 `MaxActivePods` 时不 fail closed：按预算截断并计入亏空（fail-visible）；
  API 层只保留 D1 的列级 CHECK（maxActivePods 在加密 Target 配置内，API 不解密）。
- **D5**：新指标 `synara_worker_pool_warm_deficit{capacity_class}` =
  Σ max(0, min_idle − ready_idle)，从权威行投影（`desired_idle_units` 已在权威行但未导出，一并
  导出 min）；Prometheus 告警：deficit > 0 持续 10 分钟 warning。
- **D6**：placement 不在事务内等待（勘察确认持锁）；interactive 等待补齐留给未来在事务外的
  重试形态，本增量不做。

## 3. 验证策略

- 每增量：Go focused test + 现有 PostgreSQL/SQLite 双栈测试模式；增量 B/C 需要 OrbStack 真实
  Kubernetes 验收 lane（复用 stage-4 已有 runner 模式）。
- SLO 证明：增量 A 的 tier 分位数在增量 B/C 落地前后对比，目标 warm 命中 P95 ≤ 2.5s。
- 遵守 TODO Roadmap-wide rules：迁移号跨分支唯一（新迁移从当前链尾 `000084` 起，合并前复核）；
  docs/reports 证据不可变；sweep 唯一权威。

## 4. 进度

- [x] worktree 创建（`fast-cloud-agents` @ `4e30436c`）
- [x] agent-sandbox 概念对齐
- [ ] 代码勘察（进行中；A 所需锚点已自查完成）
- [x] 增量 A（SLO 告警门禁，`prometheus-rules.yaml`）
- [ ] 增量 B（**实施中**：codex gpt-5.6-sol 后台执行，brief 见 `.codex-brief-increment-b.md`，
      报告将写入 `.codex-report-increment-b.md`；完成后由本会话验收 + 复核）
- [ ] 增量 C
- [ ] 增量 D
- [ ] 增量 E spike

exec
/bin/zsh -lc "rg -n -C 3 'Stage 4 scheduling|warm.capacity|000069|worker_pool_warm_capacity|min_idle' /Users/huang/.codex/memories/MEMORY.md" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
1253-
1254-- Stage 3, automatic Worker Release rollback, migration 000042, auto_rollback.go, policy-version-scoped observation window, Audit, Outbox, e944a2e7, 6f808f35, 9aefbfc8, synara-stage3-worm-data, all-dot-synara-hyphen-entries-absent
1255-
1256:## Task 15: Continue Stage 4 scheduling-decision, claim-release, fair-share, and warm-capacity work in OrbStack, outcome observed in progress
1257-
1258-### rollout_summary_files
1259-
1260-- extensions/chronicle/resources/2026-07-25T15-15-00-DdAd-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T15-15-00-DdAd-10min-memory-summary.md, updated_at=2026-07-25T15:15:00+00:00, thread_id=None, Kubernetes/ACK product and cost-research context [chronicle memory])
1261:- extensions/chronicle/resources/2026-07-25T14-55-00-YhbE-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T14-55-00-YhbE-10min-memory-summary.md, updated_at=2026-07-25T14:55:00+00:00, thread_id=None, Phase-Three resource optimization, warm-capacity and merge context [chronicle memory])
1262-- extensions/chronicle/resources/2026-07-25T09-26-00-Fkxv-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T09-26-00-Fkxv-10min-memory-summary.md, updated_at=2026-07-25T09:26:00+00:00, thread_id=None, merge/TODO/resource-optimization observations [chronicle memory])
1263-- extensions/chronicle/resources/2026-07-25T08-54-00-tquZ-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T08-54-00-tquZ-10min-memory-summary.md, updated_at=2026-07-25T08:54:00+00:00, thread_id=None, Kubernetes-resilience review context [chronicle memory])
1264-- extensions/chronicle/resources/2026-07-25T08-53-00-mQpu-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T08-53-00-mQpu-10min-memory-summary.md, updated_at=2026-07-25T08:53:00+00:00, thread_id=None, JetBrains diff and Codex checklist context [chronicle memory])
--
1271-- extensions/chronicle/resources/2026-07-26T03-08-00-RMyP-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-26T03-08-00-RMyP-10min-memory-summary.md, updated_at=2026-07-26T03:08:00+00:00, thread_id=None, immutable scheduling-decision and claim-release observations [chronicle memory])
1272-- extensions/chronicle/resources/2026-07-26T02-58-00-lYTB-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-26T02-58-00-lYTB-10min-memory-summary.md, updated_at=2026-07-26T02:58:00+00:00, thread_id=None, claim/fair-share and temporary PostgreSQL namespace context [chronicle memory])
1273-- extensions/chronicle/resources/2026-07-25T15-25-00-NMDN-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T15-25-00-NMDN-10min-memory-summary.md, updated_at=2026-07-25T15:25:00+00:00, thread_id=None, Stage 4 task/evidence-gap context [chronicle memory])
1274:- extensions/chronicle/resources/2026-07-25T15-05-00-xIiC-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T15-05-00-xIiC-10min-memory-summary.md, updated_at=2026-07-25T15:05:00+00:00, thread_id=None, warm-capacity work and parallel workbench context [chronicle memory])
1275-
1276-### keywords
1277-
1278:- 评估第三阶段资源优化, 000076_execution_scheduling_decisions.sql, ReleaseFact, worker_claim_facts, claim fair-share, quota fairness, warm capacity, execution_launch_target_test.go, recovery_bundle_test.go, OrbStack PostgreSQL, OrbStack Kubernetes, immutable decision record, shared allocation
1279-
1280-## User preferences
1281-

exec
/bin/zsh -lc 'git status --short --branch' in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 291ms:
## fast-cloud-agents
 M deploy/kubernetes/monitoring/prometheus-rules.yaml
?? .codex-brief-increment-b.md
?? .codex-log-increment-b.txt
?? docs/plans/fast-cloud-agents-implementation.md

codex
The worktree is already dirty from Increment A and an existing Increment B brief/log. I’m checking whether any background implementation is still writing here before editing, so I don’t collide with or overwrite concurrent work.

2026-07-26T14:11:39.274397Z ERROR codex_core::tools::router: error=Full-history forked agents inherit the parent agent type; omit agent_type, or spawn without a full-history fork.
2026-07-26T14:11:48.746370Z ERROR codex_core::tools::router: error=collab spawn failed: no thread with id: 019f9ec4-2351-7c30-abff-591b20053ca4
codex
The log is the transcript of this active Codex run, not evidence of a second writer. The only pre-existing code change is Increment A’s alert group, which I’ll preserve and extend narrowly. I’m now mapping the migration/SQLite/model paths and the reconciler’s two plan passes before patching.
exec
/bin/zsh -lc "sed -n '1,180p' services/control-plane/migrations/000058_worker_pools_placement.sql
sed -n '1,190p' services/control-plane/migrations/000069_worker_pool_warm_capacity.sql
sed -n '1,180p' services/control-plane/internal/persistence/placement_models.go
sed -n '1,180p' services/control-plane/internal/persistence/warm_capacity_models.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
exec
/bin/zsh -lc "sed -n '1,430p' services/control-plane/internal/executiontargets/kubernetes_warm_pool.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
exec
/bin/zsh -lc "sed -n '1,260p' services/control-plane/internal/httpapi/placement_api.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
package executiontargets

import (
	"crypto/sha256"
	"encoding/hex"
	"encoding/json"
	"errors"
	"fmt"
	"strconv"
	"strings"
	"time"

	"github.com/google/uuid"

	"github.com/synara-ai/synara/services/control-plane/internal/placement"
	"github.com/synara-ai/synara/services/control-plane/internal/problem"
)

type kubernetesWarmPool struct {
	ID                 uuid.UUID      `gorm:"column:id"`
	Version            int64          `gorm:"column:version"`
	CapacityClass      string         `gorm:"column:capacity_class"`
	ClusterID          string         `gorm:"column:cluster_id"`
	Namespace          string         `gorm:"column:namespace"`
	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
	MaxActiveUnits     int            `gorm:"column:max_active_units"`
	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
	Status             string         `gorm:"column:status"`
}

type kubernetesWarmWorkerState struct {
	WorkerID                uuid.UUID  `gorm:"column:id"`
	WorkerIncarnation       int64      `gorm:"column:incarnation"`
	PodName                 string     `gorm:"column:pod_name"`
	InstanceUID             string     `gorm:"column:instance_uid"`
	WorkerPoolID            *uuid.UUID `gorm:"column:worker_pool_id"`
	WorkerPoolVersion       *int64     `gorm:"column:worker_pool_version"`
	CapacityClass           *string    `gorm:"column:capacity_class"`
	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id"`
	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
	WorkerReleaseStatus     string     `gorm:"column:worker_release_status"`
	RegistrationTrustMode   string     `gorm:"column:registration_trust_mode"`
	ProtocolVersion         int        `gorm:"column:protocol_version"`
	CurrentManifestID       *uuid.UUID `gorm:"column:current_manifest_id"`
	CompatibilityStatus     string     `gorm:"column:compatibility_status"`
	LeaseSupported          bool       `gorm:"column:lease_supported"`
	FencingSupported        bool       `gorm:"column:fencing_supported"`
	Status                  string     `gorm:"column:status"`
	AdministrativeStatus    string     `gorm:"column:administrative_status"`
	LastHeartbeatAt         time.Time  `gorm:"column:last_heartbeat_at"`
	HasLease                bool       `gorm:"column:has_lease"`
}

type kubernetesWarmWorkerIdentity struct {
	PodName     string
	InstanceUID string
}

type kubernetesWarmReleaseSelection struct {
	RevisionID  *uuid.UUID
	Channel     *string
	ImageDigest *string
}

type kubernetesWarmPodPlan struct {
	Pool       kubernetesWarmPool
	Slot       int
	Release    kubernetesWarmReleaseSelection
	ConfigHash string
}

type kubernetesObservedWarmPod struct {
	Pod           kubernetesPod
	PoolID        uuid.UUID
	PoolVersion   int64
	CapacityClass string
	Slot          int
	State         *kubernetesWarmWorkerState
}

type kubernetesWarmDemandEvictionCandidate struct {
	Observed kubernetesObservedWarmPod
	Key      kubernetesWarmCapacityKey
}

func managedKubernetesWarmCapacityObservations(
	tenantID uuid.UUID,
	executionTargetID uuid.UUID,
	warmPools []kubernetesWarmPool,
	warmPoolsSupported bool,
	warmClaimedCounts map[uuid.UUID]int,
	readyWarmCapacity map[kubernetesWarmCapacityKey]int,
	warmRelease kubernetesWarmReleaseSelection,
) []ManagedKubernetesWarmCapacityObservation {
	observations := make([]ManagedKubernetesWarmCapacityObservation, 0, len(warmPools))
	for _, pool := range warmPools {
		if pool.Status != placement.PoolStatusActive {
			continue
		}
		claimed := warmClaimedCounts[pool.ID]
		if claimed < 0 {
			claimed = 0
		}
		observation := ManagedKubernetesWarmCapacityObservation{
			TenantID:          tenantID,
			ExecutionTargetID: executionTargetID,
			WorkerPoolID:      pool.ID,
			WorkerPoolVersion: pool.Version,
			WarmSupported:     warmPoolsSupported,
			ClaimedUnits:      claimed,
		}
		if !warmPoolsSupported {
			observation.Reason = managedKubernetesRoutingReasonPointer(
				"Managed Kubernetes warm capacity is unavailable while a canary Worker release is active.",
			)
			observations = append(observations, observation)
			continue
		}
		observation.WorkerReleaseRevisionID = warmRelease.RevisionID
		observation.WorkerReleaseChannel = warmRelease.Channel
		observation.DesiredTotalUnits = pool.DesiredIdleUnits + claimed
		if observation.DesiredTotalUnits > pool.MaxActiveUnits {
			observation.DesiredTotalUnits = pool.MaxActiveUnits
		}
		key := kubernetesWarmCapacityKey{
			PoolID:          pool.ID,
			PoolVersion:     pool.Version,
			CapacityClass:   pool.CapacityClass,
			ReleaseRevision: optionalUUIDString(warmRelease.RevisionID),
			ReleaseChannel:  stringValue(warmRelease.Channel),
		}
		observation.ReadyIdleUnits = readyWarmCapacity[key]
		observations = append(observations, observation)
	}
	return observations
}

func kubernetesWarmWorkerStateForPod(
	states map[kubernetesWarmWorkerIdentity]kubernetesWarmWorkerState,
	pod kubernetesPod,
) (kubernetesWarmWorkerState, bool) {
	state, found := states[kubernetesWarmWorkerIdentityKey(pod.Name, pod.UID)]
	return state, found
}

func kubernetesWarmWorkerIdentityKey(podName, instanceUID string) kubernetesWarmWorkerIdentity {
	return kubernetesWarmWorkerIdentity{
		PodName:     strings.TrimSpace(podName),
		InstanceUID: strings.TrimSpace(instanceUID),
	}
}

func kubernetesWarmPodIdentity(pod kubernetesPod) (uuid.UUID, int64, string, int, error) {
	poolID, err := uuid.Parse(strings.TrimSpace(pod.Labels[kubernetesWorkerPoolIDLabel]))
	if err != nil {
		return uuid.UUID{}, 0, "", 0, err
	}
	poolVersion, err := strconv.ParseInt(strings.TrimSpace(pod.Labels[kubernetesWorkerPoolVersionLabel]), 10, 64)
	if err != nil || poolVersion <= 0 {
		return uuid.UUID{}, 0, "", 0, errors.New("invalid worker pool version")
	}
	capacityClass := strings.TrimSpace(pod.Labels[kubernetesCapacityClassLabel])
	if capacityClass != placement.CapacityClassStandard && capacityClass != placement.CapacityClassInteractive {
		return uuid.UUID{}, 0, "", 0, errors.New("invalid capacity class")
	}
	slot, err := strconv.Atoi(strings.TrimSpace(pod.Labels[kubernetesWarmSlotLabel]))
	if err != nil || slot < 0 {
		return uuid.UUID{}, 0, "", 0, errors.New("invalid warm slot")
	}
	return poolID, poolVersion, capacityClass, slot, nil
}

func kubernetesWarmWorkerStateMatchesPlan(state kubernetesWarmWorkerState, plan kubernetesWarmPodPlan) bool {
	return state.WorkerPoolID != nil &&
		state.WorkerPoolVersion != nil &&
		state.CapacityClass != nil &&
		*state.WorkerPoolID == plan.Pool.ID &&
		*state.WorkerPoolVersion == plan.Pool.Version &&
		*state.CapacityClass == plan.Pool.CapacityClass &&
		sameOptionalUUID(state.WorkerReleaseRevisionID, plan.Release.RevisionID) &&
		sameOptionalString(state.WorkerReleaseChannel, plan.Release.Channel)
}

type kubernetesWarmCapacityKey struct {
	PoolID          uuid.UUID
	PoolVersion     int64
	CapacityClass   string
	ReleaseRevision string
	ReleaseChannel  string
}

func kubernetesWarmWorkerStateReadyIdle(
	state kubernetesWarmWorkerState,
	pod kubernetesPod,
	plan kubernetesWarmPodPlan,
	now time.Time,
	heartbeatTimeout time.Duration,
) bool {
	return kubernetesWarmWorkerIdentityKey(state.PodName, state.InstanceUID) ==
		kubernetesWarmWorkerIdentityKey(pod.Name, pod.UID) &&
		pod.Phase == "Running" &&
		!state.HasLease &&
		strings.TrimSpace(state.Status) == "online" &&
		strings.TrimSpace(state.AdministrativeStatus) == "active" &&
		state.ProtocolVersion == kubernetesWorkerProtocolVersion &&
		state.CurrentManifestID != nil &&
		strings.TrimSpace(state.CompatibilityStatus) == "compatible" &&
		state.LeaseSupported &&
		state.FencingSupported &&
		strings.TrimSpace(state.RegistrationTrustMode) == kubernetesPodBoundRegistrationTrust &&
		kubernetesWarmWorkerHeartbeatFresh(state.LastHeartbeatAt, now, heartbeatTimeout) &&
		kubernetesWarmWorkerStateMatchesPlan(state, plan) &&
		kubernetesWarmWorkerReleaseReady(state, plan)
}

func kubernetesWarmWorkerStateShouldRecycle(
	state kubernetesWarmWorkerState,
	pod kubernetesPod,
	now time.Time,
	heartbeatTimeout time.Duration,
) bool {
	if pod.Phase == "Succeeded" || pod.Phase == "Failed" {
		return true
	}
	status := strings.TrimSpace(state.Status)
	administrativeStatus := strings.TrimSpace(state.AdministrativeStatus)
	return status == "offline" ||
		status == "draining" ||
		status == "terminated" ||
		administrativeStatus == "draining" ||
		!kubernetesWarmWorkerHeartbeatFresh(state.LastHeartbeatAt, now, heartbeatTimeout)
}

func kubernetesWarmWorkerHeartbeatFresh(lastHeartbeatAt, now time.Time, heartbeatTimeout time.Duration) bool {
	if heartbeatTimeout <= 0 || lastHeartbeatAt.IsZero() {
		return false
	}
	return !lastHeartbeatAt.Before(now.Add(-heartbeatTimeout))
}

func kubernetesWarmWorkerReleaseReady(state kubernetesWarmWorkerState, plan kubernetesWarmPodPlan) bool {
	planHasRevision := plan.Release.RevisionID != nil
	planHasChannel := plan.Release.Channel != nil
	if planHasRevision != planHasChannel ||
		!sameOptionalUUID(state.WorkerReleaseRevisionID, plan.Release.RevisionID) ||
		!sameOptionalString(state.WorkerReleaseChannel, plan.Release.Channel) {
		return false
	}
	status := strings.TrimSpace(state.WorkerReleaseStatus)
	if !planHasRevision {
		return status == "" || status == "unmanaged"
	}
	return status == "active"
}

func kubernetesWarmCapacityKeyForState(state kubernetesWarmWorkerState) (kubernetesWarmCapacityKey, bool) {
	if state.WorkerPoolID == nil || state.WorkerPoolVersion == nil || state.CapacityClass == nil {
		return kubernetesWarmCapacityKey{}, false
	}
	if (state.WorkerReleaseRevisionID == nil) != (state.WorkerReleaseChannel == nil) {
		return kubernetesWarmCapacityKey{}, false
	}
	return kubernetesWarmCapacityKey{
		PoolID: *state.WorkerPoolID, PoolVersion: *state.WorkerPoolVersion, CapacityClass: *state.CapacityClass,
		ReleaseRevision: optionalUUIDString(state.WorkerReleaseRevisionID),
		ReleaseChannel:  stringValue(state.WorkerReleaseChannel),
	}, true
}

func kubernetesWarmCapacityKeyForExecution(execution kubernetesExecution) (kubernetesWarmCapacityKey, bool) {
	if execution.WorkerPool == nil || execution.WorkerPool.Mode != placement.PoolModeWarm {
		return kubernetesWarmCapacityKey{}, false
	}
	if (execution.WorkerReleaseRevisionID == nil) != (execution.WorkerReleaseChannel == nil) {
		return kubernetesWarmCapacityKey{}, false
	}
	return kubernetesWarmCapacityKey{
		PoolID: execution.WorkerPool.ID, PoolVersion: execution.WorkerPool.Version, CapacityClass: execution.WorkerPool.CapacityClass,
		ReleaseRevision: optionalUUIDString(execution.WorkerReleaseRevisionID),
		ReleaseChannel:  stringValue(execution.WorkerReleaseChannel),
	}, true
}

func kubernetesWarmCapacityKeyForPlan(plan kubernetesWarmPodPlan) (kubernetesWarmCapacityKey, bool) {
	if (plan.Release.RevisionID == nil) != (plan.Release.Channel == nil) {
		return kubernetesWarmCapacityKey{}, false
	}
	return kubernetesWarmCapacityKey{
		PoolID: plan.Pool.ID, PoolVersion: plan.Pool.Version, CapacityClass: plan.Pool.CapacityClass,
		ReleaseRevision: optionalUUIDString(plan.Release.RevisionID),
		ReleaseChannel:  stringValue(plan.Release.Channel),
	}, true
}

func optionalUUIDString(value *uuid.UUID) string {
	if value == nil {
		return ""
	}
	return value.String()
}

func sameOptionalUUID(left, right *uuid.UUID) bool {
	if left == nil || right == nil {
		return left == nil && right == nil
	}
	return *left == *right
}

func sameOptionalString(left, right *string) bool {
	if left == nil || right == nil {
		return left == nil && right == nil
	}
	return *left == *right
}

func kubernetesWarmPodPlans(
	warmPools []kubernetesWarmPool,
	warmPoolsSupported bool,
	warmClaimedCounts map[uuid.UUID]int,
	warmRelease kubernetesWarmReleaseSelection,
	podBaseHash string,
	baseImage string,
) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
	plans := make([]kubernetesWarmPodPlan, 0)
	plansByName := make(map[string]kubernetesWarmPodPlan)
	if !warmPoolsSupported {
		return plans, plansByName, nil
	}
	for _, pool := range warmPools {
		if pool.Status != placement.PoolStatusActive {
			continue
		}
		claimed := warmClaimedCounts[pool.ID]
		if claimed < 0 {
			claimed = 0
		}
		desiredTotal := pool.DesiredIdleUnits + claimed
		if desiredTotal > pool.MaxActiveUnits {
			desiredTotal = pool.MaxActiveUnits
		}
		for slot := 0; slot < desiredTotal; slot++ {
			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
			if err != nil {
				return nil, nil, err
			}
			plan.ConfigHash = configHash
			plans = append(plans, plan)
			plansByName[kubernetesWarmPodName(plan)] = plan
		}
	}
	return plans, plansByName, nil
}

func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
	compactPoolID := strings.ReplaceAll(plan.Pool.ID.String(), "-", "")
	releaseKey := "unmanaged"
	if plan.Release.RevisionID != nil && plan.Release.Channel != nil {
		compactReleaseID := strings.ReplaceAll(plan.Release.RevisionID.String(), "-", "")
		channelKey := "p"
		if *plan.Release.Channel == "canary" {
			channelKey = "c"
		}
		releaseKey = channelKey + compactReleaseID[:8]
	}
	return fmt.Sprintf("synara-warm-%s-v%s-%s-s%d", compactPoolID[:10], strconv.FormatInt(plan.Pool.Version, 16), releaseKey, plan.Slot)
}

func kubernetesWarmPoolImage(baseImage string, release kubernetesWarmReleaseSelection) (string, error) {
	if release.RevisionID == nil && release.Channel == nil && release.ImageDigest == nil {
		return baseImage, nil
	}
	if release.RevisionID == nil || release.Channel == nil || release.ImageDigest == nil ||
		(*release.Channel != "promoted" && *release.Channel != "canary") {
		return "", problem.New(409, "worker_release_execution_invalid", "Warm Worker release selection is invalid.")
	}
	return pinImageReference(baseImage, *release.ImageDigest)
}

func kubernetesWarmPoolPodHash(baseHash, baseImage string, plan kubernetesWarmPodPlan) (string, error) {
	image, err := kubernetesWarmPoolImage(baseImage, plan.Release)
	if err != nil {
		return "", err
	}
	payload, err := json.Marshal(struct {
		BaseHash      string
		Image         string
		PoolID        uuid.UUID
		PoolVersion   int64
		CapacityClass string
		Slot          int
		RevisionID    *uuid.UUID
		Channel       *string
	}{baseHash, image, plan.Pool.ID, plan.Pool.Version, plan.Pool.CapacityClass, plan.Slot, plan.Release.RevisionID, plan.Release.Channel})
	if err != nil {
		return "", err
	}
	digest := sha256.Sum256(payload)
	return hex.EncodeToString(digest[:]), nil
}

 succeeded in 0ms:
CREATE TABLE worker_pools (
  id UUID PRIMARY KEY,
  tenant_id UUID REFERENCES tenants(id) ON DELETE CASCADE,
  execution_target_id UUID NOT NULL REFERENCES execution_targets(id) ON DELETE CASCADE,
  name TEXT NOT NULL,
  mode TEXT NOT NULL CHECK (mode IN ('resident', 'per-execution', 'warm')),
  capacity_class TEXT NOT NULL CHECK (capacity_class IN ('standard', 'interactive')),
  cluster_id TEXT NOT NULL DEFAULT '',
  region TEXT NOT NULL DEFAULT '',
  namespace TEXT NOT NULL DEFAULT '',
  desired_idle_units INTEGER NOT NULL DEFAULT 0 CHECK (desired_idle_units >= 0),
  max_active_units INTEGER NOT NULL DEFAULT 1 CHECK (max_active_units >= desired_idle_units),
  scheduling_template JSONB NOT NULL DEFAULT '{}'::jsonb,
  status TEXT NOT NULL DEFAULT 'active' CHECK (status IN ('active', 'draining', 'disabled')),
  version BIGINT NOT NULL DEFAULT 1 CHECK (version > 0),
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (execution_target_id, name),
  CHECK (length(btrim(name)) BETWEEN 1 AND 160),
  CHECK (jsonb_typeof(scheduling_template) = 'object')
);

CREATE INDEX idx_worker_pools_target_status
  ON worker_pools (execution_target_id, status, capacity_class, name, id);

CREATE TABLE execution_placement_policies (
  execution_target_id UUID PRIMARY KEY REFERENCES execution_targets(id) ON DELETE CASCADE,
  tenant_id UUID REFERENCES tenants(id) ON DELETE CASCADE,
  version BIGINT NOT NULL DEFAULT 1 CHECK (version > 0),
  default_pool_id UUID NOT NULL REFERENCES worker_pools(id) ON DELETE RESTRICT,
  balanced_pool_id UUID REFERENCES worker_pools(id) ON DELETE RESTRICT,
  low_latency_pool_id UUID REFERENCES worker_pools(id) ON DELETE RESTRICT,
  updated_by UUID REFERENCES users(id) ON DELETE RESTRICT,
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX idx_execution_placement_policies_tenant_target
  ON execution_placement_policies (tenant_id, execution_target_id);

ALTER TABLE agent_executions
  ADD COLUMN worker_pool_id UUID REFERENCES worker_pools(id) ON DELETE RESTRICT,
  ADD COLUMN worker_pool_version BIGINT,
  ADD COLUMN capacity_class TEXT,
  ADD COLUMN placement_policy_version BIGINT,
  ADD CONSTRAINT chk_agent_executions_placement_snapshot CHECK (
    (worker_pool_id IS NULL AND worker_pool_version IS NULL AND capacity_class IS NULL AND placement_policy_version IS NULL)
    OR
    (worker_pool_id IS NOT NULL
      AND worker_pool_version > 0
      AND capacity_class IN ('standard', 'interactive')
      AND placement_policy_version > 0)
  );

CREATE INDEX idx_agent_executions_placement_claim
  ON agent_executions (execution_target_id, worker_pool_id, status, queued_at, id)
  WHERE status IN ('queued', 'recovering');

CREATE OR REPLACE FUNCTION assert_worker_pool_target_scope()
RETURNS TRIGGER
LANGUAGE plpgsql
AS $$
DECLARE
  target execution_targets%ROWTYPE;
BEGIN
  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
  IF NOT FOUND THEN
    RAISE EXCEPTION 'worker pool target is missing' USING ERRCODE = '23514';
  END IF;
  IF target.tenant_id IS DISTINCT FROM NEW.tenant_id THEN
    RAISE EXCEPTION 'worker pool tenant does not match its execution target' USING ERRCODE = '23514';
  END IF;
  RETURN NEW;
END;
$$;

DROP TRIGGER IF EXISTS trg_worker_pools_target_scope ON worker_pools;
CREATE CONSTRAINT TRIGGER trg_worker_pools_target_scope
AFTER INSERT OR UPDATE OF tenant_id, execution_target_id ON worker_pools
DEFERRABLE INITIALLY DEFERRED
FOR EACH ROW EXECUTE FUNCTION assert_worker_pool_target_scope();

CREATE OR REPLACE FUNCTION assert_execution_placement_policy_scope()
RETURNS TRIGGER
LANGUAGE plpgsql
AS $$
DECLARE
  target execution_targets%ROWTYPE;
  default_pool worker_pools%ROWTYPE;
  optional_pool worker_pools%ROWTYPE;
BEGIN
  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
  IF NOT FOUND THEN
    RAISE EXCEPTION 'execution placement target is missing' USING ERRCODE = '23514';
  END IF;
  IF target.tenant_id IS DISTINCT FROM NEW.tenant_id THEN
    RAISE EXCEPTION 'execution placement tenant does not match its execution target' USING ERRCODE = '23514';
  END IF;

  SELECT * INTO default_pool FROM worker_pools WHERE id = NEW.default_pool_id;
  IF NOT FOUND
     OR default_pool.execution_target_id <> NEW.execution_target_id
     OR default_pool.tenant_id IS DISTINCT FROM NEW.tenant_id
     OR default_pool.status <> 'active' THEN
    RAISE EXCEPTION 'execution placement default pool is invalid' USING ERRCODE = '23514';
  END IF;

  IF NEW.balanced_pool_id IS NOT NULL THEN
    SELECT * INTO optional_pool FROM worker_pools WHERE id = NEW.balanced_pool_id;
    IF NOT FOUND
       OR optional_pool.execution_target_id <> NEW.execution_target_id
       OR optional_pool.tenant_id IS DISTINCT FROM NEW.tenant_id THEN
      RAISE EXCEPTION 'execution placement balanced pool is invalid' USING ERRCODE = '23514';
    END IF;
  END IF;

  IF NEW.low_latency_pool_id IS NOT NULL THEN
    SELECT * INTO optional_pool FROM worker_pools WHERE id = NEW.low_latency_pool_id;
    IF NOT FOUND
       OR optional_pool.execution_target_id <> NEW.execution_target_id
       OR optional_pool.tenant_id IS DISTINCT FROM NEW.tenant_id THEN
      RAISE EXCEPTION 'execution placement low-latency pool is invalid' USING ERRCODE = '23514';
    END IF;
  END IF;
  RETURN NEW;
END;
$$;

CREATE OR REPLACE FUNCTION assert_agent_execution_placement_scope()
RETURNS TRIGGER
LANGUAGE plpgsql
AS $$
DECLARE
  selected_pool worker_pools%ROWTYPE;
BEGIN
  IF NEW.worker_pool_id IS NULL THEN
    RETURN NEW;
  END IF;
  SELECT * INTO selected_pool FROM worker_pools WHERE id = NEW.worker_pool_id;
  IF NOT FOUND
     OR selected_pool.execution_target_id <> NEW.execution_target_id
     OR selected_pool.tenant_id IS DISTINCT FROM NEW.tenant_id
     OR selected_pool.version <> NEW.worker_pool_version
     OR selected_pool.capacity_class <> NEW.capacity_class THEN
    RAISE EXCEPTION 'execution placement snapshot does not match its worker pool'
      USING ERRCODE = '23514';
  END IF;
  RETURN NEW;
END;
$$;

DROP TRIGGER IF EXISTS trg_agent_executions_placement_scope ON agent_executions;
CREATE CONSTRAINT TRIGGER trg_agent_executions_placement_scope
AFTER INSERT OR UPDATE OF tenant_id, execution_target_id, worker_pool_id, capacity_class
ON agent_executions
DEFERRABLE INITIALLY DEFERRED
FOR EACH ROW EXECUTE FUNCTION assert_agent_execution_placement_scope();

CREATE OR REPLACE FUNCTION enforce_agent_execution_placement_immutable()
RETURNS TRIGGER
LANGUAGE plpgsql
AS $$
BEGIN
  IF OLD.worker_pool_id IS NOT NULL AND (
       NEW.worker_pool_id IS DISTINCT FROM OLD.worker_pool_id
       OR NEW.worker_pool_version IS DISTINCT FROM OLD.worker_pool_version
       OR NEW.capacity_class IS DISTINCT FROM OLD.capacity_class
       OR NEW.placement_policy_version IS DISTINCT FROM OLD.placement_policy_version
     ) THEN
    RAISE EXCEPTION 'execution placement snapshot is immutable'
      USING ERRCODE = '23514';
  END IF;
  RETURN NEW;
END;
$$;

DROP TRIGGER IF EXISTS trg_agent_executions_placement_immutable ON agent_executions;
CREATE TRIGGER trg_agent_executions_placement_immutable
BEFORE UPDATE OF worker_pool_id, worker_pool_version, capacity_class, placement_policy_version ON agent_executions
FOR EACH ROW EXECUTE FUNCTION enforce_agent_execution_placement_immutable();

CREATE TABLE worker_pool_warm_capacity (
  worker_pool_id UUID NOT NULL REFERENCES worker_pools(id) ON DELETE RESTRICT,
  worker_pool_version BIGINT NOT NULL,
  tenant_id UUID NOT NULL REFERENCES tenants(id) ON DELETE RESTRICT,
  execution_target_id UUID NOT NULL REFERENCES execution_targets(id) ON DELETE RESTRICT,
  capacity_class TEXT NOT NULL CHECK (capacity_class IN ('standard', 'interactive')),
  warm_supported BOOLEAN NOT NULL DEFAULT FALSE,
  worker_release_revision_id UUID REFERENCES worker_release_revisions(id) ON DELETE RESTRICT,
  worker_release_channel TEXT,
  desired_idle_units INTEGER NOT NULL CHECK (desired_idle_units >= 0),
  max_active_units INTEGER NOT NULL CHECK (max_active_units >= 0),
  desired_total_units INTEGER NOT NULL CHECK (desired_total_units >= 0),
  claimed_units INTEGER NOT NULL CHECK (claimed_units >= 0),
  ready_idle_units INTEGER NOT NULL CHECK (ready_idle_units >= 0),
  source TEXT NOT NULL,
  reason TEXT,
  observed_at TIMESTAMPTZ NOT NULL,
  expires_at TIMESTAMPTZ NOT NULL,
  version BIGINT NOT NULL DEFAULT 1 CHECK (version > 0),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  PRIMARY KEY (worker_pool_id, worker_pool_version),
  CHECK (worker_pool_version > 0),
  CHECK (
    (worker_release_revision_id IS NULL AND worker_release_channel IS NULL)
    OR
    (worker_release_revision_id IS NOT NULL AND worker_release_channel IN ('promoted', 'canary'))
  ),
  CHECK (length(btrim(source)) BETWEEN 1 AND 160),
  CHECK (reason IS NULL OR length(reason) <= 2000),
  CHECK (expires_at > observed_at),
  CHECK (desired_idle_units <= max_active_units),
  CHECK (desired_total_units <= max_active_units),
  CHECK (ready_idle_units <= desired_total_units),
  CHECK (
    warm_supported
    OR (desired_total_units = 0 AND ready_idle_units = 0)
  )
);

CREATE INDEX idx_worker_pool_warm_capacity_target
  ON worker_pool_warm_capacity (tenant_id, execution_target_id, worker_pool_id, worker_pool_version);

CREATE INDEX idx_worker_pool_warm_capacity_expiry
  ON worker_pool_warm_capacity (observed_at DESC, execution_target_id, expires_at, worker_pool_id, worker_pool_version);

CREATE INDEX idx_worker_pool_warm_capacity_release_pair
  ON worker_pool_warm_capacity (worker_pool_id, worker_pool_version, worker_release_revision_id, worker_release_channel);

CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
RETURNS TRIGGER
LANGUAGE plpgsql
AS $$
DECLARE
  pool worker_pools%ROWTYPE;
  target execution_targets%ROWTYPE;
  release worker_release_revisions%ROWTYPE;
BEGIN
  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
  IF NOT FOUND
     OR pool.execution_target_id <> NEW.execution_target_id
     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
     OR pool.version <> NEW.worker_pool_version
     OR pool.mode <> 'warm'
     OR pool.status = 'disabled'
     OR pool.capacity_class <> NEW.capacity_class
     OR pool.desired_idle_units <> NEW.desired_idle_units
     OR pool.max_active_units <> NEW.max_active_units THEN
    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
  END IF;

  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
  IF NOT FOUND
     OR target.tenant_id IS NULL
     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
     OR target.kind <> 'kubernetes'
     OR target.status <> 'active' THEN
    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
  END IF;

  IF NEW.worker_release_revision_id IS NOT NULL THEN
    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
    IF NOT FOUND
       OR release.execution_target_id <> NEW.execution_target_id
       OR release.tenant_id <> NEW.tenant_id THEN
      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
    END IF;
  END IF;

  RETURN NEW;
END;
$$;

DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope ON worker_pool_warm_capacity;
CREATE CONSTRAINT TRIGGER trg_worker_pool_warm_capacity_scope
AFTER INSERT OR UPDATE
ON worker_pool_warm_capacity
DEFERRABLE INITIALLY DEFERRED
FOR EACH ROW EXECUTE FUNCTION assert_worker_pool_warm_capacity_scope();

CREATE OR REPLACE FUNCTION enforce_worker_pool_warm_capacity_cas()
RETURNS TRIGGER
LANGUAGE plpgsql
AS $$
BEGIN
  IF TG_OP = 'DELETE' THEN
    RAISE EXCEPTION 'Worker pool warm capacity observations cannot be deleted' USING ERRCODE = '23514';
  END IF;
  IF NEW.worker_pool_id <> OLD.worker_pool_id
     OR NEW.worker_pool_version <> OLD.worker_pool_version
     OR NEW.tenant_id <> OLD.tenant_id
     OR NEW.execution_target_id <> OLD.execution_target_id THEN
    RAISE EXCEPTION 'Worker pool warm capacity authority ownership is immutable' USING ERRCODE = '23514';
  END IF;
  IF NEW.version <> OLD.version + 1 OR NEW.observed_at <= OLD.observed_at THEN
    RAISE EXCEPTION 'Worker pool warm capacity version must advance exactly once and observed_at must advance'
      USING ERRCODE = '23514';
  END IF;
  RETURN NEW;
END;
$$;

DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_cas ON worker_pool_warm_capacity;
CREATE TRIGGER trg_worker_pool_warm_capacity_cas
BEFORE UPDATE OR DELETE ON worker_pool_warm_capacity
FOR EACH ROW EXECUTE FUNCTION enforce_worker_pool_warm_capacity_cas();
package persistence

import (
	"time"

	"github.com/google/uuid"
)

type WorkerPool struct {
	ID                 uuid.UUID      `gorm:"column:id;type:uuid;primaryKey"`
	TenantID           *uuid.UUID     `gorm:"column:tenant_id;type:uuid"`
	ExecutionTargetID  uuid.UUID      `gorm:"column:execution_target_id;type:uuid;not null;uniqueIndex:uq_worker_pool_target_name,priority:1;index:idx_worker_pool_target_status,priority:1"`
	Name               string         `gorm:"column:name;not null;uniqueIndex:uq_worker_pool_target_name,priority:2"`
	Mode               string         `gorm:"column:mode;not null"`
	CapacityClass      string         `gorm:"column:capacity_class;not null;index:idx_worker_pool_target_status,priority:3"`
	ClusterID          string         `gorm:"column:cluster_id;not null;default:''"`
	Region             string         `gorm:"column:region;not null;default:''"`
	Namespace          string         `gorm:"column:namespace;not null;default:''"`
	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
	Version            int64          `gorm:"column:version;not null;default:1"`
	CreatedAt          time.Time      `gorm:"column:created_at;not null"`
	UpdatedAt          time.Time      `gorm:"column:updated_at;not null"`
}

func (WorkerPool) TableName() string { return "worker_pools" }

type ExecutionPlacementPolicy struct {
	TenantID          *uuid.UUID `gorm:"column:tenant_id;type:uuid"`
	ExecutionTargetID uuid.UUID  `gorm:"column:execution_target_id;type:uuid;primaryKey"`
	Version           int64      `gorm:"column:version;not null;default:1"`
	DefaultPoolID     uuid.UUID  `gorm:"column:default_pool_id;type:uuid;not null"`
	BalancedPoolID    *uuid.UUID `gorm:"column:balanced_pool_id;type:uuid"`
	LowLatencyPoolID  *uuid.UUID `gorm:"column:low_latency_pool_id;type:uuid"`
	UpdatedBy         *uuid.UUID `gorm:"column:updated_by;type:uuid"`
	UpdatedAt         time.Time  `gorm:"column:updated_at;not null"`
}

func (ExecutionPlacementPolicy) TableName() string { return "execution_placement_policies" }
package persistence

import (
	"time"

	"github.com/google/uuid"
)

type WorkerPoolWarmCapacity struct {
	WorkerPoolID            uuid.UUID  `gorm:"column:worker_pool_id;type:uuid;primaryKey"`
	WorkerPoolVersion       int64      `gorm:"column:worker_pool_version;primaryKey"`
	TenantID                uuid.UUID  `gorm:"column:tenant_id;type:uuid;not null;index:idx_worker_pool_warm_capacity_target,priority:1"`
	ExecutionTargetID       uuid.UUID  `gorm:"column:execution_target_id;type:uuid;not null;index:idx_worker_pool_warm_capacity_target,priority:2;index:idx_worker_pool_warm_capacity_expiry,priority:2"`
	CapacityClass           string     `gorm:"column:capacity_class;not null"`
	WarmSupported           bool       `gorm:"column:warm_supported;not null;default:false"`
	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
	ReadyIdleUnits          int        `gorm:"column:ready_idle_units;not null;default:0"`
	Source                  string     `gorm:"column:source;not null"`
	Reason                  *string    `gorm:"column:reason"`
	ObservedAt              time.Time  `gorm:"column:observed_at;not null;index:idx_worker_pool_warm_capacity_expiry,priority:1"`
	ExpiresAt               time.Time  `gorm:"column:expires_at;not null;index:idx_worker_pool_warm_capacity_expiry,priority:3"`
	Version                 int64      `gorm:"column:version;not null;default:1"`
	UpdatedAt               time.Time  `gorm:"column:updated_at;not null"`
}

func (WorkerPoolWarmCapacity) TableName() string { return "worker_pool_warm_capacity" }

 succeeded in 0ms:
package httpapi

import (
	"net/http"

	"github.com/synara-ai/synara/services/control-plane/internal/placement"
)

func (s *Server) listWorkerPools(w http.ResponseWriter, r *http.Request) {
	tenantID, ok := s.pathUUID(w, r, "tenantID")
	if !ok {
		return
	}
	targetID, ok := s.pathUUID(w, r, "executionTargetID")
	if !ok {
		return
	}
	state, err := s.placement.List(r.Context(), mustPrincipal(r), tenantID, targetID)
	if err != nil {
		s.writeError(w, r, err)
		return
	}
	writeJSON(w, http.StatusOK, state)
}

func (s *Server) createWorkerPool(w http.ResponseWriter, r *http.Request) {
	tenantID, ok := s.pathUUID(w, r, "tenantID")
	if !ok {
		return
	}
	targetID, ok := s.pathUUID(w, r, "executionTargetID")
	if !ok {
		return
	}
	var input placement.CreatePoolInput
	if err := decodeJSON(r, &input); err != nil {
		s.writeError(w, r, err)
		return
	}
	pool, err := s.placement.CreatePool(r.Context(), mustPrincipal(r), tenantID, targetID, input)
	if err != nil {
		s.writeError(w, r, err)
		return
	}
	writeJSON(w, http.StatusCreated, pool)
}

func (s *Server) updateWorkerPool(w http.ResponseWriter, r *http.Request) {
	tenantID, ok := s.pathUUID(w, r, "tenantID")
	if !ok {
		return
	}
	targetID, ok := s.pathUUID(w, r, "executionTargetID")
	if !ok {
		return
	}
	poolID, ok := s.pathUUID(w, r, "workerPoolID")
	if !ok {
		return
	}
	var input placement.UpdatePoolInput
	if err := decodeJSON(r, &input); err != nil {
		s.writeError(w, r, err)
		return
	}
	pool, err := s.placement.UpdatePool(r.Context(), mustPrincipal(r), tenantID, targetID, poolID, input)
	if err != nil {
		s.writeError(w, r, err)
		return
	}
	writeJSON(w, http.StatusOK, pool)
}

func (s *Server) updateExecutionPlacementPolicy(w http.ResponseWriter, r *http.Request) {
	tenantID, ok := s.pathUUID(w, r, "tenantID")
	if !ok {
		return
	}
	targetID, ok := s.pathUUID(w, r, "executionTargetID")
	if !ok {
		return
	}
	var input placement.UpdatePolicyInput
	if err := decodeJSON(r, &input); err != nil {
		s.writeError(w, r, err)
		return
	}
	state, err := s.placement.UpdatePolicy(r.Context(), mustPrincipal(r), tenantID, targetID, input)
	if err != nil {
		s.writeError(w, r, err)
		return
	}
	writeJSON(w, http.StatusOK, state)
}

exec
/bin/zsh -lc "sed -n '1,360p' services/control-plane/internal/placement/service.go
sed -n '760,880p' services/control-plane/internal/placement/service.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
package placement

import (
	"context"
	"crypto/md5"
	"encoding/hex"
	"errors"
	"strings"
	"time"

	"github.com/google/uuid"
	"gorm.io/gorm"

	"github.com/synara-ai/synara/services/control-plane/internal/authorization"
	"github.com/synara-ai/synara/services/control-plane/internal/identity"
	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
	"github.com/synara-ai/synara/services/control-plane/internal/problem"
	"github.com/synara-ai/synara/services/control-plane/internal/warmcapacity"
)

const (
	PoolModeResident     = "resident"
	PoolModePerExecution = "per-execution"
	PoolModeWarm         = "warm"

	CapacityClassStandard    = "standard"
	CapacityClassInteractive = "interactive"

	PoolStatusActive   = "active"
	PoolStatusDraining = "draining"
	PoolStatusDisabled = "disabled"

	WarmPoolModeDefault    = "default"
	WarmPoolModeDisabled   = "disabled"
	WarmPoolModeBalanced   = "balanced"
	WarmPoolModeLowLatency = "low-latency"
)

type Pool struct {
	ID                 uuid.UUID      `json:"id"`
	TenantID           *uuid.UUID     `json:"tenantId"`
	ExecutionTargetID  uuid.UUID      `json:"executionTargetId"`
	Name               string         `json:"name"`
	Mode               string         `json:"mode"`
	CapacityClass      string         `json:"capacityClass"`
	ClusterID          string         `json:"clusterId"`
	Region             string         `json:"region"`
	Namespace          string         `json:"namespace"`
	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
	MaxActiveUnits     int            `json:"maxActiveUnits"`
	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
	Status             string         `json:"status"`
	Version            int64          `json:"version"`
	CreatedAt          time.Time      `json:"createdAt"`
	UpdatedAt          time.Time      `json:"updatedAt"`
}

type Policy struct {
	TenantID          *uuid.UUID `json:"tenantId"`
	ExecutionTargetID uuid.UUID  `json:"executionTargetId"`
	Version           int64      `json:"version"`
	DefaultPoolID     uuid.UUID  `json:"defaultPoolId"`
	BalancedPoolID    *uuid.UUID `json:"balancedPoolId"`
	LowLatencyPoolID  *uuid.UUID `json:"lowLatencyPoolId"`
	UpdatedBy         *uuid.UUID `json:"updatedBy"`
	UpdatedAt         time.Time  `json:"updatedAt"`
}

type State struct {
	Pools  []Pool `json:"pools"`
	Policy Policy `json:"policy"`
}

type UpdatePolicyInput struct {
	ExpectedVersion  int64      `json:"expectedVersion"`
	DefaultPoolID    uuid.UUID  `json:"defaultPoolId"`
	BalancedPoolID   *uuid.UUID `json:"balancedPoolId"`
	LowLatencyPoolID *uuid.UUID `json:"lowLatencyPoolId"`
}

type CreatePoolInput struct {
	Name               string         `json:"name"`
	Mode               string         `json:"mode"`
	CapacityClass      string         `json:"capacityClass"`
	ClusterID          string         `json:"clusterId"`
	Region             string         `json:"region"`
	Namespace          string         `json:"namespace"`
	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
	MaxActiveUnits     int            `json:"maxActiveUnits"`
	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
	Status             string         `json:"status"`
}

type UpdatePoolInput struct {
	ExpectedVersion int64 `json:"expectedVersion"`
	CreatePoolInput
}

type Selection struct {
	Pool          Pool   `json:"pool"`
	CapacityClass string `json:"capacityClass"`
	PolicyVersion int64  `json:"policyVersion"`
}

func ApplySelection(execution *persistence.AgentExecution, selection Selection) {
	if execution == nil {
		return
	}
	poolID := selection.Pool.ID
	poolVersion := selection.Pool.Version
	capacityClass := selection.CapacityClass
	policyVersion := selection.PolicyVersion
	execution.WorkerPoolID = &poolID
	execution.WorkerPoolVersion = &poolVersion
	execution.CapacityClass = &capacityClass
	execution.PlacementPolicyVersion = &policyVersion
	execution.PlacementRegion = selection.Pool.Region
	execution.PlacementClusterID = selection.Pool.ClusterID
}

type Service struct {
	db         *gorm.DB
	authorizer *authorization.Authorizer
	now        func() time.Time
}

func NewService(db *gorm.DB) *Service {
	return &Service{
		db:         db,
		authorizer: authorization.NewAuthorizer(db),
		now:        func() time.Time { return time.Now().UTC() },
	}
}

func (s *Service) List(ctx context.Context, principal identity.Principal, tenantID, targetID uuid.UUID) (State, error) {
	if err := requireActiveTenant(principal, tenantID); err != nil {
		return State{}, err
	}
	if _, err := s.authorizer.RequireTenant(ctx, principal.UserID, tenantID, authorization.WorkerRead); err != nil {
		return State{}, err
	}
	var state State
	err := persistence.InTransaction(ctx, s.db, func(tx *gorm.DB) error {
		target, err := s.loadAccessibleTarget(ctx, tx, tenantID, targetID, false)
		if err != nil {
			return err
		}
		state, err = s.EnsureDefault(ctx, tx, target)
		return err
	})
	return state, err
}

func (s *Service) UpdatePolicy(
	ctx context.Context,
	principal identity.Principal,
	tenantID, targetID uuid.UUID,
	input UpdatePolicyInput,
) (State, error) {
	if err := requireActiveTenant(principal, tenantID); err != nil {
		return State{}, err
	}
	if _, err := s.authorizer.RequireTenant(ctx, principal.UserID, tenantID, authorization.WorkerManage); err != nil {
		return State{}, err
	}
	if input.ExpectedVersion <= 0 {
		return State{}, problem.New(400, "invalid_execution_placement_policy_version", "expectedVersion must be positive.")
	}
	if input.DefaultPoolID == uuid.Nil {
		return State{}, problem.New(400, "invalid_execution_placement_policy_default_pool", "defaultPoolId is required.")
	}
	var state State
	err := persistence.InTransaction(ctx, s.db, func(tx *gorm.DB) error {
		target, err := s.loadAccessibleTarget(ctx, tx, tenantID, targetID, true)
		if err != nil {
			return err
		}
		if target.TenantID == nil {
			return problem.New(403, "shared_execution_target_placement_policy_immutable", "Platform-shared execution target placement policy cannot be changed by a tenant.")
		}
		current, models, err := s.ensureDefaultLocked(ctx, tx, target)
		if err != nil {
			return err
		}
		if input.ExpectedVersion != current.Version {
			return problem.New(409, "execution_placement_policy_version_conflict", "Execution placement policy changed; reload it before saving.")
		}
		nextBalanced, err := normalizeOptionalPoolID(input.BalancedPoolID)
		if err != nil {
			return err
		}
		nextLowLatency, err := normalizeOptionalPoolID(input.LowLatencyPoolID)
		if err != nil {
			return err
		}
		resolved, err := validatePolicyInput(target, models, input.DefaultPoolID, nextBalanced, nextLowLatency)
		if err != nil {
			return err
		}
		if current.DefaultPoolID == resolved.defaultPool.ID &&
			sameUUIDPointer(current.BalancedPoolID, resolved.balancedPoolID) &&
			sameUUIDPointer(current.LowLatencyPoolID, resolved.lowLatencyPoolID) {
			state = toState(current, models)
			return nil
		}
		now := s.now()
		updates := map[string]any{
			"version":             current.Version + 1,
			"default_pool_id":     resolved.defaultPool.ID,
			"balanced_pool_id":    resolved.balancedPoolID,
			"low_latency_pool_id": resolved.lowLatencyPoolID,
			"updated_by":          principal.UserID,
			"updated_at":          now,
		}
		result := tx.WithContext(ctx).Model(&persistence.ExecutionPlacementPolicy{}).
			Where("execution_target_id = ? AND version = ?", target.ID, current.Version).
			Updates(updates)
		if result.Error != nil {
			return problem.Wrap(500, "execution_placement_policy_update_failed", "Execution placement policy could not be updated.", result.Error)
		}
		if result.RowsAffected != 1 {
			return problem.New(409, "execution_placement_policy_version_conflict", "Execution placement policy changed; reload it before saving.")
		}
		current.Version++
		current.DefaultPoolID = resolved.defaultPool.ID
		current.BalancedPoolID = resolved.balancedPoolID
		current.LowLatencyPoolID = resolved.lowLatencyPoolID
		current.UpdatedBy = &principal.UserID
		current.UpdatedAt = now
		state = toState(current, models)
		return nil
	})
	return state, err
}

func (s *Service) CreatePool(
	ctx context.Context,
	principal identity.Principal,
	tenantID, targetID uuid.UUID,
	input CreatePoolInput,
) (Pool, error) {
	if err := requireActiveTenant(principal, tenantID); err != nil {
		return Pool{}, err
	}
	if _, err := s.authorizer.RequireTenant(ctx, principal.UserID, tenantID, authorization.WorkerManage); err != nil {
		return Pool{}, err
	}
	normalized, err := normalizePoolInput(input)
	if err != nil {
		return Pool{}, err
	}
	var created Pool
	err = persistence.InTransaction(ctx, s.db, func(tx *gorm.DB) error {
		target, err := s.loadAccessibleTarget(ctx, tx, tenantID, targetID, true)
		if err != nil {
			return err
		}
		if target.TenantID == nil {
			return problem.New(403, "shared_execution_target_worker_pool_immutable", "Platform-shared execution target worker pools cannot be changed by a tenant.")
		}
		if err := validatePoolTargetMode(target, normalized.Mode); err != nil {
			return err
		}
		now := s.now()
		model := persistence.WorkerPool{
			ID:                 uuid.New(),
			TenantID:           target.TenantID,
			ExecutionTargetID:  target.ID,
			Name:               normalized.Name,
			Mode:               normalized.Mode,
			CapacityClass:      normalized.CapacityClass,
			ClusterID:          normalized.ClusterID,
			Region:             normalized.Region,
			Namespace:          normalized.Namespace,
			DesiredIdleUnits:   normalized.DesiredIdleUnits,
			MaxActiveUnits:     normalized.MaxActiveUnits,
			SchedulingTemplate: normalized.SchedulingTemplate,
			Status:             normalized.Status,
			Version:            1,
			CreatedAt:          now,
			UpdatedAt:          now,
		}
		if err := tx.WithContext(ctx).Create(&model).Error; err != nil {
			return problem.Wrap(409, "worker_pool_create_rejected", "Worker pool could not be created.", err)
		}
		created = toPool(model)
		return nil
	})
	return created, err
}

func (s *Service) UpdatePool(
	ctx context.Context,
	principal identity.Principal,
	tenantID, targetID, poolID uuid.UUID,
	input UpdatePoolInput,
) (Pool, error) {
	if err := requireActiveTenant(principal, tenantID); err != nil {
		return Pool{}, err
	}
	if _, err := s.authorizer.RequireTenant(ctx, principal.UserID, tenantID, authorization.WorkerManage); err != nil {
		return Pool{}, err
	}
	if input.ExpectedVersion <= 0 {
		return Pool{}, problem.New(400, "invalid_worker_pool_version", "expectedVersion must be positive.")
	}
	normalized, err := normalizePoolInput(input.CreatePoolInput)
	if err != nil {
		return Pool{}, err
	}
	var updated Pool
	err = persistence.InTransaction(ctx, s.db, func(tx *gorm.DB) error {
		target, err := s.loadAccessibleTarget(ctx, tx, tenantID, targetID, true)
		if err != nil {
			return err
		}
		if target.TenantID == nil {
			return problem.New(403, "shared_execution_target_worker_pool_immutable", "Platform-shared execution target worker pools cannot be changed by a tenant.")
		}
		if err := validatePoolTargetMode(target, normalized.Mode); err != nil {
			return err
		}
		var current persistence.WorkerPool
		err = persistence.WithLocking(tx.WithContext(ctx), "UPDATE", "").
			Where("id = ? AND execution_target_id = ? AND tenant_id = ?", poolID, targetID, tenantID).
			Take(&current).Error
		if errors.Is(err, gorm.ErrRecordNotFound) {
			return problem.New(404, "worker_pool_not_found", "Worker pool not found.")
		}
		if err != nil {
			return problem.Wrap(500, "worker_pool_load_failed", "Worker pool could not be loaded.", err)
		}
		if current.Version != input.ExpectedVersion {
			return problem.New(409, "worker_pool_version_conflict", "Worker pool changed; reload it before saving.")
		}
		if current.Mode != normalized.Mode || current.CapacityClass != normalized.CapacityClass {
			return problem.New(409, "worker_pool_identity_immutable", "Worker pool mode and capacityClass cannot change after creation.")
		}
		var blocked int64
		if err := tx.WithContext(ctx).Model(&persistence.AgentExecution{}).
			Where(
				"execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ? AND status IN ?",
				target.ID,
				current.ID,
				current.Version,
				[]string{"queued", "recovering", "leased", "running", "waiting-for-approval", "suspended"},
			).
			Count(&blocked).Error; err != nil {
			return problem.Wrap(500, "worker_pool_update_probe_failed", "Worker pool update safety could not be evaluated.", err)
		}
		if blocked > 0 {
			return problem.New(
				409,
				"worker_pool_version_update_blocked",
				"Worker pool version cannot change while a nonterminal Execution still references the current pool snapshot.",
			)
		}
		now := s.now()
		expectedVersion := current.Version
		current.Name = normalized.Name
		Version:           model.Version,
		DefaultPoolID:     model.DefaultPoolID,
		BalancedPoolID:    model.BalancedPoolID,
		LowLatencyPoolID:  model.LowLatencyPoolID,
		UpdatedBy:         model.UpdatedBy,
		UpdatedAt:         model.UpdatedAt,
	}
}

func defaultPoolMode(kind string) string {
	switch strings.ToLower(strings.TrimSpace(kind)) {
	case "kubernetes":
		return PoolModePerExecution
	case "docker":
		return PoolModeResident
	default:
		return PoolModeResident
	}
}

func normalizeWarmPoolMode(mode string) (string, error) {
	normalized := strings.ToLower(strings.TrimSpace(mode))
	if normalized == "" {
		normalized = WarmPoolModeDefault
	}
	switch normalized {
	case WarmPoolModeDefault, WarmPoolModeDisabled, WarmPoolModeBalanced, WarmPoolModeLowLatency:
		return normalized, nil
	default:
		return "", problem.New(400, "invalid_warm_pool_mode", "warmPoolMode must be disabled, default, balanced, or low-latency.")
	}
}

func normalizeOptionalPoolID(value *uuid.UUID) (*uuid.UUID, error) {
	if value == nil {
		return nil, nil
	}
	if *value == uuid.Nil {
		return nil, problem.New(400, "invalid_execution_placement_pool_id", "Pool references must not be empty UUIDs.")
	}
	return value, nil
}

func normalizePoolInput(input CreatePoolInput) (CreatePoolInput, error) {
	name := strings.TrimSpace(input.Name)
	if len(name) == 0 || len(name) > 160 {
		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_name", "Worker pool name must be between 1 and 160 characters.")
	}
	mode := strings.ToLower(strings.TrimSpace(input.Mode))
	switch mode {
	case PoolModeResident, PoolModePerExecution, PoolModeWarm:
	default:
		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_mode", "Worker pool mode must be resident, per-execution, or warm.")
	}
	capacityClass := strings.ToLower(strings.TrimSpace(input.CapacityClass))
	switch capacityClass {
	case CapacityClassStandard, CapacityClassInteractive:
	default:
		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_class", "Worker pool capacityClass must be standard or interactive.")
	}
	status := strings.ToLower(strings.TrimSpace(input.Status))
	switch status {
	case PoolStatusActive, PoolStatusDraining, PoolStatusDisabled:
	default:
		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
	}
	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
	}
	template := input.SchedulingTemplate
	if template == nil {
		template = map[string]any{}
	}
	return CreatePoolInput{
		Name:               name,
		Mode:               mode,
		CapacityClass:      capacityClass,
		ClusterID:          strings.TrimSpace(input.ClusterID),
		Region:             strings.TrimSpace(input.Region),
		Namespace:          strings.TrimSpace(input.Namespace),
		DesiredIdleUnits:   input.DesiredIdleUnits,
		MaxActiveUnits:     input.MaxActiveUnits,
		SchedulingTemplate: template,
		Status:             status,
	}, nil
}

func validatePoolTargetMode(target persistence.ExecutionTarget, mode string) error {
	if mode == PoolModeWarm && !strings.EqualFold(strings.TrimSpace(target.Kind), "kubernetes") {
		return problem.New(
			409,
			"worker_pool_mode_target_unsupported",
			"Warm Worker pools are only supported on Kubernetes execution targets.",
		)
	}
	return nil
}

func deterministicUUID(seed string) uuid.UUID {
	sum := md5.Sum([]byte(seed))
	raw := hex.EncodeToString(sum[:])
	id, err := uuid.Parse(raw[0:8] + "-" + raw[8:12] + "-" + raw[12:16] + "-" + raw[16:20] + "-" + raw[20:32])
	if err != nil {
		panic(err)
	}
	return id
}

func sameUUIDPointer(left, right *uuid.UUID) bool {
	if left == nil || right == nil {
		return left == nil && right == nil
	}
	return *left == *right
}

func nonNilDB(tx, fallback *gorm.DB) *gorm.DB {
	if tx != nil {
		return tx
	}
	return fallback
}

exec
/bin/zsh -lc "sed -n '560,980p' services/control-plane/internal/executiontargets/kubernetes_reconciler.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
			failureClass, failureReasonCode := classifyKubernetesExecutionPodFailure(pod)
			if err := r.observeExecutionPod(ctx, KubernetesExecutionPodObservation{
				TenantID: execution.TenantID, ExecutionTargetID: target.ID,
				ExecutionID: executionID, Generation: generation,
				Namespace: configuration.Namespace, PodName: pod.Name, PodUID: pod.UID,
				Phase: pod.Phase, FailureClass: failureClass, FailureReasonCode: failureReasonCode,
				PendingFailureThreshold: r.podPendingFailureThreshold(), PodCreatedAt: pod.CreatedAt,
				ObservedAt: r.now(),
			}); err != nil {
				return err
			}
			if terminalPod {
				if err := r.observeTerminalExecutionPod(ctx, target.ID, configuration.Namespace, pod); err != nil {
					return err
				}
			}
		}
		if exactExecutionPod &&
			pod.Phase == "Succeeded" && kubernetesPodCompletedSuccessfully(pod) {
			if r.config.FinalizeResourceSuspend == nil {
				return problem.New(503, "kubernetes_suspend_finalizer_unavailable", "Kubernetes Pod-terminal suspension finalization is not configured.")
			}
			finalized, err := r.config.FinalizeResourceSuspend(ctx, KubernetesPodTerminalObservation{
				ExecutionTargetID: target.ID, ExecutionID: executionID, Generation: generation,
				Namespace: configuration.Namespace, PodName: pod.Name,
				PodUID: pod.UID, Phase: pod.Phase, ObservedAt: r.now(),
			})
			if err != nil {
				return problem.Wrap(500, "kubernetes_suspend_finalize_failed", "The terminal Kubernetes Worker Pod could not finalize resource suspension.", err)
			}
			if !finalized {
				// Keep the exact terminal Pod until Control Plane state catches up.
				// Deleting here would destroy the only kubelet-authored proof for this
				// Pod UID and can permanently wedge suspension if checkpoint-ready or
				// durable DB visibility lags the terminal observation briefly.
				existing[pod.Name] = pod
				continue
			}
		}
		if !exactExecutionPod || terminalPod {
			reason := "execution-pod-obsolete"
			if terminalPod {
				reason = "execution-pod-terminal"
			}
			deletedPod, _, err := r.deleteObservedPodSafely(
				ctx, client, target.ID, configuration.Namespace, pod, reason,
			)
			if err != nil {
				return err
			}
			if deletedPod {
				deleted++
			} else {
				existing[pod.Name] = pod
			}
			continue
		}
		existing[pod.Name] = pod
	}
	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
	)
	if err != nil {
		return err
	}
	readyWarmCapacity := make(map[kubernetesWarmCapacityKey]int)
	warmDemandEvictionCandidates := make([]kubernetesWarmDemandEvictionCandidate, 0)
	for _, observed := range unleasedWarmPods {
		plan, found := validationWarmPlansByName[observed.Pod.Name]
		terminalPod := observed.Pod.Phase == "Succeeded" || observed.Pod.Phase == "Failed"
		if !found || terminalPod ||
			observed.Pod.Annotations[kubernetesConfigAnnotation] != plan.ConfigHash ||
			observed.PoolID != plan.Pool.ID ||
			observed.PoolVersion != plan.Pool.Version ||
			observed.CapacityClass != plan.Pool.CapacityClass ||
			observed.Slot != plan.Slot ||
			(observed.State != nil && !kubernetesWarmWorkerStateMatchesPlan(*observed.State, plan)) {
			reason := "warm-pool-plan-mismatch"
			if !found {
				reason = "warm-pool-scale-down"
			} else if terminalPod {
				reason = "warm-pool-terminal"
			}
			deletedPod, retainedWorker, err := r.deleteObservedPodSafely(
				ctx, client, target.ID, configuration.Namespace, observed.Pod, reason,
			)
			if err != nil {
				return err
			}
			if retainedWorker != nil {
				if retainedWorker.WorkerPoolID != nil {
					warmClaimedCounts[*retainedWorker.WorkerPoolID]++
				}
				existing[observed.Pod.Name] = observed.Pod
				continue
			}
			if deletedPod {
				deleted++
				continue
			}
			existing[observed.Pod.Name] = observed.Pod
			continue
		}
		if observed.State == nil {
			existing[observed.Pod.Name] = observed.Pod
			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
					Observed: observed,
					Key:      key,
				})
			}
			continue
		}
		readinessObservedAt := r.now()
		if !kubernetesWarmWorkerStateReadyIdle(
			*observed.State,
			observed.Pod,
			plan,
			readinessObservedAt,
			r.config.WorkerHeartbeatTimeout,
		) {
			if kubernetesWarmWorkerStateShouldRecycle(
				*observed.State,
				observed.Pod,
				readinessObservedAt,
				r.config.WorkerHeartbeatTimeout,
			) {
				deletedPod, retainedWorker, err := r.deleteObservedPodSafely(
					ctx, client, target.ID, configuration.Namespace, observed.Pod, "warm-pool-not-ready",
				)
				if err != nil {
					return err
				}
				if retainedWorker != nil {
					if retainedWorker.WorkerPoolID != nil {
						warmClaimedCounts[*retainedWorker.WorkerPoolID]++
					}
					existing[observed.Pod.Name] = observed.Pod
					continue
				}
				if deletedPod {
					deleted++
					continue
				}
			}
			existing[observed.Pod.Name] = observed.Pod
			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
					Observed: observed,
					Key:      key,
				})
			}
			continue
		}
		existing[observed.Pod.Name] = observed.Pod
		if key, ok := kubernetesWarmCapacityKeyForState(*observed.State); ok {
			readyWarmCapacity[key]++
		}
	}
	readyForDemand := make(map[kubernetesWarmCapacityKey]int, len(readyWarmCapacity))
	for key, units := range readyWarmCapacity {
		readyForDemand[key] = units
	}
	coldNeeded := 0
	unmetWarmDemand := make(map[kubernetesWarmCapacityKey]int)
	for _, execution := range executions {
		if execution.Status != "queued" && execution.Status != "recovering" {
			continue
		}
		if !kubernetesExecutionWithinAbsoluteLifetime(execution, r.now()) {
			continue
		}
		if _, found := existing[kubernetesPodName(execution)]; found {
			continue
		}
		if key, ok := kubernetesWarmCapacityKeyForExecution(execution); ok {
			if readyForDemand[key] > 0 {
				readyForDemand[key]--
				continue
			}
			unmetWarmDemand[key]++
		}
		coldNeeded++
	}
	freeSlots := configuration.MaxActivePods - (len(existing) + deleted)
	if freeSlots < 0 {
		freeSlots = 0
	}
	demandEvictionsNeeded := coldNeeded - freeSlots
	if demandEvictionsNeeded > 0 {
		attempted := make(map[int]struct{}, len(warmDemandEvictionCandidates))
		evictCandidate := func(index int) error {
			attempted[index] = struct{}{}
			candidate := warmDemandEvictionCandidates[index]
			deletedPod, retainedWorker, err := r.deleteObservedPodSafely(
				ctx,
				client,
				target.ID,
				configuration.Namespace,
				candidate.Observed.Pod,
				"warm-pool-demand-fallback",
			)
			if err != nil {
				return err
			}
			if retainedWorker != nil {
				if retainedWorker.WorkerPoolID != nil {
					warmClaimedCounts[*retainedWorker.WorkerPoolID]++
				}
				return nil
			}
			if deletedPod {
				delete(existing, candidate.Observed.Pod.Name)
				deleted++
				demandEvictionsNeeded--
				if unmetWarmDemand[candidate.Key] > 0 {
					unmetWarmDemand[candidate.Key]--
				}
			}
			return nil
		}
		// Prefer evicting a not-ready Pod whose exact warm pool/release key
		// cannot satisfy queued demand. If the remaining cold demand is for a
		// general or different-pool Execution, any not-ready warm slot can still
		// be released because maxActivePods is target-wide.
		for index, candidate := range warmDemandEvictionCandidates {
			if demandEvictionsNeeded <= 0 {
				break
			}
			if unmetWarmDemand[candidate.Key] <= 0 {
				continue
			}
			if err := evictCandidate(index); err != nil {
				return err
			}
		}
		for index := range warmDemandEvictionCandidates {
			if demandEvictionsNeeded <= 0 {
				break
			}
			if _, found := attempted[index]; found {
				continue
			}
			if err := evictCandidate(index); err != nil {
				return err
			}
		}
	}
	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
	)
	if err != nil {
		return err
	}
	// A Pod accepted for deletion still consumes ResourceQuota until Kubernetes
	// finishes its grace period. Count deletion-pending Pods against the target
	// capacity so reconciliation does not create a replacement that the API
	// server must reject with an exceeded-quota error.
	scheduled := len(existing) + deleted
	// One Execution that cannot get a Pod must not starve every other queued
	// Execution on this target for the cycle: a malformed pool scheduling
	// template, a rejected Pod spec or a failed observation write is scoped to
	// its own Execution. Failures are collected and reported after the sweep,
	// so the pass is still reported as failed and warm capacity stays
	// unpublished, but the remaining Executions are still placed.
	var executionFailures []error
	for _, execution := range executions {
		if execution.Status != "queued" && execution.Status != "recovering" {
			continue
		}
		name := kubernetesPodName(execution)
		if _, found := existing[name]; found {
			acknowledgedReservations[routing.ReservationIdentity{
				ExecutionID: execution.ID, Generation: execution.Generation,
			}] = struct{}{}
			continue
		}
		if key, ok := kubernetesWarmCapacityKeyForExecution(execution); ok && readyWarmCapacity[key] > 0 {
			readyWarmCapacity[key]--
			acknowledgedReservations[routing.ReservationIdentity{
				ExecutionID: execution.ID, Generation: execution.Generation,
			}] = struct{}{}
			continue
		}
		if scheduled >= configuration.MaxActivePods {
			continue
		}
		podHash, err := kubernetesExecutionPodHash(podBaseHash, configuration.Image, execution)
		if err != nil {
			executionFailures = append(executionFailures, fmt.Errorf("execution %s: %w", execution.ID, err))
			continue
		}
		pod, err := r.executionPod(target, configuration, podHash, execution, credential)
		if err != nil {
			executionFailures = append(executionFailures, fmt.Errorf("execution %s: %w", execution.ID, err))
			continue
		}
		// The Session can cross its immutable deadline after the initial query.
		// Recheck at the external write boundary so a stale reconciliation
		// snapshot cannot create a post-expiry Pod.
		if !kubernetesExecutionWithinAbsoluteLifetime(execution, r.now()) {
			continue
		}
		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
		applyStartedAt := r.now()
		applyErr := client.Apply(ctx, path, pod)
		observation := KubernetesExecutionPodObservation{
			TenantID: execution.TenantID, ExecutionTargetID: target.ID,
			ExecutionID: execution.ID, Generation: execution.Generation + 1,
			Namespace: configuration.Namespace, PodName: name, Phase: "Applied",
			PendingFailureThreshold: r.podPendingFailureThreshold(), ObservedAt: applyStartedAt,
		}
		if applyErr != nil {
			observation.Phase = "ApplyFailed"
			observation.FailureClass = KubernetesPodFailureApplyFailed
			observation.FailureReasonCode = classifyKubernetesPodApplyFailure(applyErr)
		}
		if err := r.observeExecutionPod(ctx, observation); err != nil {
			if applyErr != nil {
				err = errors.Join(
					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", applyErr),
					err,
				)
			}
			executionFailures = append(executionFailures, fmt.Errorf("execution %s: %w", execution.ID, err))
			continue
		}
		if applyErr != nil {
			executionFailures = append(executionFailures, fmt.Errorf(
				"execution %s: %w", execution.ID,
				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", applyErr),
			))
			continue
		}
		acknowledgedReservations[routing.ReservationIdentity{
			ExecutionID: execution.ID, Generation: execution.Generation,
		}] = struct{}{}
		created++
		scheduled++
	}
	for _, plan := range desiredWarmPlans {
		if scheduled >= configuration.MaxActivePods {
			break
		}
		name := kubernetesWarmPodName(plan)
		if _, found := existing[name]; found {
			continue
		}
		pod, err := r.warmPoolPod(target, configuration, plan, credential)
		if err != nil {
			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
			continue
		}
		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
		if err := client.Apply(ctx, path, pod); err != nil {
			executionFailures = append(executionFailures, fmt.Errorf(
				"warm pod %s: %w", name,
				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
			))
			continue
		}
		created++
		scheduled++
	}
	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
		return errors.Join(append(executionFailures, err)...)
	}
	if len(executionFailures) > 0 {
		// The target itself is reachable and its status is current, so the
		// health observation published by the deferred hook stays accurate;
		// only the per-Execution placements failed.
		return errors.Join(executionFailures...)
	}
	availableCapacity := configuration.MaxActivePods
	healthObservation.Status = routing.HealthHealthy
	healthObservation.CapacityStatus = routing.CapacityAvailable
	healthObservation.AvailableCapacityUnits = &availableCapacity
	healthObservation.AllocatedCapacityUnits = scheduled
	healthObservation.ReservationAuthority.Acknowledgements = make(
		[]routing.ReservationIdentity, 0, len(acknowledgedReservations),
	)
	for identity := range acknowledgedReservations {
		healthObservation.ReservationAuthority.Acknowledgements = append(
			healthObservation.ReservationAuthority.Acknowledgements,
			identity,
		)
	}
	sort.Slice(healthObservation.ReservationAuthority.Acknowledgements, func(left, right int) bool {
		leftIdentity := healthObservation.ReservationAuthority.Acknowledgements[left]
		rightIdentity := healthObservation.ReservationAuthority.Acknowledgements[right]
		if leftIdentity.ExecutionID != rightIdentity.ExecutionID {
			return leftIdentity.ExecutionID.String() < rightIdentity.ExecutionID.String()
		}
		return leftIdentity.Generation < rightIdentity.Generation
	})
	healthObservation.Reason = nil
	if scheduled >= configuration.MaxActivePods {
		healthObservation.CapacityStatus = routing.CapacitySaturated
		healthObservation.Reason = managedKubernetesRoutingReasonPointer(
			"Managed Kubernetes scheduled pod capacity is fully allocated.",
		)
	}
	warmCapacityObservations = managedKubernetesWarmCapacityObservations(
		*target.TenantID,
		target.ID,
		warmPools,
		warmPoolsSupported,
		warmClaimedCounts,
		readyWarmCapacity,
		warmRelease,
	)
	return nil
}

func kubernetesPodCompletedSuccessfully(pod kubernetesPod) bool {
	if pod.Phase != "Succeeded" || len(pod.Containers) != 1 {
		return false
	}
	container := pod.Containers[0]
	return container.Name == "agentd" && container.Terminated && container.ExitCode == 0
}

exec
/bin/zsh -lc "sed -n '450,680p' services/control-plane/internal/observability/distributed_routing_billing_metrics.go
rg -n -C 4 'warm_capacity|warm deficit|desired_idle|Worker Pools|Live warm-capacity' docs/contracts/worker-pool-placement-v1.md" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
		Select(`schedule_kind, last_outcome,
			CASE WHEN next_attempt_at <= CURRENT_TIMESTAMP THEN 'due' ELSE 'waiting' END AS due_state,
			COUNT(*) AS count`).
		Group(`schedule_kind, last_outcome,
			CASE WHEN next_attempt_at <= CURRENT_TIMESTAMP THEN 'due' ELSE 'waiting' END`).
		Order("schedule_kind, last_outcome, due_state").
		Scan(&rows).Error; err != nil {
		return fmt.Errorf("collect billing shared allocation schedule period metrics: %w", err)
	}
	writeHelp(
		output,
		"synara_billing_shared_allocation_schedule_periods",
		"Durable shared allocation schedule period inventory by bounded schedule kind, last outcome, and due state.",
		"gauge",
	)
	for _, row := range rows {
		fmt.Fprintf(
			output,
			"synara_billing_shared_allocation_schedule_periods%s %d\n",
			labels(map[string]string{
				"schedule_kind": boundedSharedAllocationScheduleKind(row.ScheduleKind),
				"outcome":       boundedSharedAllocationScheduleOutcome(row.LastOutcome),
				"due_state":     boundedSharedAllocationScheduleDueState(row.DueState),
			}),
			row.Count,
		)
	}
	return nil
}

func boundedSharedAllocationScheduleKind(value string) string {
	switch strings.TrimSpace(value) {
	case "static", "monthly-utc":
		return strings.TrimSpace(value)
	default:
		return "other"
	}
}

func boundedSharedAllocationScheduleOutcome(value string) string {
	switch strings.TrimSpace(value) {
	case "never", "running", "completed", "failed":
		return strings.TrimSpace(value)
	default:
		return "other"
	}
}

func boundedSharedAllocationScheduleDueState(value string) string {
	switch strings.TrimSpace(value) {
	case "due", "waiting":
		return strings.TrimSpace(value)
	default:
		return "other"
	}
}

func (r *Registry) writeWorkerPoolWarmCapacityMetrics(
	ctx context.Context,
	output *bytes.Buffer,
	now time.Time,
) error {
	authorities := make(map[warmCapacityAuthorityMetricKey]int64)
	for _, freshness := range []string{"fresh", "expired"} {
		var rows []warmCapacityAuthorityGroup
		query := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
			Select("capacity_class, warm_supported, COUNT(*) AS count").
			Group("capacity_class, warm_supported")
		if freshness == "fresh" {
			query = query.Where("expires_at > ?", now)
		} else {
			query = query.Where("expires_at <= ?", now)
		}
		if err := query.Scan(&rows).Error; err != nil {
			return fmt.Errorf("collect Worker Pool warm capacity %s authority metrics: %w", freshness, err)
		}
		for _, row := range rows {
			key := warmCapacityAuthorityMetricKey{
				CapacityClass: boundedWarmCapacityClass(row.CapacityClass),
				Freshness:     freshness,
				WarmSupported: row.WarmSupported,
			}
			authorities[key] += row.Count
		}
	}

	authorityKeys := make([]warmCapacityAuthorityMetricKey, 0, len(authorities))
	for key := range authorities {
		authorityKeys = append(authorityKeys, key)
	}
	sort.Slice(authorityKeys, func(left, right int) bool {
		leftKey := authorityKeys[left]
		rightKey := authorityKeys[right]
		if leftKey.CapacityClass != rightKey.CapacityClass {
			return leftKey.CapacityClass < rightKey.CapacityClass
		}
		if leftKey.Freshness != rightKey.Freshness {
			return leftKey.Freshness < rightKey.Freshness
		}
		return !leftKey.WarmSupported && rightKey.WarmSupported
	})
	writeHelp(
		output,
		"synara_worker_pool_warm_capacity_authorities",
		"Authoritative Worker Pool warm-capacity observations by bounded capacity class, freshness, and warm-support state.",
		"gauge",
	)
	for _, key := range authorityKeys {
		fmt.Fprintf(
			output,
			"synara_worker_pool_warm_capacity_authorities%s %d\n",
			labels(map[string]string{
				"capacity_class": key.CapacityClass,
				"freshness":      key.Freshness,
				"warm_supported": strconv.FormatBool(key.WarmSupported),
			}),
			authorities[key],
		)
	}

	var unitRows []warmCapacityUnitGroup
	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
		Select(`capacity_class,
			SUM(desired_total_units) AS desired_total_units,
			SUM(claimed_units) AS claimed_units,
			SUM(ready_idle_units) AS ready_idle_units`).
		Where("expires_at > ?", now).
		Group("capacity_class").
		Scan(&unitRows).Error; err != nil {
		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
	}
	type unitTotals struct {
		desiredTotal int64
		claimed      int64
		readyIdle    int64
	}
	units := make(map[string]unitTotals)
	for _, row := range unitRows {
		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
		totals := units[capacityClass]
		totals.desiredTotal += row.DesiredTotalUnits
		totals.claimed += row.ClaimedUnits
		totals.readyIdle += row.ReadyIdleUnits
		units[capacityClass] = totals
	}
	capacityClasses := make([]string, 0, len(units))
	for capacityClass := range units {
		capacityClasses = append(capacityClasses, capacityClass)
	}
	sort.Strings(capacityClasses)
	writeHelp(
		output,
		"synara_worker_pool_warm_capacity_units",
		"Fresh authoritative Worker Pool warm-capacity units by bounded capacity class and counter kind.",
		"gauge",
	)
	for _, capacityClass := range capacityClasses {
		totals := units[capacityClass]
		for _, sample := range []struct {
			kind  string
			value int64
		}{
			{kind: "desired_total", value: totals.desiredTotal},
			{kind: "claimed", value: totals.claimed},
			{kind: "ready_idle", value: totals.readyIdle},
		} {
			fmt.Fprintf(
				output,
				"synara_worker_pool_warm_capacity_units%s %d\n",
				labels(map[string]string{"capacity_class": capacityClass, "kind": sample.kind}),
				sample.value,
			)
		}
	}
	return nil
}

func boundedWarmCapacityClass(value string) string {
	switch strings.ToLower(strings.TrimSpace(value)) {
	case "standard":
		return "standard"
	case "interactive":
		return "interactive"
	default:
		return "other"
	}
}

func boundedReconcilerLeaseName(value string) string {
	switch value {
	case "synara:docker-worker-pool-reconciler":
		return "docker"
	case "synara:kubernetes-execution-reconciler":
		return "kubernetes"
	case "synara:global-target-failover-sweep":
		return "target-failover"
	case "synara:session-resource-lifecycle":
		return "resource-lifecycle"
	case "synara:worker-release-auto-rollback":
		return "worker-release-auto-rollback"
	case "synara:tenant-retention-sweeper":
		return "retention"
	case "synara:metric-rollup":
		return "metric-rollup"
	case "synara:billing-import-scheduler":
		return "billing-import"
	case "synara:billing-shared-allocation-scheduler":
		return "billing-shared-allocation"
	default:
		return "other"
	}
}

func sortAmounts(rows []labeledAmount) {
	sort.Slice(rows, func(left, right int) bool {
		leftKey := strings.Join([]string{
			rows[left].Provider, rows[left].CurrencyCode, rows[left].ChargeKind, rows[left].ReconciliationState,
		}, "\x00")
		rightKey := strings.Join([]string{
			rows[right].Provider, rows[right].CurrencyCode, rows[right].ChargeKind, rows[right].ReconciliationState,
		}, "\x00")
		return leftKey < rightKey
	})
}
29-   bypasses the execution quota.
30-5. Workspace materialization, checkpoint, Provider credential, Memory, and Recovery Bundle authority remain scoped
31-   to the Execution/Session. An idle warm Worker has none of those bindings.
32-
33:## Worker Pools
34-
35-Each Pool has a stable UUID, Target, name, mode, capacity class, target-local location, capacity bounds, scheduling
36-template, status, and optimistic version.
37-
--
63-  - `balanced`: balanced, then default.
64-  - `low-latency`: low-latency, then balanced, then default.
65-
66-The ordered list is also the cold-fallback order. Placement remembers the first active candidate, then scans active
67:warm candidates in that same order for a fresh `worker_pool_warm_capacity` observation with
68-`warmSupported=true` and `readyIdleUnits>0`. The first such candidate is preferred. If none exists, placement returns
69-the remembered first active candidate unchanged, so missing, expired, unsupported, or zero-capacity observations do
70-not turn a valid cold launch into an admission failure. The observation is a soft hint and is never decremented or
71-reserved by placement.
--
141-Kubernetes DELETE records only `draining`; kubelet `Succeeded`/`Failed`, or a later successful target Pod list that
142-confirms the exact UID is missing, records physical termination. Deletion-pending Pods continue to count against capacity
143-during that reconcile cycle.
144-
145:## Live warm-capacity authority
146-
147-Target-wide Pod admission and warm reuse are different authorities. `execution_target_health` remains the hard,
148-expiring Target-wide ceiling/occupancy observation and includes execution Pods, idle warm Pods, claimed warm Pods, and
149:deletion-pending Pods. `worker_pool_warm_capacity` reports only the release-aware warm capacity that the Kubernetes
150-Reconciler has proved reusable now.
151-
152-Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
153-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
--
185-dispatch-to-Provider-ready P50/P95/P99 gauges. Worker-incarnation gauges expose retained runtime, active/idle seconds,
186-and requested CPU/memory/ephemeral-storage seconds. They deliberately contain no Tenant, Execution, Worker, or Pod ID
187-labels.
188-
189:The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
190:`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
191-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
192-Tenant/Target/Pool identifier is exposed as a label.
193-
194-Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and

exec
/bin/zsh -lc "sed -n '1,220p' services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
rg -n -C 8 'func \\(.*Observe|DesiredIdleUnits|MaxActiveUnits|ReadyIdleUnits' services/control-plane/internal/warmcapacity" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
package executiontargets

import (
	"context"
	"strings"
	"time"

	"github.com/google/uuid"

	"github.com/synara-ai/synara/services/control-plane/internal/warmcapacity"
)

const managedKubernetesWarmCapacityPublisherPrefix = "managed-kubernetes-warm-capacity-publisher:"

type ManagedKubernetesWarmCapacityObservation struct {
	TenantID                uuid.UUID
	ExecutionTargetID       uuid.UUID
	WorkerPoolID            uuid.UUID
	WorkerPoolVersion       int64
	WarmSupported           bool
	WorkerReleaseRevisionID *uuid.UUID
	WorkerReleaseChannel    *string
	DesiredTotalUnits       int
	ClaimedUnits            int
	ReadyIdleUnits          int
	Reason                  *string
	ObservedAt              time.Time
}

type ManagedKubernetesWarmCapacityObserver func(context.Context, ManagedKubernetesWarmCapacityObservation) error

type ManagedKubernetesWarmCapacityPublisherConfig struct {
	PublisherIdentity string
	ObservationTTL    time.Duration
}

type ManagedKubernetesWarmCapacityPublisher struct {
	warmCapacity      *warmcapacity.Service
	publisherIdentity string
	observationTTL    time.Duration
	now               func() time.Time
}

func NewManagedKubernetesWarmCapacityPublisher(
	targets *Service,
	config ManagedKubernetesWarmCapacityPublisherConfig,
) *ManagedKubernetesWarmCapacityPublisher {
	ttl := config.ObservationTTL
	if ttl <= 0 {
		ttl = 90 * time.Second
	}
	return &ManagedKubernetesWarmCapacityPublisher{
		warmCapacity:      warmcapacity.NewService(targets.db),
		publisherIdentity: normalizeManagedKubernetesWarmCapacityPublisherIdentity(config.PublisherIdentity),
		observationTTL:    ttl,
		now:               func() time.Time { return time.Now().UTC() },
	}
}

func (p *ManagedKubernetesWarmCapacityPublisher) PublishReconcile(
	ctx context.Context,
	observation ManagedKubernetesWarmCapacityObservation,
) error {
	observedAt := observation.ObservedAt.UTC()
	if observedAt.IsZero() {
		observedAt = p.now()
	}
	_, err := p.warmCapacity.Observe(ctx, warmcapacity.Observation{
		TenantID:                observation.TenantID,
		ExecutionTargetID:       observation.ExecutionTargetID,
		WorkerPoolID:            observation.WorkerPoolID,
		WorkerPoolVersion:       observation.WorkerPoolVersion,
		WarmSupported:           observation.WarmSupported,
		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
		DesiredTotalUnits:       observation.DesiredTotalUnits,
		ClaimedUnits:            observation.ClaimedUnits,
		ReadyIdleUnits:          observation.ReadyIdleUnits,
		Source:                  p.publisherIdentity,
		Reason:                  observation.Reason,
		ObservedAt:              observedAt,
		TTL:                     p.observationTTL,
	})
	return err
}

func normalizeManagedKubernetesWarmCapacityPublisherIdentity(identity string) string {
	identity = strings.TrimSpace(identity)
	if identity == "" {
		identity = managedKubernetesWarmCapacityPublisherPrefix + "default"
	}
	if len(identity) > 160 {
		identity = identity[:160]
	}
	return identity
}
services/control-plane/internal/warmcapacity/service_test.go-24-		ExecutionTargetID:       fixture.target.ID,
services/control-plane/internal/warmcapacity/service_test.go-25-		WorkerPoolID:            fixture.pool.ID,
services/control-plane/internal/warmcapacity/service_test.go-26-		WorkerPoolVersion:       fixture.pool.Version,
services/control-plane/internal/warmcapacity/service_test.go-27-		WarmSupported:           true,
services/control-plane/internal/warmcapacity/service_test.go-28-		WorkerReleaseRevisionID: &fixture.release.ID,
services/control-plane/internal/warmcapacity/service_test.go-29-		WorkerReleaseChannel:    &channel,
services/control-plane/internal/warmcapacity/service_test.go-30-		DesiredTotalUnits:       3,
services/control-plane/internal/warmcapacity/service_test.go-31-		ClaimedUnits:            1,
services/control-plane/internal/warmcapacity/service_test.go:32:		ReadyIdleUnits:          2,
services/control-plane/internal/warmcapacity/service_test.go-33-		Source:                  "reconciler/orbstack",
services/control-plane/internal/warmcapacity/service_test.go-34-		Reason:                  stringPointer("healthy"),
services/control-plane/internal/warmcapacity/service_test.go-35-		ObservedAt:              observedAt,
services/control-plane/internal/warmcapacity/service_test.go-36-		TTL:                     time.Minute,
services/control-plane/internal/warmcapacity/service_test.go-37-	})
services/control-plane/internal/warmcapacity/service_test.go-38-	if err != nil {
services/control-plane/internal/warmcapacity/service_test.go-39-		t.Fatal(err)
services/control-plane/internal/warmcapacity/service_test.go-40-	}
services/control-plane/internal/warmcapacity/service_test.go-41-	if observed.CapacityClass != fixture.pool.CapacityClass ||
services/control-plane/internal/warmcapacity/service_test.go:42:		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
services/control-plane/internal/warmcapacity/service_test.go:43:		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
services/control-plane/internal/warmcapacity/service_test.go-44-		observed.Version != 1 ||
services/control-plane/internal/warmcapacity/service_test.go-45-		observed.WorkerReleaseRevisionID == nil ||
services/control-plane/internal/warmcapacity/service_test.go-46-		*observed.WorkerReleaseRevisionID != fixture.release.ID ||
services/control-plane/internal/warmcapacity/service_test.go-47-		observed.WorkerReleaseChannel == nil ||
services/control-plane/internal/warmcapacity/service_test.go-48-		*observed.WorkerReleaseChannel != releaseChannelPromoted {
services/control-plane/internal/warmcapacity/service_test.go-49-		t.Fatalf("observed warm capacity = %#v", observed)
services/control-plane/internal/warmcapacity/service_test.go-50-	}
services/control-plane/internal/warmcapacity/service_test.go-51-
--
services/control-plane/internal/warmcapacity/service_test.go-56-		WorkerPoolVersion:       fixture.pool.Version,
services/control-plane/internal/warmcapacity/service_test.go-57-		WorkerReleaseRevisionID: &fixture.release.ID,
services/control-plane/internal/warmcapacity/service_test.go-58-		WorkerReleaseChannel:    &channel,
services/control-plane/internal/warmcapacity/service_test.go-59-		Now:                     observedAt.Add(30 * time.Second),
services/control-plane/internal/warmcapacity/service_test.go-60-	})
services/control-plane/internal/warmcapacity/service_test.go-61-	if err != nil {
services/control-plane/internal/warmcapacity/service_test.go-62-		t.Fatal(err)
services/control-plane/internal/warmcapacity/service_test.go-63-	}
services/control-plane/internal/warmcapacity/service_test.go:64:	if fresh == nil || fresh.ReadyIdleUnits != 2 || fresh.ClaimedUnits != 1 {
services/control-plane/internal/warmcapacity/service_test.go-65-		t.Fatalf("fresh warm capacity = %#v", fresh)
services/control-plane/internal/warmcapacity/service_test.go-66-	}
services/control-plane/internal/warmcapacity/service_test.go-67-
services/control-plane/internal/warmcapacity/service_test.go-68-	secondObservedAt := observedAt.Add(time.Second)
services/control-plane/internal/warmcapacity/service_test.go-69-	advanced, err := fixture.service.Observe(context.Background(), Observation{
services/control-plane/internal/warmcapacity/service_test.go-70-		TenantID:                fixture.tenantID,
services/control-plane/internal/warmcapacity/service_test.go-71-		ExecutionTargetID:       fixture.target.ID,
services/control-plane/internal/warmcapacity/service_test.go-72-		WorkerPoolID:            fixture.pool.ID,
services/control-plane/internal/warmcapacity/service_test.go-73-		WorkerPoolVersion:       fixture.pool.Version,
services/control-plane/internal/warmcapacity/service_test.go-74-		WarmSupported:           true,
services/control-plane/internal/warmcapacity/service_test.go-75-		WorkerReleaseRevisionID: &fixture.release.ID,
services/control-plane/internal/warmcapacity/service_test.go-76-		WorkerReleaseChannel:    &channel,
services/control-plane/internal/warmcapacity/service_test.go-77-		DesiredTotalUnits:       4,
services/control-plane/internal/warmcapacity/service_test.go-78-		ClaimedUnits:            2,
services/control-plane/internal/warmcapacity/service_test.go:79:		ReadyIdleUnits:          1,
services/control-plane/internal/warmcapacity/service_test.go-80-		Source:                  "reconciler/orbstack",
services/control-plane/internal/warmcapacity/service_test.go-81-		ObservedAt:              secondObservedAt,
services/control-plane/internal/warmcapacity/service_test.go-82-		TTL:                     2 * time.Minute,
services/control-plane/internal/warmcapacity/service_test.go-83-	})
services/control-plane/internal/warmcapacity/service_test.go-84-	if err != nil {
services/control-plane/internal/warmcapacity/service_test.go-85-		t.Fatal(err)
services/control-plane/internal/warmcapacity/service_test.go-86-	}
services/control-plane/internal/warmcapacity/service_test.go-87-	if advanced.Version != 2 || advanced.ObservedAt != secondObservedAt || advanced.DesiredTotalUnits != 4 {
--
services/control-plane/internal/warmcapacity/service_test.go-110-	base := Observation{
services/control-plane/internal/warmcapacity/service_test.go-111-		TenantID:          fixture.tenantID,
services/control-plane/internal/warmcapacity/service_test.go-112-		ExecutionTargetID: fixture.target.ID,
services/control-plane/internal/warmcapacity/service_test.go-113-		WorkerPoolID:      fixture.pool.ID,
services/control-plane/internal/warmcapacity/service_test.go-114-		WorkerPoolVersion: fixture.pool.Version,
services/control-plane/internal/warmcapacity/service_test.go-115-		WarmSupported:     true,
services/control-plane/internal/warmcapacity/service_test.go-116-		DesiredTotalUnits: 2,
services/control-plane/internal/warmcapacity/service_test.go-117-		ClaimedUnits:      1,
services/control-plane/internal/warmcapacity/service_test.go:118:		ReadyIdleUnits:    1,
services/control-plane/internal/warmcapacity/service_test.go-119-		Source:            "reconciler/test",
services/control-plane/internal/warmcapacity/service_test.go-120-		ObservedAt:        fixture.now,
services/control-plane/internal/warmcapacity/service_test.go-121-		TTL:               time.Minute,
services/control-plane/internal/warmcapacity/service_test.go-122-	}
services/control-plane/internal/warmcapacity/service_test.go-123-	nonWarmPool := fixture.pool
services/control-plane/internal/warmcapacity/service_test.go-124-	nonWarmPool.ID = uuid.New()
services/control-plane/internal/warmcapacity/service_test.go-125-	nonWarmPool.Name = "resident-pool"
services/control-plane/internal/warmcapacity/service_test.go-126-	nonWarmPool.Mode = "resident"
--
services/control-plane/internal/warmcapacity/service_test.go-149-			},
services/control-plane/internal/warmcapacity/service_test.go-150-			code: "warm_capacity_pool_mode_invalid",
services/control-plane/internal/warmcapacity/service_test.go-151-		},
services/control-plane/internal/warmcapacity/service_test.go-152-		{
services/control-plane/internal/warmcapacity/service_test.go-153-			name: "unsupported counters rejected",
services/control-plane/internal/warmcapacity/service_test.go-154-			mut: func(input *Observation) {
services/control-plane/internal/warmcapacity/service_test.go-155-				input.WarmSupported = false
services/control-plane/internal/warmcapacity/service_test.go-156-				input.DesiredTotalUnits = 1
services/control-plane/internal/warmcapacity/service_test.go:157:				input.ReadyIdleUnits = 1
services/control-plane/internal/warmcapacity/service_test.go-158-				input.ClaimedUnits = 3
services/control-plane/internal/warmcapacity/service_test.go-159-			},
services/control-plane/internal/warmcapacity/service_test.go-160-			code: "invalid_warm_capacity_counters",
services/control-plane/internal/warmcapacity/service_test.go-161-		},
services/control-plane/internal/warmcapacity/service_test.go-162-		{
services/control-plane/internal/warmcapacity/service_test.go-163-			name: "desired total over pool max rejected",
services/control-plane/internal/warmcapacity/service_test.go-164-			mut: func(input *Observation) {
services/control-plane/internal/warmcapacity/service_test.go:165:				input.DesiredTotalUnits = fixture.pool.MaxActiveUnits + 1
services/control-plane/internal/warmcapacity/service_test.go:166:				input.ClaimedUnits = fixture.pool.MaxActiveUnits + 3
services/control-plane/internal/warmcapacity/service_test.go:167:				input.ReadyIdleUnits = 0
services/control-plane/internal/warmcapacity/service_test.go-168-			},
services/control-plane/internal/warmcapacity/service_test.go-169-			code: "invalid_warm_capacity_counters",
services/control-plane/internal/warmcapacity/service_test.go-170-		},
services/control-plane/internal/warmcapacity/service_test.go-171-		{
services/control-plane/internal/warmcapacity/service_test.go-172-			name: "release pair shape rejected",
services/control-plane/internal/warmcapacity/service_test.go-173-			mut: func(input *Observation) {
services/control-plane/internal/warmcapacity/service_test.go-174-				channel := releaseChannelCanary
services/control-plane/internal/warmcapacity/service_test.go-175-				input.WorkerReleaseChannel = &channel
--
services/control-plane/internal/warmcapacity/service_test.go-203-		ExecutionTargetID:       fixture.target.ID,
services/control-plane/internal/warmcapacity/service_test.go-204-		WorkerPoolID:            fixture.pool.ID,
services/control-plane/internal/warmcapacity/service_test.go-205-		WorkerPoolVersion:       fixture.pool.Version,
services/control-plane/internal/warmcapacity/service_test.go-206-		WarmSupported:           true,
services/control-plane/internal/warmcapacity/service_test.go-207-		WorkerReleaseRevisionID: &fixture.release.ID,
services/control-plane/internal/warmcapacity/service_test.go-208-		WorkerReleaseChannel:    &channel,
services/control-plane/internal/warmcapacity/service_test.go-209-		DesiredTotalUnits:       2,
services/control-plane/internal/warmcapacity/service_test.go-210-		ClaimedUnits:            1,
services/control-plane/internal/warmcapacity/service_test.go:211:		ReadyIdleUnits:          1,
services/control-plane/internal/warmcapacity/service_test.go-212-		Source:                  "reconciler/test",
services/control-plane/internal/warmcapacity/service_test.go-213-		ObservedAt:              fixture.now,
services/control-plane/internal/warmcapacity/service_test.go-214-		TTL:                     20 * time.Second,
services/control-plane/internal/warmcapacity/service_test.go-215-	}); err != nil {
services/control-plane/internal/warmcapacity/service_test.go-216-		t.Fatal(err)
services/control-plane/internal/warmcapacity/service_test.go-217-	}
services/control-plane/internal/warmcapacity/service_test.go-218-
services/control-plane/internal/warmcapacity/service_test.go-219-	missing, err := fixture.service.GetFresh(context.Background(), FreshLookup{
--
services/control-plane/internal/warmcapacity/service_test.go-312-		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
services/control-plane/internal/warmcapacity/service_test.go-313-	}
services/control-plane/internal/warmcapacity/service_test.go-314-	if err := db.Create(&target).Error; err != nil {
services/control-plane/internal/warmcapacity/service_test.go-315-		t.Fatal(err)
services/control-plane/internal/warmcapacity/service_test.go-316-	}
services/control-plane/internal/warmcapacity/service_test.go-317-	pool := persistence.WorkerPool{
services/control-plane/internal/warmcapacity/service_test.go-318-		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
services/control-plane/internal/warmcapacity/service_test.go-319-		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
services/control-plane/internal/warmcapacity/service_test.go:320:		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
services/control-plane/internal/warmcapacity/service_test.go-321-		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
services/control-plane/internal/warmcapacity/service_test.go-322-	}
services/control-plane/internal/warmcapacity/service_test.go-323-	if err := db.Create(&pool).Error; err != nil {
services/control-plane/internal/warmcapacity/service_test.go-324-		t.Fatal(err)
services/control-plane/internal/warmcapacity/service_test.go-325-	}
services/control-plane/internal/warmcapacity/service_test.go-326-	manifest := persistence.WorkerManifest{
services/control-plane/internal/warmcapacity/service_test.go-327-		ID:                    manifestID,
services/control-plane/internal/warmcapacity/service_test.go-328-		ManifestHash:          uuid.NewString(),
--
services/control-plane/internal/warmcapacity/service.go-32-	ExecutionTargetID       uuid.UUID
services/control-plane/internal/warmcapacity/service.go-33-	WorkerPoolID            uuid.UUID
services/control-plane/internal/warmcapacity/service.go-34-	WorkerPoolVersion       int64
services/control-plane/internal/warmcapacity/service.go-35-	WarmSupported           bool
services/control-plane/internal/warmcapacity/service.go-36-	WorkerReleaseRevisionID *uuid.UUID
services/control-plane/internal/warmcapacity/service.go-37-	WorkerReleaseChannel    *string
services/control-plane/internal/warmcapacity/service.go-38-	DesiredTotalUnits       int
services/control-plane/internal/warmcapacity/service.go-39-	ClaimedUnits            int
services/control-plane/internal/warmcapacity/service.go:40:	ReadyIdleUnits          int
services/control-plane/internal/warmcapacity/service.go-41-	Source                  string
services/control-plane/internal/warmcapacity/service.go-42-	Reason                  *string
services/control-plane/internal/warmcapacity/service.go-43-	ObservedAt              time.Time
services/control-plane/internal/warmcapacity/service.go-44-	TTL                     time.Duration
services/control-plane/internal/warmcapacity/service.go-45-}
services/control-plane/internal/warmcapacity/service.go-46-
services/control-plane/internal/warmcapacity/service.go-47-type FreshLookup struct {
services/control-plane/internal/warmcapacity/service.go-48-	TenantID                uuid.UUID
--
services/control-plane/internal/warmcapacity/service.go-61-
services/control-plane/internal/warmcapacity/service.go-62-func NewService(db *gorm.DB) *Service {
services/control-plane/internal/warmcapacity/service.go-63-	return &Service{
services/control-plane/internal/warmcapacity/service.go-64-		db:  db,
services/control-plane/internal/warmcapacity/service.go-65-		now: func() time.Time { return time.Now().UTC() },
services/control-plane/internal/warmcapacity/service.go-66-	}
services/control-plane/internal/warmcapacity/service.go-67-}
services/control-plane/internal/warmcapacity/service.go-68-
services/control-plane/internal/warmcapacity/service.go:69:func (s *Service) Observe(ctx context.Context, input Observation) (persistence.WorkerPoolWarmCapacity, error) {
services/control-plane/internal/warmcapacity/service.go-70-	if input.TenantID == uuid.Nil || input.ExecutionTargetID == uuid.Nil || input.WorkerPoolID == uuid.Nil || input.WorkerPoolVersion <= 0 {
services/control-plane/internal/warmcapacity/service.go-71-		return persistence.WorkerPoolWarmCapacity{}, problem.New(400, "invalid_warm_capacity_scope", "Tenant, Execution Target, Worker Pool, and positive Worker Pool version are required.")
services/control-plane/internal/warmcapacity/service.go-72-	}
services/control-plane/internal/warmcapacity/service.go-73-	if input.TTL < minObservationTTL || input.TTL > maxObservationTTL {
services/control-plane/internal/warmcapacity/service.go-74-		return persistence.WorkerPoolWarmCapacity{}, problem.New(400, "invalid_warm_capacity_ttl", "Warm capacity TTL must be between 10 seconds and 1 hour.")
services/control-plane/internal/warmcapacity/service.go-75-	}
services/control-plane/internal/warmcapacity/service.go-76-	source := strings.TrimSpace(input.Source)
services/control-plane/internal/warmcapacity/service.go-77-	if source == "" || len(source) > 160 {
--
services/control-plane/internal/warmcapacity/service.go-80-	reason, err := normalizeReason(input.Reason)
services/control-plane/internal/warmcapacity/service.go-81-	if err != nil {
services/control-plane/internal/warmcapacity/service.go-82-		return persistence.WorkerPoolWarmCapacity{}, err
services/control-plane/internal/warmcapacity/service.go-83-	}
services/control-plane/internal/warmcapacity/service.go-84-	releaseRevisionID, releaseChannel, err := normalizeReleasePair(input.WorkerReleaseRevisionID, input.WorkerReleaseChannel)
services/control-plane/internal/warmcapacity/service.go-85-	if err != nil {
services/control-plane/internal/warmcapacity/service.go-86-		return persistence.WorkerPoolWarmCapacity{}, err
services/control-plane/internal/warmcapacity/service.go-87-	}
services/control-plane/internal/warmcapacity/service.go:88:	if err := validateCounters(input.WarmSupported, input.DesiredTotalUnits, input.ClaimedUnits, input.ReadyIdleUnits); err != nil {
services/control-plane/internal/warmcapacity/service.go-89-		return persistence.WorkerPoolWarmCapacity{}, err
services/control-plane/internal/warmcapacity/service.go-90-	}
services/control-plane/internal/warmcapacity/service.go-91-	observedAt := input.ObservedAt.UTC()
services/control-plane/internal/warmcapacity/service.go-92-	if observedAt.IsZero() {
services/control-plane/internal/warmcapacity/service.go-93-		observedAt = s.now()
services/control-plane/internal/warmcapacity/service.go-94-	}
services/control-plane/internal/warmcapacity/service.go-95-
services/control-plane/internal/warmcapacity/service.go-96-	var result persistence.WorkerPoolWarmCapacity
--
services/control-plane/internal/warmcapacity/service.go-106-			return problem.New(409, "warm_capacity_pool_mode_invalid", "Warm capacity authority only applies to Worker Pools in warm mode.")
services/control-plane/internal/warmcapacity/service.go-107-		}
services/control-plane/internal/warmcapacity/service.go-108-		if pool.Status == poolStatusDisabled {
services/control-plane/internal/warmcapacity/service.go-109-			return problem.New(409, "warm_capacity_pool_disabled", "Warm capacity authority cannot be written for a disabled Worker Pool.")
services/control-plane/internal/warmcapacity/service.go-110-		}
services/control-plane/internal/warmcapacity/service.go-111-		if pool.Version != input.WorkerPoolVersion {
services/control-plane/internal/warmcapacity/service.go-112-			return problem.New(409, "warm_capacity_pool_version_stale", "Warm capacity authority must reference the current Worker Pool version.")
services/control-plane/internal/warmcapacity/service.go-113-		}
services/control-plane/internal/warmcapacity/service.go:114:		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
services/control-plane/internal/warmcapacity/service.go-115-			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
services/control-plane/internal/warmcapacity/service.go-116-		}
services/control-plane/internal/warmcapacity/service.go:117:		if input.DesiredTotalUnits > pool.MaxActiveUnits {
services/control-plane/internal/warmcapacity/service.go-118-			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
services/control-plane/internal/warmcapacity/service.go-119-		}
services/control-plane/internal/warmcapacity/service.go-120-		if releaseRevisionID != nil {
services/control-plane/internal/warmcapacity/service.go-121-			var revision persistence.WorkerReleaseRevision
services/control-plane/internal/warmcapacity/service.go-122-			if err := tx.WithContext(ctx).
services/control-plane/internal/warmcapacity/service.go-123-				Where("id = ? AND tenant_id = ? AND execution_target_id = ?", *releaseRevisionID, input.TenantID, input.ExecutionTargetID).
services/control-plane/internal/warmcapacity/service.go-124-				Take(&revision).Error; err != nil {
services/control-plane/internal/warmcapacity/service.go-125-				if errors.Is(err, gorm.ErrRecordNotFound) {
--
services/control-plane/internal/warmcapacity/service.go-140-				WorkerPoolID:            input.WorkerPoolID,
services/control-plane/internal/warmcapacity/service.go-141-				WorkerPoolVersion:       input.WorkerPoolVersion,
services/control-plane/internal/warmcapacity/service.go-142-				TenantID:                input.TenantID,
services/control-plane/internal/warmcapacity/service.go-143-				ExecutionTargetID:       input.ExecutionTargetID,
services/control-plane/internal/warmcapacity/service.go-144-				CapacityClass:           pool.CapacityClass,
services/control-plane/internal/warmcapacity/service.go-145-				WarmSupported:           input.WarmSupported,
services/control-plane/internal/warmcapacity/service.go-146-				WorkerReleaseRevisionID: releaseRevisionID,
services/control-plane/internal/warmcapacity/service.go-147-				WorkerReleaseChannel:    releaseChannel,
services/control-plane/internal/warmcapacity/service.go:148:				DesiredIdleUnits:        pool.DesiredIdleUnits,
services/control-plane/internal/warmcapacity/service.go:149:				MaxActiveUnits:          pool.MaxActiveUnits,
services/control-plane/internal/warmcapacity/service.go-150-				DesiredTotalUnits:       input.DesiredTotalUnits,
services/control-plane/internal/warmcapacity/service.go-151-				ClaimedUnits:            input.ClaimedUnits,
services/control-plane/internal/warmcapacity/service.go:152:				ReadyIdleUnits:          input.ReadyIdleUnits,
services/control-plane/internal/warmcapacity/service.go-153-				Source:                  source,
services/control-plane/internal/warmcapacity/service.go-154-				Reason:                  reason,
services/control-plane/internal/warmcapacity/service.go-155-				ObservedAt:              observedAt,
services/control-plane/internal/warmcapacity/service.go-156-				ExpiresAt:               observedAt.Add(input.TTL),
services/control-plane/internal/warmcapacity/service.go-157-				Version:                 1,
services/control-plane/internal/warmcapacity/service.go-158-				UpdatedAt:               now,
services/control-plane/internal/warmcapacity/service.go-159-			}
services/control-plane/internal/warmcapacity/service.go-160-			if err := tx.WithContext(ctx).Create(&result).Error; err != nil {
--
services/control-plane/internal/warmcapacity/service.go-168-		if !observedAt.After(current.ObservedAt) {
services/control-plane/internal/warmcapacity/service.go-169-			return problem.New(409, "warm_capacity_observation_stale", "Warm capacity authority must advance observedAt monotonically.")
services/control-plane/internal/warmcapacity/service.go-170-		}
services/control-plane/internal/warmcapacity/service.go-171-		result = current
services/control-plane/internal/warmcapacity/service.go-172-		result.CapacityClass = pool.CapacityClass
services/control-plane/internal/warmcapacity/service.go-173-		result.WarmSupported = input.WarmSupported
services/control-plane/internal/warmcapacity/service.go-174-		result.WorkerReleaseRevisionID = releaseRevisionID
services/control-plane/internal/warmcapacity/service.go-175-		result.WorkerReleaseChannel = releaseChannel
services/control-plane/internal/warmcapacity/service.go:176:		result.DesiredIdleUnits = pool.DesiredIdleUnits
services/control-plane/internal/warmcapacity/service.go:177:		result.MaxActiveUnits = pool.MaxActiveUnits
services/control-plane/internal/warmcapacity/service.go-178-		result.DesiredTotalUnits = input.DesiredTotalUnits
services/control-plane/internal/warmcapacity/service.go-179-		result.ClaimedUnits = input.ClaimedUnits
services/control-plane/internal/warmcapacity/service.go:180:		result.ReadyIdleUnits = input.ReadyIdleUnits
services/control-plane/internal/warmcapacity/service.go-181-		result.Source = source
services/control-plane/internal/warmcapacity/service.go-182-		result.Reason = reason
services/control-plane/internal/warmcapacity/service.go-183-		result.ObservedAt = observedAt
services/control-plane/internal/warmcapacity/service.go-184-		result.ExpiresAt = observedAt.Add(input.TTL)
services/control-plane/internal/warmcapacity/service.go-185-		result.Version = current.Version + 1
services/control-plane/internal/warmcapacity/service.go-186-		result.UpdatedAt = now
services/control-plane/internal/warmcapacity/service.go-187-
services/control-plane/internal/warmcapacity/service.go-188-		updates := map[string]any{
services/control-plane/internal/warmcapacity/service.go-189-			"capacity_class":             result.CapacityClass,
services/control-plane/internal/warmcapacity/service.go-190-			"warm_supported":             result.WarmSupported,
services/control-plane/internal/warmcapacity/service.go-191-			"worker_release_revision_id": result.WorkerReleaseRevisionID,
services/control-plane/internal/warmcapacity/service.go-192-			"worker_release_channel":     result.WorkerReleaseChannel,
services/control-plane/internal/warmcapacity/service.go:193:			"desired_idle_units":         result.DesiredIdleUnits,
services/control-plane/internal/warmcapacity/service.go:194:			"max_active_units":           result.MaxActiveUnits,
services/control-plane/internal/warmcapacity/service.go-195-			"desired_total_units":        result.DesiredTotalUnits,
services/control-plane/internal/warmcapacity/service.go-196-			"claimed_units":              result.ClaimedUnits,
services/control-plane/internal/warmcapacity/service.go:197:			"ready_idle_units":           result.ReadyIdleUnits,
services/control-plane/internal/warmcapacity/service.go-198-			"source":                     result.Source,
services/control-plane/internal/warmcapacity/service.go-199-			"reason":                     result.Reason,
services/control-plane/internal/warmcapacity/service.go-200-			"observed_at":                result.ObservedAt,
services/control-plane/internal/warmcapacity/service.go-201-			"expires_at":                 result.ExpiresAt,
services/control-plane/internal/warmcapacity/service.go-202-			"version":                    result.Version,
services/control-plane/internal/warmcapacity/service.go-203-			"updated_at":                 result.UpdatedAt,
services/control-plane/internal/warmcapacity/service.go-204-		}
services/control-plane/internal/warmcapacity/service.go-205-		update := tx.WithContext(ctx).Model(&persistence.WorkerPoolWarmCapacity{}).

exec
/bin/zsh -lc "rg -n -C 4 'warm_capacity|desired_idle_units|assert_worker_pool|AutoMigrate|CREATE TRIGGER' services/control-plane/internal/database" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-13-		 ON billing_shared_allocation_schedule_periods (
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-14-		   next_attempt_at, execution_target_id, billing_period_start_at
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-15-		 )`,
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-16-		`DROP TRIGGER IF EXISTS trg_billing_shared_allocation_schedule_periods_insert`,
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go:17:		`CREATE TRIGGER trg_billing_shared_allocation_schedule_periods_insert
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-18-		 BEFORE INSERT ON billing_shared_allocation_schedule_periods
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-19-		 BEGIN
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-20-		   SELECT RAISE(ABORT, 'invalid billing shared allocation schedule period')
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-21-		   WHERE length(NEW.schedule_config_sha256) <> 64
--
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-78-		        )
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-79-		      );
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-80-		 END`,
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-81-		`DROP TRIGGER IF EXISTS trg_billing_shared_allocation_schedule_periods_update`,
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go:82:		`CREATE TRIGGER trg_billing_shared_allocation_schedule_periods_update
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-83-		 BEFORE UPDATE ON billing_shared_allocation_schedule_periods
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-84-		 BEGIN
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-85-		   SELECT RAISE(ABORT, 'billing shared allocation schedule period identity is immutable')
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-86-		   WHERE NEW.schedule_config_sha256 IS NOT OLD.schedule_config_sha256
--
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-143-		       OR NEW.last_success_at IS NOT OLD.last_success_at
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-144-		     );
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-145-		 END`,
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-146-		`DROP TRIGGER IF EXISTS trg_billing_shared_allocation_schedule_periods_delete`,
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go:147:		`CREATE TRIGGER trg_billing_shared_allocation_schedule_periods_delete
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-148-		 BEFORE DELETE ON billing_shared_allocation_schedule_periods
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-149-		 BEGIN
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-150-		   SELECT RAISE(ABORT, 'billing shared allocation schedule periods cannot be deleted');
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-151-		 END`,
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-152-		`DROP TRIGGER IF EXISTS trg_execution_targets_shared_schedule_restrict_delete`,
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go:153:		`CREATE TRIGGER trg_execution_targets_shared_schedule_restrict_delete
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-154-		 BEFORE DELETE ON execution_targets
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-155-		 WHEN EXISTS (
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-156-		   SELECT 1 FROM billing_shared_allocation_schedule_periods
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go-157-		   WHERE execution_target_id = OLD.id
--
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-9-
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-10-func migrateKubernetesPodDeletionFenceSQLiteSafety(ctx context.Context, db *gorm.DB) error {
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-11-	statements := []string{
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-12-		`DROP TRIGGER IF EXISTS trg_kubernetes_pod_deletion_fences_shape_insert`,
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go:13:		`CREATE TRIGGER trg_kubernetes_pod_deletion_fences_shape_insert
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-14-		 BEFORE INSERT ON kubernetes_pod_deletion_fences
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-15-		 WHEN length(trim(ifnull(NEW.namespace, ''))) NOT BETWEEN 1 AND 253
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-16-		   OR length(trim(ifnull(NEW.pod_name, ''))) NOT BETWEEN 1 AND 253
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-17-		   OR length(ifnull(NEW.pod_uid, '')) <> 36
--
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-35-		 BEGIN
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-36-		   SELECT RAISE(ABORT, 'invalid Kubernetes Pod deletion fence');
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-37-		 END`,
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-38-		`DROP TRIGGER IF EXISTS trg_kubernetes_pod_deletion_fences_immutable_update`,
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go:39:		`CREATE TRIGGER trg_kubernetes_pod_deletion_fences_immutable_update
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-40-		 BEFORE UPDATE ON kubernetes_pod_deletion_fences
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-41-		 BEGIN
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-42-		   SELECT RAISE(ABORT, 'Kubernetes Pod deletion fence is immutable');
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-43-		 END`,
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-44-		`DROP TRIGGER IF EXISTS trg_kubernetes_pod_deletion_fences_immutable_delete`,
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go:45:		`CREATE TRIGGER trg_kubernetes_pod_deletion_fences_immutable_delete
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-46-		 BEFORE DELETE ON kubernetes_pod_deletion_fences
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-47-		 WHEN EXISTS (
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-48-		   SELECT 1 FROM execution_targets AS target
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-49-		   WHERE target.id = OLD.execution_target_id
--
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-51-		 BEGIN
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-52-		   SELECT RAISE(ABORT, 'Kubernetes Pod deletion fence is immutable');
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-53-		 END`,
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-54-		`DROP TRIGGER IF EXISTS trg_execution_targets_kubernetes_pod_deletion_fences_cascade`,
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go:55:		`CREATE TRIGGER trg_execution_targets_kubernetes_pod_deletion_fences_cascade
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-56-		 AFTER DELETE ON execution_targets
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-57-		 BEGIN
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-58-		   DELETE FROM kubernetes_pod_deletion_fences
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-59-		   WHERE execution_target_id = OLD.id;
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-60-		 END`,
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-61-		`DROP TRIGGER IF EXISTS trg_worker_instances_pod_deletion_fence_insert`,
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go:62:		`CREATE TRIGGER trg_worker_instances_pod_deletion_fence_insert
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-63-		 BEFORE INSERT ON worker_instances
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-64-		 WHEN NEW.target_kind = 'kubernetes'
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-65-		   AND NEW.registration_trust_mode = 'kubernetes-pod-bound-v1'
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-66-		   AND (
--
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-77-		 BEGIN
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-78-		   SELECT RAISE(ABORT, 'Kubernetes Pod UID is deletion fenced');
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-79-		 END`,
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-80-		`DROP TRIGGER IF EXISTS trg_worker_instances_pod_deletion_fence_update`,
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go:81:		`CREATE TRIGGER trg_worker_instances_pod_deletion_fence_update
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-82-		 BEFORE UPDATE OF execution_target_id, target_kind, cluster_id, namespace, pod_name, instance_uid, registration_trust_mode
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-83-		 ON worker_instances
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-84-		 WHEN NEW.target_kind = 'kubernetes'
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-85-		   AND NEW.registration_trust_mode = 'kubernetes-pod-bound-v1'
--
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-97-		 BEGIN
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-98-		   SELECT RAISE(ABORT, 'Kubernetes Pod UID is deletion fenced');
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-99-		 END`,
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-100-		`DROP TRIGGER IF EXISTS trg_worker_instances_fenced_reactivation`,
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go:101:		`CREATE TRIGGER trg_worker_instances_fenced_reactivation
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-102-		 BEFORE UPDATE OF status, administrative_status ON worker_instances
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-103-		 WHEN NEW.target_kind = 'kubernetes'
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-104-		   AND NEW.registration_trust_mode = 'kubernetes-pod-bound-v1'
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go-105-		   AND (
--
services/control-plane/internal/database/project_git_binding_authority_sqlite_test.go-24-	if err != nil {
services/control-plane/internal/database/project_git_binding_authority_sqlite_test.go-25-		t.Fatal(err)
services/control-plane/internal/database/project_git_binding_authority_sqlite_test.go-26-	}
services/control-plane/internal/database/project_git_binding_authority_sqlite_test.go-27-	t.Cleanup(func() { _ = store.Close() })
services/control-plane/internal/database/project_git_binding_authority_sqlite_test.go:28:	if err := store.DB().WithContext(ctx).AutoMigrate(persistence.AllModels()...); err != nil {
services/control-plane/internal/database/project_git_binding_authority_sqlite_test.go-29-		t.Fatal(err)
services/control-plane/internal/database/project_git_binding_authority_sqlite_test.go-30-	}
services/control-plane/internal/database/project_git_binding_authority_sqlite_test.go-31-	domain, err := bootstrap.Ensure(ctx, store.DB(), platform.ProfilePersonal, "sqlite-project-git-authority-"+uuid.NewString())
services/control-plane/internal/database/project_git_binding_authority_sqlite_test.go-32-	if err != nil {
--
services/control-plane/internal/database/worker_claim_facts_sqlite.go-19-		`CREATE UNIQUE INDEX IF NOT EXISTS uq_worker_claim_facts_cleanup_dispatch
services/control-plane/internal/database/worker_claim_facts_sqlite.go-20-		 ON worker_claim_facts (cleanup_command_id, cleanup_dispatch_generation)
services/control-plane/internal/database/worker_claim_facts_sqlite.go-21-		 WHERE claim_kind = 'workspace-cleanup'`,
services/control-plane/internal/database/worker_claim_facts_sqlite.go-22-		`DROP TRIGGER IF EXISTS trg_worker_claim_facts_insert`,
services/control-plane/internal/database/worker_claim_facts_sqlite.go:23:		`CREATE TRIGGER trg_worker_claim_facts_insert
services/control-plane/internal/database/worker_claim_facts_sqlite.go-24-		 BEFORE INSERT ON worker_claim_facts
services/control-plane/internal/database/worker_claim_facts_sqlite.go-25-		 BEGIN
services/control-plane/internal/database/worker_claim_facts_sqlite.go-26-		   SELECT RAISE(ABORT, 'invalid Worker claim fact')
services/control-plane/internal/database/worker_claim_facts_sqlite.go-27-		   WHERE NEW.worker_incarnation <= 0
--
services/control-plane/internal/database/worker_claim_facts_sqlite.go-101-		        )
services/control-plane/internal/database/worker_claim_facts_sqlite.go-102-		      );
services/control-plane/internal/database/worker_claim_facts_sqlite.go-103-		 END`,
services/control-plane/internal/database/worker_claim_facts_sqlite.go-104-		`DROP TRIGGER IF EXISTS trg_worker_claim_facts_update`,
services/control-plane/internal/database/worker_claim_facts_sqlite.go:105:		`CREATE TRIGGER trg_worker_claim_facts_update
services/control-plane/internal/database/worker_claim_facts_sqlite.go-106-		 BEFORE UPDATE ON worker_claim_facts
services/control-plane/internal/database/worker_claim_facts_sqlite.go-107-		 BEGIN
services/control-plane/internal/database/worker_claim_facts_sqlite.go-108-		   SELECT RAISE(ABORT, 'Worker claim facts are immutable');
services/control-plane/internal/database/worker_claim_facts_sqlite.go-109-		 END`,
services/control-plane/internal/database/worker_claim_facts_sqlite.go-110-		`DROP TRIGGER IF EXISTS trg_worker_claim_facts_delete`,
services/control-plane/internal/database/worker_claim_facts_sqlite.go:111:		`CREATE TRIGGER trg_worker_claim_facts_delete
services/control-plane/internal/database/worker_claim_facts_sqlite.go-112-		 BEFORE DELETE ON worker_claim_facts
services/control-plane/internal/database/worker_claim_facts_sqlite.go-113-		 BEGIN
services/control-plane/internal/database/worker_claim_facts_sqlite.go-114-		   SELECT RAISE(ABORT, 'Worker claim facts are immutable');
services/control-plane/internal/database/worker_claim_facts_sqlite.go-115-		 END`,
services/control-plane/internal/database/worker_claim_facts_sqlite.go-116-		`DROP TRIGGER IF EXISTS trg_worker_claim_facts_restrict_tenant_delete`,
services/control-plane/internal/database/worker_claim_facts_sqlite.go:117:		`CREATE TRIGGER trg_worker_claim_facts_restrict_tenant_delete
services/control-plane/internal/database/worker_claim_facts_sqlite.go-118-		 BEFORE DELETE ON tenants
services/control-plane/internal/database/worker_claim_facts_sqlite.go-119-		 WHEN EXISTS (SELECT 1 FROM worker_claim_facts AS claim WHERE claim.tenant_id = OLD.id)
services/control-plane/internal/database/worker_claim_facts_sqlite.go-120-		 BEGIN
services/control-plane/internal/database/worker_claim_facts_sqlite.go-121-		   SELECT RAISE(ABORT, 'Worker claim fact parent is retained');
services/control-plane/internal/database/worker_claim_facts_sqlite.go-122-		 END`,
services/control-plane/internal/database/worker_claim_facts_sqlite.go-123-		`DROP TRIGGER IF EXISTS trg_worker_claim_facts_restrict_target_delete`,
services/control-plane/internal/database/worker_claim_facts_sqlite.go:124:		`CREATE TRIGGER trg_worker_claim_facts_restrict_target_delete
services/control-plane/internal/database/worker_claim_facts_sqlite.go-125-		 BEFORE DELETE ON execution_targets
services/control-plane/internal/database/worker_claim_facts_sqlite.go-126-		 WHEN EXISTS (SELECT 1 FROM worker_claim_facts AS claim WHERE claim.execution_target_id = OLD.id)
services/control-plane/internal/database/worker_claim_facts_sqlite.go-127-		 BEGIN
services/control-plane/internal/database/worker_claim_facts_sqlite.go-128-		   SELECT RAISE(ABORT, 'Worker claim fact parent is retained');
services/control-plane/internal/database/worker_claim_facts_sqlite.go-129-		 END`,
services/control-plane/internal/database/worker_claim_facts_sqlite.go-130-		`DROP TRIGGER IF EXISTS trg_worker_claim_facts_restrict_worker_fact_delete`,
services/control-plane/internal/database/worker_claim_facts_sqlite.go:131:		`CREATE TRIGGER trg_worker_claim_facts_restrict_worker_fact_delete
services/control-plane/internal/database/worker_claim_facts_sqlite.go-132-		 BEFORE DELETE ON worker_incarnation_facts
services/control-plane/internal/database/worker_claim_facts_sqlite.go-133-		 WHEN EXISTS (
services/control-plane/internal/database/worker_claim_facts_sqlite.go-134-		   SELECT 1 FROM worker_claim_facts AS claim
services/control-plane/internal/database/worker_claim_facts_sqlite.go-135-		   WHERE claim.worker_id = OLD.worker_id AND claim.worker_incarnation = OLD.worker_incarnation
--
services/control-plane/internal/database/worker_claim_facts_sqlite.go-137-		 BEGIN
services/control-plane/internal/database/worker_claim_facts_sqlite.go-138-		   SELECT RAISE(ABORT, 'Worker claim fact parent is retained');
services/control-plane/internal/database/worker_claim_facts_sqlite.go-139-		 END`,
services/control-plane/internal/database/worker_claim_facts_sqlite.go-140-		`DROP TRIGGER IF EXISTS trg_worker_claim_facts_restrict_execution_delete`,
services/control-plane/internal/database/worker_claim_facts_sqlite.go:141:		`CREATE TRIGGER trg_worker_claim_facts_restrict_execution_delete
services/control-plane/internal/database/worker_claim_facts_sqlite.go-142-		 BEFORE DELETE ON agent_executions
services/control-plane/internal/database/worker_claim_facts_sqlite.go-143-		 WHEN EXISTS (SELECT 1 FROM worker_claim_facts AS claim WHERE claim.execution_id = OLD.id)
services/control-plane/internal/database/worker_claim_facts_sqlite.go-144-		 BEGIN
services/control-plane/internal/database/worker_claim_facts_sqlite.go-145-		   SELECT RAISE(ABORT, 'Worker claim fact parent is retained');
services/control-plane/internal/database/worker_claim_facts_sqlite.go-146-		 END`,
services/control-plane/internal/database/worker_claim_facts_sqlite.go-147-		`DROP TRIGGER IF EXISTS trg_worker_claim_facts_restrict_cleanup_delete`,
services/control-plane/internal/database/worker_claim_facts_sqlite.go:148:		`CREATE TRIGGER trg_worker_claim_facts_restrict_cleanup_delete
services/control-plane/internal/database/worker_claim_facts_sqlite.go-149-		 BEFORE DELETE ON workspace_cleanup_commands
services/control-plane/internal/database/worker_claim_facts_sqlite.go-150-		 WHEN EXISTS (SELECT 1 FROM worker_claim_facts AS claim WHERE claim.cleanup_command_id = OLD.id)
services/control-plane/internal/database/worker_claim_facts_sqlite.go-151-		 BEGIN
services/control-plane/internal/database/worker_claim_facts_sqlite.go-152-		   SELECT RAISE(ABORT, 'Worker claim fact parent is retained');
--
services/control-plane/internal/database/capacity_reservation_sqlite_test.go-19-	db, err := gorm.Open(sqlite.Open("file:"+uuid.NewString()+"?mode=memory&cache=shared"), &gorm.Config{TranslateError: true})
services/control-plane/internal/database/capacity_reservation_sqlite_test.go-20-	if err != nil {
services/control-plane/internal/database/capacity_reservation_sqlite_test.go-21-		t.Fatal(err)
services/control-plane/internal/database/capacity_reservation_sqlite_test.go-22-	}
services/control-plane/internal/database/capacity_reservation_sqlite_test.go:23:	if err := db.AutoMigrate(
services/control-plane/internal/database/capacity_reservation_sqlite_test.go-24-		&persistence.ExecutionTarget{},
services/control-plane/internal/database/capacity_reservation_sqlite_test.go-25-		&persistence.AgentExecution{},
services/control-plane/internal/database/capacity_reservation_sqlite_test.go-26-		&persistence.ExecutionTargetHealth{},
services/control-plane/internal/database/capacity_reservation_sqlite_test.go-27-		&persistence.ExecutionTargetReservationAcknowledgement{},
--
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go-9-
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go-10-func migrateCloudCostAccountingSQLiteSafety(ctx context.Context, db *gorm.DB) error {
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go-11-	statements := []string{
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go-12-		`DROP TRIGGER IF EXISTS trg_billing_provider_tariffs_non_overlapping_insert`,
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go:13:		`CREATE TRIGGER trg_billing_provider_tariffs_non_overlapping_insert
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go-14-		 BEFORE INSERT ON billing_provider_tariffs
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go-15-		 BEGIN
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go-16-		   SELECT RAISE(ABORT, 'billing provider tariff effective interval overlaps another tariff')
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go-17-		   WHERE EXISTS (
--
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go-24-		       AND (existing.effective_end_at IS NULL OR existing.effective_end_at > NEW.effective_start_at)
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go-25-		   );
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go-26-		 END`,
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go-27-		`DROP TRIGGER IF EXISTS trg_billing_provider_tariffs_immutable_update`,
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go:28:		`CREATE TRIGGER trg_billing_provider_tariffs_immutable_update
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go-29-		 BEFORE UPDATE ON billing_provider_tariffs
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go-30-		 BEGIN
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go-31-		   SELECT RAISE(ABORT, 'billing provider tariffs are immutable');
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go-32-		 END`,
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go-33-		`DROP TRIGGER IF EXISTS trg_billing_provider_tariffs_immutable_delete`,
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go:34:		`CREATE TRIGGER trg_billing_provider_tariffs_immutable_delete
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go-35-		 BEFORE DELETE ON billing_provider_tariffs
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go-36-		 BEGIN
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go-37-		   SELECT RAISE(ABORT, 'billing provider tariffs are immutable');
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go-38-		 END`,
--
services/control-plane/internal/database/routing_sqlite.go-15-		`UPDATE execution_interactions
services/control-plane/internal/database/routing_sqlite.go-16-		 SET source_execution_id = execution_id
services/control-plane/internal/database/routing_sqlite.go-17-		 WHERE source_execution_id IS NULL OR source_execution_id = ''`,
services/control-plane/internal/database/routing_sqlite.go-18-		`DROP TRIGGER IF EXISTS trg_execution_target_groups_route_insert`,
services/control-plane/internal/database/routing_sqlite.go:19:		`CREATE TRIGGER trg_execution_target_groups_route_insert
services/control-plane/internal/database/routing_sqlite.go-20-		 BEFORE INSERT ON execution_target_groups
services/control-plane/internal/database/routing_sqlite.go-21-		 BEGIN
services/control-plane/internal/database/routing_sqlite.go-22-		   SELECT RAISE(ABORT, 'invalid Execution Target Group')
services/control-plane/internal/database/routing_sqlite.go-23-		   WHERE NEW.version <= 0
--
services/control-plane/internal/database/routing_sqlite.go-29-		      OR NOT json_valid(NEW.preferred_regions)
services/control-plane/internal/database/routing_sqlite.go-30-		      OR json_type(NEW.preferred_regions) <> 'array';
services/control-plane/internal/database/routing_sqlite.go-31-		 END`,
services/control-plane/internal/database/routing_sqlite.go-32-		`DROP TRIGGER IF EXISTS trg_execution_target_groups_route_update`,
services/control-plane/internal/database/routing_sqlite.go:33:		`CREATE TRIGGER trg_execution_target_groups_route_update
services/control-plane/internal/database/routing_sqlite.go-34-		 BEFORE UPDATE ON execution_target_groups
services/control-plane/internal/database/routing_sqlite.go-35-		 BEGIN
services/control-plane/internal/database/routing_sqlite.go-36-		   SELECT RAISE(ABORT, 'Execution Target Group ownership is immutable')
services/control-plane/internal/database/routing_sqlite.go-37-		   WHERE NEW.id IS NOT OLD.id OR NEW.tenant_id IS NOT OLD.tenant_id
--
services/control-plane/internal/database/routing_sqlite.go-39-		   SELECT RAISE(ABORT, 'Execution Target Group version must advance exactly once')
services/control-plane/internal/database/routing_sqlite.go-40-		   WHERE NEW.version <> OLD.version + 1;
services/control-plane/internal/database/routing_sqlite.go-41-		 END`,
services/control-plane/internal/database/routing_sqlite.go-42-		`DROP TRIGGER IF EXISTS trg_execution_target_groups_route_delete`,
services/control-plane/internal/database/routing_sqlite.go:43:		`CREATE TRIGGER trg_execution_target_groups_route_delete
services/control-plane/internal/database/routing_sqlite.go-44-		 BEFORE DELETE ON execution_target_groups
services/control-plane/internal/database/routing_sqlite.go-45-		 BEGIN
services/control-plane/internal/database/routing_sqlite.go-46-		   SELECT RAISE(ABORT, 'Execution Target Groups cannot be deleted');
services/control-plane/internal/database/routing_sqlite.go-47-		 END`,
services/control-plane/internal/database/routing_sqlite.go-48-		`DROP TRIGGER IF EXISTS trg_execution_target_group_members_route_insert`,
services/control-plane/internal/database/routing_sqlite.go:49:		`CREATE TRIGGER trg_execution_target_group_members_route_insert
services/control-plane/internal/database/routing_sqlite.go-50-		 BEFORE INSERT ON execution_target_group_members
services/control-plane/internal/database/routing_sqlite.go-51-		 BEGIN
services/control-plane/internal/database/routing_sqlite.go-52-		   SELECT RAISE(ABORT, 'invalid Execution Target Group member')
services/control-plane/internal/database/routing_sqlite.go-53-		   WHERE NEW.version <= 0 OR NEW.priority NOT BETWEEN 0 AND 1000000
--
services/control-plane/internal/database/routing_sqlite.go-65-		            OR target_group.organization_id = target.organization_id)
services/control-plane/internal/database/routing_sqlite.go-66-		      );
services/control-plane/internal/database/routing_sqlite.go-67-		 END`,
services/control-plane/internal/database/routing_sqlite.go-68-		`DROP TRIGGER IF EXISTS trg_execution_target_group_members_route_update`,
services/control-plane/internal/database/routing_sqlite.go:69:		`CREATE TRIGGER trg_execution_target_group_members_route_update
services/control-plane/internal/database/routing_sqlite.go-70-		 BEFORE UPDATE ON execution_target_group_members
services/control-plane/internal/database/routing_sqlite.go-71-		 BEGIN
services/control-plane/internal/database/routing_sqlite.go-72-		   SELECT RAISE(ABORT, 'Execution Target Group member identity is immutable')
services/control-plane/internal/database/routing_sqlite.go-73-		   WHERE NEW.id IS NOT OLD.id OR NEW.tenant_id IS NOT OLD.tenant_id
--
services/control-plane/internal/database/routing_sqlite.go-76-		   SELECT RAISE(ABORT, 'Execution Target Group member version must advance exactly once')
services/control-plane/internal/database/routing_sqlite.go-77-		   WHERE NEW.version <> OLD.version + 1;
services/control-plane/internal/database/routing_sqlite.go-78-		 END`,
services/control-plane/internal/database/routing_sqlite.go-79-		`DROP TRIGGER IF EXISTS trg_execution_target_group_members_route_delete`,
services/control-plane/internal/database/routing_sqlite.go:80:		`CREATE TRIGGER trg_execution_target_group_members_route_delete
services/control-plane/internal/database/routing_sqlite.go-81-		 BEFORE DELETE ON execution_target_group_members
services/control-plane/internal/database/routing_sqlite.go-82-		 BEGIN
services/control-plane/internal/database/routing_sqlite.go-83-		   SELECT RAISE(ABORT, 'Execution Target Group members cannot be deleted');
services/control-plane/internal/database/routing_sqlite.go-84-		 END`,
services/control-plane/internal/database/routing_sqlite.go-85-		`DROP TRIGGER IF EXISTS trg_execution_target_health_route_insert`,
services/control-plane/internal/database/routing_sqlite.go:86:		`CREATE TRIGGER trg_execution_target_health_route_insert
services/control-plane/internal/database/routing_sqlite.go-87-		 BEFORE INSERT ON execution_target_health
services/control-plane/internal/database/routing_sqlite.go-88-		 BEGIN
services/control-plane/internal/database/routing_sqlite.go-89-		   SELECT RAISE(ABORT, 'invalid Execution Target health observation')
services/control-plane/internal/database/routing_sqlite.go-90-		   WHERE NEW.status NOT IN ('healthy', 'degraded', 'unreachable', 'unknown')
--
services/control-plane/internal/database/routing_sqlite.go-95-		      OR length(trim(NEW.source)) NOT BETWEEN 1 AND 160
services/control-plane/internal/database/routing_sqlite.go-96-		      OR (NEW.reason IS NOT NULL AND length(NEW.reason) > 2000);
services/control-plane/internal/database/routing_sqlite.go-97-		 END`,
services/control-plane/internal/database/routing_sqlite.go-98-		`DROP TRIGGER IF EXISTS trg_execution_target_health_route_update`,
services/control-plane/internal/database/routing_sqlite.go:99:		`CREATE TRIGGER trg_execution_target_health_route_update
services/control-plane/internal/database/routing_sqlite.go-100-		 BEFORE UPDATE ON execution_target_health
services/control-plane/internal/database/routing_sqlite.go-101-		 BEGIN
services/control-plane/internal/database/routing_sqlite.go-102-		   SELECT RAISE(ABORT, 'Execution Target health update is stale or not monotonic')
services/control-plane/internal/database/routing_sqlite.go-103-		   WHERE NEW.execution_target_id IS NOT OLD.execution_target_id
services/control-plane/internal/database/routing_sqlite.go-104-		      OR NEW.version <> OLD.version + 1 OR NEW.observed_at <= OLD.observed_at;
services/control-plane/internal/database/routing_sqlite.go-105-		 END`,
services/control-plane/internal/database/routing_sqlite.go-106-		`DROP TRIGGER IF EXISTS trg_execution_target_health_route_delete`,
services/control-plane/internal/database/routing_sqlite.go:107:		`CREATE TRIGGER trg_execution_target_health_route_delete
services/control-plane/internal/database/routing_sqlite.go-108-			 BEFORE DELETE ON execution_target_health
services/control-plane/internal/database/routing_sqlite.go-109-			 BEGIN
services/control-plane/internal/database/routing_sqlite.go-110-			   SELECT RAISE(ABORT, 'Execution Target health observations cannot be deleted');
services/control-plane/internal/database/routing_sqlite.go-111-			 END`,
services/control-plane/internal/database/routing_sqlite.go-112-		`DROP TRIGGER IF EXISTS trg_execution_target_dr_readiness_route_insert`,
services/control-plane/internal/database/routing_sqlite.go:113:		`CREATE TRIGGER trg_execution_target_dr_readiness_route_insert
services/control-plane/internal/database/routing_sqlite.go-114-			 BEFORE INSERT ON execution_target_dr_readiness
services/control-plane/internal/database/routing_sqlite.go-115-			 BEGIN
services/control-plane/internal/database/routing_sqlite.go-116-			   SELECT RAISE(ABORT, 'invalid Execution Target DR readiness observation')
services/control-plane/internal/database/routing_sqlite.go-117-			   WHERE NEW.version <= 0
--
services/control-plane/internal/database/routing_sqlite.go-122-			      OR length(trim(NEW.publisher_identity)) NOT BETWEEN 1 AND 200
services/control-plane/internal/database/routing_sqlite.go-123-			      OR (NEW.reason IS NOT NULL AND length(NEW.reason) > 2000);
services/control-plane/internal/database/routing_sqlite.go-124-			 END`,
services/control-plane/internal/database/routing_sqlite.go-125-		`DROP TRIGGER IF EXISTS trg_execution_target_dr_readiness_route_update`,
services/control-plane/internal/database/routing_sqlite.go:126:		`CREATE TRIGGER trg_execution_target_dr_readiness_route_update
services/control-plane/internal/database/routing_sqlite.go-127-			 BEFORE UPDATE ON execution_target_dr_readiness
services/control-plane/internal/database/routing_sqlite.go-128-			 BEGIN
services/control-plane/internal/database/routing_sqlite.go-129-			   SELECT RAISE(ABORT, 'Execution Target DR readiness identity is immutable')
services/control-plane/internal/database/routing_sqlite.go-130-			   WHERE NEW.execution_target_id IS NOT OLD.execution_target_id
--
services/control-plane/internal/database/routing_sqlite.go-139-			      OR length(trim(NEW.publisher_identity)) NOT BETWEEN 1 AND 200
services/control-plane/internal/database/routing_sqlite.go-140-			      OR (NEW.reason IS NOT NULL AND length(NEW.reason) > 2000);
services/control-plane/internal/database/routing_sqlite.go-141-			 END`,
services/control-plane/internal/database/routing_sqlite.go-142-		`DROP TRIGGER IF EXISTS trg_execution_target_dr_readiness_route_delete`,
services/control-plane/internal/database/routing_sqlite.go:143:		`CREATE TRIGGER trg_execution_target_dr_readiness_route_delete
services/control-plane/internal/database/routing_sqlite.go-144-			 BEFORE DELETE ON execution_target_dr_readiness
services/control-plane/internal/database/routing_sqlite.go-145-			 BEGIN
services/control-plane/internal/database/routing_sqlite.go-146-			   SELECT RAISE(ABORT, 'Execution Target DR readiness observations cannot be deleted');
services/control-plane/internal/database/routing_sqlite.go-147-			 END`,
services/control-plane/internal/database/routing_sqlite.go-148-		`CREATE INDEX IF NOT EXISTS idx_execution_location_outages_route
services/control-plane/internal/database/routing_sqlite.go-149-		 ON execution_location_outages (tenant_id, region, cluster_id, expires_at)`,
services/control-plane/internal/database/routing_sqlite.go-150-		`DROP TRIGGER IF EXISTS trg_execution_location_outages_route_insert`,
services/control-plane/internal/database/routing_sqlite.go:151:		`CREATE TRIGGER trg_execution_location_outages_route_insert
services/control-plane/internal/database/routing_sqlite.go-152-			 BEFORE INSERT ON execution_location_outages
services/control-plane/internal/database/routing_sqlite.go-153-			 BEGIN
services/control-plane/internal/database/routing_sqlite.go-154-			   SELECT RAISE(ABORT, 'invalid location outage observation')
services/control-plane/internal/database/routing_sqlite.go-155-			   WHERE NEW.version <= 0
--
services/control-plane/internal/database/routing_sqlite.go-160-			      OR length(trim(NEW.publisher_identity)) NOT BETWEEN 1 AND 200
services/control-plane/internal/database/routing_sqlite.go-161-			      OR (NEW.reason IS NOT NULL AND length(NEW.reason) > 2000);
services/control-plane/internal/database/routing_sqlite.go-162-			 END`,
services/control-plane/internal/database/routing_sqlite.go-163-		`DROP TRIGGER IF EXISTS trg_execution_location_outages_route_update`,
services/control-plane/internal/database/routing_sqlite.go:164:		`CREATE TRIGGER trg_execution_location_outages_route_update
services/control-plane/internal/database/routing_sqlite.go-165-			 BEFORE UPDATE ON execution_location_outages
services/control-plane/internal/database/routing_sqlite.go-166-			 BEGIN
services/control-plane/internal/database/routing_sqlite.go-167-			   SELECT RAISE(ABORT, 'Location outage authority identity is immutable')
services/control-plane/internal/database/routing_sqlite.go-168-			   WHERE NEW.tenant_id IS NOT OLD.tenant_id
--
services/control-plane/internal/database/routing_sqlite.go-178-			      OR length(trim(NEW.publisher_identity)) NOT BETWEEN 1 AND 200
services/control-plane/internal/database/routing_sqlite.go-179-			      OR (NEW.reason IS NOT NULL AND length(NEW.reason) > 2000);
services/control-plane/internal/database/routing_sqlite.go-180-			 END`,
services/control-plane/internal/database/routing_sqlite.go-181-		`DROP TRIGGER IF EXISTS trg_execution_location_outages_route_delete`,
services/control-plane/internal/database/routing_sqlite.go:182:		`CREATE TRIGGER trg_execution_location_outages_route_delete
services/control-plane/internal/database/routing_sqlite.go-183-			 BEFORE DELETE ON execution_location_outages
services/control-plane/internal/database/routing_sqlite.go-184-			 BEGIN
services/control-plane/internal/database/routing_sqlite.go-185-			   SELECT RAISE(ABORT, 'Location outage observations cannot be deleted');
services/control-plane/internal/database/routing_sqlite.go-186-			 END`,
services/control-plane/internal/database/routing_sqlite.go-187-		`DROP TRIGGER IF EXISTS trg_agent_sessions_target_routing_insert`,
services/control-plane/internal/database/routing_sqlite.go:188:		`CREATE TRIGGER trg_agent_sessions_target_routing_insert
services/control-plane/internal/database/routing_sqlite.go-189-			 BEFORE INSERT ON agent_sessions
services/control-plane/internal/database/routing_sqlite.go-190-			 BEGIN
services/control-plane/internal/database/routing_sqlite.go-191-		   SELECT RAISE(ABORT, 'invalid Session Target routing shape')
services/control-plane/internal/database/routing_sqlite.go-192-		   WHERE NEW.requested_execution_target_id IS NULL
--
services/control-plane/internal/database/routing_sqlite.go-207-		          AND target_group.status = 'active' AND member.status = 'active'
services/control-plane/internal/database/routing_sqlite.go-208-		      ));
services/control-plane/internal/database/routing_sqlite.go-209-		 END`,
services/control-plane/internal/database/routing_sqlite.go-210-		`DROP TRIGGER IF EXISTS trg_agent_sessions_target_routing_update`,
services/control-plane/internal/database/routing_sqlite.go:211:		`CREATE TRIGGER trg_agent_sessions_target_routing_update
services/control-plane/internal/database/routing_sqlite.go-212-		 BEFORE UPDATE OF requested_execution_target_id, execution_target_group_id,
services/control-plane/internal/database/routing_sqlite.go-213-		   routing_policy_version, preferred_execution_region ON agent_sessions
services/control-plane/internal/database/routing_sqlite.go-214-		 BEGIN
services/control-plane/internal/database/routing_sqlite.go-215-		   SELECT RAISE(ABORT, 'invalid Session Target routing shape')
--
services/control-plane/internal/database/routing_sqlite.go-231-		          AND target_group.status = 'active' AND member.status = 'active'
services/control-plane/internal/database/routing_sqlite.go-232-		      ));
services/control-plane/internal/database/routing_sqlite.go-233-		 END`,
services/control-plane/internal/database/routing_sqlite.go-234-		`DROP TRIGGER IF EXISTS trg_agent_executions_target_routing_insert`,
services/control-plane/internal/database/routing_sqlite.go:235:		`CREATE TRIGGER trg_agent_executions_target_routing_insert
services/control-plane/internal/database/routing_sqlite.go-236-		 BEFORE INSERT ON agent_executions
services/control-plane/internal/database/routing_sqlite.go-237-		 BEGIN
services/control-plane/internal/database/routing_sqlite.go-238-		   SELECT RAISE(ABORT, 'invalid Execution Target routing snapshot')
services/control-plane/internal/database/routing_sqlite.go-239-		   WHERE NOT (
--
services/control-plane/internal/database/routing_sqlite.go-269-		       )
services/control-plane/internal/database/routing_sqlite.go-270-		   ));
services/control-plane/internal/database/routing_sqlite.go-271-		 END`,
services/control-plane/internal/database/routing_sqlite.go-272-		`DROP TRIGGER IF EXISTS trg_agent_executions_target_routing_update`,
services/control-plane/internal/database/routing_sqlite.go:273:		`CREATE TRIGGER trg_agent_executions_target_routing_update
services/control-plane/internal/database/routing_sqlite.go-274-		 BEFORE UPDATE OF target_group_id, target_group_version, target_group_member_version,
services/control-plane/internal/database/routing_sqlite.go-275-		   selected_region, selected_cluster_id, routing_reason, predecessor_execution_id ON agent_executions
services/control-plane/internal/database/routing_sqlite.go-276-		 BEGIN
services/control-plane/internal/database/routing_sqlite.go-277-		   SELECT RAISE(ABORT, 'Execution global routing snapshot is immutable')
--
services/control-plane/internal/database/routing_sqlite.go-285-		     OR NEW.predecessor_execution_id IS NOT OLD.predecessor_execution_id
services/control-plane/internal/database/routing_sqlite.go-286-		   );
services/control-plane/internal/database/routing_sqlite.go-287-		 END`,
services/control-plane/internal/database/routing_sqlite.go-288-		`DROP TRIGGER IF EXISTS trg_execution_interactions_failover_route_insert`,
services/control-plane/internal/database/routing_sqlite.go:289:		`CREATE TRIGGER trg_execution_interactions_failover_route_insert
services/control-plane/internal/database/routing_sqlite.go-290-		 BEFORE INSERT ON execution_interactions
services/control-plane/internal/database/routing_sqlite.go-291-		 BEGIN
services/control-plane/internal/database/routing_sqlite.go-292-		   SELECT RAISE(ABORT, 'Interaction source Execution is required')
services/control-plane/internal/database/routing_sqlite.go-293-		   WHERE NEW.source_execution_id IS NULL;
services/control-plane/internal/database/routing_sqlite.go-294-		 END`,
services/control-plane/internal/database/routing_sqlite.go-295-		`DROP TRIGGER IF EXISTS trg_execution_interactions_failover_route_update`,
services/control-plane/internal/database/routing_sqlite.go:296:		`CREATE TRIGGER trg_execution_interactions_failover_route_update
services/control-plane/internal/database/routing_sqlite.go-297-		 BEFORE UPDATE OF execution_id, source_execution_id ON execution_interactions
services/control-plane/internal/database/routing_sqlite.go-298-		 BEGIN
services/control-plane/internal/database/routing_sqlite.go-299-		   SELECT RAISE(ABORT, 'Interaction source Execution is immutable')
services/control-plane/internal/database/routing_sqlite.go-300-		   WHERE NEW.source_execution_id IS NOT OLD.source_execution_id;
--
services/control-plane/internal/database/routing_sqlite.go-310-		     )
services/control-plane/internal/database/routing_sqlite.go-311-		   );
services/control-plane/internal/database/routing_sqlite.go-312-		 END`,
services/control-plane/internal/database/routing_sqlite.go-313-		`DROP TRIGGER IF EXISTS trg_execution_failover_attempts_route_update`,
services/control-plane/internal/database/routing_sqlite.go:314:		`CREATE TRIGGER trg_execution_failover_attempts_route_update
services/control-plane/internal/database/routing_sqlite.go-315-		 BEFORE UPDATE ON execution_failover_attempts
services/control-plane/internal/database/routing_sqlite.go-316-		 BEGIN
services/control-plane/internal/database/routing_sqlite.go-317-		   SELECT RAISE(ABORT, 'Execution failover attempt lineage is immutable')
services/control-plane/internal/database/routing_sqlite.go-318-		   WHERE OLD.status <> 'planned' OR NEW.id IS NOT OLD.id OR NEW.tenant_id IS NOT OLD.tenant_id
--
services/control-plane/internal/database/routing_sqlite.go-327-		      OR NEW.reason IS NOT OLD.reason OR NEW.requested_at IS NOT OLD.requested_at
services/control-plane/internal/database/routing_sqlite.go-328-		      OR NEW.status NOT IN ('committed', 'failed', 'aborted');
services/control-plane/internal/database/routing_sqlite.go-329-		 END`,
services/control-plane/internal/database/routing_sqlite.go-330-		`DROP TRIGGER IF EXISTS trg_execution_failover_attempts_route_delete`,
services/control-plane/internal/database/routing_sqlite.go:331:		`CREATE TRIGGER trg_execution_failover_attempts_route_delete
services/control-plane/internal/database/routing_sqlite.go-332-		 BEFORE DELETE ON execution_failover_attempts
services/control-plane/internal/database/routing_sqlite.go-333-		 BEGIN
services/control-plane/internal/database/routing_sqlite.go-334-		   SELECT RAISE(ABORT, 'Execution failover attempts cannot be deleted');
services/control-plane/internal/database/routing_sqlite.go-335-		 END`,
--
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go-11-	statements := []string{
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go-12-		`CREATE INDEX IF NOT EXISTS idx_worker_claim_release_facts_released
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go-13-		 ON worker_claim_release_facts (released_at, claim_fact_id)`,
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go-14-		`DROP TRIGGER IF EXISTS trg_worker_claim_release_facts_insert`,
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go:15:		`CREATE TRIGGER trg_worker_claim_release_facts_insert
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go-16-		 BEFORE INSERT ON worker_claim_release_facts
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go-17-		 BEGIN
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go-18-		   SELECT RAISE(ABORT, 'invalid Worker claim release fact')
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go-19-		   WHERE NEW.release_reason NOT IN (
--
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go-39-		          AND julianday(NEW.released_at) >= julianday(claim.claimed_at)
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go-40-		      );
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go-41-		 END`,
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go-42-		`DROP TRIGGER IF EXISTS trg_worker_claim_release_facts_update`,
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go:43:		`CREATE TRIGGER trg_worker_claim_release_facts_update
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go-44-		 BEFORE UPDATE ON worker_claim_release_facts
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go-45-		 BEGIN
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go-46-		   SELECT RAISE(ABORT, 'Worker claim release facts are immutable');
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go-47-		 END`,
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go-48-		`DROP TRIGGER IF EXISTS trg_worker_claim_release_facts_delete`,
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go:49:		`CREATE TRIGGER trg_worker_claim_release_facts_delete
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go-50-		 BEFORE DELETE ON worker_claim_release_facts
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go-51-		 BEGIN
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go-52-		   SELECT RAISE(ABORT, 'Worker claim release facts are immutable');
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go-53-		 END`,
--
services/control-plane/internal/database/worker_release_sqlite.go-40-		   execution_target_id, worker_release_revision_id, worker_release_channel, queued_at, id
services/control-plane/internal/database/worker_release_sqlite.go-41-		 )
services/control-plane/internal/database/worker_release_sqlite.go-42-		 WHERE status IN ('queued', 'recovering')`,
services/control-plane/internal/database/worker_release_sqlite.go-43-		`DROP TRIGGER IF EXISTS trg_worker_release_revisions_insert_shape`,
services/control-plane/internal/database/worker_release_sqlite.go:44:		`CREATE TRIGGER trg_worker_release_revisions_insert_shape
services/control-plane/internal/database/worker_release_sqlite.go-45-		 BEFORE INSERT ON worker_release_revisions
services/control-plane/internal/database/worker_release_sqlite.go-46-		 BEGIN
services/control-plane/internal/database/worker_release_sqlite.go-47-		   SELECT RAISE(ABORT, 'invalid Worker release revision')
services/control-plane/internal/database/worker_release_sqlite.go-48-		   WHERE NEW.revision <= 0
--
services/control-plane/internal/database/worker_release_sqlite.go-57-		        WHERE manifest.id = NEW.worker_manifest_id
services/control-plane/internal/database/worker_release_sqlite.go-58-		      );
services/control-plane/internal/database/worker_release_sqlite.go-59-		 END`,
services/control-plane/internal/database/worker_release_sqlite.go-60-		`DROP TRIGGER IF EXISTS trg_worker_release_revisions_immutable_update`,
services/control-plane/internal/database/worker_release_sqlite.go:61:		`CREATE TRIGGER trg_worker_release_revisions_immutable_update
services/control-plane/internal/database/worker_release_sqlite.go-62-		 BEFORE UPDATE ON worker_release_revisions
services/control-plane/internal/database/worker_release_sqlite.go-63-		 BEGIN
services/control-plane/internal/database/worker_release_sqlite.go-64-		   SELECT RAISE(ABORT, 'Worker release revisions are immutable');
services/control-plane/internal/database/worker_release_sqlite.go-65-		 END`,
services/control-plane/internal/database/worker_release_sqlite.go-66-		`DROP TRIGGER IF EXISTS trg_worker_release_revisions_immutable_delete`,
services/control-plane/internal/database/worker_release_sqlite.go:67:		`CREATE TRIGGER trg_worker_release_revisions_immutable_delete
services/control-plane/internal/database/worker_release_sqlite.go-68-		 BEFORE DELETE ON worker_release_revisions
services/control-plane/internal/database/worker_release_sqlite.go-69-		 BEGIN
services/control-plane/internal/database/worker_release_sqlite.go-70-		   SELECT RAISE(ABORT, 'Worker release revisions are immutable');
services/control-plane/internal/database/worker_release_sqlite.go-71-		 END`,
services/control-plane/internal/database/worker_release_sqlite.go-72-		`DROP TRIGGER IF EXISTS trg_worker_release_policies_insert_shape`,
services/control-plane/internal/database/worker_release_sqlite.go:73:		`CREATE TRIGGER trg_worker_release_policies_insert_shape
services/control-plane/internal/database/worker_release_sqlite.go-74-		 BEFORE INSERT ON worker_release_policies
services/control-plane/internal/database/worker_release_sqlite.go-75-		 BEGIN
services/control-plane/internal/database/worker_release_sqlite.go-76-		   SELECT RAISE(ABORT, 'invalid Worker release policy')
services/control-plane/internal/database/worker_release_sqlite.go-77-		   WHERE NEW.policy_version <= 0
--
services/control-plane/internal/database/worker_release_sqlite.go-104-		        )
services/control-plane/internal/database/worker_release_sqlite.go-105-		      );
services/control-plane/internal/database/worker_release_sqlite.go-106-		 END`,
services/control-plane/internal/database/worker_release_sqlite.go-107-		`DROP TRIGGER IF EXISTS trg_worker_release_policies_update_cas`,
services/control-plane/internal/database/worker_release_sqlite.go:108:		`CREATE TRIGGER trg_worker_release_policies_update_cas
services/control-plane/internal/database/worker_release_sqlite.go-109-		 BEFORE UPDATE ON worker_release_policies
services/control-plane/internal/database/worker_release_sqlite.go-110-		 BEGIN
services/control-plane/internal/database/worker_release_sqlite.go-111-		   SELECT RAISE(ABORT, 'Worker release policy ownership is immutable')
services/control-plane/internal/database/worker_release_sqlite.go-112-		   WHERE NEW.tenant_id IS NOT OLD.tenant_id
--
services/control-plane/internal/database/worker_release_sqlite.go-137-		        )
services/control-plane/internal/database/worker_release_sqlite.go-138-		      );
services/control-plane/internal/database/worker_release_sqlite.go-139-		 END`,
services/control-plane/internal/database/worker_release_sqlite.go-140-		`DROP TRIGGER IF EXISTS trg_worker_release_policies_no_delete`,
services/control-plane/internal/database/worker_release_sqlite.go:141:		`CREATE TRIGGER trg_worker_release_policies_no_delete
services/control-plane/internal/database/worker_release_sqlite.go-142-		 BEFORE DELETE ON worker_release_policies
services/control-plane/internal/database/worker_release_sqlite.go-143-		 BEGIN
services/control-plane/internal/database/worker_release_sqlite.go-144-		   SELECT RAISE(ABORT, 'Worker release policies cannot be deleted');
services/control-plane/internal/database/worker_release_sqlite.go-145-		 END`,
services/control-plane/internal/database/worker_release_sqlite.go-146-		`DROP TRIGGER IF EXISTS trg_worker_release_transitions_insert_shape`,
services/control-plane/internal/database/worker_release_sqlite.go:147:		`CREATE TRIGGER trg_worker_release_transitions_insert_shape
services/control-plane/internal/database/worker_release_sqlite.go-148-		 BEFORE INSERT ON worker_release_transitions
services/control-plane/internal/database/worker_release_sqlite.go-149-		 BEGIN
services/control-plane/internal/database/worker_release_sqlite.go-150-		   SELECT RAISE(ABORT, 'invalid Worker release transition')
services/control-plane/internal/database/worker_release_sqlite.go-151-		   WHERE NEW.policy_version <= 0
--
services/control-plane/internal/database/worker_release_sqlite.go-162-		          AND policy.canary_percent = NEW.canary_percent
services/control-plane/internal/database/worker_release_sqlite.go-163-		      );
services/control-plane/internal/database/worker_release_sqlite.go-164-		 END`,
services/control-plane/internal/database/worker_release_sqlite.go-165-		`DROP TRIGGER IF EXISTS trg_worker_release_transitions_immutable_update`,
services/control-plane/internal/database/worker_release_sqlite.go:166:		`CREATE TRIGGER trg_worker_release_transitions_immutable_update
services/control-plane/internal/database/worker_release_sqlite.go-167-		 BEFORE UPDATE ON worker_release_transitions
services/control-plane/internal/database/worker_release_sqlite.go-168-		 BEGIN
services/control-plane/internal/database/worker_release_sqlite.go-169-		   SELECT RAISE(ABORT, 'Worker release transitions are immutable');
services/control-plane/internal/database/worker_release_sqlite.go-170-		 END`,
services/control-plane/internal/database/worker_release_sqlite.go-171-		`DROP TRIGGER IF EXISTS trg_worker_release_transitions_immutable_delete`,
services/control-plane/internal/database/worker_release_sqlite.go:172:		`CREATE TRIGGER trg_worker_release_transitions_immutable_delete
services/control-plane/internal/database/worker_release_sqlite.go-173-		 BEFORE DELETE ON worker_release_transitions
services/control-plane/internal/database/worker_release_sqlite.go-174-		 BEGIN
services/control-plane/internal/database/worker_release_sqlite.go-175-		   SELECT RAISE(ABORT, 'Worker release transitions are immutable');
services/control-plane/internal/database/worker_release_sqlite.go-176-		 END`,
services/control-plane/internal/database/worker_release_sqlite.go-177-		`DROP TRIGGER IF EXISTS trg_worker_release_auto_rollback_window_insert_shape`,
services/control-plane/internal/database/worker_release_sqlite.go:178:		`CREATE TRIGGER trg_worker_release_auto_rollback_window_insert_shape
services/control-plane/internal/database/worker_release_sqlite.go-179-		 BEFORE INSERT ON worker_release_auto_rollback_windows
services/control-plane/internal/database/worker_release_sqlite.go-180-		 BEGIN
services/control-plane/internal/database/worker_release_sqlite.go-181-		   SELECT RAISE(ABORT, 'invalid Worker release auto-rollback window')
services/control-plane/internal/database/worker_release_sqlite.go-182-		   WHERE NEW.policy_version <= 0
--
services/control-plane/internal/database/worker_release_sqlite.go-228-		          AND revision.id = NEW.fallback_revision_id
services/control-plane/internal/database/worker_release_sqlite.go-229-		      );
services/control-plane/internal/database/worker_release_sqlite.go-230-		 END`,
services/control-plane/internal/database/worker_release_sqlite.go-231-		`DROP TRIGGER IF EXISTS trg_worker_release_auto_rollback_window_update_shape`,
services/control-plane/internal/database/worker_release_sqlite.go:232:		`CREATE TRIGGER trg_worker_release_auto_rollback_window_update_shape
services/control-plane/internal/database/worker_release_sqlite.go-233-		 BEFORE UPDATE ON worker_release_auto_rollback_windows
services/control-plane/internal/database/worker_release_sqlite.go-234-		 BEGIN
services/control-plane/internal/database/worker_release_sqlite.go-235-		   SELECT RAISE(ABORT, 'Worker release auto-rollback window identity and policy are immutable')
services/control-plane/internal/database/worker_release_sqlite.go-236-		   WHERE NEW.id IS NOT OLD.id
--
services/control-plane/internal/database/worker_release_sqlite.go-262-		      OR (NEW.status IN ('monitoring', 'expired') AND (NEW.decision_reason IS NOT NULL OR NEW.decision_at IS NOT NULL))
services/control-plane/internal/database/worker_release_sqlite.go-263-		      OR (NEW.status IN ('rollback-pending', 'triggered') AND (NEW.decision_reason IS NULL OR NEW.decision_at IS NULL));
services/control-plane/internal/database/worker_release_sqlite.go-264-		 END`,
services/control-plane/internal/database/worker_release_sqlite.go-265-		`DROP TRIGGER IF EXISTS trg_worker_release_auto_rollback_window_delete`,
services/control-plane/internal/database/worker_release_sqlite.go:266:		`CREATE TRIGGER trg_worker_release_auto_rollback_window_delete
services/control-plane/internal/database/worker_release_sqlite.go-267-		 BEFORE DELETE ON worker_release_auto_rollback_windows
services/control-plane/internal/database/worker_release_sqlite.go-268-		 BEGIN
services/control-plane/internal/database/worker_release_sqlite.go-269-		   SELECT RAISE(ABORT, 'Worker release auto-rollback windows are durable release evidence');
services/control-plane/internal/database/worker_release_sqlite.go-270-		 END`,
services/control-plane/internal/database/worker_release_sqlite.go-271-		`DROP TRIGGER IF EXISTS trg_worker_instances_release_shape_insert`,
services/control-plane/internal/database/worker_release_sqlite.go:272:		`CREATE TRIGGER trg_worker_instances_release_shape_insert
services/control-plane/internal/database/worker_release_sqlite.go-273-		 BEFORE INSERT ON worker_instances
services/control-plane/internal/database/worker_release_sqlite.go-274-		 WHEN NOT (
services/control-plane/internal/database/worker_release_sqlite.go-275-		   (
services/control-plane/internal/database/worker_release_sqlite.go-276-		     NEW.worker_release_status = 'unmanaged'
--
services/control-plane/internal/database/worker_release_sqlite.go-298-		 BEGIN
services/control-plane/internal/database/worker_release_sqlite.go-299-		   SELECT RAISE(ABORT, 'invalid Worker release state');
services/control-plane/internal/database/worker_release_sqlite.go-300-		 END`,
services/control-plane/internal/database/worker_release_sqlite.go-301-		`DROP TRIGGER IF EXISTS trg_worker_instances_release_shape_update`,
services/control-plane/internal/database/worker_release_sqlite.go:302:		`CREATE TRIGGER trg_worker_instances_release_shape_update
services/control-plane/internal/database/worker_release_sqlite.go-303-		 BEFORE UPDATE OF worker_release_revision_id, worker_release_channel,
services/control-plane/internal/database/worker_release_sqlite.go-304-		   worker_release_status, worker_release_reason, worker_release_checked_at
services/control-plane/internal/database/worker_release_sqlite.go-305-		 ON worker_instances
services/control-plane/internal/database/worker_release_sqlite.go-306-		 WHEN NOT (
--
services/control-plane/internal/database/worker_release_sqlite.go-330-		 BEGIN
services/control-plane/internal/database/worker_release_sqlite.go-331-		   SELECT RAISE(ABORT, 'invalid Worker release state');
services/control-plane/internal/database/worker_release_sqlite.go-332-		 END`,
services/control-plane/internal/database/worker_release_sqlite.go-333-		`DROP TRIGGER IF EXISTS trg_agent_executions_release_shape_insert`,
services/control-plane/internal/database/worker_release_sqlite.go:334:		`CREATE TRIGGER trg_agent_executions_release_shape_insert
services/control-plane/internal/database/worker_release_sqlite.go-335-		 BEFORE INSERT ON agent_executions
services/control-plane/internal/database/worker_release_sqlite.go-336-		 WHEN NOT (
services/control-plane/internal/database/worker_release_sqlite.go-337-		   (NEW.worker_release_revision_id IS NULL AND NEW.worker_release_channel IS NULL)
services/control-plane/internal/database/worker_release_sqlite.go-338-		   OR
--
services/control-plane/internal/database/worker_release_sqlite.go-350-		 BEGIN
services/control-plane/internal/database/worker_release_sqlite.go-351-		   SELECT RAISE(ABORT, 'invalid Execution Worker release selection');
services/control-plane/internal/database/worker_release_sqlite.go-352-		 END`,
services/control-plane/internal/database/worker_release_sqlite.go-353-		`DROP TRIGGER IF EXISTS trg_agent_executions_release_shape_update`,
services/control-plane/internal/database/worker_release_sqlite.go:354:		`CREATE TRIGGER trg_agent_executions_release_shape_update
services/control-plane/internal/database/worker_release_sqlite.go-355-		 BEFORE UPDATE OF worker_release_revision_id, worker_release_channel ON agent_executions
services/control-plane/internal/database/worker_release_sqlite.go-356-		 WHEN NOT (
services/control-plane/internal/database/worker_release_sqlite.go-357-		   (NEW.worker_release_revision_id IS NULL AND NEW.worker_release_channel IS NULL)
services/control-plane/internal/database/worker_release_sqlite.go-358-		   OR
--
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-36-		statements := []string{
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-37-			`UPDATE projects SET git_credential_id = NULL WHERE git_credential_id IS NOT NULL`,
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-38-			`DROP INDEX IF EXISTS idx_projects_git_credential`,
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-39-			`DROP TRIGGER IF EXISTS trg_projects_legacy_git_credential_insert`,
services/control-plane/internal/database/project_git_binding_authority_sqlite.go:40:			`CREATE TRIGGER trg_projects_legacy_git_credential_insert
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-41-			 BEFORE INSERT ON projects
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-42-			 WHEN NEW.git_credential_id IS NOT NULL
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-43-			 BEGIN
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-44-			   SELECT RAISE(ABORT, 'projects.git_credential_id is retired; use credential_bindings');
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-45-			 END`,
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-46-			`DROP TRIGGER IF EXISTS trg_projects_legacy_git_credential_update`,
services/control-plane/internal/database/project_git_binding_authority_sqlite.go:47:			`CREATE TRIGGER trg_projects_legacy_git_credential_update
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-48-			 BEFORE UPDATE OF git_credential_id ON projects
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-49-			 WHEN NEW.git_credential_id IS NOT NULL
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-50-			 BEGIN
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-51-			   SELECT RAISE(ABORT, 'projects.git_credential_id is retired; use credential_bindings');
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-52-			 END`,
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-53-			`DROP TRIGGER IF EXISTS trg_projects_repository_git_binding_selector`,
services/control-plane/internal/database/project_git_binding_authority_sqlite.go:54:			`CREATE TRIGGER trg_projects_repository_git_binding_selector
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-55-			 BEFORE UPDATE OF repository_url ON projects
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-56-			 WHEN NEW.repository_url IS NOT OLD.repository_url
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-57-			   AND EXISTS (
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-58-			     SELECT 1
--
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-66-			 BEGIN
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-67-			   SELECT RAISE(ABORT, 'active Project Git Credential Bindings must be disabled before changing repository_url');
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-68-			 END`,
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-69-			`DROP TRIGGER IF EXISTS trg_credential_bindings_git_selector_insert`,
services/control-plane/internal/database/project_git_binding_authority_sqlite.go:70:			`CREATE TRIGGER trg_credential_bindings_git_selector_insert
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-71-			 BEFORE INSERT ON credential_bindings
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-72-			 WHEN NEW.project_id IS NOT NULL
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-73-			   AND NEW.binding_kind IN ('git_fetch', 'git_push')
services/control-plane/internal/database/project_git_binding_authority_sqlite.go-74-			   AND NOT EXISTS (
--
services/control-plane/internal/database/store_test.go-13-	"github.com/synara-ai/synara/services/control-plane/internal/platform"
services/control-plane/internal/database/store_test.go-14-	"github.com/synara-ai/synara/services/control-plane/migrations"
services/control-plane/internal/database/store_test.go-15-)
services/control-plane/internal/database/store_test.go-16-
services/control-plane/internal/database/store_test.go:17:func TestSQLiteMetadataStoreAutoMigratesAllModels(t *testing.T) {
services/control-plane/internal/database/store_test.go-18-	config, err := platform.Defaults(platform.ProfilePersonal)
services/control-plane/internal/database/store_test.go-19-	if err != nil {
services/control-plane/internal/database/store_test.go-20-		t.Fatal(err)
services/control-plane/internal/database/store_test.go-21-	}
--
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-23-		 ON billing_shared_actual_charge_slices (estimated_slice_id)`,
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-24-		`CREATE INDEX IF NOT EXISTS idx_billing_shared_actual_charge_slices_tenant
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-25-		 ON billing_shared_actual_charge_slices (tenant_id, allocation_kind, id)`,
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-26-		`DROP TRIGGER IF EXISTS trg_billing_shared_actual_allocation_runs_insert`,
services/control-plane/internal/database/shared_actual_allocation_sqlite.go:27:		`CREATE TRIGGER trg_billing_shared_actual_allocation_runs_insert
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-28-		 BEFORE INSERT ON billing_shared_actual_allocation_runs
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-29-		 BEGIN
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-30-		   SELECT RAISE(ABORT, 'invalid billing shared actual allocation run')
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-31-		   WHERE NEW.provider NOT GLOB '[a-z0-9]*'
--
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-65-		          AND target.tenant_id IS NULL
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-66-		      );
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-67-		 END`,
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-68-		`DROP TRIGGER IF EXISTS trg_billing_shared_actual_allocation_lines_insert`,
services/control-plane/internal/database/shared_actual_allocation_sqlite.go:69:		`CREATE TRIGGER trg_billing_shared_actual_allocation_lines_insert
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-70-		 BEFORE INSERT ON billing_shared_actual_allocation_lines
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-71-		 BEGIN
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-72-		   SELECT RAISE(ABORT, 'invalid billing shared actual allocation line')
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-73-		   WHERE NEW.estimated_amount_micros < 0
--
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-108-		          AND estimate_run.execution_target_id = allocation_run.execution_target_id
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-109-		      );
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-110-		 END`,
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-111-		`DROP TRIGGER IF EXISTS trg_billing_shared_actual_charge_slices_insert`,
services/control-plane/internal/database/shared_actual_allocation_sqlite.go:112:		`CREATE TRIGGER trg_billing_shared_actual_charge_slices_insert
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-113-		 BEFORE INSERT ON billing_shared_actual_charge_slices
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-114-		 BEGIN
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-115-		   SELECT RAISE(ABORT, 'invalid billing shared actual charge slice')
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-116-		   WHERE NEW.allocation_kind NOT IN ('tenant-claim', 'platform-idle')
--
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-145-		      );
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-146-		 END`,
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-147-		`DROP TRIGGER IF EXISTS trg_billing_shared_actual_allocation_runs_seal`,
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-148-		`DROP TRIGGER IF EXISTS trg_billing_actual_invoice_lines_shared_allocation_fence`,
services/control-plane/internal/database/shared_actual_allocation_sqlite.go:149:		`CREATE TRIGGER trg_billing_actual_invoice_lines_shared_allocation_fence
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-150-		 BEFORE INSERT ON billing_actual_invoice_lines
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-151-		 WHEN EXISTS (
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-152-		   SELECT 1
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-153-		   FROM billing_shared_actual_allocation_runs AS allocation_run
--
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-158-		 BEGIN
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-159-		   SELECT RAISE(ABORT, 'billing actual invoice import is sealed by shared allocation');
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-160-		 END`,
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-161-		`DROP TRIGGER IF EXISTS trg_billing_shared_estimated_charge_slices_actual_allocation_fence`,
services/control-plane/internal/database/shared_actual_allocation_sqlite.go:162:		`CREATE TRIGGER trg_billing_shared_estimated_charge_slices_actual_allocation_fence
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-163-		 BEFORE INSERT ON billing_shared_estimated_charge_slices
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-164-		 WHEN EXISTS (
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-165-		   SELECT 1
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-166-		   FROM billing_shared_cost_allocation_runs AS estimate_run
--
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-180-		 )
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-181-		 BEGIN
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-182-		   SELECT RAISE(ABORT, 'billing shared estimate scope is sealed by actual allocation');
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-183-		 END`,
services/control-plane/internal/database/shared_actual_allocation_sqlite.go:184:		`CREATE TRIGGER trg_billing_shared_actual_allocation_runs_seal
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-185-		 BEFORE UPDATE ON billing_shared_actual_allocation_runs
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-186-		 BEGIN
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-187-		   SELECT RAISE(ABORT, 'billing shared actual allocation run transition is invalid')
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-188-		   WHERE OLD.state <> 'building' OR NEW.state <> 'sealed'
--
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-288-		       HAVING count(DISTINCT estimate_run.execution_target_id) > 1
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-289-		     );
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-290-		 END`,
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-291-		`DROP TRIGGER IF EXISTS trg_billing_shared_actual_allocation_runs_delete`,
services/control-plane/internal/database/shared_actual_allocation_sqlite.go:292:		`CREATE TRIGGER trg_billing_shared_actual_allocation_runs_delete
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-293-		 BEFORE DELETE ON billing_shared_actual_allocation_runs
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-294-		 BEGIN SELECT RAISE(ABORT, 'billing shared actual allocation history is immutable'); END`,
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-295-		`DROP TRIGGER IF EXISTS trg_billing_shared_actual_allocation_lines_update`,
services/control-plane/internal/database/shared_actual_allocation_sqlite.go:296:		`CREATE TRIGGER trg_billing_shared_actual_allocation_lines_update
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-297-		 BEFORE UPDATE ON billing_shared_actual_allocation_lines
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-298-		 BEGIN SELECT RAISE(ABORT, 'billing shared actual allocation history is immutable'); END`,
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-299-		`DROP TRIGGER IF EXISTS trg_billing_shared_actual_allocation_lines_delete`,
services/control-plane/internal/database/shared_actual_allocation_sqlite.go:300:		`CREATE TRIGGER trg_billing_shared_actual_allocation_lines_delete
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-301-		 BEFORE DELETE ON billing_shared_actual_allocation_lines
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-302-		 BEGIN SELECT RAISE(ABORT, 'billing shared actual allocation history is immutable'); END`,
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-303-		`DROP TRIGGER IF EXISTS trg_billing_shared_actual_charge_slices_update`,
services/control-plane/internal/database/shared_actual_allocation_sqlite.go:304:		`CREATE TRIGGER trg_billing_shared_actual_charge_slices_update
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-305-		 BEFORE UPDATE ON billing_shared_actual_charge_slices
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-306-		 BEGIN SELECT RAISE(ABORT, 'billing shared actual allocation history is immutable'); END`,
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-307-		`DROP TRIGGER IF EXISTS trg_billing_shared_actual_charge_slices_delete`,
services/control-plane/internal/database/shared_actual_allocation_sqlite.go:308:		`CREATE TRIGGER trg_billing_shared_actual_charge_slices_delete
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-309-		 BEFORE DELETE ON billing_shared_actual_charge_slices
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-310-		 BEGIN SELECT RAISE(ABORT, 'billing shared actual allocation history is immutable'); END`,
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-311-	}
services/control-plane/internal/database/shared_actual_allocation_sqlite.go-312-	for _, statement := range statements {
--
services/control-plane/internal/database/credential_scope_sqlite.go-27-		`CREATE INDEX IF NOT EXISTS idx_provider_credentials_tenant_platform_scope
services/control-plane/internal/database/credential_scope_sqlite.go-28-		 ON provider_credentials (tenant_id, purpose, provider, scope, id)
services/control-plane/internal/database/credential_scope_sqlite.go-29-		 WHERE scope IN ('tenant', 'platform') AND auto_select_enabled = 1 AND revoked_at IS NULL`,
services/control-plane/internal/database/credential_scope_sqlite.go-30-		`DROP TRIGGER IF EXISTS trg_provider_credential_scope_policy_insert`,
services/control-plane/internal/database/credential_scope_sqlite.go:31:		`CREATE TRIGGER trg_provider_credential_scope_policy_insert
services/control-plane/internal/database/credential_scope_sqlite.go-32-		 BEFORE INSERT ON provider_credential_scope_policies
services/control-plane/internal/database/credential_scope_sqlite.go-33-		 WHEN NOT EXISTS (
services/control-plane/internal/database/credential_scope_sqlite.go-34-		     SELECT 1 FROM tenant_memberships AS membership
services/control-plane/internal/database/credential_scope_sqlite.go-35-		     WHERE membership.tenant_id = NEW.tenant_id
--
services/control-plane/internal/database/credential_scope_sqlite.go-52-		 BEGIN
services/control-plane/internal/database/credential_scope_sqlite.go-53-		   SELECT RAISE(ABORT, 'Platform Credentials require an enterprise installation and Tenant entitlement');
services/control-plane/internal/database/credential_scope_sqlite.go-54-		 END`,
services/control-plane/internal/database/credential_scope_sqlite.go-55-		`DROP TRIGGER IF EXISTS trg_provider_credential_scope_policy_update`,
services/control-plane/internal/database/credential_scope_sqlite.go:56:		`CREATE TRIGGER trg_provider_credential_scope_policy_update
services/control-plane/internal/database/credential_scope_sqlite.go-57-		 BEFORE UPDATE OF tenant_id, platform_credentials_enabled, platform_credential_auto_select, updated_by
services/control-plane/internal/database/credential_scope_sqlite.go-58-		 ON provider_credential_scope_policies
services/control-plane/internal/database/credential_scope_sqlite.go-59-		 WHEN NOT EXISTS (
services/control-plane/internal/database/credential_scope_sqlite.go-60-		     SELECT 1 FROM tenant_memberships AS membership
--
services/control-plane/internal/database/credential_scope_sqlite.go-78-		 BEGIN
services/control-plane/internal/database/credential_scope_sqlite.go-79-		   SELECT RAISE(ABORT, 'invalid Platform Credential policy');
services/control-plane/internal/database/credential_scope_sqlite.go-80-		 END`,
services/control-plane/internal/database/credential_scope_sqlite.go-81-		`DROP TRIGGER IF EXISTS trg_provider_credentials_scope_shape_insert`,
services/control-plane/internal/database/credential_scope_sqlite.go:82:		`CREATE TRIGGER trg_provider_credentials_scope_shape_insert
services/control-plane/internal/database/credential_scope_sqlite.go-83-		 BEFORE INSERT ON provider_credentials
services/control-plane/internal/database/credential_scope_sqlite.go-84-		 WHEN NEW.scope IS NULL
services/control-plane/internal/database/credential_scope_sqlite.go-85-		   OR NEW.scope NOT IN ('user', 'organization', 'tenant', 'platform')
services/control-plane/internal/database/credential_scope_sqlite.go-86-		   OR NOT (
--
services/control-plane/internal/database/credential_scope_sqlite.go-108-		 BEGIN
services/control-plane/internal/database/credential_scope_sqlite.go-109-		   SELECT RAISE(ABORT, 'invalid Provider Credential scope shape');
services/control-plane/internal/database/credential_scope_sqlite.go-110-		 END`,
services/control-plane/internal/database/credential_scope_sqlite.go-111-		`DROP TRIGGER IF EXISTS trg_provider_credentials_scope_membership_insert`,
services/control-plane/internal/database/credential_scope_sqlite.go:112:		`CREATE TRIGGER trg_provider_credentials_scope_membership_insert
services/control-plane/internal/database/credential_scope_sqlite.go-113-		 BEFORE INSERT ON provider_credentials
services/control-plane/internal/database/credential_scope_sqlite.go-114-		 WHEN (NEW.organization_id IS NOT NULL AND NOT EXISTS (
services/control-plane/internal/database/credential_scope_sqlite.go-115-		   SELECT 1 FROM organizations AS organization
services/control-plane/internal/database/credential_scope_sqlite.go-116-		   WHERE organization.tenant_id = NEW.tenant_id AND organization.id = NEW.organization_id
--
services/control-plane/internal/database/credential_scope_sqlite.go-138-		 BEGIN
services/control-plane/internal/database/credential_scope_sqlite.go-139-		   SELECT RAISE(ABORT, 'Provider Credential scope is not entitled');
services/control-plane/internal/database/credential_scope_sqlite.go-140-		 END`,
services/control-plane/internal/database/credential_scope_sqlite.go-141-		`DROP TRIGGER IF EXISTS trg_provider_credentials_scope_shape_update`,
services/control-plane/internal/database/credential_scope_sqlite.go:142:		`CREATE TRIGGER trg_provider_credentials_scope_shape_update
services/control-plane/internal/database/credential_scope_sqlite.go-143-		 BEFORE UPDATE OF tenant_id, purpose, provider, credential_type, scope, scope_user_id,
services/control-plane/internal/database/credential_scope_sqlite.go-144-		   organization_id, selector_organization_id, selector_model, auto_select_enabled, aad_version,
services/control-plane/internal/database/credential_scope_sqlite.go-145-		   version, encrypted_payload, encrypted_data_key, kms_provider, kms_key_id
services/control-plane/internal/database/credential_scope_sqlite.go-146-		 ON provider_credentials
--
services/control-plane/internal/database/credential_scope_sqlite.go-171-		 BEGIN
services/control-plane/internal/database/credential_scope_sqlite.go-172-		   SELECT RAISE(ABORT, 'Provider Credential scope identity is immutable');
services/control-plane/internal/database/credential_scope_sqlite.go-173-		 END`,
services/control-plane/internal/database/credential_scope_sqlite.go-174-		`DROP TRIGGER IF EXISTS trg_agent_sessions_provider_credential_scope_insert`,
services/control-plane/internal/database/credential_scope_sqlite.go:175:		`CREATE TRIGGER trg_agent_sessions_provider_credential_scope_insert
services/control-plane/internal/database/credential_scope_sqlite.go-176-		 BEFORE INSERT ON agent_sessions
services/control-plane/internal/database/credential_scope_sqlite.go-177-		 WHEN NEW.provider_credential_id IS NOT NULL AND NOT EXISTS (
services/control-plane/internal/database/credential_scope_sqlite.go-178-		   SELECT 1
services/control-plane/internal/database/credential_scope_sqlite.go-179-		   FROM provider_credentials AS credential
--
services/control-plane/internal/database/credential_scope_sqlite.go-210-		 BEGIN
services/control-plane/internal/database/credential_scope_sqlite.go-211-		   SELECT RAISE(ABORT, 'Agent Session Provider Credential violates Credential scope');
services/control-plane/internal/database/credential_scope_sqlite.go-212-		 END`,
services/control-plane/internal/database/credential_scope_sqlite.go-213-		`DROP TRIGGER IF EXISTS trg_agent_sessions_provider_credential_scope_update`,
services/control-plane/internal/database/credential_scope_sqlite.go:214:		`CREATE TRIGGER trg_agent_sessions_provider_credential_scope_update
services/control-plane/internal/database/credential_scope_sqlite.go-215-		 BEFORE UPDATE OF tenant_id, organization_id, created_by, provider, model, provider_credential_id
services/control-plane/internal/database/credential_scope_sqlite.go-216-		 ON agent_sessions
services/control-plane/internal/database/credential_scope_sqlite.go-217-		 WHEN NEW.provider_credential_id IS NOT NULL AND NOT EXISTS (
services/control-plane/internal/database/credential_scope_sqlite.go-218-		   SELECT 1
--
services/control-plane/internal/database/platform_routing_authority_sqlite.go-11-	statements := []string{
services/control-plane/internal/database/platform_routing_authority_sqlite.go-12-		`CREATE INDEX IF NOT EXISTS idx_platform_routing_publications_target_received
services/control-plane/internal/database/platform_routing_authority_sqlite.go-13-		 ON platform_routing_publications (execution_target_id, received_at DESC, publisher_identity, nonce)`,
services/control-plane/internal/database/platform_routing_authority_sqlite.go-14-		`DROP TRIGGER IF EXISTS trg_platform_routing_publications_insert`,
services/control-plane/internal/database/platform_routing_authority_sqlite.go:15:		`CREATE TRIGGER trg_platform_routing_publications_insert
services/control-plane/internal/database/platform_routing_authority_sqlite.go-16-		 BEFORE INSERT ON platform_routing_publications
services/control-plane/internal/database/platform_routing_authority_sqlite.go-17-		 BEGIN
services/control-plane/internal/database/platform_routing_authority_sqlite.go-18-		   SELECT RAISE(ABORT, 'invalid Platform routing publication receipt')
services/control-plane/internal/database/platform_routing_authority_sqlite.go-19-		   WHERE length(trim(NEW.publisher_identity)) NOT BETWEEN 1 AND 160
--
services/control-plane/internal/database/platform_routing_authority_sqlite.go-32-		        WHERE target.id = NEW.execution_target_id
services/control-plane/internal/database/platform_routing_authority_sqlite.go-33-		      );
services/control-plane/internal/database/platform_routing_authority_sqlite.go-34-		 END`,
services/control-plane/internal/database/platform_routing_authority_sqlite.go-35-		`DROP TRIGGER IF EXISTS trg_platform_routing_publications_update`,
services/control-plane/internal/database/platform_routing_authority_sqlite.go:36:		`CREATE TRIGGER trg_platform_routing_publications_update
services/control-plane/internal/database/platform_routing_authority_sqlite.go-37-		 BEFORE UPDATE ON platform_routing_publications
services/control-plane/internal/database/platform_routing_authority_sqlite.go-38-		 BEGIN
services/control-plane/internal/database/platform_routing_authority_sqlite.go-39-		   SELECT RAISE(ABORT, 'Platform routing publication receipts are immutable');
services/control-plane/internal/database/platform_routing_authority_sqlite.go-40-		 END`,
services/control-plane/internal/database/platform_routing_authority_sqlite.go-41-		`DROP TRIGGER IF EXISTS trg_platform_routing_publications_delete`,
services/control-plane/internal/database/platform_routing_authority_sqlite.go:42:		`CREATE TRIGGER trg_platform_routing_publications_delete
services/control-plane/internal/database/platform_routing_authority_sqlite.go-43-		 BEFORE DELETE ON platform_routing_publications
services/control-plane/internal/database/platform_routing_authority_sqlite.go-44-		 BEGIN
services/control-plane/internal/database/platform_routing_authority_sqlite.go-45-		   SELECT RAISE(ABORT, 'Platform routing publication receipts cannot be deleted');
services/control-plane/internal/database/platform_routing_authority_sqlite.go-46-		 END`,
--
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-24-		   run_id, allocation_kind, ifnull(tenant_id, '-'), ifnull(claim_fact_id, '-'),
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-25-		   tariff_id, charge_kind, usage_start_at, usage_end_at
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-26-		 )`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-27-		`DROP TRIGGER IF EXISTS trg_billing_shared_target_ledger_coverages_insert`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go:28:		`CREATE TRIGGER trg_billing_shared_target_ledger_coverages_insert
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-29-		 BEFORE INSERT ON billing_shared_target_ledger_coverages
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-30-		 BEGIN
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-31-		   SELECT RAISE(ABORT, 'invalid billing shared Target ledger coverage')
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-32-		   WHERE length(NEW.minimum_writer_version) NOT BETWEEN 1 AND 80
--
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-40-		        WHERE target.id = NEW.execution_target_id AND target.tenant_id IS NULL
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-41-		      );
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-42-		 END`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-43-		`DROP TRIGGER IF EXISTS trg_billing_shared_cost_allocation_runs_insert`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go:44:		`CREATE TRIGGER trg_billing_shared_cost_allocation_runs_insert
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-45-		 BEFORE INSERT ON billing_shared_cost_allocation_runs
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-46-		 BEGIN
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-47-		   SELECT RAISE(ABORT, 'invalid billing shared cost allocation run')
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-48-		   WHERE NEW.provider NOT GLOB '[a-z0-9]*'
--
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-91-		          AND julianday(NEW.usage_end_at) <= julianday(worker_fact.terminated_at)
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-92-		      );
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-93-		 END`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-94-		`DROP TRIGGER IF EXISTS trg_billing_shared_estimated_charge_slices_insert`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go:95:		`CREATE TRIGGER trg_billing_shared_estimated_charge_slices_insert
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-96-		 BEFORE INSERT ON billing_shared_estimated_charge_slices
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-97-		 BEGIN
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-98-		   SELECT RAISE(ABORT, 'invalid billing shared estimated charge slice')
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-99-		   WHERE NEW.charge_kind NOT IN ('cpu', 'memory', 'ephemeral-storage', 'request', 'pod')
--
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-259-			          )
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-260-			      );
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-261-			 END`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-262-		`DROP TRIGGER IF EXISTS trg_billing_shared_target_ledger_coverages_update`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go:263:		`CREATE TRIGGER trg_billing_shared_target_ledger_coverages_update
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-264-		 BEFORE UPDATE ON billing_shared_target_ledger_coverages
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-265-		 BEGIN SELECT RAISE(ABORT, 'billing shared allocation accounting history is immutable'); END`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-266-		`DROP TRIGGER IF EXISTS trg_billing_shared_target_ledger_coverages_delete`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go:267:		`CREATE TRIGGER trg_billing_shared_target_ledger_coverages_delete
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-268-		 BEFORE DELETE ON billing_shared_target_ledger_coverages
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-269-		 BEGIN SELECT RAISE(ABORT, 'billing shared allocation accounting history is immutable'); END`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-270-		`DROP TRIGGER IF EXISTS trg_billing_shared_cost_allocation_runs_update`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go:271:		`CREATE TRIGGER trg_billing_shared_cost_allocation_runs_update
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-272-		 BEFORE UPDATE ON billing_shared_cost_allocation_runs
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-273-		 BEGIN SELECT RAISE(ABORT, 'billing shared allocation accounting history is immutable'); END`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-274-		`DROP TRIGGER IF EXISTS trg_billing_shared_cost_allocation_runs_delete`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go:275:		`CREATE TRIGGER trg_billing_shared_cost_allocation_runs_delete
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-276-		 BEFORE DELETE ON billing_shared_cost_allocation_runs
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-277-		 BEGIN SELECT RAISE(ABORT, 'billing shared allocation accounting history is immutable'); END`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-278-		`DROP TRIGGER IF EXISTS trg_billing_shared_estimated_charge_slices_update`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go:279:		`CREATE TRIGGER trg_billing_shared_estimated_charge_slices_update
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-280-		 BEFORE UPDATE ON billing_shared_estimated_charge_slices
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-281-		 BEGIN SELECT RAISE(ABORT, 'billing shared allocation accounting history is immutable'); END`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-282-		`DROP TRIGGER IF EXISTS trg_billing_shared_estimated_charge_slices_delete`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go:283:		`CREATE TRIGGER trg_billing_shared_estimated_charge_slices_delete
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-284-		 BEFORE DELETE ON billing_shared_estimated_charge_slices
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-285-		 BEGIN SELECT RAISE(ABORT, 'billing shared allocation accounting history is immutable'); END`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-286-		`DROP TRIGGER IF EXISTS trg_execution_targets_shared_accounting_restrict_delete`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go:287:		`CREATE TRIGGER trg_execution_targets_shared_accounting_restrict_delete
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-288-		 BEFORE DELETE ON execution_targets
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-289-		 WHEN EXISTS (
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-290-		   SELECT 1 FROM billing_shared_target_ledger_coverages WHERE execution_target_id = OLD.id
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-291-		 ) OR EXISTS (
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-292-		   SELECT 1 FROM billing_shared_cost_allocation_runs WHERE execution_target_id = OLD.id
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-293-		 )
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-294-			 BEGIN SELECT RAISE(ABORT, 'billing shared allocation parent is retained'); END`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-295-		`DROP TRIGGER IF EXISTS trg_execution_targets_shared_accounting_restrict_update`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go:296:		`CREATE TRIGGER trg_execution_targets_shared_accounting_restrict_update
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-297-		 BEFORE UPDATE OF tenant_id, organization_id, kind ON execution_targets
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-298-		 WHEN (
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-299-		   NEW.tenant_id IS NOT OLD.tenant_id
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-300-		   OR NEW.organization_id IS NOT OLD.organization_id
--
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-304-		   OR EXISTS (SELECT 1 FROM billing_shared_cost_allocation_runs WHERE execution_target_id = OLD.id)
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-305-		 )
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-306-		 BEGIN SELECT RAISE(ABORT, 'billing shared allocation parent is retained'); END`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-307-		`DROP TRIGGER IF EXISTS trg_worker_incarnation_facts_shared_accounting_restrict_delete`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go:308:		`CREATE TRIGGER trg_worker_incarnation_facts_shared_accounting_restrict_delete
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-309-		 BEFORE DELETE ON worker_incarnation_facts
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-310-		 WHEN EXISTS (
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-311-		   SELECT 1 FROM billing_shared_cost_allocation_runs
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-312-		   WHERE worker_id = OLD.worker_id AND worker_incarnation = OLD.worker_incarnation
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-313-		 )
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-314-		 BEGIN SELECT RAISE(ABORT, 'billing shared allocation parent is retained'); END`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-315-		`DROP TRIGGER IF EXISTS trg_worker_claim_facts_shared_accounting_restrict_delete`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go:316:		`CREATE TRIGGER trg_worker_claim_facts_shared_accounting_restrict_delete
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-317-		 BEFORE DELETE ON worker_claim_facts
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-318-		 WHEN EXISTS (
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-319-		   SELECT 1 FROM billing_shared_estimated_charge_slices WHERE claim_fact_id = OLD.id
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-320-		 )
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-321-		 BEGIN SELECT RAISE(ABORT, 'billing shared allocation parent is retained'); END`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-322-		`DROP TRIGGER IF EXISTS trg_billing_provider_tariffs_shared_accounting_restrict_delete`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go:323:		`CREATE TRIGGER trg_billing_provider_tariffs_shared_accounting_restrict_delete
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-324-		 BEFORE DELETE ON billing_provider_tariffs
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-325-		 WHEN EXISTS (
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-326-		   SELECT 1 FROM billing_shared_estimated_charge_slices WHERE tariff_id = OLD.id
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-327-		 )
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-328-		 BEGIN SELECT RAISE(ABORT, 'billing shared allocation parent is retained'); END`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-329-		`DROP TRIGGER IF EXISTS trg_tenants_shared_accounting_restrict_delete`,
services/control-plane/internal/database/shared_cost_allocation_sqlite.go:330:		`CREATE TRIGGER trg_tenants_shared_accounting_restrict_delete
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-331-		 BEFORE DELETE ON tenants
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-332-		 WHEN EXISTS (
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-333-		   SELECT 1 FROM billing_shared_estimated_charge_slices WHERE tenant_id = OLD.id
services/control-plane/internal/database/shared_cost_allocation_sqlite.go-334-		 )
--
services/control-plane/internal/database/worker_revocation_sqlite_test.go-211-	t.Cleanup(func() { _ = store.Close() })
services/control-plane/internal/database/worker_revocation_sqlite_test.go-212-	if withSafety {
services/control-plane/internal/database/worker_revocation_sqlite_test.go-213-		err = store.Migrate(ctx, migrations.Files)
services/control-plane/internal/database/worker_revocation_sqlite_test.go-214-	} else {
services/control-plane/internal/database/worker_revocation_sqlite_test.go:215:		err = store.DB().WithContext(ctx).AutoMigrate(persistence.AllModels()...)
services/control-plane/internal/database/worker_revocation_sqlite_test.go-216-	}
services/control-plane/internal/database/worker_revocation_sqlite_test.go-217-	if err != nil {
services/control-plane/internal/database/worker_revocation_sqlite_test.go-218-		t.Fatal(err)
services/control-plane/internal/database/worker_revocation_sqlite_test.go-219-	}
--
services/control-plane/internal/database/worker_mode_sqlite_test.go-24-	if err != nil {
services/control-plane/internal/database/worker_mode_sqlite_test.go-25-		t.Fatal(err)
services/control-plane/internal/database/worker_mode_sqlite_test.go-26-	}
services/control-plane/internal/database/worker_mode_sqlite_test.go-27-	t.Cleanup(func() { _ = store.Close() })
services/control-plane/internal/database/worker_mode_sqlite_test.go:28:	if err := store.DB().WithContext(ctx).AutoMigrate(persistence.AllModels()...); err != nil {
services/control-plane/internal/database/worker_mode_sqlite_test.go-29-		t.Fatal(err)
services/control-plane/internal/database/worker_mode_sqlite_test.go-30-	}
services/control-plane/internal/database/worker_mode_sqlite_test.go-31-	domain, err := bootstrap.Ensure(ctx, store.DB(), platform.ProfilePersonal, "sqlite-worker-mode-"+uuid.NewString())
services/control-plane/internal/database/worker_mode_sqlite_test.go-32-	if err != nil {
--
services/control-plane/internal/database/execution_scheduling_decisions_sqlite_test.go-18-	db, err := gorm.Open(sqlite.Open("file:"+uuid.NewString()+"?mode=memory&cache=shared"), &gorm.Config{TranslateError: true})
services/control-plane/internal/database/execution_scheduling_decisions_sqlite_test.go-19-	if err != nil {
services/control-plane/internal/database/execution_scheduling_decisions_sqlite_test.go-20-		t.Fatal(err)
services/control-plane/internal/database/execution_scheduling_decisions_sqlite_test.go-21-	}
services/control-plane/internal/database/execution_scheduling_decisions_sqlite_test.go:22:	if err := db.AutoMigrate(
services/control-plane/internal/database/execution_scheduling_decisions_sqlite_test.go-23-		&persistence.AgentExecution{},
services/control-plane/internal/database/execution_scheduling_decisions_sqlite_test.go-24-		&persistence.ExecutionSchedulingDecision{},
services/control-plane/internal/database/execution_scheduling_decisions_sqlite_test.go-25-		&persistence.ExecutionSchedulingCandidate{},
services/control-plane/internal/database/execution_scheduling_decisions_sqlite_test.go-26-	); err != nil {
--
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-10-	"github.com/synara-ai/synara/services/control-plane/internal/schedulingdecision"
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-11-)
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-12-
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-13-func migrateExecutionSchedulingDecisionsSQLiteSafety(ctx context.Context, db *gorm.DB) error {
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go:14:	// AutoMigrate cannot compute SHA-256 backfill values. Keep the canonical
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-15-	// encoder in schedulingdecision as the one source of truth and insert each
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-16-	// legacy graph transactionally before installing/reinstalling its fences.
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-17-	var legacyExecutions []persistence.AgentExecution
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-18-	if err := db.WithContext(ctx).
--
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-58-		 ON execution_scheduling_candidates (tenant_id, decision_id) WHERE selected = 1`,
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-59-		`CREATE INDEX IF NOT EXISTS idx_execution_scheduling_decisions_decided
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-60-		 ON execution_scheduling_decisions (tenant_id, decided_at DESC, id)`,
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-61-		`DROP TRIGGER IF EXISTS trg_execution_scheduling_decisions_insert`,
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go:62:		`CREATE TRIGGER trg_execution_scheduling_decisions_insert
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-63-		 BEFORE INSERT ON execution_scheduling_decisions
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-64-		 BEGIN
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-65-		   SELECT RAISE(ABORT, 'invalid Execution Scheduling Decision shape')
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-66-		   WHERE NEW.algorithm_version NOT IN ('queue-pressure-v1', 'reservation-aware-v1', 'fixed-target-v1', 'legacy-selected-only')
--
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-90-		       AND CASE WHEN execution.target_group_id IS NOT NULL THEN execution.selected_cluster_id ELSE execution.placement_cluster_id END = NEW.selected_cluster_id
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-91-		   );
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-92-		 END`,
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-93-		`DROP TRIGGER IF EXISTS trg_execution_scheduling_candidates_insert`,
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go:94:		`CREATE TRIGGER trg_execution_scheduling_candidates_insert
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-95-		 BEFORE INSERT ON execution_scheduling_candidates
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-96-		 BEGIN
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-97-		   SELECT RAISE(ABORT, 'invalid Execution Scheduling Candidate shape')
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-98-		   WHERE NEW.ordinal NOT BETWEEN 0 AND 4095
--
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-152-		       AND CASE WHEN execution.target_group_id IS NOT NULL THEN execution.selected_cluster_id ELSE execution.placement_cluster_id END = NEW.cluster_id
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-153-		   );
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-154-		 END`,
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-155-		`DROP TRIGGER IF EXISTS trg_execution_scheduling_decisions_update`,
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go:156:		`CREATE TRIGGER trg_execution_scheduling_decisions_update BEFORE UPDATE ON execution_scheduling_decisions
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-157-		 BEGIN SELECT RAISE(ABORT, 'Execution Scheduling Decision evidence is immutable'); END`,
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-158-		`DROP TRIGGER IF EXISTS trg_execution_scheduling_decisions_delete`,
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go:159:		`CREATE TRIGGER trg_execution_scheduling_decisions_delete BEFORE DELETE ON execution_scheduling_decisions
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-160-		 BEGIN
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-161-		   SELECT RAISE(ABORT, 'Execution Scheduling Decision is retained by its Execution')
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-162-		   WHERE EXISTS (SELECT 1 FROM agent_executions AS execution
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-163-		     WHERE execution.tenant_id = OLD.tenant_id AND execution.id = OLD.execution_id);
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-164-		 END`,
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-165-		`DROP TRIGGER IF EXISTS trg_execution_scheduling_decisions_delete_candidates`,
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go:166:		`CREATE TRIGGER trg_execution_scheduling_decisions_delete_candidates
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-167-		 AFTER DELETE ON execution_scheduling_decisions
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-168-		 BEGIN
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-169-		   DELETE FROM execution_scheduling_candidates
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-170-		   WHERE tenant_id = OLD.tenant_id AND decision_id = OLD.id;
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-171-		 END`,
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-172-		`DROP TRIGGER IF EXISTS trg_execution_scheduling_candidates_update`,
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go:173:		`CREATE TRIGGER trg_execution_scheduling_candidates_update BEFORE UPDATE ON execution_scheduling_candidates
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-174-		 BEGIN SELECT RAISE(ABORT, 'Execution Scheduling Decision evidence is immutable'); END`,
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-175-		`DROP TRIGGER IF EXISTS trg_execution_scheduling_candidates_delete`,
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go:176:		`CREATE TRIGGER trg_execution_scheduling_candidates_delete BEFORE DELETE ON execution_scheduling_candidates
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-177-		 BEGIN
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-178-		   SELECT RAISE(ABORT, 'Execution Scheduling Candidate is retained by its Decision')
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-179-		   WHERE EXISTS (SELECT 1 FROM execution_scheduling_decisions AS decision
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-180-		     WHERE decision.tenant_id = OLD.tenant_id AND decision.id = OLD.decision_id);
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-181-		 END`,
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-182-		`DROP TRIGGER IF EXISTS trg_agent_executions_scheduling_decision_update`,
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go:183:		`CREATE TRIGGER trg_agent_executions_scheduling_decision_update
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-184-		 BEFORE UPDATE OF scheduling_decision_id ON agent_executions
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-185-		 BEGIN
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-186-		   SELECT RAISE(ABORT, 'Execution Scheduling Decision identity is immutable')
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-187-		   WHERE OLD.scheduling_decision_id IS NOT NULL AND NEW.scheduling_decision_id IS NOT OLD.scheduling_decision_id;
--
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-192-		       AND decision.execution_id = NEW.id
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-193-		   );
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-194-		 END`,
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-195-		`DROP TRIGGER IF EXISTS trg_agent_executions_scheduling_decision_delete`,
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go:196:		`CREATE TRIGGER trg_agent_executions_scheduling_decision_delete
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-197-		 AFTER DELETE ON agent_executions
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-198-		 BEGIN
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-199-		   DELETE FROM execution_scheduling_decisions
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go-200-		   WHERE tenant_id = OLD.tenant_id AND execution_id = OLD.id;
--
services/control-plane/internal/database/capacity_reservation_sqlite.go-14-		 (execution_target_id, health_version, execution_id)`,
services/control-plane/internal/database/capacity_reservation_sqlite.go-15-		`CREATE INDEX IF NOT EXISTS idx_execution_capacity_admissions_target_time
services/control-plane/internal/database/capacity_reservation_sqlite.go-16-		 ON execution_capacity_admissions (execution_target_id, admitted_at DESC, execution_id)`,
services/control-plane/internal/database/capacity_reservation_sqlite.go-17-		`DROP TRIGGER IF EXISTS trg_target_reservation_acknowledgements_insert`,
services/control-plane/internal/database/capacity_reservation_sqlite.go:18:		`CREATE TRIGGER trg_target_reservation_acknowledgements_insert
services/control-plane/internal/database/capacity_reservation_sqlite.go-19-		 BEFORE INSERT ON execution_target_reservation_acknowledgements
services/control-plane/internal/database/capacity_reservation_sqlite.go-20-		 BEGIN
services/control-plane/internal/database/capacity_reservation_sqlite.go-21-		   SELECT RAISE(ABORT, 'Reservation acknowledgement is not staged for the next Health version')
services/control-plane/internal/database/capacity_reservation_sqlite.go-22-		   WHERE NEW.execution_generation < 0 OR NEW.health_version <= 0
--
services/control-plane/internal/database/capacity_reservation_sqlite.go-31-		       AND execution.execution_target_id = NEW.execution_target_id
services/control-plane/internal/database/capacity_reservation_sqlite.go-32-		   );
services/control-plane/internal/database/capacity_reservation_sqlite.go-33-		 END`,
services/control-plane/internal/database/capacity_reservation_sqlite.go-34-		`DROP TRIGGER IF EXISTS trg_target_reservation_acknowledgements_update`,
services/control-plane/internal/database/capacity_reservation_sqlite.go:35:		`CREATE TRIGGER trg_target_reservation_acknowledgements_update
services/control-plane/internal/database/capacity_reservation_sqlite.go-36-		 BEFORE UPDATE ON execution_target_reservation_acknowledgements
services/control-plane/internal/database/capacity_reservation_sqlite.go-37-		 BEGIN SELECT RAISE(ABORT, 'Reservation acknowledgements cannot be updated in place'); END`,
services/control-plane/internal/database/capacity_reservation_sqlite.go-38-		`DROP TRIGGER IF EXISTS trg_execution_target_health_reservation_insert`,
services/control-plane/internal/database/capacity_reservation_sqlite.go:39:		`CREATE TRIGGER trg_execution_target_health_reservation_insert
services/control-plane/internal/database/capacity_reservation_sqlite.go-40-		 BEFORE INSERT ON execution_target_health
services/control-plane/internal/database/capacity_reservation_sqlite.go-41-		 BEGIN
services/control-plane/internal/database/capacity_reservation_sqlite.go-42-		   SELECT RAISE(ABORT, 'Health reservation authority shape is invalid')
services/control-plane/internal/database/capacity_reservation_sqlite.go-43-		   WHERE NOT (
--
services/control-plane/internal/database/capacity_reservation_sqlite.go-58-		       AND acknowledgement.health_version = NEW.version
services/control-plane/internal/database/capacity_reservation_sqlite.go-59-		   );
services/control-plane/internal/database/capacity_reservation_sqlite.go-60-		 END`,
services/control-plane/internal/database/capacity_reservation_sqlite.go-61-		`DROP TRIGGER IF EXISTS trg_execution_target_health_reservation_update`,
services/control-plane/internal/database/capacity_reservation_sqlite.go:62:		`CREATE TRIGGER trg_execution_target_health_reservation_update
services/control-plane/internal/database/capacity_reservation_sqlite.go-63-		 BEFORE UPDATE ON execution_target_health
services/control-plane/internal/database/capacity_reservation_sqlite.go-64-		 BEGIN
services/control-plane/internal/database/capacity_reservation_sqlite.go-65-		   SELECT RAISE(ABORT, 'Execution Target reservation authority cannot be removed')
services/control-plane/internal/database/capacity_reservation_sqlite.go-66-		   WHERE OLD.reservation_authority_mode IS NOT NULL
--
services/control-plane/internal/database/capacity_reservation_sqlite.go-84-		       AND acknowledgement.health_version = NEW.version
services/control-plane/internal/database/capacity_reservation_sqlite.go-85-		   );
services/control-plane/internal/database/capacity_reservation_sqlite.go-86-		 END`,
services/control-plane/internal/database/capacity_reservation_sqlite.go-87-		`DROP TRIGGER IF EXISTS trg_execution_capacity_admissions_insert`,
services/control-plane/internal/database/capacity_reservation_sqlite.go:88:		`CREATE TRIGGER trg_execution_capacity_admissions_insert
services/control-plane/internal/database/capacity_reservation_sqlite.go-89-		 BEFORE INSERT ON execution_capacity_admissions
services/control-plane/internal/database/capacity_reservation_sqlite.go-90-		 BEGIN
services/control-plane/internal/database/capacity_reservation_sqlite.go-91-		   SELECT RAISE(ABORT, 'Execution capacity admission scope is invalid')
services/control-plane/internal/database/capacity_reservation_sqlite.go-92-		   WHERE NEW.active_reservation_units < 0
--
services/control-plane/internal/database/capacity_reservation_sqlite.go-137-		       AND NEW.strict_capacity_used_units = NEW.allocated_capacity_units + NEW.unacknowledged_reservation_units)
services/control-plane/internal/database/capacity_reservation_sqlite.go-138-		   );
services/control-plane/internal/database/capacity_reservation_sqlite.go-139-		 END`,
services/control-plane/internal/database/capacity_reservation_sqlite.go-140-		`DROP TRIGGER IF EXISTS trg_execution_capacity_admissions_update`,
services/control-plane/internal/database/capacity_reservation_sqlite.go:141:		`CREATE TRIGGER trg_execution_capacity_admissions_update
services/control-plane/internal/database/capacity_reservation_sqlite.go-142-		 BEFORE UPDATE ON execution_capacity_admissions
services/control-plane/internal/database/capacity_reservation_sqlite.go-143-		 BEGIN SELECT RAISE(ABORT, 'Execution capacity admission evidence is immutable'); END`,
services/control-plane/internal/database/capacity_reservation_sqlite.go-144-		`DROP TRIGGER IF EXISTS trg_execution_capacity_admissions_delete`,
services/control-plane/internal/database/capacity_reservation_sqlite.go:145:		`CREATE TRIGGER trg_execution_capacity_admissions_delete
services/control-plane/internal/database/capacity_reservation_sqlite.go-146-		 BEFORE DELETE ON execution_capacity_admissions
services/control-plane/internal/database/capacity_reservation_sqlite.go-147-		 BEGIN
services/control-plane/internal/database/capacity_reservation_sqlite.go-148-		   SELECT RAISE(ABORT, 'Execution capacity admission is retained by its Execution')
services/control-plane/internal/database/capacity_reservation_sqlite.go-149-		   WHERE EXISTS (
--
services/control-plane/internal/database/capacity_reservation_sqlite.go-151-		     WHERE execution.tenant_id = OLD.tenant_id AND execution.id = OLD.execution_id
services/control-plane/internal/database/capacity_reservation_sqlite.go-152-		   );
services/control-plane/internal/database/capacity_reservation_sqlite.go-153-		 END`,
services/control-plane/internal/database/capacity_reservation_sqlite.go-154-		`DROP TRIGGER IF EXISTS trg_agent_executions_capacity_admission_delete`,
services/control-plane/internal/database/capacity_reservation_sqlite.go:155:		`CREATE TRIGGER trg_agent_executions_capacity_admission_delete
services/control-plane/internal/database/capacity_reservation_sqlite.go-156-		 AFTER DELETE ON agent_executions
services/control-plane/internal/database/capacity_reservation_sqlite.go-157-		 BEGIN
services/control-plane/internal/database/capacity_reservation_sqlite.go-158-		   DELETE FROM execution_capacity_admissions
services/control-plane/internal/database/capacity_reservation_sqlite.go-159-		   WHERE tenant_id = OLD.tenant_id AND execution_id = OLD.id;
--
services/control-plane/internal/database/credential_bindings_sqlite.go-9-
services/control-plane/internal/database/credential_bindings_sqlite.go-10-func migrateCredentialBindingsSQLiteSafety(ctx context.Context, db *gorm.DB) error {
services/control-plane/internal/database/credential_bindings_sqlite.go-11-	statements := []string{
services/control-plane/internal/database/credential_bindings_sqlite.go-12-		`DROP TRIGGER IF EXISTS trg_provider_credentials_workspace_shape_insert`,
services/control-plane/internal/database/credential_bindings_sqlite.go:13:		`CREATE TRIGGER trg_provider_credentials_workspace_shape_insert
services/control-plane/internal/database/credential_bindings_sqlite.go-14-		 BEFORE INSERT ON provider_credentials
services/control-plane/internal/database/credential_bindings_sqlite.go-15-		 WHEN NEW.purpose NOT IN ('provider', 'git', 'registry', 'package')
services/control-plane/internal/database/credential_bindings_sqlite.go-16-		   OR NOT (
services/control-plane/internal/database/credential_bindings_sqlite.go-17-		     NEW.purpose = 'provider'
--
services/control-plane/internal/database/credential_bindings_sqlite.go-31-		 BEGIN
services/control-plane/internal/database/credential_bindings_sqlite.go-32-		   SELECT RAISE(ABORT, 'invalid Workspace Credential shape');
services/control-plane/internal/database/credential_bindings_sqlite.go-33-		 END`,
services/control-plane/internal/database/credential_bindings_sqlite.go-34-		`DROP TRIGGER IF EXISTS trg_provider_credentials_workspace_shape_update`,
services/control-plane/internal/database/credential_bindings_sqlite.go:35:		`CREATE TRIGGER trg_provider_credentials_workspace_shape_update
services/control-plane/internal/database/credential_bindings_sqlite.go-36-		 BEFORE UPDATE OF purpose, provider, credential_type, scope, selector_organization_id,
services/control-plane/internal/database/credential_bindings_sqlite.go-37-		   selector_model, auto_select_enabled ON provider_credentials
services/control-plane/internal/database/credential_bindings_sqlite.go-38-		 WHEN NEW.purpose NOT IN ('provider', 'git', 'registry', 'package')
services/control-plane/internal/database/credential_bindings_sqlite.go-39-		   OR NOT (
--
services/control-plane/internal/database/credential_bindings_sqlite.go-104-		       AND binding.binding_kind = 'git_fetch'
services/control-plane/internal/database/credential_bindings_sqlite.go-105-		       AND binding.selector_value = project.repository_url
services/control-plane/internal/database/credential_bindings_sqlite.go-106-		       AND binding.disabled_at IS NULL
services/control-plane/internal/database/credential_bindings_sqlite.go-107-		   )`,
services/control-plane/internal/database/credential_bindings_sqlite.go:108:		`CREATE TRIGGER trg_credential_bindings_validate_insert
services/control-plane/internal/database/credential_bindings_sqlite.go-109-		 BEFORE INSERT ON credential_bindings
services/control-plane/internal/database/credential_bindings_sqlite.go-110-		 BEGIN
services/control-plane/internal/database/credential_bindings_sqlite.go-111-		   SELECT RAISE(ABORT, 'invalid Credential Binding owner shape')
services/control-plane/internal/database/credential_bindings_sqlite.go-112-		   WHERE ((NEW.project_id IS NOT NULL) + (NEW.execution_target_id IS NOT NULL)) <> 1
--
services/control-plane/internal/database/credential_bindings_sqlite.go-163-		       )
services/control-plane/internal/database/credential_bindings_sqlite.go-164-		   );
services/control-plane/internal/database/credential_bindings_sqlite.go-165-		 END`,
services/control-plane/internal/database/credential_bindings_sqlite.go-166-		`DROP TRIGGER IF EXISTS trg_credential_bindings_immutable_update`,
services/control-plane/internal/database/credential_bindings_sqlite.go:167:		`CREATE TRIGGER trg_credential_bindings_immutable_update
services/control-plane/internal/database/credential_bindings_sqlite.go-168-		 BEFORE UPDATE ON credential_bindings
services/control-plane/internal/database/credential_bindings_sqlite.go-169-		 BEGIN
services/control-plane/internal/database/credential_bindings_sqlite.go-170-		   SELECT RAISE(ABORT, 'Credential Binding identity is immutable')
services/control-plane/internal/database/credential_bindings_sqlite.go-171-		   WHERE NEW.tenant_id IS NOT OLD.tenant_id
--
services/control-plane/internal/database/credential_bindings_sqlite.go-188-		       AND membership.status = 'active'
services/control-plane/internal/database/credential_bindings_sqlite.go-189-		   );
services/control-plane/internal/database/credential_bindings_sqlite.go-190-		 END`,
services/control-plane/internal/database/credential_bindings_sqlite.go-191-		`DROP TRIGGER IF EXISTS trg_credential_bindings_no_delete`,
services/control-plane/internal/database/credential_bindings_sqlite.go:192:		`CREATE TRIGGER trg_credential_bindings_no_delete
services/control-plane/internal/database/credential_bindings_sqlite.go-193-		 BEFORE DELETE ON credential_bindings
services/control-plane/internal/database/credential_bindings_sqlite.go-194-		 BEGIN
services/control-plane/internal/database/credential_bindings_sqlite.go-195-		   SELECT RAISE(ABORT, 'Credential Binding history cannot be deleted');
services/control-plane/internal/database/credential_bindings_sqlite.go-196-		 END`,
--
services/control-plane/internal/database/credential_bindings_sqlite.go-202-		 ON execution_credential_grants (tenant_id, binding_id, generation, id)`,
services/control-plane/internal/database/credential_bindings_sqlite.go-203-		`CREATE INDEX IF NOT EXISTS idx_execution_credential_grants_credential
services/control-plane/internal/database/credential_bindings_sqlite.go-204-		 ON execution_credential_grants (tenant_id, credential_id, credential_version, id)`,
services/control-plane/internal/database/credential_bindings_sqlite.go-205-		`DROP TRIGGER IF EXISTS trg_execution_credential_grants_validate_insert`,
services/control-plane/internal/database/credential_bindings_sqlite.go:206:		`CREATE TRIGGER trg_execution_credential_grants_validate_insert
services/control-plane/internal/database/credential_bindings_sqlite.go-207-		 BEFORE INSERT ON execution_credential_grants
services/control-plane/internal/database/credential_bindings_sqlite.go-208-		 BEGIN
services/control-plane/internal/database/credential_bindings_sqlite.go-209-		   SELECT RAISE(ABORT, 'Execution Credential Grant generation is fenced')
services/control-plane/internal/database/credential_bindings_sqlite.go-210-		   WHERE NEW.generation <= 0 OR NOT EXISTS (
--
services/control-plane/internal/database/credential_bindings_sqlite.go-231-		       AND (credential.expires_at IS NULL OR credential.expires_at > CURRENT_TIMESTAMP)
services/control-plane/internal/database/credential_bindings_sqlite.go-232-		   );
services/control-plane/internal/database/credential_bindings_sqlite.go-233-		 END`,
services/control-plane/internal/database/credential_bindings_sqlite.go-234-		`DROP TRIGGER IF EXISTS trg_execution_credential_grants_no_update`,
services/control-plane/internal/database/credential_bindings_sqlite.go:235:		`CREATE TRIGGER trg_execution_credential_grants_no_update
services/control-plane/internal/database/credential_bindings_sqlite.go-236-		 BEFORE UPDATE ON execution_credential_grants
services/control-plane/internal/database/credential_bindings_sqlite.go-237-		 BEGIN
services/control-plane/internal/database/credential_bindings_sqlite.go-238-		   SELECT RAISE(ABORT, 'Execution Credential Grants are immutable');
services/control-plane/internal/database/credential_bindings_sqlite.go-239-		 END`,
services/control-plane/internal/database/credential_bindings_sqlite.go-240-		`DROP TRIGGER IF EXISTS trg_execution_credential_grants_no_delete`,
services/control-plane/internal/database/credential_bindings_sqlite.go:241:		`CREATE TRIGGER trg_execution_credential_grants_no_delete
services/control-plane/internal/database/credential_bindings_sqlite.go-242-		 BEFORE DELETE ON execution_credential_grants
services/control-plane/internal/database/credential_bindings_sqlite.go-243-		 BEGIN
services/control-plane/internal/database/credential_bindings_sqlite.go-244-		   SELECT RAISE(ABORT, 'Execution Credential Grants are immutable');
services/control-plane/internal/database/credential_bindings_sqlite.go-245-		 END`,
--
services/control-plane/internal/database/credential_bindings_sqlite.go-249-		 ON execution_provider_credential_grants (tenant_id, execution_id, generation, id)`,
services/control-plane/internal/database/credential_bindings_sqlite.go-250-		`CREATE INDEX IF NOT EXISTS idx_execution_provider_credential_grants_credential
services/control-plane/internal/database/credential_bindings_sqlite.go-251-		 ON execution_provider_credential_grants (tenant_id, credential_id, credential_version, id)`,
services/control-plane/internal/database/credential_bindings_sqlite.go-252-		`DROP TRIGGER IF EXISTS trg_execution_provider_credential_grants_validate_insert`,
services/control-plane/internal/database/credential_bindings_sqlite.go:253:		`CREATE TRIGGER trg_execution_provider_credential_grants_validate_insert
services/control-plane/internal/database/credential_bindings_sqlite.go-254-		 BEFORE INSERT ON execution_provider_credential_grants
services/control-plane/internal/database/credential_bindings_sqlite.go-255-		 BEGIN
services/control-plane/internal/database/credential_bindings_sqlite.go-256-		   SELECT RAISE(ABORT, 'Execution Provider Credential Grant generation is fenced')
services/control-plane/internal/database/credential_bindings_sqlite.go-257-		   WHERE NEW.generation <= 0 OR NOT EXISTS (
--
services/control-plane/internal/database/credential_bindings_sqlite.go-272-		       AND (credential.expires_at IS NULL OR credential.expires_at > CURRENT_TIMESTAMP)
services/control-plane/internal/database/credential_bindings_sqlite.go-273-		   );
services/control-plane/internal/database/credential_bindings_sqlite.go-274-		 END`,
services/control-plane/internal/database/credential_bindings_sqlite.go-275-		`DROP TRIGGER IF EXISTS trg_execution_provider_credential_grants_no_update`,
services/control-plane/internal/database/credential_bindings_sqlite.go:276:		`CREATE TRIGGER trg_execution_provider_credential_grants_no_update
services/control-plane/internal/database/credential_bindings_sqlite.go-277-		 BEFORE UPDATE ON execution_provider_credential_grants
services/control-plane/internal/database/credential_bindings_sqlite.go-278-		 BEGIN
services/control-plane/internal/database/credential_bindings_sqlite.go-279-		   SELECT RAISE(ABORT, 'Execution Provider Credential Grants are immutable');
services/control-plane/internal/database/credential_bindings_sqlite.go-280-		 END`,
services/control-plane/internal/database/credential_bindings_sqlite.go-281-		`DROP TRIGGER IF EXISTS trg_execution_provider_credential_grants_no_delete`,
services/control-plane/internal/database/credential_bindings_sqlite.go:282:		`CREATE TRIGGER trg_execution_provider_credential_grants_no_delete
services/control-plane/internal/database/credential_bindings_sqlite.go-283-		 BEFORE DELETE ON execution_provider_credential_grants
services/control-plane/internal/database/credential_bindings_sqlite.go-284-		 BEGIN
services/control-plane/internal/database/credential_bindings_sqlite.go-285-		   SELECT RAISE(ABORT, 'Execution Provider Credential Grants are immutable');
services/control-plane/internal/database/credential_bindings_sqlite.go-286-		 END`,
--
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-37-		   tenant_id, execution_id, generation, failure_class, first_observed_at,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-38-		   date(first_observed_at), NULL, first_observed_at, first_observed_at
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-39-		 FROM execution_generation_pod_failure_facts`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-40-		`DROP TRIGGER IF EXISTS trg_execution_generation_metric_rollups_insert`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go:41:		`CREATE TRIGGER trg_execution_generation_metric_rollups_insert
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-42-		 BEFORE INSERT ON execution_generation_metric_rollups
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-43-		 BEGIN
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-44-		   SELECT RAISE(ABORT, 'invalid Execution generation metric rollup')
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-45-		   WHERE NEW.bucket_day IS NULL
--
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-87-		        )
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-88-		      );
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-89-		 END`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-90-		`DROP TRIGGER IF EXISTS trg_execution_generation_metric_rollups_update`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go:91:		`CREATE TRIGGER trg_execution_generation_metric_rollups_update
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-92-		 BEFORE UPDATE ON execution_generation_metric_rollups
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-93-		 BEGIN
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-94-		   SELECT RAISE(ABORT, 'Execution generation metric rollup identity is immutable')
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-95-		   WHERE NEW.bucket_day IS NOT OLD.bucket_day
--
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-107-		   WHERE NEW.sample_count < OLD.sample_count
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-108-		      OR julianday(NEW.updated_at) < julianday(OLD.updated_at);
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-109-		 END`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-110-		`DROP TRIGGER IF EXISTS trg_execution_generation_metric_rollups_delete`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go:111:		`CREATE TRIGGER trg_execution_generation_metric_rollups_delete
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-112-		 BEFORE DELETE ON execution_generation_metric_rollups
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-113-		 BEGIN
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-114-		   SELECT RAISE(ABORT, 'Execution generation metric rollups cannot be deleted');
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-115-		 END`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-116-		`DROP TRIGGER IF EXISTS trg_execution_generation_metric_rollup_entries_insert`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go:117:		`CREATE TRIGGER trg_execution_generation_metric_rollup_entries_insert
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-118-		 BEFORE INSERT ON execution_generation_metric_rollup_entries
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-119-		 BEGIN
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-120-		   SELECT RAISE(ABORT, 'invalid Execution generation metric rollup entry')
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-121-		   WHERE NEW.generation <= 0
--
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-135-		          AND fact.terminal_outcome IS NOT NULL
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-136-		      );
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-137-		 END`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-138-		`DROP TRIGGER IF EXISTS trg_execution_generation_metric_rollup_entries_update`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go:139:		`CREATE TRIGGER trg_execution_generation_metric_rollup_entries_update
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-140-		 BEFORE UPDATE ON execution_generation_metric_rollup_entries
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-141-		 BEGIN
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-142-		   SELECT RAISE(ABORT, 'Execution generation metric rollup entry identity is immutable')
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-143-		   WHERE NEW.tenant_id IS NOT OLD.tenant_id
--
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-155-		   WHERE julianday(NEW.updated_at) < julianday(OLD.updated_at)
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-156-		      OR (NEW.rolled_up_at IS NOT NULL AND julianday(NEW.rolled_up_at) < julianday(NEW.created_at));
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-157-		 END`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-158-		`DROP TRIGGER IF EXISTS trg_execution_generation_metric_rollup_entries_delete`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go:159:		`CREATE TRIGGER trg_execution_generation_metric_rollup_entries_delete
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-160-		 BEFORE DELETE ON execution_generation_metric_rollup_entries
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-161-		 BEGIN
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-162-		   SELECT RAISE(ABORT, 'Execution generation metric rollup entries cannot be deleted');
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-163-		 END`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-164-		`DROP TRIGGER IF EXISTS trg_execution_generation_metric_rollup_enqueue_insert`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go:165:		`CREATE TRIGGER trg_execution_generation_metric_rollup_enqueue_insert
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-166-		 AFTER INSERT ON execution_generation_facts
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-167-		 WHEN NEW.dispatch_requested_at IS NOT NULL
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-168-		   AND NEW.terminal_at IS NOT NULL
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-169-		   AND NEW.terminal_outcome IS NOT NULL
--
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-176-		     date(NEW.dispatch_requested_at), NULL, NEW.terminal_at, NEW.terminal_at
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-177-		   );
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-178-		 END`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-179-		`DROP TRIGGER IF EXISTS trg_execution_generation_metric_rollup_enqueue_update`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go:180:		`CREATE TRIGGER trg_execution_generation_metric_rollup_enqueue_update
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-181-		 AFTER UPDATE OF dispatch_requested_at, terminal_at, terminal_outcome ON execution_generation_facts
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-182-		 WHEN NEW.dispatch_requested_at IS NOT NULL
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-183-		   AND NEW.terminal_at IS NOT NULL
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-184-		   AND NEW.terminal_outcome IS NOT NULL
--
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-191-		     date(NEW.dispatch_requested_at), NULL, NEW.terminal_at, NEW.terminal_at
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-192-		   );
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-193-		 END`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-194-		`DROP TRIGGER IF EXISTS trg_execution_generation_metric_source_sealed`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go:195:		`CREATE TRIGGER trg_execution_generation_metric_source_sealed
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-196-		 BEFORE UPDATE ON execution_generation_facts
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-197-		 WHEN EXISTS (
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-198-		   SELECT 1 FROM execution_generation_metric_rollup_entries AS entry
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-199-		   WHERE entry.tenant_id = OLD.tenant_id
--
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-213-		      OR NEW.terminal_at IS NOT OLD.terminal_at
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-214-		      OR NEW.terminal_outcome IS NOT OLD.terminal_outcome;
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-215-		 END`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-216-		`DROP TRIGGER IF EXISTS trg_execution_generation_pod_failure_metric_rollup_entries_insert`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go:217:		`CREATE TRIGGER trg_execution_generation_pod_failure_metric_rollup_entries_insert
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-218-		 BEFORE INSERT ON execution_generation_pod_failure_metric_rollup_entries
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-219-		 BEGIN
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-220-		   SELECT RAISE(ABORT, 'invalid Execution generation Pod failure metric rollup entry')
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-221-		   WHERE NEW.generation <= 0
--
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-232-		          AND failure.first_observed_at IS NEW.first_observed_at
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-233-		      );
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-234-		 END`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-235-		`DROP TRIGGER IF EXISTS trg_execution_generation_pod_failure_metric_rollup_entries_update`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go:236:		`CREATE TRIGGER trg_execution_generation_pod_failure_metric_rollup_entries_update
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-237-		 BEFORE UPDATE ON execution_generation_pod_failure_metric_rollup_entries
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-238-		 BEGIN
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-239-		   SELECT RAISE(ABORT, 'Execution generation Pod failure metric rollup entry identity is immutable')
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-240-		   WHERE NEW.tenant_id IS NOT OLD.tenant_id
--
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-252-		   WHERE julianday(NEW.updated_at) < julianday(OLD.updated_at)
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-253-		      OR (NEW.rolled_up_at IS NOT NULL AND julianday(NEW.rolled_up_at) < julianday(NEW.created_at));
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-254-		 END`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-255-		`DROP TRIGGER IF EXISTS trg_execution_generation_pod_failure_metric_rollup_entries_delete`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go:256:		`CREATE TRIGGER trg_execution_generation_pod_failure_metric_rollup_entries_delete
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-257-		 BEFORE DELETE ON execution_generation_pod_failure_metric_rollup_entries
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-258-		 BEGIN
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-259-		   SELECT RAISE(ABORT, 'Execution generation Pod failure metric rollup entries cannot be deleted');
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-260-		 END`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-261-		`DROP TRIGGER IF EXISTS trg_execution_generation_pod_failure_metric_rollup_enqueue`,
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go:262:		`CREATE TRIGGER trg_execution_generation_pod_failure_metric_rollup_enqueue
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-263-		 AFTER INSERT ON execution_generation_pod_failure_facts
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-264-		 BEGIN
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-265-		   INSERT OR IGNORE INTO execution_generation_pod_failure_metric_rollup_entries (
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go-266-		     tenant_id, execution_id, generation, failure_class, first_observed_at,
--
services/control-plane/internal/database/worker_revocation_sqlite.go-113-		   administrative_status,
services/control-plane/internal/database/worker_revocation_sqlite.go-114-		   id
services/control-plane/internal/database/worker_revocation_sqlite.go-115-		 )`,
services/control-plane/internal/database/worker_revocation_sqlite.go-116-		`DROP TRIGGER IF EXISTS trg_worker_instances_revocation_shape_insert`,
services/control-plane/internal/database/worker_revocation_sqlite.go:117:		`CREATE TRIGGER trg_worker_instances_revocation_shape_insert
services/control-plane/internal/database/worker_revocation_sqlite.go-118-		 BEFORE INSERT ON worker_instances
services/control-plane/internal/database/worker_revocation_sqlite.go-119-		 WHEN NEW.administrative_status IS NULL
services/control-plane/internal/database/worker_revocation_sqlite.go-120-		   OR NEW.administrative_status NOT IN ('active', 'draining', 'revoked')
services/control-plane/internal/database/worker_revocation_sqlite.go-121-		   OR NEW.compatibility_status IS NULL
--
services/control-plane/internal/database/worker_revocation_sqlite.go-163-		 BEGIN
services/control-plane/internal/database/worker_revocation_sqlite.go-164-		   SELECT RAISE(ABORT, 'invalid or revoked Worker administrative identity');
services/control-plane/internal/database/worker_revocation_sqlite.go-165-		 END`,
services/control-plane/internal/database/worker_revocation_sqlite.go-166-		`DROP TRIGGER IF EXISTS trg_worker_instances_revocation_shape_update`,
services/control-plane/internal/database/worker_revocation_sqlite.go:167:		`CREATE TRIGGER trg_worker_instances_revocation_shape_update
services/control-plane/internal/database/worker_revocation_sqlite.go-168-		 BEFORE UPDATE OF administrative_status, revoked_at, revoked_by, revocation_reason, compatibility_status, compatibility_checked_at
services/control-plane/internal/database/worker_revocation_sqlite.go-169-		 ON worker_instances
services/control-plane/internal/database/worker_revocation_sqlite.go-170-		 WHEN NEW.administrative_status IS NULL
services/control-plane/internal/database/worker_revocation_sqlite.go-171-		   OR NEW.administrative_status NOT IN ('active', 'draining', 'revoked')
--
services/control-plane/internal/database/worker_revocation_sqlite.go-215-		 BEGIN
services/control-plane/internal/database/worker_revocation_sqlite.go-216-		   SELECT RAISE(ABORT, 'invalid or revoked Worker administrative identity');
services/control-plane/internal/database/worker_revocation_sqlite.go-217-		 END`,
services/control-plane/internal/database/worker_revocation_sqlite.go-218-		`DROP TRIGGER IF EXISTS trg_worker_instances_identity_immutable`,
services/control-plane/internal/database/worker_revocation_sqlite.go:219:		`CREATE TRIGGER trg_worker_instances_identity_immutable
services/control-plane/internal/database/worker_revocation_sqlite.go-220-		 BEFORE UPDATE OF execution_target_id, cluster_id, namespace, pod_name
services/control-plane/internal/database/worker_revocation_sqlite.go-221-		 ON worker_instances
services/control-plane/internal/database/worker_revocation_sqlite.go-222-		 WHEN NEW.execution_target_id IS NOT OLD.execution_target_id
services/control-plane/internal/database/worker_revocation_sqlite.go-223-		   OR NEW.cluster_id IS NOT OLD.cluster_id
--
services/control-plane/internal/database/worker_revocation_sqlite.go-226-		 BEGIN
services/control-plane/internal/database/worker_revocation_sqlite.go-227-		   SELECT RAISE(ABORT, 'Worker logical identity is immutable');
services/control-plane/internal/database/worker_revocation_sqlite.go-228-		 END`,
services/control-plane/internal/database/worker_revocation_sqlite.go-229-		`DROP TRIGGER IF EXISTS trg_worker_instances_revoked_immutable_update`,
services/control-plane/internal/database/worker_revocation_sqlite.go:230:		`CREATE TRIGGER trg_worker_instances_revoked_immutable_update
services/control-plane/internal/database/worker_revocation_sqlite.go-231-		 BEFORE UPDATE ON worker_instances
services/control-plane/internal/database/worker_revocation_sqlite.go-232-		 WHEN OLD.administrative_status = 'revoked'
services/control-plane/internal/database/worker_revocation_sqlite.go-233-		 BEGIN
services/control-plane/internal/database/worker_revocation_sqlite.go-234-		   SELECT RAISE(ABORT, 'revoked Worker registration is immutable');
services/control-plane/internal/database/worker_revocation_sqlite.go-235-		 END`,
services/control-plane/internal/database/worker_revocation_sqlite.go-236-		`DROP TRIGGER IF EXISTS trg_worker_instances_revoked_immutable_delete`,
services/control-plane/internal/database/worker_revocation_sqlite.go:237:		`CREATE TRIGGER trg_worker_instances_revoked_immutable_delete
services/control-plane/internal/database/worker_revocation_sqlite.go-238-		 BEFORE DELETE ON worker_instances
services/control-plane/internal/database/worker_revocation_sqlite.go-239-		 WHEN OLD.administrative_status = 'revoked'
services/control-plane/internal/database/worker_revocation_sqlite.go-240-		 BEGIN
services/control-plane/internal/database/worker_revocation_sqlite.go-241-		   SELECT RAISE(ABORT, 'revoked Worker registration is immutable');
services/control-plane/internal/database/worker_revocation_sqlite.go-242-		 END`,
services/control-plane/internal/database/worker_revocation_sqlite.go-243-		`DROP TRIGGER IF EXISTS trg_worker_identity_tombstones_source`,
services/control-plane/internal/database/worker_revocation_sqlite.go:244:		`CREATE TRIGGER trg_worker_identity_tombstones_source
services/control-plane/internal/database/worker_revocation_sqlite.go-245-		 BEFORE INSERT ON worker_identity_tombstones
services/control-plane/internal/database/worker_revocation_sqlite.go-246-		 WHEN NOT EXISTS (
services/control-plane/internal/database/worker_revocation_sqlite.go-247-		   SELECT 1
services/control-plane/internal/database/worker_revocation_sqlite.go-248-		   FROM worker_instances AS worker
--
services/control-plane/internal/database/worker_revocation_sqlite.go-260-		 BEGIN
services/control-plane/internal/database/worker_revocation_sqlite.go-261-		   SELECT RAISE(ABORT, 'Worker identity tombstone does not match its revoked Worker source');
services/control-plane/internal/database/worker_revocation_sqlite.go-262-		 END`,
services/control-plane/internal/database/worker_revocation_sqlite.go-263-		`DROP TRIGGER IF EXISTS trg_worker_instances_tombstone_insert`,
services/control-plane/internal/database/worker_revocation_sqlite.go:264:		`CREATE TRIGGER trg_worker_instances_tombstone_insert
services/control-plane/internal/database/worker_revocation_sqlite.go-265-		 AFTER INSERT ON worker_instances
services/control-plane/internal/database/worker_revocation_sqlite.go-266-		 WHEN NEW.administrative_status = 'revoked'
services/control-plane/internal/database/worker_revocation_sqlite.go-267-		 BEGIN
services/control-plane/internal/database/worker_revocation_sqlite.go-268-		   INSERT INTO worker_identity_tombstones (
--
services/control-plane/internal/database/worker_revocation_sqlite.go-287-		     NEW.revocation_reason
services/control-plane/internal/database/worker_revocation_sqlite.go-288-		   );
services/control-plane/internal/database/worker_revocation_sqlite.go-289-		 END`,
services/control-plane/internal/database/worker_revocation_sqlite.go-290-		`DROP TRIGGER IF EXISTS trg_worker_instances_tombstone_update`,
services/control-plane/internal/database/worker_revocation_sqlite.go:291:		`CREATE TRIGGER trg_worker_instances_tombstone_update
services/control-plane/internal/database/worker_revocation_sqlite.go-292-		 AFTER UPDATE OF administrative_status ON worker_instances
services/control-plane/internal/database/worker_revocation_sqlite.go-293-		 WHEN OLD.administrative_status <> 'revoked'
services/control-plane/internal/database/worker_revocation_sqlite.go-294-		   AND NEW.administrative_status = 'revoked'
services/control-plane/internal/database/worker_revocation_sqlite.go-295-		 BEGIN
--
services/control-plane/internal/database/worker_revocation_sqlite.go-315-		     NEW.revocation_reason
services/control-plane/internal/database/worker_revocation_sqlite.go-316-		   );
services/control-plane/internal/database/worker_revocation_sqlite.go-317-		 END`,
services/control-plane/internal/database/worker_revocation_sqlite.go-318-		`DROP TRIGGER IF EXISTS trg_worker_identity_tombstones_immutable_update`,
services/control-plane/internal/database/worker_revocation_sqlite.go:319:		`CREATE TRIGGER trg_worker_identity_tombstones_immutable_update
services/control-plane/internal/database/worker_revocation_sqlite.go-320-		 BEFORE UPDATE ON worker_identity_tombstones
services/control-plane/internal/database/worker_revocation_sqlite.go-321-		 BEGIN
services/control-plane/internal/database/worker_revocation_sqlite.go-322-		   SELECT RAISE(ABORT, 'Worker identity tombstone is immutable');
services/control-plane/internal/database/worker_revocation_sqlite.go-323-		 END`,
services/control-plane/internal/database/worker_revocation_sqlite.go-324-		`DROP TRIGGER IF EXISTS trg_worker_identity_tombstones_immutable_delete`,
services/control-plane/internal/database/worker_revocation_sqlite.go:325:		`CREATE TRIGGER trg_worker_identity_tombstones_immutable_delete
services/control-plane/internal/database/worker_revocation_sqlite.go-326-		 BEFORE DELETE ON worker_identity_tombstones
services/control-plane/internal/database/worker_revocation_sqlite.go-327-		 BEGIN
services/control-plane/internal/database/worker_revocation_sqlite.go-328-		   SELECT RAISE(ABORT, 'Worker identity tombstone is immutable');
services/control-plane/internal/database/worker_revocation_sqlite.go-329-		 END`,
--
services/control-plane/internal/database/store.go-84-func (s *store) Migrate(ctx context.Context, files fs.FS) error {
services/control-plane/internal/database/store.go-85-	if s.kind == platform.MetadataPostgres {
services/control-plane/internal/database/store.go-86-		return Migrate(ctx, s.db, files, s.migrationLockTimeout)
services/control-plane/internal/database/store.go-87-	}
services/control-plane/internal/database/store.go:88:	if err := s.db.WithContext(ctx).AutoMigrate(persistence.AllModels()...); err != nil {
services/control-plane/internal/database/store.go-89-		return fmt.Errorf("auto-migrate sqlite metadata schema: %w", err)
services/control-plane/internal/database/store.go-90-	}
services/control-plane/internal/database/store.go-91-	if err := migrateSQLiteSafety(ctx, s.db); err != nil {
services/control-plane/internal/database/store.go-92-		return err
--
services/control-plane/internal/database/store.go-265-		     ), 'disabled')
services/control-plane/internal/database/store.go-266-		 WHERE warm_pool_mode_snapshot IS NULL
services/control-plane/internal/database/store.go-267-		    OR trim(warm_pool_mode_snapshot) = ''`,
services/control-plane/internal/database/store.go-268-		`DROP TRIGGER IF EXISTS trg_worker_instances_worker_mode_insert`,
services/control-plane/internal/database/store.go:269:		`CREATE TRIGGER trg_worker_instances_worker_mode_insert
services/control-plane/internal/database/store.go-270-		 BEFORE INSERT ON worker_instances
services/control-plane/internal/database/store.go-271-		 WHEN NEW.worker_mode IS NULL
services/control-plane/internal/database/store.go-272-		   OR NEW.worker_mode NOT IN ('execution-pinned', 'warm-pool', 'general-pool')
services/control-plane/internal/database/store.go-273-		 BEGIN
services/control-plane/internal/database/store.go-274-		   SELECT RAISE(ABORT, 'invalid Worker mode');
services/control-plane/internal/database/store.go-275-		 END`,
services/control-plane/internal/database/store.go-276-		`DROP TRIGGER IF EXISTS trg_worker_instances_worker_mode_update`,
services/control-plane/internal/database/store.go:277:		`CREATE TRIGGER trg_worker_instances_worker_mode_update
services/control-plane/internal/database/store.go-278-		 BEFORE UPDATE OF worker_mode ON worker_instances
services/control-plane/internal/database/store.go-279-		 WHEN NEW.worker_mode IS NULL
services/control-plane/internal/database/store.go-280-		   OR NEW.worker_mode NOT IN ('execution-pinned', 'warm-pool', 'general-pool')
services/control-plane/internal/database/store.go-281-		 BEGIN
services/control-plane/internal/database/store.go-282-		   SELECT RAISE(ABORT, 'invalid Worker mode');
services/control-plane/internal/database/store.go-283-		 END`,
services/control-plane/internal/database/store.go-284-		`DROP TRIGGER IF EXISTS trg_worker_instances_identity_insert`,
services/control-plane/internal/database/store.go:285:		`CREATE TRIGGER trg_worker_instances_identity_insert
services/control-plane/internal/database/store.go-286-		 BEFORE INSERT ON worker_instances
services/control-plane/internal/database/store.go-287-		 BEGIN
services/control-plane/internal/database/store.go-288-		   SELECT RAISE(ABORT, 'invalid Worker identity snapshot')
services/control-plane/internal/database/store.go-289-		   WHERE NOT (
--
services/control-plane/internal/database/store.go-327-		       AND pool.capacity_class = NEW.capacity_class
services/control-plane/internal/database/store.go-328-		   );
services/control-plane/internal/database/store.go-329-		 END`,
services/control-plane/internal/database/store.go-330-		`DROP TRIGGER IF EXISTS trg_worker_instances_identity_update`,
services/control-plane/internal/database/store.go:331:		`CREATE TRIGGER trg_worker_instances_identity_update
services/control-plane/internal/database/store.go-332-		 BEFORE UPDATE OF worker_mode, assigned_execution_id, worker_pool_id, worker_pool_version, capacity_class, execution_target_id, target_kind
services/control-plane/internal/database/store.go-333-		 ON worker_instances
services/control-plane/internal/database/store.go-334-		 BEGIN
services/control-plane/internal/database/store.go-335-		   SELECT RAISE(ABORT, 'invalid Worker identity snapshot')
--
services/control-plane/internal/database/store.go-374-		       AND pool.capacity_class = NEW.capacity_class
services/control-plane/internal/database/store.go-375-		   );
services/control-plane/internal/database/store.go-376-		 END`,
services/control-plane/internal/database/store.go-377-		`DROP TRIGGER IF EXISTS trg_agent_executions_warm_pool_mode_snapshot_insert`,
services/control-plane/internal/database/store.go:378:		`CREATE TRIGGER trg_agent_executions_warm_pool_mode_snapshot_insert
services/control-plane/internal/database/store.go-379-		 BEFORE INSERT ON agent_executions
services/control-plane/internal/database/store.go-380-		 WHEN NEW.warm_pool_mode_snapshot IS NULL
services/control-plane/internal/database/store.go-381-		   OR NEW.warm_pool_mode_snapshot NOT IN ('disabled', 'balanced', 'low-latency')
services/control-plane/internal/database/store.go-382-		 BEGIN
services/control-plane/internal/database/store.go-383-		   SELECT RAISE(ABORT, 'invalid warm_pool_mode_snapshot');
services/control-plane/internal/database/store.go-384-		 END`,
services/control-plane/internal/database/store.go-385-		`DROP TRIGGER IF EXISTS trg_agent_executions_warm_pool_mode_snapshot_update`,
services/control-plane/internal/database/store.go:386:		`CREATE TRIGGER trg_agent_executions_warm_pool_mode_snapshot_update
services/control-plane/internal/database/store.go-387-		 BEFORE UPDATE OF warm_pool_mode_snapshot ON agent_executions
services/control-plane/internal/database/store.go-388-		 BEGIN
services/control-plane/internal/database/store.go-389-		   SELECT RAISE(ABORT, 'invalid warm_pool_mode_snapshot')
services/control-plane/internal/database/store.go-390-		   WHERE NEW.warm_pool_mode_snapshot IS NULL
--
services/control-plane/internal/database/store.go-412-		 ON agent_executions (execution_target_id, target_kind, status, queued_at, id)
services/control-plane/internal/database/store.go-413-		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
services/control-plane/internal/database/store.go-414-		`INSERT INTO worker_pools (
services/control-plane/internal/database/store.go-415-		   id, tenant_id, execution_target_id, name, mode, capacity_class,
services/control-plane/internal/database/store.go:416:		   cluster_id, region, namespace, desired_idle_units, max_active_units,
services/control-plane/internal/database/store.go-417-		   scheduling_template, status, version, created_at, updated_at
services/control-plane/internal/database/store.go-418-		 )
services/control-plane/internal/database/store.go-419-		 SELECT target.id, target.tenant_id, target.id, 'default',
services/control-plane/internal/database/store.go-420-		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
--
services/control-plane/internal/database/store.go-435-		   SELECT 1 FROM execution_placement_policies AS policy
services/control-plane/internal/database/store.go-436-		   WHERE policy.execution_target_id = target.id
services/control-plane/internal/database/store.go-437-		 )`,
services/control-plane/internal/database/store.go-438-		`DROP TRIGGER IF EXISTS trg_worker_pools_shape_insert`,
services/control-plane/internal/database/store.go:439:		`CREATE TRIGGER trg_worker_pools_shape_insert
services/control-plane/internal/database/store.go-440-		 BEFORE INSERT ON worker_pools
services/control-plane/internal/database/store.go-441-		 BEGIN
services/control-plane/internal/database/store.go-442-		   SELECT RAISE(ABORT, 'invalid Worker pool shape')
services/control-plane/internal/database/store.go-443-		   WHERE NEW.mode NOT IN ('resident', 'per-execution', 'warm')
services/control-plane/internal/database/store.go-444-		      OR NEW.capacity_class NOT IN ('standard', 'interactive')
services/control-plane/internal/database/store.go-445-		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
services/control-plane/internal/database/store.go-446-		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
services/control-plane/internal/database/store.go:447:		      OR NEW.desired_idle_units < 0
services/control-plane/internal/database/store.go:448:		      OR NEW.max_active_units < NEW.desired_idle_units
services/control-plane/internal/database/store.go-449-		      OR NEW.version <= 0
services/control-plane/internal/database/store.go-450-		      OR json_valid(NEW.scheduling_template) = 0
services/control-plane/internal/database/store.go-451-		      OR json_type(NEW.scheduling_template) <> 'object';
services/control-plane/internal/database/store.go-452-
--
services/control-plane/internal/database/store.go-457-		       AND target.tenant_id IS NEW.tenant_id
services/control-plane/internal/database/store.go-458-		   );
services/control-plane/internal/database/store.go-459-		 END`,
services/control-plane/internal/database/store.go-460-		`DROP TRIGGER IF EXISTS trg_worker_pools_shape_update`,
services/control-plane/internal/database/store.go:461:		`CREATE TRIGGER trg_worker_pools_shape_update
services/control-plane/internal/database/store.go-462-		 BEFORE UPDATE ON worker_pools
services/control-plane/internal/database/store.go-463-		 BEGIN
services/control-plane/internal/database/store.go-464-		   SELECT RAISE(ABORT, 'Worker pool ownership is immutable')
services/control-plane/internal/database/store.go-465-		   WHERE NEW.execution_target_id IS NOT OLD.execution_target_id
--
services/control-plane/internal/database/store.go-470-
services/control-plane/internal/database/store.go-471-		   SELECT RAISE(ABORT, 'invalid Worker pool update')
services/control-plane/internal/database/store.go-472-		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
services/control-plane/internal/database/store.go-473-		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
services/control-plane/internal/database/store.go:474:		      OR NEW.desired_idle_units < 0
services/control-plane/internal/database/store.go:475:		      OR NEW.max_active_units < NEW.desired_idle_units
services/control-plane/internal/database/store.go-476-		      OR NEW.version <> OLD.version + 1
services/control-plane/internal/database/store.go-477-		      OR json_valid(NEW.scheduling_template) = 0
services/control-plane/internal/database/store.go-478-		      OR json_type(NEW.scheduling_template) <> 'object';
services/control-plane/internal/database/store.go-479-
--
services/control-plane/internal/database/store.go-483-		     WHERE policy.default_pool_id = NEW.id
services/control-plane/internal/database/store.go-484-		   );
services/control-plane/internal/database/store.go-485-		 END`,
services/control-plane/internal/database/store.go-486-		`DROP TRIGGER IF EXISTS trg_worker_pools_delete`,
services/control-plane/internal/database/store.go:487:		`CREATE TRIGGER trg_worker_pools_delete
services/control-plane/internal/database/store.go-488-		 BEFORE DELETE ON worker_pools
services/control-plane/internal/database/store.go-489-		 BEGIN
services/control-plane/internal/database/store.go-490-		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
services/control-plane/internal/database/store.go-491-		 END`,
services/control-plane/internal/database/store.go-492-		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
services/control-plane/internal/database/store.go:493:		`CREATE TRIGGER trg_execution_placement_policy_insert
services/control-plane/internal/database/store.go-494-		 BEFORE INSERT ON execution_placement_policies
services/control-plane/internal/database/store.go-495-		 BEGIN
services/control-plane/internal/database/store.go-496-		   SELECT RAISE(ABORT, 'invalid Execution placement policy')
services/control-plane/internal/database/store.go-497-		   WHERE NEW.version <= 0
--
services/control-plane/internal/database/store.go-519-		          AND pool.tenant_id IS NEW.tenant_id
services/control-plane/internal/database/store.go-520-		      ));
services/control-plane/internal/database/store.go-521-		 END`,
services/control-plane/internal/database/store.go-522-		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_update`,
services/control-plane/internal/database/store.go:523:		`CREATE TRIGGER trg_execution_placement_policy_update
services/control-plane/internal/database/store.go-524-		 BEFORE UPDATE ON execution_placement_policies
services/control-plane/internal/database/store.go-525-		 BEGIN
services/control-plane/internal/database/store.go-526-		   SELECT RAISE(ABORT, 'Execution placement policy ownership is immutable')
services/control-plane/internal/database/store.go-527-		   WHERE NEW.execution_target_id IS NOT OLD.execution_target_id
--
services/control-plane/internal/database/store.go-549-		          AND pool.tenant_id IS NEW.tenant_id
services/control-plane/internal/database/store.go-550-		      ));
services/control-plane/internal/database/store.go-551-		 END`,
services/control-plane/internal/database/store.go-552-		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_delete`,
services/control-plane/internal/database/store.go:553:		`CREATE TRIGGER trg_execution_placement_policy_delete
services/control-plane/internal/database/store.go-554-		 BEFORE DELETE ON execution_placement_policies
services/control-plane/internal/database/store.go-555-		 BEGIN
services/control-plane/internal/database/store.go-556-		   SELECT RAISE(ABORT, 'Execution placement policies cannot be deleted');
services/control-plane/internal/database/store.go-557-		 END`,
services/control-plane/internal/database/store.go-558-		`DROP TRIGGER IF EXISTS trg_agent_executions_placement_insert`,
services/control-plane/internal/database/store.go:559:		`CREATE TRIGGER trg_agent_executions_placement_insert
services/control-plane/internal/database/store.go-560-		 BEFORE INSERT ON agent_executions
services/control-plane/internal/database/store.go-561-		 BEGIN
services/control-plane/internal/database/store.go-562-		   SELECT RAISE(ABORT, 'invalid Execution placement snapshot')
services/control-plane/internal/database/store.go-563-		   WHERE NOT (
--
services/control-plane/internal/database/store.go-577-		       ))
services/control-plane/internal/database/store.go-578-		   );
services/control-plane/internal/database/store.go-579-		 END`,
services/control-plane/internal/database/store.go-580-		`DROP TRIGGER IF EXISTS trg_agent_executions_placement_update`,
services/control-plane/internal/database/store.go:581:		`CREATE TRIGGER trg_agent_executions_placement_update
services/control-plane/internal/database/store.go-582-		 BEFORE UPDATE OF worker_pool_id, worker_pool_version, capacity_class, placement_policy_version
services/control-plane/internal/database/store.go-583-		 ON agent_executions
services/control-plane/internal/database/store.go-584-		 WHEN OLD.worker_pool_id IS NOT NULL AND (
services/control-plane/internal/database/store.go-585-		   NEW.worker_pool_id IS NOT OLD.worker_pool_id
--
services/control-plane/internal/database/store.go-723-		 ON execution_generation_pod_failure_facts (
services/control-plane/internal/database/store.go-724-		   failure_class, first_observed_at, execution_target_id
services/control-plane/internal/database/store.go-725-		 )`,
services/control-plane/internal/database/store.go-726-		`DROP TRIGGER IF EXISTS trg_execution_generation_facts_insert`,
services/control-plane/internal/database/store.go:727:		`CREATE TRIGGER trg_execution_generation_facts_insert
services/control-plane/internal/database/store.go-728-		 BEFORE INSERT ON execution_generation_facts
services/control-plane/internal/database/store.go-729-		 BEGIN
services/control-plane/internal/database/store.go-730-		   SELECT RAISE(ABORT, 'invalid Execution generation fact')
services/control-plane/internal/database/store.go-731-		   WHERE NEW.generation <= 0
--
services/control-plane/internal/database/store.go-754-		          AND execution.execution_target_id = NEW.execution_target_id
services/control-plane/internal/database/store.go-755-		      );
services/control-plane/internal/database/store.go-756-		 END`,
services/control-plane/internal/database/store.go-757-		`DROP TRIGGER IF EXISTS trg_execution_generation_facts_update`,
services/control-plane/internal/database/store.go:758:		`CREATE TRIGGER trg_execution_generation_facts_update
services/control-plane/internal/database/store.go-759-		 BEFORE UPDATE ON execution_generation_facts
services/control-plane/internal/database/store.go-760-		 BEGIN
services/control-plane/internal/database/store.go-761-		   SELECT RAISE(ABORT, 'Execution generation fact identity is immutable')
services/control-plane/internal/database/store.go-762-		   WHERE NEW.tenant_id IS NOT OLD.tenant_id
--
services/control-plane/internal/database/store.go-820-		      OR (NEW.pod_last_observed_at IS NOT NULL AND NEW.pod_running_at IS NOT NULL
services/control-plane/internal/database/store.go-821-		       AND NEW.pod_last_observed_at < NEW.pod_running_at);
services/control-plane/internal/database/store.go-822-		 END`,
services/control-plane/internal/database/store.go-823-		`DROP TRIGGER IF EXISTS trg_execution_generation_facts_delete`,
services/control-plane/internal/database/store.go:824:		`CREATE TRIGGER trg_execution_generation_facts_delete
services/control-plane/internal/database/store.go-825-		 BEFORE DELETE ON execution_generation_facts
services/control-plane/internal/database/store.go-826-		 BEGIN
services/control-plane/internal/database/store.go-827-		   SELECT RAISE(ABORT, 'Execution generation facts cannot be deleted');
services/control-plane/internal/database/store.go-828-		 END`,
services/control-plane/internal/database/store.go-829-		`DROP TRIGGER IF EXISTS trg_execution_generation_pod_failure_facts_insert`,
services/control-plane/internal/database/store.go:830:		`CREATE TRIGGER trg_execution_generation_pod_failure_facts_insert
services/control-plane/internal/database/store.go-831-		 BEFORE INSERT ON execution_generation_pod_failure_facts
services/control-plane/internal/database/store.go-832-		 BEGIN
services/control-plane/internal/database/store.go-833-		   SELECT RAISE(ABORT, 'invalid Execution generation Pod failure fact')
services/control-plane/internal/database/store.go-834-		   WHERE NEW.generation <= 0
--
services/control-plane/internal/database/store.go-853-		          AND generation.execution_target_id = NEW.execution_target_id
services/control-plane/internal/database/store.go-854-		      );
services/control-plane/internal/database/store.go-855-		 END`,
services/control-plane/internal/database/store.go-856-		`DROP TRIGGER IF EXISTS trg_execution_generation_pod_failure_facts_update`,
services/control-plane/internal/database/store.go:857:		`CREATE TRIGGER trg_execution_generation_pod_failure_facts_update
services/control-plane/internal/database/store.go-858-		 BEFORE UPDATE ON execution_generation_pod_failure_facts
services/control-plane/internal/database/store.go-859-		 BEGIN
services/control-plane/internal/database/store.go-860-		   SELECT RAISE(ABORT, 'Execution generation Pod failure fact identity is immutable')
services/control-plane/internal/database/store.go-861-		   WHERE NEW.tenant_id IS NOT OLD.tenant_id
--
services/control-plane/internal/database/store.go-872-		   SELECT RAISE(ABORT, 'Execution generation Pod failure observation cannot move backwards')
services/control-plane/internal/database/store.go-873-		   WHERE NEW.last_observed_at IS NULL OR NEW.last_observed_at < OLD.last_observed_at;
services/control-plane/internal/database/store.go-874-		 END`,
services/control-plane/internal/database/store.go-875-		`DROP TRIGGER IF EXISTS trg_execution_generation_pod_failure_facts_delete`,
services/control-plane/internal/database/store.go:876:		`CREATE TRIGGER trg_execution_generation_pod_failure_facts_delete
services/control-plane/internal/database/store.go-877-		 BEFORE DELETE ON execution_generation_pod_failure_facts
services/control-plane/internal/database/store.go-878-		 BEGIN
services/control-plane/internal/database/store.go-879-		   SELECT RAISE(ABORT, 'Execution generation Pod failure facts cannot be deleted');
services/control-plane/internal/database/store.go-880-		 END`,
--
services/control-plane/internal/database/store.go-934-		 ON worker_incarnation_facts (target_kind, pool_mode, capacity_class, current_state)`,
services/control-plane/internal/database/store.go-935-		`CREATE INDEX IF NOT EXISTS idx_worker_incarnation_facts_target_state
services/control-plane/internal/database/store.go-936-		 ON worker_incarnation_facts (execution_target_id, current_state, worker_id, worker_incarnation)`,
services/control-plane/internal/database/store.go-937-		`DROP TRIGGER IF EXISTS trg_worker_incarnation_facts_insert`,
services/control-plane/internal/database/store.go:938:		`CREATE TRIGGER trg_worker_incarnation_facts_insert
services/control-plane/internal/database/store.go-939-		 BEFORE INSERT ON worker_incarnation_facts
services/control-plane/internal/database/store.go-940-		 BEGIN
services/control-plane/internal/database/store.go-941-		   SELECT RAISE(ABORT, 'invalid Worker incarnation fact')
services/control-plane/internal/database/store.go-942-		   WHERE NEW.worker_incarnation <= 0
--
services/control-plane/internal/database/store.go-1020-		        )
services/control-plane/internal/database/store.go-1021-		      );
services/control-plane/internal/database/store.go-1022-		 END`,
services/control-plane/internal/database/store.go-1023-		`DROP TRIGGER IF EXISTS trg_worker_incarnation_facts_update`,
services/control-plane/internal/database/store.go:1024:		`CREATE TRIGGER trg_worker_incarnation_facts_update
services/control-plane/internal/database/store.go-1025-		 BEFORE UPDATE ON worker_incarnation_facts
services/control-plane/internal/database/store.go-1026-		 BEGIN
services/control-plane/internal/database/store.go-1027-		   SELECT RAISE(ABORT, 'Worker incarnation fact identity is immutable')
services/control-plane/internal/database/store.go-1028-		   WHERE NEW.worker_id IS NOT OLD.worker_id
--
services/control-plane/internal/database/store.go-1092-		        ))
services/control-plane/internal/database/store.go-1093-		      );
services/control-plane/internal/database/store.go-1094-		 END`,
services/control-plane/internal/database/store.go-1095-		`DROP TRIGGER IF EXISTS trg_worker_incarnation_facts_delete`,
services/control-plane/internal/database/store.go:1096:		`CREATE TRIGGER trg_worker_incarnation_facts_delete
services/control-plane/internal/database/store.go-1097-		 BEFORE DELETE ON worker_incarnation_facts
services/control-plane/internal/database/store.go-1098-		 BEGIN
services/control-plane/internal/database/store.go-1099-		   SELECT RAISE(ABORT, 'Worker incarnation facts cannot be deleted');
services/control-plane/internal/database/store.go-1100-		 END`,
--
services/control-plane/internal/database/store.go-1113-		 FROM worker_incarnation_facts
services/control-plane/internal/database/store.go-1114-		 WHERE current_state = 'terminated'
services/control-plane/internal/database/store.go-1115-		   AND terminated_at IS NOT NULL`,
services/control-plane/internal/database/store.go-1116-		`DROP TRIGGER IF EXISTS trg_worker_incarnation_metric_rollups_insert`,
services/control-plane/internal/database/store.go:1117:		`CREATE TRIGGER trg_worker_incarnation_metric_rollups_insert
services/control-plane/internal/database/store.go-1118-		 BEFORE INSERT ON worker_incarnation_metric_rollups
services/control-plane/internal/database/store.go-1119-		 BEGIN
services/control-plane/internal/database/store.go-1120-		   SELECT RAISE(ABORT, 'invalid Worker incarnation metric rollup')
services/control-plane/internal/database/store.go-1121-		   WHERE NEW.bucket_day IS NULL
--
services/control-plane/internal/database/store.go-1137-		      OR NEW.requested_ephemeral_storage_byte_seconds >= 1e308
services/control-plane/internal/database/store.go-1138-		      OR NEW.created_at > NEW.updated_at;
services/control-plane/internal/database/store.go-1139-		 END`,
services/control-plane/internal/database/store.go-1140-		`DROP TRIGGER IF EXISTS trg_worker_incarnation_metric_rollups_update`,
services/control-plane/internal/database/store.go:1141:		`CREATE TRIGGER trg_worker_incarnation_metric_rollups_update
services/control-plane/internal/database/store.go-1142-		 BEFORE UPDATE ON worker_incarnation_metric_rollups
services/control-plane/internal/database/store.go-1143-		 BEGIN
services/control-plane/internal/database/store.go-1144-		   SELECT RAISE(ABORT, 'Worker incarnation metric rollup identity is immutable')
services/control-plane/internal/database/store.go-1145-		   WHERE NEW.bucket_day IS NOT OLD.bucket_day
--
services/control-plane/internal/database/store.go-1164-		      OR NEW.requested_ephemeral_storage_byte_seconds >= 1e308
services/control-plane/internal/database/store.go-1165-		      OR NEW.updated_at < OLD.updated_at;
services/control-plane/internal/database/store.go-1166-		 END`,
services/control-plane/internal/database/store.go-1167-		`DROP TRIGGER IF EXISTS trg_worker_incarnation_metric_rollups_delete`,
services/control-plane/internal/database/store.go:1168:		`CREATE TRIGGER trg_worker_incarnation_metric_rollups_delete
services/control-plane/internal/database/store.go-1169-		 BEFORE DELETE ON worker_incarnation_metric_rollups
services/control-plane/internal/database/store.go-1170-		 BEGIN
services/control-plane/internal/database/store.go-1171-		   SELECT RAISE(ABORT, 'Worker incarnation metric rollups cannot be deleted');
services/control-plane/internal/database/store.go-1172-		 END`,
services/control-plane/internal/database/store.go-1173-		`DROP TRIGGER IF EXISTS trg_worker_incarnation_metric_rollup_entries_insert`,
services/control-plane/internal/database/store.go:1174:		`CREATE TRIGGER trg_worker_incarnation_metric_rollup_entries_insert
services/control-plane/internal/database/store.go-1175-		 BEFORE INSERT ON worker_incarnation_metric_rollup_entries
services/control-plane/internal/database/store.go-1176-		 BEGIN
services/control-plane/internal/database/store.go-1177-		   SELECT RAISE(ABORT, 'invalid Worker incarnation metric rollup entry')
services/control-plane/internal/database/store.go-1178-		   WHERE NEW.worker_incarnation <= 0
--
services/control-plane/internal/database/store.go-1189-		          AND fact.terminated_at IS NEW.terminal_at
services/control-plane/internal/database/store.go-1190-		      );
services/control-plane/internal/database/store.go-1191-		 END`,
services/control-plane/internal/database/store.go-1192-		`DROP TRIGGER IF EXISTS trg_worker_incarnation_metric_rollup_entries_update`,
services/control-plane/internal/database/store.go:1193:		`CREATE TRIGGER trg_worker_incarnation_metric_rollup_entries_update
services/control-plane/internal/database/store.go-1194-		 BEFORE UPDATE ON worker_incarnation_metric_rollup_entries
services/control-plane/internal/database/store.go-1195-		 BEGIN
services/control-plane/internal/database/store.go-1196-		   SELECT RAISE(ABORT, 'Worker incarnation metric rollup entry identity is immutable')
services/control-plane/internal/database/store.go-1197-		   WHERE NEW.worker_id IS NOT OLD.worker_id
--
services/control-plane/internal/database/store.go-1208-		   WHERE NEW.updated_at < OLD.updated_at
services/control-plane/internal/database/store.go-1209-		      OR (NEW.rolled_up_at IS NOT NULL AND NEW.rolled_up_at < NEW.created_at);
services/control-plane/internal/database/store.go-1210-		 END`,
services/control-plane/internal/database/store.go-1211-		`DROP TRIGGER IF EXISTS trg_worker_incarnation_metric_rollup_entries_delete`,
services/control-plane/internal/database/store.go:1212:		`CREATE TRIGGER trg_worker_incarnation_metric_rollup_entries_delete
services/control-plane/internal/database/store.go-1213-		 BEFORE DELETE ON worker_incarnation_metric_rollup_entries
services/control-plane/internal/database/store.go-1214-		 BEGIN
services/control-plane/internal/database/store.go-1215-		   SELECT RAISE(ABORT, 'Worker incarnation metric rollup entries cannot be deleted');
services/control-plane/internal/database/store.go-1216-		 END`,
services/control-plane/internal/database/store.go-1217-		`DROP TRIGGER IF EXISTS trg_worker_incarnation_metric_rollup_enqueue_insert`,
services/control-plane/internal/database/store.go:1218:		`CREATE TRIGGER trg_worker_incarnation_metric_rollup_enqueue_insert
services/control-plane/internal/database/store.go-1219-		 AFTER INSERT ON worker_incarnation_facts
services/control-plane/internal/database/store.go-1220-		 WHEN NEW.current_state = 'terminated' AND NEW.terminated_at IS NOT NULL
services/control-plane/internal/database/store.go-1221-		 BEGIN
services/control-plane/internal/database/store.go-1222-		   INSERT OR IGNORE INTO worker_incarnation_metric_rollup_entries (
--
services/control-plane/internal/database/store.go-1227-		     NULL, NEW.terminated_at, NEW.terminated_at
services/control-plane/internal/database/store.go-1228-		   );
services/control-plane/internal/database/store.go-1229-		 END`,
services/control-plane/internal/database/store.go-1230-		`DROP TRIGGER IF EXISTS trg_worker_incarnation_metric_rollup_enqueue_update`,
services/control-plane/internal/database/store.go:1231:		`CREATE TRIGGER trg_worker_incarnation_metric_rollup_enqueue_update
services/control-plane/internal/database/store.go-1232-		 AFTER UPDATE OF current_state, terminated_at ON worker_incarnation_facts
services/control-plane/internal/database/store.go-1233-		 WHEN NEW.current_state = 'terminated'
services/control-plane/internal/database/store.go-1234-		   AND NEW.terminated_at IS NOT NULL
services/control-plane/internal/database/store.go-1235-		   AND OLD.current_state <> 'terminated'
--
services/control-plane/internal/database/store.go-1242-		     NULL, NEW.terminated_at, NEW.terminated_at
services/control-plane/internal/database/store.go-1243-		   );
services/control-plane/internal/database/store.go-1244-		 END`,
services/control-plane/internal/database/store.go-1245-		`DROP TRIGGER IF EXISTS trg_worker_instances_registration_trust_insert`,
services/control-plane/internal/database/store.go:1246:		`CREATE TRIGGER trg_worker_instances_registration_trust_insert
services/control-plane/internal/database/store.go-1247-		 BEFORE INSERT ON worker_instances
services/control-plane/internal/database/store.go-1248-		 WHEN NEW.registration_trust_mode IS NULL
services/control-plane/internal/database/store.go-1249-		   OR NEW.registration_trust_mode NOT IN ('shared-token', 'kubernetes-pod-bound-v1')
services/control-plane/internal/database/store.go-1250-		 BEGIN
services/control-plane/internal/database/store.go-1251-		   SELECT RAISE(ABORT, 'invalid Worker registration trust mode');
services/control-plane/internal/database/store.go-1252-		 END`,
services/control-plane/internal/database/store.go-1253-		`DROP TRIGGER IF EXISTS trg_worker_instances_registration_trust_update`,
services/control-plane/internal/database/store.go:1254:		`CREATE TRIGGER trg_worker_instances_registration_trust_update
services/control-plane/internal/database/store.go-1255-		 BEFORE UPDATE OF registration_trust_mode ON worker_instances
services/control-plane/internal/database/store.go-1256-		 WHEN NEW.registration_trust_mode IS NULL
services/control-plane/internal/database/store.go-1257-		   OR NEW.registration_trust_mode NOT IN ('shared-token', 'kubernetes-pod-bound-v1')
services/control-plane/internal/database/store.go-1258-		 BEGIN
--
services/control-plane/internal/database/store.go-1264-		   AND process_containment_trust_mode = 'none'
services/control-plane/internal/database/store.go-1265-		   AND process_containment_attestation_key_id IS NULL
services/control-plane/internal/database/store.go-1266-		   AND process_containment_attestation_key_sha256 IS NULL`,
services/control-plane/internal/database/store.go-1267-		`DROP TRIGGER IF EXISTS trg_worker_manifest_process_containment_insert`,
services/control-plane/internal/database/store.go:1268:		`CREATE TRIGGER trg_worker_manifest_process_containment_insert
services/control-plane/internal/database/store.go-1269-		 BEFORE INSERT ON worker_manifests
services/control-plane/internal/database/store.go-1270-		 WHEN NOT (
services/control-plane/internal/database/store.go-1271-		   (
services/control-plane/internal/database/store.go-1272-		     NEW.process_containment_mode = 'none'
--
services/control-plane/internal/database/store.go-1310-		 BEGIN
services/control-plane/internal/database/store.go-1311-		   SELECT RAISE(ABORT, 'invalid Worker Manifest process containment evidence');
services/control-plane/internal/database/store.go-1312-		 END`,
services/control-plane/internal/database/store.go-1313-		`DROP TRIGGER IF EXISTS trg_worker_manifest_process_containment_immutable`,
services/control-plane/internal/database/store.go:1314:		`CREATE TRIGGER trg_worker_manifest_process_containment_immutable
services/control-plane/internal/database/store.go-1315-		 BEFORE UPDATE OF process_containment_mode, process_containment_supervisor_version,
services/control-plane/internal/database/store.go-1316-		   process_containment_probe_version, process_containment_probe_sha256,
services/control-plane/internal/database/store.go-1317-		   process_containment_supervisor_identity, process_containment_provider_identity,
services/control-plane/internal/database/store.go-1318-		   process_containment_trust_mode, process_containment_attestation_key_id,
--
services/control-plane/internal/database/store.go-1330-		 BEGIN
services/control-plane/internal/database/store.go-1331-		   SELECT RAISE(ABORT, 'Worker Manifest process containment evidence is immutable');
services/control-plane/internal/database/store.go-1332-		 END`,
services/control-plane/internal/database/store.go-1333-		`DROP TRIGGER IF EXISTS trg_execution_recovery_bundles_insert`,
services/control-plane/internal/database/store.go:1334:		`CREATE TRIGGER trg_execution_recovery_bundles_insert
services/control-plane/internal/database/store.go-1335-		 BEFORE INSERT ON execution_recovery_bundles
services/control-plane/internal/database/store.go-1336-		 BEGIN
services/control-plane/internal/database/store.go-1337-		   SELECT RAISE(ABORT, 'Recovery Bundle shape is invalid')
services/control-plane/internal/database/store.go-1338-		   WHERE NEW.generation IS NULL
--
services/control-plane/internal/database/store.go-1418-		       )
services/control-plane/internal/database/store.go-1419-		     );
services/control-plane/internal/database/store.go-1420-		 END`,
services/control-plane/internal/database/store.go-1421-		`DROP TRIGGER IF EXISTS trg_execution_recovery_bundles_immutable_update`,
services/control-plane/internal/database/store.go:1422:		`CREATE TRIGGER trg_execution_recovery_bundles_immutable_update
services/control-plane/internal/database/store.go-1423-		 BEFORE UPDATE ON execution_recovery_bundles
services/control-plane/internal/database/store.go-1424-		 BEGIN
services/control-plane/internal/database/store.go-1425-		   SELECT RAISE(ABORT, 'Execution Recovery Bundles are immutable');
services/control-plane/internal/database/store.go-1426-		 END`,
services/control-plane/internal/database/store.go-1427-		`DROP TRIGGER IF EXISTS trg_execution_recovery_bundles_immutable_delete`,
services/control-plane/internal/database/store.go:1428:		`CREATE TRIGGER trg_execution_recovery_bundles_immutable_delete
services/control-plane/internal/database/store.go-1429-		 BEFORE DELETE ON execution_recovery_bundles
services/control-plane/internal/database/store.go-1430-		 BEGIN
services/control-plane/internal/database/store.go-1431-		   SELECT RAISE(ABORT, 'Execution Recovery Bundles are immutable');
services/control-plane/internal/database/store.go-1432-		 END`,
services/control-plane/internal/database/store.go-1433-		`DROP TRIGGER IF EXISTS trg_agent_memory_revisions_insert`,
services/control-plane/internal/database/store.go:1434:		`CREATE TRIGGER trg_agent_memory_revisions_insert
services/control-plane/internal/database/store.go-1435-		 BEFORE INSERT ON agent_memory_revisions
services/control-plane/internal/database/store.go-1436-		 BEGIN
services/control-plane/internal/database/store.go-1437-		   SELECT RAISE(ABORT, 'Memory Revision shape is invalid')
services/control-plane/internal/database/store.go-1438-		   WHERE NEW.revision_number IS NULL
--
services/control-plane/internal/database/store.go-1482-		       )
services/control-plane/internal/database/store.go-1483-		   );
services/control-plane/internal/database/store.go-1484-		 END`,
services/control-plane/internal/database/store.go-1485-		`DROP TRIGGER IF EXISTS trg_agent_memory_heads_validate_insert`,
services/control-plane/internal/database/store.go:1486:		`CREATE TRIGGER trg_agent_memory_heads_validate_insert
services/control-plane/internal/database/store.go-1487-		 BEFORE INSERT ON agent_memory_heads
services/control-plane/internal/database/store.go-1488-		 BEGIN
services/control-plane/internal/database/store.go-1489-		   SELECT RAISE(ABORT, 'Memory Head scope, key, or version shape is invalid')
services/control-plane/internal/database/store.go-1490-		   WHERE NEW.scope_type IS NULL
--
services/control-plane/internal/database/store.go-1539-		         AND revision.revision_number = NEW.version
services/control-plane/internal/database/store.go-1540-		     );
services/control-plane/internal/database/store.go-1541-		 END`,
services/control-plane/internal/database/store.go-1542-		`DROP TRIGGER IF EXISTS trg_agent_memory_heads_validate_update`,
services/control-plane/internal/database/store.go:1543:		`CREATE TRIGGER trg_agent_memory_heads_validate_update
services/control-plane/internal/database/store.go-1544-		 BEFORE UPDATE ON agent_memory_heads
services/control-plane/internal/database/store.go-1545-		 BEGIN
services/control-plane/internal/database/store.go-1546-		   SELECT RAISE(ABORT, 'Memory Head scope, key, or version shape is invalid')
services/control-plane/internal/database/store.go-1547-		   WHERE NEW.scope_type IS NULL
--
services/control-plane/internal/database/store.go-1587-		   SELECT RAISE(ABORT, 'Enabled Memory Head requires a current Revision')
services/control-plane/internal/database/store.go-1588-		   WHERE NEW.enabled = 1 AND NEW.current_revision_id IS NULL;
services/control-plane/internal/database/store.go-1589-		 END`,
services/control-plane/internal/database/store.go-1590-		`DROP TRIGGER IF EXISTS trg_agent_memory_revisions_immutable_update`,
services/control-plane/internal/database/store.go:1591:		`CREATE TRIGGER trg_agent_memory_revisions_immutable_update
services/control-plane/internal/database/store.go-1592-		 BEFORE UPDATE ON agent_memory_revisions
services/control-plane/internal/database/store.go-1593-		 BEGIN
services/control-plane/internal/database/store.go-1594-		   SELECT RAISE(ABORT, 'Agent Memory Revisions are immutable');
services/control-plane/internal/database/store.go-1595-		 END`,
services/control-plane/internal/database/store.go-1596-		`DROP TRIGGER IF EXISTS trg_agent_memory_revisions_immutable_delete`,
services/control-plane/internal/database/store.go:1597:		`CREATE TRIGGER trg_agent_memory_revisions_immutable_delete
services/control-plane/internal/database/store.go-1598-		 BEFORE DELETE ON agent_memory_revisions
services/control-plane/internal/database/store.go-1599-		 BEGIN
services/control-plane/internal/database/store.go-1600-		   SELECT RAISE(ABORT, 'Agent Memory Revisions are immutable');
services/control-plane/internal/database/store.go-1601-		 END`,
services/control-plane/internal/database/store.go-1602-		`DROP TRIGGER IF EXISTS trg_artifacts_agent_memory_protected`,
services/control-plane/internal/database/store.go:1603:		`CREATE TRIGGER trg_artifacts_agent_memory_protected
services/control-plane/internal/database/store.go-1604-		 BEFORE UPDATE OF kind, status, deleted_at, sha256, content_type, size_bytes, object_key ON artifacts
services/control-plane/internal/database/store.go-1605-		 WHEN EXISTS (
services/control-plane/internal/database/store.go-1606-		   SELECT 1 FROM agent_memory_revisions AS revision
services/control-plane/internal/database/store.go-1607-		   WHERE revision.tenant_id = OLD.tenant_id
--
services/control-plane/internal/database/store.go-1618-		 BEGIN
services/control-plane/internal/database/store.go-1619-		   SELECT RAISE(ABORT, 'Artifact is pinned by an immutable Agent Memory Revision');
services/control-plane/internal/database/store.go-1620-		 END`,
services/control-plane/internal/database/store.go-1621-		`DROP TRIGGER IF EXISTS trg_artifacts_agent_memory_protected_delete`,
services/control-plane/internal/database/store.go:1622:		`CREATE TRIGGER trg_artifacts_agent_memory_protected_delete
services/control-plane/internal/database/store.go-1623-		 BEFORE DELETE ON artifacts
services/control-plane/internal/database/store.go-1624-		 WHEN EXISTS (
services/control-plane/internal/database/store.go-1625-		   SELECT 1 FROM agent_memory_revisions AS revision
services/control-plane/internal/database/store.go-1626-		   WHERE revision.tenant_id = OLD.tenant_id
--
services/control-plane/internal/database/store.go-1629-		 BEGIN
services/control-plane/internal/database/store.go-1630-		   SELECT RAISE(ABORT, 'Artifact is pinned by an immutable Agent Memory Revision');
services/control-plane/internal/database/store.go-1631-		 END`,
services/control-plane/internal/database/store.go-1632-		`DROP TRIGGER IF EXISTS trg_agent_sessions_lifecycle_policy_immutable`,
services/control-plane/internal/database/store.go:1633:		`CREATE TRIGGER trg_agent_sessions_lifecycle_policy_immutable
services/control-plane/internal/database/store.go-1634-		 BEFORE UPDATE OF waiting_keep_alive_seconds, suspend_after_idle_seconds,
services/control-plane/internal/database/store.go-1635-		   absolute_session_lifetime_seconds, workspace_retention_days, warm_pool_mode
services/control-plane/internal/database/store.go-1636-		 ON agent_sessions
services/control-plane/internal/database/store.go-1637-		 WHEN NEW.waiting_keep_alive_seconds IS NOT OLD.waiting_keep_alive_seconds
--
services/control-plane/internal/database/store.go-1642-		 BEGIN
services/control-plane/internal/database/store.go-1643-		   SELECT RAISE(ABORT, 'Session Resource Lifecycle Policy snapshot is immutable');
services/control-plane/internal/database/store.go-1644-		 END`,
services/control-plane/internal/database/store.go-1645-		`DROP TRIGGER IF EXISTS trg_agent_sessions_resource_lifecycle_validate_insert`,
services/control-plane/internal/database/store.go:1646:		`CREATE TRIGGER trg_agent_sessions_resource_lifecycle_validate_insert
services/control-plane/internal/database/store.go-1647-		 BEFORE INSERT ON agent_sessions
services/control-plane/internal/database/store.go-1648-		 WHEN NEW.created_at IS NULL
services/control-plane/internal/database/store.go-1649-		   OR NEW.last_event_sequence IS NULL
services/control-plane/internal/database/store.go-1650-		   OR NEW.meaningful_activity_sequence IS NULL
--
services/control-plane/internal/database/store.go-1676-		 BEGIN
services/control-plane/internal/database/store.go-1677-		   SELECT RAISE(ABORT, 'Session Resource Lifecycle fields are invalid');
services/control-plane/internal/database/store.go-1678-		 END`,
services/control-plane/internal/database/store.go-1679-		`DROP TRIGGER IF EXISTS trg_agent_sessions_resource_lifecycle_validate_update`,
services/control-plane/internal/database/store.go:1680:		`CREATE TRIGGER trg_agent_sessions_resource_lifecycle_validate_update
services/control-plane/internal/database/store.go-1681-		 BEFORE UPDATE OF resource_state, meaningful_activity_at, meaningful_activity_sequence,
services/control-plane/internal/database/store.go-1682-		   resource_idle_since, absolute_expires_at, created_at, last_event_sequence,
services/control-plane/internal/database/store.go-1683-		   waiting_keep_alive_seconds, suspend_after_idle_seconds, absolute_session_lifetime_seconds,
services/control-plane/internal/database/store.go-1684-		   workspace_retention_days, warm_pool_mode ON agent_sessions
--
services/control-plane/internal/database/store.go-1713-		 BEGIN
services/control-plane/internal/database/store.go-1714-		   SELECT RAISE(ABORT, 'Session Resource Lifecycle fields are invalid');
services/control-plane/internal/database/store.go-1715-		 END`,
services/control-plane/internal/database/store.go-1716-		`DROP TRIGGER IF EXISTS trg_tenant_resource_lifecycle_policy_validate_insert`,
services/control-plane/internal/database/store.go:1717:		`CREATE TRIGGER trg_tenant_resource_lifecycle_policy_validate_insert
services/control-plane/internal/database/store.go-1718-		 BEFORE INSERT ON tenant_resource_lifecycle_policies
services/control-plane/internal/database/store.go-1719-		 WHEN NEW.version IS NULL
services/control-plane/internal/database/store.go-1720-		   OR (NEW.waiting_keep_alive_seconds IS NOT NULL AND NEW.waiting_keep_alive_seconds NOT BETWEEN 60 AND 86400)
services/control-plane/internal/database/store.go-1721-		   OR (NEW.suspend_after_idle_seconds IS NOT NULL AND NEW.suspend_after_idle_seconds NOT BETWEEN 60 AND 604800)
--
services/control-plane/internal/database/store.go-1726-		 BEGIN
services/control-plane/internal/database/store.go-1727-		   SELECT RAISE(ABORT, 'Tenant Resource Lifecycle Policy is invalid');
services/control-plane/internal/database/store.go-1728-		 END`,
services/control-plane/internal/database/store.go-1729-		`DROP TRIGGER IF EXISTS trg_tenant_resource_lifecycle_policy_parent_insert`,
services/control-plane/internal/database/store.go:1730:		`CREATE TRIGGER trg_tenant_resource_lifecycle_policy_parent_insert
services/control-plane/internal/database/store.go-1731-		 BEFORE INSERT ON tenant_resource_lifecycle_policies
services/control-plane/internal/database/store.go-1732-		 BEGIN
services/control-plane/internal/database/store.go-1733-		   SELECT RAISE(ABORT, 'Tenant Resource Lifecycle Policy must reference an existing Tenant')
services/control-plane/internal/database/store.go-1734-		   WHERE NOT EXISTS (
--
services/control-plane/internal/database/store.go-1742-		     WHERE user.id = NEW.updated_by
services/control-plane/internal/database/store.go-1743-		   );
services/control-plane/internal/database/store.go-1744-		 END`,
services/control-plane/internal/database/store.go-1745-		`DROP TRIGGER IF EXISTS trg_tenant_resource_lifecycle_policy_validate_update`,
services/control-plane/internal/database/store.go:1746:		`CREATE TRIGGER trg_tenant_resource_lifecycle_policy_validate_update
services/control-plane/internal/database/store.go-1747-		 BEFORE UPDATE ON tenant_resource_lifecycle_policies
services/control-plane/internal/database/store.go-1748-		 WHEN NEW.version IS NULL
services/control-plane/internal/database/store.go-1749-		   OR (NEW.waiting_keep_alive_seconds IS NOT NULL AND NEW.waiting_keep_alive_seconds NOT BETWEEN 60 AND 86400)
services/control-plane/internal/database/store.go-1750-		   OR (NEW.suspend_after_idle_seconds IS NOT NULL AND NEW.suspend_after_idle_seconds NOT BETWEEN 60 AND 604800)
--
services/control-plane/internal/database/store.go-1755-		 BEGIN
services/control-plane/internal/database/store.go-1756-		   SELECT RAISE(ABORT, 'Tenant Resource Lifecycle Policy is invalid');
services/control-plane/internal/database/store.go-1757-		 END`,
services/control-plane/internal/database/store.go-1758-		`DROP TRIGGER IF EXISTS trg_tenant_resource_lifecycle_policy_parent_update`,
services/control-plane/internal/database/store.go:1759:		`CREATE TRIGGER trg_tenant_resource_lifecycle_policy_parent_update
services/control-plane/internal/database/store.go-1760-		 BEFORE UPDATE ON tenant_resource_lifecycle_policies
services/control-plane/internal/database/store.go-1761-		 BEGIN
services/control-plane/internal/database/store.go-1762-		   SELECT RAISE(ABORT, 'Tenant Resource Lifecycle Policy must reference an existing Tenant')
services/control-plane/internal/database/store.go-1763-		   WHERE NOT EXISTS (
--
services/control-plane/internal/database/store.go-1771-		     WHERE user.id = NEW.updated_by
services/control-plane/internal/database/store.go-1772-		   );
services/control-plane/internal/database/store.go-1773-		 END`,
services/control-plane/internal/database/store.go-1774-		`DROP TRIGGER IF EXISTS trg_project_resource_lifecycle_policy_validate_insert`,
services/control-plane/internal/database/store.go:1775:		`CREATE TRIGGER trg_project_resource_lifecycle_policy_validate_insert
services/control-plane/internal/database/store.go-1776-		 BEFORE INSERT ON project_resource_lifecycle_policies
services/control-plane/internal/database/store.go-1777-		 WHEN NEW.version IS NULL
services/control-plane/internal/database/store.go-1778-		   OR (NEW.waiting_keep_alive_seconds IS NOT NULL AND NEW.waiting_keep_alive_seconds NOT BETWEEN 60 AND 86400)
services/control-plane/internal/database/store.go-1779-		   OR (NEW.suspend_after_idle_seconds IS NOT NULL AND NEW.suspend_after_idle_seconds NOT BETWEEN 60 AND 604800)
--
services/control-plane/internal/database/store.go-1784-		 BEGIN
services/control-plane/internal/database/store.go-1785-		   SELECT RAISE(ABORT, 'Project Resource Lifecycle Policy is invalid');
services/control-plane/internal/database/store.go-1786-		 END`,
services/control-plane/internal/database/store.go-1787-		`DROP TRIGGER IF EXISTS trg_project_resource_lifecycle_policy_parent_insert`,
services/control-plane/internal/database/store.go:1788:		`CREATE TRIGGER trg_project_resource_lifecycle_policy_parent_insert
services/control-plane/internal/database/store.go-1789-		 BEFORE INSERT ON project_resource_lifecycle_policies
services/control-plane/internal/database/store.go-1790-		 BEGIN
services/control-plane/internal/database/store.go-1791-		   SELECT RAISE(ABORT, 'Project Resource Lifecycle Policy must reference an existing Tenant Project')
services/control-plane/internal/database/store.go-1792-		   WHERE NOT EXISTS (
--
services/control-plane/internal/database/store.go-1801-		     WHERE user.id = NEW.updated_by
services/control-plane/internal/database/store.go-1802-		   );
services/control-plane/internal/database/store.go-1803-		 END`,
services/control-plane/internal/database/store.go-1804-		`DROP TRIGGER IF EXISTS trg_project_resource_lifecycle_policy_validate_update`,
services/control-plane/internal/database/store.go:1805:		`CREATE TRIGGER trg_project_resource_lifecycle_policy_validate_update
services/control-plane/internal/database/store.go-1806-		 BEFORE UPDATE ON project_resource_lifecycle_policies
services/control-plane/internal/database/store.go-1807-		 WHEN NEW.version IS NULL
services/control-plane/internal/database/store.go-1808-		   OR (NEW.waiting_keep_alive_seconds IS NOT NULL AND NEW.waiting_keep_alive_seconds NOT BETWEEN 60 AND 86400)
services/control-plane/internal/database/store.go-1809-		   OR (NEW.suspend_after_idle_seconds IS NOT NULL AND NEW.suspend_after_idle_seconds NOT BETWEEN 60 AND 604800)
--
services/control-plane/internal/database/store.go-1814-		 BEGIN
services/control-plane/internal/database/store.go-1815-		   SELECT RAISE(ABORT, 'Project Resource Lifecycle Policy is invalid');
services/control-plane/internal/database/store.go-1816-		 END`,
services/control-plane/internal/database/store.go-1817-		`DROP TRIGGER IF EXISTS trg_project_resource_lifecycle_policy_parent_update`,
services/control-plane/internal/database/store.go:1818:		`CREATE TRIGGER trg_project_resource_lifecycle_policy_parent_update
services/control-plane/internal/database/store.go-1819-		 BEFORE UPDATE ON project_resource_lifecycle_policies
services/control-plane/internal/database/store.go-1820-		 BEGIN
services/control-plane/internal/database/store.go-1821-		   SELECT RAISE(ABORT, 'Project Resource Lifecycle Policy must reference an existing Tenant Project')
services/control-plane/internal/database/store.go-1822-		   WHERE NOT EXISTS (
--
services/control-plane/internal/database/store.go-1839-		`CREATE UNIQUE INDEX IF NOT EXISTS uq_execution_control_commands_primary_operation
services/control-plane/internal/database/store.go-1840-		 ON execution_control_commands (tenant_id, execution_id)
services/control-plane/internal/database/store.go-1841-		 WHERE command_type IN ('CompactSession', 'RollbackSession', 'ForkSession', 'StartReview')`,
services/control-plane/internal/database/store.go-1842-		`DROP TRIGGER IF EXISTS trg_agent_turns_advanced_shape_insert`,
services/control-plane/internal/database/store.go:1843:		`CREATE TRIGGER trg_agent_turns_advanced_shape_insert
services/control-plane/internal/database/store.go-1844-		 BEFORE INSERT ON agent_turns
services/control-plane/internal/database/store.go-1845-		 WHEN NEW.turn_kind IS NULL
services/control-plane/internal/database/store.go-1846-		   OR NEW.turn_kind NOT IN ('message', 'compact', 'review', 'rollback', 'fork')
services/control-plane/internal/database/store.go-1847-		   OR NEW.input_text IS NULL
--
services/control-plane/internal/database/store.go-1850-		 BEGIN
services/control-plane/internal/database/store.go-1851-		   SELECT RAISE(ABORT, 'invalid advanced Turn kind or input shape');
services/control-plane/internal/database/store.go-1852-		 END`,
services/control-plane/internal/database/store.go-1853-		`DROP TRIGGER IF EXISTS trg_agent_turns_advanced_shape_update`,
services/control-plane/internal/database/store.go:1854:		`CREATE TRIGGER trg_agent_turns_advanced_shape_update
services/control-plane/internal/database/store.go-1855-		 BEFORE UPDATE OF turn_kind, input_text ON agent_turns
services/control-plane/internal/database/store.go-1856-		 WHEN NEW.turn_kind IS NOT OLD.turn_kind
services/control-plane/internal/database/store.go-1857-		   OR NEW.turn_kind IS NULL
services/control-plane/internal/database/store.go-1858-		   OR NEW.turn_kind NOT IN ('message', 'compact', 'review', 'rollback', 'fork')
--
services/control-plane/internal/database/store.go-1862-		 BEGIN
services/control-plane/internal/database/store.go-1863-		   SELECT RAISE(ABORT, 'invalid or mutable advanced Turn kind');
services/control-plane/internal/database/store.go-1864-		 END`,
services/control-plane/internal/database/store.go-1865-		`DROP TRIGGER IF EXISTS trg_agent_sessions_fork_lineage_insert`,
services/control-plane/internal/database/store.go:1866:		`CREATE TRIGGER trg_agent_sessions_fork_lineage_insert
services/control-plane/internal/database/store.go-1867-		 BEFORE INSERT ON agent_sessions
services/control-plane/internal/database/store.go-1868-		 BEGIN
services/control-plane/internal/database/store.go-1869-		   SELECT RAISE(ABORT, 'invalid Fork lineage shape')
services/control-plane/internal/database/store.go-1870-		   WHERE NOT (
--
services/control-plane/internal/database/store.go-1973-		         AND json_extract(source_event.payload, '$.turnId') = NEW.fork_source_turn_id
services/control-plane/internal/database/store.go-1974-		     );
services/control-plane/internal/database/store.go-1975-		 END`,
services/control-plane/internal/database/store.go-1976-		`DROP TRIGGER IF EXISTS trg_agent_sessions_fork_lineage_immutable`,
services/control-plane/internal/database/store.go:1977:		`CREATE TRIGGER trg_agent_sessions_fork_lineage_immutable
services/control-plane/internal/database/store.go-1978-		 BEFORE UPDATE OF fork_source_session_id, fork_source_turn_id, fork_source_event_sequence, fork_strategy
services/control-plane/internal/database/store.go-1979-		 ON agent_sessions
services/control-plane/internal/database/store.go-1980-		 WHEN NEW.fork_source_session_id IS NOT OLD.fork_source_session_id
services/control-plane/internal/database/store.go-1981-		   OR NEW.fork_source_turn_id IS NOT OLD.fork_source_turn_id
--
services/control-plane/internal/database/store.go-1984-		 BEGIN
services/control-plane/internal/database/store.go-1985-		   SELECT RAISE(ABORT, 'Fork lineage is immutable');
services/control-plane/internal/database/store.go-1986-		 END`,
services/control-plane/internal/database/store.go-1987-		`DROP TRIGGER IF EXISTS trg_execution_control_commands_primary_kind_insert`,
services/control-plane/internal/database/store.go:1988:		`CREATE TRIGGER trg_execution_control_commands_primary_kind_insert
services/control-plane/internal/database/store.go-1989-		 BEFORE INSERT ON execution_control_commands
services/control-plane/internal/database/store.go-1990-		 WHEN NEW.command_type IN ('CompactSession', 'RollbackSession', 'ForkSession', 'StartReview')
services/control-plane/internal/database/store.go-1991-		   AND NOT EXISTS (
services/control-plane/internal/database/store.go-1992-		     SELECT 1
--
services/control-plane/internal/database/store.go-2009-		 BEGIN
services/control-plane/internal/database/store.go-2010-		   SELECT RAISE(ABORT, 'Primary Control command does not match the Execution Turn kind');
services/control-plane/internal/database/store.go-2011-		 END`,
services/control-plane/internal/database/store.go-2012-		`DROP TRIGGER IF EXISTS trg_execution_control_commands_primary_kind_update`,
services/control-plane/internal/database/store.go:2013:		`CREATE TRIGGER trg_execution_control_commands_primary_kind_update
services/control-plane/internal/database/store.go-2014-		 BEFORE UPDATE OF tenant_id, execution_id, session_id, turn_id, command_type
services/control-plane/internal/database/store.go-2015-		 ON execution_control_commands
services/control-plane/internal/database/store.go-2016-		 WHEN NEW.command_type IN ('CompactSession', 'RollbackSession', 'ForkSession', 'StartReview')
services/control-plane/internal/database/store.go-2017-		   AND NOT EXISTS (
--
services/control-plane/internal/database/store.go-2035-		 BEGIN
services/control-plane/internal/database/store.go-2036-		   SELECT RAISE(ABORT, 'Primary Control command does not match the Execution Turn kind');
services/control-plane/internal/database/store.go-2037-		 END`,
services/control-plane/internal/database/store.go-2038-		`DROP TRIGGER IF EXISTS trg_execution_control_commands_primary_preserved_update`,
services/control-plane/internal/database/store.go:2039:		`CREATE TRIGGER trg_execution_control_commands_primary_preserved_update
services/control-plane/internal/database/store.go-2040-		 BEFORE UPDATE OF tenant_id, execution_id, session_id, turn_id, command_type
services/control-plane/internal/database/store.go-2041-		 ON execution_control_commands
services/control-plane/internal/database/store.go-2042-		 WHEN OLD.command_type IN ('CompactSession', 'RollbackSession', 'ForkSession', 'StartReview')
services/control-plane/internal/database/store.go-2043-		   AND (
--
services/control-plane/internal/database/store.go-2050-		 BEGIN
services/control-plane/internal/database/store.go-2051-		   SELECT RAISE(ABORT, 'Special Turn Execution must retain its matching primary Control command');
services/control-plane/internal/database/store.go-2052-		 END`,
services/control-plane/internal/database/store.go-2053-		`DROP TRIGGER IF EXISTS trg_execution_control_commands_primary_preserved_delete`,
services/control-plane/internal/database/store.go:2054:		`CREATE TRIGGER trg_execution_control_commands_primary_preserved_delete
services/control-plane/internal/database/store.go-2055-		 BEFORE DELETE ON execution_control_commands
services/control-plane/internal/database/store.go-2056-		 WHEN OLD.command_type IN ('CompactSession', 'RollbackSession', 'ForkSession', 'StartReview')
services/control-plane/internal/database/store.go-2057-		   AND EXISTS (
services/control-plane/internal/database/store.go-2058-		     SELECT 1
--
services/control-plane/internal/database/store.go-2075-		 BEGIN
services/control-plane/internal/database/store.go-2076-		   SELECT RAISE(ABORT, 'Special Turn Execution must retain its matching primary Control command');
services/control-plane/internal/database/store.go-2077-		 END`,
services/control-plane/internal/database/store.go-2078-		`DROP TRIGGER IF EXISTS trg_agent_executions_control_commands_cascade`,
services/control-plane/internal/database/store.go:2079:		`CREATE TRIGGER trg_agent_executions_control_commands_cascade
services/control-plane/internal/database/store.go-2080-		 AFTER DELETE ON agent_executions
services/control-plane/internal/database/store.go-2081-		 BEGIN
services/control-plane/internal/database/store.go-2082-		   DELETE FROM execution_control_commands
services/control-plane/internal/database/store.go-2083-		   WHERE tenant_id = OLD.tenant_id AND execution_id = OLD.id;
--
services/control-plane/internal/database/store.go-2094-		   SELECT 1 FROM worker_instances AS worker
services/control-plane/internal/database/store.go-2095-		   WHERE worker.id = worker_leases.worker_id
services/control-plane/internal/database/store.go-2096-		 )`,
services/control-plane/internal/database/store.go-2097-		`DROP TRIGGER IF EXISTS trg_worker_leases_matches_execution_insert`,
services/control-plane/internal/database/store.go:2098:		`CREATE TRIGGER trg_worker_leases_matches_execution_insert
services/control-plane/internal/database/store.go-2099-		 BEFORE INSERT ON worker_leases
services/control-plane/internal/database/store.go-2100-		 BEGIN
services/control-plane/internal/database/store.go-2101-		   SELECT RAISE(ABORT, 'worker lease does not match the current execution generation')
services/control-plane/internal/database/store.go-2102-		   WHERE NOT EXISTS (
--
services/control-plane/internal/database/store.go-2117-		       )
services/control-plane/internal/database/store.go-2118-		   );
services/control-plane/internal/database/store.go-2119-		 END`,
services/control-plane/internal/database/store.go-2120-		`DROP TRIGGER IF EXISTS trg_worker_leases_matches_execution_update`,
services/control-plane/internal/database/store.go:2121:		`CREATE TRIGGER trg_worker_leases_matches_execution_update
services/control-plane/internal/database/store.go-2122-		 BEFORE UPDATE OF tenant_id, execution_id, worker_id, worker_incarnation, worker_instance_uid, generation ON worker_leases
services/control-plane/internal/database/store.go-2123-		 BEGIN
services/control-plane/internal/database/store.go-2124-		   SELECT RAISE(ABORT, 'worker lease does not match the current execution generation')
services/control-plane/internal/database/store.go-2125-		   WHERE NOT EXISTS (
--
services/control-plane/internal/database/store.go-2140-		       )
services/control-plane/internal/database/store.go-2141-		   );
services/control-plane/internal/database/store.go-2142-		 END`,
services/control-plane/internal/database/store.go-2143-		`DROP TRIGGER IF EXISTS trg_worker_leases_incarnation_immutable`,
services/control-plane/internal/database/store.go:2144:		`CREATE TRIGGER trg_worker_leases_incarnation_immutable
services/control-plane/internal/database/store.go-2145-		 BEFORE UPDATE OF worker_id, worker_incarnation, worker_instance_uid ON worker_leases
services/control-plane/internal/database/store.go-2146-		 WHEN NEW.worker_id IS NOT OLD.worker_id
services/control-plane/internal/database/store.go-2147-		   OR NEW.worker_incarnation IS NOT OLD.worker_incarnation
services/control-plane/internal/database/store.go-2148-		   OR NEW.worker_instance_uid IS NOT OLD.worker_instance_uid
services/control-plane/internal/database/store.go-2149-		 BEGIN
services/control-plane/internal/database/store.go-2150-		   SELECT RAISE(ABORT, 'Worker lease incarnation lineage is immutable');
services/control-plane/internal/database/store.go-2151-		 END`,
services/control-plane/internal/database/store.go-2152-		`DROP TRIGGER IF EXISTS trg_worker_leases_provider_credential_access_insert`,
services/control-plane/internal/database/store.go:2153:		`CREATE TRIGGER trg_worker_leases_provider_credential_access_insert
services/control-plane/internal/database/store.go-2154-		 BEFORE INSERT ON worker_leases
services/control-plane/internal/database/store.go-2155-		 BEGIN
services/control-plane/internal/database/store.go-2156-		   SELECT RAISE(ABORT, 'Worker lease Provider Credential access fields are invalid')
services/control-plane/internal/database/store.go-2157-		   WHERE NOT (
--
services/control-plane/internal/database/store.go-2203-		         AND grant.generation = NEW.generation
services/control-plane/internal/database/store.go-2204-		     );
services/control-plane/internal/database/store.go-2205-		 END`,
services/control-plane/internal/database/store.go-2206-		`DROP TRIGGER IF EXISTS trg_worker_leases_provider_credential_access_update`,
services/control-plane/internal/database/store.go:2207:		`CREATE TRIGGER trg_worker_leases_provider_credential_access_update
services/control-plane/internal/database/store.go-2208-		 BEFORE UPDATE OF tenant_id, execution_id, generation,
services/control-plane/internal/database/store.go-2209-		   provider_credential_grant_id, provider_credential_access_serial,
services/control-plane/internal/database/store.go-2210-		   provider_credential_activity_sequence, provider_credential_activity_at,
services/control-plane/internal/database/store.go-2211-		   provider_credential_access_issued_at, provider_credential_access_renewed_at,
--
services/control-plane/internal/database/store.go-2315-		       OR julianday(NEW.provider_credential_access_expires_at) < julianday(OLD.provider_credential_access_expires_at)
services/control-plane/internal/database/store.go-2316-		     );
services/control-plane/internal/database/store.go-2317-		 END`,
services/control-plane/internal/database/store.go-2318-		`DROP TRIGGER IF EXISTS trg_agent_executions_resource_suspend_shape_insert`,
services/control-plane/internal/database/store.go:2319:		`CREATE TRIGGER trg_agent_executions_resource_suspend_shape_insert
services/control-plane/internal/database/store.go-2320-		 BEFORE INSERT ON agent_executions
services/control-plane/internal/database/store.go-2321-		 WHEN (NEW.status = 'suspended' AND (
services/control-plane/internal/database/store.go-2322-		     NEW.worker_id IS NOT NULL
services/control-plane/internal/database/store.go-2323-		     OR NEW.next_recovery_reason IS NOT 'suspend-resume'
--
services/control-plane/internal/database/store.go-2330-		 BEGIN
services/control-plane/internal/database/store.go-2331-		   SELECT RAISE(ABORT, 'invalid suspended Execution shape');
services/control-plane/internal/database/store.go-2332-		 END`,
services/control-plane/internal/database/store.go-2333-		`DROP TRIGGER IF EXISTS trg_agent_executions_resource_suspend_shape_update`,
services/control-plane/internal/database/store.go:2334:		`CREATE TRIGGER trg_agent_executions_resource_suspend_shape_update
services/control-plane/internal/database/store.go-2335-		 BEFORE UPDATE OF status, worker_id, next_recovery_reason ON agent_executions
services/control-plane/internal/database/store.go-2336-		 WHEN (NEW.status = 'suspended' AND (
services/control-plane/internal/database/store.go-2337-		     NEW.worker_id IS NOT NULL
services/control-plane/internal/database/store.go-2338-		     OR NEW.next_recovery_reason IS NOT 'suspend-resume'
--
services/control-plane/internal/database/store.go-2345-		 BEGIN
services/control-plane/internal/database/store.go-2346-		   SELECT RAISE(ABORT, 'invalid suspended Execution shape');
services/control-plane/internal/database/store.go-2347-		 END`,
services/control-plane/internal/database/store.go-2348-		`DROP TRIGGER IF EXISTS trg_execution_suspend_attempts_shape_insert`,
services/control-plane/internal/database/store.go:2349:		`CREATE TRIGGER trg_execution_suspend_attempts_shape_insert
services/control-plane/internal/database/store.go-2350-		 BEFORE INSERT ON execution_suspend_attempts
services/control-plane/internal/database/store.go-2351-		 WHEN NEW.reason IS NULL
services/control-plane/internal/database/store.go-2352-		   OR NEW.status IS NULL
services/control-plane/internal/database/store.go-2353-		   OR NEW.completion_mode IS NULL
--
services/control-plane/internal/database/store.go-2523-		 BEGIN
services/control-plane/internal/database/store.go-2524-		   SELECT RAISE(ABORT, 'invalid Execution suspend attempt shape');
services/control-plane/internal/database/store.go-2525-		 END`,
services/control-plane/internal/database/store.go-2526-		`DROP TRIGGER IF EXISTS trg_execution_suspend_attempts_lineage_insert`,
services/control-plane/internal/database/store.go:2527:		`CREATE TRIGGER trg_execution_suspend_attempts_lineage_insert
services/control-plane/internal/database/store.go-2528-		 BEFORE INSERT ON execution_suspend_attempts
services/control-plane/internal/database/store.go-2529-		 BEGIN
services/control-plane/internal/database/store.go-2530-		   SELECT RAISE(ABORT, 'Execution suspend attempt must reference an existing Tenant Session Turn')
services/control-plane/internal/database/store.go-2531-		   WHERE NOT EXISTS (
--
services/control-plane/internal/database/store.go-2621-		         AND worker.pod_name = NEW.worker_pod_name
services/control-plane/internal/database/store.go-2622-		     );
services/control-plane/internal/database/store.go-2623-		 END`,
services/control-plane/internal/database/store.go-2624-		`DROP TRIGGER IF EXISTS trg_execution_suspend_attempts_shape_update`,
services/control-plane/internal/database/store.go:2625:		`CREATE TRIGGER trg_execution_suspend_attempts_shape_update
services/control-plane/internal/database/store.go-2626-		 BEFORE UPDATE ON execution_suspend_attempts
services/control-plane/internal/database/store.go-2627-		 WHEN NEW.reason IS NULL
services/control-plane/internal/database/store.go-2628-		   OR NEW.status IS NULL
services/control-plane/internal/database/store.go-2629-		   OR NEW.completion_mode IS NULL
--
services/control-plane/internal/database/store.go-2854-		 BEGIN
services/control-plane/internal/database/store.go-2855-		   SELECT RAISE(ABORT, 'invalid Execution suspend attempt shape');
services/control-plane/internal/database/store.go-2856-		 END`,
services/control-plane/internal/database/store.go-2857-		`DROP TRIGGER IF EXISTS trg_execution_suspend_attempts_finished_update`,
services/control-plane/internal/database/store.go:2858:		`CREATE TRIGGER trg_execution_suspend_attempts_finished_update
services/control-plane/internal/database/store.go-2859-		 BEFORE UPDATE ON execution_suspend_attempts
services/control-plane/internal/database/store.go-2860-		 WHEN OLD.status IN ('completed', 'aborted', 'superseded')
services/control-plane/internal/database/store.go-2861-		   AND (
services/control-plane/internal/database/store.go-2862-		     NEW.status IS NOT OLD.status
--
services/control-plane/internal/database/store.go-2957-		 BEGIN
services/control-plane/internal/database/store.go-2958-		   SELECT RAISE(ABORT, 'Finished Execution suspend attempts are immutable');
services/control-plane/internal/database/store.go-2959-		 END`,
services/control-plane/internal/database/store.go-2960-		`DROP TRIGGER IF EXISTS trg_execution_suspend_attempts_lineage_update`,
services/control-plane/internal/database/store.go:2961:		`CREATE TRIGGER trg_execution_suspend_attempts_lineage_update
services/control-plane/internal/database/store.go-2962-		 BEFORE UPDATE OF tenant_id, session_id, turn_id, execution_id, worker_id, execution_target_id,
services/control-plane/internal/database/store.go-2963-		   generation, reason, completion_mode, control_command_id, boundary_meaningful_activity_sequence, provider_runtime_binding_id,
services/control-plane/internal/database/store.go-2964-		   worker_incarnation, worker_instance_uid, worker_cluster_id,
services/control-plane/internal/database/store.go-2965-		   worker_namespace, worker_pod_name
--
services/control-plane/internal/database/store.go-3038-		         AND worker.pod_name = NEW.worker_pod_name
services/control-plane/internal/database/store.go-3039-		     );
services/control-plane/internal/database/store.go-3040-		 END`,
services/control-plane/internal/database/store.go-3041-		`DROP TRIGGER IF EXISTS trg_execution_suspend_attempts_resume_reference_update`,
services/control-plane/internal/database/store.go:3042:		`CREATE TRIGGER trg_execution_suspend_attempts_resume_reference_update
services/control-plane/internal/database/store.go-3043-		 BEFORE UPDATE OF resume_bundle_id, resume_generation ON execution_suspend_attempts
services/control-plane/internal/database/store.go-3044-		 WHEN NEW.resume_bundle_id IS NOT NULL
services/control-plane/internal/database/store.go-3045-		   AND NOT EXISTS (
services/control-plane/internal/database/store.go-3046-		     SELECT 1
--
services/control-plane/internal/database/store.go-3056-		 END`,
services/control-plane/internal/database/store.go-3057-		`DROP TRIGGER IF EXISTS trg_execution_interactions_resume_recorded_insert`,
services/control-plane/internal/database/store.go-3058-		`DROP TRIGGER IF EXISTS trg_execution_interactions_resume_recorded_update`,
services/control-plane/internal/database/store.go-3059-		`DROP TRIGGER IF EXISTS trg_execution_interactions_delivery_shape_insert`,
services/control-plane/internal/database/store.go:3060:		`CREATE TRIGGER trg_execution_interactions_delivery_shape_insert
services/control-plane/internal/database/store.go-3061-		 BEFORE INSERT ON execution_interactions
services/control-plane/internal/database/store.go-3062-		 BEGIN
services/control-plane/internal/database/store.go-3063-		   SELECT RAISE(ABORT, 'invalid Interaction delivery shape')
services/control-plane/internal/database/store.go-3064-		   WHERE NEW.delivery_status IS NULL
--
services/control-plane/internal/database/store.go-3112-		         AND bundle.generation = NEW.resume_generation
services/control-plane/internal/database/store.go-3113-		     );
services/control-plane/internal/database/store.go-3114-		 END`,
services/control-plane/internal/database/store.go-3115-		`DROP TRIGGER IF EXISTS trg_execution_interactions_delivery_shape_update`,
services/control-plane/internal/database/store.go:3116:		`CREATE TRIGGER trg_execution_interactions_delivery_shape_update
services/control-plane/internal/database/store.go-3117-		 BEFORE UPDATE OF status, resolution_kind, resolution_command_id, delivery_status, delivery_worker_id,
services/control-plane/internal/database/store.go-3118-		   delivery_generation, delivery_available_at, delivered_at, acknowledged_at, delivery_error,
services/control-plane/internal/database/store.go-3119-		   resume_bundle_id, resume_generation, resume_bound_at ON execution_interactions
services/control-plane/internal/database/store.go-3120-		 BEGIN
--
services/control-plane/internal/database/store.go-3158-		      OR (NEW.delivery_status IN ('delivered', 'acknowledged') AND NEW.delivered_at IS NULL)
services/control-plane/internal/database/store.go-3159-		      OR (NEW.delivery_status = 'acknowledged' AND NEW.acknowledged_at IS NULL);
services/control-plane/internal/database/store.go-3160-		 END`,
services/control-plane/internal/database/store.go-3161-		`DROP TRIGGER IF EXISTS trg_execution_interactions_resume_binding_update`,
services/control-plane/internal/database/store.go:3162:		`CREATE TRIGGER trg_execution_interactions_resume_binding_update
services/control-plane/internal/database/store.go-3163-		 BEFORE UPDATE OF delivery_status, resume_bundle_id, resume_generation, resume_bound_at ON execution_interactions
services/control-plane/internal/database/store.go-3164-		 BEGIN
services/control-plane/internal/database/store.go-3165-		   SELECT RAISE(ABORT, 'Recovery-bound Interaction resolution cannot be rebound or replayed')
services/control-plane/internal/database/store.go-3166-		   WHERE OLD.delivery_status = 'resume-bound'
--
services/control-plane/internal/database/store.go-3258-		 ON execution_scheduling_policy_heads (tenant_id, scope_kind, scope_id)`,
services/control-plane/internal/database/store.go-3259-		`CREATE UNIQUE INDEX IF NOT EXISTS uq_execution_scheduling_policy_revision_number
services/control-plane/internal/database/store.go-3260-		 ON execution_scheduling_policy_revisions (tenant_id, policy_head_id, revision_number)`,
services/control-plane/internal/database/store.go-3261-		`DROP TRIGGER IF EXISTS trg_execution_scheduling_policy_heads_insert`,
services/control-plane/internal/database/store.go:3262:		`CREATE TRIGGER trg_execution_scheduling_policy_heads_insert
services/control-plane/internal/database/store.go-3263-		 BEFORE INSERT ON execution_scheduling_policy_heads
services/control-plane/internal/database/store.go-3264-		 BEGIN
services/control-plane/internal/database/store.go-3265-		   SELECT RAISE(ABORT, 'Execution Scheduling Policy Head scope is invalid')
services/control-plane/internal/database/store.go-3266-		   WHERE NEW.scope_kind NOT IN ('tenant', 'organization')
--
services/control-plane/internal/database/store.go-3274-		   SELECT RAISE(ABORT, 'Execution Scheduling Policy updater is unavailable')
services/control-plane/internal/database/store.go-3275-		   WHERE NOT EXISTS (SELECT 1 FROM users u WHERE u.id = NEW.updated_by);
services/control-plane/internal/database/store.go-3276-		 END`,
services/control-plane/internal/database/store.go-3277-		`DROP TRIGGER IF EXISTS trg_execution_scheduling_policy_revisions_insert`,
services/control-plane/internal/database/store.go:3278:		`CREATE TRIGGER trg_execution_scheduling_policy_revisions_insert
services/control-plane/internal/database/store.go-3279-		 BEFORE INSERT ON execution_scheduling_policy_revisions
services/control-plane/internal/database/store.go-3280-		 BEGIN
services/control-plane/internal/database/store.go-3281-		   SELECT RAISE(ABORT, 'Execution Scheduling Policy Revision shape is invalid')
services/control-plane/internal/database/store.go-3282-		   WHERE NEW.revision_number <= 0 OR length(NEW.sha256) <> 64 OR NEW.sha256 GLOB '*[^0-9a-f]*';
--
services/control-plane/internal/database/store.go-3289-		   SELECT RAISE(ABORT, 'Execution Scheduling Policy creator is unavailable')
services/control-plane/internal/database/store.go-3290-		   WHERE NOT EXISTS (SELECT 1 FROM users u WHERE u.id = NEW.created_by);
services/control-plane/internal/database/store.go-3291-		 END`,
services/control-plane/internal/database/store.go-3292-		`DROP TRIGGER IF EXISTS trg_execution_scheduling_policy_rules_insert`,
services/control-plane/internal/database/store.go:3293:		`CREATE TRIGGER trg_execution_scheduling_policy_rules_insert
services/control-plane/internal/database/store.go-3294-		 BEFORE INSERT ON execution_scheduling_policy_rules
services/control-plane/internal/database/store.go-3295-		 BEGIN
services/control-plane/internal/database/store.go-3296-		   SELECT RAISE(ABORT, 'Execution Scheduling Policy rule is invalid')
services/control-plane/internal/database/store.go-3297-		   WHERE NEW.dimension NOT IN ('target', 'region', 'cluster', 'provider', 'capacity_class')
--
services/control-plane/internal/database/store.go-3304-		       AND r.revision_number = h.version + 1
services/control-plane/internal/database/store.go-3305-		   );
services/control-plane/internal/database/store.go-3306-		 END`,
services/control-plane/internal/database/store.go-3307-		`DROP TRIGGER IF EXISTS trg_execution_scheduling_policy_rule_values_insert`,
services/control-plane/internal/database/store.go:3308:		`CREATE TRIGGER trg_execution_scheduling_policy_rule_values_insert
services/control-plane/internal/database/store.go-3309-		 BEFORE INSERT ON execution_scheduling_policy_rule_values
services/control-plane/internal/database/store.go-3310-		 BEGIN
services/control-plane/internal/database/store.go-3311-		   SELECT RAISE(ABORT, 'Execution Scheduling Policy rule value is invalid')
services/control-plane/internal/database/store.go-3312-		   WHERE length(NEW.value) < 1 OR length(NEW.value) > 200 OR trim(NEW.value) IS NOT NEW.value;
--
services/control-plane/internal/database/store.go-3322-		       AND revision.revision_number = h.version + 1
services/control-plane/internal/database/store.go-3323-		   );
services/control-plane/internal/database/store.go-3324-		 END`,
services/control-plane/internal/database/store.go-3325-		`DROP TRIGGER IF EXISTS trg_execution_scheduling_policy_heads_update`,
services/control-plane/internal/database/store.go:3326:		`CREATE TRIGGER trg_execution_scheduling_policy_heads_update
services/control-plane/internal/database/store.go-3327-		 BEFORE UPDATE ON execution_scheduling_policy_heads
services/control-plane/internal/database/store.go-3328-		 BEGIN
services/control-plane/internal/database/store.go-3329-		   SELECT RAISE(ABORT, 'Execution Scheduling Policy Head identity is immutable')
services/control-plane/internal/database/store.go-3330-		   WHERE NEW.id IS NOT OLD.id OR NEW.tenant_id IS NOT OLD.tenant_id
--
services/control-plane/internal/database/store.go-3342-		   SELECT RAISE(ABORT, 'Execution Scheduling Policy updater is unavailable')
services/control-plane/internal/database/store.go-3343-		   WHERE NOT EXISTS (SELECT 1 FROM users u WHERE u.id = NEW.updated_by);
services/control-plane/internal/database/store.go-3344-		 END`,
services/control-plane/internal/database/store.go-3345-		`DROP TRIGGER IF EXISTS trg_execution_scheduling_policy_revisions_update`,
services/control-plane/internal/database/store.go:3346:		`CREATE TRIGGER trg_execution_scheduling_policy_revisions_update BEFORE UPDATE ON execution_scheduling_policy_revisions
services/control-plane/internal/database/store.go-3347-		 BEGIN SELECT RAISE(ABORT, 'Execution Scheduling Policy Revisions are immutable'); END`,
services/control-plane/internal/database/store.go-3348-		`DROP TRIGGER IF EXISTS trg_execution_scheduling_policy_revisions_delete`,
services/control-plane/internal/database/store.go:3349:		`CREATE TRIGGER trg_execution_scheduling_policy_revisions_delete BEFORE DELETE ON execution_scheduling_policy_revisions
services/control-plane/internal/database/store.go-3350-		 BEGIN SELECT RAISE(ABORT, 'Execution Scheduling Policy Revisions are immutable'); END`,
services/control-plane/internal/database/store.go-3351-		`DROP TRIGGER IF EXISTS trg_execution_scheduling_policy_rules_update`,
services/control-plane/internal/database/store.go:3352:		`CREATE TRIGGER trg_execution_scheduling_policy_rules_update BEFORE UPDATE ON execution_scheduling_policy_rules
services/control-plane/internal/database/store.go-3353-		 BEGIN SELECT RAISE(ABORT, 'Execution Scheduling Policy rules are immutable'); END`,
services/control-plane/internal/database/store.go-3354-		`DROP TRIGGER IF EXISTS trg_execution_scheduling_policy_rules_delete`,
services/control-plane/internal/database/store.go:3355:		`CREATE TRIGGER trg_execution_scheduling_policy_rules_delete BEFORE DELETE ON execution_scheduling_policy_rules
services/control-plane/internal/database/store.go-3356-		 BEGIN SELECT RAISE(ABORT, 'Execution Scheduling Policy rules are immutable'); END`,
services/control-plane/internal/database/store.go-3357-		`DROP TRIGGER IF EXISTS trg_execution_scheduling_policy_rule_values_update`,
services/control-plane/internal/database/store.go:3358:		`CREATE TRIGGER trg_execution_scheduling_policy_rule_values_update BEFORE UPDATE ON execution_scheduling_policy_rule_values
services/control-plane/internal/database/store.go-3359-		 BEGIN SELECT RAISE(ABORT, 'Execution Scheduling Policy rule values are immutable'); END`,
services/control-plane/internal/database/store.go-3360-		`DROP TRIGGER IF EXISTS trg_execution_scheduling_policy_rule_values_delete`,
services/control-plane/internal/database/store.go:3361:		`CREATE TRIGGER trg_execution_scheduling_policy_rule_values_delete BEFORE DELETE ON execution_scheduling_policy_rule_values
services/control-plane/internal/database/store.go-3362-		 BEGIN SELECT RAISE(ABORT, 'Execution Scheduling Policy rule values are immutable'); END`,
services/control-plane/internal/database/store.go-3363-		`DROP TRIGGER IF EXISTS trg_organizations_execution_scheduling_policy_protected`,
services/control-plane/internal/database/store.go:3364:		`CREATE TRIGGER trg_organizations_execution_scheduling_policy_protected
services/control-plane/internal/database/store.go-3365-		 BEFORE DELETE ON organizations
services/control-plane/internal/database/store.go-3366-		 WHEN EXISTS (
services/control-plane/internal/database/store.go-3367-		   SELECT 1 FROM execution_scheduling_policy_heads h
services/control-plane/internal/database/store.go-3368-		   WHERE h.tenant_id = OLD.tenant_id AND h.scope_kind = 'organization' AND h.scope_id = OLD.id
services/control-plane/internal/database/store.go-3369-		 )
services/control-plane/internal/database/store.go-3370-		 BEGIN SELECT RAISE(ABORT, 'Organization is protected by its Execution Scheduling Policy'); END`,
services/control-plane/internal/database/store.go-3371-		`DROP TRIGGER IF EXISTS trg_tenants_execution_scheduling_policy_protected`,
services/control-plane/internal/database/store.go:3372:		`CREATE TRIGGER trg_tenants_execution_scheduling_policy_protected
services/control-plane/internal/database/store.go-3373-		 BEFORE DELETE ON tenants
services/control-plane/internal/database/store.go-3374-		 WHEN EXISTS (
services/control-plane/internal/database/store.go-3375-		   SELECT 1 FROM execution_scheduling_policy_heads h WHERE h.tenant_id = OLD.id
services/control-plane/internal/database/store.go-3376-		 )
--
services/control-plane/internal/database/store.go-3389-		db.Migrator().HasColumn(&persistence.AgentExecution{}, "placement_cluster_id") {
services/control-plane/internal/database/store.go-3390-		if err := db.WithContext(ctx).Exec(`DROP TRIGGER IF EXISTS trg_agent_executions_scheduling_policy_snapshot_insert`).Error; err != nil {
services/control-plane/internal/database/store.go-3391-			return fmt.Errorf("drop sqlite Execution Scheduling Policy snapshot insert trigger: %w", err)
services/control-plane/internal/database/store.go-3392-		}
services/control-plane/internal/database/store.go:3393:		if err := db.WithContext(ctx).Exec(`CREATE TRIGGER trg_agent_executions_scheduling_policy_snapshot_insert
services/control-plane/internal/database/store.go-3394-		 BEFORE INSERT ON agent_executions
services/control-plane/internal/database/store.go-3395-		 BEGIN
services/control-plane/internal/database/store.go-3396-		   SELECT RAISE(ABORT, 'Execution Scheduling Policy snapshot is invalid')
services/control-plane/internal/database/store.go-3397-		   WHERE NEW.tenant_scheduling_policy_version < 0
--
services/control-plane/internal/database/store.go-3478-		}
services/control-plane/internal/database/store.go-3479-		if err := db.WithContext(ctx).Exec(`DROP TRIGGER IF EXISTS trg_agent_executions_scheduling_policy_snapshot_immutable`).Error; err != nil {
services/control-plane/internal/database/store.go-3480-			return fmt.Errorf("drop sqlite Execution Scheduling Policy snapshot trigger: %w", err)
services/control-plane/internal/database/store.go-3481-		}
services/control-plane/internal/database/store.go:3482:		if err := db.WithContext(ctx).Exec(`CREATE TRIGGER trg_agent_executions_scheduling_policy_snapshot_immutable
services/control-plane/internal/database/store.go-3483-		 BEFORE UPDATE OF tenant_scheduling_policy_version, tenant_scheduling_policy_digest,
services/control-plane/internal/database/store.go-3484-		   organization_scheduling_policy_version, organization_scheduling_policy_digest,
services/control-plane/internal/database/store.go-3485-		   placement_region, placement_cluster_id ON agent_executions
services/control-plane/internal/database/store.go-3486-		 BEGIN

exec
/bin/zsh -lc "rg -n 'kubernetesWarmPodPlans|demand-fallback|warm pod|warmPlans|DesiredIdleUnits' services/control-plane/internal/executiontargets/*_test.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
exec
/bin/zsh -lc "rg -n 'kubernetesWarmPodPlans|warm-pool-demand-fallback|WarmPlans|warmPlans|MaxActivePods' services/control-plane/internal/executiontargets/kubernetes_reconciler.go services/control-plane/internal/executiontargets/kubernetes_warm_pool.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:719:		t.Fatalf("expected initial unleased warm pod, got %#v", client.pods)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:727:		t.Fatalf("active canary did not delete unleased warm pod exactly once: deleted=%#v active=%#v", client.deletedPods, client.pods)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:733:		t.Fatalf("active canary unexpectedly recreated warm pods: created=%d active=%#v", client.kindCount("Pod"), client.pods)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1620:		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1623:	plans, _, err := kubernetesWarmPodPlans(
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2174:		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,

 succeeded in 0ms:
services/control-plane/internal/executiontargets/kubernetes_warm_pool.go:316:func kubernetesWarmPodPlans(
services/control-plane/internal/executiontargets/kubernetes_reconciler.go:119:	MaxActivePods                 int               `json:"maxActivePods"`
services/control-plane/internal/executiontargets/kubernetes_reconciler.go:619:	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
services/control-plane/internal/executiontargets/kubernetes_reconciler.go:628:		plan, found := validationWarmPlansByName[observed.Pod.Name]
services/control-plane/internal/executiontargets/kubernetes_reconciler.go:744:	freeSlots := configuration.MaxActivePods - (len(existing) + deleted)
services/control-plane/internal/executiontargets/kubernetes_reconciler.go:760:				"warm-pool-demand-fallback",
services/control-plane/internal/executiontargets/kubernetes_reconciler.go:808:	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
services/control-plane/internal/executiontargets/kubernetes_reconciler.go:844:		if scheduled >= configuration.MaxActivePods {
services/control-plane/internal/executiontargets/kubernetes_reconciler.go:900:	for _, plan := range desiredWarmPlans {
services/control-plane/internal/executiontargets/kubernetes_reconciler.go:901:		if scheduled >= configuration.MaxActivePods {
services/control-plane/internal/executiontargets/kubernetes_reconciler.go:933:	availableCapacity := configuration.MaxActivePods
services/control-plane/internal/executiontargets/kubernetes_reconciler.go:956:	if scheduled >= configuration.MaxActivePods {
services/control-plane/internal/executiontargets/kubernetes_reconciler.go:1634:	if configuration.MaxActivePods == 0 {
services/control-plane/internal/executiontargets/kubernetes_reconciler.go:1635:		configuration.MaxActivePods = 50
services/control-plane/internal/executiontargets/kubernetes_reconciler.go:1659:	if configuration.MaxActivePods < 1 || configuration.MaxActivePods > 10_000 || len(configuration.EgressCIDRs) == 0 {

exec
/bin/zsh -lc "rg -n 'type (warmCapacityUnitGroup|warmCapacityAuthorityGroup)|writeWorkerPoolWarmCapacityMetrics|synara_worker_pool_warm_capacity_units' services/control-plane/internal/observability/*_test.go services/control-plane/internal/observability/distributed_routing_billing_metrics.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
services/control-plane/internal/observability/distributed_routing_billing_metrics.go:29:type warmCapacityAuthorityGroup struct {
services/control-plane/internal/observability/distributed_routing_billing_metrics.go:35:type warmCapacityUnitGroup struct {
services/control-plane/internal/observability/distributed_routing_billing_metrics.go:112:	if err := r.writeWorkerPoolWarmCapacityMetrics(ctx, output, now); err != nil {
services/control-plane/internal/observability/distributed_routing_billing_metrics.go:507:func (r *Registry) writeWorkerPoolWarmCapacityMetrics(
services/control-plane/internal/observability/distributed_routing_billing_metrics.go:602:		"synara_worker_pool_warm_capacity_units",
services/control-plane/internal/observability/distributed_routing_billing_metrics.go:618:				"synara_worker_pool_warm_capacity_units%s %d\n",
services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go:44:	if err := registry.writeWorkerPoolWarmCapacityMetrics(context.Background(), &first, now); err != nil {
services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go:48:	if err := registry.writeWorkerPoolWarmCapacityMetrics(context.Background(), &second, now); err != nil {
services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go:62:		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go:63:		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go:64:		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go:65:		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go:66:		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go:67:		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go:68:		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go:76:			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`

exec
/bin/zsh -lc "rg -n 'normalizePoolInput|CreatePoolInput|UpdatePool|DesiredIdleUnits|MaxActiveUnits' services/control-plane/internal/placement/*_test.go services/control-plane/internal/httpapi/*_test.go services/control-plane/internal/placement/service.go services/control-plane/internal/httpapi/placement_api.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
services/control-plane/internal/httpapi/placement_api.go:35:	var input placement.CreatePoolInput
services/control-plane/internal/httpapi/placement_api.go:61:	var input placement.UpdatePoolInput
services/control-plane/internal/httpapi/placement_api.go:66:	pool, err := s.placement.UpdatePool(r.Context(), mustPrincipal(r), tenantID, targetID, poolID, input)
services/control-plane/internal/placement/service_test.go:39:				selected.Pool.CapacityClass != CapacityClassStandard || selected.Pool.DesiredIdleUnits != 0 ||
services/control-plane/internal/placement/service_test.go:40:				selected.Pool.MaxActiveUnits != 1 || selected.Pool.Status != PoolStatusActive {
services/control-plane/internal/placement/service_test.go:275:func TestCreateAndUpdatePoolValidateAndEnforceCAS(t *testing.T) {
services/control-plane/internal/placement/service_test.go:280:	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
services/control-plane/internal/placement/service_test.go:287:		DesiredIdleUnits:   1,
services/control-plane/internal/placement/service_test.go:288:		MaxActiveUnits:     3,
services/control-plane/internal/placement/service_test.go:299:	updated, err := fixture.service.UpdatePool(ctx, fixture.owner, fixture.tenantID, target.ID, created.ID, UpdatePoolInput{
services/control-plane/internal/placement/service_test.go:301:		CreatePoolInput: CreatePoolInput{
services/control-plane/internal/placement/service_test.go:308:			DesiredIdleUnits:   0,
services/control-plane/internal/placement/service_test.go:309:			MaxActiveUnits:     2,
services/control-plane/internal/placement/service_test.go:322:	if _, err := fixture.service.UpdatePool(ctx, fixture.owner, fixture.tenantID, target.ID, created.ID, UpdatePoolInput{
services/control-plane/internal/placement/service_test.go:324:		CreatePoolInput: CreatePoolInput{
services/control-plane/internal/placement/service_test.go:326:			DesiredIdleUnits: 0, MaxActiveUnits: 1, SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
services/control-plane/internal/placement/service_test.go:332:	if _, err := fixture.service.UpdatePool(ctx, fixture.owner, fixture.tenantID, target.ID, created.ID, UpdatePoolInput{
services/control-plane/internal/placement/service_test.go:334:		CreatePoolInput: CreatePoolInput{
services/control-plane/internal/placement/service_test.go:336:			DesiredIdleUnits: 0, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
services/control-plane/internal/placement/service_test.go:343:	if _, err := fixture.service.CreatePool(ctx, member, fixture.tenantID, target.ID, CreatePoolInput{
services/control-plane/internal/placement/service_test.go:345:		DesiredIdleUnits: 0, MaxActiveUnits: 1, SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
services/control-plane/internal/placement/service_test.go:350:	if _, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
services/control-plane/internal/placement/service_test.go:352:		DesiredIdleUnits: 0, MaxActiveUnits: 1, SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
services/control-plane/internal/placement/service_test.go:358:	if _, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, sharedTarget.ID, CreatePoolInput{
services/control-plane/internal/placement/service_test.go:360:		DesiredIdleUnits: 0, MaxActiveUnits: 1, SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
services/control-plane/internal/placement/service_test.go:366:	if _, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, sshTarget.ID, CreatePoolInput{
services/control-plane/internal/placement/service_test.go:368:		DesiredIdleUnits: 0, MaxActiveUnits: 1, SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
services/control-plane/internal/placement/service_test.go:374:func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
services/control-plane/internal/placement/service_test.go:379:	pool, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
services/control-plane/internal/placement/service_test.go:383:		DesiredIdleUnits:   1,
services/control-plane/internal/placement/service_test.go:384:		MaxActiveUnits:     2,
services/control-plane/internal/placement/service_test.go:433:		if _, err := fixture.service.UpdatePool(ctx, fixture.owner, fixture.tenantID, target.ID, pool.ID, UpdatePoolInput{
services/control-plane/internal/placement/service_test.go:435:			CreatePoolInput: CreatePoolInput{
services/control-plane/internal/placement/service_test.go:439:				DesiredIdleUnits:   0,
services/control-plane/internal/placement/service_test.go:440:				MaxActiveUnits:     2,
services/control-plane/internal/placement/service_test.go:530:		DesiredIdleUnits: 0, MaxActiveUnits: 1, SchedulingTemplate: map[string]any{},
services/control-plane/internal/placement/service_test.go:560:		DesiredIdleUnits:  pool.DesiredIdleUnits,
services/control-plane/internal/placement/service_test.go:561:		MaxActiveUnits:    pool.MaxActiveUnits,
services/control-plane/internal/placement/service.go:49:	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
services/control-plane/internal/placement/service.go:50:	MaxActiveUnits     int            `json:"maxActiveUnits"`
services/control-plane/internal/placement/service.go:81:type CreatePoolInput struct {
services/control-plane/internal/placement/service.go:88:	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
services/control-plane/internal/placement/service.go:89:	MaxActiveUnits     int            `json:"maxActiveUnits"`
services/control-plane/internal/placement/service.go:94:type UpdatePoolInput struct {
services/control-plane/internal/placement/service.go:96:	CreatePoolInput
services/control-plane/internal/placement/service.go:240:	input CreatePoolInput,
services/control-plane/internal/placement/service.go:248:	normalized, err := normalizePoolInput(input)
services/control-plane/internal/placement/service.go:275:			DesiredIdleUnits:   normalized.DesiredIdleUnits,
services/control-plane/internal/placement/service.go:276:			MaxActiveUnits:     normalized.MaxActiveUnits,
services/control-plane/internal/placement/service.go:292:func (s *Service) UpdatePool(
services/control-plane/internal/placement/service.go:296:	input UpdatePoolInput,
services/control-plane/internal/placement/service.go:307:	normalized, err := normalizePoolInput(input.CreatePoolInput)
services/control-plane/internal/placement/service.go:366:		current.DesiredIdleUnits = normalized.DesiredIdleUnits
services/control-plane/internal/placement/service.go:367:		current.MaxActiveUnits = normalized.MaxActiveUnits
services/control-plane/internal/placement/service.go:547:		DesiredIdleUnits:   0,
services/control-plane/internal/placement/service.go:548:		MaxActiveUnits:     1,
services/control-plane/internal/placement/service.go:662:		DesiredIdleUnits:   0,
services/control-plane/internal/placement/service.go:663:		MaxActiveUnits:     1,
services/control-plane/internal/placement/service.go:746:		DesiredIdleUnits:   model.DesiredIdleUnits,
services/control-plane/internal/placement/service.go:747:		MaxActiveUnits:     model.MaxActiveUnits,
services/control-plane/internal/placement/service.go:803:func normalizePoolInput(input CreatePoolInput) (CreatePoolInput, error) {
services/control-plane/internal/placement/service.go:806:		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_name", "Worker pool name must be between 1 and 160 characters.")
services/control-plane/internal/placement/service.go:812:		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_mode", "Worker pool mode must be resident, per-execution, or warm.")
services/control-plane/internal/placement/service.go:818:		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_class", "Worker pool capacityClass must be standard or interactive.")
services/control-plane/internal/placement/service.go:824:		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
services/control-plane/internal/placement/service.go:826:	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
services/control-plane/internal/placement/service.go:827:		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
services/control-plane/internal/placement/service.go:833:	return CreatePoolInput{
services/control-plane/internal/placement/service.go:840:		DesiredIdleUnits:   input.DesiredIdleUnits,
services/control-plane/internal/placement/service.go:841:		MaxActiveUnits:     input.MaxActiveUnits,

exec
/bin/zsh -lc "rg --files services/control-plane/internal/database | rg '(warm|placement|sqlite)'" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite.go
services/control-plane/internal/database/billing_shared_allocation_schedule_sqlite_test.go
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite.go
services/control-plane/internal/database/routing_sqlite_test.go
services/control-plane/internal/database/cloud_cost_accounting_sqlite.go
services/control-plane/internal/database/capacity_reservation_sqlite_test.go
services/control-plane/internal/database/active_turn_suspend_sqlite_test.go
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go
services/control-plane/internal/database/project_git_binding_authority_sqlite_test.go
services/control-plane/internal/database/worker_pool_placement_sqlite_test.go
services/control-plane/internal/database/execution_generation_facts_sqlite_test.go
services/control-plane/internal/database/routing_sqlite.go
services/control-plane/internal/database/credential_scope_sqlite.go
services/control-plane/internal/database/credential_scope_sqlite_test.go
services/control-plane/internal/database/credential_bindings_sqlite_test.go
services/control-plane/internal/database/project_git_binding_authority_sqlite.go
services/control-plane/internal/database/worker_claim_facts_sqlite.go
services/control-plane/internal/database/worker_revocation_sqlite_test.go
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
services/control-plane/internal/database/capacity_reservation_sqlite.go
services/control-plane/internal/database/execution_generation_metric_rollup_sqlite.go
services/control-plane/internal/database/worker_incarnation_facts_sqlite_test.go
services/control-plane/internal/database/worker_release_sqlite.go
services/control-plane/internal/database/cloud_cost_accounting_sqlite_test.go
services/control-plane/internal/database/execution_scheduling_policy_sqlite_test.go
services/control-plane/internal/database/execution_scheduling_decisions_sqlite.go
services/control-plane/internal/database/shared_cost_allocation_sqlite_test.go
services/control-plane/internal/database/execution_scheduling_decisions_sqlite_test.go
services/control-plane/internal/database/kubernetes_pod_deletion_fence_sqlite_test.go
services/control-plane/internal/database/advanced_operations_sqlite_test.go
services/control-plane/internal/database/worker_claim_release_facts_sqlite.go
services/control-plane/internal/database/credential_bindings_sqlite.go
services/control-plane/internal/database/shared_actual_allocation_sqlite.go
services/control-plane/internal/database/worker_release_sqlite_test.go
services/control-plane/internal/database/platform_routing_authority_sqlite.go
services/control-plane/internal/database/shared_cost_allocation_sqlite.go
services/control-plane/internal/database/kubernetes_terminal_suspend_sqlite_test.go
services/control-plane/internal/database/semantic_credential_access_sqlite_test.go
services/control-plane/internal/database/worker_mode_sqlite_test.go
services/control-plane/internal/database/worker_revocation_sqlite.go
services/control-plane/internal/database/worker_claim_facts_sqlite_test.go
services/control-plane/internal/database/worker_pool_placement_migration_integration_test.go

exec
/bin/zsh -lc "sed -n '1,230p' services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
sed -n '300,380p' services/control-plane/internal/executiontargets/kubernetes_warm_pool.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
package executiontargets

import (
	"crypto/sha256"
	"encoding/hex"
	"encoding/json"
	"errors"
	"fmt"
	"strconv"
	"strings"
	"time"

	"github.com/google/uuid"

	"github.com/synara-ai/synara/services/control-plane/internal/placement"
	"github.com/synara-ai/synara/services/control-plane/internal/problem"
)

type kubernetesWarmPool struct {
	ID                 uuid.UUID      `gorm:"column:id"`
	Version            int64          `gorm:"column:version"`
	CapacityClass      string         `gorm:"column:capacity_class"`
	ClusterID          string         `gorm:"column:cluster_id"`
	Namespace          string         `gorm:"column:namespace"`
	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
	MaxActiveUnits     int            `gorm:"column:max_active_units"`
	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
	Status             string         `gorm:"column:status"`
}

type kubernetesWarmWorkerState struct {
	WorkerID                uuid.UUID  `gorm:"column:id"`
	WorkerIncarnation       int64      `gorm:"column:incarnation"`
	PodName                 string     `gorm:"column:pod_name"`
	InstanceUID             string     `gorm:"column:instance_uid"`
	WorkerPoolID            *uuid.UUID `gorm:"column:worker_pool_id"`
	WorkerPoolVersion       *int64     `gorm:"column:worker_pool_version"`
	CapacityClass           *string    `gorm:"column:capacity_class"`
	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id"`
	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
	WorkerReleaseStatus     string     `gorm:"column:worker_release_status"`
	RegistrationTrustMode   string     `gorm:"column:registration_trust_mode"`
	ProtocolVersion         int        `gorm:"column:protocol_version"`
	CurrentManifestID       *uuid.UUID `gorm:"column:current_manifest_id"`
	CompatibilityStatus     string     `gorm:"column:compatibility_status"`
	LeaseSupported          bool       `gorm:"column:lease_supported"`
	FencingSupported        bool       `gorm:"column:fencing_supported"`
	Status                  string     `gorm:"column:status"`
	AdministrativeStatus    string     `gorm:"column:administrative_status"`
	LastHeartbeatAt         time.Time  `gorm:"column:last_heartbeat_at"`
	HasLease                bool       `gorm:"column:has_lease"`
}

type kubernetesWarmWorkerIdentity struct {
	PodName     string
	InstanceUID string
}

type kubernetesWarmReleaseSelection struct {
	RevisionID  *uuid.UUID
	Channel     *string
	ImageDigest *string
}

type kubernetesWarmPodPlan struct {
	Pool       kubernetesWarmPool
	Slot       int
	Release    kubernetesWarmReleaseSelection
	ConfigHash string
}

type kubernetesObservedWarmPod struct {
	Pod           kubernetesPod
	PoolID        uuid.UUID
	PoolVersion   int64
	CapacityClass string
	Slot          int
	State         *kubernetesWarmWorkerState
}

type kubernetesWarmDemandEvictionCandidate struct {
	Observed kubernetesObservedWarmPod
	Key      kubernetesWarmCapacityKey
}

func managedKubernetesWarmCapacityObservations(
	tenantID uuid.UUID,
	executionTargetID uuid.UUID,
	warmPools []kubernetesWarmPool,
	warmPoolsSupported bool,
	warmClaimedCounts map[uuid.UUID]int,
	readyWarmCapacity map[kubernetesWarmCapacityKey]int,
	warmRelease kubernetesWarmReleaseSelection,
) []ManagedKubernetesWarmCapacityObservation {
	observations := make([]ManagedKubernetesWarmCapacityObservation, 0, len(warmPools))
	for _, pool := range warmPools {
		if pool.Status != placement.PoolStatusActive {
			continue
		}
		claimed := warmClaimedCounts[pool.ID]
		if claimed < 0 {
			claimed = 0
		}
		observation := ManagedKubernetesWarmCapacityObservation{
			TenantID:          tenantID,
			ExecutionTargetID: executionTargetID,
			WorkerPoolID:      pool.ID,
			WorkerPoolVersion: pool.Version,
			WarmSupported:     warmPoolsSupported,
			ClaimedUnits:      claimed,
		}
		if !warmPoolsSupported {
			observation.Reason = managedKubernetesRoutingReasonPointer(
				"Managed Kubernetes warm capacity is unavailable while a canary Worker release is active.",
			)
			observations = append(observations, observation)
			continue
		}
		observation.WorkerReleaseRevisionID = warmRelease.RevisionID
		observation.WorkerReleaseChannel = warmRelease.Channel
		observation.DesiredTotalUnits = pool.DesiredIdleUnits + claimed
		if observation.DesiredTotalUnits > pool.MaxActiveUnits {
			observation.DesiredTotalUnits = pool.MaxActiveUnits
		}
		key := kubernetesWarmCapacityKey{
			PoolID:          pool.ID,
			PoolVersion:     pool.Version,
			CapacityClass:   pool.CapacityClass,
			ReleaseRevision: optionalUUIDString(warmRelease.RevisionID),
			ReleaseChannel:  stringValue(warmRelease.Channel),
		}
		observation.ReadyIdleUnits = readyWarmCapacity[key]
		observations = append(observations, observation)
	}
	return observations
}

func kubernetesWarmWorkerStateForPod(
	states map[kubernetesWarmWorkerIdentity]kubernetesWarmWorkerState,
	pod kubernetesPod,
) (kubernetesWarmWorkerState, bool) {
	state, found := states[kubernetesWarmWorkerIdentityKey(pod.Name, pod.UID)]
	return state, found
}

func kubernetesWarmWorkerIdentityKey(podName, instanceUID string) kubernetesWarmWorkerIdentity {
	return kubernetesWarmWorkerIdentity{
		PodName:     strings.TrimSpace(podName),
		InstanceUID: strings.TrimSpace(instanceUID),
	}
}

func kubernetesWarmPodIdentity(pod kubernetesPod) (uuid.UUID, int64, string, int, error) {
	poolID, err := uuid.Parse(strings.TrimSpace(pod.Labels[kubernetesWorkerPoolIDLabel]))
	if err != nil {
		return uuid.UUID{}, 0, "", 0, err
	}
	poolVersion, err := strconv.ParseInt(strings.TrimSpace(pod.Labels[kubernetesWorkerPoolVersionLabel]), 10, 64)
	if err != nil || poolVersion <= 0 {
		return uuid.UUID{}, 0, "", 0, errors.New("invalid worker pool version")
	}
	capacityClass := strings.TrimSpace(pod.Labels[kubernetesCapacityClassLabel])
	if capacityClass != placement.CapacityClassStandard && capacityClass != placement.CapacityClassInteractive {
		return uuid.UUID{}, 0, "", 0, errors.New("invalid capacity class")
	}
	slot, err := strconv.Atoi(strings.TrimSpace(pod.Labels[kubernetesWarmSlotLabel]))
	if err != nil || slot < 0 {
		return uuid.UUID{}, 0, "", 0, errors.New("invalid warm slot")
	}
	return poolID, poolVersion, capacityClass, slot, nil
}

func kubernetesWarmWorkerStateMatchesPlan(state kubernetesWarmWorkerState, plan kubernetesWarmPodPlan) bool {
	return state.WorkerPoolID != nil &&
		state.WorkerPoolVersion != nil &&
		state.CapacityClass != nil &&
		*state.WorkerPoolID == plan.Pool.ID &&
		*state.WorkerPoolVersion == plan.Pool.Version &&
		*state.CapacityClass == plan.Pool.CapacityClass &&
		sameOptionalUUID(state.WorkerReleaseRevisionID, plan.Release.RevisionID) &&
		sameOptionalString(state.WorkerReleaseChannel, plan.Release.Channel)
}

type kubernetesWarmCapacityKey struct {
	PoolID          uuid.UUID
	PoolVersion     int64
	CapacityClass   string
	ReleaseRevision string
	ReleaseChannel  string
}

func kubernetesWarmWorkerStateReadyIdle(
	state kubernetesWarmWorkerState,
	pod kubernetesPod,
	plan kubernetesWarmPodPlan,
	now time.Time,
	heartbeatTimeout time.Duration,
) bool {
	return kubernetesWarmWorkerIdentityKey(state.PodName, state.InstanceUID) ==
		kubernetesWarmWorkerIdentityKey(pod.Name, pod.UID) &&
		pod.Phase == "Running" &&
		!state.HasLease &&
		strings.TrimSpace(state.Status) == "online" &&
		strings.TrimSpace(state.AdministrativeStatus) == "active" &&
		state.ProtocolVersion == kubernetesWorkerProtocolVersion &&
		state.CurrentManifestID != nil &&
		strings.TrimSpace(state.CompatibilityStatus) == "compatible" &&
		state.LeaseSupported &&
		state.FencingSupported &&
		strings.TrimSpace(state.RegistrationTrustMode) == kubernetesPodBoundRegistrationTrust &&
		kubernetesWarmWorkerHeartbeatFresh(state.LastHeartbeatAt, now, heartbeatTimeout) &&
		kubernetesWarmWorkerStateMatchesPlan(state, plan) &&
		kubernetesWarmWorkerReleaseReady(state, plan)
}

func kubernetesWarmWorkerStateShouldRecycle(
	state kubernetesWarmWorkerState,
	pod kubernetesPod,
	now time.Time,
	heartbeatTimeout time.Duration,
) bool {
	if pod.Phase == "Succeeded" || pod.Phase == "Failed" {
		return true
	}
	status := strings.TrimSpace(state.Status)
	administrativeStatus := strings.TrimSpace(state.AdministrativeStatus)
	return status == "offline" ||
		status == "draining" ||
		status == "terminated" ||
		administrativeStatus == "draining" ||
}

func sameOptionalUUID(left, right *uuid.UUID) bool {
	if left == nil || right == nil {
		return left == nil && right == nil
	}
	return *left == *right
}

func sameOptionalString(left, right *string) bool {
	if left == nil || right == nil {
		return left == nil && right == nil
	}
	return *left == *right
}

func kubernetesWarmPodPlans(
	warmPools []kubernetesWarmPool,
	warmPoolsSupported bool,
	warmClaimedCounts map[uuid.UUID]int,
	warmRelease kubernetesWarmReleaseSelection,
	podBaseHash string,
	baseImage string,
) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
	plans := make([]kubernetesWarmPodPlan, 0)
	plansByName := make(map[string]kubernetesWarmPodPlan)
	if !warmPoolsSupported {
		return plans, plansByName, nil
	}
	for _, pool := range warmPools {
		if pool.Status != placement.PoolStatusActive {
			continue
		}
		claimed := warmClaimedCounts[pool.ID]
		if claimed < 0 {
			claimed = 0
		}
		desiredTotal := pool.DesiredIdleUnits + claimed
		if desiredTotal > pool.MaxActiveUnits {
			desiredTotal = pool.MaxActiveUnits
		}
		for slot := 0; slot < desiredTotal; slot++ {
			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
			if err != nil {
				return nil, nil, err
			}
			plan.ConfigHash = configHash
			plans = append(plans, plan)
			plansByName[kubernetesWarmPodName(plan)] = plan
		}
	}
	return plans, plansByName, nil
}

func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
	compactPoolID := strings.ReplaceAll(plan.Pool.ID.String(), "-", "")
	releaseKey := "unmanaged"
	if plan.Release.RevisionID != nil && plan.Release.Channel != nil {
		compactReleaseID := strings.ReplaceAll(plan.Release.RevisionID.String(), "-", "")
		channelKey := "p"
		if *plan.Release.Channel == "canary" {
			channelKey = "c"
		}
		releaseKey = channelKey + compactReleaseID[:8]
	}
	return fmt.Sprintf("synara-warm-%s-v%s-%s-s%d", compactPoolID[:10], strconv.FormatInt(plan.Pool.Version, 16), releaseKey, plan.Slot)
}

func kubernetesWarmPoolImage(baseImage string, release kubernetesWarmReleaseSelection) (string, error) {
	if release.RevisionID == nil && release.Channel == nil && release.ImageDigest == nil {
		return baseImage, nil
	}
	if release.RevisionID == nil || release.Channel == nil || release.ImageDigest == nil ||
		(*release.Channel != "promoted" && *release.Channel != "canary") {
		return "", problem.New(409, "worker_release_execution_invalid", "Warm Worker release selection is invalid.")
	}
	return pinImageReference(baseImage, *release.ImageDigest)
}

func kubernetesWarmPoolPodHash(baseHash, baseImage string, plan kubernetesWarmPodPlan) (string, error) {

exec
/bin/zsh -lc "sed -n '1,180p' services/control-plane/internal/httpapi/placement_api.go
sed -n '260,380p' services/control-plane/internal/placement/service_test.go
sed -n '520,585p' services/control-plane/internal/placement/service_test.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
package httpapi

import (
	"net/http"

	"github.com/synara-ai/synara/services/control-plane/internal/placement"
)

func (s *Server) listWorkerPools(w http.ResponseWriter, r *http.Request) {
	tenantID, ok := s.pathUUID(w, r, "tenantID")
	if !ok {
		return
	}
	targetID, ok := s.pathUUID(w, r, "executionTargetID")
	if !ok {
		return
	}
	state, err := s.placement.List(r.Context(), mustPrincipal(r), tenantID, targetID)
	if err != nil {
		s.writeError(w, r, err)
		return
	}
	writeJSON(w, http.StatusOK, state)
}

func (s *Server) createWorkerPool(w http.ResponseWriter, r *http.Request) {
	tenantID, ok := s.pathUUID(w, r, "tenantID")
	if !ok {
		return
	}
	targetID, ok := s.pathUUID(w, r, "executionTargetID")
	if !ok {
		return
	}
	var input placement.CreatePoolInput
	if err := decodeJSON(r, &input); err != nil {
		s.writeError(w, r, err)
		return
	}
	pool, err := s.placement.CreatePool(r.Context(), mustPrincipal(r), tenantID, targetID, input)
	if err != nil {
		s.writeError(w, r, err)
		return
	}
	writeJSON(w, http.StatusCreated, pool)
}

func (s *Server) updateWorkerPool(w http.ResponseWriter, r *http.Request) {
	tenantID, ok := s.pathUUID(w, r, "tenantID")
	if !ok {
		return
	}
	targetID, ok := s.pathUUID(w, r, "executionTargetID")
	if !ok {
		return
	}
	poolID, ok := s.pathUUID(w, r, "workerPoolID")
	if !ok {
		return
	}
	var input placement.UpdatePoolInput
	if err := decodeJSON(r, &input); err != nil {
		s.writeError(w, r, err)
		return
	}
	pool, err := s.placement.UpdatePool(r.Context(), mustPrincipal(r), tenantID, targetID, poolID, input)
	if err != nil {
		s.writeError(w, r, err)
		return
	}
	writeJSON(w, http.StatusOK, pool)
}

func (s *Server) updateExecutionPlacementPolicy(w http.ResponseWriter, r *http.Request) {
	tenantID, ok := s.pathUUID(w, r, "tenantID")
	if !ok {
		return
	}
	targetID, ok := s.pathUUID(w, r, "executionTargetID")
	if !ok {
		return
	}
	var input placement.UpdatePolicyInput
	if err := decodeJSON(r, &input); err != nil {
		s.writeError(w, r, err)
		return
	}
	state, err := s.placement.UpdatePolicy(r.Context(), mustPrincipal(r), tenantID, targetID, input)
	if err != nil {
		s.writeError(w, r, err)
		return
	}
	writeJSON(w, http.StatusOK, state)
}
	}

	sharedTarget := fixture.createTarget(t, "docker", true)
	sharedState, err := fixture.service.List(ctx, fixture.owner, fixture.tenantID, sharedTarget.ID)
	if err != nil {
		t.Fatal(err)
	}
	if _, err := fixture.service.UpdatePolicy(ctx, fixture.owner, fixture.tenantID, sharedTarget.ID, UpdatePolicyInput{
		ExpectedVersion: sharedState.Policy.Version,
		DefaultPoolID:   sharedState.Policy.DefaultPoolID,
	}); problemCode(err) != "shared_execution_target_placement_policy_immutable" {
		t.Fatalf("shared update err = %v", err)
	}
}

func TestCreateAndUpdatePoolValidateAndEnforceCAS(t *testing.T) {
	fixture := newPlacementFixture(t)
	ctx := context.Background()
	target := fixture.createTarget(t, "kubernetes", false)

	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
		Name:               "Warm Interactive",
		Mode:               PoolModeWarm,
		CapacityClass:      CapacityClassInteractive,
		ClusterID:          "cluster-a",
		Region:             "cn-sha",
		Namespace:          "workers",
		DesiredIdleUnits:   1,
		MaxActiveUnits:     3,
		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
		Status:             PoolStatusActive,
	})
	if err != nil {
		t.Fatal(err)
	}
	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
		t.Fatalf("created pool = %#v", created)
	}

	updated, err := fixture.service.UpdatePool(ctx, fixture.owner, fixture.tenantID, target.ID, created.ID, UpdatePoolInput{
		ExpectedVersion: 1,
		CreatePoolInput: CreatePoolInput{
			Name:               "Warm Interactive 2",
			Mode:               PoolModeWarm,
			CapacityClass:      CapacityClassInteractive,
			ClusterID:          "cluster-b",
			Region:             "us-west",
			Namespace:          "workers-2",
			DesiredIdleUnits:   0,
			MaxActiveUnits:     2,
			SchedulingTemplate: map[string]any{"nodeSelector": map[string]any{"pool": "warm"}},
			Status:             PoolStatusDraining,
		},
	})
	if err != nil {
		t.Fatal(err)
	}
	if updated.Version != 2 || updated.Name != "Warm Interactive 2" || updated.Status != PoolStatusDraining ||
		updated.CapacityClass != CapacityClassInteractive || updated.ClusterID != "cluster-b" {
		t.Fatalf("updated pool = %#v", updated)
	}

	if _, err := fixture.service.UpdatePool(ctx, fixture.owner, fixture.tenantID, target.ID, created.ID, UpdatePoolInput{
		ExpectedVersion: 1,
		CreatePoolInput: CreatePoolInput{
			Name: "stale", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
			DesiredIdleUnits: 0, MaxActiveUnits: 1, SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
		},
	}); problemCode(err) != "worker_pool_version_conflict" {
		t.Fatalf("stale update err = %v", err)
	}

	if _, err := fixture.service.UpdatePool(ctx, fixture.owner, fixture.tenantID, target.ID, created.ID, UpdatePoolInput{
		ExpectedVersion: 2,
		CreatePoolInput: CreatePoolInput{
			Name: "identity change", Mode: PoolModeWarm, CapacityClass: CapacityClassStandard,
			DesiredIdleUnits: 0, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
		},
	}); problemCode(err) != "worker_pool_identity_immutable" {
		t.Fatalf("identity update err = %v", err)
	}

	member := fixture.createMember(t, "member")
	if _, err := fixture.service.CreatePool(ctx, member, fixture.tenantID, target.ID, CreatePoolInput{
		Name: "forbidden", Mode: PoolModeWarm, CapacityClass: CapacityClassStandard,
		DesiredIdleUnits: 0, MaxActiveUnits: 1, SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
	}); problemCode(err) != "tenant_forbidden" {
		t.Fatalf("member create err = %v", err)
	}

	if _, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
		Name: "bad", Mode: "invalid", CapacityClass: CapacityClassStandard,
		DesiredIdleUnits: 0, MaxActiveUnits: 1, SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
	}); problemCode(err) != "invalid_worker_pool_mode" {
		t.Fatalf("invalid create err = %v", err)
	}

	sharedTarget := fixture.createTarget(t, "docker", true)
	if _, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, sharedTarget.ID, CreatePoolInput{
		Name: "shared", Mode: PoolModeWarm, CapacityClass: CapacityClassStandard,
		DesiredIdleUnits: 0, MaxActiveUnits: 1, SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
	}); problemCode(err) != "shared_execution_target_worker_pool_immutable" {
		t.Fatalf("shared create err = %v", err)
	}

	sshTarget := fixture.createTarget(t, "ssh", false)
	if _, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, sshTarget.ID, CreatePoolInput{
		Name: "ssh-warm", Mode: PoolModeWarm, CapacityClass: CapacityClassStandard,
		DesiredIdleUnits: 0, MaxActiveUnits: 1, SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
	}); problemCode(err) != "worker_pool_mode_target_unsupported" {
		t.Fatalf("ssh warm create err = %v", err)
	}
}

func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
	fixture := newPlacementFixture(t)
	ctx := context.Background()
	target := fixture.createTarget(t, "kubernetes", false)

	pool, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
		Name:               "Warm Interactive",

func (f placementFixture) createPool(
	t *testing.T,
	target persistence.ExecutionTarget,
	name, mode, status string,
) persistence.WorkerPool {
	t.Helper()
	pool := persistence.WorkerPool{
		ID: uuid.New(), TenantID: target.TenantID, ExecutionTargetID: target.ID, Name: name,
		Mode: mode, CapacityClass: CapacityClassStandard, ClusterID: "", Region: "", Namespace: "",
		DesiredIdleUnits: 0, MaxActiveUnits: 1, SchedulingTemplate: map[string]any{},
		Status: status, Version: 1, CreatedAt: f.now, UpdatedAt: f.now,
	}
	if err := f.db.Create(&pool).Error; err != nil {
		t.Fatal(err)
	}
	return pool
}

func (f placementFixture) createWarmCapacity(
	t *testing.T,
	target persistence.ExecutionTarget,
	pool persistence.WorkerPool,
	warmSupported bool,
	readyIdle int,
	expiresAt time.Time,
) {
	t.Helper()
	desiredTotal := 1
	if !warmSupported {
		desiredTotal = 0
		readyIdle = 0
	}
	if err := f.db.Create(&persistence.WorkerPoolWarmCapacity{
		WorkerPoolID:      pool.ID,
		WorkerPoolVersion: pool.Version,
		TenantID:          f.tenantID,
		ExecutionTargetID: target.ID,
		CapacityClass:     pool.CapacityClass,
		WarmSupported:     warmSupported,
		DesiredIdleUnits:  pool.DesiredIdleUnits,
		MaxActiveUnits:    pool.MaxActiveUnits,
		DesiredTotalUnits: desiredTotal,
		ReadyIdleUnits:    readyIdle,
		Source:            "placement-test",
		ObservedAt:        f.now.Add(-time.Second),
		ExpiresAt:         expiresAt,
		Version:           1,
		UpdatedAt:         f.now,
	}).Error; err != nil {
		t.Fatal(err)
	}
}

func (f placementFixture) createMember(t *testing.T, role string) identity.Principal {
	t.Helper()
	userID := uuid.New()
	if err := f.db.Create(&persistence.User{
		ID: userID, Email: "member-" + uuid.NewString() + "@example.com", DisplayName: "Member",
		Status: "active", EmailVerifiedAt: &f.now, CreatedAt: f.now, UpdatedAt: f.now,
	}).Error; err != nil {
		t.Fatal(err)
	}
	if err := f.db.Create(&persistence.TenantMembership{
		TenantID: f.tenantID, UserID: userID, Role: role, Status: "active",
		JoinedAt: &f.now, CreatedAt: f.now, UpdatedAt: f.now,

exec
/bin/zsh -lc "sed -n '1,880p' services/control-plane/internal/placement/service.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
package placement

import (
	"context"
	"crypto/md5"
	"encoding/hex"
	"errors"
	"strings"
	"time"

	"github.com/google/uuid"
	"gorm.io/gorm"

	"github.com/synara-ai/synara/services/control-plane/internal/authorization"
	"github.com/synara-ai/synara/services/control-plane/internal/identity"
	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
	"github.com/synara-ai/synara/services/control-plane/internal/problem"
	"github.com/synara-ai/synara/services/control-plane/internal/warmcapacity"
)

const (
	PoolModeResident     = "resident"
	PoolModePerExecution = "per-execution"
	PoolModeWarm         = "warm"

	CapacityClassStandard    = "standard"
	CapacityClassInteractive = "interactive"

	PoolStatusActive   = "active"
	PoolStatusDraining = "draining"
	PoolStatusDisabled = "disabled"

	WarmPoolModeDefault    = "default"
	WarmPoolModeDisabled   = "disabled"
	WarmPoolModeBalanced   = "balanced"
	WarmPoolModeLowLatency = "low-latency"
)

type Pool struct {
	ID                 uuid.UUID      `json:"id"`
	TenantID           *uuid.UUID     `json:"tenantId"`
	ExecutionTargetID  uuid.UUID      `json:"executionTargetId"`
	Name               string         `json:"name"`
	Mode               string         `json:"mode"`
	CapacityClass      string         `json:"capacityClass"`
	ClusterID          string         `json:"clusterId"`
	Region             string         `json:"region"`
	Namespace          string         `json:"namespace"`
	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
	MaxActiveUnits     int            `json:"maxActiveUnits"`
	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
	Status             string         `json:"status"`
	Version            int64          `json:"version"`
	CreatedAt          time.Time      `json:"createdAt"`
	UpdatedAt          time.Time      `json:"updatedAt"`
}

type Policy struct {
	TenantID          *uuid.UUID `json:"tenantId"`
	ExecutionTargetID uuid.UUID  `json:"executionTargetId"`
	Version           int64      `json:"version"`
	DefaultPoolID     uuid.UUID  `json:"defaultPoolId"`
	BalancedPoolID    *uuid.UUID `json:"balancedPoolId"`
	LowLatencyPoolID  *uuid.UUID `json:"lowLatencyPoolId"`
	UpdatedBy         *uuid.UUID `json:"updatedBy"`
	UpdatedAt         time.Time  `json:"updatedAt"`
}

type State struct {
	Pools  []Pool `json:"pools"`
	Policy Policy `json:"policy"`
}

type UpdatePolicyInput struct {
	ExpectedVersion  int64      `json:"expectedVersion"`
	DefaultPoolID    uuid.UUID  `json:"defaultPoolId"`
	BalancedPoolID   *uuid.UUID `json:"balancedPoolId"`
	LowLatencyPoolID *uuid.UUID `json:"lowLatencyPoolId"`
}

type CreatePoolInput struct {
	Name               string         `json:"name"`
	Mode               string         `json:"mode"`
	CapacityClass      string         `json:"capacityClass"`
	ClusterID          string         `json:"clusterId"`
	Region             string         `json:"region"`
	Namespace          string         `json:"namespace"`
	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
	MaxActiveUnits     int            `json:"maxActiveUnits"`
	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
	Status             string         `json:"status"`
}

type UpdatePoolInput struct {
	ExpectedVersion int64 `json:"expectedVersion"`
	CreatePoolInput
}

type Selection struct {
	Pool          Pool   `json:"pool"`
	CapacityClass string `json:"capacityClass"`
	PolicyVersion int64  `json:"policyVersion"`
}

func ApplySelection(execution *persistence.AgentExecution, selection Selection) {
	if execution == nil {
		return
	}
	poolID := selection.Pool.ID
	poolVersion := selection.Pool.Version
	capacityClass := selection.CapacityClass
	policyVersion := selection.PolicyVersion
	execution.WorkerPoolID = &poolID
	execution.WorkerPoolVersion = &poolVersion
	execution.CapacityClass = &capacityClass
	execution.PlacementPolicyVersion = &policyVersion
	execution.PlacementRegion = selection.Pool.Region
	execution.PlacementClusterID = selection.Pool.ClusterID
}

type Service struct {
	db         *gorm.DB
	authorizer *authorization.Authorizer
	now        func() time.Time
}

func NewService(db *gorm.DB) *Service {
	return &Service{
		db:         db,
		authorizer: authorization.NewAuthorizer(db),
		now:        func() time.Time { return time.Now().UTC() },
	}
}

func (s *Service) List(ctx context.Context, principal identity.Principal, tenantID, targetID uuid.UUID) (State, error) {
	if err := requireActiveTenant(principal, tenantID); err != nil {
		return State{}, err
	}
	if _, err := s.authorizer.RequireTenant(ctx, principal.UserID, tenantID, authorization.WorkerRead); err != nil {
		return State{}, err
	}
	var state State
	err := persistence.InTransaction(ctx, s.db, func(tx *gorm.DB) error {
		target, err := s.loadAccessibleTarget(ctx, tx, tenantID, targetID, false)
		if err != nil {
			return err
		}
		state, err = s.EnsureDefault(ctx, tx, target)
		return err
	})
	return state, err
}

func (s *Service) UpdatePolicy(
	ctx context.Context,
	principal identity.Principal,
	tenantID, targetID uuid.UUID,
	input UpdatePolicyInput,
) (State, error) {
	if err := requireActiveTenant(principal, tenantID); err != nil {
		return State{}, err
	}
	if _, err := s.authorizer.RequireTenant(ctx, principal.UserID, tenantID, authorization.WorkerManage); err != nil {
		return State{}, err
	}
	if input.ExpectedVersion <= 0 {
		return State{}, problem.New(400, "invalid_execution_placement_policy_version", "expectedVersion must be positive.")
	}
	if input.DefaultPoolID == uuid.Nil {
		return State{}, problem.New(400, "invalid_execution_placement_policy_default_pool", "defaultPoolId is required.")
	}
	var state State
	err := persistence.InTransaction(ctx, s.db, func(tx *gorm.DB) error {
		target, err := s.loadAccessibleTarget(ctx, tx, tenantID, targetID, true)
		if err != nil {
			return err
		}
		if target.TenantID == nil {
			return problem.New(403, "shared_execution_target_placement_policy_immutable", "Platform-shared execution target placement policy cannot be changed by a tenant.")
		}
		current, models, err := s.ensureDefaultLocked(ctx, tx, target)
		if err != nil {
			return err
		}
		if input.ExpectedVersion != current.Version {
			return problem.New(409, "execution_placement_policy_version_conflict", "Execution placement policy changed; reload it before saving.")
		}
		nextBalanced, err := normalizeOptionalPoolID(input.BalancedPoolID)
		if err != nil {
			return err
		}
		nextLowLatency, err := normalizeOptionalPoolID(input.LowLatencyPoolID)
		if err != nil {
			return err
		}
		resolved, err := validatePolicyInput(target, models, input.DefaultPoolID, nextBalanced, nextLowLatency)
		if err != nil {
			return err
		}
		if current.DefaultPoolID == resolved.defaultPool.ID &&
			sameUUIDPointer(current.BalancedPoolID, resolved.balancedPoolID) &&
			sameUUIDPointer(current.LowLatencyPoolID, resolved.lowLatencyPoolID) {
			state = toState(current, models)
			return nil
		}
		now := s.now()
		updates := map[string]any{
			"version":             current.Version + 1,
			"default_pool_id":     resolved.defaultPool.ID,
			"balanced_pool_id":    resolved.balancedPoolID,
			"low_latency_pool_id": resolved.lowLatencyPoolID,
			"updated_by":          principal.UserID,
			"updated_at":          now,
		}
		result := tx.WithContext(ctx).Model(&persistence.ExecutionPlacementPolicy{}).
			Where("execution_target_id = ? AND version = ?", target.ID, current.Version).
			Updates(updates)
		if result.Error != nil {
			return problem.Wrap(500, "execution_placement_policy_update_failed", "Execution placement policy could not be updated.", result.Error)
		}
		if result.RowsAffected != 1 {
			return problem.New(409, "execution_placement_policy_version_conflict", "Execution placement policy changed; reload it before saving.")
		}
		current.Version++
		current.DefaultPoolID = resolved.defaultPool.ID
		current.BalancedPoolID = resolved.balancedPoolID
		current.LowLatencyPoolID = resolved.lowLatencyPoolID
		current.UpdatedBy = &principal.UserID
		current.UpdatedAt = now
		state = toState(current, models)
		return nil
	})
	return state, err
}

func (s *Service) CreatePool(
	ctx context.Context,
	principal identity.Principal,
	tenantID, targetID uuid.UUID,
	input CreatePoolInput,
) (Pool, error) {
	if err := requireActiveTenant(principal, tenantID); err != nil {
		return Pool{}, err
	}
	if _, err := s.authorizer.RequireTenant(ctx, principal.UserID, tenantID, authorization.WorkerManage); err != nil {
		return Pool{}, err
	}
	normalized, err := normalizePoolInput(input)
	if err != nil {
		return Pool{}, err
	}
	var created Pool
	err = persistence.InTransaction(ctx, s.db, func(tx *gorm.DB) error {
		target, err := s.loadAccessibleTarget(ctx, tx, tenantID, targetID, true)
		if err != nil {
			return err
		}
		if target.TenantID == nil {
			return problem.New(403, "shared_execution_target_worker_pool_immutable", "Platform-shared execution target worker pools cannot be changed by a tenant.")
		}
		if err := validatePoolTargetMode(target, normalized.Mode); err != nil {
			return err
		}
		now := s.now()
		model := persistence.WorkerPool{
			ID:                 uuid.New(),
			TenantID:           target.TenantID,
			ExecutionTargetID:  target.ID,
			Name:               normalized.Name,
			Mode:               normalized.Mode,
			CapacityClass:      normalized.CapacityClass,
			ClusterID:          normalized.ClusterID,
			Region:             normalized.Region,
			Namespace:          normalized.Namespace,
			DesiredIdleUnits:   normalized.DesiredIdleUnits,
			MaxActiveUnits:     normalized.MaxActiveUnits,
			SchedulingTemplate: normalized.SchedulingTemplate,
			Status:             normalized.Status,
			Version:            1,
			CreatedAt:          now,
			UpdatedAt:          now,
		}
		if err := tx.WithContext(ctx).Create(&model).Error; err != nil {
			return problem.Wrap(409, "worker_pool_create_rejected", "Worker pool could not be created.", err)
		}
		created = toPool(model)
		return nil
	})
	return created, err
}

func (s *Service) UpdatePool(
	ctx context.Context,
	principal identity.Principal,
	tenantID, targetID, poolID uuid.UUID,
	input UpdatePoolInput,
) (Pool, error) {
	if err := requireActiveTenant(principal, tenantID); err != nil {
		return Pool{}, err
	}
	if _, err := s.authorizer.RequireTenant(ctx, principal.UserID, tenantID, authorization.WorkerManage); err != nil {
		return Pool{}, err
	}
	if input.ExpectedVersion <= 0 {
		return Pool{}, problem.New(400, "invalid_worker_pool_version", "expectedVersion must be positive.")
	}
	normalized, err := normalizePoolInput(input.CreatePoolInput)
	if err != nil {
		return Pool{}, err
	}
	var updated Pool
	err = persistence.InTransaction(ctx, s.db, func(tx *gorm.DB) error {
		target, err := s.loadAccessibleTarget(ctx, tx, tenantID, targetID, true)
		if err != nil {
			return err
		}
		if target.TenantID == nil {
			return problem.New(403, "shared_execution_target_worker_pool_immutable", "Platform-shared execution target worker pools cannot be changed by a tenant.")
		}
		if err := validatePoolTargetMode(target, normalized.Mode); err != nil {
			return err
		}
		var current persistence.WorkerPool
		err = persistence.WithLocking(tx.WithContext(ctx), "UPDATE", "").
			Where("id = ? AND execution_target_id = ? AND tenant_id = ?", poolID, targetID, tenantID).
			Take(&current).Error
		if errors.Is(err, gorm.ErrRecordNotFound) {
			return problem.New(404, "worker_pool_not_found", "Worker pool not found.")
		}
		if err != nil {
			return problem.Wrap(500, "worker_pool_load_failed", "Worker pool could not be loaded.", err)
		}
		if current.Version != input.ExpectedVersion {
			return problem.New(409, "worker_pool_version_conflict", "Worker pool changed; reload it before saving.")
		}
		if current.Mode != normalized.Mode || current.CapacityClass != normalized.CapacityClass {
			return problem.New(409, "worker_pool_identity_immutable", "Worker pool mode and capacityClass cannot change after creation.")
		}
		var blocked int64
		if err := tx.WithContext(ctx).Model(&persistence.AgentExecution{}).
			Where(
				"execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ? AND status IN ?",
				target.ID,
				current.ID,
				current.Version,
				[]string{"queued", "recovering", "leased", "running", "waiting-for-approval", "suspended"},
			).
			Count(&blocked).Error; err != nil {
			return problem.Wrap(500, "worker_pool_update_probe_failed", "Worker pool update safety could not be evaluated.", err)
		}
		if blocked > 0 {
			return problem.New(
				409,
				"worker_pool_version_update_blocked",
				"Worker pool version cannot change while a nonterminal Execution still references the current pool snapshot.",
			)
		}
		now := s.now()
		expectedVersion := current.Version
		current.Name = normalized.Name
		current.Mode = normalized.Mode
		current.CapacityClass = normalized.CapacityClass
		current.ClusterID = normalized.ClusterID
		current.Region = normalized.Region
		current.Namespace = normalized.Namespace
		current.DesiredIdleUnits = normalized.DesiredIdleUnits
		current.MaxActiveUnits = normalized.MaxActiveUnits
		current.SchedulingTemplate = normalized.SchedulingTemplate
		current.Status = normalized.Status
		current.Version++
		current.UpdatedAt = now
		result := tx.WithContext(ctx).Model(&persistence.WorkerPool{}).
			Where("id = ? AND version = ?", current.ID, expectedVersion).
			Select(
				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
			).
			Updates(&current)
		if result.Error != nil {
			return problem.Wrap(500, "worker_pool_update_failed", "Worker pool could not be updated.", result.Error)
		}
		if result.RowsAffected != 1 {
			return problem.New(409, "worker_pool_version_conflict", "Worker pool changed; reload it before saving.")
		}
		updated = toPool(current)
		return nil
	})
	return updated, err
}

func (s *Service) EnsureDefault(ctx context.Context, tx *gorm.DB, target persistence.ExecutionTarget) (State, error) {
	policy, pools, err := s.ensureDefaultLocked(ctx, nonNilDB(tx, s.db), target)
	if err != nil {
		return State{}, err
	}
	return toState(policy, pools), nil
}

func (s *Service) SelectExecution(
	ctx context.Context,
	tx *gorm.DB,
	target persistence.ExecutionTarget,
	warmPoolMode string,
) (Selection, error) {
	policy, models, err := s.ensureDefaultLocked(ctx, nonNilDB(tx, s.db), target)
	if err != nil {
		return Selection{}, err
	}
	return s.selectExecutionFromPolicy(ctx, nonNilDB(tx, s.db), target, policy, models, warmPoolMode)
}

func (s *Service) PreviewExecution(
	ctx context.Context,
	tx *gorm.DB,
	target persistence.ExecutionTarget,
	warmPoolMode string,
) (Selection, error) {
	mode, err := normalizeWarmPoolMode(warmPoolMode)
	if err != nil {
		return Selection{}, err
	}
	models, err := s.loadPools(ctx, nonNilDB(tx, s.db), target.ID)
	if err != nil {
		return Selection{}, err
	}
	var policy persistence.ExecutionPlacementPolicy
	err = nonNilDB(tx, s.db).WithContext(ctx).
		Where("execution_target_id = ?", target.ID).
		Take(&policy).Error
	if err == nil {
		return s.selectExecutionFromPolicy(ctx, nonNilDB(tx, s.db), target, policy, models, mode)
	}
	if !errors.Is(err, gorm.ErrRecordNotFound) {
		return Selection{}, problem.Wrap(500, "execution_placement_policy_load_failed", "Execution placement policy could not be loaded.", err)
	}

	defaultPool := previewDefaultPool(target, models)
	if defaultPool.Status != PoolStatusActive {
		return Selection{}, problem.New(409, "execution_placement_policy_invalid", "Execution placement default pool must be active.")
	}
	selected := toPool(defaultPool)
	return Selection{Pool: selected, CapacityClass: selected.CapacityClass, PolicyVersion: 1}, nil
}

func (s *Service) selectExecutionFromPolicy(
	ctx context.Context,
	db *gorm.DB,
	target persistence.ExecutionTarget,
	policy persistence.ExecutionPlacementPolicy,
	models []persistence.WorkerPool,
	warmPoolMode string,
) (Selection, error) {
	mode, err := normalizeWarmPoolMode(warmPoolMode)
	if err != nil {
		return Selection{}, err
	}
	byID := make(map[uuid.UUID]persistence.WorkerPool, len(models))
	for _, model := range models {
		byID[model.ID] = model
	}
	candidates := make([]uuid.UUID, 0, 3)
	switch mode {
	case WarmPoolModeDisabled, WarmPoolModeDefault:
		candidates = append(candidates, policy.DefaultPoolID)
	case WarmPoolModeBalanced:
		if policy.BalancedPoolID != nil {
			candidates = append(candidates, *policy.BalancedPoolID)
		}
		candidates = append(candidates, policy.DefaultPoolID)
	case WarmPoolModeLowLatency:
		if policy.LowLatencyPoolID != nil {
			candidates = append(candidates, *policy.LowLatencyPoolID)
		}
		if policy.BalancedPoolID != nil {
			candidates = append(candidates, *policy.BalancedPoolID)
		}
		candidates = append(candidates, policy.DefaultPoolID)
	}
	seen := make(map[uuid.UUID]struct{}, len(candidates))
	var coldFallback *persistence.WorkerPool
	var capacity *warmcapacity.Service
	if target.TenantID != nil {
		capacity = warmcapacity.NewService(db)
	}
	now := s.now()
	for _, candidateID := range candidates {
		if _, exists := seen[candidateID]; exists {
			continue
		}
		seen[candidateID] = struct{}{}
		pool, ok := byID[candidateID]
		if !ok || pool.Status != PoolStatusActive {
			continue
		}
		if coldFallback == nil {
			candidate := pool
			coldFallback = &candidate
		}
		if capacity == nil || pool.Mode != PoolModeWarm {
			continue
		}
		observation, err := capacity.GetFresh(ctx, warmcapacity.FreshLookup{
			TenantID:          *target.TenantID,
			ExecutionTargetID: target.ID,
			WorkerPoolID:      pool.ID,
			WorkerPoolVersion: pool.Version,
			Now:               now,
		})
		if err != nil {
			return Selection{}, err
		}
		if observation != nil && observation.WarmSupported && observation.ReadyIdleUnits > 0 {
			return selectionForPool(pool, policy.Version), nil
		}
	}
	if coldFallback != nil {
		return selectionForPool(*coldFallback, policy.Version), nil
	}
	return Selection{}, problem.New(409, "execution_placement_policy_invalid", "Execution placement policy does not resolve to an active Worker pool.")
}

func selectionForPool(pool persistence.WorkerPool, policyVersion int64) Selection {
	selected := toPool(pool)
	return Selection{Pool: selected, CapacityClass: selected.CapacityClass, PolicyVersion: policyVersion}
}

func previewDefaultPool(
	target persistence.ExecutionTarget,
	models []persistence.WorkerPool,
) persistence.WorkerPool {
	defaultID := deterministicUUID(target.ID.String() + ":worker-pool:default:000058")
	for _, model := range models {
		if model.ID == defaultID || strings.EqualFold(model.Name, "default") {
			return model
		}
	}
	return persistence.WorkerPool{
		ID:                 defaultID,
		TenantID:           target.TenantID,
		ExecutionTargetID:  target.ID,
		Name:               "default",
		Mode:               defaultPoolMode(target.Kind),
		CapacityClass:      CapacityClassStandard,
		ClusterID:          "",
		Region:             "",
		Namespace:          "",
		DesiredIdleUnits:   0,
		MaxActiveUnits:     1,
		SchedulingTemplate: map[string]any{},
		Status:             PoolStatusActive,
		Version:            1,
	}
}

type resolvedPolicyInput struct {
	defaultPool      persistence.WorkerPool
	balancedPoolID   *uuid.UUID
	lowLatencyPoolID *uuid.UUID
}

func validatePolicyInput(
	target persistence.ExecutionTarget,
	models []persistence.WorkerPool,
	defaultPoolID uuid.UUID,
	balancedPoolID, lowLatencyPoolID *uuid.UUID,
) (resolvedPolicyInput, error) {
	byID := make(map[uuid.UUID]persistence.WorkerPool, len(models))
	for _, model := range models {
		byID[model.ID] = model
	}
	defaultPool, ok := byID[defaultPoolID]
	if !ok || !sameUUIDPointer(defaultPool.TenantID, target.TenantID) || defaultPool.ExecutionTargetID != target.ID {
		return resolvedPolicyInput{}, problem.New(409, "execution_placement_pool_not_found", "Execution placement default pool is unavailable for this target.")
	}
	if defaultPool.Status != PoolStatusActive {
		return resolvedPolicyInput{}, problem.New(409, "execution_placement_policy_invalid", "Execution placement default pool must be active.")
	}
	if balancedPoolID != nil {
		if balanced, ok := byID[*balancedPoolID]; !ok || !sameUUIDPointer(balanced.TenantID, target.TenantID) || balanced.ExecutionTargetID != target.ID {
			return resolvedPolicyInput{}, problem.New(409, "execution_placement_pool_not_found", "Execution placement balanced pool is unavailable for this target.")
		}
	}
	if lowLatencyPoolID != nil {
		if lowLatency, ok := byID[*lowLatencyPoolID]; !ok || !sameUUIDPointer(lowLatency.TenantID, target.TenantID) || lowLatency.ExecutionTargetID != target.ID {
			return resolvedPolicyInput{}, problem.New(409, "execution_placement_pool_not_found", "Execution placement low-latency pool is unavailable for this target.")
		}
	}
	return resolvedPolicyInput{
		defaultPool:      defaultPool,
		balancedPoolID:   balancedPoolID,
		lowLatencyPoolID: lowLatencyPoolID,
	}, nil
}

func (s *Service) ensureDefaultLocked(
	ctx context.Context,
	tx *gorm.DB,
	target persistence.ExecutionTarget,
) (persistence.ExecutionPlacementPolicy, []persistence.WorkerPool, error) {
	lockedTarget, err := s.lockTarget(ctx, tx, target.ID)
	if err != nil {
		return persistence.ExecutionPlacementPolicy{}, nil, err
	}
	models, err := s.loadPools(ctx, tx, lockedTarget.ID)
	if err != nil {
		return persistence.ExecutionPlacementPolicy{}, nil, err
	}
	var policy persistence.ExecutionPlacementPolicy
	err = persistence.WithLocking(tx.WithContext(ctx), "UPDATE", "").
		Where("execution_target_id = ?", lockedTarget.ID).
		Take(&policy).Error
	if err == nil {
		if !sameUUIDPointer(policy.TenantID, lockedTarget.TenantID) {
			return persistence.ExecutionPlacementPolicy{}, nil, problem.New(409, "execution_placement_policy_invalid", "Execution placement policy ownership does not match the execution target.")
		}
		return policy, models, nil
	}
	if !errors.Is(err, gorm.ErrRecordNotFound) {
		return persistence.ExecutionPlacementPolicy{}, nil, problem.Wrap(500, "execution_placement_policy_load_failed", "Execution placement policy could not be loaded.", err)
	}
	defaultPool, models, err := s.ensureDefaultPool(ctx, tx, lockedTarget, models)
	if err != nil {
		return persistence.ExecutionPlacementPolicy{}, nil, err
	}
	now := s.now()
	policy = persistence.ExecutionPlacementPolicy{
		TenantID:          lockedTarget.TenantID,
		ExecutionTargetID: lockedTarget.ID,
		Version:           1,
		DefaultPoolID:     defaultPool.ID,
		UpdatedAt:         now,
	}
	if err := tx.WithContext(ctx).Create(&policy).Error; err != nil {
		return persistence.ExecutionPlacementPolicy{}, nil, problem.Wrap(409, "execution_placement_policy_create_rejected", "Execution placement policy could not be created.", err)
	}
	return policy, models, nil
}

func (s *Service) ensureDefaultPool(
	ctx context.Context,
	tx *gorm.DB,
	target persistence.ExecutionTarget,
	models []persistence.WorkerPool,
) (persistence.WorkerPool, []persistence.WorkerPool, error) {
	defaultID := deterministicUUID(target.ID.String() + ":worker-pool:default:000058")
	for _, model := range models {
		if model.ID == defaultID || strings.EqualFold(model.Name, "default") {
			return model, models, nil
		}
	}
	now := s.now()
	model := persistence.WorkerPool{
		ID:                 defaultID,
		TenantID:           target.TenantID,
		ExecutionTargetID:  target.ID,
		Name:               "default",
		Mode:               defaultPoolMode(target.Kind),
		CapacityClass:      CapacityClassStandard,
		ClusterID:          "",
		Region:             "",
		Namespace:          "",
		DesiredIdleUnits:   0,
		MaxActiveUnits:     1,
		SchedulingTemplate: map[string]any{},
		Status:             PoolStatusActive,
		Version:            1,
		CreatedAt:          now,
		UpdatedAt:          now,
	}
	if err := tx.WithContext(ctx).Create(&model).Error; err != nil {
		return persistence.WorkerPool{}, nil, problem.Wrap(409, "worker_pool_create_rejected", "Worker pool could not be created.", err)
	}
	models = append(models, model)
	return model, models, nil
}

func (s *Service) loadAccessibleTarget(
	ctx context.Context,
	tx *gorm.DB,
	tenantID, targetID uuid.UUID,
	forUpdate bool,
) (persistence.ExecutionTarget, error) {
	query := tx.WithContext(ctx)
	if forUpdate {
		query = persistence.WithLocking(query, "UPDATE", "")
	}
	var target persistence.ExecutionTarget
	err := query.Where("id = ? AND (tenant_id = ? OR tenant_id IS NULL)", targetID, tenantID).Take(&target).Error
	if errors.Is(err, gorm.ErrRecordNotFound) {
		return persistence.ExecutionTarget{}, problem.New(404, "execution_target_not_found", "Execution target not found.")
	}
	if err != nil {
		return persistence.ExecutionTarget{}, problem.Wrap(500, "execution_target_lookup_failed", "Execution target could not be loaded.", err)
	}
	return target, nil
}

func (s *Service) lockTarget(ctx context.Context, tx *gorm.DB, targetID uuid.UUID) (persistence.ExecutionTarget, error) {
	var target persistence.ExecutionTarget
	err := persistence.WithLocking(tx.WithContext(ctx), "UPDATE", "").
		Where("id = ?", targetID).
		Take(&target).Error
	if errors.Is(err, gorm.ErrRecordNotFound) {
		return persistence.ExecutionTarget{}, problem.New(404, "execution_target_not_found", "Execution target not found.")
	}
	if err != nil {
		return persistence.ExecutionTarget{}, problem.Wrap(500, "execution_target_lookup_failed", "Execution target could not be loaded.", err)
	}
	return target, nil
}

func (s *Service) loadPools(ctx context.Context, tx *gorm.DB, targetID uuid.UUID) ([]persistence.WorkerPool, error) {
	models := make([]persistence.WorkerPool, 0)
	if err := tx.WithContext(ctx).
		Where("execution_target_id = ?", targetID).
		Order("CASE WHEN status = 'active' THEN 0 ELSE 1 END, LOWER(name), id").
		Find(&models).Error; err != nil {
		return nil, problem.Wrap(500, "worker_pools_load_failed", "Worker pools could not be loaded.", err)
	}
	return models, nil
}

func toState(policy persistence.ExecutionPlacementPolicy, models []persistence.WorkerPool) State {
	pools := make([]Pool, 0, len(models))
	for _, model := range models {
		pools = append(pools, toPool(model))
	}
	return State{Pools: pools, Policy: toPolicy(policy)}
}

func toPool(model persistence.WorkerPool) Pool {
	template := model.SchedulingTemplate
	if template == nil {
		template = map[string]any{}
	}
	return Pool{
		ID:                 model.ID,
		TenantID:           model.TenantID,
		ExecutionTargetID:  model.ExecutionTargetID,
		Name:               model.Name,
		Mode:               model.Mode,
		CapacityClass:      model.CapacityClass,
		ClusterID:          model.ClusterID,
		Region:             model.Region,
		Namespace:          model.Namespace,
		DesiredIdleUnits:   model.DesiredIdleUnits,
		MaxActiveUnits:     model.MaxActiveUnits,
		SchedulingTemplate: template,
		Status:             model.Status,
		Version:            model.Version,
		CreatedAt:          model.CreatedAt,
		UpdatedAt:          model.UpdatedAt,
	}
}

func toPolicy(model persistence.ExecutionPlacementPolicy) Policy {
	return Policy{
		TenantID:          model.TenantID,
		ExecutionTargetID: model.ExecutionTargetID,
		Version:           model.Version,
		DefaultPoolID:     model.DefaultPoolID,
		BalancedPoolID:    model.BalancedPoolID,
		LowLatencyPoolID:  model.LowLatencyPoolID,
		UpdatedBy:         model.UpdatedBy,
		UpdatedAt:         model.UpdatedAt,
	}
}

func defaultPoolMode(kind string) string {
	switch strings.ToLower(strings.TrimSpace(kind)) {
	case "kubernetes":
		return PoolModePerExecution
	case "docker":
		return PoolModeResident
	default:
		return PoolModeResident
	}
}

func normalizeWarmPoolMode(mode string) (string, error) {
	normalized := strings.ToLower(strings.TrimSpace(mode))
	if normalized == "" {
		normalized = WarmPoolModeDefault
	}
	switch normalized {
	case WarmPoolModeDefault, WarmPoolModeDisabled, WarmPoolModeBalanced, WarmPoolModeLowLatency:
		return normalized, nil
	default:
		return "", problem.New(400, "invalid_warm_pool_mode", "warmPoolMode must be disabled, default, balanced, or low-latency.")
	}
}

func normalizeOptionalPoolID(value *uuid.UUID) (*uuid.UUID, error) {
	if value == nil {
		return nil, nil
	}
	if *value == uuid.Nil {
		return nil, problem.New(400, "invalid_execution_placement_pool_id", "Pool references must not be empty UUIDs.")
	}
	return value, nil
}

func normalizePoolInput(input CreatePoolInput) (CreatePoolInput, error) {
	name := strings.TrimSpace(input.Name)
	if len(name) == 0 || len(name) > 160 {
		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_name", "Worker pool name must be between 1 and 160 characters.")
	}
	mode := strings.ToLower(strings.TrimSpace(input.Mode))
	switch mode {
	case PoolModeResident, PoolModePerExecution, PoolModeWarm:
	default:
		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_mode", "Worker pool mode must be resident, per-execution, or warm.")
	}
	capacityClass := strings.ToLower(strings.TrimSpace(input.CapacityClass))
	switch capacityClass {
	case CapacityClassStandard, CapacityClassInteractive:
	default:
		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_class", "Worker pool capacityClass must be standard or interactive.")
	}
	status := strings.ToLower(strings.TrimSpace(input.Status))
	switch status {
	case PoolStatusActive, PoolStatusDraining, PoolStatusDisabled:
	default:
		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
	}
	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
	}
	template := input.SchedulingTemplate
	if template == nil {
		template = map[string]any{}
	}
	return CreatePoolInput{
		Name:               name,
		Mode:               mode,
		CapacityClass:      capacityClass,
		ClusterID:          strings.TrimSpace(input.ClusterID),
		Region:             strings.TrimSpace(input.Region),
		Namespace:          strings.TrimSpace(input.Namespace),
		DesiredIdleUnits:   input.DesiredIdleUnits,
		MaxActiveUnits:     input.MaxActiveUnits,
		SchedulingTemplate: template,
		Status:             status,
	}, nil
}

func validatePoolTargetMode(target persistence.ExecutionTarget, mode string) error {
	if mode == PoolModeWarm && !strings.EqualFold(strings.TrimSpace(target.Kind), "kubernetes") {
		return problem.New(
			409,
			"worker_pool_mode_target_unsupported",
			"Warm Worker pools are only supported on Kubernetes execution targets.",
		)
	}
	return nil
}

func deterministicUUID(seed string) uuid.UUID {
	sum := md5.Sum([]byte(seed))
	raw := hex.EncodeToString(sum[:])
	id, err := uuid.Parse(raw[0:8] + "-" + raw[8:12] + "-" + raw[12:16] + "-" + raw[16:20] + "-" + raw[20:32])
	if err != nil {
		panic(err)
	}
	return id
}

func sameUUIDPointer(left, right *uuid.UUID) bool {
	if left == nil || right == nil {
		return left == nil && right == nil
	}
	return *left == *right
}

func nonNilDB(tx, fallback *gorm.DB) *gorm.DB {
	if tx != nil {
		return tx
	}
	return fallback
}

exec
/bin/zsh -lc "rg -n 'worker_pool_warm_capacity' services/control-plane/internal/database/*.go | head -n 160" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
exec
/bin/zsh -lc "sed -n '1,130p' services/control-plane/internal/observability/distributed_routing_billing_metrics.go
sed -n '1,130p' services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
package observability

import (
	"bytes"
	"context"
	"fmt"
	"sort"
	"strconv"
	"strings"
	"time"

	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
)

type labeledAmount struct {
	Provider            string `gorm:"column:provider"`
	CurrencyCode        string `gorm:"column:currency_code"`
	ChargeKind          string `gorm:"column:charge_kind"`
	ReconciliationState string `gorm:"column:reconciliation_state"`
	AmountMicros        int64  `gorm:"column:amount_micros"`
	MatchedMicros       int64  `gorm:"column:matched_micros"`
}

type leaseStateMetric struct {
	LeaseName string    `gorm:"column:lease_name"`
	ExpiresAt time.Time `gorm:"column:expires_at"`
}

type warmCapacityAuthorityGroup struct {
	CapacityClass string `gorm:"column:capacity_class"`
	WarmSupported bool   `gorm:"column:warm_supported"`
	Count         int64  `gorm:"column:count"`
}

type warmCapacityUnitGroup struct {
	CapacityClass     string `gorm:"column:capacity_class"`
	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
	ClaimedUnits      int64  `gorm:"column:claimed_units"`
	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
}

type warmCapacityAuthorityMetricKey struct {
	CapacityClass string
	Freshness     string
	WarmSupported bool
}

type sharedAllocationSchedulePeriodMetric struct {
	ScheduleKind string `gorm:"column:schedule_kind"`
	LastOutcome  string `gorm:"column:last_outcome"`
	DueState     string `gorm:"column:due_state"`
	Count        int64  `gorm:"column:count"`
}

type sharedActualAllocationMetric struct {
	Provider                string `gorm:"column:provider"`
	CurrencyCode            string `gorm:"column:currency_code"`
	State                   string `gorm:"column:state"`
	RunCount                int64  `gorm:"column:run_count"`
	SourceLineCount         int64  `gorm:"column:source_line_count"`
	UnallocatedLineCount    int64  `gorm:"column:unallocated_line_count"`
	SourceAmountMicros      int64  `gorm:"column:source_amount_micros"`
	UnallocatedAmountMicros int64  `gorm:"column:unallocated_amount_micros"`
}

type capacityAdmissionMetric struct {
	AdmissionMode string `gorm:"column:admission_mode"`
	Count         int64  `gorm:"column:count"`
}

type reservationAuthorityMetric struct {
	Freshness         string `gorm:"column:freshness"`
	TargetCount       int64  `gorm:"column:target_count"`
	AcknowledgedUnits int64  `gorm:"column:acknowledged_units"`
	AllocatedUnits    int64  `gorm:"column:allocated_units"`
	Unacknowledged    int64  `gorm:"column:unacknowledged_units"`
}

func (r *Registry) writeDistributedRoutingLeadershipBillingMetrics(
	ctx context.Context,
	output *bytes.Buffer,
	now time.Time,
) error {
	targetHealth, err := pairedCountsWhere(
		ctx, r.db, "execution_target_health", "status", "capacity_status", "",
	)
	if err != nil {
		return fmt.Errorf("collect Execution Target health metrics: %w", err)
	}
	failovers, err := pairedCountsWhere(
		ctx, r.db, "execution_failover_attempts", "status", "reason", "",
	)
	if err != nil {
		return fmt.Errorf("collect Execution failover metrics: %w", err)
	}
	writePairedGauge(
		output, "synara_execution_target_health",
		"Authoritative expiring Execution Target health observations by health and capacity state.",
		"status", "capacity_status", targetHealth,
	)
	writePairedGauge(
		output, "synara_execution_failover_attempts",
		"Authoritative cross-Target successor attempts by immutable status and reason.",
		"status", "reason", failovers,
	)
	if err := r.writePlatformRoutingPublicationMetrics(ctx, output); err != nil {
		return err
	}
	if err := r.writeCapacityReservationMetrics(ctx, output, now); err != nil {
		return err
	}
	if err := r.writeWorkerPoolWarmCapacityMetrics(ctx, output, now); err != nil {
		return err
	}
	if err := r.writeBillingSharedAllocationScheduleMetrics(ctx, output, now); err != nil {
		return err
	}
	if err := r.writeBillingSharedActualAllocationMetrics(ctx, output); err != nil {
		return err
	}

	var leases []leaseStateMetric
	if err := r.db.WithContext(ctx).Table("reconciler_leases").
		Select("lease_name, expires_at").Order("lease_name").Scan(&leases).Error; err != nil {
		return fmt.Errorf("collect Reconciler leadership metrics: %w", err)
	}
	writeHelp(
		output, "synara_reconciler_leadership",
		"Database-authoritative Reconciler lease inventory by bounded controller and active or expired state.",
		"gauge",
package observability

import (
	"bytes"
	"context"
	"strings"
	"testing"
	"time"

	"github.com/glebarez/sqlite"
	"github.com/google/uuid"
	"gorm.io/gorm"

	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
)

func TestWorkerPoolWarmCapacityMetricsAreFreshBoundedAndStable(t *testing.T) {
	db, err := gorm.Open(sqlite.Open(":memory:"), &gorm.Config{})
	if err != nil {
		t.Fatal(err)
	}
	if err := db.AutoMigrate(&persistence.WorkerPoolWarmCapacity{}); err != nil {
		t.Fatal(err)
	}

	now := time.Date(2026, time.July, 25, 12, 0, 0, 0, time.UTC)
	tenantID := uuid.New()
	targetID := uuid.New()
	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
	unboundedClass := "tenant-" + uuid.NewString()
	rows := []persistence.WorkerPoolWarmCapacity{
		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
	}
	if err := db.Create(&rows).Error; err != nil {
		t.Fatal(err)
	}

	registry := New(db)
	var first bytes.Buffer
	if err := registry.writeWorkerPoolWarmCapacityMetrics(context.Background(), &first, now); err != nil {
		t.Fatal(err)
	}
	var second bytes.Buffer
	if err := registry.writeWorkerPoolWarmCapacityMetrics(context.Background(), &second, now); err != nil {
		t.Fatal(err)
	}
	if first.String() != second.String() {
		t.Fatalf("warm-capacity metric output is not stable:\nfirst:\n%s\nsecond:\n%s", first.String(), second.String())
	}

	metrics := first.String()
	for _, expected := range []string{
		`synara_worker_pool_warm_capacity_authorities{capacity_class="interactive",freshness="expired",warm_supported="false"} 1`,
		`synara_worker_pool_warm_capacity_authorities{capacity_class="interactive",freshness="fresh",warm_supported="true"} 1`,
		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
	} {
		if !strings.Contains(metrics, expected) {
			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
		}
	}
	for _, capacityClass := range []string{"interactive", "other", "standard"} {
		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
			if count := strings.Count(metrics, sample); count != 1 {
				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
			}
		}
	}
	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
	}
	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
		if strings.Contains(metrics, forbidden) {
			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
		}
	}
}

func warmCapacityMetricFixture(
	poolID, tenantID, targetID uuid.UUID,
	capacityClass string,
	warmSupported bool,
	desiredTotal, claimed, readyIdle int,
	expiresAt time.Time,
) persistence.WorkerPoolWarmCapacity {
	observedAt := expiresAt.Add(-time.Minute)
	return persistence.WorkerPoolWarmCapacity{
		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
		CapacityClass: capacityClass, WarmSupported: warmSupported,
		DesiredIdleUnits: 2, MaxActiveUnits: 10,
		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
	}
}

func uuidStrings(values []uuid.UUID) []string {
	result := make([]string, 0, len(values))
	for _, value := range values {
		result = append(result, value.String())
	}
	return result
}

exec
/bin/zsh -lc "sed -n '600,940p' services/control-plane/internal/executiontargets/kubernetes_reconciler.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
			reason := "execution-pod-obsolete"
			if terminalPod {
				reason = "execution-pod-terminal"
			}
			deletedPod, _, err := r.deleteObservedPodSafely(
				ctx, client, target.ID, configuration.Namespace, pod, reason,
			)
			if err != nil {
				return err
			}
			if deletedPod {
				deleted++
			} else {
				existing[pod.Name] = pod
			}
			continue
		}
		existing[pod.Name] = pod
	}
	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
	)
	if err != nil {
		return err
	}
	readyWarmCapacity := make(map[kubernetesWarmCapacityKey]int)
	warmDemandEvictionCandidates := make([]kubernetesWarmDemandEvictionCandidate, 0)
	for _, observed := range unleasedWarmPods {
		plan, found := validationWarmPlansByName[observed.Pod.Name]
		terminalPod := observed.Pod.Phase == "Succeeded" || observed.Pod.Phase == "Failed"
		if !found || terminalPod ||
			observed.Pod.Annotations[kubernetesConfigAnnotation] != plan.ConfigHash ||
			observed.PoolID != plan.Pool.ID ||
			observed.PoolVersion != plan.Pool.Version ||
			observed.CapacityClass != plan.Pool.CapacityClass ||
			observed.Slot != plan.Slot ||
			(observed.State != nil && !kubernetesWarmWorkerStateMatchesPlan(*observed.State, plan)) {
			reason := "warm-pool-plan-mismatch"
			if !found {
				reason = "warm-pool-scale-down"
			} else if terminalPod {
				reason = "warm-pool-terminal"
			}
			deletedPod, retainedWorker, err := r.deleteObservedPodSafely(
				ctx, client, target.ID, configuration.Namespace, observed.Pod, reason,
			)
			if err != nil {
				return err
			}
			if retainedWorker != nil {
				if retainedWorker.WorkerPoolID != nil {
					warmClaimedCounts[*retainedWorker.WorkerPoolID]++
				}
				existing[observed.Pod.Name] = observed.Pod
				continue
			}
			if deletedPod {
				deleted++
				continue
			}
			existing[observed.Pod.Name] = observed.Pod
			continue
		}
		if observed.State == nil {
			existing[observed.Pod.Name] = observed.Pod
			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
					Observed: observed,
					Key:      key,
				})
			}
			continue
		}
		readinessObservedAt := r.now()
		if !kubernetesWarmWorkerStateReadyIdle(
			*observed.State,
			observed.Pod,
			plan,
			readinessObservedAt,
			r.config.WorkerHeartbeatTimeout,
		) {
			if kubernetesWarmWorkerStateShouldRecycle(
				*observed.State,
				observed.Pod,
				readinessObservedAt,
				r.config.WorkerHeartbeatTimeout,
			) {
				deletedPod, retainedWorker, err := r.deleteObservedPodSafely(
					ctx, client, target.ID, configuration.Namespace, observed.Pod, "warm-pool-not-ready",
				)
				if err != nil {
					return err
				}
				if retainedWorker != nil {
					if retainedWorker.WorkerPoolID != nil {
						warmClaimedCounts[*retainedWorker.WorkerPoolID]++
					}
					existing[observed.Pod.Name] = observed.Pod
					continue
				}
				if deletedPod {
					deleted++
					continue
				}
			}
			existing[observed.Pod.Name] = observed.Pod
			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
					Observed: observed,
					Key:      key,
				})
			}
			continue
		}
		existing[observed.Pod.Name] = observed.Pod
		if key, ok := kubernetesWarmCapacityKeyForState(*observed.State); ok {
			readyWarmCapacity[key]++
		}
	}
	readyForDemand := make(map[kubernetesWarmCapacityKey]int, len(readyWarmCapacity))
	for key, units := range readyWarmCapacity {
		readyForDemand[key] = units
	}
	coldNeeded := 0
	unmetWarmDemand := make(map[kubernetesWarmCapacityKey]int)
	for _, execution := range executions {
		if execution.Status != "queued" && execution.Status != "recovering" {
			continue
		}
		if !kubernetesExecutionWithinAbsoluteLifetime(execution, r.now()) {
			continue
		}
		if _, found := existing[kubernetesPodName(execution)]; found {
			continue
		}
		if key, ok := kubernetesWarmCapacityKeyForExecution(execution); ok {
			if readyForDemand[key] > 0 {
				readyForDemand[key]--
				continue
			}
			unmetWarmDemand[key]++
		}
		coldNeeded++
	}
	freeSlots := configuration.MaxActivePods - (len(existing) + deleted)
	if freeSlots < 0 {
		freeSlots = 0
	}
	demandEvictionsNeeded := coldNeeded - freeSlots
	if demandEvictionsNeeded > 0 {
		attempted := make(map[int]struct{}, len(warmDemandEvictionCandidates))
		evictCandidate := func(index int) error {
			attempted[index] = struct{}{}
			candidate := warmDemandEvictionCandidates[index]
			deletedPod, retainedWorker, err := r.deleteObservedPodSafely(
				ctx,
				client,
				target.ID,
				configuration.Namespace,
				candidate.Observed.Pod,
				"warm-pool-demand-fallback",
			)
			if err != nil {
				return err
			}
			if retainedWorker != nil {
				if retainedWorker.WorkerPoolID != nil {
					warmClaimedCounts[*retainedWorker.WorkerPoolID]++
				}
				return nil
			}
			if deletedPod {
				delete(existing, candidate.Observed.Pod.Name)
				deleted++
				demandEvictionsNeeded--
				if unmetWarmDemand[candidate.Key] > 0 {
					unmetWarmDemand[candidate.Key]--
				}
			}
			return nil
		}
		// Prefer evicting a not-ready Pod whose exact warm pool/release key
		// cannot satisfy queued demand. If the remaining cold demand is for a
		// general or different-pool Execution, any not-ready warm slot can still
		// be released because maxActivePods is target-wide.
		for index, candidate := range warmDemandEvictionCandidates {
			if demandEvictionsNeeded <= 0 {
				break
			}
			if unmetWarmDemand[candidate.Key] <= 0 {
				continue
			}
			if err := evictCandidate(index); err != nil {
				return err
			}
		}
		for index := range warmDemandEvictionCandidates {
			if demandEvictionsNeeded <= 0 {
				break
			}
			if _, found := attempted[index]; found {
				continue
			}
			if err := evictCandidate(index); err != nil {
				return err
			}
		}
	}
	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
	)
	if err != nil {
		return err
	}
	// A Pod accepted for deletion still consumes ResourceQuota until Kubernetes
	// finishes its grace period. Count deletion-pending Pods against the target
	// capacity so reconciliation does not create a replacement that the API
	// server must reject with an exceeded-quota error.
	scheduled := len(existing) + deleted
	// One Execution that cannot get a Pod must not starve every other queued
	// Execution on this target for the cycle: a malformed pool scheduling
	// template, a rejected Pod spec or a failed observation write is scoped to
	// its own Execution. Failures are collected and reported after the sweep,
	// so the pass is still reported as failed and warm capacity stays
	// unpublished, but the remaining Executions are still placed.
	var executionFailures []error
	for _, execution := range executions {
		if execution.Status != "queued" && execution.Status != "recovering" {
			continue
		}
		name := kubernetesPodName(execution)
		if _, found := existing[name]; found {
			acknowledgedReservations[routing.ReservationIdentity{
				ExecutionID: execution.ID, Generation: execution.Generation,
			}] = struct{}{}
			continue
		}
		if key, ok := kubernetesWarmCapacityKeyForExecution(execution); ok && readyWarmCapacity[key] > 0 {
			readyWarmCapacity[key]--
			acknowledgedReservations[routing.ReservationIdentity{
				ExecutionID: execution.ID, Generation: execution.Generation,
			}] = struct{}{}
			continue
		}
		if scheduled >= configuration.MaxActivePods {
			continue
		}
		podHash, err := kubernetesExecutionPodHash(podBaseHash, configuration.Image, execution)
		if err != nil {
			executionFailures = append(executionFailures, fmt.Errorf("execution %s: %w", execution.ID, err))
			continue
		}
		pod, err := r.executionPod(target, configuration, podHash, execution, credential)
		if err != nil {
			executionFailures = append(executionFailures, fmt.Errorf("execution %s: %w", execution.ID, err))
			continue
		}
		// The Session can cross its immutable deadline after the initial query.
		// Recheck at the external write boundary so a stale reconciliation
		// snapshot cannot create a post-expiry Pod.
		if !kubernetesExecutionWithinAbsoluteLifetime(execution, r.now()) {
			continue
		}
		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
		applyStartedAt := r.now()
		applyErr := client.Apply(ctx, path, pod)
		observation := KubernetesExecutionPodObservation{
			TenantID: execution.TenantID, ExecutionTargetID: target.ID,
			ExecutionID: execution.ID, Generation: execution.Generation + 1,
			Namespace: configuration.Namespace, PodName: name, Phase: "Applied",
			PendingFailureThreshold: r.podPendingFailureThreshold(), ObservedAt: applyStartedAt,
		}
		if applyErr != nil {
			observation.Phase = "ApplyFailed"
			observation.FailureClass = KubernetesPodFailureApplyFailed
			observation.FailureReasonCode = classifyKubernetesPodApplyFailure(applyErr)
		}
		if err := r.observeExecutionPod(ctx, observation); err != nil {
			if applyErr != nil {
				err = errors.Join(
					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", applyErr),
					err,
				)
			}
			executionFailures = append(executionFailures, fmt.Errorf("execution %s: %w", execution.ID, err))
			continue
		}
		if applyErr != nil {
			executionFailures = append(executionFailures, fmt.Errorf(
				"execution %s: %w", execution.ID,
				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", applyErr),
			))
			continue
		}
		acknowledgedReservations[routing.ReservationIdentity{
			ExecutionID: execution.ID, Generation: execution.Generation,
		}] = struct{}{}
		created++
		scheduled++
	}
	for _, plan := range desiredWarmPlans {
		if scheduled >= configuration.MaxActivePods {
			break
		}
		name := kubernetesWarmPodName(plan)
		if _, found := existing[name]; found {
			continue
		}
		pod, err := r.warmPoolPod(target, configuration, plan, credential)
		if err != nil {
			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
			continue
		}
		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
		if err := client.Apply(ctx, path, pod); err != nil {
			executionFailures = append(executionFailures, fmt.Errorf(
				"warm pod %s: %w", name,
				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
			))
			continue
		}
		created++
		scheduled++
	}
	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
		return errors.Join(append(executionFailures, err)...)
	}
	if len(executionFailures) > 0 {
		// The target itself is reachable and its status is current, so the
		// health observation published by the deferred hook stays accurate;
		// only the per-Execution placements failed.
		return errors.Join(executionFailures...)
	}
	availableCapacity := configuration.MaxActivePods
	healthObservation.Status = routing.HealthHealthy
	healthObservation.CapacityStatus = routing.CapacityAvailable
	healthObservation.AvailableCapacityUnits = &availableCapacity
	healthObservation.AllocatedCapacityUnits = scheduled
	healthObservation.ReservationAuthority.Acknowledgements = make(
		[]routing.ReservationIdentity, 0, len(acknowledgedReservations),
	)

exec
/bin/zsh -lc "sed -n '1,230p' services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
sed -n '1,250p' services/control-plane/internal/warmcapacity/service.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
package executiontargets

import (
	"context"
	"strings"
	"time"

	"github.com/google/uuid"

	"github.com/synara-ai/synara/services/control-plane/internal/warmcapacity"
)

const managedKubernetesWarmCapacityPublisherPrefix = "managed-kubernetes-warm-capacity-publisher:"

type ManagedKubernetesWarmCapacityObservation struct {
	TenantID                uuid.UUID
	ExecutionTargetID       uuid.UUID
	WorkerPoolID            uuid.UUID
	WorkerPoolVersion       int64
	WarmSupported           bool
	WorkerReleaseRevisionID *uuid.UUID
	WorkerReleaseChannel    *string
	DesiredTotalUnits       int
	ClaimedUnits            int
	ReadyIdleUnits          int
	Reason                  *string
	ObservedAt              time.Time
}

type ManagedKubernetesWarmCapacityObserver func(context.Context, ManagedKubernetesWarmCapacityObservation) error

type ManagedKubernetesWarmCapacityPublisherConfig struct {
	PublisherIdentity string
	ObservationTTL    time.Duration
}

type ManagedKubernetesWarmCapacityPublisher struct {
	warmCapacity      *warmcapacity.Service
	publisherIdentity string
	observationTTL    time.Duration
	now               func() time.Time
}

func NewManagedKubernetesWarmCapacityPublisher(
	targets *Service,
	config ManagedKubernetesWarmCapacityPublisherConfig,
) *ManagedKubernetesWarmCapacityPublisher {
	ttl := config.ObservationTTL
	if ttl <= 0 {
		ttl = 90 * time.Second
	}
	return &ManagedKubernetesWarmCapacityPublisher{
		warmCapacity:      warmcapacity.NewService(targets.db),
		publisherIdentity: normalizeManagedKubernetesWarmCapacityPublisherIdentity(config.PublisherIdentity),
		observationTTL:    ttl,
		now:               func() time.Time { return time.Now().UTC() },
	}
}

func (p *ManagedKubernetesWarmCapacityPublisher) PublishReconcile(
	ctx context.Context,
	observation ManagedKubernetesWarmCapacityObservation,
) error {
	observedAt := observation.ObservedAt.UTC()
	if observedAt.IsZero() {
		observedAt = p.now()
	}
	_, err := p.warmCapacity.Observe(ctx, warmcapacity.Observation{
		TenantID:                observation.TenantID,
		ExecutionTargetID:       observation.ExecutionTargetID,
		WorkerPoolID:            observation.WorkerPoolID,
		WorkerPoolVersion:       observation.WorkerPoolVersion,
		WarmSupported:           observation.WarmSupported,
		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
		DesiredTotalUnits:       observation.DesiredTotalUnits,
		ClaimedUnits:            observation.ClaimedUnits,
		ReadyIdleUnits:          observation.ReadyIdleUnits,
		Source:                  p.publisherIdentity,
		Reason:                  observation.Reason,
		ObservedAt:              observedAt,
		TTL:                     p.observationTTL,
	})
	return err
}

func normalizeManagedKubernetesWarmCapacityPublisherIdentity(identity string) string {
	identity = strings.TrimSpace(identity)
	if identity == "" {
		identity = managedKubernetesWarmCapacityPublisherPrefix + "default"
	}
	if len(identity) > 160 {
		identity = identity[:160]
	}
	return identity
}
package warmcapacity

import (
	"context"
	"errors"
	"strings"
	"time"

	"github.com/google/uuid"
	"gorm.io/gorm"

	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
	"github.com/synara-ai/synara/services/control-plane/internal/problem"
)

const (
	poolModeWarm             = "warm"
	poolStatusDisabled       = "disabled"
	targetKindKubernetes     = "kubernetes"
	targetStatusActive       = "active"
	capacityClassStandard    = "standard"
	capacityClassInteractive = "interactive"
	releaseChannelPromoted   = "promoted"
	releaseChannelCanary     = "canary"
	minObservationTTL        = 10 * time.Second
	maxObservationTTL        = time.Hour
	maxReasonLength          = 2000
)

type Observation struct {
	TenantID                uuid.UUID
	ExecutionTargetID       uuid.UUID
	WorkerPoolID            uuid.UUID
	WorkerPoolVersion       int64
	WarmSupported           bool
	WorkerReleaseRevisionID *uuid.UUID
	WorkerReleaseChannel    *string
	DesiredTotalUnits       int
	ClaimedUnits            int
	ReadyIdleUnits          int
	Source                  string
	Reason                  *string
	ObservedAt              time.Time
	TTL                     time.Duration
}

type FreshLookup struct {
	TenantID                uuid.UUID
	ExecutionTargetID       uuid.UUID
	WorkerPoolID            uuid.UUID
	WorkerPoolVersion       int64
	WorkerReleaseRevisionID *uuid.UUID
	WorkerReleaseChannel    *string
	Now                     time.Time
}

type Service struct {
	db  *gorm.DB
	now func() time.Time
}

func NewService(db *gorm.DB) *Service {
	return &Service{
		db:  db,
		now: func() time.Time { return time.Now().UTC() },
	}
}

func (s *Service) Observe(ctx context.Context, input Observation) (persistence.WorkerPoolWarmCapacity, error) {
	if input.TenantID == uuid.Nil || input.ExecutionTargetID == uuid.Nil || input.WorkerPoolID == uuid.Nil || input.WorkerPoolVersion <= 0 {
		return persistence.WorkerPoolWarmCapacity{}, problem.New(400, "invalid_warm_capacity_scope", "Tenant, Execution Target, Worker Pool, and positive Worker Pool version are required.")
	}
	if input.TTL < minObservationTTL || input.TTL > maxObservationTTL {
		return persistence.WorkerPoolWarmCapacity{}, problem.New(400, "invalid_warm_capacity_ttl", "Warm capacity TTL must be between 10 seconds and 1 hour.")
	}
	source := strings.TrimSpace(input.Source)
	if source == "" || len(source) > 160 {
		return persistence.WorkerPoolWarmCapacity{}, problem.New(400, "invalid_warm_capacity_source", "Warm capacity source must be between 1 and 160 characters.")
	}
	reason, err := normalizeReason(input.Reason)
	if err != nil {
		return persistence.WorkerPoolWarmCapacity{}, err
	}
	releaseRevisionID, releaseChannel, err := normalizeReleasePair(input.WorkerReleaseRevisionID, input.WorkerReleaseChannel)
	if err != nil {
		return persistence.WorkerPoolWarmCapacity{}, err
	}
	if err := validateCounters(input.WarmSupported, input.DesiredTotalUnits, input.ClaimedUnits, input.ReadyIdleUnits); err != nil {
		return persistence.WorkerPoolWarmCapacity{}, err
	}
	observedAt := input.ObservedAt.UTC()
	if observedAt.IsZero() {
		observedAt = s.now()
	}

	var result persistence.WorkerPoolWarmCapacity
	err = persistence.InTransaction(ctx, s.db, func(tx *gorm.DB) error {
		target, pool, err := loadScopedWarmPool(ctx, tx, input.TenantID, input.ExecutionTargetID, input.WorkerPoolID)
		if err != nil {
			return err
		}
		if target.Kind != targetKindKubernetes || target.Status != targetStatusActive || target.TenantID == nil {
			return problem.New(409, "warm_capacity_target_unsupported", "Warm capacity authority is only available for active tenant-owned Kubernetes Execution Targets.")
		}
		if pool.Mode != poolModeWarm {
			return problem.New(409, "warm_capacity_pool_mode_invalid", "Warm capacity authority only applies to Worker Pools in warm mode.")
		}
		if pool.Status == poolStatusDisabled {
			return problem.New(409, "warm_capacity_pool_disabled", "Warm capacity authority cannot be written for a disabled Worker Pool.")
		}
		if pool.Version != input.WorkerPoolVersion {
			return problem.New(409, "warm_capacity_pool_version_stale", "Warm capacity authority must reference the current Worker Pool version.")
		}
		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
		}
		if input.DesiredTotalUnits > pool.MaxActiveUnits {
			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
		}
		if releaseRevisionID != nil {
			var revision persistence.WorkerReleaseRevision
			if err := tx.WithContext(ctx).
				Where("id = ? AND tenant_id = ? AND execution_target_id = ?", *releaseRevisionID, input.TenantID, input.ExecutionTargetID).
				Take(&revision).Error; err != nil {
				if errors.Is(err, gorm.ErrRecordNotFound) {
					return problem.New(409, "warm_capacity_release_pair_invalid", "Warm capacity authority must reference a Worker release revision owned by this Execution Target.")
				}
				return problem.Wrap(500, "warm_capacity_release_lookup_failed", "Warm capacity release metadata could not be loaded.", err)
			}
		}

		var current persistence.WorkerPoolWarmCapacity
		err = persistence.WithLocking(tx.WithContext(ctx), "UPDATE", "").
			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
				input.TenantID, input.ExecutionTargetID, input.WorkerPoolID, input.WorkerPoolVersion).
			Take(&current).Error
		now := s.now()
		if errors.Is(err, gorm.ErrRecordNotFound) {
			result = persistence.WorkerPoolWarmCapacity{
				WorkerPoolID:            input.WorkerPoolID,
				WorkerPoolVersion:       input.WorkerPoolVersion,
				TenantID:                input.TenantID,
				ExecutionTargetID:       input.ExecutionTargetID,
				CapacityClass:           pool.CapacityClass,
				WarmSupported:           input.WarmSupported,
				WorkerReleaseRevisionID: releaseRevisionID,
				WorkerReleaseChannel:    releaseChannel,
				DesiredIdleUnits:        pool.DesiredIdleUnits,
				MaxActiveUnits:          pool.MaxActiveUnits,
				DesiredTotalUnits:       input.DesiredTotalUnits,
				ClaimedUnits:            input.ClaimedUnits,
				ReadyIdleUnits:          input.ReadyIdleUnits,
				Source:                  source,
				Reason:                  reason,
				ObservedAt:              observedAt,
				ExpiresAt:               observedAt.Add(input.TTL),
				Version:                 1,
				UpdatedAt:               now,
			}
			if err := tx.WithContext(ctx).Create(&result).Error; err != nil {
				return problem.Wrap(409, "warm_capacity_create_rejected", "Warm capacity authority could not be created.", err)
			}
			return nil
		}
		if err != nil {
			return problem.Wrap(500, "warm_capacity_load_failed", "Warm capacity authority could not be loaded.", err)
		}
		if !observedAt.After(current.ObservedAt) {
			return problem.New(409, "warm_capacity_observation_stale", "Warm capacity authority must advance observedAt monotonically.")
		}
		result = current
		result.CapacityClass = pool.CapacityClass
		result.WarmSupported = input.WarmSupported
		result.WorkerReleaseRevisionID = releaseRevisionID
		result.WorkerReleaseChannel = releaseChannel
		result.DesiredIdleUnits = pool.DesiredIdleUnits
		result.MaxActiveUnits = pool.MaxActiveUnits
		result.DesiredTotalUnits = input.DesiredTotalUnits
		result.ClaimedUnits = input.ClaimedUnits
		result.ReadyIdleUnits = input.ReadyIdleUnits
		result.Source = source
		result.Reason = reason
		result.ObservedAt = observedAt
		result.ExpiresAt = observedAt.Add(input.TTL)
		result.Version = current.Version + 1
		result.UpdatedAt = now

		updates := map[string]any{
			"capacity_class":             result.CapacityClass,
			"warm_supported":             result.WarmSupported,
			"worker_release_revision_id": result.WorkerReleaseRevisionID,
			"worker_release_channel":     result.WorkerReleaseChannel,
			"desired_idle_units":         result.DesiredIdleUnits,
			"max_active_units":           result.MaxActiveUnits,
			"desired_total_units":        result.DesiredTotalUnits,
			"claimed_units":              result.ClaimedUnits,
			"ready_idle_units":           result.ReadyIdleUnits,
			"source":                     result.Source,
			"reason":                     result.Reason,
			"observed_at":                result.ObservedAt,
			"expires_at":                 result.ExpiresAt,
			"version":                    result.Version,
			"updated_at":                 result.UpdatedAt,
		}
		update := tx.WithContext(ctx).Model(&persistence.WorkerPoolWarmCapacity{}).
			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ? AND version = ?",
				input.TenantID, input.ExecutionTargetID, input.WorkerPoolID, input.WorkerPoolVersion, current.Version).
			Updates(updates)
		if update.Error != nil {
			return problem.Wrap(409, "warm_capacity_update_rejected", "Warm capacity authority could not be advanced.", update.Error)
		}
		if update.RowsAffected != 1 {
			return problem.New(409, "warm_capacity_version_conflict", "Warm capacity authority changed concurrently; reload and retry.")
		}
		return nil
	})
	return result, err
}

func (s *Service) GetFresh(ctx context.Context, lookup FreshLookup) (*persistence.WorkerPoolWarmCapacity, error) {
	if lookup.TenantID == uuid.Nil || lookup.ExecutionTargetID == uuid.Nil || lookup.WorkerPoolID == uuid.Nil || lookup.WorkerPoolVersion <= 0 {
		return nil, problem.New(400, "invalid_warm_capacity_lookup", "Tenant, Execution Target, Worker Pool, and positive Worker Pool version are required.")
	}
	releaseRevisionID, releaseChannel, err := normalizeReleasePair(lookup.WorkerReleaseRevisionID, lookup.WorkerReleaseChannel)
	if err != nil {
		return nil, err
	}
	at := lookup.Now.UTC()
	if at.IsZero() {
		at = s.now()
	}

	var row persistence.WorkerPoolWarmCapacity
	err = s.db.WithContext(ctx).
		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
			lookup.TenantID, lookup.ExecutionTargetID, lookup.WorkerPoolID, lookup.WorkerPoolVersion).
		Take(&row).Error
	if errors.Is(err, gorm.ErrRecordNotFound) {
		return nil, nil
	}
	if err != nil {
		return nil, problem.Wrap(500, "warm_capacity_lookup_failed", "Warm capacity authority could not be loaded.", err)
	}
	if !row.ExpiresAt.After(at) {
		return nil, nil
	}
	if !releasePairMatches(row, releaseRevisionID, releaseChannel) {
		return nil, nil
	}
	return &row, nil

exec
/bin/zsh -lc "sed -n '600,840p' services/control-plane/internal/executiontargets/kubernetes_reconciler.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
			reason := "execution-pod-obsolete"
			if terminalPod {
				reason = "execution-pod-terminal"
			}
			deletedPod, _, err := r.deleteObservedPodSafely(
				ctx, client, target.ID, configuration.Namespace, pod, reason,
			)
			if err != nil {
				return err
			}
			if deletedPod {
				deleted++
			} else {
				existing[pod.Name] = pod
			}
			continue
		}
		existing[pod.Name] = pod
	}
	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
	)
	if err != nil {
		return err
	}
	readyWarmCapacity := make(map[kubernetesWarmCapacityKey]int)
	warmDemandEvictionCandidates := make([]kubernetesWarmDemandEvictionCandidate, 0)
	for _, observed := range unleasedWarmPods {
		plan, found := validationWarmPlansByName[observed.Pod.Name]
		terminalPod := observed.Pod.Phase == "Succeeded" || observed.Pod.Phase == "Failed"
		if !found || terminalPod ||
			observed.Pod.Annotations[kubernetesConfigAnnotation] != plan.ConfigHash ||
			observed.PoolID != plan.Pool.ID ||
			observed.PoolVersion != plan.Pool.Version ||
			observed.CapacityClass != plan.Pool.CapacityClass ||
			observed.Slot != plan.Slot ||
			(observed.State != nil && !kubernetesWarmWorkerStateMatchesPlan(*observed.State, plan)) {
			reason := "warm-pool-plan-mismatch"
			if !found {
				reason = "warm-pool-scale-down"
			} else if terminalPod {
				reason = "warm-pool-terminal"
			}
			deletedPod, retainedWorker, err := r.deleteObservedPodSafely(
				ctx, client, target.ID, configuration.Namespace, observed.Pod, reason,
			)
			if err != nil {
				return err
			}
			if retainedWorker != nil {
				if retainedWorker.WorkerPoolID != nil {
					warmClaimedCounts[*retainedWorker.WorkerPoolID]++
				}
				existing[observed.Pod.Name] = observed.Pod
				continue
			}
			if deletedPod {
				deleted++
				continue
			}
			existing[observed.Pod.Name] = observed.Pod
			continue
		}
		if observed.State == nil {
			existing[observed.Pod.Name] = observed.Pod
			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
					Observed: observed,
					Key:      key,
				})
			}
			continue
		}
		readinessObservedAt := r.now()
		if !kubernetesWarmWorkerStateReadyIdle(
			*observed.State,
			observed.Pod,
			plan,
			readinessObservedAt,
			r.config.WorkerHeartbeatTimeout,
		) {
			if kubernetesWarmWorkerStateShouldRecycle(
				*observed.State,
				observed.Pod,
				readinessObservedAt,
				r.config.WorkerHeartbeatTimeout,
			) {
				deletedPod, retainedWorker, err := r.deleteObservedPodSafely(
					ctx, client, target.ID, configuration.Namespace, observed.Pod, "warm-pool-not-ready",
				)
				if err != nil {
					return err
				}
				if retainedWorker != nil {
					if retainedWorker.WorkerPoolID != nil {
						warmClaimedCounts[*retainedWorker.WorkerPoolID]++
					}
					existing[observed.Pod.Name] = observed.Pod
					continue
				}
				if deletedPod {
					deleted++
					continue
				}
			}
			existing[observed.Pod.Name] = observed.Pod
			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
					Observed: observed,
					Key:      key,
				})
			}
			continue
		}
		existing[observed.Pod.Name] = observed.Pod
		if key, ok := kubernetesWarmCapacityKeyForState(*observed.State); ok {
			readyWarmCapacity[key]++
		}
	}
	readyForDemand := make(map[kubernetesWarmCapacityKey]int, len(readyWarmCapacity))
	for key, units := range readyWarmCapacity {
		readyForDemand[key] = units
	}
	coldNeeded := 0
	unmetWarmDemand := make(map[kubernetesWarmCapacityKey]int)
	for _, execution := range executions {
		if execution.Status != "queued" && execution.Status != "recovering" {
			continue
		}
		if !kubernetesExecutionWithinAbsoluteLifetime(execution, r.now()) {
			continue
		}
		if _, found := existing[kubernetesPodName(execution)]; found {
			continue
		}
		if key, ok := kubernetesWarmCapacityKeyForExecution(execution); ok {
			if readyForDemand[key] > 0 {
				readyForDemand[key]--
				continue
			}
			unmetWarmDemand[key]++
		}
		coldNeeded++
	}
	freeSlots := configuration.MaxActivePods - (len(existing) + deleted)
	if freeSlots < 0 {
		freeSlots = 0
	}
	demandEvictionsNeeded := coldNeeded - freeSlots
	if demandEvictionsNeeded > 0 {
		attempted := make(map[int]struct{}, len(warmDemandEvictionCandidates))
		evictCandidate := func(index int) error {
			attempted[index] = struct{}{}
			candidate := warmDemandEvictionCandidates[index]
			deletedPod, retainedWorker, err := r.deleteObservedPodSafely(
				ctx,
				client,
				target.ID,
				configuration.Namespace,
				candidate.Observed.Pod,
				"warm-pool-demand-fallback",
			)
			if err != nil {
				return err
			}
			if retainedWorker != nil {
				if retainedWorker.WorkerPoolID != nil {
					warmClaimedCounts[*retainedWorker.WorkerPoolID]++
				}
				return nil
			}
			if deletedPod {
				delete(existing, candidate.Observed.Pod.Name)
				deleted++
				demandEvictionsNeeded--
				if unmetWarmDemand[candidate.Key] > 0 {
					unmetWarmDemand[candidate.Key]--
				}
			}
			return nil
		}
		// Prefer evicting a not-ready Pod whose exact warm pool/release key
		// cannot satisfy queued demand. If the remaining cold demand is for a
		// general or different-pool Execution, any not-ready warm slot can still
		// be released because maxActivePods is target-wide.
		for index, candidate := range warmDemandEvictionCandidates {
			if demandEvictionsNeeded <= 0 {
				break
			}
			if unmetWarmDemand[candidate.Key] <= 0 {
				continue
			}
			if err := evictCandidate(index); err != nil {
				return err
			}
		}
		for index := range warmDemandEvictionCandidates {
			if demandEvictionsNeeded <= 0 {
				break
			}
			if _, found := attempted[index]; found {
				continue
			}
			if err := evictCandidate(index); err != nil {
				return err
			}
		}
	}
	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
	)
	if err != nil {
		return err
	}
	// A Pod accepted for deletion still consumes ResourceQuota until Kubernetes
	// finishes its grace period. Count deletion-pending Pods against the target
	// capacity so reconciliation does not create a replacement that the API
	// server must reject with an exceeded-quota error.
	scheduled := len(existing) + deleted
	// One Execution that cannot get a Pod must not starve every other queued
	// Execution on this target for the cycle: a malformed pool scheduling
	// template, a rejected Pod spec or a failed observation write is scoped to
	// its own Execution. Failures are collected and reported after the sweep,
	// so the pass is still reported as failed and warm capacity stays
	// unpublished, but the remaining Executions are still placed.
	var executionFailures []error
	for _, execution := range executions {
		if execution.Status != "queued" && execution.Status != "recovering" {
			continue
		}
		name := kubernetesPodName(execution)
		if _, found := existing[name]; found {
			acknowledgedReservations[routing.ReservationIdentity{
				ExecutionID: execution.ID, Generation: execution.Generation,
			}] = struct{}{}
			continue
		}
		if key, ok := kubernetesWarmCapacityKeyForExecution(execution); ok && readyWarmCapacity[key] > 0 {
			readyWarmCapacity[key]--
			acknowledgedReservations[routing.ReservationIdentity{
				ExecutionID: execution.ID, Generation: execution.Generation,

exec
/bin/zsh -lc "rg -l 'WorkerPoolWarmCapacity|worker_pool_warm_capacity' services/control-plane/internal/database services/control-plane/internal/persistence services/control-plane/internal/app | sort" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
rg: services/control-plane/internal/app: No such file or directory (os error 2)
services/control-plane/internal/database/kubernetes_pod_deletion_fences_migration_integration_test.go
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
services/control-plane/internal/persistence/schema.go
services/control-plane/internal/persistence/warm_capacity_models.go

exec
/bin/zsh -lc "sed -n '840,980p' services/control-plane/internal/executiontargets/kubernetes_reconciler.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
				ExecutionID: execution.ID, Generation: execution.Generation,
			}] = struct{}{}
			continue
		}
		if scheduled >= configuration.MaxActivePods {
			continue
		}
		podHash, err := kubernetesExecutionPodHash(podBaseHash, configuration.Image, execution)
		if err != nil {
			executionFailures = append(executionFailures, fmt.Errorf("execution %s: %w", execution.ID, err))
			continue
		}
		pod, err := r.executionPod(target, configuration, podHash, execution, credential)
		if err != nil {
			executionFailures = append(executionFailures, fmt.Errorf("execution %s: %w", execution.ID, err))
			continue
		}
		// The Session can cross its immutable deadline after the initial query.
		// Recheck at the external write boundary so a stale reconciliation
		// snapshot cannot create a post-expiry Pod.
		if !kubernetesExecutionWithinAbsoluteLifetime(execution, r.now()) {
			continue
		}
		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
		applyStartedAt := r.now()
		applyErr := client.Apply(ctx, path, pod)
		observation := KubernetesExecutionPodObservation{
			TenantID: execution.TenantID, ExecutionTargetID: target.ID,
			ExecutionID: execution.ID, Generation: execution.Generation + 1,
			Namespace: configuration.Namespace, PodName: name, Phase: "Applied",
			PendingFailureThreshold: r.podPendingFailureThreshold(), ObservedAt: applyStartedAt,
		}
		if applyErr != nil {
			observation.Phase = "ApplyFailed"
			observation.FailureClass = KubernetesPodFailureApplyFailed
			observation.FailureReasonCode = classifyKubernetesPodApplyFailure(applyErr)
		}
		if err := r.observeExecutionPod(ctx, observation); err != nil {
			if applyErr != nil {
				err = errors.Join(
					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", applyErr),
					err,
				)
			}
			executionFailures = append(executionFailures, fmt.Errorf("execution %s: %w", execution.ID, err))
			continue
		}
		if applyErr != nil {
			executionFailures = append(executionFailures, fmt.Errorf(
				"execution %s: %w", execution.ID,
				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", applyErr),
			))
			continue
		}
		acknowledgedReservations[routing.ReservationIdentity{
			ExecutionID: execution.ID, Generation: execution.Generation,
		}] = struct{}{}
		created++
		scheduled++
	}
	for _, plan := range desiredWarmPlans {
		if scheduled >= configuration.MaxActivePods {
			break
		}
		name := kubernetesWarmPodName(plan)
		if _, found := existing[name]; found {
			continue
		}
		pod, err := r.warmPoolPod(target, configuration, plan, credential)
		if err != nil {
			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
			continue
		}
		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
		if err := client.Apply(ctx, path, pod); err != nil {
			executionFailures = append(executionFailures, fmt.Errorf(
				"warm pod %s: %w", name,
				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
			))
			continue
		}
		created++
		scheduled++
	}
	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
		return errors.Join(append(executionFailures, err)...)
	}
	if len(executionFailures) > 0 {
		// The target itself is reachable and its status is current, so the
		// health observation published by the deferred hook stays accurate;
		// only the per-Execution placements failed.
		return errors.Join(executionFailures...)
	}
	availableCapacity := configuration.MaxActivePods
	healthObservation.Status = routing.HealthHealthy
	healthObservation.CapacityStatus = routing.CapacityAvailable
	healthObservation.AvailableCapacityUnits = &availableCapacity
	healthObservation.AllocatedCapacityUnits = scheduled
	healthObservation.ReservationAuthority.Acknowledgements = make(
		[]routing.ReservationIdentity, 0, len(acknowledgedReservations),
	)
	for identity := range acknowledgedReservations {
		healthObservation.ReservationAuthority.Acknowledgements = append(
			healthObservation.ReservationAuthority.Acknowledgements,
			identity,
		)
	}
	sort.Slice(healthObservation.ReservationAuthority.Acknowledgements, func(left, right int) bool {
		leftIdentity := healthObservation.ReservationAuthority.Acknowledgements[left]
		rightIdentity := healthObservation.ReservationAuthority.Acknowledgements[right]
		if leftIdentity.ExecutionID != rightIdentity.ExecutionID {
			return leftIdentity.ExecutionID.String() < rightIdentity.ExecutionID.String()
		}
		return leftIdentity.Generation < rightIdentity.Generation
	})
	healthObservation.Reason = nil
	if scheduled >= configuration.MaxActivePods {
		healthObservation.CapacityStatus = routing.CapacitySaturated
		healthObservation.Reason = managedKubernetesRoutingReasonPointer(
			"Managed Kubernetes scheduled pod capacity is fully allocated.",
		)
	}
	warmCapacityObservations = managedKubernetesWarmCapacityObservations(
		*target.TenantID,
		target.ID,
		warmPools,
		warmPoolsSupported,
		warmClaimedCounts,
		readyWarmCapacity,
		warmRelease,
	)
	return nil
}

func kubernetesPodCompletedSuccessfully(pod kubernetesPod) bool {
	if pod.Phase != "Succeeded" || len(pod.Containers) != 1 {
		return false
	}
	container := pod.Containers[0]
	return container.Name == "agentd" && container.Terminated && container.ExitCode == 0
}

exec
/bin/zsh -lc "sed -n '300,370p' services/control-plane/internal/executiontargets/kubernetes_warm_pool.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
}

func sameOptionalUUID(left, right *uuid.UUID) bool {
	if left == nil || right == nil {
		return left == nil && right == nil
	}
	return *left == *right
}

func sameOptionalString(left, right *string) bool {
	if left == nil || right == nil {
		return left == nil && right == nil
	}
	return *left == *right
}

func kubernetesWarmPodPlans(
	warmPools []kubernetesWarmPool,
	warmPoolsSupported bool,
	warmClaimedCounts map[uuid.UUID]int,
	warmRelease kubernetesWarmReleaseSelection,
	podBaseHash string,
	baseImage string,
) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
	plans := make([]kubernetesWarmPodPlan, 0)
	plansByName := make(map[string]kubernetesWarmPodPlan)
	if !warmPoolsSupported {
		return plans, plansByName, nil
	}
	for _, pool := range warmPools {
		if pool.Status != placement.PoolStatusActive {
			continue
		}
		claimed := warmClaimedCounts[pool.ID]
		if claimed < 0 {
			claimed = 0
		}
		desiredTotal := pool.DesiredIdleUnits + claimed
		if desiredTotal > pool.MaxActiveUnits {
			desiredTotal = pool.MaxActiveUnits
		}
		for slot := 0; slot < desiredTotal; slot++ {
			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
			if err != nil {
				return nil, nil, err
			}
			plan.ConfigHash = configHash
			plans = append(plans, plan)
			plansByName[kubernetesWarmPodName(plan)] = plan
		}
	}
	return plans, plansByName, nil
}

func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
	compactPoolID := strings.ReplaceAll(plan.Pool.ID.String(), "-", "")
	releaseKey := "unmanaged"
	if plan.Release.RevisionID != nil && plan.Release.Channel != nil {
		compactReleaseID := strings.ReplaceAll(plan.Release.RevisionID.String(), "-", "")
		channelKey := "p"
		if *plan.Release.Channel == "canary" {
			channelKey = "c"
		}
		releaseKey = channelKey + compactReleaseID[:8]
	}
	return fmt.Sprintf("synara-warm-%s-v%s-%s-s%d", compactPoolID[:10], strconv.FormatInt(plan.Pool.Version, 16), releaseKey, plan.Slot)
}

func kubernetesWarmPoolImage(baseImage string, release kubernetesWarmReleaseSelection) (string, error) {
	if release.RevisionID == nil && release.Channel == nil && release.ImageDigest == nil {

exec
/bin/zsh -lc "rg -n 'AutoMigrate\\(' services/control-plane/internal/database services/control-plane/internal | head -n 100" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
services/control-plane/internal/placement/service_test.go:466:	if err := db.AutoMigrate(persistence.AllModels()...); err != nil {
services/control-plane/internal/database/capacity_reservation_sqlite_test.go:23:	if err := db.AutoMigrate(
services/control-plane/internal/database/project_git_binding_authority_sqlite_test.go:28:	if err := store.DB().WithContext(ctx).AutoMigrate(persistence.AllModels()...); err != nil {
services/control-plane/internal/database/worker_revocation_sqlite_test.go:215:		err = store.DB().WithContext(ctx).AutoMigrate(persistence.AllModels()...)
services/control-plane/internal/warmcapacity/service_test.go:287:	if err := db.AutoMigrate(persistence.AllModels()...); err != nil {
services/control-plane/internal/database/store.go:88:	if err := s.db.WithContext(ctx).AutoMigrate(persistence.AllModels()...); err != nil {
services/control-plane/internal/credentialscope/resolver_test.go:186:	if err := db.AutoMigrate(
services/control-plane/internal/enterpriseidentity/service_test.go:426:	if err := db.AutoMigrate(persistence.AllModels()...); err != nil {
services/control-plane/internal/database/execution_scheduling_decisions_sqlite_test.go:22:	if err := db.AutoMigrate(
services/control-plane/internal/routing/service_test.go:840:	if err := db.AutoMigrate(
services/control-plane/internal/database/worker_mode_sqlite_test.go:28:	if err := store.DB().WithContext(ctx).AutoMigrate(persistence.AllModels()...); err != nil {
services/control-plane/internal/scim/service_test.go:55:	if err := db.AutoMigrate(persistence.AllModels()...); err != nil {
services/control-plane/internal/leadership/service_test.go:193:	if err := db.AutoMigrate(&persistence.ReconcilerLease{}); err != nil {
services/control-plane/internal/leadership/write_fence_test.go:17:	if err := db.AutoMigrate(&fencedMutationRecord{}); err != nil {
services/control-plane/internal/workerfacts/service_test.go:347:	if err := db.AutoMigrate(
services/control-plane/internal/reconcilerleadership/runner_test.go:188:	if err := db.AutoMigrate(&persistence.ReconcilerLease{}); err != nil {
services/control-plane/internal/schedulingdecision/schedulingdecision_test.go:151:	if err := db.AutoMigrate(
services/control-plane/internal/metricrollup/service_test.go:20:	if err := db.AutoMigrate(
services/control-plane/internal/metricrollup/service_test.go:107:	if err := db.AutoMigrate(
services/control-plane/internal/metricrollup/service_test.go:192:	if err := db.AutoMigrate(
services/control-plane/internal/sessions/logical_history_test.go:129:	if err := db.AutoMigrate(&persistence.AgentSession{}, &persistence.SessionEvent{}); err != nil {
services/control-plane/internal/database/capacity_reservation_sqlite_test.go:23:	if err := db.AutoMigrate(
services/control-plane/internal/database/project_git_binding_authority_sqlite_test.go:28:	if err := store.DB().WithContext(ctx).AutoMigrate(persistence.AllModels()...); err != nil {
services/control-plane/internal/billing/shared_allocation_test.go:374:	if err := db.AutoMigrate(
services/control-plane/internal/serviceaccounts/service_test.go:60:	if err := db.AutoMigrate(persistence.AllModels()...); err != nil {
services/control-plane/internal/observability/worker_incarnation_fact_metrics_test.go:22:	if err := db.AutoMigrate(&persistence.WorkerIncarnationFact{}); err != nil {
services/control-plane/internal/observability/worker_incarnation_fact_metrics_test.go:109:	if err := db.AutoMigrate(
services/control-plane/internal/database/worker_revocation_sqlite_test.go:215:		err = store.DB().WithContext(ctx).AutoMigrate(persistence.AllModels()...)
services/control-plane/internal/billing/service_test.go:719:	if err := db.AutoMigrate(
services/control-plane/internal/observability/platform_routing_authority_metrics_test.go:22:	if err := db.AutoMigrate(&persistence.ExecutionTarget{}, &persistence.PlatformRoutingPublication{}); err != nil {
services/control-plane/internal/observability/billing_shared_allocation_schedule_metrics_test.go:22:	if err := db.AutoMigrate(&persistence.BillingSharedAllocationSchedulePeriod{}); err != nil {
services/control-plane/internal/database/execution_scheduling_decisions_sqlite_test.go:22:	if err := db.AutoMigrate(
services/control-plane/internal/database/worker_mode_sqlite_test.go:28:	if err := store.DB().WithContext(ctx).AutoMigrate(persistence.AllModels()...); err != nil {
services/control-plane/internal/observability/metrics_test.go:33:	if err := db.AutoMigrate(models...); err != nil {
services/control-plane/internal/observability/metrics_test.go:402:	if err := db.AutoMigrate(
services/control-plane/internal/database/store.go:88:	if err := s.db.WithContext(ctx).AutoMigrate(persistence.AllModels()...); err != nil {
services/control-plane/internal/observability/execution_queue_metrics_test.go:22:	if err := db.AutoMigrate(&persistence.AgentExecution{}); err != nil {
services/control-plane/internal/observability/billing_shared_actual_allocation_metrics_test.go:22:	if err := db.AutoMigrate(&persistence.BillingSharedActualAllocationRun{}); err != nil {
services/control-plane/internal/observability/capacity_reservation_metrics_test.go:22:	if err := db.AutoMigrate(
services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go:22:	if err := db.AutoMigrate(&persistence.WorkerPoolWarmCapacity{}); err != nil {
services/control-plane/internal/observability/generation_fact_metrics_test.go:25:	if err := db.AutoMigrate(
services/control-plane/internal/observability/generation_fact_metrics_test.go:91:	if err := db.AutoMigrate(
services/control-plane/internal/observability/generation_fact_metrics_test.go:204:	if err := db.AutoMigrate(&persistence.SessionEvent{}, &persistence.ExecutionGenerationFact{}); err != nil {
services/control-plane/internal/observability/generation_fact_metrics_test.go:237:	if err := db.AutoMigrate(
services/control-plane/internal/observability/generation_fact_metrics_test.go:299:	if err := db.AutoMigrate(
services/control-plane/internal/billing/scheduler_test.go:151:	if err := fixture.db.AutoMigrate(&persistence.AuditLog{}); err != nil {
services/control-plane/internal/billing/scheduler_test.go:241:	if err := fixture.db.AutoMigrate(&persistence.AuditLog{}); err != nil {
services/control-plane/internal/billing/scheduler_test.go:326:	if err := fixture.db.AutoMigrate(&persistence.AuditLog{}); err != nil {
services/control-plane/internal/outbox/service_test.go:224:	if err := db.AutoMigrate(&persistence.OutboxMessage{}); err != nil {
services/control-plane/internal/outbox/service_test.go:236:	if err := db.AutoMigrate(
services/control-plane/internal/executions/workload_test.go:324:	if err := db.AutoMigrate(&persistence.AgentSession{}, &persistence.SessionEvent{}); err != nil {
services/control-plane/internal/executions/workload_test.go:535:	if err := db.AutoMigrate(&persistence.AgentSession{}, &persistence.Artifact{}); err != nil {
services/control-plane/internal/executions/resume_snapshot_history_test.go:306:	if err := db.AutoMigrate(
services/control-plane/internal/executions/claim_fair_queue_test.go:20:	if err := db.AutoMigrate(&persistence.AgentExecution{}); err != nil {
services/control-plane/internal/executions/claim_fair_queue_test.go:64:	if err := db.AutoMigrate(&persistence.AgentExecution{}); err != nil {
services/control-plane/internal/executions/generation_facts_test.go:353:	if err := db.AutoMigrate(&persistence.ExecutionGenerationFact{}); err != nil {

exec
/bin/zsh -lc "rg -n 'install.*SQLite|sqlite.*Safet|workerPool|WarmCapacity|warm capacity|warm_capacity' services/control-plane/internal/database/store.go services/control-plane/internal/database/*.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
exec
/bin/zsh -lc "sed -n '1,320p' services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
sed -n '1,300p' services/control-plane/internal/database/worker_pool_placement_sqlite_test.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
services/control-plane/internal/database/kubernetes_pod_deletion_fences_migration_integration_test.go:23:	fixture := seedWorkerPoolWarmCapacityFixture(t, db)
services/control-plane/internal/database/kubernetes_pod_deletion_fences_migration_integration_test.go:190:	fixture := seedWorkerPoolWarmCapacityFixture(t, db)
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:17:func TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation(t *testing.T) {
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:24:	fixture := seedWorkerPoolWarmCapacityFixture(t, db)
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:26:	valid := persistence.WorkerPoolWarmCapacity{
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:47:		t.Fatalf("create valid warm capacity authority: %v", err)
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:54:	assertWorkerPoolWarmCapacityConstraintRejected(
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:57:		"Worker pool warm capacity scope is invalid",
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:64:	assertWorkerPoolWarmCapacityConstraintRejected(
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:67:		"Worker pool warm capacity scope is invalid",
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:70:	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:79:		t.Fatalf("valid warm capacity update rejected: %v", err)
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:81:	assertWorkerPoolWarmCapacityConstraintRejected(
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:83:		db.Model(&persistence.WorkerPoolWarmCapacity{}).
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:91:		"Worker pool warm capacity version must advance exactly once and observed_at must advance",
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:93:	assertWorkerPoolWarmCapacityConstraintRejected(
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:95:		db.Delete(&persistence.WorkerPoolWarmCapacity{},
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:99:		"Worker pool warm capacity observations cannot be deleted",
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:103:func assertWorkerPoolWarmCapacityConstraintRejected(t *testing.T, err error, expected string) {
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:106:		t.Fatalf("PostgreSQL accepted invalid Worker Pool warm capacity state (want containing %q)", expected)
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:112:		"PostgreSQL returned the wrong Worker Pool warm capacity rejection: %v (want containing %q or a translated check constraint)",
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:118:type workerPoolWarmCapacityFixture struct {
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:129:func seedWorkerPoolWarmCapacityFixture(t *testing.T, db *gorm.DB) workerPoolWarmCapacityFixture {
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:234:	return workerPoolWarmCapacityFixture{

 succeeded in 0ms:
package database

import (
	"context"
	"errors"
	"strings"
	"testing"
	"time"

	"github.com/google/uuid"
	"gorm.io/gorm"

	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
	"github.com/synara-ai/synara/services/control-plane/migrations"
)

func TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation(t *testing.T) {
	ctx := context.Background()
	db := openPostgresIntegrationDB(t)
	if err := Migrate(ctx, db, migrations.Files); err != nil {
		t.Fatal(err)
	}

	fixture := seedWorkerPoolWarmCapacityFixture(t, db)
	releaseChannel := "promoted"
	valid := persistence.WorkerPoolWarmCapacity{
		WorkerPoolID:            fixture.pool.ID,
		WorkerPoolVersion:       fixture.pool.Version,
		TenantID:                fixture.tenantID,
		ExecutionTargetID:       fixture.target.ID,
		CapacityClass:           fixture.pool.CapacityClass,
		WarmSupported:           true,
		WorkerReleaseRevisionID: &fixture.release.ID,
		WorkerReleaseChannel:    &releaseChannel,
		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
		DesiredTotalUnits:       3,
		ClaimedUnits:            1,
		ReadyIdleUnits:          2,
		Source:                  "postgres/reconciler",
		ObservedAt:              fixture.now,
		ExpiresAt:               fixture.now.Add(time.Minute),
		Version:                 1,
		UpdatedAt:               fixture.now,
	}
	if err := db.Create(&valid).Error; err != nil {
		t.Fatalf("create valid warm capacity authority: %v", err)
	}

	invalidTarget := valid
	invalidTarget.WorkerPoolID = fixture.otherPool.ID
	invalidTarget.WorkerPoolVersion = fixture.otherPool.Version
	invalidTarget.ExecutionTargetID = fixture.target.ID
	assertWorkerPoolWarmCapacityConstraintRejected(
		t,
		db.Create(&invalidTarget).Error,
		"Worker pool warm capacity scope is invalid",
	)

	invalidRelease := valid
	invalidRelease.WorkerPoolID = fixture.otherWarmPoolSameTarget.ID
	invalidRelease.WorkerPoolVersion = fixture.otherWarmPoolSameTarget.Version
	invalidRelease.WorkerReleaseRevisionID = &fixture.otherTargetRelease.ID
	assertWorkerPoolWarmCapacityConstraintRejected(
		t,
		db.Create(&invalidRelease).Error,
		"Worker pool warm capacity scope is invalid",
	)

	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
			fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
		Updates(map[string]any{
			"version":     2,
			"observed_at": fixture.now.Add(time.Second),
			"expires_at":  fixture.now.Add(2 * time.Minute),
			"updated_at":  fixture.now.Add(time.Second),
		}).Error; err != nil {
		t.Fatalf("valid warm capacity update rejected: %v", err)
	}
	assertWorkerPoolWarmCapacityConstraintRejected(
		t,
		db.Model(&persistence.WorkerPoolWarmCapacity{}).
			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
			Updates(map[string]any{
				"version":     4,
				"observed_at": fixture.now.Add(500 * time.Millisecond),
				"updated_at":  fixture.now.Add(2 * time.Second),
			}).Error,
		"Worker pool warm capacity version must advance exactly once and observed_at must advance",
	)
	assertWorkerPoolWarmCapacityConstraintRejected(
		t,
		db.Delete(&persistence.WorkerPoolWarmCapacity{},
			"tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
			fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version,
		).Error,
		"Worker pool warm capacity observations cannot be deleted",
	)
}

func assertWorkerPoolWarmCapacityConstraintRejected(t *testing.T, err error, expected string) {
	t.Helper()
	if err == nil {
		t.Fatalf("PostgreSQL accepted invalid Worker Pool warm capacity state (want containing %q)", expected)
	}
	if strings.Contains(err.Error(), expected) || errors.Is(err, gorm.ErrCheckConstraintViolated) {
		return
	}
	t.Fatalf(
		"PostgreSQL returned the wrong Worker Pool warm capacity rejection: %v (want containing %q or a translated check constraint)",
		err,
		expected,
	)
}

type workerPoolWarmCapacityFixture struct {
	now                     time.Time
	tenantID                uuid.UUID
	target                  persistence.ExecutionTarget
	pool                    persistence.WorkerPool
	otherPool               persistence.WorkerPool
	otherWarmPoolSameTarget persistence.WorkerPool
	release                 persistence.WorkerReleaseRevision
	otherTargetRelease      persistence.WorkerReleaseRevision
}

func seedWorkerPoolWarmCapacityFixture(t *testing.T, db *gorm.DB) workerPoolWarmCapacityFixture {
	t.Helper()
	now := time.Now().UTC().Truncate(time.Microsecond)
	userID := uuid.New()
	tenantID := uuid.New()
	if err := db.Create(&persistence.User{
		ID: userID, Email: uuid.NewString() + "@example.com", DisplayName: "Warm Capacity PG",
		Status: "active", CreatedAt: now, UpdatedAt: now,
	}).Error; err != nil {
		t.Fatal(err)
	}
	if err := db.Transaction(func(tx *gorm.DB) error {
		if err := tx.Create(&persistence.Tenant{
			ID: tenantID, Slug: "warm-capacity-pg-" + uuid.NewString()[:8], Name: "Warm Capacity PG Tenant",
			Status: "active", PlanCode: "enterprise", Region: "default", Settings: map[string]any{},
			CreatedBy: userID, CreatedAt: now, UpdatedAt: now,
		}).Error; err != nil {
			return err
		}
		return tx.Create(&persistence.TenantMembership{
			TenantID: tenantID, UserID: userID, Role: "owner", Status: "active",
			JoinedAt: &now, CreatedAt: now, UpdatedAt: now,
		}).Error
	}); err != nil {
		t.Fatal(err)
	}

	target := persistence.ExecutionTarget{
		ID: uuid.New(), TenantID: &tenantID, Kind: "kubernetes", Name: "tenant-k8s-a", Status: "active",
		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
	}
	otherTarget := persistence.ExecutionTarget{
		ID: uuid.New(), TenantID: &tenantID, Kind: "kubernetes", Name: "tenant-k8s-b", Status: "active",
		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
	}
	for _, targetModel := range []*persistence.ExecutionTarget{&target, &otherTarget} {
		if err := db.Create(targetModel).Error; err != nil {
			t.Fatal(err)
		}
	}

	pool := persistence.WorkerPool{
		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
	}
	otherPool := persistence.WorkerPool{
		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: otherTarget.ID,
		Name: "warm-b", Mode: "warm", CapacityClass: "interactive",
		DesiredIdleUnits: 1, MaxActiveUnits: 4, SchedulingTemplate: map[string]any{},
		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
	}
	otherWarmPoolSameTarget := persistence.WorkerPool{
		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
		Name: "warm-c", Mode: "warm", CapacityClass: "interactive",
		DesiredIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
	}
	for _, poolModel := range []*persistence.WorkerPool{&pool, &otherPool, &otherWarmPoolSameTarget} {
		if err := db.Create(poolModel).Error; err != nil {
			t.Fatal(err)
		}
	}

	firstManifest := persistence.WorkerManifest{
		ID:                    uuid.New(),
		ManifestHash:          strings.Repeat(strings.ReplaceAll(uuid.NewString(), "-", ""), 2),
		WorkerBuildVersion:    "1.0.0",
		WorkerProtocolMinimum: 2,
		WorkerProtocolMaximum: 2,
		RuntimeEventMinimum:   2,
		RuntimeEventMaximum:   2,
		OperatingSystem:       "linux",
		Architecture:          "amd64",
		FeatureFlags:          map[string]any{},
		CreatedAt:             now,
	}
	secondManifest := firstManifest
	secondManifest.ID = uuid.New()
	secondManifest.ManifestHash = strings.Repeat(strings.ReplaceAll(uuid.NewString(), "-", ""), 2)
	if err := db.Create(&firstManifest).Error; err != nil {
		t.Fatal(err)
	}
	if err := db.Create(&secondManifest).Error; err != nil {
		t.Fatal(err)
	}

	release := persistence.WorkerReleaseRevision{
		ID: uuid.New(), TenantID: tenantID, ExecutionTargetID: target.ID,
		Revision: 1, WorkerManifestID: firstManifest.ID, Description: "release-a",
		CreatedBy: userID, CreatedAt: now,
	}
	otherTargetRelease := persistence.WorkerReleaseRevision{
		ID: uuid.New(), TenantID: tenantID, ExecutionTargetID: otherTarget.ID,
		Revision: 1, WorkerManifestID: secondManifest.ID, Description: "release-b",
		CreatedBy: userID, CreatedAt: now,
	}
	if err := db.Create(&release).Error; err != nil {
		t.Fatal(err)
	}
	if err := db.Create(&otherTargetRelease).Error; err != nil {
		t.Fatal(err)
	}

	return workerPoolWarmCapacityFixture{
		now:                     now,
		tenantID:                tenantID,
		target:                  target,
		pool:                    pool,
		otherPool:               otherPool,
		otherWarmPoolSameTarget: otherWarmPoolSameTarget,
		release:                 release,
		otherTargetRelease:      otherTargetRelease,
	}
}
package database

import (
	"testing"
	"time"

	"github.com/google/uuid"

	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
)

func TestSQLiteExecutionPlacementPolicyUpdateFencesOptionalPoolScope(t *testing.T) {
	fixture := openStage4SQLiteFixture(t)
	now := time.Now().UTC().Truncate(time.Second)
	otherTargetID := uuid.New()
	if err := fixture.db.Create(&persistence.ExecutionTarget{
		ID: otherTargetID, TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
		Kind: "local", Name: "Other placement target", Status: "active",
		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
	}).Error; err != nil {
		t.Fatal(err)
	}

	newPool := func(targetID uuid.UUID, name string) persistence.WorkerPool {
		return persistence.WorkerPool{
			ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: targetID,
			Name: name, Mode: "resident", CapacityClass: "standard",
			SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
			MaxActiveUnits: 1, CreatedAt: now, UpdatedAt: now,
		}
	}
	defaultPool := newPool(fixture.targetID, "default-placement")
	balancedPool := newPool(fixture.targetID, "balanced-placement")
	foreignPool := newPool(otherTargetID, "foreign-placement")
	for _, pool := range []*persistence.WorkerPool{&defaultPool, &balancedPool, &foreignPool} {
		if err := fixture.db.Create(pool).Error; err != nil {
			t.Fatalf("create Worker pool %s: %v", pool.Name, err)
		}
	}
	policy := persistence.ExecutionPlacementPolicy{
		TenantID: &fixture.tenantID, ExecutionTargetID: fixture.targetID,
		Version: 1, DefaultPoolID: defaultPool.ID, UpdatedAt: now,
	}
	if err := fixture.db.Create(&policy).Error; err != nil {
		t.Fatalf("create placement policy: %v", err)
	}

	assertSQLiteStage4Rejected(
		t,
		fixture.db.Exec(`
			UPDATE execution_placement_policies
			SET version = 2, balanced_pool_id = ?
			WHERE execution_target_id = ?`, foreignPool.ID, fixture.targetID,
		).Error,
		"invalid Execution placement policy update",
	)
	assertSQLiteStage4Rejected(
		t,
		fixture.db.Exec(`
			UPDATE execution_placement_policies
			SET version = 2, low_latency_pool_id = ?
			WHERE execution_target_id = ?`, uuid.New(), fixture.targetID,
		).Error,
		"invalid Execution placement policy update",
	)
	if err := fixture.db.Exec(`
		UPDATE execution_placement_policies
		SET version = 2, balanced_pool_id = ?
		WHERE execution_target_id = ?`, balancedPool.ID, fixture.targetID,
	).Error; err != nil {
		t.Fatalf("SQLite rejected valid optional placement pool update: %v", err)
	}
}

exec
/bin/zsh -lc "sed -n '1,240p' services/control-plane/internal/database/store.go
sed -n '1,260p' services/control-plane/internal/persistence/schema.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
package database

import (
	"context"
	"fmt"
	"io/fs"
	"os"
	"path/filepath"
	"time"

	"github.com/glebarez/sqlite"
	"gorm.io/gorm"

	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
	"github.com/synara-ai/synara/services/control-plane/internal/platform"
)

type MetadataStore interface {
	DB() *gorm.DB
	Kind() platform.MetadataStore
	Migrate(context.Context, fs.FS) error
	Close() error
}

type store struct {
	db                   *gorm.DB
	kind                 platform.MetadataStore
	migrationLockTimeout time.Duration
}

func OpenMetadataStore(ctx context.Context, config platform.Config, databaseURL, sqlitePath string, values ...Options) (MetadataStore, error) {
	options := resolveOptions(values)
	switch config.MetadataStore {
	case platform.MetadataPostgres:
		db, err := Open(ctx, databaseURL, options)
		if err != nil {
			return nil, err
		}
		return &store{db: db, kind: platform.MetadataPostgres, migrationLockTimeout: options.MigrationLockTimeout}, nil
	case platform.MetadataSQLite:
		if err := os.MkdirAll(filepath.Dir(sqlitePath), 0o700); err != nil {
			return nil, fmt.Errorf("create sqlite metadata directory: %w", err)
		}
		db, err := gorm.Open(sqlite.Open(sqlitePath), &gorm.Config{
			TranslateError: true, SkipDefaultTransaction: true,
			Logger: gormLogger(),
		})
		if err != nil {
			return nil, fmt.Errorf("open sqlite metadata store: %w", err)
		}
		sqlDB, err := db.DB()
		if err != nil {
			return nil, fmt.Errorf("resolve sqlite pool: %w", err)
		}
		sqlDB.SetMaxOpenConns(1)
		sqlDB.SetMaxIdleConns(1)
		sqlDB.SetConnMaxLifetime(0)
		if err := db.WithContext(ctx).Exec("PRAGMA foreign_keys = ON").Error; err != nil {
			_ = sqlDB.Close()
			return nil, fmt.Errorf("enable sqlite foreign keys: %w", err)
		}
		if err := db.WithContext(ctx).Exec("PRAGMA busy_timeout = 5000").Error; err != nil {
			_ = sqlDB.Close()
			return nil, fmt.Errorf("configure sqlite busy timeout: %w", err)
		}
		if err := db.WithContext(ctx).Exec("PRAGMA journal_mode = WAL").Error; err != nil {
			_ = sqlDB.Close()
			return nil, fmt.Errorf("configure sqlite journal mode: %w", err)
		}
		if err := db.WithContext(ctx).Exec("PRAGMA synchronous = NORMAL").Error; err != nil {
			_ = sqlDB.Close()
			return nil, fmt.Errorf("configure sqlite synchronous mode: %w", err)
		}
		return &store{db: db, kind: platform.MetadataSQLite, migrationLockTimeout: options.MigrationLockTimeout}, nil
	default:
		return nil, fmt.Errorf("unsupported metadata store %q", config.MetadataStore)
	}
}

func (s *store) DB() *gorm.DB { return s.db }

func (s *store) Kind() platform.MetadataStore { return s.kind }

func (s *store) Migrate(ctx context.Context, files fs.FS) error {
	if s.kind == platform.MetadataPostgres {
		return Migrate(ctx, s.db, files, s.migrationLockTimeout)
	}
	if err := s.db.WithContext(ctx).AutoMigrate(persistence.AllModels()...); err != nil {
		return fmt.Errorf("auto-migrate sqlite metadata schema: %w", err)
	}
	if err := migrateSQLiteSafety(ctx, s.db); err != nil {
		return err
	}
	return nil
}

func migrateSQLiteSafety(ctx context.Context, db *gorm.DB) error {
	statements := []string{
		`UPDATE agent_sessions
		 SET resource_state = CASE
		       WHEN EXISTS (
		         SELECT 1 FROM agent_executions AS execution
		         WHERE execution.tenant_id = agent_sessions.tenant_id
		           AND execution.session_id = agent_sessions.id
		           AND execution.status = 'suspended'
		       ) THEN 'suspended'
		       WHEN EXISTS (
		         SELECT 1 FROM agent_executions AS execution
		         WHERE execution.tenant_id = agent_sessions.tenant_id
		           AND execution.session_id = agent_sessions.id
		           AND execution.status = 'recovering'
		       ) THEN 'restoring'
		       WHEN EXISTS (
		         SELECT 1 FROM agent_executions AS execution
		         WHERE execution.tenant_id = agent_sessions.tenant_id
		           AND execution.session_id = agent_sessions.id
		           AND execution.status = 'queued'
		       ) THEN 'provisioning'
		       WHEN EXISTS (
		         SELECT 1 FROM agent_executions AS execution
		         WHERE execution.tenant_id = agent_sessions.tenant_id
		           AND execution.session_id = agent_sessions.id
		           AND execution.status = 'waiting-for-approval'
		       ) THEN 'waiting'
		       WHEN EXISTS (
		         SELECT 1 FROM agent_executions AS execution
		         WHERE execution.tenant_id = agent_sessions.tenant_id
		           AND execution.session_id = agent_sessions.id
		           AND execution.status IN ('leased', 'running')
		       ) THEN 'active'
		       ELSE 'idle'
		     END
		 WHERE resource_state IS NULL OR resource_state = ''`,
		`UPDATE agent_sessions
		 SET resource_idle_since = CASE
		       WHEN EXISTS (
		         SELECT 1 FROM agent_executions AS execution
		         WHERE execution.tenant_id = agent_sessions.tenant_id
		           AND execution.session_id = agent_sessions.id
		           AND execution.status IN ('queued', 'leased', 'running', 'waiting-for-approval', 'recovering')
		       ) THEN NULL
		       ELSE COALESCE(updated_at, created_at, CURRENT_TIMESTAMP)
		     END,
		     meaningful_activity_at = COALESCE(updated_at, created_at, CURRENT_TIMESTAMP)
		 WHERE meaningful_activity_at IS NULL`,
		`UPDATE agent_sessions
		 SET meaningful_activity_sequence = last_event_sequence
		 WHERE meaningful_activity_sequence IS NULL
		    OR meaningful_activity_sequence < 0
		    OR meaningful_activity_sequence > last_event_sequence`,
		`UPDATE agent_sessions
		 SET provider_resume_cursor_encrypted = NULL,
		     provider_resume_cursor_state = 'absent',
		     provider_resume_cursor_source_execution_id = NULL,
		     provider_resume_cursor_source_generation = NULL,
		     provider_resume_cursor_history_sequence = NULL
		 WHERE provider_resume_cursor_encrypted IS NULL
		    OR length(provider_resume_cursor_encrypted) = 0`,
		`UPDATE agent_sessions
		 SET provider_resume_cursor_state = 'quarantined',
		     provider_resume_cursor_source_execution_id = NULL,
		     provider_resume_cursor_source_generation = NULL,
		     provider_resume_cursor_history_sequence = NULL
		 WHERE length(provider_resume_cursor_encrypted) > 0
		   AND (provider_resume_cursor_state IS NULL
		     OR provider_resume_cursor_state = ''
		     OR provider_resume_cursor_state = 'absent')`,
		`DROP INDEX IF EXISTS uq_agent_executions_session_active`,
		`CREATE UNIQUE INDEX IF NOT EXISTS uq_agent_executions_session_active
		 ON agent_executions (tenant_id, session_id)
		 WHERE status IN ('queued', 'leased', 'running', 'waiting-for-approval', 'recovering', 'suspended')`,
		`CREATE UNIQUE INDEX IF NOT EXISTS uq_execution_recovery_bundle_generation
		 ON execution_recovery_bundles (tenant_id, execution_id, generation)`,
		`CREATE INDEX IF NOT EXISTS idx_execution_recovery_bundles_session_created
		 ON execution_recovery_bundles (tenant_id, session_id, created_at DESC, id)`,
		`CREATE INDEX IF NOT EXISTS idx_session_events_execution_started_lookup
		 ON session_events (tenant_id, session_id, execution_id, generation, occurred_at)
		 WHERE event_type = 'execution.started'
		   AND execution_id IS NOT NULL
		   AND generation IS NOT NULL`,
		`CREATE INDEX IF NOT EXISTS idx_session_events_execution_generation_lifecycle
		 ON session_events (tenant_id, execution_id, generation, event_type, occurred_at, event_id)
		 WHERE execution_id IS NOT NULL
		   AND generation IS NOT NULL
		   AND event_type IN (
		     'execution.started',
		     'session.started',
		     'execution.completed',
		     'execution.failed',
		     'execution.cancelled',
		     'execution.interrupted'
		   )`,
		`CREATE INDEX IF NOT EXISTS idx_session_events_provider_resume_claim_decision
		 ON session_events (occurred_at, tenant_id, event_id)
		 WHERE event_type = 'execution.leased'`,
		`CREATE INDEX IF NOT EXISTS idx_session_events_provider_resume_runtime_fallback
		 ON session_events (occurred_at, tenant_id, event_id)
		 WHERE event_type = 'runtime.warning'`,
		`CREATE UNIQUE INDEX IF NOT EXISTS uq_execution_suspend_attempts_active
		 ON execution_suspend_attempts (tenant_id, execution_id, generation)
		 WHERE status = 'checkpointing'`,
		`CREATE INDEX IF NOT EXISTS idx_execution_suspend_attempts_execution
		 ON execution_suspend_attempts (tenant_id, execution_id, requested_at DESC, id)`,
		`CREATE INDEX IF NOT EXISTS idx_execution_suspend_attempts_deadline
		 ON execution_suspend_attempts (checkpoint_deadline_at, tenant_id, execution_id, id)
		 WHERE status = 'checkpointing'`,
		`UPDATE execution_suspend_attempts
		 SET completion_mode = 'worker-attested-v1'
		 WHERE completion_mode IS NULL
		    OR trim(completion_mode) = ''`,
		`UPDATE execution_suspend_attempts AS attempt
		 SET execution_target_id = execution.execution_target_id
		 FROM agent_executions AS execution
		 WHERE attempt.execution_target_id IS NULL
		   AND execution.tenant_id = attempt.tenant_id
		   AND execution.id = attempt.execution_id`,
		`CREATE INDEX IF NOT EXISTS idx_agent_sessions_resource_idle
		 ON agent_sessions (tenant_id, resource_idle_since, id)
		 WHERE status = 'active' AND resource_idle_since IS NOT NULL`,
		`CREATE INDEX IF NOT EXISTS idx_agent_sessions_absolute_expiry
		 ON agent_sessions (absolute_expires_at, tenant_id, id)
		 WHERE status IN ('active', 'suspended') AND absolute_expires_at IS NOT NULL`,
		`CREATE INDEX IF NOT EXISTS idx_worker_leases_provider_credential_access_expiry
		 ON worker_leases (provider_credential_access_expires_at, tenant_id, execution_id)
		 WHERE provider_credential_grant_id IS NOT NULL`,
		`CREATE INDEX IF NOT EXISTS idx_worker_leases_provider_credential_refresh_deadline
		 ON worker_leases (provider_credential_refresh_deadline_at, tenant_id, execution_id)
		 WHERE provider_credential_grant_id IS NOT NULL`,
		`CREATE UNIQUE INDEX IF NOT EXISTS uq_agent_memory_heads_user_key
		 ON agent_memory_heads (tenant_id, scope_user_id, memory_key)
		 WHERE scope_type = 'user'`,
		`CREATE UNIQUE INDEX IF NOT EXISTS uq_agent_memory_heads_project_key
		 ON agent_memory_heads (tenant_id, scope_project_id, memory_key)
		 WHERE scope_type = 'project'`,
		`CREATE UNIQUE INDEX IF NOT EXISTS uq_agent_memory_heads_session_key
		 ON agent_memory_heads (tenant_id, scope_session_id, memory_key)
		 WHERE scope_type = 'session'`,
		`CREATE UNIQUE INDEX IF NOT EXISTS uq_agent_memory_revision_number
		 ON agent_memory_revisions (tenant_id, memory_head_id, revision_number)`,
		`CREATE INDEX IF NOT EXISTS idx_agent_memory_revisions_artifact
package persistence

import (
	"gorm.io/gorm"
	"gorm.io/gorm/clause"
)

func AllModels() []any {
	return []any{
		&PlatformInstallation{}, &MetadataImport{}, &User{}, &UserIdentity{}, &Tenant{},
		&TenantMembership{}, &Organization{}, &OrganizationMembership{}, &LoginSession{},
		&TenantInvitation{}, &AuditLog{}, &OutboxMessage{}, &TenantQuota{}, &Project{}, &ExecutionTarget{}, &KubernetesPodDeletionFence{},
		&AgentSession{}, &AgentTurn{}, &SessionEvent{}, &Automation{}, &WorkerInstance{}, &WorkerIdentityTombstone{},
		&AgentExecution{}, &ExecutionSchedulingDecision{}, &ExecutionSchedulingCandidate{}, &ExecutionCapacityAdmission{}, &ExecutionRecoveryBundle{}, &ExecutionSuspendAttempt{}, &WorkerLease{}, &WorkerRequestReceipt{}, &APIIdempotencyKey{}, &ExecutionInteraction{},
		&ExecutionControlCommand{}, &Artifact{},
		&ArtifactPayloadMigration{}, &ArtifactAccessToken{}, &ProviderCredential{}, &ProviderCredentialScopePolicy{},
		&AgentMemoryHead{}, &AgentMemoryRevision{},
		&CredentialBinding{}, &ExecutionCredentialGrant{}, &ExecutionProviderCredentialGrant{},
		&WorkerManifest{}, &WorkerProviderManifest{}, &WorkerReleaseRevision{}, &WorkerReleasePolicy{},
		&WorkerReleaseTransition{}, &WorkerReleaseAutoRollbackWindow{}, &WorkerPool{}, &ExecutionPlacementPolicy{},
		&WorkerPoolWarmCapacity{},
		&ExecutionTargetGroup{}, &ExecutionTargetGroupMember{}, &ExecutionTargetHealth{}, &ExecutionTargetReservationAcknowledgement{},
		&ExecutionTargetDRReadiness{}, &ExecutionLocationOutage{}, &ExecutionFailoverAttempt{},
		&PlatformRoutingPublication{},
		&ExecutionSchedulingPolicyHead{}, &ExecutionSchedulingPolicyRevision{},
		&ExecutionSchedulingPolicyRule{}, &ExecutionSchedulingPolicyRuleValue{},
		&ExecutionGenerationFact{}, &ExecutionGenerationPodFailureFact{},
		&WorkerIncarnationFact{}, &WorkerIncarnationMetricRollup{}, &WorkerIncarnationMetricRollupEntry{},
		&ExecutionGenerationMetricRollup{}, &ExecutionGenerationMetricRollupEntry{},
		&ExecutionGenerationPodFailureMetricRollupEntry{},
		&WorkerClaimFact{}, &WorkerClaimReleaseFact{},
		&BillingProviderTariff{}, &BillingEstimatedUsageCharge{}, &BillingActualInvoiceImport{}, &BillingActualInvoiceLine{},
		&BillingSharedTargetLedgerCoverage{}, &BillingSharedCostAllocationRun{}, &BillingSharedEstimatedChargeSlice{},
		&BillingSharedAllocationSchedulePeriod{},
		&BillingSharedActualAllocationRun{}, &BillingSharedActualAllocationLine{}, &BillingSharedActualChargeSlice{},
		&ProviderRuntimeBinding{}, &RemoteWorkspace{},
		&WorkspaceMaterialization{}, &WorkspaceCleanupCommand{}, &WorkspaceCheckpoint{},
		&SSEConnectionLease{}, &ReconcilerLease{},
		&TenantRetentionPolicy{}, &TenantResourceLifecyclePolicy{}, &ProjectResourceLifecyclePolicy{},
		&IdentityConnection{}, &IdentityLoginAttempt{},
		&ServiceAccount{}, &ServiceAccountToken{}, &IdentityGroup{}, &IdentityGroupMember{},
		&IdentityGroupMapping{},
	}
}

func WithLocking(db *gorm.DB, strength, options string) *gorm.DB {
	if db.Dialector.Name() != "postgres" {
		return db
	}
	return db.Clauses(clause.Locking{Strength: strength, Options: options})
}

exec
/bin/zsh -lc "sed -n '240,560p' services/control-plane/internal/database/store.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
		`CREATE INDEX IF NOT EXISTS idx_agent_memory_revisions_artifact
		 ON agent_memory_revisions (tenant_id, artifact_id, id)`,
		`CREATE INDEX IF NOT EXISTS idx_project_resource_lifecycle_policies_tenant
		 ON project_resource_lifecycle_policies (tenant_id, project_id)`,
		`UPDATE worker_instances
		 SET registration_trust_mode = 'shared-token'
		 WHERE registration_trust_mode IS NULL
		    OR trim(registration_trust_mode) = ''`,
		`UPDATE worker_instances
		 SET worker_mode = CASE
		       WHEN target_kind = 'kubernetes' THEN 'execution-pinned'
		       ELSE 'general-pool'
		     END
		 WHERE worker_mode IS NULL
		    OR trim(worker_mode) = ''`,
		`UPDATE agent_executions
		 SET warm_pool_mode_snapshot = COALESCE((
		       SELECT CASE
		         WHEN lower(trim(session.warm_pool_mode)) IN ('disabled', 'balanced', 'low-latency')
		           THEN lower(trim(session.warm_pool_mode))
		         ELSE 'disabled'
		       END
		       FROM agent_sessions AS session
		       WHERE session.tenant_id = agent_executions.tenant_id
		         AND session.id = agent_executions.session_id
		     ), 'disabled')
		 WHERE warm_pool_mode_snapshot IS NULL
		    OR trim(warm_pool_mode_snapshot) = ''`,
		`DROP TRIGGER IF EXISTS trg_worker_instances_worker_mode_insert`,
		`CREATE TRIGGER trg_worker_instances_worker_mode_insert
		 BEFORE INSERT ON worker_instances
		 WHEN NEW.worker_mode IS NULL
		   OR NEW.worker_mode NOT IN ('execution-pinned', 'warm-pool', 'general-pool')
		 BEGIN
		   SELECT RAISE(ABORT, 'invalid Worker mode');
		 END`,
		`DROP TRIGGER IF EXISTS trg_worker_instances_worker_mode_update`,
		`CREATE TRIGGER trg_worker_instances_worker_mode_update
		 BEFORE UPDATE OF worker_mode ON worker_instances
		 WHEN NEW.worker_mode IS NULL
		   OR NEW.worker_mode NOT IN ('execution-pinned', 'warm-pool', 'general-pool')
		 BEGIN
		   SELECT RAISE(ABORT, 'invalid Worker mode');
		 END`,
		`DROP TRIGGER IF EXISTS trg_worker_instances_identity_insert`,
		`CREATE TRIGGER trg_worker_instances_identity_insert
		 BEFORE INSERT ON worker_instances
		 BEGIN
		   SELECT RAISE(ABORT, 'invalid Worker identity snapshot')
		   WHERE NOT (
		     (NEW.worker_mode = 'execution-pinned'
		       AND NEW.assigned_execution_id IS NOT NULL
		       AND NEW.worker_pool_id IS NULL
		       AND NEW.worker_pool_version IS NULL
		       AND NEW.capacity_class IS NULL)
		     OR
		     (NEW.worker_mode = 'warm-pool'
		       AND NEW.assigned_execution_id IS NULL
		       AND NEW.worker_pool_id IS NOT NULL
		       AND NEW.worker_pool_version > 0
		       AND NEW.capacity_class IN ('standard', 'interactive'))
		     OR
		     (NEW.worker_mode = 'general-pool'
		       AND NEW.assigned_execution_id IS NULL
		       AND NEW.worker_pool_id IS NULL
		       AND NEW.worker_pool_version IS NULL
		       AND NEW.capacity_class IS NULL)
		   );

		   SELECT RAISE(ABORT, 'Worker assigned Execution scope mismatch')
		   WHERE NEW.assigned_execution_id IS NOT NULL AND NOT EXISTS (
		     SELECT 1
		     FROM agent_executions AS execution
		     WHERE execution.id = NEW.assigned_execution_id
		       AND execution.execution_target_id = NEW.execution_target_id
		       AND execution.target_kind = NEW.target_kind
		   );

		   SELECT RAISE(ABORT, 'Worker pool identity mismatch')
		   WHERE NEW.worker_pool_id IS NOT NULL AND NOT EXISTS (
		     SELECT 1
		     FROM worker_pools AS pool
		     WHERE pool.id = NEW.worker_pool_id
		       AND pool.execution_target_id = NEW.execution_target_id
		       AND pool.mode = 'warm'
		       AND pool.status = 'active'
		       AND pool.version = NEW.worker_pool_version
		       AND pool.capacity_class = NEW.capacity_class
		   );
		 END`,
		`DROP TRIGGER IF EXISTS trg_worker_instances_identity_update`,
		`CREATE TRIGGER trg_worker_instances_identity_update
		 BEFORE UPDATE OF worker_mode, assigned_execution_id, worker_pool_id, worker_pool_version, capacity_class, execution_target_id, target_kind
		 ON worker_instances
		 BEGIN
		   SELECT RAISE(ABORT, 'invalid Worker identity snapshot')
		   WHERE NOT (
		     (NEW.worker_mode = 'execution-pinned'
		       AND NEW.assigned_execution_id IS NOT NULL
		       AND NEW.worker_pool_id IS NULL
		       AND NEW.worker_pool_version IS NULL
		       AND NEW.capacity_class IS NULL)
		     OR
		     (NEW.worker_mode = 'warm-pool'
		       AND NEW.assigned_execution_id IS NULL
		       AND NEW.worker_pool_id IS NOT NULL
		       AND NEW.worker_pool_version > 0
		       AND NEW.capacity_class IN ('standard', 'interactive'))
		     OR
		     (NEW.worker_mode = 'general-pool'
		       AND NEW.assigned_execution_id IS NULL
		       AND NEW.worker_pool_id IS NULL
		       AND NEW.worker_pool_version IS NULL
		       AND NEW.capacity_class IS NULL)
		   );

		   SELECT RAISE(ABORT, 'Worker assigned Execution scope mismatch')
		   WHERE NEW.assigned_execution_id IS NOT NULL AND NOT EXISTS (
		     SELECT 1
		     FROM agent_executions AS execution
		     WHERE execution.id = NEW.assigned_execution_id
		       AND execution.execution_target_id = NEW.execution_target_id
		       AND execution.target_kind = NEW.target_kind
		   );

		   SELECT RAISE(ABORT, 'Worker pool identity mismatch')
		   WHERE NEW.worker_pool_id IS NOT NULL AND NOT EXISTS (
		     SELECT 1
		     FROM worker_pools AS pool
		     WHERE pool.id = NEW.worker_pool_id
		       AND pool.execution_target_id = NEW.execution_target_id
		       AND pool.mode = 'warm'
		       AND pool.status = 'active'
		       AND pool.version = NEW.worker_pool_version
		       AND pool.capacity_class = NEW.capacity_class
		   );
		 END`,
		`DROP TRIGGER IF EXISTS trg_agent_executions_warm_pool_mode_snapshot_insert`,
		`CREATE TRIGGER trg_agent_executions_warm_pool_mode_snapshot_insert
		 BEFORE INSERT ON agent_executions
		 WHEN NEW.warm_pool_mode_snapshot IS NULL
		   OR NEW.warm_pool_mode_snapshot NOT IN ('disabled', 'balanced', 'low-latency')
		 BEGIN
		   SELECT RAISE(ABORT, 'invalid warm_pool_mode_snapshot');
		 END`,
		`DROP TRIGGER IF EXISTS trg_agent_executions_warm_pool_mode_snapshot_update`,
		`CREATE TRIGGER trg_agent_executions_warm_pool_mode_snapshot_update
		 BEFORE UPDATE OF warm_pool_mode_snapshot ON agent_executions
		 BEGIN
		   SELECT RAISE(ABORT, 'invalid warm_pool_mode_snapshot')
		   WHERE NEW.warm_pool_mode_snapshot IS NULL
		      OR NEW.warm_pool_mode_snapshot NOT IN ('disabled', 'balanced', 'low-latency');
		   SELECT RAISE(ABORT, 'Execution warm-pool demand snapshot is immutable')
		   WHERE NEW.warm_pool_mode_snapshot IS NOT OLD.warm_pool_mode_snapshot;
		 END`,
		`CREATE INDEX IF NOT EXISTS idx_worker_instances_assigned_execution
		 ON worker_instances (execution_target_id, assigned_execution_id, id)
		 WHERE assigned_execution_id IS NOT NULL`,
		`CREATE INDEX IF NOT EXISTS idx_worker_instances_pool_identity
		 ON worker_instances (execution_target_id, worker_pool_id, worker_pool_version, capacity_class, id)
		 WHERE worker_pool_id IS NOT NULL`,
		`CREATE UNIQUE INDEX IF NOT EXISTS uq_worker_pool_target_name
		 ON worker_pools (execution_target_id, name)`,
		`CREATE INDEX IF NOT EXISTS idx_worker_pool_target_status
		 ON worker_pools (execution_target_id, status, capacity_class, name, id)`,
		`CREATE INDEX IF NOT EXISTS idx_agent_executions_placement_claim
		 ON agent_executions (execution_target_id, worker_pool_id, status, queued_at, id)
		 WHERE status IN ('queued', 'recovering')`,
		`CREATE INDEX IF NOT EXISTS idx_agent_executions_fair_share_active
		 ON agent_executions (execution_target_id, target_kind, tenant_id)
		 WHERE status IN ('leased', 'running', 'waiting-for-approval')`,
		`CREATE INDEX IF NOT EXISTS idx_agent_executions_target_nonterminal
		 ON agent_executions (execution_target_id, target_kind, status, queued_at, id)
		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
		`INSERT INTO worker_pools (
		   id, tenant_id, execution_target_id, name, mode, capacity_class,
		   cluster_id, region, namespace, desired_idle_units, max_active_units,
		   scheduling_template, status, version, created_at, updated_at
		 )
		 SELECT target.id, target.tenant_id, target.id, 'default',
		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
		 FROM execution_targets AS target
		 WHERE NOT EXISTS (
		   SELECT 1 FROM worker_pools AS pool
		   WHERE pool.execution_target_id = target.id AND lower(pool.name) = 'default'
		 )`,
		`INSERT INTO execution_placement_policies (
		   execution_target_id, tenant_id, version, default_pool_id, updated_at
		 )
		 SELECT target.id, target.tenant_id, 1, pool.id, CURRENT_TIMESTAMP
		 FROM execution_targets AS target
		 JOIN worker_pools AS pool
		   ON pool.execution_target_id = target.id AND lower(pool.name) = 'default'
		 WHERE NOT EXISTS (
		   SELECT 1 FROM execution_placement_policies AS policy
		   WHERE policy.execution_target_id = target.id
		 )`,
		`DROP TRIGGER IF EXISTS trg_worker_pools_shape_insert`,
		`CREATE TRIGGER trg_worker_pools_shape_insert
		 BEFORE INSERT ON worker_pools
		 BEGIN
		   SELECT RAISE(ABORT, 'invalid Worker pool shape')
		   WHERE NEW.mode NOT IN ('resident', 'per-execution', 'warm')
		      OR NEW.capacity_class NOT IN ('standard', 'interactive')
		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
		      OR NEW.desired_idle_units < 0
		      OR NEW.max_active_units < NEW.desired_idle_units
		      OR NEW.version <= 0
		      OR json_valid(NEW.scheduling_template) = 0
		      OR json_type(NEW.scheduling_template) <> 'object';

		   SELECT RAISE(ABORT, 'Worker pool target scope mismatch')
		   WHERE NOT EXISTS (
		     SELECT 1 FROM execution_targets AS target
		     WHERE target.id = NEW.execution_target_id
		       AND target.tenant_id IS NEW.tenant_id
		   );
		 END`,
		`DROP TRIGGER IF EXISTS trg_worker_pools_shape_update`,
		`CREATE TRIGGER trg_worker_pools_shape_update
		 BEFORE UPDATE ON worker_pools
		 BEGIN
		   SELECT RAISE(ABORT, 'Worker pool ownership is immutable')
		   WHERE NEW.execution_target_id IS NOT OLD.execution_target_id
		      OR NEW.tenant_id IS NOT OLD.tenant_id;

		   SELECT RAISE(ABORT, 'Worker pool mode and capacity class are immutable')
		   WHERE NEW.mode IS NOT OLD.mode OR NEW.capacity_class IS NOT OLD.capacity_class;

		   SELECT RAISE(ABORT, 'invalid Worker pool update')
		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
		      OR NEW.desired_idle_units < 0
		      OR NEW.max_active_units < NEW.desired_idle_units
		      OR NEW.version <> OLD.version + 1
		      OR json_valid(NEW.scheduling_template) = 0
		      OR json_type(NEW.scheduling_template) <> 'object';

		   SELECT RAISE(ABORT, 'Execution placement default pool must remain active')
		   WHERE NEW.status <> 'active' AND EXISTS (
		     SELECT 1 FROM execution_placement_policies AS policy
		     WHERE policy.default_pool_id = NEW.id
		   );
		 END`,
		`DROP TRIGGER IF EXISTS trg_worker_pools_delete`,
		`CREATE TRIGGER trg_worker_pools_delete
		 BEFORE DELETE ON worker_pools
		 BEGIN
		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
		 END`,
		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
		`CREATE TRIGGER trg_execution_placement_policy_insert
		 BEFORE INSERT ON execution_placement_policies
		 BEGIN
		   SELECT RAISE(ABORT, 'invalid Execution placement policy')
		   WHERE NEW.version <= 0
		      OR NOT EXISTS (
		        SELECT 1 FROM execution_targets AS target
		        WHERE target.id = NEW.execution_target_id AND target.tenant_id IS NEW.tenant_id
		      )
		      OR NOT EXISTS (
		        SELECT 1 FROM worker_pools AS pool
		        WHERE pool.id = NEW.default_pool_id
		          AND pool.execution_target_id = NEW.execution_target_id
		          AND pool.tenant_id IS NEW.tenant_id
		          AND pool.status = 'active'
		      )
		      OR (NEW.balanced_pool_id IS NOT NULL AND NOT EXISTS (
		        SELECT 1 FROM worker_pools AS pool
		        WHERE pool.id = NEW.balanced_pool_id
		          AND pool.execution_target_id = NEW.execution_target_id
		          AND pool.tenant_id IS NEW.tenant_id
		      ))
		      OR (NEW.low_latency_pool_id IS NOT NULL AND NOT EXISTS (
		        SELECT 1 FROM worker_pools AS pool
		        WHERE pool.id = NEW.low_latency_pool_id
		          AND pool.execution_target_id = NEW.execution_target_id
		          AND pool.tenant_id IS NEW.tenant_id
		      ));
		 END`,
		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_update`,
		`CREATE TRIGGER trg_execution_placement_policy_update
		 BEFORE UPDATE ON execution_placement_policies
		 BEGIN
		   SELECT RAISE(ABORT, 'Execution placement policy ownership is immutable')
		   WHERE NEW.execution_target_id IS NOT OLD.execution_target_id
		      OR NEW.tenant_id IS NOT OLD.tenant_id;

		   SELECT RAISE(ABORT, 'invalid Execution placement policy update')
		   WHERE NEW.version <> OLD.version + 1
		      OR NOT EXISTS (
		        SELECT 1 FROM worker_pools AS pool
		        WHERE pool.id = NEW.default_pool_id
		          AND pool.execution_target_id = NEW.execution_target_id
		          AND pool.tenant_id IS NEW.tenant_id
		          AND pool.status = 'active'
		      )
		      OR (NEW.balanced_pool_id IS NOT NULL AND NOT EXISTS (
		        SELECT 1 FROM worker_pools AS pool
		        WHERE pool.id = NEW.balanced_pool_id
		          AND pool.execution_target_id = NEW.execution_target_id
		          AND pool.tenant_id IS NEW.tenant_id
		      ))
		      OR (NEW.low_latency_pool_id IS NOT NULL AND NOT EXISTS (
		        SELECT 1 FROM worker_pools AS pool
		        WHERE pool.id = NEW.low_latency_pool_id
		          AND pool.execution_target_id = NEW.execution_target_id
		          AND pool.tenant_id IS NEW.tenant_id
		      ));
		 END`,
		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_delete`,
		`CREATE TRIGGER trg_execution_placement_policy_delete
		 BEFORE DELETE ON execution_placement_policies
		 BEGIN
		   SELECT RAISE(ABORT, 'Execution placement policies cannot be deleted');
		 END`,
		`DROP TRIGGER IF EXISTS trg_agent_executions_placement_insert`,
		`CREATE TRIGGER trg_agent_executions_placement_insert
		 BEFORE INSERT ON agent_executions

exec
/bin/zsh -lc "rg -n -C 6 'trg_worker_pool_warm_capacity|warm capacity authority|invalid Worker pool warm|desired_idle_units <>|desired_idle_units "'!='"' services/control-plane/internal/database/store.go services/control-plane/internal/database/*.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-41-		ObservedAt:              fixture.now,
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-42-		ExpiresAt:               fixture.now.Add(time.Minute),
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-43-		Version:                 1,
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-44-		UpdatedAt:               fixture.now,
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-45-	}
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-46-	if err := db.Create(&valid).Error; err != nil {
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:47:		t.Fatalf("create valid warm capacity authority: %v", err)
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-48-	}
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-49-
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-50-	invalidTarget := valid
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-51-	invalidTarget.WorkerPoolID = fixture.otherPool.ID
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-52-	invalidTarget.WorkerPoolVersion = fixture.otherPool.Version
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-53-	invalidTarget.ExecutionTargetID = fixture.target.ID

exec
/bin/zsh -lc "rg -n 'func migrateSQLiteSafety|statements = append|sqlite.*Statements|placementSQLite|warmCapacity' services/control-plane/internal/database/store.go services/control-plane/internal/database/*.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
services/control-plane/internal/database/store.go:97:func migrateSQLiteSafety(ctx context.Context, db *gorm.DB) error {
services/control-plane/internal/database/store.go:97:func migrateSQLiteSafety(ctx context.Context, db *gorm.DB) error {

exec
/bin/zsh -lc "sed -n '560,960p' services/control-plane/internal/database/store.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
		 BEFORE INSERT ON agent_executions
		 BEGIN
		   SELECT RAISE(ABORT, 'invalid Execution placement snapshot')
		   WHERE NOT (
		     (NEW.worker_pool_id IS NULL AND NEW.worker_pool_version IS NULL
		       AND NEW.capacity_class IS NULL AND NEW.placement_policy_version IS NULL)
		     OR
		     (NEW.worker_pool_id IS NOT NULL AND NEW.worker_pool_version > 0
		       AND NEW.capacity_class IN ('standard', 'interactive')
		       AND NEW.placement_policy_version > 0
		       AND EXISTS (
		         SELECT 1 FROM worker_pools AS pool
		         WHERE pool.id = NEW.worker_pool_id
		           AND pool.version = NEW.worker_pool_version
		           AND pool.execution_target_id = NEW.execution_target_id
		           AND pool.tenant_id IS NEW.tenant_id
		           AND pool.capacity_class = NEW.capacity_class
		       ))
		   );
		 END`,
		`DROP TRIGGER IF EXISTS trg_agent_executions_placement_update`,
		`CREATE TRIGGER trg_agent_executions_placement_update
		 BEFORE UPDATE OF worker_pool_id, worker_pool_version, capacity_class, placement_policy_version
		 ON agent_executions
		 WHEN OLD.worker_pool_id IS NOT NULL AND (
		   NEW.worker_pool_id IS NOT OLD.worker_pool_id
		   OR NEW.worker_pool_version IS NOT OLD.worker_pool_version
		   OR NEW.capacity_class IS NOT OLD.capacity_class
		   OR NEW.placement_policy_version IS NOT OLD.placement_policy_version
		 )
		 BEGIN
		   SELECT RAISE(ABORT, 'Execution placement snapshot is immutable');
		 END`,
		`WITH lifecycle AS (
		   SELECT
		     tenant_id,
		     execution_id,
		     generation,
		     MIN(CASE WHEN event_type = 'execution.started' THEN occurred_at END) AS execution_started_at,
		     MIN(CASE WHEN event_type = 'session.started' THEN occurred_at END) AS provider_ready_at
		   FROM session_events
		   WHERE execution_id IS NOT NULL
		     AND generation IS NOT NULL
		     AND event_type IN ('execution.started', 'session.started')
		   GROUP BY tenant_id, execution_id, generation
		 )
		 INSERT OR IGNORE INTO execution_generation_facts (
		   tenant_id, execution_id, generation, session_id, turn_id,
		   execution_target_id, target_kind, provider, recovery_reason,
		   warm_pool_mode, warm_pool_result, dispatch_requested_at,
		   bundle_created_at, leased_at, execution_started_at, provider_ready_at,
		   provider_resume_strategy, created_at, updated_at
		 )
		 SELECT
		   execution.tenant_id,
		   execution.id,
		   execution.generation,
		   execution.session_id,
		   execution.turn_id,
		   execution.execution_target_id,
		   execution.target_kind,
		   COALESCE(NULLIF(execution.provider, ''), session.provider),
		   CASE
		     WHEN bundle.recovery_reason IS NOT NULL THEN bundle.recovery_reason
		     WHEN EXISTS (
		       SELECT 1 FROM execution_recovery_bundles AS previous
		       WHERE previous.tenant_id = execution.tenant_id
		         AND previous.execution_id = execution.id
		         AND previous.generation < execution.generation
		     ) THEN CASE
		     WHEN execution.next_recovery_reason = 'suspend-resume' THEN 'suspend-resume'
		     WHEN execution.next_recovery_reason = 'disaster-recovery' THEN 'disaster-recovery'
		       ELSE 'execution-recovery'
		     END
		     WHEN execution.generation > 1 THEN 'legacy-adoption'
		     ELSE 'initial-claim'
		   END,
		   session.warm_pool_mode,
		   CASE
		     WHEN session.warm_pool_mode = 'disabled' THEN 'not-requested'
		     WHEN worker.worker_mode = 'warm-pool' THEN 'hit'
		     WHEN worker.id IS NOT NULL THEN 'fallback'
		     ELSE 'pending'
		   END,
		   CASE
		     WHEN execution.generation = 1 THEN execution.queued_at
		     ELSE COALESCE(bundle.created_at, lease.acquired_at, execution.queued_at)
		   END,
		   bundle.created_at,
		   CASE
		     WHEN lease.acquired_at IS NULL THEN NULL
		     ELSE MAX(
		       lease.acquired_at,
		       CASE
		         WHEN execution.generation = 1 THEN execution.queued_at
		         ELSE COALESCE(bundle.created_at, lease.acquired_at, execution.queued_at)
		       END
		     )
		   END,
		   CASE
		     WHEN lifecycle.execution_started_at IS NULL THEN NULL
		     ELSE MAX(
		       lifecycle.execution_started_at,
		       COALESCE(lease.acquired_at, bundle.created_at, execution.queued_at),
		       CASE
		         WHEN execution.generation = 1 THEN execution.queued_at
		         ELSE COALESCE(bundle.created_at, lease.acquired_at, execution.queued_at)
		       END
		     )
		   END,
		   CASE
		     WHEN lifecycle.provider_ready_at IS NULL THEN NULL
		     ELSE MAX(
		       lifecycle.provider_ready_at,
		       COALESCE(lifecycle.execution_started_at, lease.acquired_at, bundle.created_at, execution.queued_at),
		       COALESCE(lease.acquired_at, bundle.created_at, execution.queued_at),
		       CASE
		         WHEN execution.generation = 1 THEN execution.queued_at
		         ELSE COALESCE(bundle.created_at, lease.acquired_at, execution.queued_at)
		       END
		     )
		   END,
		   COALESCE(NULLIF(execution.provider_resume_strategy_snapshot, ''), 'authoritative-history'),
		   CASE
		     WHEN execution.generation = 1 THEN execution.queued_at
		     ELSE COALESCE(bundle.created_at, lease.acquired_at, execution.queued_at)
		   END,
		   MAX(
		     CASE
		       WHEN execution.generation = 1 THEN execution.queued_at
		       ELSE COALESCE(bundle.created_at, lease.acquired_at, execution.queued_at)
		     END,
		     COALESCE(bundle.created_at, lease.acquired_at, execution.queued_at),
		     COALESCE(lease.acquired_at, bundle.created_at, execution.queued_at),
		     COALESCE(lifecycle.execution_started_at, lease.acquired_at, bundle.created_at, execution.queued_at),
		     COALESCE(lifecycle.provider_ready_at, lifecycle.execution_started_at, lease.acquired_at, bundle.created_at, execution.queued_at)
		   )
		 FROM agent_executions AS execution
		 JOIN agent_sessions AS session
		   ON session.tenant_id = execution.tenant_id AND session.id = execution.session_id
		 LEFT JOIN execution_recovery_bundles AS bundle
		   ON bundle.tenant_id = execution.tenant_id
		  AND bundle.execution_id = execution.id
		  AND bundle.generation = execution.generation
		 LEFT JOIN worker_leases AS lease
		   ON lease.tenant_id = execution.tenant_id
		  AND lease.execution_id = execution.id
		  AND lease.generation = execution.generation
		 LEFT JOIN worker_instances AS worker ON worker.id = execution.worker_id
		 LEFT JOIN lifecycle
		   ON lifecycle.tenant_id = execution.tenant_id
		  AND lifecycle.execution_id = execution.id
		  AND lifecycle.generation = execution.generation
		 WHERE execution.generation > 0`,
		`CREATE INDEX IF NOT EXISTS idx_execution_generation_facts_metrics
		 ON execution_generation_facts (target_kind, recovery_reason, warm_pool_mode, warm_pool_result)`,
		`CREATE INDEX IF NOT EXISTS idx_execution_generation_facts_terminal
		 ON execution_generation_facts (terminal_outcome, terminal_at)`,
		`CREATE INDEX IF NOT EXISTS idx_execution_generation_facts_pod_provisioning
		 ON execution_generation_facts (
		   target_kind, pod_provisioning_started_at, pod_running_at, pod_pending_since_at
		 )`,
		`CREATE INDEX IF NOT EXISTS idx_execution_generation_pod_failure_metrics
		 ON execution_generation_pod_failure_facts (
		   failure_class, first_observed_at, execution_target_id
		 )`,
		`DROP TRIGGER IF EXISTS trg_execution_generation_facts_insert`,
		`CREATE TRIGGER trg_execution_generation_facts_insert
		 BEFORE INSERT ON execution_generation_facts
		 BEGIN
		   SELECT RAISE(ABORT, 'invalid Execution generation fact')
		   WHERE NEW.generation <= 0
		      OR NEW.recovery_reason NOT IN ('initial-claim', 'legacy-adoption', 'execution-recovery', 'suspend-resume', 'disaster-recovery')
		      OR NEW.warm_pool_mode NOT IN ('disabled', 'balanced', 'low-latency')
		      OR NEW.warm_pool_result NOT IN ('pending', 'not-requested', 'hit', 'fallback')
		      OR (NEW.terminal_outcome IS NOT NULL AND NEW.terminal_outcome NOT IN (
		        'completed', 'failed', 'cancelled', 'interrupted', 'recovering'
		      ))
		      OR (NEW.pod_provisioning_started_at IS NOT NULL AND NEW.dispatch_requested_at IS NOT NULL
		        AND NEW.pod_provisioning_started_at < NEW.dispatch_requested_at)
		      OR (NEW.pod_pending_since_at IS NOT NULL AND NEW.pod_provisioning_started_at IS NOT NULL
		        AND NEW.pod_pending_since_at < NEW.pod_provisioning_started_at)
		      OR (NEW.pod_running_at IS NOT NULL AND NEW.pod_provisioning_started_at IS NOT NULL
		        AND NEW.pod_running_at < NEW.pod_provisioning_started_at)
		      OR (NEW.pod_last_observed_at IS NOT NULL AND NEW.pod_provisioning_started_at IS NOT NULL
		        AND NEW.pod_last_observed_at < NEW.pod_provisioning_started_at)
		      OR (NEW.pod_last_observed_at IS NOT NULL AND NEW.pod_pending_since_at IS NOT NULL
		        AND NEW.pod_last_observed_at < NEW.pod_pending_since_at)
		      OR (NEW.pod_last_observed_at IS NOT NULL AND NEW.pod_running_at IS NOT NULL
		        AND NEW.pod_last_observed_at < NEW.pod_running_at)
		      OR NOT EXISTS (
		        SELECT 1 FROM agent_executions AS execution
		        WHERE execution.tenant_id = NEW.tenant_id AND execution.id = NEW.execution_id
		          AND execution.session_id = NEW.session_id AND execution.turn_id = NEW.turn_id
		          AND execution.execution_target_id = NEW.execution_target_id
		      );
		 END`,
		`DROP TRIGGER IF EXISTS trg_execution_generation_facts_update`,
		`CREATE TRIGGER trg_execution_generation_facts_update
		 BEFORE UPDATE ON execution_generation_facts
		 BEGIN
		   SELECT RAISE(ABORT, 'Execution generation fact identity is immutable')
		   WHERE NEW.tenant_id IS NOT OLD.tenant_id
		      OR NEW.execution_id IS NOT OLD.execution_id
		      OR NEW.generation IS NOT OLD.generation
		      OR NEW.session_id IS NOT OLD.session_id
		      OR NEW.turn_id IS NOT OLD.turn_id
		      OR NEW.execution_target_id IS NOT OLD.execution_target_id
		      OR NEW.target_kind IS NOT OLD.target_kind
		      OR NEW.provider IS NOT OLD.provider
		      OR NEW.recovery_reason IS NOT OLD.recovery_reason
		      OR NEW.warm_pool_mode IS NOT OLD.warm_pool_mode;

		   SELECT RAISE(ABORT, 'Execution generation warm-pool result is immutable after decision')
		   WHERE OLD.warm_pool_result <> 'pending'
		     AND NEW.warm_pool_result IS NOT OLD.warm_pool_result;

		   SELECT RAISE(ABORT, 'Execution generation terminal outcome is immutable')
		   WHERE OLD.terminal_outcome IS NOT NULL AND (
		     NEW.terminal_outcome IS NOT OLD.terminal_outcome
		     OR NEW.terminal_at IS NOT OLD.terminal_at
		   );

		   SELECT RAISE(ABORT, 'Execution generation Pod provisioning start is immutable')
		   WHERE OLD.pod_provisioning_started_at IS NOT NULL
		     AND NEW.pod_provisioning_started_at IS NOT OLD.pod_provisioning_started_at;

		   SELECT RAISE(ABORT, 'Execution generation Pod Pending start is immutable')
		   WHERE OLD.pod_pending_since_at IS NOT NULL
		     AND NEW.pod_pending_since_at IS NOT OLD.pod_pending_since_at;

		   SELECT RAISE(ABORT, 'Execution generation Pod Running time is immutable')
		   WHERE OLD.pod_running_at IS NOT NULL
		     AND NEW.pod_running_at IS NOT OLD.pod_running_at;

		   SELECT RAISE(ABORT, 'Execution generation Pod observation time cannot move backwards')
		   WHERE OLD.pod_last_observed_at IS NOT NULL AND (
		     NEW.pod_last_observed_at IS NULL
		     OR NEW.pod_last_observed_at < OLD.pod_last_observed_at
		   );

		   SELECT RAISE(ABORT, 'Execution generation fact timeline is invalid')
		   WHERE (NEW.dispatch_requested_at IS NOT NULL AND NEW.leased_at IS NOT NULL
		       AND NEW.dispatch_requested_at > NEW.leased_at)
		      OR (NEW.leased_at IS NOT NULL AND NEW.execution_started_at IS NOT NULL
		       AND NEW.leased_at > NEW.execution_started_at)
		      OR (NEW.execution_started_at IS NOT NULL AND NEW.provider_ready_at IS NOT NULL
		       AND NEW.execution_started_at > NEW.provider_ready_at)
		      OR (NEW.dispatch_requested_at IS NOT NULL AND NEW.terminal_at IS NOT NULL
		       AND NEW.dispatch_requested_at > NEW.terminal_at)
		      OR (NEW.pod_provisioning_started_at IS NOT NULL AND NEW.dispatch_requested_at IS NOT NULL
		       AND NEW.pod_provisioning_started_at < NEW.dispatch_requested_at)
		      OR (NEW.pod_pending_since_at IS NOT NULL AND NEW.pod_provisioning_started_at IS NOT NULL
		       AND NEW.pod_pending_since_at < NEW.pod_provisioning_started_at)
		      OR (NEW.pod_running_at IS NOT NULL AND NEW.pod_provisioning_started_at IS NOT NULL
		       AND NEW.pod_running_at < NEW.pod_provisioning_started_at)
		      OR (NEW.pod_last_observed_at IS NOT NULL AND NEW.pod_provisioning_started_at IS NOT NULL
		       AND NEW.pod_last_observed_at < NEW.pod_provisioning_started_at)
		      OR (NEW.pod_last_observed_at IS NOT NULL AND NEW.pod_pending_since_at IS NOT NULL
		       AND NEW.pod_last_observed_at < NEW.pod_pending_since_at)
		      OR (NEW.pod_last_observed_at IS NOT NULL AND NEW.pod_running_at IS NOT NULL
		       AND NEW.pod_last_observed_at < NEW.pod_running_at);
		 END`,
		`DROP TRIGGER IF EXISTS trg_execution_generation_facts_delete`,
		`CREATE TRIGGER trg_execution_generation_facts_delete
		 BEFORE DELETE ON execution_generation_facts
		 BEGIN
		   SELECT RAISE(ABORT, 'Execution generation facts cannot be deleted');
		 END`,
		`DROP TRIGGER IF EXISTS trg_execution_generation_pod_failure_facts_insert`,
		`CREATE TRIGGER trg_execution_generation_pod_failure_facts_insert
		 BEFORE INSERT ON execution_generation_pod_failure_facts
		 BEGIN
		   SELECT RAISE(ABORT, 'invalid Execution generation Pod failure fact')
		   WHERE NEW.generation <= 0
		      OR NEW.failure_class IS NULL
		      OR NEW.failure_class NOT IN (
		        'pod-apply-failed', 'pending-timeout', 'unschedulable', 'image-pull',
		        'container-start', 'evicted', 'oom-killed', 'pod-failed'
		      )
		      OR NEW.namespace IS NULL OR length(NEW.namespace) NOT BETWEEN 1 AND 253
		      OR NEW.pod_name IS NULL OR length(NEW.pod_name) NOT BETWEEN 1 AND 253
		      OR (NEW.pod_uid IS NOT NULL AND length(NEW.pod_uid) NOT BETWEEN 1 AND 160)
		      OR NEW.reason_code IS NULL OR length(NEW.reason_code) NOT BETWEEN 1 AND 160
		      OR (NEW.failure_class = 'pod-apply-failed' AND NEW.pod_uid IS NOT NULL)
		      OR (NEW.failure_class <> 'pod-apply-failed' AND NEW.pod_uid IS NULL)
		      OR NEW.first_observed_at IS NULL OR NEW.last_observed_at IS NULL
		      OR NEW.last_observed_at < NEW.first_observed_at
		      OR NOT EXISTS (
		        SELECT 1 FROM execution_generation_facts AS generation
		        WHERE generation.tenant_id = NEW.tenant_id
		          AND generation.execution_id = NEW.execution_id
		          AND generation.generation = NEW.generation
		          AND generation.execution_target_id = NEW.execution_target_id
		      );
		 END`,
		`DROP TRIGGER IF EXISTS trg_execution_generation_pod_failure_facts_update`,
		`CREATE TRIGGER trg_execution_generation_pod_failure_facts_update
		 BEFORE UPDATE ON execution_generation_pod_failure_facts
		 BEGIN
		   SELECT RAISE(ABORT, 'Execution generation Pod failure fact identity is immutable')
		   WHERE NEW.tenant_id IS NOT OLD.tenant_id
		      OR NEW.execution_id IS NOT OLD.execution_id
		      OR NEW.generation IS NOT OLD.generation
		      OR NEW.failure_class IS NOT OLD.failure_class
		      OR NEW.execution_target_id IS NOT OLD.execution_target_id
		      OR NEW.namespace IS NOT OLD.namespace
		      OR NEW.pod_name IS NOT OLD.pod_name
		      OR NEW.pod_uid IS NOT OLD.pod_uid
		      OR NEW.reason_code IS NOT OLD.reason_code
		      OR NEW.first_observed_at IS NOT OLD.first_observed_at;

		   SELECT RAISE(ABORT, 'Execution generation Pod failure observation cannot move backwards')
		   WHERE NEW.last_observed_at IS NULL OR NEW.last_observed_at < OLD.last_observed_at;
		 END`,
		`DROP TRIGGER IF EXISTS trg_execution_generation_pod_failure_facts_delete`,
		`CREATE TRIGGER trg_execution_generation_pod_failure_facts_delete
		 BEFORE DELETE ON execution_generation_pod_failure_facts
		 BEGIN
		   SELECT RAISE(ABORT, 'Execution generation Pod failure facts cannot be deleted');
		 END`,
		`INSERT OR IGNORE INTO worker_incarnation_facts (
		   worker_id, worker_incarnation, tenant_id, execution_target_id,
		   target_kind, worker_mode, worker_pool_id, worker_pool_version,
		   pool_mode, capacity_class, cluster_id, region, namespace, pod_name,
		   instance_uid, registered_at, current_state, state_changed_at,
		   terminated_at, terminal_reason, accumulated_active_seconds,
		   accumulated_idle_seconds, claim_count, created_at, updated_at
		 )
		 SELECT
		   worker.id,
		   worker.incarnation,
		   target.tenant_id,
		   worker.execution_target_id,
		   worker.target_kind,
		   worker.worker_mode,
		   worker.worker_pool_id,
		   worker.worker_pool_version,
		   pool.mode,
		   worker.capacity_class,
		   worker.cluster_id,
		   COALESCE(pool.region, ''),
		   worker.namespace,
		   worker.pod_name,
		   worker.instance_uid,
		   worker.registered_at,
		   CASE
		     WHEN worker.status = 'terminated' THEN 'terminated'
		     WHEN worker.status = 'draining' THEN 'draining'
		     WHEN worker.status = 'offline' THEN 'offline'
		     WHEN lease.execution_id IS NOT NULL THEN 'active'
		     ELSE 'idle'
		   END,
		   CASE
		     WHEN worker.status = 'terminated' THEN COALESCE(worker.terminated_at, worker.last_heartbeat_at, worker.registered_at)
		     WHEN worker.status = 'draining' THEN COALESCE(worker.draining_at, worker.last_heartbeat_at, worker.registered_at)
		     ELSE COALESCE(worker.last_heartbeat_at, worker.registered_at)
		   END,
		   CASE WHEN worker.status = 'terminated' THEN COALESCE(worker.terminated_at, worker.last_heartbeat_at, worker.registered_at) END,
		   CASE WHEN worker.status = 'terminated' THEN 'legacy-observed-terminal' END,
		   0,
		   0,
		   CASE WHEN lease.execution_id IS NULL THEN 0 ELSE 1 END,
		   worker.registered_at,
		   CASE
		     WHEN worker.status = 'terminated' THEN COALESCE(worker.terminated_at, worker.last_heartbeat_at, worker.registered_at)
		     ELSE COALESCE(worker.last_heartbeat_at, worker.registered_at)
		   END
		 FROM worker_instances AS worker
		 JOIN execution_targets AS target ON target.id = worker.execution_target_id
		 LEFT JOIN worker_pools AS pool ON pool.id = worker.worker_pool_id
		 LEFT JOIN worker_leases AS lease
		   ON lease.worker_id = worker.id AND lease.worker_incarnation = worker.incarnation`,
		`CREATE INDEX IF NOT EXISTS idx_worker_incarnation_facts_metrics
		 ON worker_incarnation_facts (target_kind, pool_mode, capacity_class, current_state)`,
		`CREATE INDEX IF NOT EXISTS idx_worker_incarnation_facts_target_state
		 ON worker_incarnation_facts (execution_target_id, current_state, worker_id, worker_incarnation)`,
		`DROP TRIGGER IF EXISTS trg_worker_incarnation_facts_insert`,
		`CREATE TRIGGER trg_worker_incarnation_facts_insert
		 BEFORE INSERT ON worker_incarnation_facts
		 BEGIN
		   SELECT RAISE(ABORT, 'invalid Worker incarnation fact')
		   WHERE NEW.worker_incarnation <= 0
		      OR NEW.target_kind NOT IN ('local', 'ssh', 'docker', 'kubernetes')
		      OR NEW.worker_mode NOT IN ('execution-pinned', 'warm-pool', 'general-pool')
		      OR NEW.current_state NOT IN ('idle', 'active', 'draining', 'offline', 'terminated')
		      OR (
		        (NEW.worker_pool_id IS NULL AND (
		          NEW.worker_pool_version IS NOT NULL OR NEW.pool_mode IS NOT NULL OR NEW.capacity_class IS NOT NULL
		        ))
		        OR
		        (NEW.worker_pool_id IS NOT NULL AND (
		          NEW.worker_pool_version IS NULL OR NEW.worker_pool_version <= 0
		          OR NEW.pool_mode NOT IN ('resident', 'per-execution', 'warm')
		          OR NEW.capacity_class NOT IN ('standard', 'interactive')
		        ))
		      )
		      OR length(trim(ifnull(NEW.cluster_id, ''))) NOT BETWEEN 1 AND 160
		      OR length(ifnull(NEW.region, '')) > 160
		      OR length(trim(ifnull(NEW.namespace, ''))) NOT BETWEEN 1 AND 253
		      OR length(trim(ifnull(NEW.pod_name, ''))) NOT BETWEEN 1 AND 253

exec
/bin/zsh -lc "rg -n -C 5 'warm_capacity|Warm capacity' services/control-plane/internal/database/store.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 exited 1 in 0ms:
exec
/bin/zsh -lc "rg -n -C 5 'trg_worker_pool_warm_capacity|invalid Worker Pool warm capacity|Worker pool warm capacity scope|warm capacity scope' services/control-plane" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-63-     OR pool.mode <> 'warm'
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-64-     OR pool.status = 'disabled'
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-65-     OR pool.capacity_class <> NEW.capacity_class
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-66-     OR pool.desired_idle_units <> NEW.desired_idle_units
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-67-     OR pool.max_active_units <> NEW.max_active_units THEN
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql:68:    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-69-  END IF;
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-70-
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-71-  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-72-  IF NOT FOUND
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-73-     OR target.tenant_id IS NULL
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-74-     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-75-     OR target.kind <> 'kubernetes'
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-76-     OR target.status <> 'active' THEN
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql:77:    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-78-  END IF;
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-79-
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-80-  IF NEW.worker_release_revision_id IS NOT NULL THEN
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-81-    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-82-    IF NOT FOUND
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-83-       OR release.execution_target_id <> NEW.execution_target_id
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-84-       OR release.tenant_id <> NEW.tenant_id THEN
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql:85:      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-86-    END IF;
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-87-  END IF;
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-88-
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-89-  RETURN NEW;
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-90-END;
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-91-$$;
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-92-
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql:93:DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope ON worker_pool_warm_capacity;
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql:94:CREATE CONSTRAINT TRIGGER trg_worker_pool_warm_capacity_scope
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-95-AFTER INSERT OR UPDATE
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-96-ON worker_pool_warm_capacity
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-97-DEFERRABLE INITIALLY DEFERRED
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-98-FOR EACH ROW EXECUTE FUNCTION assert_worker_pool_warm_capacity_scope();
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-99-
--
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-117-  END IF;
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-118-  RETURN NEW;
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-119-END;
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-120-$$;
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-121-
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql:122:DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_cas ON worker_pool_warm_capacity;
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql:123:CREATE TRIGGER trg_worker_pool_warm_capacity_cas
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-124-BEFORE UPDATE OR DELETE ON worker_pool_warm_capacity
services/control-plane/migrations/000069_worker_pool_warm_capacity.sql-125-FOR EACH ROW EXECUTE FUNCTION enforce_worker_pool_warm_capacity_cas();
--
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-52-	invalidTarget.WorkerPoolVersion = fixture.otherPool.Version
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-53-	invalidTarget.ExecutionTargetID = fixture.target.ID
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-54-	assertWorkerPoolWarmCapacityConstraintRejected(
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-55-		t,
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-56-		db.Create(&invalidTarget).Error,
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:57:		"Worker pool warm capacity scope is invalid",
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-58-	)
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-59-
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-60-	invalidRelease := valid
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-61-	invalidRelease.WorkerPoolID = fixture.otherWarmPoolSameTarget.ID
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-62-	invalidRelease.WorkerPoolVersion = fixture.otherWarmPoolSameTarget.Version
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-63-	invalidRelease.WorkerReleaseRevisionID = &fixture.otherTargetRelease.ID
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-64-	assertWorkerPoolWarmCapacityConstraintRejected(
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-65-		t,
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-66-		db.Create(&invalidRelease).Error,
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:67:		"Worker pool warm capacity scope is invalid",
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-68-	)
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-69-
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-70-	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-71-		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-72-			fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
--
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-101-}
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-102-
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-103-func assertWorkerPoolWarmCapacityConstraintRejected(t *testing.T, err error, expected string) {
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-104-	t.Helper()
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-105-	if err == nil {
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:106:		t.Fatalf("PostgreSQL accepted invalid Worker Pool warm capacity state (want containing %q)", expected)
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-107-	}
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-108-	if strings.Contains(err.Error(), expected) || errors.Is(err, gorm.ErrCheckConstraintViolated) {
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-109-		return
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-110-	}
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go-111-	t.Fatalf(

exec
/bin/zsh -lc 'tail -n 120 services/control-plane/internal/database/store.go' in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
		db.Migrator().HasColumn(&persistence.AgentExecution{}, "placement_region") &&
		db.Migrator().HasColumn(&persistence.AgentExecution{}, "placement_cluster_id") {
		if err := db.WithContext(ctx).Exec(`DROP TRIGGER IF EXISTS trg_agent_executions_scheduling_policy_snapshot_insert`).Error; err != nil {
			return fmt.Errorf("drop sqlite Execution Scheduling Policy snapshot insert trigger: %w", err)
		}
		if err := db.WithContext(ctx).Exec(`CREATE TRIGGER trg_agent_executions_scheduling_policy_snapshot_insert
		 BEFORE INSERT ON agent_executions
		 BEGIN
		   SELECT RAISE(ABORT, 'Execution Scheduling Policy snapshot is invalid')
		   WHERE NEW.tenant_scheduling_policy_version < 0
		      OR length(NEW.tenant_scheduling_policy_digest) <> 64
		      OR NEW.tenant_scheduling_policy_digest GLOB '*[^0-9a-f]*'
		      OR NEW.organization_scheduling_policy_version < 0
		      OR length(NEW.organization_scheduling_policy_digest) <> 64
		      OR NEW.organization_scheduling_policy_digest GLOB '*[^0-9a-f]*'
		      OR length(NEW.placement_region) > 120 OR trim(NEW.placement_region) IS NOT NEW.placement_region
		      OR length(NEW.placement_cluster_id) > 200 OR trim(NEW.placement_cluster_id) IS NOT NEW.placement_cluster_id;
		   SELECT RAISE(ABORT, 'Execution Scheduling Policy snapshot Session is outside the Tenant')
		   WHERE NOT EXISTS (
		     SELECT 1 FROM agent_sessions s WHERE s.tenant_id = NEW.tenant_id AND s.id = NEW.session_id
		   );
		   SELECT RAISE(ABORT, 'Execution Tenant Scheduling Policy Head is corrupt')
		   WHERE EXISTS (
		     SELECT 1 FROM execution_scheduling_policy_heads h
		     WHERE h.tenant_id = NEW.tenant_id AND h.scope_kind = 'tenant' AND h.scope_id = NEW.tenant_id
		   ) AND NOT EXISTS (
		     SELECT 1 FROM execution_scheduling_policy_heads h
		     JOIN execution_scheduling_policy_revisions r
		       ON r.tenant_id = h.tenant_id AND r.id = h.current_revision_id
		      AND r.policy_head_id = h.id AND r.revision_number = h.version
		     WHERE h.tenant_id = NEW.tenant_id AND h.scope_kind = 'tenant' AND h.scope_id = NEW.tenant_id
		   );
		   SELECT RAISE(ABORT, 'Execution Tenant Scheduling Policy snapshot is stale')
		   WHERE NEW.tenant_scheduling_policy_version IS NOT COALESCE((
		       SELECT h.version FROM execution_scheduling_policy_heads h
		       WHERE h.tenant_id = NEW.tenant_id AND h.scope_kind = 'tenant' AND h.scope_id = NEW.tenant_id
		     ), 0)
		      OR NEW.tenant_scheduling_policy_digest IS NOT COALESCE((
		       SELECT r.sha256 FROM execution_scheduling_policy_heads h
		       JOIN execution_scheduling_policy_revisions r
		         ON r.tenant_id = h.tenant_id AND r.id = h.current_revision_id
		        AND r.policy_head_id = h.id AND r.revision_number = h.version
		       WHERE h.tenant_id = NEW.tenant_id AND h.scope_kind = 'tenant' AND h.scope_id = NEW.tenant_id
		     ), '48646d468c45b8a2257c2080fce3ef0697f7ec181ca7e085eb49a194a92a90b2');
		   SELECT RAISE(ABORT, 'Execution Organization Scheduling Policy Head is corrupt')
		   WHERE EXISTS (
		     SELECT 1 FROM execution_scheduling_policy_heads h
		     JOIN agent_sessions s ON s.tenant_id = h.tenant_id AND s.organization_id = h.scope_id
		     WHERE s.tenant_id = NEW.tenant_id AND s.id = NEW.session_id AND h.scope_kind = 'organization'
		   ) AND NOT EXISTS (
		     SELECT 1 FROM execution_scheduling_policy_heads h
		     JOIN agent_sessions s ON s.tenant_id = h.tenant_id AND s.organization_id = h.scope_id
		     JOIN execution_scheduling_policy_revisions r
		       ON r.tenant_id = h.tenant_id AND r.id = h.current_revision_id
		      AND r.policy_head_id = h.id AND r.revision_number = h.version
		     WHERE s.tenant_id = NEW.tenant_id AND s.id = NEW.session_id AND h.scope_kind = 'organization'
		   );
		   SELECT RAISE(ABORT, 'Execution Organization Scheduling Policy snapshot is stale')
		   WHERE NEW.organization_scheduling_policy_version IS NOT COALESCE((
		       SELECT h.version FROM execution_scheduling_policy_heads h
		       JOIN agent_sessions s ON s.tenant_id = h.tenant_id AND s.organization_id = h.scope_id
		       WHERE s.tenant_id = NEW.tenant_id AND s.id = NEW.session_id AND h.scope_kind = 'organization'
		     ), 0)
		      OR NEW.organization_scheduling_policy_digest IS NOT COALESCE((
		       SELECT r.sha256 FROM execution_scheduling_policy_heads h
		       JOIN agent_sessions s ON s.tenant_id = h.tenant_id AND s.organization_id = h.scope_id
		       JOIN execution_scheduling_policy_revisions r
		         ON r.tenant_id = h.tenant_id AND r.id = h.current_revision_id
		        AND r.policy_head_id = h.id AND r.revision_number = h.version
		       WHERE s.tenant_id = NEW.tenant_id AND s.id = NEW.session_id AND h.scope_kind = 'organization'
		     ), '48646d468c45b8a2257c2080fce3ef0697f7ec181ca7e085eb49a194a92a90b2');
		   SELECT RAISE(ABORT, 'Execution placement location Worker Pool is unavailable for the Target')
		   WHERE NEW.worker_pool_id IS NOT NULL AND NOT EXISTS (
		     SELECT 1 FROM worker_pools p WHERE p.id = NEW.worker_pool_id AND p.execution_target_id = NEW.execution_target_id
		   );
		   SELECT RAISE(ABORT, 'Execution routed placement location does not match its Member and Worker Pool authority')
		   WHERE NEW.target_group_id IS NOT NULL AND (
		     NEW.selected_region IS NULL OR NEW.selected_cluster_id IS NULL
		     OR EXISTS (SELECT 1 FROM worker_pools p WHERE p.id = NEW.worker_pool_id AND p.region <> '' AND p.region IS NOT NEW.selected_region)
		     OR EXISTS (SELECT 1 FROM worker_pools p WHERE p.id = NEW.worker_pool_id AND p.cluster_id <> '' AND p.cluster_id IS NOT NEW.selected_cluster_id)
		     OR NEW.placement_region IS NOT COALESCE((SELECT NULLIF(p.region, '') FROM worker_pools p WHERE p.id = NEW.worker_pool_id), NEW.selected_region)
		     OR NEW.placement_cluster_id IS NOT COALESCE((SELECT NULLIF(p.cluster_id, '') FROM worker_pools p WHERE p.id = NEW.worker_pool_id), NEW.selected_cluster_id)
		   );
		   SELECT RAISE(ABORT, 'Execution fixed-Target placement location does not match its Worker Pool authority')
		   WHERE NEW.target_group_id IS NULL AND (
		     NEW.placement_region IS NOT COALESCE((SELECT p.region FROM worker_pools p WHERE p.id = NEW.worker_pool_id), '')
		     OR NEW.placement_cluster_id IS NOT COALESCE((SELECT p.cluster_id FROM worker_pools p WHERE p.id = NEW.worker_pool_id), '')
		   );
		 END`).Error; err != nil {
			return fmt.Errorf("create sqlite Execution Scheduling Policy snapshot insert trigger: %w", err)
		}
		if err := db.WithContext(ctx).Exec(`DROP TRIGGER IF EXISTS trg_agent_executions_scheduling_policy_snapshot_immutable`).Error; err != nil {
			return fmt.Errorf("drop sqlite Execution Scheduling Policy snapshot trigger: %w", err)
		}
		if err := db.WithContext(ctx).Exec(`CREATE TRIGGER trg_agent_executions_scheduling_policy_snapshot_immutable
		 BEFORE UPDATE OF tenant_scheduling_policy_version, tenant_scheduling_policy_digest,
		   organization_scheduling_policy_version, organization_scheduling_policy_digest,
		   placement_region, placement_cluster_id ON agent_executions
		 BEGIN
		   SELECT RAISE(ABORT, 'Execution Scheduling Policy snapshot is immutable')
		   WHERE NEW.tenant_scheduling_policy_version IS NOT OLD.tenant_scheduling_policy_version
		      OR NEW.tenant_scheduling_policy_digest IS NOT OLD.tenant_scheduling_policy_digest
		      OR NEW.organization_scheduling_policy_version IS NOT OLD.organization_scheduling_policy_version
		      OR NEW.organization_scheduling_policy_digest IS NOT OLD.organization_scheduling_policy_digest
		      OR NEW.placement_region IS NOT OLD.placement_region
		      OR NEW.placement_cluster_id IS NOT OLD.placement_cluster_id;
		 END`).Error; err != nil {
			return fmt.Errorf("create sqlite Execution Scheduling Policy snapshot trigger: %w", err)
		}
	}
	return nil
}

func (s *store) Close() error {
	sqlDB, err := s.db.DB()
	if err != nil {
		return err
	}
	return sqlDB.Close()
}

exec
/bin/zsh -lc "rg -n 'CREATE TRIGGER trg_.*worker_pool|worker_pool_warm' services/control-plane/internal/database/store.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
439:		`CREATE TRIGGER trg_worker_pools_shape_insert
461:		`CREATE TRIGGER trg_worker_pools_shape_update
487:		`CREATE TRIGGER trg_worker_pools_delete

exec
/bin/zsh -lc "rg -n 'func openStage4SQLiteFixture|type stage4SQLiteFixture|assertSQLiteStage4Rejected' services/control-plane/internal/database/*_test.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
services/control-plane/internal/database/active_turn_suspend_sqlite_test.go:65:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/active_turn_suspend_sqlite_test.go:77:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/active_turn_suspend_sqlite_test.go:90:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/active_turn_suspend_sqlite_test.go:116:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/active_turn_suspend_sqlite_test.go:153:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/active_turn_suspend_sqlite_test.go:169:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/active_turn_suspend_sqlite_test.go:190:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/active_turn_suspend_sqlite_test.go:221:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/active_turn_suspend_sqlite_test.go:235:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/cloud_cost_accounting_sqlite_test.go:57:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/cloud_cost_accounting_sqlite_test.go:72:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/cloud_cost_accounting_sqlite_test.go:79:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/worker_claim_facts_sqlite_test.go:61:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/worker_claim_facts_sqlite_test.go:89:	assertSQLiteStage4Rejected(t, store.DB().Create(&tooEarlyRelease).Error, "invalid Worker claim release fact")
services/control-plane/internal/database/worker_claim_facts_sqlite_test.go:92:	assertSQLiteStage4Rejected(t, store.DB().Create(&invalidTimeline).Error, "invalid Worker claim release fact")
services/control-plane/internal/database/worker_claim_facts_sqlite_test.go:102:	assertSQLiteStage4Rejected(t, store.DB().Create(&invalidRelease).Error, "invalid Worker claim release fact")
services/control-plane/internal/database/worker_claim_facts_sqlite_test.go:103:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/worker_claim_facts_sqlite_test.go:110:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/worker_claim_facts_sqlite_test.go:129:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/worker_claim_facts_sqlite_test.go:140:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/worker_claim_facts_sqlite_test.go:145:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/worker_claim_facts_sqlite_test.go:152:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/semantic_credential_access_sqlite_test.go:39:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/semantic_credential_access_sqlite_test.go:58:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/semantic_credential_access_sqlite_test.go:126:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/semantic_credential_access_sqlite_test.go:160:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/semantic_credential_access_sqlite_test.go:167:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/semantic_credential_access_sqlite_test.go:174:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/semantic_credential_access_sqlite_test.go:185:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/semantic_credential_access_sqlite_test.go:195:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/semantic_credential_access_sqlite_test.go:206:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/semantic_credential_access_sqlite_test.go:234:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/semantic_credential_access_sqlite_test.go:245:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/kubernetes_terminal_suspend_sqlite_test.go:67:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/kubernetes_terminal_suspend_sqlite_test.go:93:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/kubernetes_terminal_suspend_sqlite_test.go:117:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/kubernetes_terminal_suspend_sqlite_test.go:131:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/kubernetes_terminal_suspend_sqlite_test.go:145:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/kubernetes_terminal_suspend_sqlite_test.go:162:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:19:type stage4SQLiteFixture struct {
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:83:	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidBundle).Error, "existing Tenant Session Turn")
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:89:	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidProjectHead).Error, "existing Tenant Project")
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:94:	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidKeyHead).Error, "scope, key, or version shape")
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:125:	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidRevision).Error, "creator must reference an existing User")
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:132:	assertSQLiteStage4Rejected(t, fixture.db.Create(&malformedRevision).Error, "Memory Revision shape is invalid")
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:133:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:140:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:147:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:154:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:178:	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidTenantPolicy).Error, "updater must reference an existing User")
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:183:	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidProjectPolicy).Error, "existing Tenant Project")
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:193:	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidSessionTimestamp).Error, "Session Resource Lifecycle fields are invalid")
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:201:	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidSessionRange).Error, "Session Resource Lifecycle fields are invalid")
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:202:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:226:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:237:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:250:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:263:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:275:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:282:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:289:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:299:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:325:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:348:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:366:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:375:func openStage4SQLiteFixture(t *testing.T) stage4SQLiteFixture {
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:547:func assertSQLiteStage4Rejected(t *testing.T, err error, expected string) {
services/control-plane/internal/database/worker_pool_placement_sqlite_test.go:48:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/worker_pool_placement_sqlite_test.go:57:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/worker_incarnation_facts_sqlite_test.go:122:		assertSQLiteStage4Rejected(
services/control-plane/internal/database/worker_incarnation_facts_sqlite_test.go:140:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/worker_incarnation_facts_sqlite_test.go:148:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/worker_incarnation_facts_sqlite_test.go:210:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/worker_incarnation_facts_sqlite_test.go:218:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/worker_incarnation_facts_sqlite_test.go:224:	assertSQLiteStage4Rejected(
services/control-plane/internal/database/worker_incarnation_facts_sqlite_test.go:231:	assertSQLiteStage4Rejected(

exec
/bin/zsh -lc "sed -n '1,130p' services/control-plane/internal/placement/service.go
sed -n '220,390p' services/control-plane/internal/placement/service.go
sed -n '520,685p' services/control-plane/internal/placement/service.go
sed -n '725,760p' services/control-plane/internal/placement/service.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
package placement

import (
	"context"
	"crypto/md5"
	"encoding/hex"
	"errors"
	"strings"
	"time"

	"github.com/google/uuid"
	"gorm.io/gorm"

	"github.com/synara-ai/synara/services/control-plane/internal/authorization"
	"github.com/synara-ai/synara/services/control-plane/internal/identity"
	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
	"github.com/synara-ai/synara/services/control-plane/internal/problem"
	"github.com/synara-ai/synara/services/control-plane/internal/warmcapacity"
)

const (
	PoolModeResident     = "resident"
	PoolModePerExecution = "per-execution"
	PoolModeWarm         = "warm"

	CapacityClassStandard    = "standard"
	CapacityClassInteractive = "interactive"

	PoolStatusActive   = "active"
	PoolStatusDraining = "draining"
	PoolStatusDisabled = "disabled"

	WarmPoolModeDefault    = "default"
	WarmPoolModeDisabled   = "disabled"
	WarmPoolModeBalanced   = "balanced"
	WarmPoolModeLowLatency = "low-latency"
)

type Pool struct {
	ID                 uuid.UUID      `json:"id"`
	TenantID           *uuid.UUID     `json:"tenantId"`
	ExecutionTargetID  uuid.UUID      `json:"executionTargetId"`
	Name               string         `json:"name"`
	Mode               string         `json:"mode"`
	CapacityClass      string         `json:"capacityClass"`
	ClusterID          string         `json:"clusterId"`
	Region             string         `json:"region"`
	Namespace          string         `json:"namespace"`
	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
	MaxActiveUnits     int            `json:"maxActiveUnits"`
	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
	Status             string         `json:"status"`
	Version            int64          `json:"version"`
	CreatedAt          time.Time      `json:"createdAt"`
	UpdatedAt          time.Time      `json:"updatedAt"`
}

type Policy struct {
	TenantID          *uuid.UUID `json:"tenantId"`
	ExecutionTargetID uuid.UUID  `json:"executionTargetId"`
	Version           int64      `json:"version"`
	DefaultPoolID     uuid.UUID  `json:"defaultPoolId"`
	BalancedPoolID    *uuid.UUID `json:"balancedPoolId"`
	LowLatencyPoolID  *uuid.UUID `json:"lowLatencyPoolId"`
	UpdatedBy         *uuid.UUID `json:"updatedBy"`
	UpdatedAt         time.Time  `json:"updatedAt"`
}

type State struct {
	Pools  []Pool `json:"pools"`
	Policy Policy `json:"policy"`
}

type UpdatePolicyInput struct {
	ExpectedVersion  int64      `json:"expectedVersion"`
	DefaultPoolID    uuid.UUID  `json:"defaultPoolId"`
	BalancedPoolID   *uuid.UUID `json:"balancedPoolId"`
	LowLatencyPoolID *uuid.UUID `json:"lowLatencyPoolId"`
}

type CreatePoolInput struct {
	Name               string         `json:"name"`
	Mode               string         `json:"mode"`
	CapacityClass      string         `json:"capacityClass"`
	ClusterID          string         `json:"clusterId"`
	Region             string         `json:"region"`
	Namespace          string         `json:"namespace"`
	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
	MaxActiveUnits     int            `json:"maxActiveUnits"`
	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
	Status             string         `json:"status"`
}

type UpdatePoolInput struct {
	ExpectedVersion int64 `json:"expectedVersion"`
	CreatePoolInput
}

type Selection struct {
	Pool          Pool   `json:"pool"`
	CapacityClass string `json:"capacityClass"`
	PolicyVersion int64  `json:"policyVersion"`
}

func ApplySelection(execution *persistence.AgentExecution, selection Selection) {
	if execution == nil {
		return
	}
	poolID := selection.Pool.ID
	poolVersion := selection.Pool.Version
	capacityClass := selection.CapacityClass
	policyVersion := selection.PolicyVersion
	execution.WorkerPoolID = &poolID
	execution.WorkerPoolVersion = &poolVersion
	execution.CapacityClass = &capacityClass
	execution.PlacementPolicyVersion = &policyVersion
	execution.PlacementRegion = selection.Pool.Region
	execution.PlacementClusterID = selection.Pool.ClusterID
}

type Service struct {
	db         *gorm.DB
	authorizer *authorization.Authorizer
	now        func() time.Time
}

func NewService(db *gorm.DB) *Service {
	return &Service{
		db:         db,
		authorizer: authorization.NewAuthorizer(db),
		}
		if result.RowsAffected != 1 {
			return problem.New(409, "execution_placement_policy_version_conflict", "Execution placement policy changed; reload it before saving.")
		}
		current.Version++
		current.DefaultPoolID = resolved.defaultPool.ID
		current.BalancedPoolID = resolved.balancedPoolID
		current.LowLatencyPoolID = resolved.lowLatencyPoolID
		current.UpdatedBy = &principal.UserID
		current.UpdatedAt = now
		state = toState(current, models)
		return nil
	})
	return state, err
}

func (s *Service) CreatePool(
	ctx context.Context,
	principal identity.Principal,
	tenantID, targetID uuid.UUID,
	input CreatePoolInput,
) (Pool, error) {
	if err := requireActiveTenant(principal, tenantID); err != nil {
		return Pool{}, err
	}
	if _, err := s.authorizer.RequireTenant(ctx, principal.UserID, tenantID, authorization.WorkerManage); err != nil {
		return Pool{}, err
	}
	normalized, err := normalizePoolInput(input)
	if err != nil {
		return Pool{}, err
	}
	var created Pool
	err = persistence.InTransaction(ctx, s.db, func(tx *gorm.DB) error {
		target, err := s.loadAccessibleTarget(ctx, tx, tenantID, targetID, true)
		if err != nil {
			return err
		}
		if target.TenantID == nil {
			return problem.New(403, "shared_execution_target_worker_pool_immutable", "Platform-shared execution target worker pools cannot be changed by a tenant.")
		}
		if err := validatePoolTargetMode(target, normalized.Mode); err != nil {
			return err
		}
		now := s.now()
		model := persistence.WorkerPool{
			ID:                 uuid.New(),
			TenantID:           target.TenantID,
			ExecutionTargetID:  target.ID,
			Name:               normalized.Name,
			Mode:               normalized.Mode,
			CapacityClass:      normalized.CapacityClass,
			ClusterID:          normalized.ClusterID,
			Region:             normalized.Region,
			Namespace:          normalized.Namespace,
			DesiredIdleUnits:   normalized.DesiredIdleUnits,
			MaxActiveUnits:     normalized.MaxActiveUnits,
			SchedulingTemplate: normalized.SchedulingTemplate,
			Status:             normalized.Status,
			Version:            1,
			CreatedAt:          now,
			UpdatedAt:          now,
		}
		if err := tx.WithContext(ctx).Create(&model).Error; err != nil {
			return problem.Wrap(409, "worker_pool_create_rejected", "Worker pool could not be created.", err)
		}
		created = toPool(model)
		return nil
	})
	return created, err
}

func (s *Service) UpdatePool(
	ctx context.Context,
	principal identity.Principal,
	tenantID, targetID, poolID uuid.UUID,
	input UpdatePoolInput,
) (Pool, error) {
	if err := requireActiveTenant(principal, tenantID); err != nil {
		return Pool{}, err
	}
	if _, err := s.authorizer.RequireTenant(ctx, principal.UserID, tenantID, authorization.WorkerManage); err != nil {
		return Pool{}, err
	}
	if input.ExpectedVersion <= 0 {
		return Pool{}, problem.New(400, "invalid_worker_pool_version", "expectedVersion must be positive.")
	}
	normalized, err := normalizePoolInput(input.CreatePoolInput)
	if err != nil {
		return Pool{}, err
	}
	var updated Pool
	err = persistence.InTransaction(ctx, s.db, func(tx *gorm.DB) error {
		target, err := s.loadAccessibleTarget(ctx, tx, tenantID, targetID, true)
		if err != nil {
			return err
		}
		if target.TenantID == nil {
			return problem.New(403, "shared_execution_target_worker_pool_immutable", "Platform-shared execution target worker pools cannot be changed by a tenant.")
		}
		if err := validatePoolTargetMode(target, normalized.Mode); err != nil {
			return err
		}
		var current persistence.WorkerPool
		err = persistence.WithLocking(tx.WithContext(ctx), "UPDATE", "").
			Where("id = ? AND execution_target_id = ? AND tenant_id = ?", poolID, targetID, tenantID).
			Take(&current).Error
		if errors.Is(err, gorm.ErrRecordNotFound) {
			return problem.New(404, "worker_pool_not_found", "Worker pool not found.")
		}
		if err != nil {
			return problem.Wrap(500, "worker_pool_load_failed", "Worker pool could not be loaded.", err)
		}
		if current.Version != input.ExpectedVersion {
			return problem.New(409, "worker_pool_version_conflict", "Worker pool changed; reload it before saving.")
		}
		if current.Mode != normalized.Mode || current.CapacityClass != normalized.CapacityClass {
			return problem.New(409, "worker_pool_identity_immutable", "Worker pool mode and capacityClass cannot change after creation.")
		}
		var blocked int64
		if err := tx.WithContext(ctx).Model(&persistence.AgentExecution{}).
			Where(
				"execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ? AND status IN ?",
				target.ID,
				current.ID,
				current.Version,
				[]string{"queued", "recovering", "leased", "running", "waiting-for-approval", "suspended"},
			).
			Count(&blocked).Error; err != nil {
			return problem.Wrap(500, "worker_pool_update_probe_failed", "Worker pool update safety could not be evaluated.", err)
		}
		if blocked > 0 {
			return problem.New(
				409,
				"worker_pool_version_update_blocked",
				"Worker pool version cannot change while a nonterminal Execution still references the current pool snapshot.",
			)
		}
		now := s.now()
		expectedVersion := current.Version
		current.Name = normalized.Name
		current.Mode = normalized.Mode
		current.CapacityClass = normalized.CapacityClass
		current.ClusterID = normalized.ClusterID
		current.Region = normalized.Region
		current.Namespace = normalized.Namespace
		current.DesiredIdleUnits = normalized.DesiredIdleUnits
		current.MaxActiveUnits = normalized.MaxActiveUnits
		current.SchedulingTemplate = normalized.SchedulingTemplate
		current.Status = normalized.Status
		current.Version++
		current.UpdatedAt = now
		result := tx.WithContext(ctx).Model(&persistence.WorkerPool{}).
			Where("id = ? AND version = ?", current.ID, expectedVersion).
			Select(
				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
			).
			Updates(&current)
		if result.Error != nil {
			return problem.Wrap(500, "worker_pool_update_failed", "Worker pool could not be updated.", result.Error)
		}
		if result.RowsAffected != 1 {
			return problem.New(409, "worker_pool_version_conflict", "Worker pool changed; reload it before saving.")
		}
		updated = toPool(current)
		return nil
	})
	return updated, err
}

}

func selectionForPool(pool persistence.WorkerPool, policyVersion int64) Selection {
	selected := toPool(pool)
	return Selection{Pool: selected, CapacityClass: selected.CapacityClass, PolicyVersion: policyVersion}
}

func previewDefaultPool(
	target persistence.ExecutionTarget,
	models []persistence.WorkerPool,
) persistence.WorkerPool {
	defaultID := deterministicUUID(target.ID.String() + ":worker-pool:default:000058")
	for _, model := range models {
		if model.ID == defaultID || strings.EqualFold(model.Name, "default") {
			return model
		}
	}
	return persistence.WorkerPool{
		ID:                 defaultID,
		TenantID:           target.TenantID,
		ExecutionTargetID:  target.ID,
		Name:               "default",
		Mode:               defaultPoolMode(target.Kind),
		CapacityClass:      CapacityClassStandard,
		ClusterID:          "",
		Region:             "",
		Namespace:          "",
		DesiredIdleUnits:   0,
		MaxActiveUnits:     1,
		SchedulingTemplate: map[string]any{},
		Status:             PoolStatusActive,
		Version:            1,
	}
}

type resolvedPolicyInput struct {
	defaultPool      persistence.WorkerPool
	balancedPoolID   *uuid.UUID
	lowLatencyPoolID *uuid.UUID
}

func validatePolicyInput(
	target persistence.ExecutionTarget,
	models []persistence.WorkerPool,
	defaultPoolID uuid.UUID,
	balancedPoolID, lowLatencyPoolID *uuid.UUID,
) (resolvedPolicyInput, error) {
	byID := make(map[uuid.UUID]persistence.WorkerPool, len(models))
	for _, model := range models {
		byID[model.ID] = model
	}
	defaultPool, ok := byID[defaultPoolID]
	if !ok || !sameUUIDPointer(defaultPool.TenantID, target.TenantID) || defaultPool.ExecutionTargetID != target.ID {
		return resolvedPolicyInput{}, problem.New(409, "execution_placement_pool_not_found", "Execution placement default pool is unavailable for this target.")
	}
	if defaultPool.Status != PoolStatusActive {
		return resolvedPolicyInput{}, problem.New(409, "execution_placement_policy_invalid", "Execution placement default pool must be active.")
	}
	if balancedPoolID != nil {
		if balanced, ok := byID[*balancedPoolID]; !ok || !sameUUIDPointer(balanced.TenantID, target.TenantID) || balanced.ExecutionTargetID != target.ID {
			return resolvedPolicyInput{}, problem.New(409, "execution_placement_pool_not_found", "Execution placement balanced pool is unavailable for this target.")
		}
	}
	if lowLatencyPoolID != nil {
		if lowLatency, ok := byID[*lowLatencyPoolID]; !ok || !sameUUIDPointer(lowLatency.TenantID, target.TenantID) || lowLatency.ExecutionTargetID != target.ID {
			return resolvedPolicyInput{}, problem.New(409, "execution_placement_pool_not_found", "Execution placement low-latency pool is unavailable for this target.")
		}
	}
	return resolvedPolicyInput{
		defaultPool:      defaultPool,
		balancedPoolID:   balancedPoolID,
		lowLatencyPoolID: lowLatencyPoolID,
	}, nil
}

func (s *Service) ensureDefaultLocked(
	ctx context.Context,
	tx *gorm.DB,
	target persistence.ExecutionTarget,
) (persistence.ExecutionPlacementPolicy, []persistence.WorkerPool, error) {
	lockedTarget, err := s.lockTarget(ctx, tx, target.ID)
	if err != nil {
		return persistence.ExecutionPlacementPolicy{}, nil, err
	}
	models, err := s.loadPools(ctx, tx, lockedTarget.ID)
	if err != nil {
		return persistence.ExecutionPlacementPolicy{}, nil, err
	}
	var policy persistence.ExecutionPlacementPolicy
	err = persistence.WithLocking(tx.WithContext(ctx), "UPDATE", "").
		Where("execution_target_id = ?", lockedTarget.ID).
		Take(&policy).Error
	if err == nil {
		if !sameUUIDPointer(policy.TenantID, lockedTarget.TenantID) {
			return persistence.ExecutionPlacementPolicy{}, nil, problem.New(409, "execution_placement_policy_invalid", "Execution placement policy ownership does not match the execution target.")
		}
		return policy, models, nil
	}
	if !errors.Is(err, gorm.ErrRecordNotFound) {
		return persistence.ExecutionPlacementPolicy{}, nil, problem.Wrap(500, "execution_placement_policy_load_failed", "Execution placement policy could not be loaded.", err)
	}
	defaultPool, models, err := s.ensureDefaultPool(ctx, tx, lockedTarget, models)
	if err != nil {
		return persistence.ExecutionPlacementPolicy{}, nil, err
	}
	now := s.now()
	policy = persistence.ExecutionPlacementPolicy{
		TenantID:          lockedTarget.TenantID,
		ExecutionTargetID: lockedTarget.ID,
		Version:           1,
		DefaultPoolID:     defaultPool.ID,
		UpdatedAt:         now,
	}
	if err := tx.WithContext(ctx).Create(&policy).Error; err != nil {
		return persistence.ExecutionPlacementPolicy{}, nil, problem.Wrap(409, "execution_placement_policy_create_rejected", "Execution placement policy could not be created.", err)
	}
	return policy, models, nil
}

func (s *Service) ensureDefaultPool(
	ctx context.Context,
	tx *gorm.DB,
	target persistence.ExecutionTarget,
	models []persistence.WorkerPool,
) (persistence.WorkerPool, []persistence.WorkerPool, error) {
	defaultID := deterministicUUID(target.ID.String() + ":worker-pool:default:000058")
	for _, model := range models {
		if model.ID == defaultID || strings.EqualFold(model.Name, "default") {
			return model, models, nil
		}
	}
	now := s.now()
	model := persistence.WorkerPool{
		ID:                 defaultID,
		TenantID:           target.TenantID,
		ExecutionTargetID:  target.ID,
		Name:               "default",
		Mode:               defaultPoolMode(target.Kind),
		CapacityClass:      CapacityClassStandard,
		ClusterID:          "",
		Region:             "",
		Namespace:          "",
		DesiredIdleUnits:   0,
		MaxActiveUnits:     1,
		SchedulingTemplate: map[string]any{},
		Status:             PoolStatusActive,
		Version:            1,
		CreatedAt:          now,
		UpdatedAt:          now,
	}
	if err := tx.WithContext(ctx).Create(&model).Error; err != nil {
		return persistence.WorkerPool{}, nil, problem.Wrap(409, "worker_pool_create_rejected", "Worker pool could not be created.", err)
	}
	models = append(models, model)
	return model, models, nil
}

func (s *Service) loadAccessibleTarget(
	ctx context.Context,
	tx *gorm.DB,
	tenantID, targetID uuid.UUID,
	forUpdate bool,
) (persistence.ExecutionTarget, error) {
	query := tx.WithContext(ctx)
	if forUpdate {
		query = persistence.WithLocking(query, "UPDATE", "")
	for _, model := range models {
		pools = append(pools, toPool(model))
	}
	return State{Pools: pools, Policy: toPolicy(policy)}
}

func toPool(model persistence.WorkerPool) Pool {
	template := model.SchedulingTemplate
	if template == nil {
		template = map[string]any{}
	}
	return Pool{
		ID:                 model.ID,
		TenantID:           model.TenantID,
		ExecutionTargetID:  model.ExecutionTargetID,
		Name:               model.Name,
		Mode:               model.Mode,
		CapacityClass:      model.CapacityClass,
		ClusterID:          model.ClusterID,
		Region:             model.Region,
		Namespace:          model.Namespace,
		DesiredIdleUnits:   model.DesiredIdleUnits,
		MaxActiveUnits:     model.MaxActiveUnits,
		SchedulingTemplate: template,
		Status:             model.Status,
		Version:            model.Version,
		CreatedAt:          model.CreatedAt,
		UpdatedAt:          model.UpdatedAt,
	}
}

func toPolicy(model persistence.ExecutionPlacementPolicy) Policy {
	return Policy{
		TenantID:          model.TenantID,
		ExecutionTargetID: model.ExecutionTargetID,
		Version:           model.Version,

exec
/bin/zsh -lc "rg -n 'type .*Input struct|\\*int .*json|intPointer|pointer.*int|optionalInt|IntValue' services/control-plane/internal | head -n 160" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
services/control-plane/internal/placement/service.go:74:type UpdatePolicyInput struct {
services/control-plane/internal/placement/service.go:81:type CreatePoolInput struct {
services/control-plane/internal/placement/service.go:94:type UpdatePoolInput struct {
services/control-plane/internal/placement/service.go:555:type resolvedPolicyInput struct {
services/control-plane/internal/tenancy/models.go:61:type CreateTenantInput struct {
services/control-plane/internal/tenancy/models.go:68:type UpdateTenantInput struct {
services/control-plane/internal/tenancy/models.go:75:type InviteTenantMemberInput struct {
services/control-plane/internal/tenancy/models.go:90:type UpdateTenantMemberInput struct {
services/control-plane/internal/tenancy/models.go:95:type CreateOrganizationInput struct {
services/control-plane/internal/tenancy/models.go:103:type UpdateOrganizationInput struct {
services/control-plane/internal/tenancy/models.go:109:type PutOrganizationMemberInput struct {
services/control-plane/internal/tenancy/models.go:115:type UpdateOrganizationMemberInput struct {
services/control-plane/internal/credentials/service.go:51:type CreateInput struct {
services/control-plane/internal/credentials/service.go:66:type RotateInput struct {
services/control-plane/internal/credentials/service.go:72:type SetAutoSelectInput struct {
services/control-plane/internal/credentials/service.go:85:type UpdateScopePolicyInput struct {
services/control-plane/internal/schedulingpolicy/service.go:80:type UpdateInput struct {
services/control-plane/internal/retention/service.go:26:	SessionArchiveAfterDays   *int      `json:"sessionArchiveAfterDays"`
services/control-plane/internal/retention/service.go:27:	ArtifactDeleteAfterDays   *int      `json:"artifactDeleteAfterDays"`
services/control-plane/internal/retention/service.go:28:	WorkspaceCleanupAfterDays *int      `json:"workspaceCleanupAfterDays"`
services/control-plane/internal/retention/service.go:34:type UpdateInput struct {
services/control-plane/internal/retention/service.go:35:	SessionArchiveAfterDays   *int `json:"sessionArchiveAfterDays"`
services/control-plane/internal/retention/service.go:36:	ArtifactDeleteAfterDays   *int `json:"artifactDeleteAfterDays"`
services/control-plane/internal/retention/service.go:37:	WorkspaceCleanupAfterDays *int `json:"workspaceCleanupAfterDays"`
services/control-plane/internal/artifacts/models.go:36:type CreateInput struct {
services/control-plane/internal/artifacts/models.go:43:type CompleteInput struct {
services/control-plane/internal/artifacts/models.go:49:type WorkerCreateInput struct {
services/control-plane/internal/artifacts/models.go:60:type WorkerCompleteInput struct {
services/control-plane/internal/workerfacts/service.go:42:type EnsureInput struct {
services/control-plane/internal/workerfacts/service.go:52:type TransitionInput struct {
services/control-plane/internal/scim/service.go:42:type UserInput struct {
services/control-plane/internal/scim/service.go:65:type GroupInput struct {
services/control-plane/internal/routing/capacity_reservations_test.go:20:		AvailableCapacityUnits: intPointer(2), AllocatedCapacityUnits: 1,
services/control-plane/internal/routing/capacity_reservations_test.go:45:		AvailableCapacityUnits: intPointer(2), AllocatedCapacityUnits: 1,
services/control-plane/internal/routing/capacity_reservations_test.go:55:		AvailableCapacityUnits: intPointer(2), AllocatedCapacityUnits: 1,
services/control-plane/internal/routing/capacity_reservations_test.go:80:		AvailableCapacityUnits: intPointer(2), AllocatedCapacityUnits: 1,
services/control-plane/internal/routing/capacity_reservations_test.go:114:		AvailableCapacityUnits: intPointer(2), AllocatedCapacityUnits: 1,
services/control-plane/internal/routing/capacity_reservations_test.go:154:		AvailableCapacityUnits: intPointer(2), AllocatedCapacityUnits: 1,
services/control-plane/internal/projects/models.go:26:type CreateProjectInput struct {
services/control-plane/internal/projects/models.go:34:type UpdateProjectInput struct {
services/control-plane/internal/credentialbindings/models.go:24:type CreateInput struct {
services/control-plane/internal/billing/shared_management.go:33:type SealSharedTargetLedgerCoverageInput struct {
services/control-plane/internal/billing/shared_management.go:49:type SweepSharedUsageChargesInput struct {
services/control-plane/internal/routing/commit_validation_postgres_integration_test.go:151:		AvailableCapacityUnits: intPointer(1), AllocatedCapacityUnits: 0,
services/control-plane/internal/routing/commit_validation_postgres_integration_test.go:327:		AvailableCapacityUnits: intPointer(10), Source: "postgres-routing-commit", ObservedAt: now, TTL: time.Minute,
services/control-plane/internal/sessions/models.go:76:type CreateSessionInput struct {
services/control-plane/internal/sessions/models.go:88:type CreateTurnInput struct {
services/control-plane/internal/sessions/models.go:100:type SwitchModelInput struct {
services/control-plane/internal/sessions/models.go:106:type RollbackSessionInput struct {
services/control-plane/internal/sessions/models.go:124:type ForkSessionInput struct {
services/control-plane/internal/quotas/service.go:20:	MaxConcurrentExecutions *int      `json:"maxConcurrentExecutions"`
services/control-plane/internal/quotas/service.go:24:type PutInput struct {
services/control-plane/internal/quotas/service.go:25:	MaxConcurrentExecutions *int   `json:"maxConcurrentExecutions"`
services/control-plane/internal/serviceaccounts/service.go:56:type CreateInput struct {
services/control-plane/internal/routing/platform_authority_http_integration_test.go:58:			AvailableCapacityUnits: intPointer(20), AllocatedCapacityUnits: 5, TTLSeconds: 300,
services/control-plane/internal/billing/shared_allocation.go:28:type AllocateSharedUsageChargesInput struct {
services/control-plane/internal/identity/service.go:61:type DevLoginInput struct {
services/control-plane/internal/identity/service.go:76:type ExternalLoginInput struct {
services/control-plane/internal/routing/platform_authority_test.go:97:		AvailableCapacityUnits: intPointer(4), AllocatedCapacityUnits: 1, Source: "seed-health",
services/control-plane/internal/routing/platform_authority_test.go:306:			AvailableCapacityUnits: intPointer(10), AllocatedCapacityUnits: 2, TTLSeconds: 600,
services/control-plane/internal/executiontargets/ssh_provisioner.go:85:	CgroupV2ProviderUID        *int     `json:"cgroupV2ProviderUid"`
services/control-plane/internal/executiontargets/ssh_provisioner.go:86:	CgroupV2ProviderGID        *int     `json:"cgroupV2ProviderGid"`
services/control-plane/internal/executiontargets/ssh_provisioner.go:91:type sshDialInput struct {
services/control-plane/internal/workerreleases/models.go:68:type CreateRevisionInput struct {
services/control-plane/internal/workerreleases/models.go:73:type PolicyChangeInput struct {
services/control-plane/internal/workerreleases/models.go:80:type AutoRollbackInput struct {
services/control-plane/internal/routing/service_test.go:593:		AvailableCapacityUnits: intPointer(10), Source: "bypassed-service",
services/control-plane/internal/routing/service_test.go:805:		AvailableCapacityUnits: intPointer(4), Source: "test", ObservedAt: fixture.now, TTL: time.Minute,
services/control-plane/internal/routing/service_test.go:871:		MaxFailoverAttempts: intPointer(2), HealthMaxStalenessSeconds: 90,
services/control-plane/internal/routing/service_test.go:923:		AvailableCapacityUnits: intPointer(available), AllocatedCapacityUnits: allocated,
services/control-plane/internal/routing/service_test.go:1003:func intPointer(value int) *int { return &value }
services/control-plane/internal/routing/capacity_reservations.go:453:	writeCapacityEvidenceOptional(&canonical, "capacity_ceiling_units", intPointerString(value.CapacityCeilingUnits))
services/control-plane/internal/routing/capacity_reservations.go:454:	writeCapacityEvidenceOptional(&canonical, "allocated_capacity_units", intPointerString(value.AllocatedCapacityUnits))
services/control-plane/internal/routing/capacity_reservations.go:456:	writeCapacityEvidenceOptional(&canonical, "reservation_acknowledged_units", intPointerString(value.ReservationAcknowledgedUnits))
services/control-plane/internal/routing/capacity_reservations.go:484:func intPointerString(value *int) *string {
services/control-plane/internal/billing/shared_actual_allocation.go:33:type AllocateSharedActualInvoiceInput struct {
services/control-plane/internal/sessions/service.go:1146:type InternalEventInput struct {
services/control-plane/internal/sessions/service.go:1202:type eventInput struct {
services/control-plane/internal/observability/metrics_test.go:336:func pointerInt64(value int64) *int64 { return &value }
services/control-plane/internal/routing/platform_authority_postgres_integration_test.go:58:			AvailableCapacityUnits: intPointer(12), AllocatedCapacityUnits: 4, TTLSeconds: 120,
services/control-plane/internal/routing/platform_authority.go:63:	AvailableCapacityUnits *int                             `json:"availableCapacityUnits,omitempty"`
services/control-plane/internal/executiontargets/service.go:35:type CreateInput struct {
services/control-plane/internal/routing/service.go:51:type CreateGroupInput struct {
services/control-plane/internal/routing/service.go:63:type AddMemberInput struct {
services/control-plane/internal/routing/service.go:230:	AvailableCapacityUnits *int                            `json:"availableCapacityUnits,omitempty"`
services/control-plane/internal/schedulingdecision/schedulingdecision.go:49:type Input struct {
services/control-plane/internal/memories/service.go:34:type PublishInput struct {
services/control-plane/internal/billing/service.go:40:type CreateTariffInput struct {
services/control-plane/internal/billing/service.go:77:type EstimateUsageChargesInput struct {
services/control-plane/internal/httpapi/routing_api.go:16:type createExecutionTargetGroupInput struct {
services/control-plane/internal/httpapi/routing_api.go:22:	MaxFailoverAttempts       *int       `json:"maxFailoverAttempts,omitempty"`
services/control-plane/internal/httpapi/routing_api.go:26:type addExecutionTargetGroupMemberInput struct {
services/control-plane/internal/httpapi/routing_api.go:34:type observeExecutionTargetHealthInput struct {
services/control-plane/internal/httpapi/routing_api.go:39:	AvailableCapacityUnits *int                                    `json:"availableCapacityUnits,omitempty"`
services/control-plane/internal/httpapi/routing_api.go:47:type observeExecutionTargetDRReadinessInput struct {
services/control-plane/internal/httpapi/routing_api.go:60:type observeLocationOutageInput struct {
services/control-plane/internal/agentd/supervisor.go:20:type LocalSupervisorInput struct {
services/control-plane/internal/agentd/terminal_log_collector.go:311:		value, found, err := optionalInt64MapField(payload, key)
services/control-plane/internal/agentd/terminal_log_collector.go:324:func optionalInt64MapField(value map[string]any, key string) (int64, bool, error) {
services/control-plane/internal/agentd/models.go:15:type RunnerInput struct {
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:161:		TenantID: fixture.tenantID, WaitingKeepAliveSeconds: intPointer(900), Version: 1, UpdatedBy: fixture.userID,
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:168:		ProjectID: fixture.projectID, TenantID: fixture.tenantID, SuspendAfterIdleSeconds: intPointer(1800),
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:175:		TenantID: fixture.otherTenantID, WaitingKeepAliveSeconds: intPointer(900), Version: 1, UpdatedBy: uuid.New(),
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:180:		ProjectID: fixture.otherProjectID, TenantID: fixture.tenantID, SuspendAfterIdleSeconds: intPointer(1800),
services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go:554:func intPointer(value int) *int { return &value }
services/control-plane/internal/providercapabilities/projection.go:71:type TargetInput struct {
services/control-plane/internal/providercapabilities/projection.go:79:type ExecutionInput struct {
services/control-plane/internal/enterpriseidentity/service.go:71:type CreateConnectionInput struct {
services/control-plane/internal/enterpriseidentity/service.go:81:type MappingInput struct {
services/control-plane/internal/executions/advanced_operations_test.go:203:	fixture.observeTargetHealth(t, badTarget.ID, routing.HealthHealthy, routing.CapacityAvailable, intPointer(10), 0, now)
services/control-plane/internal/executions/advanced_operations_test.go:204:	fixture.observeTargetHealth(t, goodTarget.ID, routing.HealthHealthy, routing.CapacityAvailable, intPointer(10), 0, now)
services/control-plane/internal/executions/advanced_operations_test.go:360:	fixture.observeTargetHealth(t, source.ID, routing.HealthHealthy, routing.CapacityAvailable, intPointer(10), 0, now)
services/control-plane/internal/executions/advanced_operations_test.go:361:	fixture.observeTargetHealth(t, destination.ID, routing.HealthHealthy, routing.CapacityAvailable, intPointer(10), 0, now)
services/control-plane/internal/executions/advanced_operations_test.go:430:	fixture.observeTargetHealth(t, source.ID, routing.HealthHealthy, routing.CapacityAvailable, intPointer(10), 0, now)
services/control-plane/internal/executions/advanced_operations_test.go:431:	fixture.observeTargetHealth(t, destination.ID, routing.HealthHealthy, routing.CapacityAvailable, intPointer(10), 0, now)
services/control-plane/internal/executions/advanced_operations_test.go:454:	fixture.observeTargetHealth(t, destination.ID, routing.HealthHealthy, routing.CapacityAvailable, intPointer(10), 0, readyAt.Add(2*time.Second))
services/control-plane/internal/executions/advanced_operations_test.go:535:		AllowCrossRegion: true, MaxFailoverAttempts: intPointer(3), HealthMaxStalenessSeconds: 90,
services/control-plane/internal/executions/advanced_operations_test.go:572:	fixture.observeTargetHealth(t, source.ID, routing.HealthHealthy, routing.CapacityAvailable, intPointer(10), 0, now)
services/control-plane/internal/executions/advanced_operations_test.go:573:	fixture.observeTargetHealth(t, avoidDestination.ID, routing.HealthHealthy, routing.CapacityAvailable, intPointer(10), 0, now)
services/control-plane/internal/executions/advanced_operations_test.go:574:	fixture.observeTargetHealth(t, preferDestination.ID, routing.HealthHealthy, routing.CapacityAvailable, intPointer(10), 0, now)
services/control-plane/internal/executions/advanced_operations_test.go:597:	fixture.observeTargetHealth(t, avoidDestination.ID, routing.HealthHealthy, routing.CapacityAvailable, intPointer(10), 0, readyAt.Add(2*time.Second))
services/control-plane/internal/executions/advanced_operations_test.go:598:	fixture.observeTargetHealth(t, preferDestination.ID, routing.HealthHealthy, routing.CapacityAvailable, intPointer(10), 0, readyAt.Add(3*time.Second))
services/control-plane/internal/executions/advanced_operations_test.go:949:		AllowCrossRegion: true, MaxFailoverAttempts: intPointer(3), HealthMaxStalenessSeconds: 90,
services/control-plane/internal/executions/advanced_operations_test.go:1402:func intPointer(value int) *int { return &value }
services/control-plane/internal/database/stage3_migration_integration_test.go:1355:func pointerInt(value int) *int { return &value }
services/control-plane/internal/executions/models.go:109:type RevokeWorkerInput struct {
services/control-plane/internal/executions/models.go:201:type WorkspaceCleanupClaimInput struct {
services/control-plane/internal/executions/models.go:231:type WorkspaceCleanupLeaseInput struct {
services/control-plane/internal/executions/models.go:236:type WorkspaceCleanupFailedInput struct {
services/control-plane/internal/executions/models.go:334:	ProviderCredentialVersion           *int       `json:"providerCredentialVersion,omitempty"`
services/control-plane/internal/executions/models.go:531:type RegisterWorkerInput struct {
services/control-plane/internal/executions/models.go:559:type HeartbeatInput struct {
services/control-plane/internal/executions/models.go:567:type ClaimExecutionInput struct {
services/control-plane/internal/executions/models.go:573:type LeaseInput struct {
services/control-plane/internal/executions/models.go:579:type WorkspaceReadyInput struct {
services/control-plane/internal/executions/models.go:588:type WorkspaceFailedInput struct {
services/control-plane/internal/executions/models.go:594:type WorkspaceDirtyInput struct {
services/control-plane/internal/executions/models.go:613:type CreateWorkspaceCheckpointInput struct {
services/control-plane/internal/executions/models.go:621:	FileCount      *int           `json:"fileCount,omitempty"`
services/control-plane/internal/executions/models.go:626:type WorkspaceCheckpointReadyInput struct {
services/control-plane/internal/executions/models.go:632:type WorkspaceCheckpointFailedInput struct {
services/control-plane/internal/executions/models.go:653:	FileCount      *int           `json:"fileCount,omitempty"`
services/control-plane/internal/executions/models.go:664:type RenewLeaseInput struct {
services/control-plane/internal/executions/models.go:669:type CompleteExecutionInput struct {
services/control-plane/internal/executions/models.go:675:type FailExecutionInput struct {
services/control-plane/internal/executions/models.go:682:type ReleaseLeaseInput struct {
services/control-plane/internal/executions/models.go:688:type PullResourceDirectiveInput struct {
services/control-plane/internal/executions/models.go:703:type CompleteResourceSuspendInput struct {
services/control-plane/internal/executions/models.go:709:type MarkResourceSuspendCheckpointReadyInput struct {
services/control-plane/internal/executions/models.go:732:type MarkResourceSuspendQuiescedInput struct {
services/control-plane/internal/executions/models.go:742:type AbortResourceSuspendInput struct {
services/control-plane/internal/executions/models.go:755:type RuntimeEventInput struct {
services/control-plane/internal/executions/models.go:817:type ResolveApprovalInput struct {
services/control-plane/internal/executions/models.go:821:type ResolveUserInputInput struct {
services/control-plane/internal/executions/models.go:825:type PullInteractionResolutionsInput struct {
services/control-plane/internal/executions/models.go:843:type InteractionResolutionDeliveryInput struct {
services/control-plane/internal/executions/models.go:869:type PullControlCommandsInput struct {
services/control-plane/internal/executions/models.go:876:type PullControlUpdatesInput struct {
services/control-plane/internal/executions/models.go:887:type SteerActiveTurnInput struct {
services/control-plane/internal/executions/models.go:891:type CompactSessionInput struct {

exec
/bin/zsh -lc "rg -n 'CreatePoolInput\\{' services/control-plane --glob '*.go'" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
services/control-plane/internal/placement/service_test.go:280:	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
services/control-plane/internal/placement/service_test.go:301:		CreatePoolInput: CreatePoolInput{
services/control-plane/internal/placement/service_test.go:324:		CreatePoolInput: CreatePoolInput{
services/control-plane/internal/placement/service_test.go:334:		CreatePoolInput: CreatePoolInput{
services/control-plane/internal/placement/service_test.go:343:	if _, err := fixture.service.CreatePool(ctx, member, fixture.tenantID, target.ID, CreatePoolInput{
services/control-plane/internal/placement/service_test.go:350:	if _, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
services/control-plane/internal/placement/service_test.go:358:	if _, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, sharedTarget.ID, CreatePoolInput{
services/control-plane/internal/placement/service_test.go:366:	if _, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, sshTarget.ID, CreatePoolInput{
services/control-plane/internal/placement/service_test.go:379:	pool, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
services/control-plane/internal/placement/service_test.go:435:			CreatePoolInput: CreatePoolInput{
services/control-plane/internal/placement/service.go:806:		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_name", "Worker pool name must be between 1 and 160 characters.")
services/control-plane/internal/placement/service.go:812:		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_mode", "Worker pool mode must be resident, per-execution, or warm.")
services/control-plane/internal/placement/service.go:818:		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_class", "Worker pool capacityClass must be standard or interactive.")
services/control-plane/internal/placement/service.go:824:		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
services/control-plane/internal/placement/service.go:827:		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
services/control-plane/internal/placement/service.go:833:	return CreatePoolInput{

exec
/bin/zsh -lc "sed -n '1,210p' services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
exec
/bin/zsh -lc "sed -n '1,245p' services/control-plane/internal/warmcapacity/service.go
sed -n '1,380p' services/control-plane/internal/warmcapacity/service_test.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
package executiontargets

import (
	"context"
	"strings"
	"time"

	"github.com/google/uuid"

	"github.com/synara-ai/synara/services/control-plane/internal/warmcapacity"
)

const managedKubernetesWarmCapacityPublisherPrefix = "managed-kubernetes-warm-capacity-publisher:"

type ManagedKubernetesWarmCapacityObservation struct {
	TenantID                uuid.UUID
	ExecutionTargetID       uuid.UUID
	WorkerPoolID            uuid.UUID
	WorkerPoolVersion       int64
	WarmSupported           bool
	WorkerReleaseRevisionID *uuid.UUID
	WorkerReleaseChannel    *string
	DesiredTotalUnits       int
	ClaimedUnits            int
	ReadyIdleUnits          int
	Reason                  *string
	ObservedAt              time.Time
}

type ManagedKubernetesWarmCapacityObserver func(context.Context, ManagedKubernetesWarmCapacityObservation) error

type ManagedKubernetesWarmCapacityPublisherConfig struct {
	PublisherIdentity string
	ObservationTTL    time.Duration
}

type ManagedKubernetesWarmCapacityPublisher struct {
	warmCapacity      *warmcapacity.Service
	publisherIdentity string
	observationTTL    time.Duration
	now               func() time.Time
}

func NewManagedKubernetesWarmCapacityPublisher(
	targets *Service,
	config ManagedKubernetesWarmCapacityPublisherConfig,
) *ManagedKubernetesWarmCapacityPublisher {
	ttl := config.ObservationTTL
	if ttl <= 0 {
		ttl = 90 * time.Second
	}
	return &ManagedKubernetesWarmCapacityPublisher{
		warmCapacity:      warmcapacity.NewService(targets.db),
		publisherIdentity: normalizeManagedKubernetesWarmCapacityPublisherIdentity(config.PublisherIdentity),
		observationTTL:    ttl,
		now:               func() time.Time { return time.Now().UTC() },
	}
}

func (p *ManagedKubernetesWarmCapacityPublisher) PublishReconcile(
	ctx context.Context,
	observation ManagedKubernetesWarmCapacityObservation,
) error {
	observedAt := observation.ObservedAt.UTC()
	if observedAt.IsZero() {
		observedAt = p.now()
	}
	_, err := p.warmCapacity.Observe(ctx, warmcapacity.Observation{
		TenantID:                observation.TenantID,
		ExecutionTargetID:       observation.ExecutionTargetID,
		WorkerPoolID:            observation.WorkerPoolID,
		WorkerPoolVersion:       observation.WorkerPoolVersion,
		WarmSupported:           observation.WarmSupported,
		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
		DesiredTotalUnits:       observation.DesiredTotalUnits,
		ClaimedUnits:            observation.ClaimedUnits,
		ReadyIdleUnits:          observation.ReadyIdleUnits,
		Source:                  p.publisherIdentity,
		Reason:                  observation.Reason,
		ObservedAt:              observedAt,
		TTL:                     p.observationTTL,
	})
	return err
}

func normalizeManagedKubernetesWarmCapacityPublisherIdentity(identity string) string {
	identity = strings.TrimSpace(identity)
	if identity == "" {
		identity = managedKubernetesWarmCapacityPublisherPrefix + "default"
	}
	if len(identity) > 160 {
		identity = identity[:160]
	}
	return identity
}

 succeeded in 0ms:
package warmcapacity

import (
	"context"
	"errors"
	"strings"
	"time"

	"github.com/google/uuid"
	"gorm.io/gorm"

	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
	"github.com/synara-ai/synara/services/control-plane/internal/problem"
)

const (
	poolModeWarm             = "warm"
	poolStatusDisabled       = "disabled"
	targetKindKubernetes     = "kubernetes"
	targetStatusActive       = "active"
	capacityClassStandard    = "standard"
	capacityClassInteractive = "interactive"
	releaseChannelPromoted   = "promoted"
	releaseChannelCanary     = "canary"
	minObservationTTL        = 10 * time.Second
	maxObservationTTL        = time.Hour
	maxReasonLength          = 2000
)

type Observation struct {
	TenantID                uuid.UUID
	ExecutionTargetID       uuid.UUID
	WorkerPoolID            uuid.UUID
	WorkerPoolVersion       int64
	WarmSupported           bool
	WorkerReleaseRevisionID *uuid.UUID
	WorkerReleaseChannel    *string
	DesiredTotalUnits       int
	ClaimedUnits            int
	ReadyIdleUnits          int
	Source                  string
	Reason                  *string
	ObservedAt              time.Time
	TTL                     time.Duration
}

type FreshLookup struct {
	TenantID                uuid.UUID
	ExecutionTargetID       uuid.UUID
	WorkerPoolID            uuid.UUID
	WorkerPoolVersion       int64
	WorkerReleaseRevisionID *uuid.UUID
	WorkerReleaseChannel    *string
	Now                     time.Time
}

type Service struct {
	db  *gorm.DB
	now func() time.Time
}

func NewService(db *gorm.DB) *Service {
	return &Service{
		db:  db,
		now: func() time.Time { return time.Now().UTC() },
	}
}

func (s *Service) Observe(ctx context.Context, input Observation) (persistence.WorkerPoolWarmCapacity, error) {
	if input.TenantID == uuid.Nil || input.ExecutionTargetID == uuid.Nil || input.WorkerPoolID == uuid.Nil || input.WorkerPoolVersion <= 0 {
		return persistence.WorkerPoolWarmCapacity{}, problem.New(400, "invalid_warm_capacity_scope", "Tenant, Execution Target, Worker Pool, and positive Worker Pool version are required.")
	}
	if input.TTL < minObservationTTL || input.TTL > maxObservationTTL {
		return persistence.WorkerPoolWarmCapacity{}, problem.New(400, "invalid_warm_capacity_ttl", "Warm capacity TTL must be between 10 seconds and 1 hour.")
	}
	source := strings.TrimSpace(input.Source)
	if source == "" || len(source) > 160 {
		return persistence.WorkerPoolWarmCapacity{}, problem.New(400, "invalid_warm_capacity_source", "Warm capacity source must be between 1 and 160 characters.")
	}
	reason, err := normalizeReason(input.Reason)
	if err != nil {
		return persistence.WorkerPoolWarmCapacity{}, err
	}
	releaseRevisionID, releaseChannel, err := normalizeReleasePair(input.WorkerReleaseRevisionID, input.WorkerReleaseChannel)
	if err != nil {
		return persistence.WorkerPoolWarmCapacity{}, err
	}
	if err := validateCounters(input.WarmSupported, input.DesiredTotalUnits, input.ClaimedUnits, input.ReadyIdleUnits); err != nil {
		return persistence.WorkerPoolWarmCapacity{}, err
	}
	observedAt := input.ObservedAt.UTC()
	if observedAt.IsZero() {
		observedAt = s.now()
	}

	var result persistence.WorkerPoolWarmCapacity
	err = persistence.InTransaction(ctx, s.db, func(tx *gorm.DB) error {
		target, pool, err := loadScopedWarmPool(ctx, tx, input.TenantID, input.ExecutionTargetID, input.WorkerPoolID)
		if err != nil {
			return err
		}
		if target.Kind != targetKindKubernetes || target.Status != targetStatusActive || target.TenantID == nil {
			return problem.New(409, "warm_capacity_target_unsupported", "Warm capacity authority is only available for active tenant-owned Kubernetes Execution Targets.")
		}
		if pool.Mode != poolModeWarm {
			return problem.New(409, "warm_capacity_pool_mode_invalid", "Warm capacity authority only applies to Worker Pools in warm mode.")
		}
		if pool.Status == poolStatusDisabled {
			return problem.New(409, "warm_capacity_pool_disabled", "Warm capacity authority cannot be written for a disabled Worker Pool.")
		}
		if pool.Version != input.WorkerPoolVersion {
			return problem.New(409, "warm_capacity_pool_version_stale", "Warm capacity authority must reference the current Worker Pool version.")
		}
		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
		}
		if input.DesiredTotalUnits > pool.MaxActiveUnits {
			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
		}
		if releaseRevisionID != nil {
			var revision persistence.WorkerReleaseRevision
			if err := tx.WithContext(ctx).
				Where("id = ? AND tenant_id = ? AND execution_target_id = ?", *releaseRevisionID, input.TenantID, input.ExecutionTargetID).
				Take(&revision).Error; err != nil {
				if errors.Is(err, gorm.ErrRecordNotFound) {
					return problem.New(409, "warm_capacity_release_pair_invalid", "Warm capacity authority must reference a Worker release revision owned by this Execution Target.")
				}
				return problem.Wrap(500, "warm_capacity_release_lookup_failed", "Warm capacity release metadata could not be loaded.", err)
			}
		}

		var current persistence.WorkerPoolWarmCapacity
		err = persistence.WithLocking(tx.WithContext(ctx), "UPDATE", "").
			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
				input.TenantID, input.ExecutionTargetID, input.WorkerPoolID, input.WorkerPoolVersion).
			Take(&current).Error
		now := s.now()
		if errors.Is(err, gorm.ErrRecordNotFound) {
			result = persistence.WorkerPoolWarmCapacity{
				WorkerPoolID:            input.WorkerPoolID,
				WorkerPoolVersion:       input.WorkerPoolVersion,
				TenantID:                input.TenantID,
				ExecutionTargetID:       input.ExecutionTargetID,
				CapacityClass:           pool.CapacityClass,
				WarmSupported:           input.WarmSupported,
				WorkerReleaseRevisionID: releaseRevisionID,
				WorkerReleaseChannel:    releaseChannel,
				DesiredIdleUnits:        pool.DesiredIdleUnits,
				MaxActiveUnits:          pool.MaxActiveUnits,
				DesiredTotalUnits:       input.DesiredTotalUnits,
				ClaimedUnits:            input.ClaimedUnits,
				ReadyIdleUnits:          input.ReadyIdleUnits,
				Source:                  source,
				Reason:                  reason,
				ObservedAt:              observedAt,
				ExpiresAt:               observedAt.Add(input.TTL),
				Version:                 1,
				UpdatedAt:               now,
			}
			if err := tx.WithContext(ctx).Create(&result).Error; err != nil {
				return problem.Wrap(409, "warm_capacity_create_rejected", "Warm capacity authority could not be created.", err)
			}
			return nil
		}
		if err != nil {
			return problem.Wrap(500, "warm_capacity_load_failed", "Warm capacity authority could not be loaded.", err)
		}
		if !observedAt.After(current.ObservedAt) {
			return problem.New(409, "warm_capacity_observation_stale", "Warm capacity authority must advance observedAt monotonically.")
		}
		result = current
		result.CapacityClass = pool.CapacityClass
		result.WarmSupported = input.WarmSupported
		result.WorkerReleaseRevisionID = releaseRevisionID
		result.WorkerReleaseChannel = releaseChannel
		result.DesiredIdleUnits = pool.DesiredIdleUnits
		result.MaxActiveUnits = pool.MaxActiveUnits
		result.DesiredTotalUnits = input.DesiredTotalUnits
		result.ClaimedUnits = input.ClaimedUnits
		result.ReadyIdleUnits = input.ReadyIdleUnits
		result.Source = source
		result.Reason = reason
		result.ObservedAt = observedAt
		result.ExpiresAt = observedAt.Add(input.TTL)
		result.Version = current.Version + 1
		result.UpdatedAt = now

		updates := map[string]any{
			"capacity_class":             result.CapacityClass,
			"warm_supported":             result.WarmSupported,
			"worker_release_revision_id": result.WorkerReleaseRevisionID,
			"worker_release_channel":     result.WorkerReleaseChannel,
			"desired_idle_units":         result.DesiredIdleUnits,
			"max_active_units":           result.MaxActiveUnits,
			"desired_total_units":        result.DesiredTotalUnits,
			"claimed_units":              result.ClaimedUnits,
			"ready_idle_units":           result.ReadyIdleUnits,
			"source":                     result.Source,
			"reason":                     result.Reason,
			"observed_at":                result.ObservedAt,
			"expires_at":                 result.ExpiresAt,
			"version":                    result.Version,
			"updated_at":                 result.UpdatedAt,
		}
		update := tx.WithContext(ctx).Model(&persistence.WorkerPoolWarmCapacity{}).
			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ? AND version = ?",
				input.TenantID, input.ExecutionTargetID, input.WorkerPoolID, input.WorkerPoolVersion, current.Version).
			Updates(updates)
		if update.Error != nil {
			return problem.Wrap(409, "warm_capacity_update_rejected", "Warm capacity authority could not be advanced.", update.Error)
		}
		if update.RowsAffected != 1 {
			return problem.New(409, "warm_capacity_version_conflict", "Warm capacity authority changed concurrently; reload and retry.")
		}
		return nil
	})
	return result, err
}

func (s *Service) GetFresh(ctx context.Context, lookup FreshLookup) (*persistence.WorkerPoolWarmCapacity, error) {
	if lookup.TenantID == uuid.Nil || lookup.ExecutionTargetID == uuid.Nil || lookup.WorkerPoolID == uuid.Nil || lookup.WorkerPoolVersion <= 0 {
		return nil, problem.New(400, "invalid_warm_capacity_lookup", "Tenant, Execution Target, Worker Pool, and positive Worker Pool version are required.")
	}
	releaseRevisionID, releaseChannel, err := normalizeReleasePair(lookup.WorkerReleaseRevisionID, lookup.WorkerReleaseChannel)
	if err != nil {
		return nil, err
	}
	at := lookup.Now.UTC()
	if at.IsZero() {
		at = s.now()
	}

	var row persistence.WorkerPoolWarmCapacity
	err = s.db.WithContext(ctx).
		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
			lookup.TenantID, lookup.ExecutionTargetID, lookup.WorkerPoolID, lookup.WorkerPoolVersion).
		Take(&row).Error
	if errors.Is(err, gorm.ErrRecordNotFound) {
		return nil, nil
	}
	if err != nil {
		return nil, problem.Wrap(500, "warm_capacity_lookup_failed", "Warm capacity authority could not be loaded.", err)
	}
	if !row.ExpiresAt.After(at) {
		return nil, nil
package warmcapacity

import (
	"context"
	"errors"
	"testing"
	"time"

	"github.com/glebarez/sqlite"
	"github.com/google/uuid"
	"gorm.io/gorm"
	"gorm.io/gorm/logger"

	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
	"github.com/synara-ai/synara/services/control-plane/internal/problem"
)

func TestObserveAndGetFreshRoundTrip(t *testing.T) {
	fixture := newWarmCapacityFixture(t)
	observedAt := fixture.now
	channel := releaseChannelPromoted
	observed, err := fixture.service.Observe(context.Background(), Observation{
		TenantID:                fixture.tenantID,
		ExecutionTargetID:       fixture.target.ID,
		WorkerPoolID:            fixture.pool.ID,
		WorkerPoolVersion:       fixture.pool.Version,
		WarmSupported:           true,
		WorkerReleaseRevisionID: &fixture.release.ID,
		WorkerReleaseChannel:    &channel,
		DesiredTotalUnits:       3,
		ClaimedUnits:            1,
		ReadyIdleUnits:          2,
		Source:                  "reconciler/orbstack",
		Reason:                  stringPointer("healthy"),
		ObservedAt:              observedAt,
		TTL:                     time.Minute,
	})
	if err != nil {
		t.Fatal(err)
	}
	if observed.CapacityClass != fixture.pool.CapacityClass ||
		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
		observed.Version != 1 ||
		observed.WorkerReleaseRevisionID == nil ||
		*observed.WorkerReleaseRevisionID != fixture.release.ID ||
		observed.WorkerReleaseChannel == nil ||
		*observed.WorkerReleaseChannel != releaseChannelPromoted {
		t.Fatalf("observed warm capacity = %#v", observed)
	}

	fresh, err := fixture.service.GetFresh(context.Background(), FreshLookup{
		TenantID:                fixture.tenantID,
		ExecutionTargetID:       fixture.target.ID,
		WorkerPoolID:            fixture.pool.ID,
		WorkerPoolVersion:       fixture.pool.Version,
		WorkerReleaseRevisionID: &fixture.release.ID,
		WorkerReleaseChannel:    &channel,
		Now:                     observedAt.Add(30 * time.Second),
	})
	if err != nil {
		t.Fatal(err)
	}
	if fresh == nil || fresh.ReadyIdleUnits != 2 || fresh.ClaimedUnits != 1 {
		t.Fatalf("fresh warm capacity = %#v", fresh)
	}

	secondObservedAt := observedAt.Add(time.Second)
	advanced, err := fixture.service.Observe(context.Background(), Observation{
		TenantID:                fixture.tenantID,
		ExecutionTargetID:       fixture.target.ID,
		WorkerPoolID:            fixture.pool.ID,
		WorkerPoolVersion:       fixture.pool.Version,
		WarmSupported:           true,
		WorkerReleaseRevisionID: &fixture.release.ID,
		WorkerReleaseChannel:    &channel,
		DesiredTotalUnits:       4,
		ClaimedUnits:            2,
		ReadyIdleUnits:          1,
		Source:                  "reconciler/orbstack",
		ObservedAt:              secondObservedAt,
		TTL:                     2 * time.Minute,
	})
	if err != nil {
		t.Fatal(err)
	}
	if advanced.Version != 2 || advanced.ObservedAt != secondObservedAt || advanced.DesiredTotalUnits != 4 {
		t.Fatalf("advanced warm capacity = %#v", advanced)
	}
}

func TestObserveRejectsInvalidScopeAndCounters(t *testing.T) {
	fixture := newWarmCapacityFixture(t)
	sharedTarget := fixture.target
	sharedTarget.ID = uuid.New()
	sharedTarget.TenantID = nil
	sharedTarget.Name = "shared-kubernetes"
	if err := fixture.db.Create(&sharedTarget).Error; err != nil {
		t.Fatal(err)
	}
	sharedPool := fixture.pool
	sharedPool.ID = uuid.New()
	sharedPool.TenantID = nil
	sharedPool.ExecutionTargetID = sharedTarget.ID
	sharedPool.Name = "shared-warm"
	if err := fixture.db.Create(&sharedPool).Error; err != nil {
		t.Fatal(err)
	}

	base := Observation{
		TenantID:          fixture.tenantID,
		ExecutionTargetID: fixture.target.ID,
		WorkerPoolID:      fixture.pool.ID,
		WorkerPoolVersion: fixture.pool.Version,
		WarmSupported:     true,
		DesiredTotalUnits: 2,
		ClaimedUnits:      1,
		ReadyIdleUnits:    1,
		Source:            "reconciler/test",
		ObservedAt:        fixture.now,
		TTL:               time.Minute,
	}
	nonWarmPool := fixture.pool
	nonWarmPool.ID = uuid.New()
	nonWarmPool.Name = "resident-pool"
	nonWarmPool.Mode = "resident"
	if err := fixture.db.Create(&nonWarmPool).Error; err != nil {
		t.Fatal(err)
	}

	cases := []struct {
		name string
		mut  func(*Observation)
		code string
	}{
		{
			name: "shared target rejected",
			mut: func(input *Observation) {
				input.ExecutionTargetID = sharedTarget.ID
				input.WorkerPoolID = sharedPool.ID
			},
			code: "warm_capacity_target_not_found",
		},
		{
			name: "non warm mode rejected",
			mut: func(input *Observation) {
				input.WorkerPoolID = nonWarmPool.ID
				input.WorkerPoolVersion = nonWarmPool.Version
			},
			code: "warm_capacity_pool_mode_invalid",
		},
		{
			name: "unsupported counters rejected",
			mut: func(input *Observation) {
				input.WarmSupported = false
				input.DesiredTotalUnits = 1
				input.ReadyIdleUnits = 1
				input.ClaimedUnits = 3
			},
			code: "invalid_warm_capacity_counters",
		},
		{
			name: "desired total over pool max rejected",
			mut: func(input *Observation) {
				input.DesiredTotalUnits = fixture.pool.MaxActiveUnits + 1
				input.ClaimedUnits = fixture.pool.MaxActiveUnits + 3
				input.ReadyIdleUnits = 0
			},
			code: "invalid_warm_capacity_counters",
		},
		{
			name: "release pair shape rejected",
			mut: func(input *Observation) {
				channel := releaseChannelCanary
				input.WorkerReleaseChannel = &channel
			},
			code: "invalid_warm_capacity_release_pair",
		},
		{
			name: "stale pool version rejected",
			mut: func(input *Observation) {
				input.WorkerPoolVersion = fixture.pool.Version + 1
			},
			code: "warm_capacity_pool_version_stale",
		},
	}

	for _, tc := range cases {
		t.Run(tc.name, func(t *testing.T) {
			input := base
			tc.mut(&input)
			_, err := fixture.service.Observe(context.Background(), input)
			assertProblemCode(t, err, tc.code)
		})
	}
}

func TestGetFreshReturnsNilForExpiredMissingOrReleaseMismatch(t *testing.T) {
	fixture := newWarmCapacityFixture(t)
	channel := releaseChannelPromoted
	if _, err := fixture.service.Observe(context.Background(), Observation{
		TenantID:                fixture.tenantID,
		ExecutionTargetID:       fixture.target.ID,
		WorkerPoolID:            fixture.pool.ID,
		WorkerPoolVersion:       fixture.pool.Version,
		WarmSupported:           true,
		WorkerReleaseRevisionID: &fixture.release.ID,
		WorkerReleaseChannel:    &channel,
		DesiredTotalUnits:       2,
		ClaimedUnits:            1,
		ReadyIdleUnits:          1,
		Source:                  "reconciler/test",
		ObservedAt:              fixture.now,
		TTL:                     20 * time.Second,
	}); err != nil {
		t.Fatal(err)
	}

	missing, err := fixture.service.GetFresh(context.Background(), FreshLookup{
		TenantID:          fixture.tenantID,
		ExecutionTargetID: fixture.target.ID,
		WorkerPoolID:      uuid.New(),
		WorkerPoolVersion: 1,
		Now:               fixture.now.Add(5 * time.Second),
	})
	if err != nil {
		t.Fatal(err)
	}
	if missing != nil {
		t.Fatalf("missing warm capacity = %#v", missing)
	}

	mismatchChannel := releaseChannelCanary
	mismatched, err := fixture.service.GetFresh(context.Background(), FreshLookup{
		TenantID:                fixture.tenantID,
		ExecutionTargetID:       fixture.target.ID,
		WorkerPoolID:            fixture.pool.ID,
		WorkerPoolVersion:       fixture.pool.Version,
		WorkerReleaseRevisionID: &fixture.release.ID,
		WorkerReleaseChannel:    &mismatchChannel,
		Now:                     fixture.now.Add(5 * time.Second),
	})
	if err != nil {
		t.Fatal(err)
	}
	if mismatched != nil {
		t.Fatalf("mismatched warm capacity = %#v", mismatched)
	}

	expired, err := fixture.service.GetFresh(context.Background(), FreshLookup{
		TenantID:          fixture.tenantID,
		ExecutionTargetID: fixture.target.ID,
		WorkerPoolID:      fixture.pool.ID,
		WorkerPoolVersion: fixture.pool.Version,
		Now:               fixture.now.Add(25 * time.Second),
	})
	if err != nil {
		t.Fatal(err)
	}
	if expired != nil {
		t.Fatalf("expired warm capacity = %#v", expired)
	}
}

type warmCapacityFixture struct {
	db       *gorm.DB
	service  *Service
	now      time.Time
	tenantID uuid.UUID
	target   persistence.ExecutionTarget
	pool     persistence.WorkerPool
	release  persistence.WorkerReleaseRevision
}

func newWarmCapacityFixture(t *testing.T) warmCapacityFixture {
	t.Helper()
	db, err := gorm.Open(sqlite.Open("file:"+uuid.NewString()+"?mode=memory&cache=shared"), &gorm.Config{
		TranslateError: true,
		Logger:         logger.Default.LogMode(logger.Silent),
	})
	if err != nil {
		t.Fatal(err)
	}
	if err := db.Exec("PRAGMA foreign_keys = ON").Error; err != nil {
		t.Fatal(err)
	}
	if err := db.AutoMigrate(persistence.AllModels()...); err != nil {
		t.Fatal(err)
	}

	now := time.Now().UTC().Truncate(time.Second)
	userID := uuid.New()
	tenantID := uuid.New()
	targetID := uuid.New()
	manifestID := uuid.New()
	releaseID := uuid.New()
	if err := db.Create(&persistence.User{
		ID: userID, Email: uuid.NewString() + "@example.com", DisplayName: "Warm Capacity",
		Status: "active", CreatedAt: now, UpdatedAt: now,
	}).Error; err != nil {
		t.Fatal(err)
	}
	if err := db.Create(&persistence.Tenant{
		ID: tenantID, Slug: "warm-" + uuid.NewString()[:8], Name: "Warm Capacity Tenant",
		Status: "active", PlanCode: "enterprise", Region: "default", Settings: map[string]any{},
		CreatedBy: userID, CreatedAt: now, UpdatedAt: now,
	}).Error; err != nil {
		t.Fatal(err)
	}
	target := persistence.ExecutionTarget{
		ID: targetID, TenantID: &tenantID, Kind: targetKindKubernetes, Name: "tenant-k8s", Status: targetStatusActive,
		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
	}
	if err := db.Create(&target).Error; err != nil {
		t.Fatal(err)
	}
	pool := persistence.WorkerPool{
		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
	}
	if err := db.Create(&pool).Error; err != nil {
		t.Fatal(err)
	}
	manifest := persistence.WorkerManifest{
		ID:                    manifestID,
		ManifestHash:          uuid.NewString(),
		WorkerBuildVersion:    "1.0.0",
		WorkerProtocolMinimum: 2,
		WorkerProtocolMaximum: 2,
		RuntimeEventMinimum:   2,
		RuntimeEventMaximum:   2,
		OperatingSystem:       "linux",
		Architecture:          "amd64",
		FeatureFlags:          map[string]any{},
		CreatedAt:             now,
	}
	if err := db.Create(&manifest).Error; err != nil {
		t.Fatal(err)
	}
	release := persistence.WorkerReleaseRevision{
		ID: releaseID, TenantID: tenantID, ExecutionTargetID: targetID,
		Revision: 1, WorkerManifestID: manifestID, Description: "warm release",
		CreatedBy: userID, CreatedAt: now,
	}
	if err := db.Create(&release).Error; err != nil {
		t.Fatal(err)
	}

	service := NewService(db)
	service.now = func() time.Time { return now }
	return warmCapacityFixture{
		db:       db,
		service:  service,
		now:      now,
		tenantID: tenantID,
		target:   target,
		pool:     pool,
		release:  release,
	}
}

func assertProblemCode(t *testing.T, err error, expected string) {
	t.Helper()
	var problemErr *problem.Error
	if err == nil || !errors.As(err, &problemErr) || problemErr.Code != expected {
		t.Fatalf("problem code = %v, want %q (error: %v)", problemErr, expected, err)
	}
}

func stringPointer(value string) *string { return &value }

exec
/bin/zsh -lc "rg -n 'ManagedKubernetesWarmCapacityObservation|DesiredIdleUnits|MinIdleUnits|DesiredTotalUnits' services/control-plane/internal/executiontargets --glob '*.go'" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go:15:type ManagedKubernetesWarmCapacityObservation struct {
services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go:23:	DesiredTotalUnits       int
services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go:30:type ManagedKubernetesWarmCapacityObserver func(context.Context, ManagedKubernetesWarmCapacityObservation) error
services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go:62:	observation ManagedKubernetesWarmCapacityObservation,
services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go:76:		DesiredTotalUnits:       observation.DesiredTotalUnits,
services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go:31:	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go:34:		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go:49:	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go:82:	if capacity.Version != 1 || capacity.DesiredTotalUnits != 1 || capacity.ReadyIdleUnits != 0 {
services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go:105:	if capacity.Version != 3 || capacity.DesiredTotalUnits != 1 || capacity.ReadyIdleUnits != 0 {
services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go:179:	if capacity.WarmSupported || capacity.DesiredTotalUnits != 0 || capacity.ReadyIdleUnits != 0 || capacity.ClaimedUnits != 1 {
services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go:203:	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
services/control-plane/internal/executiontargets/kubernetes_warm_pool.go:25:	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
services/control-plane/internal/executiontargets/kubernetes_warm_pool.go:94:) []ManagedKubernetesWarmCapacityObservation {
services/control-plane/internal/executiontargets/kubernetes_warm_pool.go:95:	observations := make([]ManagedKubernetesWarmCapacityObservation, 0, len(warmPools))
services/control-plane/internal/executiontargets/kubernetes_warm_pool.go:104:		observation := ManagedKubernetesWarmCapacityObservation{
services/control-plane/internal/executiontargets/kubernetes_warm_pool.go:121:		observation.DesiredTotalUnits = pool.DesiredIdleUnits + claimed
services/control-plane/internal/executiontargets/kubernetes_warm_pool.go:122:		if observation.DesiredTotalUnits > pool.MaxActiveUnits {
services/control-plane/internal/executiontargets/kubernetes_warm_pool.go:123:			observation.DesiredTotalUnits = pool.MaxActiveUnits
services/control-plane/internal/executiontargets/kubernetes_warm_pool.go:337:		desiredTotal := pool.DesiredIdleUnits + claimed
services/control-plane/internal/executiontargets/kubernetes_reconciler.go:308:	var warmCapacityObservations []ManagedKubernetesWarmCapacityObservation
services/control-plane/internal/executiontargets/kubernetes_reconciler.go:1220:		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
services/control-plane/internal/executiontargets/kubernetes_reconciler.go:1245:			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:955:	var observations []ManagedKubernetesWarmCapacityObservation
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:956:	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1620:		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2174:		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,

exec
/bin/zsh -lc "sed -n '1,250p' services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
package executiontargets

import (
	"context"
	"errors"
	"strings"
	"testing"
	"time"

	"github.com/google/uuid"

	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
	"github.com/synara-ai/synara/services/control-plane/internal/placement"
)

func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
	fixture := newKubernetesReconcileFixture(t, "")
	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
		Where("id = ?", fixture.targetID).
		Update("status", "active").Error; err != nil {
		t.Fatal(err)
	}
	observedAt := time.Date(2026, time.July, 25, 15, 0, 0, 0, time.UTC)
	publisher := NewManagedKubernetesWarmCapacityPublisher(
		fixture.reconciler.targets,
		ManagedKubernetesWarmCapacityPublisherConfig{ObservationTTL: 10 * time.Second},
	)
	publisher.now = func() time.Time { return observedAt }

	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
	}); err != nil {
		t.Fatal(err)
	}

	var capacity persistence.WorkerPoolWarmCapacity
	if err := fixture.db.Where("worker_pool_id = ? AND worker_pool_version = ?", pool.ID, pool.Version).
		Take(&capacity).Error; err != nil {
		t.Fatal(err)
	}
	if capacity.Source != managedKubernetesWarmCapacityPublisherPrefix+"default" ||
		!capacity.ObservedAt.Equal(observedAt) ||
		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
	}
	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
		t.Fatalf("warm capacity publisher counters = %#v", capacity)
	}
}

func TestKubernetesReconcilerPublishesPostQueueReadyWarmCapacityOnlyAfterSuccess(t *testing.T) {
	fixture := newKubernetesReconcileFixture(t, "")
	configuration := kubernetesTestConfiguration("")
	configuration["maxActivePods"] = 2
	fixture.updateConfiguration(t, configuration)
	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
	if err := fixture.db.Model(&persistence.AgentExecution{}).
		Where("execution_target_id = ?", fixture.targetID).
		Update("status", "completed").Error; err != nil {
		t.Fatal(err)
	}
	client := newFakeKubernetesClient()
	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
	currentNow := time.Now().UTC().Truncate(time.Second)
	fixture.reconciler.now = func() time.Time { return currentNow }
	publisher := NewManagedKubernetesWarmCapacityPublisher(
		fixture.reconciler.targets,
		ManagedKubernetesWarmCapacityPublisherConfig{
			PublisherIdentity: "managed-kubernetes-warm-capacity-test",
			ObservationTTL:    10 * time.Second,
		},
	)
	fixture.reconciler.config.PublishWarmCapacity = publisher.PublishReconcile

	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	capacity := loadWorkerPoolWarmCapacity(t, fixture, pool)
	if capacity.Version != 1 || capacity.DesiredTotalUnits != 1 || capacity.ReadyIdleUnits != 0 {
		t.Fatalf("initial warm capacity = %#v", capacity)
	}
	warmPod := onlyFakePod(t, client)
	warmPod.Phase = "Running"
	client.pods[warmPod.Name] = warmPod
	fixture.registerWarmWorker(t, pool, warmPod, "online", "active", nil, nil)

	currentNow = currentNow.Add(time.Second)
	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	capacity = loadWorkerPoolWarmCapacity(t, fixture, pool)
	if capacity.Version != 2 || capacity.ReadyIdleUnits != 1 {
		t.Fatalf("ready warm capacity = %#v", capacity)
	}

	fixture.assignExecutionToPool(t, fixture.executionIDs[0], pool, "queued")
	currentNow = currentNow.Add(time.Second)
	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	capacity = loadWorkerPoolWarmCapacity(t, fixture, pool)
	if capacity.Version != 3 || capacity.DesiredTotalUnits != 1 || capacity.ReadyIdleUnits != 0 {
		t.Fatalf("post-queue warm capacity = %#v", capacity)
	}
	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
		t.Fatalf("queued execution received a cold Pod despite reserved ready warm capacity: %#v", client.pods)
	}

	lastObservedAt := capacity.ObservedAt
	lastExpiresAt := capacity.ExpiresAt
	currentNow = currentNow.Add(30 * time.Second)
	fixture.reconciler.factory = failingManagedKubernetesFactory{err: errors.New("API transport unavailable")}
	if err := fixture.reconciler.ReconcileOnce(context.Background()); err == nil {
		t.Fatal("expected failed reconcile")
	}
	capacity = loadWorkerPoolWarmCapacity(t, fixture, pool)
	if capacity.Version != 3 || !capacity.ObservedAt.Equal(lastObservedAt) || !capacity.ExpiresAt.Equal(lastExpiresAt) {
		t.Fatalf("failed reconcile refreshed warm capacity: %#v", capacity)
	}
	if capacity.ExpiresAt.After(currentNow) {
		t.Fatalf("old warm capacity did not expire after failed reconcile: %#v", capacity)
	}
}

func TestKubernetesReconcilerPublishesUnsupportedCanaryCapacityWithRetainedClaim(t *testing.T) {
	fixture := newKubernetesReconcileFixture(t, "")
	configuration := kubernetesTestConfiguration("")
	configuration["image"] = "ghcr.io/synara/worker:mutable"
	configuration["maxActivePods"] = 2
	fixture.updateConfiguration(t, configuration)
	promotedDigest := "sha256:" + strings.Repeat("d", 64)
	canaryDigest := "sha256:" + strings.Repeat("e", 64)
	promotedRevision := fixture.seedReleaseRevision(t, 1, promotedDigest)
	canaryRevision := fixture.seedReleaseRevision(t, 2, canaryDigest)
	fixture.seedReleasePolicy(t, promotedRevision, nil, 0)
	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
	if err := fixture.db.Model(&persistence.AgentExecution{}).
		Where("execution_target_id = ?", fixture.targetID).
		Update("status", "completed").Error; err != nil {
		t.Fatal(err)
	}
	client := newFakeKubernetesClient()
	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
	currentNow := time.Now().UTC().Truncate(time.Second)
	fixture.reconciler.now = func() time.Time { return currentNow }
	publisher := NewManagedKubernetesWarmCapacityPublisher(
		fixture.reconciler.targets,
		ManagedKubernetesWarmCapacityPublisherConfig{
			PublisherIdentity: "managed-kubernetes-warm-capacity-test",
			ObservationTTL:    time.Minute,
		},
	)
	fixture.reconciler.config.PublishWarmCapacity = publisher.PublishReconcile

	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	warmPod := onlyFakePod(t, client)
	warmPod.Phase = "Running"
	client.pods[warmPod.Name] = warmPod
	promotedChannel := "promoted"
	worker := fixture.registerWarmWorker(t, pool, warmPod, "online", "active", &promotedRevision, &promotedChannel)
	if err := fixture.db.Model(&persistence.AgentExecution{}).
		Where("id = ?", fixture.executionIDs[0]).
		Updates(map[string]any{"worker_id": worker.ID, "generation": 1, "status": "leased"}).Error; err != nil {
		t.Fatal(err)
	}
	fixture.createWarmWorkerLease(t, worker, fixture.executionIDs[0])
	fixture.seedReleasePolicy(t, promotedRevision, &canaryRevision, 20)

	currentNow = currentNow.Add(time.Second)
	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	capacity := loadWorkerPoolWarmCapacity(t, fixture, pool)
	if capacity.WarmSupported || capacity.DesiredTotalUnits != 0 || capacity.ReadyIdleUnits != 0 || capacity.ClaimedUnits != 1 {
		t.Fatalf("unsupported canary warm capacity = %#v", capacity)
	}
	if capacity.WorkerReleaseRevisionID != nil || capacity.WorkerReleaseChannel != nil {
		t.Fatalf("unsupported canary warm capacity retained a release pair: %#v", capacity)
	}
	if capacity.Reason == nil || *capacity.Reason != "Managed Kubernetes warm capacity is unavailable while a canary Worker release is active." {
		t.Fatalf("unsupported canary reason = %#v", capacity.Reason)
	}
}

func TestKubernetesReconcilerAttemptsEveryActiveWarmCapacityPublication(t *testing.T) {
	fixture := newKubernetesReconcileFixture(t, "")
	if err := fixture.db.Model(&persistence.AgentExecution{}).
		Where("execution_target_id = ?", fixture.targetID).
		Update("status", "completed").Error; err != nil {
		t.Fatal(err)
	}
	first := fixture.createWarmPool(t, placement.CapacityClassStandard, 0, 1, placement.PoolStatusActive)
	second := fixture.createWarmPool(t, placement.CapacityClassInteractive, 0, 1, placement.PoolStatusActive)
	disabled := fixture.createWarmPool(t, placement.CapacityClassInteractive, 0, 1, placement.PoolStatusDisabled)
	fixture.reconciler.factory = &fakeKubernetesFactory{client: newFakeKubernetesClient()}
	publishFailure := errors.New("warm capacity sink unavailable")
	called := make(map[uuid.UUID]int)
	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
		called[observation.WorkerPoolID]++
		if observation.WorkerPoolID == first.ID {
			return publishFailure
		}
		return nil
	}

	err := fixture.reconciler.ReconcileOnce(context.Background())
	if !errors.Is(err, publishFailure) {
		t.Fatalf("reconcile publication error = %v", err)
	}
	if called[first.ID] != 1 || called[second.ID] != 1 || called[disabled.ID] != 0 || len(called) != 2 {
		t.Fatalf("warm capacity publication calls = %#v", called)
	}
}

func loadWorkerPoolWarmCapacity(
	t *testing.T,
	fixture kubernetesReconcileFixture,
	pool persistence.WorkerPool,
) persistence.WorkerPoolWarmCapacity {
	t.Helper()
	var capacity persistence.WorkerPoolWarmCapacity
	if err := fixture.db.Where("worker_pool_id = ? AND worker_pool_version = ?", pool.ID, pool.Version).
		Take(&capacity).Error; err != nil {
		t.Fatal(err)
	}
	return capacity
}

exec
/bin/zsh -lc "sed -n '1180,1270p' services/control-plane/internal/executiontargets/kubernetes_reconciler.go
sed -n '280,340p' services/control-plane/internal/executiontargets/kubernetes_reconciler.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
	queuedByID := make(map[uuid.UUID]kubernetesExecution, len(items))
	activeItems := make([]kubernetesExecution, 0, len(items))
	for _, item := range items {
		if fairqueue.IsQueuedStatus(item.Status) {
			queuedCandidates = append(queuedCandidates, fairqueue.Candidate{
				TenantID: item.TenantID,
				ID:       item.ID,
				QueuedAt: item.QueuedAt,
			})
			queuedByID[item.ID] = item
		} else if fairqueue.IsActiveServiceStatus(item.Status) {
			activeServiceUnits[item.TenantID]++
			activeItems = append(activeItems, item)
		} else {
			return nil, fairqueue.ErrInvalidCandidate
		}
	}
	orderedCandidates, err := fairqueue.Order(queuedCandidates, activeServiceUnits)
	if err != nil {
		return nil, err
	}
	ordered := make([]kubernetesExecution, 0, len(items))
	ordered = append(ordered, activeItems...)
	for _, candidate := range orderedCandidates {
		item, ok := queuedByID[candidate.ID]
		if !ok {
			return nil, fairqueue.ErrInvalidCandidate
		}
		ordered = append(ordered, item)
	}
	return ordered, nil
}

func (r *KubernetesReconciler) loadKubernetesWarmPools(ctx context.Context, targetID uuid.UUID) ([]kubernetesWarmPool, error) {
	type warmPoolRow struct {
		ID                 uuid.UUID `gorm:"column:id"`
		Version            int64     `gorm:"column:version"`
		CapacityClass      string    `gorm:"column:capacity_class"`
		ClusterID          string    `gorm:"column:cluster_id"`
		Namespace          string    `gorm:"column:namespace"`
		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
		MaxActiveUnits     int       `gorm:"column:max_active_units"`
		SchedulingTemplate string    `gorm:"column:scheduling_template"`
		Status             string    `gorm:"column:status"`
	}
	var rows []warmPoolRow
	err := r.targets.db.WithContext(ctx).Table("worker_pools").
		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
		Order("capacity_class, id").
		Scan(&rows).Error
	if err != nil {
		return nil, problem.Wrap(500, "kubernetes_worker_pools_load_failed", "Kubernetes warm Worker pools could not be loaded.", err)
	}
	items := make([]kubernetesWarmPool, 0, len(rows))
	for _, row := range rows {
		template := map[string]any{}
		if strings.TrimSpace(row.SchedulingTemplate) != "" {
			if err := json.Unmarshal([]byte(row.SchedulingTemplate), &template); err != nil {
				return nil, problem.Wrap(500, "kubernetes_worker_pools_load_failed", "Kubernetes warm Worker pool schedulingTemplate could not be decoded.", err)
			}
		}
		items = append(items, kubernetesWarmPool{
			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
			ClusterID: row.ClusterID, Namespace: row.Namespace,
			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
			SchedulingTemplate: template, Status: row.Status,
		})
	}
	return items, nil
}

func (r *KubernetesReconciler) loadKubernetesWarmWorkerStates(ctx context.Context, targetID uuid.UUID) ([]kubernetesWarmWorkerState, error) {
	var items []kubernetesWarmWorkerState
	err := r.targets.db.WithContext(ctx).Table("worker_instances AS worker").
		Select(`worker.id,
			worker.incarnation,
			worker.pod_name,
			worker.instance_uid,
			worker.worker_pool_id,
			worker.worker_pool_version,
			worker.capacity_class,
			worker.worker_release_revision_id,
			worker.worker_release_channel,
			worker.worker_release_status,
			worker.registration_trust_mode,
			worker.protocol_version,
			worker.current_manifest_id,
			worker.compatibility_status,
			worker.lease_supported,
			worker.fencing_supported,
}

type kubernetesExecutionPoolSnapshot struct {
	ID                 uuid.UUID
	Version            int64
	CapacityClass      string
	Mode               string
	ClusterID          string
	Namespace          string
	SchedulingTemplate map[string]any
}

type kubernetesWorkerPodState struct {
	PodName     string `gorm:"column:pod_name"`
	InstanceUID string `gorm:"column:instance_uid"`
}

func (r *KubernetesReconciler) reconcileTarget(ctx context.Context, target persistence.ExecutionTarget) (err error) {
	healthObservation := ManagedKubernetesRoutingHealthObservation{
		ExecutionTargetID: target.ID,
		TenantOwned:       target.TenantID != nil,
		Status:            routing.HealthUnknown,
		CapacityStatus:    routing.CapacityUnknown,
		ReservationAuthority: &routing.ReservationAuthorityObservation{
			Mode: routing.ReservationAuthorityExactActiveV1,
		},
	}
	acknowledgedReservations := make(map[routing.ReservationIdentity]struct{})
	var warmCapacityObservations []ManagedKubernetesWarmCapacityObservation
	defer func() {
		if ctx.Err() != nil || target.TenantID == nil {
			return
		}
		reconcileSucceeded := err == nil
		observedAt := r.now()
		if r.config.PublishRoutingHealth != nil {
			healthObservation.ObservedAt = observedAt
			if publishErr := r.config.PublishRoutingHealth(ctx, healthObservation); publishErr != nil {
				err = errors.Join(err, publishErr)
			}
		}
		if reconcileSucceeded && r.config.PublishWarmCapacity != nil {
			for _, observation := range warmCapacityObservations {
				observation.ObservedAt = observedAt
				if publishErr := r.config.PublishWarmCapacity(ctx, observation); publishErr != nil {
					err = errors.Join(err, publishErr)
				}
			}
		}
	}()
	configuration, err := r.loadConfiguration(target)
	if err != nil {
		healthObservation.Reason = managedKubernetesRoutingReasonPointer(
			"Managed Kubernetes target configuration is unavailable.",
		)
		r.setKubernetesStatus(ctx, target, "offline", false, false, 0, 0)
		return err
	}
	client, err := r.factory.Open(configuration)
	if err != nil {
		healthObservation.Status = routing.HealthUnreachable

exec
/bin/zsh -lc "sed -n '900,1040p' services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
sed -n '1580,1665p' services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
sed -n '2120,2205p' services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
	fixture.updateConfiguration(t, configuration)
	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
	if err := fixture.db.Model(&persistence.AgentExecution{}).
		Where("execution_target_id = ?", fixture.targetID).
		Update("status", "completed").Error; err != nil {
		t.Fatal(err)
	}
	client := newFakeKubernetesClient()
	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}

	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	warmPod := onlyFakePod(t, client)
	warmPod.Phase = "Running"
	client.pods[warmPod.Name] = warmPod
	fixture.registerWarmWorker(t, pool, warmPod, "offline", "active", nil, nil)
	fixture.assignExecutionToPool(t, fixture.executionIDs[0], pool, "queued")

	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
		t.Fatalf("queued execution did not fall back to a cold Pod when the warm Worker was offline: %#v", client.pods)
	}
	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPod.Name {
		t.Fatalf("offline warm Pod was not recycled exactly once: deleted=%#v active=%#v", client.deletedPods, client.pods)
	}
}

func TestKubernetesReconcilerRetainsIncompatibleWarmWorkerWithoutReportingReadyCapacity(t *testing.T) {
	fixture := newKubernetesReconcileFixture(t, "")
	configuration := kubernetesTestConfiguration("")
	configuration["maxActivePods"] = 2
	fixture.updateConfiguration(t, configuration)
	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
	if err := fixture.db.Model(&persistence.AgentExecution{}).
		Where("execution_target_id = ?", fixture.targetID).
		Update("status", "completed").Error; err != nil {
		t.Fatal(err)
	}
	client := newFakeKubernetesClient()
	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	warmPod := onlyFakePod(t, client)
	warmPod.Phase = "Running"
	client.pods[warmPod.Name] = warmPod
	worker := fixture.registerWarmWorker(t, pool, warmPod, "online", "active", nil, nil)
	if err := fixture.db.Model(&persistence.WorkerInstance{}).Where("id = ?", worker.ID).
		Update("compatibility_status", "incompatible").Error; err != nil {
		t.Fatal(err)
	}
	fixture.assignExecutionToPool(t, fixture.executionIDs[0], pool, "queued")
	var observations []ManagedKubernetesWarmCapacityObservation
	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
		observations = append(observations, observation)
		return nil
	}

	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	if _, found := client.pods[warmPod.Name]; !found {
		t.Fatalf("incompatible warm Pod was deleted: deleted=%#v active=%#v", client.deletedPods, client.pods)
	}
	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
		t.Fatalf("queued execution did not receive a cold fallback: %#v", client.pods)
	}
	if len(observations) != 1 || observations[0].ReadyIdleUnits != 0 {
		t.Fatalf("incompatible warm capacity observations = %#v, want readyIdleUnits=0", observations)
	}
	createdPods := client.kindCount("Pod")
	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	if _, found := client.pods[warmPod.Name]; !found || len(client.deletedPods) != 0 || client.kindCount("Pod") != createdPods {
		t.Fatalf("incompatible warm Pod churned on a later pass: deleted=%#v active=%#v created=%d->%d", client.deletedPods, client.pods, createdPods, client.kindCount("Pod"))
	}
}

func TestKubernetesReconcilerEvictsNotReadyWarmWorkerForColdDemandAtTargetCap(t *testing.T) {
	tests := []struct {
		name       string
		assignWarm bool
	}{
		{name: "matching warm pool demand", assignWarm: true},
		{name: "general cold demand"},
	}
	for _, test := range tests {
		t.Run(test.name, func(t *testing.T) {
			fixture := newKubernetesReconcileFixture(t, "")
			pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
			if err := fixture.db.Model(&persistence.AgentExecution{}).
				Where("execution_target_id = ?", fixture.targetID).
				Update("status", "completed").Error; err != nil {
				t.Fatal(err)
			}
			client := newFakeKubernetesClient()
			fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
			if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
				t.Fatal(err)
			}
			warmPod := onlyFakePod(t, client)
			warmPod.Phase = "Running"
			client.pods[warmPod.Name] = warmPod
			worker := fixture.registerWarmWorker(t, pool, warmPod, "online", "active", nil, nil)
			if err := fixture.db.Model(&persistence.WorkerInstance{}).Where("id = ?", worker.ID).
				Update("compatibility_status", "incompatible").Error; err != nil {
				t.Fatal(err)
			}
			if test.assignWarm {
				fixture.assignExecutionToPool(t, fixture.executionIDs[0], pool, "queued")
			} else if err := fixture.db.Model(&persistence.AgentExecution{}).
				Where("id = ?", fixture.executionIDs[0]).Update("status", "queued").Error; err != nil {
				t.Fatal(err)
			}

			if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
				t.Fatal(err)
			}
			if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPod.Name || len(client.pods) != 0 {
				t.Fatalf("first pass did not evict only the not-ready warm Pod: deleted=%#v active=%#v", client.deletedPods, client.pods)
			}
			if client.kindCount("Pod") != 1 {
				t.Fatalf("first pass recreated a Pod before deletion released quota: created=%d", client.kindCount("Pod"))
			}

			if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
				t.Fatal(err)
			}
			executionPod, found := findExecutionPod(client, fixture.executionIDs[0])
			if !found || executionPod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeExecutionPinned {
				t.Fatalf("second pass did not create the cold execution Pod: %#v", client.pods)
			}
			createdPods := client.kindCount("Pod")
			if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
				t.Fatal(err)
			}
			if len(client.deletedPods) != 1 || client.kindCount("Pod") != createdPods || len(client.pods) != 1 {
	}
}

func TestKubernetesReconcilerConfirmsMissingRegisteredPodAfterSuccessfulList(t *testing.T) {
	fixture := newKubernetesReconcileFixture(t, "")
	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
	if err := fixture.db.Model(&persistence.AgentExecution{}).
		Where("execution_target_id = ?", fixture.targetID).
		Update("status", "completed").Error; err != nil {
		t.Fatal(err)
	}
	client := newFakeKubernetesClient()
	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	warmPod := onlyFakePod(t, client)
	warmPod.Phase = "Running"
	client.pods[warmPod.Name] = warmPod
	fixture.registerWarmWorker(t, pool, warmPod, "draining", "active", nil, nil)
	delete(client.pods, warmPod.Name)

	observations := make([]KubernetesWorkerPodObservation, 0)
	fixture.reconciler.config.ObserveWorkerPod = func(_ context.Context, observation KubernetesWorkerPodObservation) error {
		observations = append(observations, observation)
		return nil
	}
	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	if len(observations) != 1 || observations[0].PodName != warmPod.Name ||
		observations[0].PodUID != warmPod.UID || observations[0].Phase != "Missing" ||
		observations[0].Reason != "confirmed-missing:reconcile-list" {
		t.Fatalf("confirmed missing observations = %#v", observations)
	}
}

func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
	pool := kubernetesWarmPool{
		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
		Status: placement.PoolStatusActive,
	}
	plans, _, err := kubernetesWarmPodPlans(
		[]kubernetesWarmPool{pool},
		true,
		map[uuid.UUID]int{pool.ID: 1},
		kubernetesWarmReleaseSelection{},
		strings.Repeat("a", 64),
		"ghcr.io/synara/worker:latest",
	)
	if err != nil {
		t.Fatal(err)
	}
	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
	}
}

func TestKubernetesReconcilerAppliesSelectedPoolSchedulingTemplateToColdFallback(t *testing.T) {
	fixture := newKubernetesReconcileFixture(t, "")
	configuration := kubernetesTestConfiguration("")
	configuration["maxActivePods"] = 1
	fixture.updateConfiguration(t, configuration)
	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 0, 1, placement.PoolStatusActive)
	pool.ClusterID = kubernetesLocalClusterID
	pool.Namespace = "synara-test"
	pool.SchedulingTemplate = map[string]any{
		"priorityClassName": "interactive-high",
		"nodeSelector":      map[string]any{"pool": "warm"},
		"tolerations":       []any{map[string]any{"key": "warm", "operator": "Exists"}},
	}
	nextPoolVersion := pool.Version + 1
	if err := fixture.db.Model(&persistence.WorkerPool{}).Where("id = ? AND version = ?", pool.ID, pool.Version).Updates(&persistence.WorkerPool{
		ClusterID:          pool.ClusterID,
		Namespace:          pool.Namespace,
		SchedulingTemplate: pool.SchedulingTemplate,
		Version:            nextPoolVersion,
		UpdatedAt:          time.Now().UTC(),
	}).Error; err != nil {
		t.Fatal(err)
	}
	pool.Version = nextPoolVersion
	fixture.assignExecutionToPool(t, fixture.executionIDs[0], pool, "queued")
	if err := fixture.db.Model(&persistence.AgentExecution{}).Where("id = ?", fixture.executionIDs[1]).
		Update("status", "completed").Error; err != nil {
		t.Fatal(err)
	}
	return revisionID
}

func (f kubernetesReconcileFixture) seedReleasePolicy(
	t *testing.T,
	promotedRevision uuid.UUID,
	canaryRevision *uuid.UUID,
	canaryPercent int,
) {
	t.Helper()
	var existing persistence.WorkerReleasePolicy
	err := f.db.Where("execution_target_id = ?", f.targetID).Take(&existing).Error
	if err == nil {
		updated := map[string]any{
			"policy_version":       existing.PolicyVersion + 1,
			"promoted_revision_id": promotedRevision,
			"canary_revision_id":   canaryRevision,
			"canary_percent":       canaryPercent,
			"updated_by":           f.userID,
			"updated_at":           time.Now().UTC(),
		}
		if err := f.db.Model(&persistence.WorkerReleasePolicy{}).
			Where("execution_target_id = ?", f.targetID).
			Updates(updated).Error; err != nil {
			t.Fatal(err)
		}
		return
	}
	if err != nil && !errors.Is(err, gorm.ErrRecordNotFound) {
		t.Fatal(err)
	}
	if err := f.db.Create(&persistence.WorkerReleasePolicy{
		TenantID: f.tenantID, ExecutionTargetID: f.targetID, PolicyVersion: 1,
		PromotedRevisionID: promotedRevision, CanaryRevisionID: canaryRevision, CanaryPercent: canaryPercent,
		UpdatedBy: f.userID, UpdatedAt: time.Now().UTC(),
	}).Error; err != nil {
		t.Fatal(err)
	}
}

func (f kubernetesReconcileFixture) createWarmPool(
	t *testing.T,
	capacityClass string,
	desiredIdleUnits, maxActiveUnits int,
	status string,
) persistence.WorkerPool {
	t.Helper()
	now := time.Now().UTC()
	pool := persistence.WorkerPool{
		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
		CreatedAt: now, UpdatedAt: now,
	}
	if err := f.db.Create(&pool).Error; err != nil {
		t.Fatal(err)
	}
	return pool
}

func (f kubernetesReconcileFixture) assignExecutionToPool(
	t *testing.T,
	executionID uuid.UUID,
	pool persistence.WorkerPool,
	status string,
) {
	t.Helper()
	updates := map[string]any{
		"worker_pool_id":      pool.ID,
		"worker_pool_version": pool.Version,
		"capacity_class":      pool.CapacityClass,
	}
	if status != "" {
		updates["status"] = status
	}
	if err := f.db.Model(&persistence.AgentExecution{}).Where("id = ?", executionID).Updates(updates).Error; err != nil {
		t.Fatal(err)
	}
}

func (f kubernetesReconcileFixture) registerWarmWorker(
	t *testing.T,

exec
/bin/zsh -lc "sed -n '760,940p' services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
sed -n '1040,1220p' services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
	configuration["maxActivePods"] = 2
	fixture.updateConfiguration(t, configuration)
	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
	if err := fixture.db.Model(&persistence.AgentExecution{}).
		Where("execution_target_id = ?", fixture.targetID).
		Update("status", "completed").Error; err != nil {
		t.Fatal(err)
	}
	client := newFakeKubernetesClient()
	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}

	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	warmPod := onlyFakePod(t, client)
	warmPod.Phase = "Running"
	client.pods[warmPod.Name] = warmPod
	fixture.registerWarmWorker(t, pool, warmPod, "online", "active", nil, nil)
	for _, executionID := range fixture.executionIDs {
		fixture.assignExecutionToPool(t, executionID, pool, "queued")
	}

	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
		t.Fatalf("first execution unexpectedly received a cold Pod despite ready warm capacity: %#v", client.pods)
	}
	executionPod, found := findExecutionPod(client, fixture.executionIDs[1])
	if !found {
		t.Fatalf("second execution did not fall back to a cold Pod: %#v", client.pods)
	}
	if executionPod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeExecutionPinned {
		t.Fatalf("fallback Pod worker mode = %#v", executionPod.Labels)
	}
	if len(client.deletedPods) != 0 {
		t.Fatalf("ready warm capacity unexpectedly deleted a Pod: %#v", client.deletedPods)
	}
}

func TestKubernetesReconcilerIgnoresTerminatedWarmWorkerWithReusedPodName(t *testing.T) {
	fixture := newKubernetesReconcileFixture(t, "")
	configuration := kubernetesTestConfiguration("")
	configuration["maxActivePods"] = 2
	fixture.updateConfiguration(t, configuration)
	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
	if err := fixture.db.Model(&persistence.AgentExecution{}).
		Where("execution_target_id = ?", fixture.targetID).
		Update("status", "completed").Error; err != nil {
		t.Fatal(err)
	}
	client := newFakeKubernetesClient()
	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	warmPod := onlyFakePod(t, client)
	warmPod.Phase = "Running"
	client.pods[warmPod.Name] = warmPod
	current := fixture.registerWarmWorker(t, pool, warmPod, "online", "active", nil, nil)
	terminatedAt := time.Now().UTC()
	old := current
	old.ID = uuid.New()
	old.InstanceUID = uuid.NewString()
	old.AuthTokenHash = []byte("terminated-warm-worker-hash")
	old.Status = "terminated"
	old.TerminatedAt = &terminatedAt
	if err := fixture.db.Create(&old).Error; err != nil {
		t.Fatal(err)
	}

	states, err := fixture.reconciler.loadKubernetesWarmWorkerStates(context.Background(), fixture.targetID)
	if err != nil {
		t.Fatal(err)
	}
	if len(states) != 1 || states[0].WorkerID != current.ID || states[0].InstanceUID != warmPod.UID {
		t.Fatalf("loaded warm Worker states = %#v, want only the current Pod UID", states)
	}
	for _, executionID := range fixture.executionIDs {
		fixture.assignExecutionToPool(t, executionID, pool, "queued")
	}
	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
		t.Fatalf("current warm Worker was hidden by a terminated same-name identity: %#v", client.pods)
	}
	if _, found := findExecutionPod(client, fixture.executionIDs[1]); !found {
		t.Fatalf("second execution did not receive the expected cold fallback: %#v", client.pods)
	}
}

func TestKubernetesWarmWorkerStateLookupUsesPodNameAndUID(t *testing.T) {
	podName := "reused-warm-pod"
	oldUID := uuid.NewString()
	currentUID := uuid.NewString()
	old := kubernetesWarmWorkerState{WorkerID: uuid.New(), PodName: podName, InstanceUID: oldUID}
	current := kubernetesWarmWorkerState{WorkerID: uuid.New(), PodName: podName, InstanceUID: currentUID}
	states := map[kubernetesWarmWorkerIdentity]kubernetesWarmWorkerState{
		kubernetesWarmWorkerIdentityKey(old.PodName, old.InstanceUID):         old,
		kubernetesWarmWorkerIdentityKey(current.PodName, current.InstanceUID): current,
	}

	matched, found := kubernetesWarmWorkerStateForPod(states, kubernetesPod{Name: podName, UID: currentUID})
	if !found || matched.WorkerID != current.WorkerID {
		t.Fatalf("composite warm Worker lookup = (%#v, %t), want current UID", matched, found)
	}
}

func TestKubernetesReconcilerFallsBackToColdPodWhenWarmPodIsUnregistered(t *testing.T) {
	fixture := newKubernetesReconcileFixture(t, "")
	configuration := kubernetesTestConfiguration("")
	configuration["maxActivePods"] = 2
	fixture.updateConfiguration(t, configuration)
	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
	if err := fixture.db.Model(&persistence.AgentExecution{}).
		Where("execution_target_id = ?", fixture.targetID).
		Update("status", "completed").Error; err != nil {
		t.Fatal(err)
	}
	client := newFakeKubernetesClient()
	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}

	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	fixture.assignExecutionToPool(t, fixture.executionIDs[0], pool, "queued")

	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
		t.Fatalf("queued execution did not fall back to a cold Pod with only an unregistered warm Pod: %#v", client.pods)
	}
}

func TestKubernetesReconcilerFallsBackToColdPodWhenWarmWorkerIsOffline(t *testing.T) {
	fixture := newKubernetesReconcileFixture(t, "")
	configuration := kubernetesTestConfiguration("")
	configuration["maxActivePods"] = 2
	fixture.updateConfiguration(t, configuration)
	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
	if err := fixture.db.Model(&persistence.AgentExecution{}).
		Where("execution_target_id = ?", fixture.targetID).
		Update("status", "completed").Error; err != nil {
		t.Fatal(err)
	}
	client := newFakeKubernetesClient()
	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}

	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	warmPod := onlyFakePod(t, client)
	warmPod.Phase = "Running"
	client.pods[warmPod.Name] = warmPod
	fixture.registerWarmWorker(t, pool, warmPod, "offline", "active", nil, nil)
	fixture.assignExecutionToPool(t, fixture.executionIDs[0], pool, "queued")

	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
		t.Fatalf("queued execution did not fall back to a cold Pod when the warm Worker was offline: %#v", client.pods)
	}
	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPod.Name {
		t.Fatalf("offline warm Pod was not recycled exactly once: deleted=%#v active=%#v", client.deletedPods, client.pods)
	}
}

func TestKubernetesReconcilerRetainsIncompatibleWarmWorkerWithoutReportingReadyCapacity(t *testing.T) {
	fixture := newKubernetesReconcileFixture(t, "")
	configuration := kubernetesTestConfiguration("")
	configuration["maxActivePods"] = 2
	fixture.updateConfiguration(t, configuration)
	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
	if err := fixture.db.Model(&persistence.AgentExecution{}).
		Where("execution_target_id = ?", fixture.targetID).
		Update("status", "completed").Error; err != nil {
		t.Fatal(err)
	}
			if len(client.deletedPods) != 1 || client.kindCount("Pod") != createdPods || len(client.pods) != 1 {
				t.Fatalf("later pass rebuilt warm capacity or repeated deletion: deleted=%#v active=%#v created=%d->%d", client.deletedPods, client.pods, createdPods, client.kindCount("Pod"))
			}
		})
	}
}

func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
	fixture := newKubernetesReconcileFixture(t, "")
	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
	if err := fixture.db.Model(&persistence.AgentExecution{}).
		Where("execution_target_id = ?", fixture.targetID).
		Update("status", "completed").Error; err != nil {
		t.Fatal(err)
	}
	client := newFakeKubernetesClient()
	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	warmPod := onlyFakePod(t, client)
	warmPod.Phase = "Running"
	client.pods[warmPod.Name] = warmPod
	worker := fixture.registerWarmWorker(t, pool, warmPod, "online", "active", nil, nil)
	staleHeartbeat := time.Now().UTC().Add(-fixture.reconciler.config.WorkerHeartbeatTimeout - time.Second)
	if err := fixture.db.Model(&persistence.WorkerInstance{}).Where("id = ?", worker.ID).
		Update("last_heartbeat_at", staleHeartbeat).Error; err != nil {
		t.Fatal(err)
	}

	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPod.Name || len(client.pods) != 0 {
		t.Fatalf("stale warm Pod was not recycled exactly once: deleted=%#v active=%#v", client.deletedPods, client.pods)
	}
}

func TestKubernetesReconcilerTreatsExpiredWarmWorkerLeaseRowAsBusy(t *testing.T) {
	fixture := newKubernetesReconcileFixture(t, "")
	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
	if err := fixture.db.Model(&persistence.AgentExecution{}).
		Where("execution_target_id = ?", fixture.targetID).
		Update("status", "completed").Error; err != nil {
		t.Fatal(err)
	}
	client := newFakeKubernetesClient()
	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	warmPod := onlyFakePod(t, client)
	warmPod.Phase = "Running"
	client.pods[warmPod.Name] = warmPod
	worker := fixture.registerWarmWorker(t, pool, warmPod, "offline", "active", nil, nil)
	if err := fixture.db.Model(&persistence.AgentExecution{}).Where("id = ?", fixture.executionIDs[0]).
		Updates(map[string]any{
			"status": "leased", "worker_id": worker.ID, "generation": 1, "finished_at": nil,
		}).Error; err != nil {
		t.Fatal(err)
	}
	now := time.Now().UTC()
	if err := fixture.db.Create(&persistence.WorkerLease{
		ExecutionID: fixture.executionIDs[0], TenantID: fixture.tenantID,
		WorkerID: worker.ID, WorkerIncarnation: worker.Incarnation, WorkerInstanceUID: worker.InstanceUID,
		Generation: 1, LeaseTokenHash: []byte("expired-warm-worker-lease"),
		AcquiredAt: now.Add(-3 * time.Minute), HeartbeatAt: now.Add(-2 * time.Minute), ExpiresAt: now.Add(-time.Minute),
	}).Error; err != nil {
		t.Fatal(err)
	}

	states, err := fixture.reconciler.loadKubernetesWarmWorkerStates(context.Background(), fixture.targetID)
	if err != nil {
		t.Fatal(err)
	}
	if len(states) != 1 || !states[0].HasLease {
		t.Fatalf("expired lease row did not keep the warm Worker busy: %#v", states)
	}
	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	if len(client.deletedPods) != 0 {
		t.Fatalf("busy warm Worker with an expired lease row was deleted: %#v", client.deletedPods)
	}
	if _, found := client.pods[warmPod.Name]; !found {
		t.Fatalf("busy warm Pod was not retained: %#v", client.pods)
	}
}

func TestKubernetesWarmPodDeletionRechecksRegistrationAndLeaseAtDeleteBoundary(t *testing.T) {
	fixture := newKubernetesReconcileFixture(t, "")
	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
	if err := fixture.db.Model(&persistence.AgentExecution{}).
		Where("execution_target_id = ?", fixture.targetID).
		Update("status", "completed").Error; err != nil {
		t.Fatal(err)
	}
	client := newFakeKubernetesClient()
	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	warmPod := onlyFakePod(t, client)
	warmPod.Phase = "Running"
	client.pods[warmPod.Name] = warmPod
	states, err := fixture.reconciler.loadKubernetesWarmWorkerStates(context.Background(), fixture.targetID)
	if err != nil {
		t.Fatal(err)
	}
	if len(states) != 0 {
		t.Fatalf("pre-delete snapshot unexpectedly included a registered Worker: %#v", states)
	}

	worker := fixture.registerWarmWorker(t, pool, warmPod, "online", "active", nil, nil)
	if err := fixture.db.Model(&persistence.AgentExecution{}).Where("id = ?", fixture.executionIDs[0]).
		Updates(map[string]any{
			"status": "leased", "worker_id": worker.ID, "generation": 1, "finished_at": nil,
		}).Error; err != nil {
		t.Fatal(err)
	}
	fixture.createWarmWorkerLease(t, worker, fixture.executionIDs[0])

	deleted, retainedWorker, err := fixture.reconciler.deleteObservedPodSafely(
		context.Background(),
		client,
		fixture.targetID,
		"synara-test",
		warmPod,
		"registration-claim-race-test",
	)
	if err != nil {
		t.Fatal(err)
	}
	if deleted || retainedWorker == nil || retainedWorker.ID != worker.ID {
		t.Fatalf("delete boundary result = deleted=%t retained=%#v, want the newly registered leased Worker", deleted, retainedWorker)
	}
	if len(client.deletedPods) != 0 {
		t.Fatalf("leased Pod was deleted after a state=nil snapshot: %#v", client.deletedPods)
	}
	if _, found := client.pods[warmPod.Name]; !found {
		t.Fatalf("leased Pod disappeared after delete-boundary recheck: %#v", client.pods)
	}
	var stored persistence.WorkerInstance
	if err := fixture.db.Where("id = ?", worker.ID).Take(&stored).Error; err != nil {
		t.Fatal(err)
	}
	if stored.Status != "online" || stored.DrainingAt != nil {
		t.Fatalf("leased Worker was drained during delete-boundary recheck: %#v", stored)
	}
	var fenceCount int64
	if err := fixture.db.Model(&persistence.KubernetesPodDeletionFence{}).
		Where("execution_target_id = ? AND namespace = ? AND pod_name = ? AND pod_uid = ?", fixture.targetID, "synara-test", warmPod.Name, warmPod.UID).
		Count(&fenceCount).Error; err != nil {
		t.Fatal(err)
	}
	if fenceCount != 0 {
		t.Fatalf("leased Worker deletion wrote %d durable fences", fenceCount)
	}
}

func TestKubernetesPodDeletionRetainsWorkerWithActiveWorkspaceCleanupLease(t *testing.T) {
	fixture := newKubernetesReconcileFixture(t, "")
	client := newFakeKubernetesClient()
	pod := kubernetesPod{
		Name: "cleanup-busy-" + uuid.NewString(), UID: uuid.NewString(), Phase: "Running",
		Labels: map[string]string{kubernetesTargetLabel: fixture.targetID.String()},
	}
	client.pods[pod.Name] = pod
	now := time.Now().UTC().Truncate(time.Millisecond)
	worker := persistence.WorkerInstance{
		ID: uuid.New(), Incarnation: 1, InstanceUID: pod.UID,
		ExecutionTargetID: fixture.targetID, TargetKind: "kubernetes", WorkerMode: kubernetesWorkerModeGeneralPool,
		RegistrationTrustMode: kubernetesPodBoundRegistrationTrust,
		ClusterID:             kubernetesLocalClusterID,
		Namespace:             "synara-test",
		PodName:               pod.Name,
		Version:               "cleanup-busy-worker",
		ProtocolVersion:       kubernetesWorkerProtocolVersion,
		Capabilities:          map[string]any{},
		LeaseSupported:        true,
		FencingSupported:      true,

exec
/bin/zsh -lc "rg -n '"'^func TestKubernetesReconciler.*Warm|''^func TestKubernetesWarm|warm-pool-demand-fallback|deletedPods|kindCount'"' services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
103:		if client.kindCount(kind) == 0 {
114:	if client.kindCount("Pod") != 1 {
115:		t.Fatalf("maxActivePods=1 created %d Pods", client.kindCount("Pod"))
227:	if len(client.deletedPods) != 1 || client.kindCount("Pod") != 1 {
228:		t.Fatalf("Kubernetes scheduler created a replacement before the deleted Pod released quota: deleted=%#v pods=%d", client.deletedPods, client.kindCount("Pod"))
233:	if client.kindCount("Pod") != 2 {
234:		t.Fatalf("Kubernetes scheduler did not create the next Pod after deletion settled: pods=%d", client.kindCount("Pod"))
287:	if len(client.deletedPods) != 1 || len(client.pods) != 0 {
288:		t.Fatalf("suspended execution Pod was not deleted exactly once: deleted=%#v active=%#v", client.deletedPods, client.pods)
290:	createdPods := client.kindCount("Pod")
294:	if len(client.pods) != 0 || client.kindCount("Pod") != createdPods {
295:		t.Fatalf("suspended execution created a replacement Pod: active=%#v createdBefore=%d createdAfter=%d", client.pods, createdPods, client.kindCount("Pod"))
337:	if finalizeCalls != 1 || len(client.deletedPods) != 0 {
338:		t.Fatalf("uncommitted suspend proof was deleted: finalizeCalls=%d deleted=%#v", finalizeCalls, client.deletedPods)
343:	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
344:		t.Fatalf("reconciler created a replacement Pod while terminal proof remained pending: active=%#v created=%d", client.pods, client.kindCount("Pod"))
351:	if finalizeCalls != 2 || len(client.deletedPods) != 1 || client.deletedPods[0] != podName {
352:		t.Fatalf("durably finalized terminal Pod was not deleted exactly once: finalizeCalls=%d deleted=%#v", finalizeCalls, client.deletedPods)
374:	if client.kindCount("Pod") != 0 || len(client.pods) != 0 {
375:		t.Fatalf("absolute-expired Sessions created Kubernetes Pods: created=%d active=%#v", client.kindCount("Pod"), client.pods)
400:	if len(client.deletedPods) != 1 || len(client.pods) != 0 {
401:		t.Fatalf("absolute-expired Pod was not deleted exactly once: deleted=%#v active=%#v", client.deletedPods, client.pods)
431:	if client.kindCount("Pod") != 0 || len(client.pods) != 0 {
432:		t.Fatalf("stale pre-expiry query created a post-expiry Pod: created=%d active=%#v", client.kindCount("Pod"), client.pods)
468:	if len(client.deletedPods) != 1 || len(client.pods) != 0 {
469:		t.Fatalf("stale pre-expiry query retained a post-expiry Pod: deleted=%#v active=%#v", client.deletedPods, client.pods)
634:	if len(client.deletedPods) != 1 {
635:		t.Fatalf("release drift deleted Pods = %#v", client.deletedPods)
647:func TestKubernetesReconcilerCreatesWarmPoolPodForActiveWarmPool(t *testing.T) {
668:	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
669:		t.Fatalf("warm-pool reconciliation created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
697:func TestKubernetesReconcilerDeletesUnleasedWarmPoolPodDuringActiveCanary(t *testing.T) {
726:	if len(client.deletedPods) != 1 || len(client.pods) != 0 {
727:		t.Fatalf("active canary did not delete unleased warm pod exactly once: deleted=%#v active=%#v", client.deletedPods, client.pods)
732:	if len(client.pods) != 0 || client.kindCount("Pod") != 1 {
733:		t.Fatalf("active canary unexpectedly recreated warm pods: created=%d active=%#v", client.kindCount("Pod"), client.pods)
737:func TestKubernetesReconcilerPrioritizesExecutionPodsBeforeWarmPoolsAtTargetCap(t *testing.T) {
746:	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
747:		t.Fatalf("target cap reconciliation created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
757:func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
795:	if len(client.deletedPods) != 0 {
796:		t.Fatalf("ready warm capacity unexpectedly deleted a Pod: %#v", client.deletedPods)
800:func TestKubernetesReconcilerIgnoresTerminatedWarmWorkerWithReusedPodName(t *testing.T) {
852:func TestKubernetesWarmWorkerStateLookupUsesPodNameAndUID(t *testing.T) {
869:func TestKubernetesReconcilerFallsBackToColdPodWhenWarmPodIsUnregistered(t *testing.T) {
896:func TestKubernetesReconcilerFallsBackToColdPodWhenWarmWorkerIsOffline(t *testing.T) {
925:	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPod.Name {
926:		t.Fatalf("offline warm Pod was not recycled exactly once: deleted=%#v active=%#v", client.deletedPods, client.pods)
930:func TestKubernetesReconcilerRetainsIncompatibleWarmWorkerWithoutReportingReadyCapacity(t *testing.T) {
965:		t.Fatalf("incompatible warm Pod was deleted: deleted=%#v active=%#v", client.deletedPods, client.pods)
973:	createdPods := client.kindCount("Pod")
977:	if _, found := client.pods[warmPod.Name]; !found || len(client.deletedPods) != 0 || client.kindCount("Pod") != createdPods {
978:		t.Fatalf("incompatible warm Pod churned on a later pass: deleted=%#v active=%#v created=%d->%d", client.deletedPods, client.pods, createdPods, client.kindCount("Pod"))
982:func TestKubernetesReconcilerEvictsNotReadyWarmWorkerForColdDemandAtTargetCap(t *testing.T) {
1022:			if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPod.Name || len(client.pods) != 0 {
1023:				t.Fatalf("first pass did not evict only the not-ready warm Pod: deleted=%#v active=%#v", client.deletedPods, client.pods)
1025:			if client.kindCount("Pod") != 1 {
1026:				t.Fatalf("first pass recreated a Pod before deletion released quota: created=%d", client.kindCount("Pod"))
1036:			createdPods := client.kindCount("Pod")
1040:			if len(client.deletedPods) != 1 || client.kindCount("Pod") != createdPods || len(client.pods) != 1 {
1041:				t.Fatalf("later pass rebuilt warm capacity or repeated deletion: deleted=%#v active=%#v created=%d->%d", client.deletedPods, client.pods, createdPods, client.kindCount("Pod"))
1047:func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
1073:	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPod.Name || len(client.pods) != 0 {
1074:		t.Fatalf("stale warm Pod was not recycled exactly once: deleted=%#v active=%#v", client.deletedPods, client.pods)
1078:func TestKubernetesReconcilerTreatsExpiredWarmWorkerLeaseRowAsBusy(t *testing.T) {
1121:	if len(client.deletedPods) != 0 {
1122:		t.Fatalf("busy warm Worker with an expired lease row was deleted: %#v", client.deletedPods)
1129:func TestKubernetesWarmPodDeletionRechecksRegistrationAndLeaseAtDeleteBoundary(t *testing.T) {
1176:	if len(client.deletedPods) != 0 {
1177:		t.Fatalf("leased Pod was deleted after a state=nil snapshot: %#v", client.deletedPods)
1288:	if len(client.deletedPods) != 0 {
1289:		t.Fatalf("cleanup-busy Pod was deleted: %#v", client.deletedPods)
1302:func TestKubernetesWarmPodDeletionFenceSurvivesUnknownDeleteAndRetriesIdempotently(t *testing.T) {
1415:func TestKubernetesWarmWorkerReadinessAndRecyclePredicates(t *testing.T) {
1514:func TestKubernetesReconcilerFallsBackAndDrainsWarmPodOnReleaseMismatch(t *testing.T) {
1560:	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPod.Name {
1561:		t.Fatalf("stale warm release Pod was not deleted exactly once: deleted=%#v active=%#v", client.deletedPods, client.pods)
1617:func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
2322:	deletedPods    []string
2393:	c.deletedPods = append(c.deletedPods, name)
2398:func (c *fakeKubernetesClient) kindCount(kind string) int {
2737:	if len(client.deletedPods) != 1 || client.deletedPods[0] != pod.Name {
2738:		t.Fatalf("failed Pod deletion = %#v", client.deletedPods)

exec
/bin/zsh -lc "rg -n 'type fakeKubernetesClient|func \\(.*fakeKubernetesClient.*Apply|created|applied|kindCount|onlyFakePod' services/control-plane/internal/executiontargets/*_test.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go:85:	warmPod := onlyFakePod(t, client)
services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go:161:	warmPod := onlyFakePod(t, client)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:103:		if client.kindCount(kind) == 0 {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:114:	if client.kindCount("Pod") != 1 {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:115:		t.Fatalf("maxActivePods=1 created %d Pods", client.kindCount("Pod"))
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:152:		t.Fatalf("Kubernetes default Pod created a redundant Git cache volume: %#v", volumes)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:166:		t.Fatalf("Kubernetes default Pod created a redundant Git cache mount: %#v", volumeMounts)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:227:	if len(client.deletedPods) != 1 || client.kindCount("Pod") != 1 {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:228:		t.Fatalf("Kubernetes scheduler created a replacement before the deleted Pod released quota: deleted=%#v pods=%d", client.deletedPods, client.kindCount("Pod"))
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:233:	if client.kindCount("Pod") != 2 {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:234:		t.Fatalf("Kubernetes scheduler did not create the next Pod after deletion settled: pods=%d", client.kindCount("Pod"))
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:290:	createdPods := client.kindCount("Pod")
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:294:	if len(client.pods) != 0 || client.kindCount("Pod") != createdPods {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:295:		t.Fatalf("suspended execution created a replacement Pod: active=%#v createdBefore=%d createdAfter=%d", client.pods, createdPods, client.kindCount("Pod"))
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:343:	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:344:		t.Fatalf("reconciler created a replacement Pod while terminal proof remained pending: active=%#v created=%d", client.pods, client.kindCount("Pod"))
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:374:	if client.kindCount("Pod") != 0 || len(client.pods) != 0 {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:375:		t.Fatalf("absolute-expired Sessions created Kubernetes Pods: created=%d active=%#v", client.kindCount("Pod"), client.pods)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:431:	if client.kindCount("Pod") != 0 || len(client.pods) != 0 {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:432:		t.Fatalf("stale pre-expiry query created a post-expiry Pod: created=%d active=%#v", client.kindCount("Pod"), client.pods)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:668:	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:669:		t.Fatalf("warm-pool reconciliation created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:732:	if len(client.pods) != 0 || client.kindCount("Pod") != 1 {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:733:		t.Fatalf("active canary unexpectedly recreated warm pods: created=%d active=%#v", client.kindCount("Pod"), client.pods)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:746:	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:747:		t.Fatalf("target cap reconciliation created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:774:	warmPod := onlyFakePod(t, client)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:816:	warmPod := onlyFakePod(t, client)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:913:	warmPod := onlyFakePod(t, client)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:946:	warmPod := onlyFakePod(t, client)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:973:	createdPods := client.kindCount("Pod")
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:977:	if _, found := client.pods[warmPod.Name]; !found || len(client.deletedPods) != 0 || client.kindCount("Pod") != createdPods {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:978:		t.Fatalf("incompatible warm Pod churned on a later pass: deleted=%#v active=%#v created=%d->%d", client.deletedPods, client.pods, createdPods, client.kindCount("Pod"))
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1004:			warmPod := onlyFakePod(t, client)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1025:			if client.kindCount("Pod") != 1 {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1026:				t.Fatalf("first pass recreated a Pod before deletion released quota: created=%d", client.kindCount("Pod"))
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1036:			createdPods := client.kindCount("Pod")
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1040:			if len(client.deletedPods) != 1 || client.kindCount("Pod") != createdPods || len(client.pods) != 1 {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1041:				t.Fatalf("later pass rebuilt warm capacity or repeated deletion: deleted=%#v active=%#v created=%d->%d", client.deletedPods, client.pods, createdPods, client.kindCount("Pod"))
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1060:	warmPod := onlyFakePod(t, client)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1091:	warmPod := onlyFakePod(t, client)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1142:	warmPod := onlyFakePod(t, client)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1315:	warmPod := onlyFakePod(t, client)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1537:	warmPod := onlyFakePod(t, client)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1596:	warmPod := onlyFakePod(t, client)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1740:		t.Fatalf("authoritative Credential failure created a new Pod: before=%d after=%d", initialPods, len(client.pods))
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1746:	if bytes.Contains(mustJSON(t, client.applied), []byte("registry-password-secret")) {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1773:	applied := len(client.applied)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1779:	if len(client.applied) != applied {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1780:		t.Fatalf("transient Credential failure mutated Kubernetes resources: before=%d after=%d", applied, len(client.applied))
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1807:		t.Fatalf("unsupported bearer Credential created Pods: %#v", client.pods)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1813:	if bytes.Contains(mustJSON(t, client.applied), []byte("registry-bearer-secret")) {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2290:func onlyFakePod(t *testing.T, client *fakeKubernetesClient) kubernetesPod {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2319:type fakeKubernetesClient struct {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2320:	applied        []map[string]any
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2333:func (c *fakeKubernetesClient) Apply(_ context.Context, _ string, object map[string]any) error {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2345:	c.applied = append(c.applied, object)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2398:func (c *fakeKubernetesClient) kindCount(kind string) int {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2400:	for _, object := range c.applied {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2409:	for index := len(c.applied) - 1; index >= 0; index-- {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2410:		if c.applied[index]["kind"] == kind {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2411:			return c.applied[index]
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2418:	for index := len(c.applied) - 1; index >= 0; index-- {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2419:		object := c.applied[index]
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2443:		t.Fatal("Kubernetes Registry Secret was not applied")
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2674:	if err == nil || !strings.Contains(err.Error(), "A Kubernetes Worker Pod could not be applied") {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2677:	// Every queued Execution is attempted: one Pod that cannot be applied does
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2709:		t.Fatalf("initial Execution Pod was not created: %#v", client.pods)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2760:	if err == nil || !strings.Contains(err.Error(), "A Kubernetes Worker Pod could not be applied") {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2768:	appliedPods := make(map[string]struct{})
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2769:	for _, object := range client.applied {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2774:		appliedPods[name] = struct{}{}
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2776:	if _, found := appliedPods[survivorPod]; !found {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2777:		t.Fatalf("a failed Execution starved execution %s: applied=%v", survivor, appliedPods)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2779:	if _, found := appliedPods[blockedPod]; found {
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2780:		t.Fatalf("the rejected Pod %s was recorded as applied", blockedPod)
services/control-plane/internal/executiontargets/docker_reconciler_test.go:44:	if len(engine.containers) != 2 || engine.ensureImageCalls != 1 || len(engine.createdSpecs) != 2 {
services/control-plane/internal/executiontargets/docker_reconciler_test.go:45:		t.Fatalf("unexpected initial Docker reconciliation: containers=%d pulls=%d creates=%d", len(engine.containers), engine.ensureImageCalls, len(engine.createdSpecs))
services/control-plane/internal/executiontargets/docker_reconciler_test.go:47:	for _, spec := range engine.createdSpecs {
services/control-plane/internal/executiontargets/docker_reconciler_test.go:49:			t.Fatalf("Docker resource limits were not applied: %#v", spec)
services/control-plane/internal/executiontargets/docker_reconciler_test.go:93:	if len(engine.createdSpecs) != 2 {
services/control-plane/internal/executiontargets/docker_reconciler_test.go:94:		t.Fatalf("stable reconciliation created redundant containers: %d", len(engine.createdSpecs))
services/control-plane/internal/executiontargets/docker_reconciler_test.go:149:	if len(engine.createdSpecs) != 1 {
services/control-plane/internal/executiontargets/docker_reconciler_test.go:150:		t.Fatalf("Docker Worker create count = %d, want 1", len(engine.createdSpecs))
services/control-plane/internal/executiontargets/docker_reconciler_test.go:152:	if got := engine.createdSpecs[0].ExtraHosts; len(got) != 1 || got[0] != "host.docker.internal:host-gateway" {
services/control-plane/internal/executiontargets/docker_reconciler_test.go:171:	if first.ID == "" || len(engine.createdSpecs) != 1 {
services/control-plane/internal/executiontargets/docker_reconciler_test.go:172:		t.Fatalf("initial Worker was not created: %#v", engine.containers)
services/control-plane/internal/executiontargets/docker_reconciler_test.go:182:	if len(engine.createdSpecs) != 1 {
services/control-plane/internal/executiontargets/docker_reconciler_test.go:183:		t.Fatalf("busy stale Worker caused a conflicting replacement create: %d", len(engine.createdSpecs))
services/control-plane/internal/executiontargets/docker_reconciler_test.go:200:	if len(engine.createdSpecs) != 2 {
services/control-plane/internal/executiontargets/docker_reconciler_test.go:201:		t.Fatalf("cleared Lease did not create the desired replacement: %d", len(engine.createdSpecs))
services/control-plane/internal/executiontargets/docker_reconciler_test.go:241:	if len(engine.createdSpecs) != 4 || len(engine.ensuredImages) != 2 {
services/control-plane/internal/executiontargets/docker_reconciler_test.go:242:		t.Fatalf("release reconciliation creates=%d images=%#v", len(engine.createdSpecs), engine.ensuredImages)
services/control-plane/internal/executiontargets/docker_reconciler_test.go:245:	for _, spec := range engine.createdSpecs {
services/control-plane/internal/executiontargets/docker_reconciler_test.go:296:		t.Fatalf("baseline promoted Worker %q was not created", busyName)
services/control-plane/internal/executiontargets/docker_reconciler_test.go:528:	createdSpecs     []dockerContainerSpec
services/control-plane/internal/executiontargets/docker_reconciler_test.go:568:	e.createdSpecs = append(e.createdSpecs, spec)
services/control-plane/internal/executiontargets/target_lock_postgres_integration_test.go:43:	if err := db.Where("status = ?", "active").Order("created_at").Take(&scope).Error; err != nil {
services/control-plane/internal/executiontargets/service_test.go:46:	created, err := service.Create(ctx, principal, domain.TenantID, CreateInput{
services/control-plane/internal/executiontargets/service_test.go:54:	if created.Status != "offline" {
services/control-plane/internal/executiontargets/service_test.go:55:		t.Fatalf("new SSH target status = %q, want offline", created.Status)
services/control-plane/internal/executiontargets/service_test.go:57:	encoded, err := json.Marshal(created)
services/control-plane/internal/executiontargets/service_test.go:64:	defaultPolicy, err := ParseProviderPolicy(created.Capabilities)
services/control-plane/internal/executiontargets/service_test.go:71:	containmentPolicy, err := ParseProcessContainmentPolicy(created.Capabilities)
services/control-plane/internal/executiontargets/service_test.go:78:	if _, found := created.Capabilities["processContainmentPolicy"]; found {
services/control-plane/internal/executiontargets/service_test.go:79:		t.Fatalf("default process containment policy should not be materialized: %#v", created.Capabilities["processContainmentPolicy"])
services/control-plane/internal/executiontargets/service_test.go:82:	if err := store.DB().Where("id = ?", created.ID).Take(&persisted).Error; err != nil {
services/control-plane/internal/executiontargets/service_test.go:118:	created, err := service.Create(ctx, principal, domain.TenantID, CreateInput{
services/control-plane/internal/executiontargets/service_test.go:146:	assertProviderPolicy("created", created.Capabilities)
services/control-plane/internal/executiontargets/service_test.go:148:	if err := store.DB().Where("id = ?", created.ID).Take(&persisted).Error; err != nil {
services/control-plane/internal/executiontargets/service_test.go:185:	created, err := service.Create(ctx, principal, domain.TenantID, CreateInput{
services/control-plane/internal/executiontargets/service_test.go:199:	assertProcessContainmentPolicy(t, created.Capabilities, ProcessContainmentTrustSignedV1, "test-key", processContainmentTestPublicKeyBase64())
services/control-plane/internal/executiontargets/service_test.go:200:	assertNormalizedProcessContainmentCapability(t, created.Capabilities, ProcessContainmentTrustSignedV1, "test-key", processContainmentTestPublicKeyBase64())
services/control-plane/internal/executiontargets/service_test.go:202:	if err := store.DB().Where("id = ?", created.ID).Take(&persisted).Error; err != nil {

exec
/bin/zsh -lc "sed -n '1,260p' services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
package executiontargets

import (
	"bytes"
	"context"
	"encoding/base64"
	"encoding/json"
	"errors"
	"fmt"
	"log/slog"
	"net/http"
	"net/http/httptest"
	"path/filepath"
	"strings"
	"testing"
	"time"

	"github.com/google/uuid"
	"gorm.io/gorm"

	"github.com/synara-ai/synara/services/control-plane/internal/bootstrap"
	"github.com/synara-ai/synara/services/control-plane/internal/database"
	"github.com/synara-ai/synara/services/control-plane/internal/identity"
	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
	"github.com/synara-ai/synara/services/control-plane/internal/placement"
	"github.com/synara-ai/synara/services/control-plane/internal/platform"
	"github.com/synara-ai/synara/services/control-plane/internal/problem"
	"github.com/synara-ai/synara/services/control-plane/internal/secret"
	"github.com/synara-ai/synara/services/control-plane/migrations"
)

func TestOrderKubernetesExecutionsForServiceUsesTenantEqualShare(t *testing.T) {
	base := time.Date(2026, time.July, 26, 12, 0, 0, 0, time.UTC)
	tenantA := uuid.MustParse("00000000-0000-0000-0000-00000000000a")
	tenantB := uuid.MustParse("00000000-0000-0000-0000-00000000000b")
	a1 := kubernetesExecution{
		ID:       uuid.MustParse("10000000-0000-0000-0000-000000000001"),
		TenantID: tenantA, Status: "queued", QueuedAt: base,
	}
	b1 := kubernetesExecution{
		ID:       uuid.MustParse("20000000-0000-0000-0000-000000000001"),
		TenantID: tenantB, Status: "queued", QueuedAt: base.Add(time.Minute),
	}
	b2 := kubernetesExecution{
		ID:       uuid.MustParse("20000000-0000-0000-0000-000000000002"),
		TenantID: tenantB, Status: "recovering", QueuedAt: base.Add(2 * time.Minute),
	}
	active := []kubernetesExecution{
		{ID: uuid.New(), TenantID: tenantA, Status: "leased"},
		{ID: uuid.New(), TenantID: tenantA, Status: "waiting-for-approval"},
	}
	items := append(append([]kubernetesExecution{}, active...), a1, b2, b1)

	ordered, err := orderKubernetesExecutionsForService(items)
	if err != nil {
		t.Fatal(err)
	}
	if len(ordered) != len(items) || ordered[0].ID != active[0].ID || ordered[1].ID != active[1].ID ||
		ordered[2].ID != b1.ID || ordered[3].ID != b2.ID || ordered[4].ID != a1.ID {
		t.Fatalf("active-aware Kubernetes fair queue = %#v", ordered)
	}

	invalid := append([]kubernetesExecution{}, items...)
	invalid = append(invalid, kubernetesExecution{ID: uuid.New(), TenantID: tenantA, Status: "completed"})
	if _, err := orderKubernetesExecutionsForService(invalid); err == nil {
		t.Fatal("Kubernetes fair queue accepted an unexpected execution status")
	}
}

func TestKubernetesReconcilerAppliesSecurityFoundationAndExecutionPods(t *testing.T) {
	fixture := newKubernetesReconcileFixture(t, "")
	client := newFakeKubernetesClient()
	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
	recoveryCalls := 0
	var observedPodUIDSets [][]string
	fixture.reconciler.config.RecoverExpired = func(context.Context, int) error {
		recoveryCalls++
		return nil
	}
	fixture.reconciler.config.ReconcileEphemeralWorkspaceCleanup = func(
		_ context.Context,
		targetID uuid.UUID,
		podUIDs []string,
		_ time.Time,
	) (int, error) {
		if targetID != fixture.targetID {
			t.Fatalf("unexpected ephemeral cleanup target: %s", targetID)
		}
		observedPodUIDSets = append(observedPodUIDSets, append([]string(nil), podUIDs...))
		return 0, nil
	}

	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	if recoveryCalls != 1 {
		t.Fatalf("Kubernetes reconciliation did not run lease recovery: %d", recoveryCalls)
	}
	if len(observedPodUIDSets) != 1 || len(observedPodUIDSets[0]) != 0 {
		t.Fatalf("first ephemeral cleanup pass saw unexpected Pods: %#v", observedPodUIDSets)
	}
	for _, kind := range []string{"Namespace", "ServiceAccount", "Secret", "ResourceQuota", "NetworkPolicy", "Pod"} {
		if client.kindCount(kind) == 0 {
			t.Fatalf("Kubernetes reconciliation omitted %s", kind)
		}
	}
	namespace := client.lastKind("Namespace")
	namespaceLabels := namespace["metadata"].(map[string]any)["labels"].(map[string]string)
	if namespaceLabels["pod-security.kubernetes.io/enforce"] != "restricted" ||
		namespaceLabels["pod-security.kubernetes.io/audit"] != "restricted" ||
		namespaceLabels["pod-security.kubernetes.io/warn"] != "restricted" {
		t.Fatalf("Kubernetes managed Namespace did not enforce restricted Pod Security labels: %#v", namespaceLabels)
	}
	if client.kindCount("Pod") != 1 {
		t.Fatalf("maxActivePods=1 created %d Pods", client.kindCount("Pod"))
	}
	pod := client.lastKind("Pod")
	metadata := pod["metadata"].(map[string]any)
	labels := metadata["labels"].(map[string]string)
	if labels["synara.io/tenant-id"] != fixture.tenantID.String() ||
		labels["synara.io/organization-id"] != fixture.organizationID.String() ||
		labels["synara.io/project-id"] != fixture.projectID.String() ||
		labels["synara.io/session-id"] != fixture.sessionID.String() ||
		labels[kubernetesExecutionLabel] != fixture.executionIDs[0].String() || labels[kubernetesGenerationLabel] != "1" {
		t.Fatalf("Kubernetes Pod ownership/fencing labels are incomplete: %#v", labels)
	}
	spec := pod["spec"].(map[string]any)
	if spec["restartPolicy"] != "Never" || spec["automountServiceAccountToken"] != false ||
		spec["terminationGracePeriodSeconds"] != 30 || spec["enableServiceLinks"] != false ||
		spec["hostNetwork"] != false || spec["hostPID"] != false || spec["hostIPC"] != false {
		t.Fatalf("Kubernetes Pod runtime policy is unsafe: %#v", spec)
	}
	container := spec["containers"].([]any)[0].(map[string]any)
	if value, found := kubernetesEnvironmentValue(container, "SYNARA_AGENTD_WORKSPACE_ROOT"); !found || value != "/data/workspaces" {
		t.Fatalf("Kubernetes workspace root environment is %q", value)
	}
	if value, found := kubernetesEnvironmentValue(container, "SYNARA_AGENTD_GIT_CACHE_ROOT"); !found || value != "/data/git-cache" {
		t.Fatalf("Kubernetes default Git cache root environment is %q", value)
	}
	if fieldPath, found := kubernetesEnvironmentFieldPath(container, "SYNARA_AGENTD_INSTANCE_UID"); !found || fieldPath != "metadata.uid" {
		t.Fatalf("Kubernetes Pod UID environment uses %q", fieldPath)
	}
	if value, found := kubernetesEnvironmentValue(container, "SYNARA_AGENTD_VERSION"); found {
		t.Fatalf("Kubernetes Worker overrides the immutable image version with %q", value)
	}
	volumes := spec["volumes"].([]any)
	workspaceVolume := kubernetesNamedObject(volumes, "workspace")
	if workspaceVolume == nil || workspaceVolume["emptyDir"] == nil {
		t.Fatalf("Kubernetes default workspace volume is not emptyDir: %#v", volumes)
	}
	if kubernetesNamedObject(volumes, "git-cache") != nil {
		t.Fatalf("Kubernetes default Pod created a redundant Git cache volume: %#v", volumes)
	}
	identityVolume := kubernetesNamedObject(volumes, kubernetesWorkloadIdentityVolume)
	identityJSON, _ := json.Marshal(identityVolume)
	if identityVolume == nil || !bytes.Contains(identityJSON, []byte(KubernetesWorkerRegistrationAudience(fixture.targetID))) ||
		!bytes.Contains(identityJSON, []byte(`"expirationSeconds":600`)) {
		t.Fatalf("Kubernetes Pod-bound identity projection is invalid: %s", identityJSON)
	}
	volumeMounts := container["volumeMounts"].([]any)
	workspaceMount := kubernetesNamedObject(volumeMounts, "workspace")
	if workspaceMount == nil || workspaceMount["mountPath"] != "/data" {
		t.Fatalf("Kubernetes workspace mount is invalid: %#v", volumeMounts)
	}
	if kubernetesNamedObject(volumeMounts, "git-cache") != nil {
		t.Fatalf("Kubernetes default Pod created a redundant Git cache mount: %#v", volumeMounts)
	}
	identityMount := kubernetesNamedObject(volumeMounts, kubernetesWorkloadIdentityVolume)
	if identityMount == nil || identityMount["readOnly"] != true {
		t.Fatalf("Kubernetes Pod-bound identity mount is invalid: %#v", identityMount)
	}
	securityContext := container["securityContext"].(map[string]any)
	if securityContext["runAsNonRoot"] != true || securityContext["readOnlyRootFilesystem"] != true ||
		securityContext["allowPrivilegeEscalation"] != false {
		t.Fatalf("Kubernetes container security context is incomplete: %#v", securityContext)
	}
	seccompProfile, ok := securityContext["seccompProfile"].(map[string]any)
	if !ok || seccompProfile["type"] != "RuntimeDefault" {
		t.Fatalf("Kubernetes container seccomp profile is invalid: %#v", securityContext["seccompProfile"])
	}
	environment, err := json.Marshal(container["env"])
	if err != nil {
		t.Fatal(err)
	}
	if bytes.Contains(environment, []byte("kubernetes-registration-secret")) ||
		bytes.Contains(environment, []byte("secretKeyRef")) ||
		!bytes.Contains(environment, []byte("SYNARA_WORKER_REGISTRATION_TOKEN_FILE")) ||
		!bytes.Contains(environment, []byte(kubernetesWorkloadIdentityTokenPath)) ||
		!bytes.Contains(environment, []byte("SYNARA_AGENTD_ASSIGNED_EXECUTION_ID")) ||
		!bytes.Contains(environment, []byte("SYNARA_AGENTD_PROVIDER_HOST_PROTOCOL")) ||
		!bytes.Contains(environment, []byte(`"name":"SYNARA_AGENTD_LEASE_RENEW_INTERVAL","value":"2s"`)) ||
		!bytes.Contains(environment, []byte("SYNARA_AGENTD_DRAIN_TIMEOUT")) {
		t.Fatalf("Kubernetes Pod secret/assignment environment is invalid: %s", environment)
	}
	secret := client.lastKind("Secret")
	secretJSON, _ := json.Marshal(secret)
	if bytes.Contains(secretJSON, []byte("kubernetes-registration-secret")) ||
		client.namedKind("Secret", kubernetesSecretName(fixture.targetID)) != nil {
		t.Fatal("Kubernetes reconciliation persisted the shared Worker registration token")
	}
	networkPolicy := client.lastKind("NetworkPolicy")
	networkJSON, _ := json.Marshal(networkPolicy)
	if !bytes.Contains(networkJSON, []byte("0.0.0.0/0")) || !bytes.Contains(networkJSON, []byte("policyTypes")) {
		t.Fatalf("Kubernetes NetworkPolicy is incomplete: %s", networkJSON)
	}
	quota := client.lastKind("ResourceQuota")
	quotaJSON, _ := json.Marshal(quota)
	if !bytes.Contains(quotaJSON, []byte(`"pods":"1"`)) {
		t.Fatalf("Kubernetes ResourceQuota is incomplete: %s", quotaJSON)
	}

	if err := fixture.db.Model(&persistence.AgentExecution{}).Where("id = ?", fixture.executionIDs[0]).Update("status", "completed").Error; err != nil {
		t.Fatal(err)
	}
	driftedPodUID := uuid.NewString()
	client.pods["label-drifted-worker"] = kubernetesPod{
		Name: "label-drifted-worker", UID: driftedPodUID, Phase: "Running",
		Labels: map[string]string{kubernetesTargetLabel: uuid.NewString()},
	}
	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	if len(observedPodUIDSets) != 2 || len(observedPodUIDSets[1]) != 2 ||
		!containsString(observedPodUIDSets[1], driftedPodUID) {
		t.Fatalf("ephemeral cleanup did not receive the namespace-wide Pod UID set: %#v", observedPodUIDSets)
	}
	if len(client.deletedPods) != 1 || client.kindCount("Pod") != 1 {
		t.Fatalf("Kubernetes scheduler created a replacement before the deleted Pod released quota: deleted=%#v pods=%d", client.deletedPods, client.kindCount("Pod"))
	}
	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	if client.kindCount("Pod") != 2 {
		t.Fatalf("Kubernetes scheduler did not create the next Pod after deletion settled: pods=%d", client.kindCount("Pod"))
	}
	secondPod := client.lastKind("Pod")
	secondLabels := secondPod["metadata"].(map[string]any)["labels"].(map[string]string)
	if secondLabels[kubernetesExecutionLabel] != fixture.executionIDs[1].String() {
		t.Fatalf("Kubernetes scheduler did not advance to the next queued execution: %#v", secondLabels)
	}

	var target persistence.ExecutionTarget
	if err := fixture.db.Where("id = ?", fixture.targetID).Take(&target).Error; err != nil {
		t.Fatal(err)
	}
	if target.Status != "active" {
		t.Fatalf("Kubernetes target status is %q", target.Status)
	}
	var audits []persistence.AuditLog
	if err := fixture.db.Where("resource_id = ? AND action = ?", fixture.targetID, "execution_target.kubernetes_reconciled").Find(&audits).Error; err != nil {
		t.Fatal(err)
	}
	if len(audits) != 3 {
		t.Fatalf("expected three material Kubernetes reconciliation audits, got %d", len(audits))
	}
	encodedAudits, _ := json.Marshal(audits)
	if bytes.Contains(encodedAudits, []byte("kubernetes-registration-secret")) || bytes.Contains(encodedAudits, []byte("kubernetes-api-token")) {
		t.Fatalf("Kubernetes secrets leaked into Audit metadata: %s", encodedAudits)
	}
}

exec
/bin/zsh -lc "sed -n '630,805p' services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
sed -n '2310,2428p' services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
	}
	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	if len(client.deletedPods) != 1 {
		t.Fatalf("release drift deleted Pods = %#v", client.deletedPods)
	}
	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	newPod := client.lastKind("Pod")
	newContainer := newPod["spec"].(map[string]any)["containers"].([]any)[0].(map[string]any)
	if newContainer["image"] != "ghcr.io/synara/worker@"+secondDigest {
		t.Fatalf("replacement release Pod image = %q", newContainer["image"])
	}
}

func TestKubernetesReconcilerCreatesWarmPoolPodForActiveWarmPool(t *testing.T) {
	fixture := newKubernetesReconcileFixture(t, "")
	configuration := kubernetesTestConfiguration("")
	configuration["image"] = "ghcr.io/synara/worker:mutable"
	configuration["maxActivePods"] = 2
	fixture.updateConfiguration(t, configuration)
	if err := fixture.db.Model(&persistence.AgentExecution{}).
		Where("execution_target_id = ?", fixture.targetID).
		Update("status", "completed").Error; err != nil {
		t.Fatal(err)
	}
	promotedDigest := "sha256:" + strings.Repeat("d", 64)
	promotedRevision := fixture.seedReleaseRevision(t, 1, promotedDigest)
	fixture.seedReleasePolicy(t, promotedRevision, nil, 0)
	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
	client := newFakeKubernetesClient()
	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}

	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
		t.Fatalf("warm-pool reconciliation created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
	}
	pod := client.lastKind("Pod")
	metadata := pod["metadata"].(map[string]any)
	labels := metadata["labels"].(map[string]string)
	if labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
		labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() ||
		labels[kubernetesWorkerPoolVersionLabel] != "1" ||
		labels[kubernetesCapacityClassLabel] != placement.CapacityClassInteractive ||
		labels[kubernetesReleaseLabel] != promotedRevision.String() ||
		labels[kubernetesChannelLabel] != "promoted" {
		t.Fatalf("warm-pool labels = %#v", labels)
	}
	if _, found := labels[kubernetesExecutionLabel]; found {
		t.Fatalf("warm-pool pod unexpectedly carried execution label: %#v", labels)
	}
	container := pod["spec"].(map[string]any)["containers"].([]any)[0].(map[string]any)
	if container["image"] != "ghcr.io/synara/worker@"+promotedDigest {
		t.Fatalf("warm-pool image = %q", container["image"])
	}
	if value, found := kubernetesEnvironmentValue(container, "SYNARA_AGENTD_WORKER_MODE"); !found || value != kubernetesWorkerModeWarmPool {
		t.Fatalf("warm-pool worker mode env = %q", value)
	}
	if _, found := kubernetesEnvironmentValue(container, "SYNARA_AGENTD_ASSIGNED_EXECUTION_ID"); found {
		t.Fatal("warm-pool pod unexpectedly carried an assigned execution environment")
	}
}

func TestKubernetesReconcilerDeletesUnleasedWarmPoolPodDuringActiveCanary(t *testing.T) {
	fixture := newKubernetesReconcileFixture(t, "")
	configuration := kubernetesTestConfiguration("")
	configuration["image"] = "ghcr.io/synara/worker:mutable"
	configuration["maxActivePods"] = 2
	fixture.updateConfiguration(t, configuration)
	if err := fixture.db.Model(&persistence.AgentExecution{}).
		Where("execution_target_id = ?", fixture.targetID).
		Update("status", "completed").Error; err != nil {
		t.Fatal(err)
	}
	promotedRevision := fixture.seedReleaseRevision(t, 1, "sha256:"+strings.Repeat("d", 64))
	canaryRevision := fixture.seedReleaseRevision(t, 2, "sha256:"+strings.Repeat("e", 64))
	fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
	client := newFakeKubernetesClient()
	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}

	fixture.seedReleasePolicy(t, promotedRevision, nil, 0)
	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	if len(client.pods) != 1 {
		t.Fatalf("expected initial unleased warm pod, got %#v", client.pods)
	}

	fixture.seedReleasePolicy(t, promotedRevision, &canaryRevision, 20)
	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	if len(client.deletedPods) != 1 || len(client.pods) != 0 {
		t.Fatalf("active canary did not delete unleased warm pod exactly once: deleted=%#v active=%#v", client.deletedPods, client.pods)
	}
	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	if len(client.pods) != 0 || client.kindCount("Pod") != 1 {
		t.Fatalf("active canary unexpectedly recreated warm pods: created=%d active=%#v", client.kindCount("Pod"), client.pods)
	}
}

func TestKubernetesReconcilerPrioritizesExecutionPodsBeforeWarmPoolsAtTargetCap(t *testing.T) {
	fixture := newKubernetesReconcileFixture(t, "")
	fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
	client := newFakeKubernetesClient()
	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}

	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
		t.Fatalf("target cap reconciliation created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
	}
	pod := client.lastKind("Pod")
	labels := pod["metadata"].(map[string]any)["labels"].(map[string]string)
	if labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeExecutionPinned ||
		labels[kubernetesExecutionLabel] != fixture.executionIDs[0].String() {
		t.Fatalf("execution pod was not prioritized ahead of warm pool: %#v", labels)
	}
}

func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
	fixture := newKubernetesReconcileFixture(t, "")
	configuration := kubernetesTestConfiguration("")
	configuration["maxActivePods"] = 2
	fixture.updateConfiguration(t, configuration)
	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
	if err := fixture.db.Model(&persistence.AgentExecution{}).
		Where("execution_target_id = ?", fixture.targetID).
		Update("status", "completed").Error; err != nil {
		t.Fatal(err)
	}
	client := newFakeKubernetesClient()
	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}

	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	warmPod := onlyFakePod(t, client)
	warmPod.Phase = "Running"
	client.pods[warmPod.Name] = warmPod
	fixture.registerWarmWorker(t, pool, warmPod, "online", "active", nil, nil)
	for _, executionID := range fixture.executionIDs {
		fixture.assignExecutionToPool(t, executionID, pool, "queued")
	}

	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
		t.Fatal(err)
	}
	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
		t.Fatalf("first execution unexpectedly received a cold Pod despite ready warm capacity: %#v", client.pods)
	}
	executionPod, found := findExecutionPod(client, fixture.executionIDs[1])
	if !found {
		t.Fatalf("second execution did not fall back to a cold Pod: %#v", client.pods)
	}
	if executionPod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeExecutionPinned {
		t.Fatalf("fallback Pod worker mode = %#v", executionPod.Labels)
	}
	if len(client.deletedPods) != 0 {
		t.Fatalf("ready warm capacity unexpectedly deleted a Pod: %#v", client.deletedPods)
	}
}

func TestKubernetesReconcilerIgnoresTerminatedWarmWorkerWithReusedPodName(t *testing.T) {
	fixture := newKubernetesReconcileFixture(t, "")
	configuration := kubernetesTestConfiguration("")
	configuration["maxActivePods"] = 2
	fixture.updateConfiguration(t, configuration)
	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)

type fakeKubernetesFactory struct {
	client kubernetesClient
}

func (f *fakeKubernetesFactory) Open(kubernetesTargetConfiguration) (kubernetesClient, error) {
	return f.client, nil
}

type fakeKubernetesClient struct {
	applied        []map[string]any
	pods           map[string]kubernetesPod
	deletedPods    []string
	podApplyErr    error
	podApplyErrFor map[string]error
	listPodUIDsErr error
	deletePodErr   error
}

func newFakeKubernetesClient() *fakeKubernetesClient {
	return &fakeKubernetesClient{pods: map[string]kubernetesPod{}}
}

func (c *fakeKubernetesClient) Apply(_ context.Context, _ string, object map[string]any) error {
	if object["kind"] == "Pod" {
		if c.podApplyErr != nil {
			return c.podApplyErr
		}
		if len(c.podApplyErrFor) > 0 {
			name, _ := object["metadata"].(map[string]any)["name"].(string)
			if err, found := c.podApplyErrFor[name]; found {
				return err
			}
		}
	}
	c.applied = append(c.applied, object)
	if object["kind"] == "Pod" {
		metadata := object["metadata"].(map[string]any)
		name := metadata["name"].(string)
		labels := metadata["labels"].(map[string]string)
		annotations := map[string]string{}
		if raw, ok := metadata["annotations"].(map[string]any); ok {
			for key, value := range raw {
				annotations[key], _ = value.(string)
			}
		}
		c.pods[name] = kubernetesPod{
			Name: name, UID: uuid.NewString(), Phase: "Pending", CreatedAt: time.Now().UTC(),
			Labels: labels, Annotations: annotations,
		}
	}
	return nil
}

func (c *fakeKubernetesClient) ListPods(_ context.Context, _ string, targetID uuid.UUID) ([]kubernetesPod, error) {
	items := make([]kubernetesPod, 0, len(c.pods))
	for _, pod := range c.pods {
		if pod.Labels[kubernetesTargetLabel] == targetID.String() {
			items = append(items, pod)
		}
	}
	return items, nil
}

func (c *fakeKubernetesClient) ListPodUIDs(_ context.Context, _ string) ([]string, error) {
	if c.listPodUIDsErr != nil {
		return nil, c.listPodUIDsErr
	}
	uids := make([]string, 0, len(c.pods))
	for _, pod := range c.pods {
		uids = append(uids, pod.UID)
	}
	return uids, nil
}

func (c *fakeKubernetesClient) DeletePod(_ context.Context, _ string, name, uid string) error {
	if c.deletePodErr != nil {
		return c.deletePodErr
	}
	pod, found := c.pods[name]
	if found && pod.UID != uid {
		return fmt.Errorf("%w: current=%s requested=%s", errKubernetesPodUIDPreconditionFailed, pod.UID, uid)
	}
	c.deletedPods = append(c.deletedPods, name)
	delete(c.pods, name)
	return nil
}

func (c *fakeKubernetesClient) kindCount(kind string) int {
	count := 0
	for _, object := range c.applied {
		if object["kind"] == kind {
			count++
		}
	}
	return count
}

func (c *fakeKubernetesClient) lastKind(kind string) map[string]any {
	for index := len(c.applied) - 1; index >= 0; index-- {
		if c.applied[index]["kind"] == kind {
			return c.applied[index]
		}
	}
	return nil
}

func (c *fakeKubernetesClient) namedKind(kind, name string) map[string]any {
	for index := len(c.applied) - 1; index >= 0; index-- {
		object := c.applied[index]
		if object["kind"] != kind {
			continue
		}
		metadata, _ := object["metadata"].(map[string]any)
		if metadata["name"] == name {
			return object
		}
	}
	return nil

exec
/bin/zsh -lc "sed -n '430,625p' services/control-plane/internal/executiontargets/kubernetes_reconciler.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
	}
	observedPodUIDs := make(map[string]struct{}, len(pods))
	for _, pod := range pods {
		if uid := strings.TrimSpace(pod.UID); uid != "" {
			observedPodUIDs[uid] = struct{}{}
		}
	}
	for _, state := range workerPodStates {
		uid := strings.TrimSpace(state.InstanceUID)
		if _, found := observedPodUIDs[uid]; found {
			continue
		}
		if err := r.observeWorkerPod(ctx, KubernetesWorkerPodObservation{
			ExecutionTargetID: target.ID,
			Namespace:         configuration.Namespace,
			PodName:           state.PodName,
			PodUID:            uid,
			Phase:             "Missing",
			Reason:            "confirmed-missing:reconcile-list",
			ObservedAt:        r.now(),
		}); err != nil {
			return err
		}
	}
	if r.config.ReconcileEphemeralWorkspaceCleanup != nil {
		activePodUIDs, err := client.ListPodUIDs(ctx, configuration.Namespace)
		if err != nil {
			return problem.Wrap(502, "kubernetes_pod_identities_load_failed", "The complete Kubernetes Pod identity set could not be loaded.", err)
		}
		for index := range activePodUIDs {
			activePodUIDs[index] = strings.TrimSpace(activePodUIDs[index])
			if activePodUIDs[index] == "" {
				return problem.New(502, "kubernetes_pod_identities_invalid", "The Kubernetes Pod identity list contained an empty UID.")
			}
		}
		if _, err := r.config.ReconcileEphemeralWorkspaceCleanup(ctx, target.ID, activePodUIDs, r.now()); err != nil {
			return problem.Wrap(500, "kubernetes_workspace_cleanup_reconcile_failed", "Kubernetes ephemeral Workspace cleanup could not be reconciled.", err)
		}
	}
	active := make(map[uuid.UUID]kubernetesExecution, len(executions))
	for _, execution := range executions {
		active[execution.ID] = execution
	}
	warmWorkerStatesByIdentity := make(map[kubernetesWarmWorkerIdentity]kubernetesWarmWorkerState, len(warmWorkerStates))
	for _, state := range warmWorkerStates {
		warmWorkerStatesByIdentity[kubernetesWarmWorkerIdentityKey(state.PodName, state.InstanceUID)] = state
	}
	existing := make(map[string]kubernetesPod, len(pods))
	warmClaimedCounts := make(map[uuid.UUID]int)
	unleasedWarmPods := make([]kubernetesObservedWarmPod, 0)
	created, deleted := 0, 0
	for _, pod := range pods {
		if strings.TrimSpace(pod.Labels[kubernetesWorkerModeLabel]) == kubernetesWorkerModeWarmPool {
			state, matchedState := kubernetesWarmWorkerStateForPod(warmWorkerStatesByIdentity, pod)
			if matchedState && state.HasLease {
				if state.WorkerPoolID != nil {
					warmClaimedCounts[*state.WorkerPoolID]++
				}
				existing[pod.Name] = pod
				continue
			}
			poolID, poolVersion, capacityClass, slot, parseErr := kubernetesWarmPodIdentity(pod)
			if parseErr != nil {
				deletedPod, retainedWorker, err := r.deleteObservedPodSafely(
					ctx, client, target.ID, configuration.Namespace, pod, "warm-pool-invalid-identity",
				)
				if err != nil {
					return err
				}
				if retainedWorker != nil {
					if retainedWorker.WorkerPoolID != nil {
						warmClaimedCounts[*retainedWorker.WorkerPoolID]++
					}
					existing[pod.Name] = pod
					continue
				}
				if deletedPod {
					deleted++
				} else {
					existing[pod.Name] = pod
				}
				continue
			}
			observed := kubernetesObservedWarmPod{
				Pod: pod, PoolID: poolID, PoolVersion: poolVersion, CapacityClass: capacityClass, Slot: slot,
			}
			if matchedState {
				stateCopy := state
				observed.State = &stateCopy
			}
			unleasedWarmPods = append(unleasedWarmPods, observed)
			continue
		}
		executionID, parseErr := uuid.Parse(pod.Labels[kubernetesExecutionLabel])
		if parseErr != nil {
			deletedPod, _, err := r.deleteObservedPodSafely(
				ctx, client, target.ID, configuration.Namespace, pod, "execution-pod-invalid-identity",
			)
			if err != nil {
				return err
			}
			if deletedPod {
				deleted++
			} else {
				existing[pod.Name] = pod
			}
			continue
		}
		execution, found := active[executionID]
		if found && !kubernetesExecutionWithinAbsoluteLifetime(execution, r.now()) {
			found = false
		}
		expectedName := ""
		expectedHash := ""
		if found {
			expectedName = kubernetesPodName(execution)
			expectedHash, err = kubernetesExecutionPodHash(podBaseHash, configuration.Image, execution)
			if err != nil {
				return err
			}
		}
		terminalPod := pod.Phase == "Succeeded" || pod.Phase == "Failed"
		exactExecutionPod := found && pod.Name == expectedName &&
			pod.Annotations[kubernetesConfigAnnotation] == expectedHash
		generation := int64(0)
		if exactExecutionPod {
			generation, err = kubernetesObservedExecutionPodGeneration(execution, pod)
			if err != nil {
				return err
			}
			failureClass, failureReasonCode := classifyKubernetesExecutionPodFailure(pod)
			if err := r.observeExecutionPod(ctx, KubernetesExecutionPodObservation{
				TenantID: execution.TenantID, ExecutionTargetID: target.ID,
				ExecutionID: executionID, Generation: generation,
				Namespace: configuration.Namespace, PodName: pod.Name, PodUID: pod.UID,
				Phase: pod.Phase, FailureClass: failureClass, FailureReasonCode: failureReasonCode,
				PendingFailureThreshold: r.podPendingFailureThreshold(), PodCreatedAt: pod.CreatedAt,
				ObservedAt: r.now(),
			}); err != nil {
				return err
			}
			if terminalPod {
				if err := r.observeTerminalExecutionPod(ctx, target.ID, configuration.Namespace, pod); err != nil {
					return err
				}
			}
		}
		if exactExecutionPod &&
			pod.Phase == "Succeeded" && kubernetesPodCompletedSuccessfully(pod) {
			if r.config.FinalizeResourceSuspend == nil {
				return problem.New(503, "kubernetes_suspend_finalizer_unavailable", "Kubernetes Pod-terminal suspension finalization is not configured.")
			}
			finalized, err := r.config.FinalizeResourceSuspend(ctx, KubernetesPodTerminalObservation{
				ExecutionTargetID: target.ID, ExecutionID: executionID, Generation: generation,
				Namespace: configuration.Namespace, PodName: pod.Name,
				PodUID: pod.UID, Phase: pod.Phase, ObservedAt: r.now(),
			})
			if err != nil {
				return problem.Wrap(500, "kubernetes_suspend_finalize_failed", "The terminal Kubernetes Worker Pod could not finalize resource suspension.", err)
			}
			if !finalized {
				// Keep the exact terminal Pod until Control Plane state catches up.
				// Deleting here would destroy the only kubelet-authored proof for this
				// Pod UID and can permanently wedge suspension if checkpoint-ready or
				// durable DB visibility lags the terminal observation briefly.
				existing[pod.Name] = pod
				continue
			}
		}
		if !exactExecutionPod || terminalPod {
			reason := "execution-pod-obsolete"
			if terminalPod {
				reason = "execution-pod-terminal"
			}
			deletedPod, _, err := r.deleteObservedPodSafely(
				ctx, client, target.ID, configuration.Namespace, pod, reason,
			)
			if err != nil {
				return err
			}
			if deletedPod {
				deleted++
			} else {
				existing[pod.Name] = pod
			}
			continue
		}
		existing[pod.Name] = pod
	}
	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
	)
	if err != nil {
		return err
	}
	readyWarmCapacity := make(map[kubernetesWarmCapacityKey]int)

exec
/bin/zsh -lc "sed -n '125,190p' deploy/kubernetes/monitoring/prometheus-rules.yaml" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
          expr: |
            histogram_quantile(
              0.95,
              sum by (le) (rate(synara_sse_catchup_duration_seconds_bucket[5m]))
            ) > 5
          for: 5m
          labels:
            severity: warning
          annotations:
            summary: Synara SSE catch-up is delayed
            description: PostgreSQL-backed SSE catch-up p95 latency has exceeded five seconds for five minutes.
        - alert: SynaraExpiredSSELeases
          expr: synara_sse_connections{state="expired"} > 0
          for: 2m
          labels:
            severity: warning
          annotations:
            summary: Synara has expired SSE connection leases
            description: Expired SSE leases remain and should be removed by reconnect or retention cleanup.
    - name: synara-control-plane.fast-provision-slo
      rules:
        - alert: SynaraWarmHitColdStartSLOBreach
          expr: |
            max by (target_kind, warm_pool_mode) (
              synara_execution_cold_start_duration_seconds_30d{warm_pool_result="hit", quantile="p95"}
            ) > 2.5
          for: 15m
          labels:
            severity: warning
          annotations:
            summary: Warm-hit agent start P95 exceeds the interactive SLO
            description: Trailing-30-day dispatch-to-Provider-ready P95 for warm-pool hits has exceeded 2.5 seconds for 15 minutes; the fast-provision interactive SLO is breached even when warm capacity is available.
        - alert: SynaraWarmPoolFallbackSurge
          expr: |
            sum(synara_warm_pool_acquisitions_30d{result="fallback"})
              /
            clamp_min(sum(synara_warm_pool_acquisitions_30d{result=~"hit|fallback"}), 1)
            > 0.2
          for: 30m
          labels:
            severity: warning
          annotations:
            summary: Warm Pool requests are frequently falling back to cold starts
            description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.

exec
/bin/zsh -lc "ls services/control-plane/migrations | tail -n 15
sed -n '1,120p' services/control-plane/migrations/embed.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
000072_ssh_bootstrap_authority.sql
000073_ssh_worker_bootstrap_generation.sql
000074_execution_scheduling_policies.sql
000075_worker_claim_release_facts.sql
000076_execution_scheduling_decisions.sql
000077_shared_target_cost_allocation.sql
000078_kubernetes_pod_failure_facts.sql
000079_worker_incarnation_metric_rollups.sql
000080_billing_shared_allocation_calendar_schedules.sql
000081_execution_generation_metric_rollups.sql
000082_shared_actual_invoice_allocation.sql
000083_platform_routing_authority_publications.sql
000084_execution_capacity_reservation_authority.sql
000085_agent_executions_claim_indexes.sql
embed.go
package migrations

import "embed"

// Files is the authoritative, versioned PostgreSQL DDL shipped with the control plane.
//
//go:embed *.sql
var Files embed.FS

exec
/bin/zsh -lc "sed -n '33,92p' docs/contracts/worker-pool-placement-v1.md
sed -n '145,198p' docs/contracts/worker-pool-placement-v1.md" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
## Worker Pools

Each Pool has a stable UUID, Target, name, mode, capacity class, target-local location, capacity bounds, scheduling
template, status, and optimistic version.

Pool modes:

- `resident`: long-lived general Workers, such as the existing Docker pool.
- `per-execution`: capacity is created for one assigned Execution Generation.
- `warm`: an idle Worker/Pod may wait without Session state, claim one compatible Generation, and then terminate.

Capacity classes begin with `standard` and `interactive`. They are scheduling codes, not billing SKUs. CPU, memory,
storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
introduced.

`clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.

Pool status is `active`, `draining`, or `disabled`. New placements can select only `active` Pools. Existing leases on
a draining Pool remain fenced and are allowed to reach their normal drain boundary.

## Placement Policy

Each Target has one versioned placement policy:

- `defaultPoolId` handles `warmPoolMode=disabled` and all legacy/default traffic.
- `balancedPoolId`, when present, is preferred for `warmPoolMode=balanced`.
- `lowLatencyPoolId`, when present, is preferred for `warmPoolMode=low-latency`.
- Missing optional mappings fall back in this exact order:
  - `balanced`: balanced, then default.
  - `low-latency`: low-latency, then balanced, then default.

The ordered list is also the cold-fallback order. Placement remembers the first active candidate, then scans active
warm candidates in that same order for a fresh `worker_pool_warm_capacity` observation with
`warmSupported=true` and `readyIdleUnits>0`. The first such candidate is preferred. If none exists, placement returns
the remembered first active candidate unchanged, so missing, expired, unsupported, or zero-capacity observations do
not turn a valid cold launch into an admission failure. The observation is a soft hint and is never decremented or
reserved by placement.

Policy updates use compare-and-swap on `version`. The selected Pool ID, capacity class, and policy version are frozen
on the queued Execution before an Outbox dispatch becomes visible. Claim-time recomputation is forbidden.

The current schema stores one mutable Pool row rather than a revision-history table. Therefore a Pool version update is
rejected while any queued, recovering, leased, running, waiting, or suspended Execution still references that version.
This conservative fence prevents a frozen scheduling template from being overwritten underneath recovery.

Legacy Executions may have no placement snapshot. They use the Target's version-1 default Pool only; they must not be
silently routed to a newly introduced warm Pool.

## Worker Mode

Worker mode is explicit and persisted:

- `execution-pinned`: can request only its assigned Execution and exits after that attempt.
- `warm-pool`: can request compatible warm-pool work, cannot claim Workspace cleanup, and exits after one claimed
  Execution attempt.
- `general-pool`: may keep polling for work and may claim Workspace cleanup when otherwise authorized.

For Kubernetes, Worker mode and Pool identity come from the TokenReview-authenticated, Pod-UID-matched Pod labels.
## Live warm-capacity authority

Target-wide Pod admission and warm reuse are different authorities. `execution_target_health` remains the hard,
expiring Target-wide ceiling/occupancy observation and includes execution Pods, idle warm Pods, claimed warm Pods, and
deletion-pending Pods. `worker_pool_warm_capacity` reports only the release-aware warm capacity that the Kubernetes
Reconciler has proved reusable now.

Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.

`readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
execution quota. Launches between two reconcile cycles may contend for the same last warm unit; the Worker Lease/Claim
transaction remains the correctness boundary, and excess work may use the existing cold execution-Pod fallback or wait
behind the Target-wide Pod ceiling. `maxActiveUnits` must never be reinterpreted as a count of active Sessions or
Executions.

A resource-suspended Execution consumes neither a Worker Lease nor tenant concurrent-execution quota. Every
`suspended -> recovering` transition must reacquire tenant quota under the same durable tenant admission lock used by new
Turn/review/compact creation. The frozen Pool snapshot is preserved, but warm availability cannot block Resume because
the Reconciler may legitimately restore that Generation through the cold fallback path.

## Durable Observability

Metrics must be derived from bounded durable facts rather than unbounded Session Event joins:

- one Generation fact keyed by `(tenantId, executionId, generation)` records dispatch, lease, execution start,
  Provider ready, terminal outcome, recovery reason, requested warm mode, and warm allocation result;
- one physical Pod/Worker-incarnation fact records registration, active/idle transitions, termination, and resource
  class.

Required bounded labels include Target kind, recovery reason, Pool mode, requested warm mode, allocation result, and
terminal outcome. Tenant, Session, Execution, Worker, and Pod IDs are never Prometheus labels.

Pod runtime and requested-resource seconds can be reported once their lifecycle facts are complete. Currency cost
must not be exposed until an authoritative tariff or cloud-billing source is bound to the resource class.

The first implementation exports durable trailing-30-day Generation outcomes, warm allocation results, and
dispatch-to-Provider-ready P50/P95/P99 gauges. Worker-incarnation gauges expose retained runtime, active/idle seconds,
and requested CPU/memory/ephemeral-storage seconds. They deliberately contain no Tenant, Execution, Worker, or Pod ID
labels.

The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
Tenant/Target/Pool identifier is exposed as a label.

Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
`synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
unknown label values to `unknown | other`, and omit Tenant, Target, Pool, Session, and Execution identifiers.

## Rollout Order

exec
/bin/zsh -lc "sed -n '375,565p' services/control-plane/internal/database/stage4_resource_lifecycle_sqlite_test.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
func openStage4SQLiteFixture(t *testing.T) stage4SQLiteFixture {
	t.Helper()
	ctx := context.Background()
	config, err := platform.Defaults(platform.ProfilePersonal)
	if err != nil {
		t.Fatal(err)
	}
	store, err := OpenMetadataStore(ctx, config, "", filepath.Join(t.TempDir(), "metadata.sqlite"))
	if err != nil {
		t.Fatal(err)
	}
	t.Cleanup(func() { _ = store.Close() })
	if err := store.Migrate(ctx, migrations.Files); err != nil {
		t.Fatal(err)
	}
	domain, err := bootstrap.Ensure(ctx, store.DB(), platform.ProfilePersonal, "sqlite-stage4-"+uuid.NewString())
	if err != nil {
		t.Fatal(err)
	}
	now := time.Now().UTC().Truncate(time.Second)
	projectID := uuid.New()
	if err := store.DB().Create(&persistence.Project{
		ID: projectID, TenantID: domain.TenantID, OrganizationID: domain.OrganizationID,
		Name: "SQLite Stage 4", DefaultBranch: "main", Visibility: "private",
		CreatedBy: domain.UserID, CreatedAt: now, UpdatedAt: now,
	}).Error; err != nil {
		t.Fatal(err)
	}
	otherTenantID := uuid.New()
	otherOrganizationID := uuid.New()
	otherProjectID := uuid.New()
	if err := store.DB().Create(&persistence.Tenant{
		ID: otherTenantID, Slug: "sqlite-stage4-other", Name: "SQLite Stage 4 Other", Status: "active",
		PlanCode: "personal", Region: "local", Settings: map[string]any{}, CreatedBy: domain.UserID,
		CreatedAt: now, UpdatedAt: now,
	}).Error; err != nil {
		t.Fatal(err)
	}
	if err := store.DB().Create(&persistence.Organization{
		ID: otherOrganizationID, TenantID: otherTenantID, Slug: "other", Name: "Other",
		Kind: "root", Status: "active", Settings: map[string]any{}, CreatedBy: domain.UserID,
		CreatedAt: now, UpdatedAt: now,
	}).Error; err != nil {
		t.Fatal(err)
	}
	if err := store.DB().Create(&persistence.Project{
		ID: otherProjectID, TenantID: otherTenantID, OrganizationID: otherOrganizationID,
		Name: "SQLite Stage 4 Other", DefaultBranch: "main", Visibility: "private",
		CreatedBy: domain.UserID, CreatedAt: now, UpdatedAt: now,
	}).Error; err != nil {
		t.Fatal(err)
	}
	return stage4SQLiteFixture{
		db: store.DB(), tenantID: domain.TenantID, organizationID: domain.OrganizationID,
		projectID: projectID, userID: domain.UserID, targetID: domain.ExecutionTargetID,
		otherTenantID: otherTenantID, otherOrganizationID: otherOrganizationID, otherProjectID: otherProjectID,
	}
}

func (fixture stage4SQLiteFixture) createSession(
	t *testing.T,
	tenantID, organizationID, projectID uuid.UUID,
	title string,
	now time.Time,
) uuid.UUID {
	t.Helper()
	sessionID := uuid.New()
	if err := fixture.db.Create(&persistence.AgentSession{
		ID: sessionID, TenantID: tenantID, OrganizationID: organizationID, ProjectID: projectID,
		CreatedBy: fixture.userID, Title: title, Status: "active", Visibility: "private",
		Provider: "codex", ExecutionTargetID: fixture.targetID, ProviderResumeCursorState: "absent",
		ResourceState: "idle", MeaningfulActivityAt: now, WaitingKeepAliveSeconds: 900, SuspendAfterIdleSeconds: 1800,
		WorkspaceRetentionDays: 30, WarmPoolMode: "disabled", CreatedAt: now, UpdatedAt: now,
	}).Error; err != nil {
		t.Fatal(err)
	}
	return sessionID
}

func (fixture stage4SQLiteFixture) createTurn(t *testing.T, tenantID, sessionID uuid.UUID, now time.Time) uuid.UUID {
	t.Helper()
	turnID := uuid.New()
	if err := fixture.db.Create(&persistence.AgentTurn{
		ID: turnID, TenantID: tenantID, SessionID: sessionID, CreatedBy: fixture.userID,
		Status: "queued", InputText: "hello", TurnKind: "message",
		RuntimeMode: "full-access", InteractionMode: "default", CreatedAt: now,
	}).Error; err != nil {
		t.Fatal(err)
	}
	return turnID
}

func (fixture stage4SQLiteFixture) createWorker(t *testing.T, podSuffix string, now time.Time) (uuid.UUID, string) {
	t.Helper()
	workerID := uuid.New()
	instanceUID := uuid.NewString()
	if err := fixture.db.Create(&persistence.WorkerInstance{
		ID: workerID, Incarnation: 1, InstanceUID: instanceUID, ExecutionTargetID: fixture.targetID,
		TargetKind: "local", ClusterID: "sqlite-stage4", Namespace: "default", PodName: podSuffix,
		Version: "stage4", ProtocolVersion: 1, Capabilities: map[string]any{}, LeaseSupported: true,
		FencingSupported: true, AuthTokenHash: []byte("token-" + workerID.String()), Status: "online",
		AdministrativeStatus: "active", RegisteredAt: now, LastHeartbeatAt: now,
	}).Error; err != nil {
		t.Fatal(err)
	}
	return workerID, instanceUID
}

func (fixture stage4SQLiteFixture) createExecution(
	t *testing.T,
	sessionID, turnID, workerID uuid.UUID,
	generation int64,
	status string,
	now time.Time,
) uuid.UUID {
	t.Helper()
	executionID := uuid.New()
	if err := fixture.db.Create(&persistence.AgentExecution{
		ID: executionID, TenantID: fixture.tenantID, SessionID: sessionID, TurnID: turnID,
		Attempt: 1, Status: status, ExecutionTargetID: fixture.targetID, TargetKind: "local",
		WorkerID: &workerID, Generation: generation, RequestedBy: fixture.userID, QueuedAt: now, StartedAt: &now,
	}).Error; err != nil {
		t.Fatal(err)
	}
	return executionID
}

func (fixture stage4SQLiteFixture) createMemoryArtifact(
	t *testing.T,
	sessionID uuid.UUID,
	sha string,
	now time.Time,
) persistence.Artifact {
	t.Helper()
	contentType := "text/markdown; charset=utf-8"
	sizeBytes := int64(16)
	artifact := persistence.Artifact{
		ID: uuid.New(), TenantID: fixture.tenantID, OrganizationID: fixture.organizationID, ProjectID: fixture.projectID,
		SessionID: sessionID, Kind: "memory", Status: "ready", Bucket: "sqlite-stage4",
		ObjectKey: "memory/" + uuid.NewString(), ContentType: &contentType, SizeBytes: &sizeBytes, SHA256: &sha,
		CreatedByType: "user", CreatedByID: fixture.userID, ReadyAt: &now, CreatedAt: now,
	}
	if err := fixture.db.Create(&artifact).Error; err != nil {
		t.Fatal(err)
	}
	return artifact
}

func (fixture stage4SQLiteFixture) createResumeRecordedInteraction(
	t *testing.T,
	sessionID, turnID, executionID, workerID uuid.UUID,
	generation int64,
	requestID string,
	now time.Time,
) persistence.ExecutionInteraction {
	t.Helper()
	resolutionKind := "approved"
	resolutionCommandID := requestID + ":resolution"
	interaction := persistence.ExecutionInteraction{
		ID: uuid.New(), TenantID: fixture.tenantID, ExecutionID: executionID, SessionID: sessionID, TurnID: turnID,
		WorkerID: workerID, Generation: generation, Provider: "codex", RequestID: requestID, EventVersion: 1,
		Kind: "approval", Status: "resolved", Payload: map[string]any{"summary": "Run"},
		Resolution: map[string]any{"decision": "accept"}, RequestedAt: now.Add(-time.Minute), ExpiresAt: now.Add(23 * time.Hour),
		ResolvedAt: &now, ResolvedBy: &fixture.userID, ResolutionKind: &resolutionKind,
		ResolutionCommandID: &resolutionCommandID, DeliveryStatus: "resume-recorded", DeliveryAvailableAt: &now,
	}
	if err := fixture.db.Create(&interaction).Error; err != nil {
		t.Fatal(err)
	}
	return interaction
}

func assertSQLiteStage4Rejected(t *testing.T, err error, expected string) {
	t.Helper()
	if err == nil || !strings.Contains(err.Error(), expected) {
		t.Fatalf("SQLite accepted invalid Stage 4 state or returned wrong error: %v (want containing %q)", err, expected)
	}
}

func intPointer(value int) *int { return &value }

func timePointer(value time.Time) *time.Time { return &value }

codex
The implementation shape is clear: min-idle remains inside `desiredIdleUnits`; plan generation will return guaranteed and best-effort slices plus one shared name/hash map; only guaranteed slices run before cold Pods. Deficit stays authority-derived, so budget truncation is visible without turning a reconcile pass into an error.
apply patch
patch: completed
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/persistence/placement_models.go
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/persistence/warm_capacity_models.go
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/placement/service.go
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/migrations/000086_worker_pool_min_idle.sql
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..8b650f08bb7514c7a04689f1ff73b0e2249e863c
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -744,6 +748,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,7 +828,11 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
+	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
 	}
 	template := input.SchedulingTemplate
@@ -838,6 +847,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..8b650f08bb7514c7a04689f1ff73b0e2249e863c
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -744,6 +748,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,7 +828,11 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
+	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
 	}
 	template := input.SchedulingTemplate
@@ -838,6 +847,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

2026-07-26T14:19:14.106293Z ERROR codex_core::tools::router: error=apply_patch verification failed: Failed to find expected lines in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/database/store.go:
		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
		      OR NEW.desired_idle_units < 0
		      OR NEW.max_active_units < NEW.desired_idle_units
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..8b650f08bb7514c7a04689f1ff73b0e2249e863c
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -744,6 +748,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,7 +828,11 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
+	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
 	}
 	template := input.SchedulingTemplate
@@ -838,6 +847,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

exec
/bin/zsh -lc "sed -n '410,510p' services/control-plane/internal/database/store.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
		 WHERE status IN ('leased', 'running', 'waiting-for-approval')`,
		`CREATE INDEX IF NOT EXISTS idx_agent_executions_target_nonterminal
		 ON agent_executions (execution_target_id, target_kind, status, queued_at, id)
		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
		`INSERT INTO worker_pools (
		   id, tenant_id, execution_target_id, name, mode, capacity_class,
		   cluster_id, region, namespace, desired_idle_units, max_active_units,
		   scheduling_template, status, version, created_at, updated_at
		 )
		 SELECT target.id, target.tenant_id, target.id, 'default',
		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
		 FROM execution_targets AS target
		 WHERE NOT EXISTS (
		   SELECT 1 FROM worker_pools AS pool
		   WHERE pool.execution_target_id = target.id AND lower(pool.name) = 'default'
		 )`,
		`INSERT INTO execution_placement_policies (
		   execution_target_id, tenant_id, version, default_pool_id, updated_at
		 )
		 SELECT target.id, target.tenant_id, 1, pool.id, CURRENT_TIMESTAMP
		 FROM execution_targets AS target
		 JOIN worker_pools AS pool
		   ON pool.execution_target_id = target.id AND lower(pool.name) = 'default'
		 WHERE NOT EXISTS (
		   SELECT 1 FROM execution_placement_policies AS policy
		   WHERE policy.execution_target_id = target.id
		 )`,
		`DROP TRIGGER IF EXISTS trg_worker_pools_shape_insert`,
		`CREATE TRIGGER trg_worker_pools_shape_insert
		 BEFORE INSERT ON worker_pools
		 BEGIN
		   SELECT RAISE(ABORT, 'invalid Worker pool shape')
		   WHERE NEW.mode NOT IN ('resident', 'per-execution', 'warm')
		      OR NEW.capacity_class NOT IN ('standard', 'interactive')
		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
		      OR NEW.desired_idle_units < 0
		      OR NEW.max_active_units < NEW.desired_idle_units
		      OR NEW.version <= 0
		      OR json_valid(NEW.scheduling_template) = 0
		      OR json_type(NEW.scheduling_template) <> 'object';

		   SELECT RAISE(ABORT, 'Worker pool target scope mismatch')
		   WHERE NOT EXISTS (
		     SELECT 1 FROM execution_targets AS target
		     WHERE target.id = NEW.execution_target_id
		       AND target.tenant_id IS NEW.tenant_id
		   );
		 END`,
		`DROP TRIGGER IF EXISTS trg_worker_pools_shape_update`,
		`CREATE TRIGGER trg_worker_pools_shape_update
		 BEFORE UPDATE ON worker_pools
		 BEGIN
		   SELECT RAISE(ABORT, 'Worker pool ownership is immutable')
		   WHERE NEW.execution_target_id IS NOT OLD.execution_target_id
		      OR NEW.tenant_id IS NOT OLD.tenant_id;

		   SELECT RAISE(ABORT, 'Worker pool mode and capacity class are immutable')
		   WHERE NEW.mode IS NOT OLD.mode OR NEW.capacity_class IS NOT OLD.capacity_class;

		   SELECT RAISE(ABORT, 'invalid Worker pool update')
		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
		      OR NEW.desired_idle_units < 0
		      OR NEW.max_active_units < NEW.desired_idle_units
		      OR NEW.version <> OLD.version + 1
		      OR json_valid(NEW.scheduling_template) = 0
		      OR json_type(NEW.scheduling_template) <> 'object';

		   SELECT RAISE(ABORT, 'Execution placement default pool must remain active')
		   WHERE NEW.status <> 'active' AND EXISTS (
		     SELECT 1 FROM execution_placement_policies AS policy
		     WHERE policy.default_pool_id = NEW.id
		   );
		 END`,
		`DROP TRIGGER IF EXISTS trg_worker_pools_delete`,
		`CREATE TRIGGER trg_worker_pools_delete
		 BEFORE DELETE ON worker_pools
		 BEGIN
		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
		 END`,
		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
		`CREATE TRIGGER trg_execution_placement_policy_insert
		 BEFORE INSERT ON execution_placement_policies
		 BEGIN
		   SELECT RAISE(ABORT, 'invalid Execution placement policy')
		   WHERE NEW.version <= 0
		      OR NOT EXISTS (
		        SELECT 1 FROM execution_targets AS target
		        WHERE target.id = NEW.execution_target_id AND target.tenant_id IS NEW.tenant_id
		      )
		      OR NOT EXISTS (
		        SELECT 1 FROM worker_pools AS pool
		        WHERE pool.id = NEW.default_pool_id
		          AND pool.execution_target_id = NEW.execution_target_id
		          AND pool.tenant_id IS NEW.tenant_id
		          AND pool.status = 'active'
		      )
		      OR (NEW.balanced_pool_id IS NOT NULL AND NOT EXISTS (
		        SELECT 1 FROM worker_pools AS pool

diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..8b650f08bb7514c7a04689f1ff73b0e2249e863c
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -744,6 +748,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,7 +828,11 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
+	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
 	}
 	template := input.SchedulingTemplate
@@ -838,6 +847,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

apply patch
patch: completed
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/database/store.go
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..0cd42fb8cbcdb536d78c6f043cbb67d3826723af
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..8b650f08bb7514c7a04689f1ff73b0e2249e863c
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -744,6 +748,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,7 +828,11 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
+	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
 	}
 	template := input.SchedulingTemplate
@@ -838,6 +847,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..0cd42fb8cbcdb536d78c6f043cbb67d3826723af
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..8b650f08bb7514c7a04689f1ff73b0e2249e863c
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -744,6 +748,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,7 +828,11 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
+	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
 	}
 	template := input.SchedulingTemplate
@@ -838,6 +847,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

apply patch
patch: completed
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/database/store.go
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..8b650f08bb7514c7a04689f1ff73b0e2249e863c
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -744,6 +748,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,7 +828,11 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
+	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
 	}
 	template := input.SchedulingTemplate
@@ -838,6 +847,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..8b650f08bb7514c7a04689f1ff73b0e2249e863c
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -744,6 +748,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,7 +828,11 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
+	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
 	}
 	template := input.SchedulingTemplate
@@ -838,6 +847,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

exec
/bin/zsh -lc "rg -n 'warmcapacity\\.Observation\\{|Observation\\{' services/control-plane/internal/warmcapacity services/control-plane/internal/executiontargets | rg -n 'warmcapacity|service_test|managed_kubernetes' | head -n 100" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
1:services/control-plane/internal/warmcapacity/service_test.go:22:	observed, err := fixture.service.Observe(context.Background(), Observation{
2:services/control-plane/internal/warmcapacity/service_test.go:69:	advanced, err := fixture.service.Observe(context.Background(), Observation{
3:services/control-plane/internal/warmcapacity/service_test.go:110:	base := Observation{
4:services/control-plane/internal/warmcapacity/service_test.go:201:	if _, err := fixture.service.Observe(context.Background(), Observation{
5:services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go:68:	_, err := p.warmCapacity.Observe(ctx, warmcapacity.Observation{
6:services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go:31:	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
7:services/control-plane/internal/executiontargets/managed_kubernetes_routing_publisher_test.go:150:	seeded, err := routing.NewService(fixture.db).ObserveHealth(context.Background(), routing.HealthObservation{
8:services/control-plane/internal/executiontargets/managed_kubernetes_routing_publisher_test.go:219:	err := publisher.PublishReconcile(context.Background(), ManagedKubernetesRoutingHealthObservation{
9:services/control-plane/internal/executiontargets/managed_kubernetes_routing_publisher_test.go:250:	err := publisher.PublishReconcile(context.Background(), ManagedKubernetesRoutingHealthObservation{
18:services/control-plane/internal/executiontargets/managed_kubernetes_routing_publisher.go:88:	_, err := p.routing.ObserveHealth(ctx, routing.HealthObservation{

diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..8b650f08bb7514c7a04689f1ff73b0e2249e863c
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -744,6 +748,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,7 +828,11 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
+	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
 	}
 	template := input.SchedulingTemplate
@@ -838,6 +847,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

apply patch
patch: completed
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/warmcapacity/service.go
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..444e66fd3604464aef8b72fa31fab708217e8f2c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -1218,13 +1218,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1243,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..7bdcd45ced5fb92b76c1f720356e846733dc46ce
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -107,6 +108,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..8b650f08bb7514c7a04689f1ff73b0e2249e863c
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -744,6 +748,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,7 +828,11 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
+	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
 	}
 	template := input.SchedulingTemplate
@@ -838,6 +847,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..444e66fd3604464aef8b72fa31fab708217e8f2c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -1218,13 +1218,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1243,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..7bdcd45ced5fb92b76c1f720356e846733dc46ce
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -107,6 +108,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..8b650f08bb7514c7a04689f1ff73b0e2249e863c
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -744,6 +748,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,7 +828,11 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
+	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
 	}
 	template := input.SchedulingTemplate
@@ -838,6 +847,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

apply patch
patch: completed
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..8cc7e3a711643aec5482b919c2eb7b29e09ee171
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -1218,13 +1218,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1243,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..68b28103294769efdbe724a19b5dbd7a574f354b
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -107,6 +108,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +322,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +345,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..8b650f08bb7514c7a04689f1ff73b0e2249e863c
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -744,6 +748,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,7 +828,11 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
+	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
 	}
 	template := input.SchedulingTemplate
@@ -838,6 +847,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..8cc7e3a711643aec5482b919c2eb7b29e09ee171
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -1218,13 +1218,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1243,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..68b28103294769efdbe724a19b5dbd7a574f354b
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -107,6 +108,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +322,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +345,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..8b650f08bb7514c7a04689f1ff73b0e2249e863c
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -744,6 +748,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,7 +828,11 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
+	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
 	}
 	template := input.SchedulingTemplate
@@ -838,6 +847,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

apply patch
patch: completed
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..13f19f33b31590b72840faabfe982fc37e0f2cb4
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -1218,13 +1218,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1243,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..8b650f08bb7514c7a04689f1ff73b0e2249e863c
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -744,6 +748,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,7 +828,11 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
+	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
 	}
 	template := input.SchedulingTemplate
@@ -838,6 +847,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..13f19f33b31590b72840faabfe982fc37e0f2cb4
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -1218,13 +1218,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1243,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..8b650f08bb7514c7a04689f1ff73b0e2249e863c
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -744,6 +748,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,7 +828,11 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
+	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
 	}
 	template := input.SchedulingTemplate
@@ -838,6 +847,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

apply patch
patch: completed
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..8b650f08bb7514c7a04689f1ff73b0e2249e863c
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -744,6 +748,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,7 +828,11 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
+	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
 	}
 	template := input.SchedulingTemplate
@@ -838,6 +847,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..8b650f08bb7514c7a04689f1ff73b0e2249e863c
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -744,6 +748,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,7 +828,11 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
+	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
 	}
 	template := input.SchedulingTemplate
@@ -838,6 +847,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

apply patch
patch: completed
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..8b650f08bb7514c7a04689f1ff73b0e2249e863c
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -744,6 +748,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,7 +828,11 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
+	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
 	}
 	template := input.SchedulingTemplate
@@ -838,6 +847,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..8b650f08bb7514c7a04689f1ff73b0e2249e863c
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -744,6 +748,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,7 +828,11 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
+	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
 	}
 	template := input.SchedulingTemplate
@@ -838,6 +847,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

apply patch
patch: completed
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/deploy/kubernetes/monitoring/prometheus-rules.yaml
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/docs/contracts/worker-pool-placement-v1.md
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..3ad2b654ab1a947e4375642694b0204e9254fc08
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,12 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +156,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +193,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..8b650f08bb7514c7a04689f1ff73b0e2249e863c
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -744,6 +748,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,7 +828,11 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
+	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
 	}
 	template := input.SchedulingTemplate
@@ -838,6 +847,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..3ad2b654ab1a947e4375642694b0204e9254fc08
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,12 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +156,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +193,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..8b650f08bb7514c7a04689f1ff73b0e2249e863c
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -744,6 +748,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,7 +828,11 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
+	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
 	}
 	template := input.SchedulingTemplate
@@ -838,6 +847,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

exec
/bin/zsh -lc "rg -n 'kubernetesWarmPodPlans\\(' services/control-plane/internal/executiontargets" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
services/control-plane/internal/executiontargets/kubernetes_warm_pool.go:322:func kubernetesWarmPodPlans(
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1623:	plans, _, err := kubernetesWarmPodPlans(
services/control-plane/internal/executiontargets/kubernetes_reconciler.go:619:	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
services/control-plane/internal/executiontargets/kubernetes_reconciler.go:808:	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..3ad2b654ab1a947e4375642694b0204e9254fc08
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,12 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +156,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +193,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..8b650f08bb7514c7a04689f1ff73b0e2249e863c
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -744,6 +748,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,7 +828,11 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
+	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
 	}
 	template := input.SchedulingTemplate
@@ -838,6 +847,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

apply patch
patch: completed
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/placement/service.go
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/placement/service_test.go
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..3ad2b654ab1a947e4375642694b0204e9254fc08
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,12 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +156,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +193,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..3ad2b654ab1a947e4375642694b0204e9254fc08
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,12 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +156,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +193,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

exec
/bin/zsh -lc "rg -n -C 2 'MinIdleUnits' services/control-plane/internal/placement/service.go services/control-plane/internal/placement/service_test.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
services/control-plane/internal/placement/service_test.go-287-		Namespace:          "workers",
services/control-plane/internal/placement/service_test.go-288-		DesiredIdleUnits:   1,
services/control-plane/internal/placement/service_test.go:289:		MinIdleUnits:       &minIdleUnits,
services/control-plane/internal/placement/service_test.go-290-		MaxActiveUnits:     3,
services/control-plane/internal/placement/service_test.go-291-		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
--
services/control-plane/internal/placement/service_test.go-295-		t.Fatal(err)
services/control-plane/internal/placement/service_test.go-296-	}
services/control-plane/internal/placement/service_test.go:297:	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
services/control-plane/internal/placement/service_test.go-298-		t.Fatalf("created pool = %#v", created)
services/control-plane/internal/placement/service_test.go-299-	}
--
services/control-plane/internal/placement/service_test.go-374-}
services/control-plane/internal/placement/service_test.go-375-
services/control-plane/internal/placement/service_test.go:376:func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
services/control-plane/internal/placement/service_test.go-377-	tests := []struct {
services/control-plane/internal/placement/service_test.go-378-		name        string
--
services/control-plane/internal/placement/service_test.go-391-			normalized, err := normalizePoolInput(CreatePoolInput{
services/control-plane/internal/placement/service_test.go-392-				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
services/control-plane/internal/placement/service_test.go:393:				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
services/control-plane/internal/placement/service_test.go-394-				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
services/control-plane/internal/placement/service_test.go-395-			})
--
services/control-plane/internal/placement/service_test.go-403-				t.Fatal(err)
services/control-plane/internal/placement/service_test.go-404-			}
services/control-plane/internal/placement/service_test.go:405:			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
services/control-plane/internal/placement/service_test.go:406:				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
services/control-plane/internal/placement/service_test.go-407-			}
services/control-plane/internal/placement/service_test.go-408-		})
--
services/control-plane/internal/placement/service.go-48-	Namespace          string         `json:"namespace"`
services/control-plane/internal/placement/service.go-49-	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
services/control-plane/internal/placement/service.go:50:	MinIdleUnits       int            `json:"minIdleUnits"`
services/control-plane/internal/placement/service.go-51-	MaxActiveUnits     int            `json:"maxActiveUnits"`
services/control-plane/internal/placement/service.go-52-	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
--
services/control-plane/internal/placement/service.go-88-	Namespace          string         `json:"namespace"`
services/control-plane/internal/placement/service.go-89-	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
services/control-plane/internal/placement/service.go:90:	MinIdleUnits       *int           `json:"minIdleUnits"`
services/control-plane/internal/placement/service.go-91-	MaxActiveUnits     int            `json:"maxActiveUnits"`
services/control-plane/internal/placement/service.go-92-	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
--
services/control-plane/internal/placement/service.go-276-			Namespace:          normalized.Namespace,
services/control-plane/internal/placement/service.go-277-			DesiredIdleUnits:   normalized.DesiredIdleUnits,
services/control-plane/internal/placement/service.go:278:			MinIdleUnits:       *normalized.MinIdleUnits,
services/control-plane/internal/placement/service.go-279-			MaxActiveUnits:     normalized.MaxActiveUnits,
services/control-plane/internal/placement/service.go-280-			SchedulingTemplate: normalized.SchedulingTemplate,
--
services/control-plane/internal/placement/service.go-368-		current.Namespace = normalized.Namespace
services/control-plane/internal/placement/service.go-369-		current.DesiredIdleUnits = normalized.DesiredIdleUnits
services/control-plane/internal/placement/service.go:370:		current.MinIdleUnits = *normalized.MinIdleUnits
services/control-plane/internal/placement/service.go-371-		current.MaxActiveUnits = normalized.MaxActiveUnits
services/control-plane/internal/placement/service.go-372-		current.SchedulingTemplate = normalized.SchedulingTemplate
--
services/control-plane/internal/placement/service.go-550-		Namespace:          "",
services/control-plane/internal/placement/service.go-551-		DesiredIdleUnits:   0,
services/control-plane/internal/placement/service.go:552:		MinIdleUnits:       0,
services/control-plane/internal/placement/service.go-553-		MaxActiveUnits:     1,
services/control-plane/internal/placement/service.go-554-		SchedulingTemplate: map[string]any{},
--
services/control-plane/internal/placement/service.go-666-		Namespace:          "",
services/control-plane/internal/placement/service.go-667-		DesiredIdleUnits:   0,
services/control-plane/internal/placement/service.go:668:		MinIdleUnits:       0,
services/control-plane/internal/placement/service.go-669-		MaxActiveUnits:     1,
services/control-plane/internal/placement/service.go-670-		SchedulingTemplate: map[string]any{},
--
services/control-plane/internal/placement/service.go-751-		Namespace:          model.Namespace,
services/control-plane/internal/placement/service.go-752-		DesiredIdleUnits:   model.DesiredIdleUnits,
services/control-plane/internal/placement/service.go:753:		MinIdleUnits:       model.MinIdleUnits,
services/control-plane/internal/placement/service.go-754-		MaxActiveUnits:     model.MaxActiveUnits,
services/control-plane/internal/placement/service.go-755-		SchedulingTemplate: template,
--
services/control-plane/internal/placement/service.go-832-	}
services/control-plane/internal/placement/service.go-833-	minIdleUnits := 0
services/control-plane/internal/placement/service.go:834:	if input.MinIdleUnits != nil {
services/control-plane/internal/placement/service.go:835:		minIdleUnits = *input.MinIdleUnits
services/control-plane/internal/placement/service.go-836-	}
services/control-plane/internal/placement/service.go-837-	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
--
services/control-plane/internal/placement/service.go-850-		Namespace:          strings.TrimSpace(input.Namespace),
services/control-plane/internal/placement/service.go-851-		DesiredIdleUnits:   input.DesiredIdleUnits,
services/control-plane/internal/placement/service.go:852:		MinIdleUnits:       &minIdleUnits,
services/control-plane/internal/placement/service.go-853-		MaxActiveUnits:     input.MaxActiveUnits,
services/control-plane/internal/placement/service.go-854-		SchedulingTemplate: template,

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..3ad2b654ab1a947e4375642694b0204e9254fc08
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,12 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +156,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +193,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

apply patch
patch: completed
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..3ad2b654ab1a947e4375642694b0204e9254fc08
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,12 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +156,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +193,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..3ad2b654ab1a947e4375642694b0204e9254fc08
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,12 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +156,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +193,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

apply patch
patch: completed
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/warmcapacity/service_test.go
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..3ad2b654ab1a947e4375642694b0204e9254fc08
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,12 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +156,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +193,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..3ad2b654ab1a947e4375642694b0204e9254fc08
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,12 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +156,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +193,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

exec
/bin/zsh -lc "rg -n -A 16 'Observe\\(context.Background\\(\\), Observation\\{' services/control-plane/internal/warmcapacity/service_test.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
22:	observed, err := fixture.service.Observe(context.Background(), Observation{
23-		TenantID:                fixture.tenantID,
24-		ExecutionTargetID:       fixture.target.ID,
25-		WorkerPoolID:            fixture.pool.ID,
26-		WorkerPoolVersion:       fixture.pool.Version,
27-		WarmSupported:           true,
28-		WorkerReleaseRevisionID: &fixture.release.ID,
29-		WorkerReleaseChannel:    &channel,
30-		MinIdleUnits:            fixture.pool.MinIdleUnits,
31-		DesiredTotalUnits:       3,
32-		ClaimedUnits:            1,
33-		ReadyIdleUnits:          2,
34-		Source:                  "reconciler/orbstack",
35-		Reason:                  stringPointer("healthy"),
36-		ObservedAt:              observedAt,
37-		TTL:                     time.Minute,
38-	})
--
71:	advanced, err := fixture.service.Observe(context.Background(), Observation{
72-		TenantID:                fixture.tenantID,
73-		ExecutionTargetID:       fixture.target.ID,
74-		WorkerPoolID:            fixture.pool.ID,
75-		WorkerPoolVersion:       fixture.pool.Version,
76-		WarmSupported:           true,
77-		WorkerReleaseRevisionID: &fixture.release.ID,
78-		WorkerReleaseChannel:    &channel,
79-		MinIdleUnits:            fixture.pool.MinIdleUnits,
80-		DesiredTotalUnits:       4,
81-		ClaimedUnits:            2,
82-		ReadyIdleUnits:          1,
83-		Source:                  "reconciler/orbstack",
84-		ObservedAt:              secondObservedAt,
85-		TTL:                     2 * time.Minute,
86-	})
87-	if err != nil {
--
212:	if _, err := fixture.service.Observe(context.Background(), Observation{
213-		TenantID:                fixture.tenantID,
214-		ExecutionTargetID:       fixture.target.ID,
215-		WorkerPoolID:            fixture.pool.ID,
216-		WorkerPoolVersion:       fixture.pool.Version,
217-		WarmSupported:           true,
218-		WorkerReleaseRevisionID: &fixture.release.ID,
219-		WorkerReleaseChannel:    &channel,
220-		MinIdleUnits:            fixture.pool.MinIdleUnits,
221-		DesiredTotalUnits:       2,
222-		ClaimedUnits:            1,
223-		ReadyIdleUnits:          1,
224-		Source:                  "reconciler/test",
225-		ObservedAt:              fixture.now,
226-		TTL:                     20 * time.Second,
227-	}); err != nil {
228-		t.Fatal(err)

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..3ad2b654ab1a947e4375642694b0204e9254fc08
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,12 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +156,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +193,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

apply patch
patch: completed
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..3ad2b654ab1a947e4375642694b0204e9254fc08
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,12 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +156,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +193,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..3ad2b654ab1a947e4375642694b0204e9254fc08
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,12 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +156,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +193,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

apply patch
patch: completed
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..3ad2b654ab1a947e4375642694b0204e9254fc08
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,12 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +156,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +193,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..3ad2b654ab1a947e4375642694b0204e9254fc08
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,12 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +156,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +193,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

apply patch
patch: completed
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..3ad2b654ab1a947e4375642694b0204e9254fc08
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,12 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +156,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +193,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..3ad2b654ab1a947e4375642694b0204e9254fc08
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,12 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +156,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +193,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

apply patch
patch: completed
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..3ad2b654ab1a947e4375642694b0204e9254fc08
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,12 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +156,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +193,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..3ad2b654ab1a947e4375642694b0204e9254fc08
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,12 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +156,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +193,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

apply patch
patch: completed
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/docs/contracts/worker-pool-placement-v1.md
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

exec
/bin/zsh -lc "rg -n 'MinIdleUnits' services/control-plane | sort" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:188:		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go:36:		MinIdleUnits:            fixture.pool.MinIdleUnits,
services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go:26:		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go:37:	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go:44:		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
services/control-plane/internal/executiontargets/kubernetes_reconciler.go:1228:		MinIdleUnits       int       `gorm:"column:min_idle_units"`
services/control-plane/internal/executiontargets/kubernetes_reconciler.go:1253:			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1717:		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2286:		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:790:	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
services/control-plane/internal/executiontargets/kubernetes_warm_pool.go:115:			MinIdleUnits:      pool.MinIdleUnits,
services/control-plane/internal/executiontargets/kubernetes_warm_pool.go:26:	MinIdleUnits       int            `gorm:"column:min_idle_units"`
services/control-plane/internal/executiontargets/kubernetes_warm_pool.go:355:			if slot < pool.MinIdleUnits {
services/control-plane/internal/executiontargets/kubernetes_warm_pool.go:88:	return plan.Slot < plan.Pool.MinIdleUnits
services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go:23:	MinIdleUnits            int
services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go:77:		MinIdleUnits:            observation.MinIdleUnits,
services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go:34:		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go:49:	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
services/control-plane/internal/observability/distributed_routing_billing_metrics.go:38:	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
services/control-plane/internal/observability/distributed_routing_billing_metrics.go:603:		totals.minIdle += row.MinIdleUnits
services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go:116:		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
services/control-plane/internal/persistence/placement_models.go:20:	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
services/control-plane/internal/persistence/warm_capacity_models.go:19:	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
services/control-plane/internal/placement/service.go:278:			MinIdleUnits:       *normalized.MinIdleUnits,
services/control-plane/internal/placement/service.go:370:		current.MinIdleUnits = *normalized.MinIdleUnits
services/control-plane/internal/placement/service.go:50:	MinIdleUnits       int            `json:"minIdleUnits"`
services/control-plane/internal/placement/service.go:552:		MinIdleUnits:       0,
services/control-plane/internal/placement/service.go:668:		MinIdleUnits:       0,
services/control-plane/internal/placement/service.go:753:		MinIdleUnits:       model.MinIdleUnits,
services/control-plane/internal/placement/service.go:834:	if input.MinIdleUnits != nil {
services/control-plane/internal/placement/service.go:835:		minIdleUnits = *input.MinIdleUnits
services/control-plane/internal/placement/service.go:852:		MinIdleUnits:       &minIdleUnits,
services/control-plane/internal/placement/service.go:90:	MinIdleUnits       *int           `json:"minIdleUnits"`
services/control-plane/internal/placement/service_test.go:289:		MinIdleUnits:       &minIdleUnits,
services/control-plane/internal/placement/service_test.go:297:	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
services/control-plane/internal/placement/service_test.go:376:func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
services/control-plane/internal/placement/service_test.go:393:				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
services/control-plane/internal/placement/service_test.go:405:			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
services/control-plane/internal/placement/service_test.go:406:				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
services/control-plane/internal/warmcapacity/service.go:118:		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
services/control-plane/internal/warmcapacity/service.go:121:		if input.MinIdleUnits != pool.MinIdleUnits {
services/control-plane/internal/warmcapacity/service.go:156:				MinIdleUnits:            input.MinIdleUnits,
services/control-plane/internal/warmcapacity/service.go:185:		result.MinIdleUnits = input.MinIdleUnits
services/control-plane/internal/warmcapacity/service.go:203:			"min_idle_units":             result.MinIdleUnits,
services/control-plane/internal/warmcapacity/service.go:38:	MinIdleUnits            int
services/control-plane/internal/warmcapacity/service_test.go:119:		MinIdleUnits:      fixture.pool.MinIdleUnits,
services/control-plane/internal/warmcapacity/service_test.go:159:				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
services/control-plane/internal/warmcapacity/service_test.go:220:		MinIdleUnits:            fixture.pool.MinIdleUnits,
services/control-plane/internal/warmcapacity/service_test.go:30:		MinIdleUnits:            fixture.pool.MinIdleUnits,
services/control-plane/internal/warmcapacity/service_test.go:332:		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
services/control-plane/internal/warmcapacity/service_test.go:44:		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
services/control-plane/internal/warmcapacity/service_test.go:79:		MinIdleUnits:            fixture.pool.MinIdleUnits,

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

codex
Core code, schema, metrics, alert, and contract updates are in place. I’ve also added the requested focused cases for validation, split/budget priority, eviction exemption, fresh-only deficit aggregation, and SQLite scope enforcement; next I’m formatting and running narrow tests to catch integration issues before the full verification pass.
exec
/bin/zsh -lc 'gofmt -w internal/persistence/placement_models.go internal/persistence/warm_capacity_models.go internal/placement/service.go internal/placement/service_test.go internal/database/store.go internal/database/worker_pool_warm_capacity_sqlite_test.go internal/database/worker_pool_warm_capacity_migration_integration_test.go internal/executiontargets/kubernetes_warm_pool.go internal/executiontargets/kubernetes_reconciler.go internal/executiontargets/kubernetes_reconciler_test.go internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go internal/warmcapacity/service.go internal/warmcapacity/service_test.go internal/observability/distributed_routing_billing_metrics.go internal/observability/distributed_routing_billing_metrics_test.go' in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane
 succeeded in 0ms:
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

exec
/bin/zsh -lc 'go test ./internal/placement ./internal/database ./internal/executiontargets ./internal/warmcapacity ./internal/observability' in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

 succeeded in 8180ms:
ok  	github.com/synara-ai/synara/services/control-plane/internal/placement	0.717s
ok  	github.com/synara-ai/synara/services/control-plane/internal/database	5.086s
ok  	github.com/synara-ai/synara/services/control-plane/internal/executiontargets	6.369s
ok  	github.com/synara-ai/synara/services/control-plane/internal/warmcapacity	0.701s
ok  	github.com/synara-ai/synara/services/control-plane/internal/observability	1.994s

exec
/bin/zsh -lc 'git diff -- services/control-plane/internal/database/store.go services/control-plane/migrations/000086_worker_pool_min_idle.sql services/control-plane/internal/observability/distributed_routing_billing_metrics.go' in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b..e04316b6 100644
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@ func migrateSQLiteSafety(ctx context.Context, db *gorm.DB) error {
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@ func migrateSQLiteSafety(ctx context.Context, db *gorm.DB) error {
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@ func migrateSQLiteSafety(ctx context.Context, db *gorm.DB) error {
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@ func migrateSQLiteSafety(ctx context.Context, db *gorm.DB) error {
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb6..26c9532d 100644
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@ type warmCapacityAuthorityGroup struct {
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@ func (r *Registry) writeWorkerPoolWarmCapacityMetrics(
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@ func (r *Registry) writeWorkerPoolWarmCapacityMetrics(
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@ func (r *Registry) writeWorkerPoolWarmCapacityMetrics(
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 

exec
/bin/zsh -lc "rg -n 'desiredIdleUnits|maxActiveUnits' services/control-plane/internal/httpapi services/control-plane | head -n 120" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
services/control-plane/internal/placement/service.go:49:	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
services/control-plane/internal/placement/service.go:51:	MaxActiveUnits     int            `json:"maxActiveUnits"`
services/control-plane/internal/placement/service.go:89:	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
services/control-plane/internal/placement/service.go:91:	MaxActiveUnits     int            `json:"maxActiveUnits"`
services/control-plane/internal/placement/service.go:838:		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
services/control-plane/internal/httpapi/placement_api_test.go:69:		"desiredIdleUnits":   2,
services/control-plane/internal/httpapi/placement_api_test.go:70:		"maxActiveUnits":     6,
services/control-plane/internal/httpapi/placement_api_test.go:100:		"desiredIdleUnits":   3,
services/control-plane/internal/httpapi/placement_api_test.go:101:		"maxActiveUnits":     8,
services/control-plane/internal/httpapi/placement_api_test.go:174:			"desiredIdleUnits":   1,
services/control-plane/internal/httpapi/placement_api_test.go:175:			"maxActiveUnits":     4,
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2268:	desiredIdleUnits, maxActiveUnits int,
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2271:	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2277:	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:2286:		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
services/control-plane/internal/httpapi/placement_api_test.go:69:		"desiredIdleUnits":   2,
services/control-plane/internal/httpapi/placement_api_test.go:70:		"maxActiveUnits":     6,
services/control-plane/internal/httpapi/placement_api_test.go:100:		"desiredIdleUnits":   3,
services/control-plane/internal/httpapi/placement_api_test.go:101:		"maxActiveUnits":     8,
services/control-plane/internal/httpapi/placement_api_test.go:174:			"desiredIdleUnits":   1,
services/control-plane/internal/httpapi/placement_api_test.go:175:			"maxActiveUnits":     4,

exec
/bin/zsh -lc 'git diff --stat
git diff -- services/control-plane/internal/executiontargets/kubernetes_warm_pool.go services/control-plane/internal/executiontargets/kubernetes_reconciler.go' in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
 deploy/kubernetes/monitoring/prometheus-rules.yaml |  33 ++++++
 docs/contracts/worker-pool-placement-v1.md         |  23 +++-
 services/control-plane/internal/database/store.go  |  72 +++++++++++-
 ...ool_warm_capacity_migration_integration_test.go |  17 ++-
 .../executiontargets/kubernetes_reconciler.go      |  70 ++++++------
 .../executiontargets/kubernetes_reconciler_test.go | 122 ++++++++++++++++++++-
 .../executiontargets/kubernetes_warm_pool.go       |  23 +++-
 .../managed_kubernetes_warm_capacity_publisher.go  |   2 +
 ...aged_kubernetes_warm_capacity_publisher_test.go |   6 +-
 .../distributed_routing_billing_metrics.go         |  33 +++++-
 .../distributed_routing_billing_metrics_test.go    |  27 +++--
 .../internal/persistence/placement_models.go       |   1 +
 .../internal/persistence/warm_capacity_models.go   |   1 +
 .../control-plane/internal/placement/service.go    |  18 ++-
 .../internal/placement/service_test.go             |  42 ++++++-
 .../control-plane/internal/warmcapacity/service.go |  10 ++
 .../internal/warmcapacity/service_test.go          |  14 ++-
 17 files changed, 447 insertions(+), 67 deletions(-)
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb..35abfe12 100644
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@ func (r *KubernetesReconciler) reconcileTarget(ctx context.Context, target persi
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@ func (r *KubernetesReconciler) reconcileTarget(ctx context.Context, target persi
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@ func (r *KubernetesReconciler) reconcileTarget(ctx context.Context, target persi
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@ func (r *KubernetesReconciler) reconcileTarget(ctx context.Context, target persi
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@ func (r *KubernetesReconciler) reconcileTarget(ctx context.Context, target persi
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@ func (r *KubernetesReconciler) reconcileTarget(ctx context.Context, target persi
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@ func (r *KubernetesReconciler) loadKubernetesWarmPools(ctx context.Context, targ
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@ func (r *KubernetesReconciler) loadKubernetesWarmPools(ctx context.Context, targ
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689f..e021224e 100644
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@ type kubernetesWarmPool struct {
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@ type kubernetesWarmDemandEvictionCandidate struct {
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@ func managedKubernetesWarmCapacityObservations(
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@ func kubernetesWarmPodPlans(
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@ func kubernetesWarmPodPlans(
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {

exec
/bin/zsh -lc 'git diff --check
git status --short' in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
 M deploy/kubernetes/monitoring/prometheus-rules.yaml
 M docs/contracts/worker-pool-placement-v1.md
 M services/control-plane/internal/database/store.go
 M services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
 M services/control-plane/internal/executiontargets/kubernetes_reconciler.go
 M services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
 M services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
 M services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
 M services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
 M services/control-plane/internal/observability/distributed_routing_billing_metrics.go
 M services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
 M services/control-plane/internal/persistence/placement_models.go
 M services/control-plane/internal/persistence/warm_capacity_models.go
 M services/control-plane/internal/placement/service.go
 M services/control-plane/internal/placement/service_test.go
 M services/control-plane/internal/warmcapacity/service.go
 M services/control-plane/internal/warmcapacity/service_test.go
?? .codex-brief-increment-b.md
?? .codex-log-increment-b.txt
?? docs/plans/fast-cloud-agents-implementation.md
?? services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
?? services/control-plane/migrations/000086_worker_pool_min_idle.sql

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

exec
/bin/zsh -lc "sed -n '1,220p' services/control-plane/internal/httpapi/placement_api_test.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
package httpapi

import (
	"encoding/json"
	"net/http"
	"net/http/httptest"
	"strings"
	"testing"
	"time"

	"github.com/google/uuid"

	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
	"github.com/synara-ai/synara/services/control-plane/internal/placement"
)

func TestExecutionPlacementRoutesEnforceTenantReadManageAndCAS(t *testing.T) {
	fixture := newWorkerManifestHTTPFixture(t)
	now := time.Now().UTC()
	kubernetesTargetID := uuid.New()
	if err := fixture.db.Create(&persistence.ExecutionTarget{
		ID: kubernetesTargetID, TenantID: &fixture.tenantID, Kind: "kubernetes",
		Name: "placement-kubernetes", Status: "active",
		ConfigurationEncrypted: []byte("{}"), Capabilities: map[string]any{},
		CreatedAt: now, UpdatedAt: now,
	}).Error; err != nil {
		t.Fatal(err)
	}
	fixture.targetID = kubernetesTargetID
	basePath := fixture.ownerPlacementPath()

	for _, tc := range []struct {
		name   string
		token  string
		status int
		code   string
	}{
		{name: "unauthenticated list", status: http.StatusUnauthorized, code: "authentication_required"},
		{name: "active tenant mismatch list", token: fixture.crossTenantToken, status: http.StatusNotFound, code: "tenant_not_found"},
		{name: "missing worker read list", token: fixture.memberToken, status: http.StatusForbidden, code: "tenant_forbidden"},
	} {
		t.Run(tc.name, func(t *testing.T) {
			assertProblemResponse(
				t,
				fixture.placementRequest(t, http.MethodGet, basePath, tc.token, ""),
				tc.status,
				tc.code,
			)
		})
	}

	readable := fixture.placementRequest(t, http.MethodGet, basePath, fixture.readOnlyToken, "")
	if readable.Code != http.StatusOK {
		t.Fatalf("read-only list status = %d, body = %s", readable.Code, readable.Body.String())
	}
	var initial placement.State
	decodePlacementHTTPResponse(t, readable, &initial)
	if len(initial.Pools) != 1 || initial.Policy.Version != 1 || initial.Pools[0].Name != "default" {
		t.Fatalf("initial placement state = %#v", initial)
	}

	createBody := placementHTTPJSON(t, map[string]any{
		"name":               "interactive-warm",
		"mode":               "warm",
		"capacityClass":      "interactive",
		"clusterId":          "",
		"region":             "",
		"namespace":          "",
		"desiredIdleUnits":   2,
		"maxActiveUnits":     6,
		"schedulingTemplate": map[string]any{},
		"status":             "active",
	})
	assertProblemResponse(
		t,
		fixture.placementRequest(t, http.MethodPost, basePath, fixture.readOnlyToken, createBody),
		http.StatusForbidden,
		"tenant_forbidden",
	)

	createdResponse := fixture.placementRequest(t, http.MethodPost, basePath, fixture.ownerToken, createBody)
	if createdResponse.Code != http.StatusCreated {
		t.Fatalf("create status = %d, body = %s", createdResponse.Code, createdResponse.Body.String())
	}
	var created placement.Pool
	decodePlacementHTTPResponse(t, createdResponse, &created)
	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
		t.Fatalf("created pool = %#v", created)
	}

	patchPath := basePath + "/" + created.ID.String()
	patchBody := placementHTTPJSON(t, map[string]any{
		"expectedVersion":    1,
		"name":               "interactive-warm-primary",
		"mode":               "warm",
		"capacityClass":      "interactive",
		"clusterId":          "cluster-a",
		"region":             "cn-east-1",
		"namespace":          "synara-workers",
		"desiredIdleUnits":   3,
		"maxActiveUnits":     8,
		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
		"status":             "draining",
	})
	assertProblemResponse(
		t,
		fixture.placementRequest(t, http.MethodPatch, patchPath, fixture.readOnlyToken, patchBody),
		http.StatusForbidden,
		"tenant_forbidden",
	)
	updatedResponse := fixture.placementRequest(t, http.MethodPatch, patchPath, fixture.ownerToken, patchBody)
	if updatedResponse.Code != http.StatusOK {
		t.Fatalf("patch status = %d, body = %s", updatedResponse.Code, updatedResponse.Body.String())
	}
	var updated placement.Pool
	decodePlacementHTTPResponse(t, updatedResponse, &updated)
	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
		t.Fatalf("updated pool = %#v", updated)
	}

	policyPath := basePath[:len(basePath)-len("/worker-pools")] + "/placement-policy"
	assertProblemResponse(
		t,
		fixture.placementRequest(t, http.MethodPut, policyPath, fixture.readOnlyToken, placementHTTPJSON(t, map[string]any{
			"expectedVersion": 1,
			"defaultPoolId":   initial.Policy.DefaultPoolID,
		})),
		http.StatusForbidden,
		"tenant_forbidden",
	)
	assertProblemResponse(
		t,
		fixture.placementRequest(t, http.MethodPut, policyPath, fixture.ownerToken, placementHTTPJSON(t, map[string]any{
			"expectedVersion": 1,
			"defaultPoolId":   uuid.NewString(),
		})),
		http.StatusConflict,
		"execution_placement_pool_not_found",
	)

	updatePolicyBody := placementHTTPJSON(t, map[string]any{
		"expectedVersion":  initial.Policy.Version,
		"defaultPoolId":    initial.Policy.DefaultPoolID,
		"balancedPoolId":   updated.ID,
		"lowLatencyPoolId": updated.ID,
	})
	policyResponse := fixture.placementRequest(t, http.MethodPut, policyPath, fixture.ownerToken, updatePolicyBody)
	if policyResponse.Code != http.StatusOK {
		t.Fatalf("policy update status = %d, body = %s", policyResponse.Code, policyResponse.Body.String())
	}
	var updatedState placement.State
	decodePlacementHTTPResponse(t, policyResponse, &updatedState)
	if updatedState.Policy.Version != 2 || updatedState.Policy.BalancedPoolID == nil ||
		updatedState.Policy.LowLatencyPoolID == nil || *updatedState.Policy.LowLatencyPoolID != updated.ID {
		t.Fatalf("updated placement state = %#v", updatedState)
	}

	assertProblemResponse(
		t,
		fixture.placementRequest(t, http.MethodPut, policyPath, fixture.ownerToken, updatePolicyBody),
		http.StatusConflict,
		"execution_placement_policy_version_conflict",
	)
	assertProblemResponse(
		t,
		fixture.placementRequest(t, http.MethodPatch, patchPath, fixture.ownerToken, placementHTTPJSON(t, map[string]any{
			"expectedVersion":    1,
			"name":               "stale-update",
			"mode":               "warm",
			"capacityClass":      "interactive",
			"clusterId":          "cluster-a",
			"region":             "cn-east-1",
			"namespace":          "synara-workers",
			"desiredIdleUnits":   1,
			"maxActiveUnits":     4,
			"schedulingTemplate": map[string]any{},
			"status":             "active",
		})),
		http.StatusConflict,
		"worker_pool_version_conflict",
	)
}

func (f workerManifestHTTPFixture) ownerPlacementPath() string {
	return "/v1/tenants/" + f.tenantID.String() + "/execution-targets/" + f.targetID.String() + "/worker-pools"
}

func (f workerManifestHTTPFixture) placementRequest(
	t *testing.T,
	method, path, token, body string,
) *httptest.ResponseRecorder {
	t.Helper()
	request := httptest.NewRequest(method, path, strings.NewReader(body))
	if token != "" {
		request.AddCookie(&http.Cookie{Name: f.cookieName, Value: token})
	}
	if body != "" {
		request.Header.Set("Content-Type", "application/json")
	}
	response := httptest.NewRecorder()
	f.handler.ServeHTTP(response, request)
	return response
}

func placementHTTPJSON(t *testing.T, value any) string {
	t.Helper()
	body, err := json.Marshal(value)
	if err != nil {
		t.Fatal(err)
	}
	return string(body)
}

func decodePlacementHTTPResponse(t *testing.T, response *httptest.ResponseRecorder, out any) {
	t.Helper()
	if err := json.Unmarshal(response.Body.Bytes(), out); err != nil {
		t.Fatalf("decode body %s: %v", response.Body.String(), err)
	}
}

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

apply patch
patch: completed
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/httpapi/placement_api_test.go
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

exec
/bin/zsh -lc "rg -n 'desired_idle_units' services/control-plane/internal | rg -v '_test.go|placement_models|warm_capacity_models|kubernetes_reconciler.go|service.go|metrics.go|store.go'" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
exec
/bin/zsh -lc "rg -n '000085|85 migration|migration.*85|len\\(.*migration|migrations.*count' services/control-plane/internal services/control-plane/migrations --glob '*.go' --glob '*.sql'" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
services/control-plane/internal/executiontargets/kubernetes_warm_pool.go:25:	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`

 succeeded in 0ms:
services/control-plane/internal/database/stage3_migration_integration_test.go:1285:	if countErr := db.Table("control_plane_schema_migrations").Where("version = ?", 25).Count(&applied).Error; countErr != nil {
services/control-plane/internal/database/ssh_bootstrap_authority_migration_integration_test.go:163:		if err := db.Table("control_plane_schema_migrations").Where("version = ?", version).Count(&count).Error; err != nil {

exec
/bin/zsh -lc "rg -n 'WorkerPool\\{' services/control-plane/internal --glob '*.go' | wc -l
rg -n 'WorkerPoolWarmCapacity\\{' services/control-plane/internal --glob '*.go' | wc -l" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
      40
      20

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

exec
/bin/zsh -lc 'gofmt -w internal/httpapi/placement_api_test.go
git diff --check' in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane
 succeeded in 0ms:
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

exec
/bin/zsh -lc 'git diff -- services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go services/control-plane/internal/httpapi/placement_api_test.go' in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9d..c16cabab 100644
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@ func TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation(t *testing.T) {
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@ func TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation(t *testing.T) {
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@ func seedWorkerPoolWarmCapacityFixture(t *testing.T, db *gorm.DB) workerPoolWarm
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d..09106d5a 100644
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@ func TestKubernetesReconcilerPrioritizesExecutionPodsBeforeWarmPoolsAtTargetCap(
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@ func TestKubernetesReconcilerEvictsNotReadyWarmWorkerForColdDemandAtTargetCap(t
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@ func TestKubernetesReconcilerConfirmsMissingRegisteredPodAfterSuccessfulList(t *
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@ func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2164,6 +2267,15 @@ func (f kubernetesReconcileFixture) createWarmPool(
 	capacityClass string,
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
+) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
 ) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
@@ -2171,7 +2283,7 @@ func (f kubernetesReconcileFixture) createWarmPool(
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498f..91aad631 100644
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@ func TestExecutionPlacementRoutesEnforceTenantReadManageAndCAS(t *testing.T) {
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@ func TestExecutionPlacementRoutesEnforceTenantReadManageAndCAS(t *testing.T) {
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@ func TestExecutionPlacementRoutesEnforceTenantReadManageAndCAS(t *testing.T) {
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@ func TestExecutionPlacementRoutesEnforceTenantReadManageAndCAS(t *testing.T) {
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@ func TestExecutionPlacementRoutesEnforceTenantReadManageAndCAS(t *testing.T) {
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621..809489b3 100644
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@ func TestWorkerPoolWarmCapacityMetricsAreFreshBoundedAndStable(t *testing.T) {
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@ func TestWorkerPoolWarmCapacityMetricsAreFreshBoundedAndStable(t *testing.T) {
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@ func TestWorkerPoolWarmCapacityMetricsAreFreshBoundedAndStable(t *testing.T) {
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@ func warmCapacityMetricFixture(
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@ func warmCapacityMetricFixture(
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}

exec
/bin/zsh -lc 'git diff -- services/control-plane/internal/warmcapacity/service.go services/control-plane/internal/warmcapacity/service_test.go services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go services/control-plane/internal/persistence/warm_capacity_models.go' in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4..d08c624e 100644
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@ type ManagedKubernetesWarmCapacityObservation struct {
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@ func (p *ManagedKubernetesWarmCapacityPublisher) PublishReconcile(
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e11..ceb32d96 100644
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@ import (
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@ func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testin
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@ func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testin
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614a..db52018c 100644
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@ type WorkerPoolWarmCapacity struct {
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513..6c5d0f32 100644
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@ type Observation struct {
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@ func (s *Service) Observe(ctx context.Context, input Observation) (persistence.W
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@ func (s *Service) Observe(ctx context.Context, input Observation) (persistence.W
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@ func (s *Service) Observe(ctx context.Context, input Observation) (persistence.W
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@ func (s *Service) Observe(ctx context.Context, input Observation) (persistence.W
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408..afabf83f 100644
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@ func TestObserveAndGetFreshRoundTrip(t *testing.T) {
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@ func TestObserveAndGetFreshRoundTrip(t *testing.T) {
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@ func TestObserveAndGetFreshRoundTrip(t *testing.T) {
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@ func TestObserveRejectsInvalidScopeAndCounters(t *testing.T) {
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -149,6 +153,13 @@ func TestObserveRejectsInvalidScopeAndCounters(t *testing.T) {
 			},
 			code: "warm_capacity_pool_mode_invalid",
 		},
+		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
 		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
@@ -206,6 +217,7 @@ func TestGetFreshReturnsNilForExpiredMissingOrReleaseMismatch(t *testing.T) {
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@ func newWarmCapacityFixture(t *testing.T) warmCapacityFixture {
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {

exec
/bin/zsh -lc "sed -n '1,180p' services/control-plane/migrations/000086_worker_pool_min_idle.sql
sed -n '1,180p' services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
git diff -- docs/contracts/worker-pool-placement-v1.md deploy/kubernetes/monitoring/prometheus-rules.yaml" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
ALTER TABLE worker_pools
  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;

ALTER TABLE worker_pools
  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
  );

ALTER TABLE worker_pool_warm_capacity
  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;

CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
RETURNS TRIGGER
LANGUAGE plpgsql
AS $$
DECLARE
  pool worker_pools%ROWTYPE;
  target execution_targets%ROWTYPE;
  release worker_release_revisions%ROWTYPE;
BEGIN
  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
  IF NOT FOUND
     OR pool.execution_target_id <> NEW.execution_target_id
     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
     OR pool.version <> NEW.worker_pool_version
     OR pool.mode <> 'warm'
     OR pool.status = 'disabled'
     OR pool.capacity_class <> NEW.capacity_class
     OR pool.desired_idle_units <> NEW.desired_idle_units
     OR pool.min_idle_units <> NEW.min_idle_units
     OR pool.max_active_units <> NEW.max_active_units THEN
    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
  END IF;

  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
  IF NOT FOUND
     OR target.tenant_id IS NULL
     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
     OR target.kind <> 'kubernetes'
     OR target.status <> 'active' THEN
    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
  END IF;

  IF NEW.worker_release_revision_id IS NOT NULL THEN
    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
    IF NOT FOUND
       OR release.execution_target_id <> NEW.execution_target_id
       OR release.tenant_id <> NEW.tenant_id THEN
      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
    END IF;
  END IF;

  RETURN NEW;
END;
$$;
package database

import (
	"testing"
	"time"

	"github.com/google/uuid"

	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
)

func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
	fixture := openStage4SQLiteFixture(t)
	now := time.Now().UTC().Truncate(time.Second)
	target := persistence.ExecutionTarget{
		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
	}
	if err := fixture.db.Create(&target).Error; err != nil {
		t.Fatal(err)
	}
	pool := persistence.WorkerPool{
		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
		CreatedAt: now, UpdatedAt: now,
	}
	if err := fixture.db.Create(&pool).Error; err != nil {
		t.Fatal(err)
	}

	invalidPool := pool
	invalidPool.ID = uuid.New()
	invalidPool.Name = "invalid-guaranteed-warm"
	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")

	authority := persistence.WorkerPoolWarmCapacity{
		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
		CapacityClass: pool.CapacityClass, WarmSupported: true,
		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
	}
	if err := fixture.db.Create(&authority).Error; err != nil {
		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
	}
	assertSQLiteStage4Rejected(
		t,
		fixture.db.Exec(`
			UPDATE worker_pool_warm_capacity
			SET min_idle_units = 0
			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
		).Error,
		"Worker pool warm capacity scope is invalid",
	)
}
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 036ae9bc..cd10ea64 100644
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -141,3 +141,36 @@ spec:
           annotations:
             summary: Synara has expired SSE connection leases
             description: Expired SSE leases remain and should be removed by reconnect or retention cleanup.
+    - name: synara-control-plane.fast-provision-slo
+      rules:
+        - alert: SynaraWarmHitColdStartSLOBreach
+          expr: |
+            max by (target_kind, warm_pool_mode) (
+              synara_execution_cold_start_duration_seconds_30d{warm_pool_result="hit", quantile="p95"}
+            ) > 2.5
+          for: 15m
+          labels:
+            severity: warning
+          annotations:
+            summary: Warm-hit agent start P95 exceeds the interactive SLO
+            description: Trailing-30-day dispatch-to-Provider-ready P95 for warm-pool hits has exceeded 2.5 seconds for 15 minutes; the fast-provision interactive SLO is breached even when warm capacity is available.
+        - alert: SynaraWarmPoolFallbackSurge
+          expr: |
+            sum(synara_warm_pool_acquisitions_30d{result="fallback"})
+              /
+            clamp_min(sum(synara_warm_pool_acquisitions_30d{result=~"hit|fallback"}), 1)
+            > 0.2
+          for: 30m
+          labels:
+            severity: warning
+          annotations:
+            summary: Warm Pool requests are frequently falling back to cold starts
+            description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579..990c0206 100644
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@ Capacity classes begin with `standard` and `interactive`. They are scheduling co
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@ deletion-pending Pods. `worker_pool_warm_capacity` reports only the release-awar
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@ and requested CPU/memory/ephemeral-storage seconds. They deliberately contain no
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound

exec
/bin/zsh -lc 'git diff -- services/control-plane/internal/placement/service.go services/control-plane/internal/placement/service_test.go services/control-plane/internal/httpapi/placement_api_test.go services/control-plane/internal/persistence/placement_models.go' in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498f..91aad631 100644
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@ func TestExecutionPlacementRoutesEnforceTenantReadManageAndCAS(t *testing.T) {
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@ func TestExecutionPlacementRoutesEnforceTenantReadManageAndCAS(t *testing.T) {
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@ func TestExecutionPlacementRoutesEnforceTenantReadManageAndCAS(t *testing.T) {
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@ func TestExecutionPlacementRoutesEnforceTenantReadManageAndCAS(t *testing.T) {
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@ func TestExecutionPlacementRoutesEnforceTenantReadManageAndCAS(t *testing.T) {
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f..89833e35 100644
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@ type WorkerPool struct {
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c008..bf84c9f6 100644
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@ type Pool struct {
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@ type CreatePoolInput struct {
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@ func (s *Service) CreatePool(
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@ func (s *Service) UpdatePool(
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@ func (s *Service) UpdatePool(
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@ func previewDefaultPool(
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@ func (s *Service) ensureDefaultPool(
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@ func toPool(model persistence.WorkerPool) Pool {
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,8 +830,12 @@ func normalizePoolInput(input CreatePoolInput) (CreatePoolInput, error) {
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
+	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
 	}
 	template := input.SchedulingTemplate
 	if template == nil {
@@ -838,6 +849,7 @@ func normalizePoolInput(input CreatePoolInput) (CreatePoolInput, error) {
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21..b8b233e4 100644
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@ func TestCreateAndUpdatePoolValidateAndEnforceCAS(t *testing.T) {
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@ func TestCreateAndUpdatePoolValidateAndEnforceCAS(t *testing.T) {
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@ func TestCreateAndUpdatePoolValidateAndEnforceCAS(t *testing.T) {
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@ func TestCreateAndUpdatePoolValidateAndEnforceCAS(t *testing.T) {
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@ func (f placementFixture) createPool(
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

Implementation and focused tests are complete; the initial package-level test pass succeeded. Starting the user-requested final Go build and full scoped test command.
  ✓ Read frozen plan, relevant memory, Git state, and existing implementation/tests
  ✓ Implement migration, SQLite parity, models, placement API, reconciler, capacity writer, metrics, alert, and contract docs
  ✓ Add focused tests for validation, split/budget, eviction, metrics freshness, and SQLite scope parity
  → Run requested Go build/tests, fix failures, and write .codex-report-increment-b.md
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

codex
The focused package pass is green. I’m now running the requested final verification exactly as specified—`go build ./...` followed by the scoped recursive `go test` command—and will use that final output verbatim in the report.
exec
/bin/zsh -lc 'go build ./...' in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

 exited 1 in 165ms:
pattern ./...: open /Users/huang/Library/Caches/go-build/6b/6b19028cf5f2ad7387f41c933178f6b2d2102964c62ebf2852b9ebf34e48fdaf-d: operation not permitted

codex
The build hit the macOS Go cache outside the writable sandbox (`~/Library/Caches/go-build`), not a code failure. I’m rerunning the same Go verification with `GOCACHE` redirected to `/private/tmp`; no repository files or verification scope change.
exec
/bin/zsh -lc 'mkdir -p /private/tmp/synara-increment-b-go-cache
GOCACHE=/private/tmp/synara-increment-b-go-cache go build ./...' in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

 succeeded in 12471ms:
exec
/bin/zsh -lc 'GOCACHE=/private/tmp/synara-increment-b-go-cache go test ./internal/placement/... ./internal/executiontargets/... ./internal/warmcapacity/... ./internal/observability/... ./internal/persistence/... ./internal/database/... ./internal/httpapi/...' in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

 exited 1 in 10054ms:
ok  	github.com/synara-ai/synara/services/control-plane/internal/placement	1.668s
--- FAIL: TestKubernetesClientDeletePodUsesExactUIDPrecondition (0.00s)
panic: httptest: failed to listen on a port: listen tcp6 [::1]:0: bind: operation not permitted [recovered, repanicked]

goroutine 884 [running]:
testing.tRunner.func1.2({0x10588f8a0, 0x4977b1188730})
	/opt/homebrew/Cellar/go/1.26.5/libexec/src/testing/testing.go:1974 +0x1a0
testing.tRunner.func1()
	/opt/homebrew/Cellar/go/1.26.5/libexec/src/testing/testing.go:1977 +0x318
panic({0x10588f8a0?, 0x4977b1188730?})
	/opt/homebrew/Cellar/go/1.26.5/libexec/src/runtime/panic.go:860 +0x12c
net/http/httptest.newLocalListener()
	/opt/homebrew/Cellar/go/1.26.5/libexec/src/net/http/httptest/server.go:72 +0xd4
net/http/httptest.NewUnstartedServer(...)
	/opt/homebrew/Cellar/go/1.26.5/libexec/src/net/http/httptest/server.go:120
net/http/httptest.NewServer({0x105a02ee0, 0x4977b105dd40})
	/opt/homebrew/Cellar/go/1.26.5/libexec/src/net/http/httptest/server.go:107 +0x28
github.com/synara-ai/synara/services/control-plane/internal/executiontargets.TestKubernetesClientDeletePodUsesExactUIDPrecondition(0x4977b0ac5b08)
	/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go:1982 +0xac
testing.tRunner(0x4977b0ac5b08, 0x1059f9728)
	/opt/homebrew/Cellar/go/1.26.5/libexec/src/testing/testing.go:2036 +0xc4
created by testing.(*T).Run in goroutine 1
	/opt/homebrew/Cellar/go/1.26.5/libexec/src/testing/testing.go:2101 +0x3a8
FAIL	github.com/synara-ai/synara/services/control-plane/internal/executiontargets	6.691s
ok  	github.com/synara-ai/synara/services/control-plane/internal/warmcapacity	3.727s
ok  	github.com/synara-ai/synara/services/control-plane/internal/observability	1.954s
ok  	github.com/synara-ai/synara/services/control-plane/internal/persistence	2.642s
ok  	github.com/synara-ai/synara/services/control-plane/internal/database	5.532s
--- FAIL: TestSessionEventStreamCatchesUpAcrossServiceInstances (0.06s)
panic: httptest: failed to listen on a port: listen tcp6 [::1]:0: bind: operation not permitted [recovered, repanicked]

goroutine 1236 [running]:
testing.tRunner.func1.2({0x10352f2a0, 0x25111b065880})
	/opt/homebrew/Cellar/go/1.26.5/libexec/src/testing/testing.go:1974 +0x1a0
testing.tRunner.func1()
	/opt/homebrew/Cellar/go/1.26.5/libexec/src/testing/testing.go:1977 +0x318
panic({0x10352f2a0?, 0x25111b065880?})
	/opt/homebrew/Cellar/go/1.26.5/libexec/src/runtime/panic.go:860 +0x12c
net/http/httptest.newLocalListener()
	/opt/homebrew/Cellar/go/1.26.5/libexec/src/net/http/httptest/server.go:72 +0xd4
net/http/httptest.NewUnstartedServer(...)
	/opt/homebrew/Cellar/go/1.26.5/libexec/src/net/http/httptest/server.go:120
net/http/httptest.NewServer({0x10397a740, 0x25111b4b8540})
	/opt/homebrew/Cellar/go/1.26.5/libexec/src/net/http/httptest/server.go:107 +0x28
github.com/synara-ai/synara/services/control-plane/internal/httpapi.TestSessionEventStreamCatchesUpAcrossServiceInstances(0x25111b1a2fc8)
	/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane/internal/httpapi/session_events_sse_test.go:123 +0xd60
testing.tRunner(0x25111b1a2fc8, 0x10396a1c0)
	/opt/homebrew/Cellar/go/1.26.5/libexec/src/testing/testing.go:2036 +0xc4
created by testing.(*T).Run in goroutine 1
	/opt/homebrew/Cellar/go/1.26.5/libexec/src/testing/testing.go:2101 +0x3a8
FAIL	github.com/synara-ai/synara/services/control-plane/internal/httpapi	5.527s
FAIL

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

codex
The fresh full test run reached two pre-existing `httptest.NewServer` cases, but this environment forbids all loopback binds (`listen tcp6 [::1]:0: operation not permitted`). The feature tests and non-listener packages passed before that. I’m separating code verification from this sandbox limitation by running the new/changed tests explicitly and then the two affected packages with only their socket-listener cases skipped; I won’t alter unrelated tests to mask the environment.
exec
/bin/zsh -lc "rg -n 'httptest\\.New(Server|TLSServer|UnstartedServer)' internal/executiontargets internal/httpapi --glob '*_test.go'" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane
 succeeded in 0ms:
internal/httpapi/session_events_sse_test.go:123:	httpServer := httptest.NewServer(mux)
internal/httpapi/session_events_sse_test.go:244:	httpServer := httptest.NewServer(mux)
internal/executiontargets/kubernetes_reconciler_test.go:1982:	server := httptest.NewServer(http.HandlerFunc(func(writer http.ResponseWriter, request *http.Request) {
internal/executiontargets/kubernetes_reconciler_test.go:2025:	server := httptest.NewServer(http.HandlerFunc(func(writer http.ResponseWriter, request *http.Request) {
internal/executiontargets/kubernetes_reconciler_test.go:2063:	server := httptest.NewServer(http.HandlerFunc(func(writer http.ResponseWriter, request *http.Request) {
internal/executiontargets/kubernetes_reconciler_test.go:2741:	server := httptest.NewServer(http.HandlerFunc(func(writer http.ResponseWriter, request *http.Request) {
internal/executiontargets/kubernetes_workload_identity_test.go:68:	server := httptest.NewTLSServer(http.HandlerFunc(serverState.serveHTTP))
internal/executiontargets/kubernetes_workload_identity_test.go:111:	server := httptest.NewTLSServer(http.HandlerFunc((&kubernetesWorkloadIdentityServerState{
internal/executiontargets/kubernetes_workload_identity_test.go:169:	server := httptest.NewTLSServer(http.HandlerFunc((&kubernetesWorkloadIdentityServerState{
internal/executiontargets/kubernetes_workload_identity_test.go:214:	server := httptest.NewTLSServer(http.HandlerFunc((&kubernetesWorkloadIdentityServerState{
internal/executiontargets/kubernetes_workload_identity_test.go:253:	server := httptest.NewTLSServer(http.HandlerFunc((&kubernetesWorkloadIdentityServerState{
internal/executiontargets/kubernetes_workload_identity_test.go:293:	server := httptest.NewTLSServer(http.HandlerFunc((&kubernetesWorkloadIdentityServerState{
internal/executiontargets/kubernetes_workload_identity_test.go:333:	server := httptest.NewTLSServer(http.HandlerFunc((&kubernetesWorkloadIdentityServerState{
internal/executiontargets/kubernetes_workload_identity_test.go:373:	server := httptest.NewTLSServer(http.HandlerFunc((&kubernetesWorkloadIdentityServerState{
internal/executiontargets/kubernetes_workload_identity_test.go:429:	server := httptest.NewTLSServer(http.HandlerFunc((&kubernetesWorkloadIdentityServerState{
internal/executiontargets/kubernetes_workload_identity_test.go:468:	server := httptest.NewTLSServer(http.HandlerFunc((&kubernetesWorkloadIdentityServerState{

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

exec
/bin/zsh -lc "rg -n '"'^func Test'"' internal/httpapi/session_events_sse_test.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane
 succeeded in 0ms:
32:func TestSessionEventCursorPrefersExplicitSequence(t *testing.T) {
44:func TestSessionEventCursorRejectsInvalidLastEventID(t *testing.T) {
52:func TestWriteSessionEventUsesSequenceAsSSEID(t *testing.T) {
68:func TestSessionEventStreamCatchesUpAcrossServiceInstances(t *testing.T) {
162:func TestSessionEventStreamRechecksInteractionAccessAfterRoleDowngrade(t *testing.T) {
305:func TestSSEWriteAppliesAndClearsDeadline(t *testing.T) {

exec
/bin/zsh -lc "rg -n '"'^func Test'"' internal/executiontargets/kubernetes_reconciler_test.go | awk -F: '"'$1 >= 1950 && $1 <= 2100 || $1 >= 2700 && $1 <= 2770 {print}'"'
rg -n '"'^func Test'"' internal/executiontargets/kubernetes_workload_identity_test.go" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane
 succeeded in 0ms:
1956:func TestKubernetesReconcilerRejectsEmptyNamespacePodUID(t *testing.T) {
1979:func TestKubernetesClientDeletePodUsesExactUIDPrecondition(t *testing.T) {
2022:func TestKubernetesClientListsNamespaceWidePodUIDsForCleanup(t *testing.T) {
2061:func TestKubernetesClientRejectsIncompletePaginatedPodUIDList(t *testing.T) {
2738:func TestKubernetesClientParsesPodFailureEvidence(t *testing.T) {
18:func TestVerifyKubernetesWorkloadIdentity(t *testing.T) {
102:func TestVerifyKubernetesWorkloadIdentityUsesExplicitWorkerModeLabel(t *testing.T) {
161:func TestVerifyKubernetesWorkloadIdentityRejectsMissingWorkerModeLabels(t *testing.T) {
206:func TestVerifyKubernetesWorkloadIdentityRejectsWrongAudience(t *testing.T) {
246:func TestVerifyKubernetesWorkloadIdentityRejectsWrongServiceAccount(t *testing.T) {
285:func TestVerifyKubernetesWorkloadIdentityRejectsWrongPodClaims(t *testing.T) {
325:func TestVerifyKubernetesWorkloadIdentityRejectsReplacedPodUID(t *testing.T) {
365:func TestVerifyKubernetesWorkloadIdentityRejectsTerminatingPod(t *testing.T) {
421:func TestVerifyKubernetesWorkloadIdentityRejectsWrongPodLabels(t *testing.T) {
461:func TestVerifyKubernetesWorkloadIdentitySurfacesTokenReviewAPIFailure(t *testing.T) {

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

exec
/bin/zsh -lc "GOCACHE=/private/tmp/synara-increment-b-go-cache go test -skip '"'^TestSessionEventStream'"' ./internal/httpapi" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane
exec
/bin/zsh -lc "GOCACHE=/private/tmp/synara-increment-b-go-cache go test -skip '"'^(TestKubernetesClient(DeletePodUsesExactUIDPrecondition|ListsNamespaceWidePodUIDsForCleanup|RejectsIncompletePaginatedPodUIDList|ParsesPodFailureEvidence)|TestVerifyKubernetesWorkloadIdentity.*)$'"' ./internal/executiontargets" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

 succeeded in 7516ms:
ok  	github.com/synara-ai/synara/services/control-plane/internal/httpapi	5.045s

 succeeded in 8079ms:
ok  	github.com/synara-ai/synara/services/control-plane/internal/executiontargets	6.554s

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

exec
/bin/zsh -lc "GOCACHE=/private/tmp/synara-increment-b-go-cache go test ./internal/database -run '"'^TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation$'"' -v" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/services/control-plane
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

 succeeded in 2970ms:
=== RUN   TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation
    worker_pool_warm_capacity_migration_integration_test.go:19: SYNARA_TEST_DATABASE_URL is not configured
--- SKIP: TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation (0.00s)
PASS
ok  	github.com/synara-ai/synara/services/control-plane/internal/database	1.675s

diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

exec
/bin/zsh -lc "test -e .codex-report-increment-b.md && sed -n '1,260p' .codex-report-increment-b.md || true" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

codex
Implementation verification is clean except for the sandbox’s loopback-listener restriction; all affected packages pass when only the pre-existing socket-backed tests are skipped, and the PostgreSQL integration lane is unavailable because `SYNARA_TEST_DATABASE_URL` is unset. I’m recording those exact boundaries—not overstating them—in the requested worktree report.
apply patch
patch: completed
/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/.codex-report-increment-b.md
diff --git a/.codex-report-increment-b.md b/.codex-report-increment-b.md
new file mode 100644
index 0000000000000000000000000000000000000000..d0bd133b85b11838edecf52d026b68d345a97d06
--- /dev/null
+++ b/.codex-report-increment-b.md
@@ -0,0 +1,109 @@
+# Increment B implementation report
+
+## Outcome
+
+Implemented guaranteed warm-pool `minIdleUnits` end to end in the Go control plane. Guaranteed slot plans are created
+before cold Execution Pods, remain subject to the target-wide Pod ceiling, are exempt from demand-fallback eviction,
+and project a fresh authority-derived deficit metric and warning alert. The worktree remains uncommitted.
+
+## Changed files
+
+- `services/control-plane/migrations/000086_worker_pool_min_idle.sql`
+  - Adds `worker_pools.min_idle_units`, the named `0 <= min <= desired` check, the authority mirror column, and the
+    `CREATE OR REPLACE` scope assertion.
+- `services/control-plane/internal/persistence/placement_models.go`
+- `services/control-plane/internal/persistence/warm_capacity_models.go`
+  - Adds the GORM model columns for PostgreSQL and SQLite AutoMigrate parity.
+- `services/control-plane/internal/database/store.go`
+- `services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go`
+- `services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go`
+  - Extends SQLite Worker Pool shape checks, adds insert/update authority-scope mirrors, and covers SQLite plus
+    PostgreSQL min-idle mismatch rejection.
+- `services/control-plane/internal/placement/service.go`
+- `services/control-plane/internal/placement/service_test.go`
+- `services/control-plane/internal/httpapi/placement_api_test.go`
+  - Adds request normalization/defaulting, bounds validation, persistence, response projection, and HTTP round-trip
+    coverage. `placement_api.go` already decodes and encodes the placement service DTOs directly, so no separate
+    handler mapper required a code change.
+- `services/control-plane/internal/executiontargets/kubernetes_warm_pool.go`
+- `services/control-plane/internal/executiontargets/kubernetes_reconciler.go`
+- `services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go`
+  - Splits stable plans into guaranteed and best-effort slices, prioritizes guaranteed creation, exempts guaranteed
+    slots from demand eviction, loads min-idle configuration, and tests split, truncation, and eviction behavior.
+- `services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go`
+- `services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go`
+- `services/control-plane/internal/warmcapacity/service.go`
+- `services/control-plane/internal/warmcapacity/service_test.go`
+  - Carries, validates, inserts, and CAS-updates the min-idle authority snapshot.
+- `services/control-plane/internal/observability/distributed_routing_billing_metrics.go`
+- `services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go`
+  - Exports bounded `desired_idle` and `min_idle` unit kinds and fresh-only per-row-clamped warm deficit sums.
+- `deploy/kubernetes/monitoring/prometheus-rules.yaml`
+  - Preserves the pre-existing Increment A rules and appends `SynaraWarmPoolMinIdleDeficit`.
+- `docs/contracts/worker-pool-placement-v1.md`
+  - Documents the guarantee, ceiling, eviction, deficit, and non-reservation/non-waiting boundaries.
+
+## Frozen design decisions applied
+
+- D1: `minIdleUnits` is a non-negative floor inside `desiredIdleUnits`; PostgreSQL authority scope and SQLite safety
+  triggers require the authority snapshot to match its Pool.
+- D2: slot indices `[0, minIdleUnits)` are guaranteed plans and are applied before the cold Pod loop. Remaining plans
+  retain their prior post-cold position. Pod naming and hashing still use the same plan objects in both reconcile passes.
+- D3: only plans with `slot >= minIdleUnits` enter the `warm-pool-demand-fallback` candidate set. Other recycling and
+  identity-safety deletion rules remain unchanged.
+- D4: the target-wide `maxActivePods` budget still caps all creation. Truncating guaranteed plans is not a reconcile
+  error; the published min/ready authority exposes the shortfall.
+- D5: `synara_worker_pool_warm_deficit{capacity_class}` sums
+  `max(0, min_idle_units - ready_idle_units)` per fresh row after bounded class collapse. Existing units also expose the
+  bounded `desired_idle` and `min_idle` kinds.
+- D6: no launch-transaction wait or retry was added. Placement keeps its existing cold-fallback behavior.
+
+## Verification
+
+No Bun command was run.
+
+`GOCACHE=/private/tmp/synara-increment-b-go-cache go build ./...` passed with no output.
+
+The requested unfiltered Go test command was run with the writable `GOCACHE`. All non-listener packages passed, but the
+managed sandbox prohibits `httptest.NewServer` from binding loopback ports. The exact output tail was:
+
+```text
+ok  	github.com/synara-ai/synara/services/control-plane/internal/placement	1.668s
+FAIL	github.com/synara-ai/synara/services/control-plane/internal/executiontargets	6.691s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/warmcapacity	3.727s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/observability	1.954s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/persistence	2.642s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/database	5.532s
+FAIL	github.com/synara-ai/synara/services/control-plane/internal/httpapi	5.527s
+FAIL
+```
+
+Both failures were pre-existing socket-backed tests whose panic was
+`httptest: failed to listen on a port: listen tcp6 [::1]:0: bind: operation not permitted`.
+
+Fresh supplemental package runs skipped only those pre-existing listener-backed cases and passed:
+
+```text
+ok  	github.com/synara-ai/synara/services/control-plane/internal/executiontargets	6.554s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/httpapi	5.045s
+```
+
+The focused PostgreSQL migration test was discovered but skipped because the integration database is not configured:
+
+```text
+=== RUN   TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation
+    worker_pool_warm_capacity_migration_integration_test.go:19: SYNARA_TEST_DATABASE_URL is not configured
+--- SKIP: TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation (0.00s)
+PASS
+ok  	github.com/synara-ai/synara/services/control-plane/internal/database	1.675s
+```
+
+## Remaining risks and intentionally unimplemented edges
+
+- Rerun the exact unfiltered Go test command in an environment that permits loopback test listeners; no code failure was
+  observed before the sandbox bind panics.
+- Run the PostgreSQL integration lane with `SYNARA_TEST_DATABASE_URL` to exercise migration `000086` and its deferred
+  scope trigger against PostgreSQL, not only SQLite/model/service coverage.
+- No live Kubernetes/OrbStack acceptance lane was run in this task; reconcile behavior is covered by the existing fake
+  Kubernetes client tests.
+- The out-of-transaction interactive wait/retry shape remains intentionally deferred per D6.
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

diff --git a/.codex-report-increment-b.md b/.codex-report-increment-b.md
new file mode 100644
index 0000000000000000000000000000000000000000..d0bd133b85b11838edecf52d026b68d345a97d06
--- /dev/null
+++ b/.codex-report-increment-b.md
@@ -0,0 +1,109 @@
+# Increment B implementation report
+
+## Outcome
+
+Implemented guaranteed warm-pool `minIdleUnits` end to end in the Go control plane. Guaranteed slot plans are created
+before cold Execution Pods, remain subject to the target-wide Pod ceiling, are exempt from demand-fallback eviction,
+and project a fresh authority-derived deficit metric and warning alert. The worktree remains uncommitted.
+
+## Changed files
+
+- `services/control-plane/migrations/000086_worker_pool_min_idle.sql`
+  - Adds `worker_pools.min_idle_units`, the named `0 <= min <= desired` check, the authority mirror column, and the
+    `CREATE OR REPLACE` scope assertion.
+- `services/control-plane/internal/persistence/placement_models.go`
+- `services/control-plane/internal/persistence/warm_capacity_models.go`
+  - Adds the GORM model columns for PostgreSQL and SQLite AutoMigrate parity.
+- `services/control-plane/internal/database/store.go`
+- `services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go`
+- `services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go`
+  - Extends SQLite Worker Pool shape checks, adds insert/update authority-scope mirrors, and covers SQLite plus
+    PostgreSQL min-idle mismatch rejection.
+- `services/control-plane/internal/placement/service.go`
+- `services/control-plane/internal/placement/service_test.go`
+- `services/control-plane/internal/httpapi/placement_api_test.go`
+  - Adds request normalization/defaulting, bounds validation, persistence, response projection, and HTTP round-trip
+    coverage. `placement_api.go` already decodes and encodes the placement service DTOs directly, so no separate
+    handler mapper required a code change.
+- `services/control-plane/internal/executiontargets/kubernetes_warm_pool.go`
+- `services/control-plane/internal/executiontargets/kubernetes_reconciler.go`
+- `services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go`
+  - Splits stable plans into guaranteed and best-effort slices, prioritizes guaranteed creation, exempts guaranteed
+    slots from demand eviction, loads min-idle configuration, and tests split, truncation, and eviction behavior.
+- `services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go`
+- `services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go`
+- `services/control-plane/internal/warmcapacity/service.go`
+- `services/control-plane/internal/warmcapacity/service_test.go`
+  - Carries, validates, inserts, and CAS-updates the min-idle authority snapshot.
+- `services/control-plane/internal/observability/distributed_routing_billing_metrics.go`
+- `services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go`
+  - Exports bounded `desired_idle` and `min_idle` unit kinds and fresh-only per-row-clamped warm deficit sums.
+- `deploy/kubernetes/monitoring/prometheus-rules.yaml`
+  - Preserves the pre-existing Increment A rules and appends `SynaraWarmPoolMinIdleDeficit`.
+- `docs/contracts/worker-pool-placement-v1.md`
+  - Documents the guarantee, ceiling, eviction, deficit, and non-reservation/non-waiting boundaries.
+
+## Frozen design decisions applied
+
+- D1: `minIdleUnits` is a non-negative floor inside `desiredIdleUnits`; PostgreSQL authority scope and SQLite safety
+  triggers require the authority snapshot to match its Pool.
+- D2: slot indices `[0, minIdleUnits)` are guaranteed plans and are applied before the cold Pod loop. Remaining plans
+  retain their prior post-cold position. Pod naming and hashing still use the same plan objects in both reconcile passes.
+- D3: only plans with `slot >= minIdleUnits` enter the `warm-pool-demand-fallback` candidate set. Other recycling and
+  identity-safety deletion rules remain unchanged.
+- D4: the target-wide `maxActivePods` budget still caps all creation. Truncating guaranteed plans is not a reconcile
+  error; the published min/ready authority exposes the shortfall.
+- D5: `synara_worker_pool_warm_deficit{capacity_class}` sums
+  `max(0, min_idle_units - ready_idle_units)` per fresh row after bounded class collapse. Existing units also expose the
+  bounded `desired_idle` and `min_idle` kinds.
+- D6: no launch-transaction wait or retry was added. Placement keeps its existing cold-fallback behavior.
+
+## Verification
+
+No Bun command was run.
+
+`GOCACHE=/private/tmp/synara-increment-b-go-cache go build ./...` passed with no output.
+
+The requested unfiltered Go test command was run with the writable `GOCACHE`. All non-listener packages passed, but the
+managed sandbox prohibits `httptest.NewServer` from binding loopback ports. The exact output tail was:
+
+```text
+ok  	github.com/synara-ai/synara/services/control-plane/internal/placement	1.668s
+FAIL	github.com/synara-ai/synara/services/control-plane/internal/executiontargets	6.691s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/warmcapacity	3.727s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/observability	1.954s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/persistence	2.642s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/database	5.532s
+FAIL	github.com/synara-ai/synara/services/control-plane/internal/httpapi	5.527s
+FAIL
+```
+
+Both failures were pre-existing socket-backed tests whose panic was
+`httptest: failed to listen on a port: listen tcp6 [::1]:0: bind: operation not permitted`.
+
+Fresh supplemental package runs skipped only those pre-existing listener-backed cases and passed:
+
+```text
+ok  	github.com/synara-ai/synara/services/control-plane/internal/executiontargets	6.554s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/httpapi	5.045s
+```
+
+The focused PostgreSQL migration test was discovered but skipped because the integration database is not configured:
+
+```text
+=== RUN   TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation
+    worker_pool_warm_capacity_migration_integration_test.go:19: SYNARA_TEST_DATABASE_URL is not configured
+--- SKIP: TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation (0.00s)
+PASS
+ok  	github.com/synara-ai/synara/services/control-plane/internal/database	1.675s
+```
+
+## Remaining risks and intentionally unimplemented edges
+
+- Rerun the exact unfiltered Go test command in an environment that permits loopback test listeners; no code failure was
+  observed before the sandbox bind panics.
+- Run the PostgreSQL integration lane with `SYNARA_TEST_DATABASE_URL` to exercise migration `000086` and its deferred
+  scope trigger against PostgreSQL, not only SQLite/model/service coverage.
+- No live Kubernetes/OrbStack acceptance lane was run in this task; reconcile behavior is covered by the existing fake
+  Kubernetes client tests.
+- The out-of-transaction interactive wait/retry shape remains intentionally deferred per D6.
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

exec
/bin/zsh -lc "sed -n '1,280p' .codex-report-increment-b.md" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
# Increment B implementation report

## Outcome

Implemented guaranteed warm-pool `minIdleUnits` end to end in the Go control plane. Guaranteed slot plans are created
before cold Execution Pods, remain subject to the target-wide Pod ceiling, are exempt from demand-fallback eviction,
and project a fresh authority-derived deficit metric and warning alert. The worktree remains uncommitted.

## Changed files

- `services/control-plane/migrations/000086_worker_pool_min_idle.sql`
  - Adds `worker_pools.min_idle_units`, the named `0 <= min <= desired` check, the authority mirror column, and the
    `CREATE OR REPLACE` scope assertion.
- `services/control-plane/internal/persistence/placement_models.go`
- `services/control-plane/internal/persistence/warm_capacity_models.go`
  - Adds the GORM model columns for PostgreSQL and SQLite AutoMigrate parity.
- `services/control-plane/internal/database/store.go`
- `services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go`
- `services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go`
  - Extends SQLite Worker Pool shape checks, adds insert/update authority-scope mirrors, and covers SQLite plus
    PostgreSQL min-idle mismatch rejection.
- `services/control-plane/internal/placement/service.go`
- `services/control-plane/internal/placement/service_test.go`
- `services/control-plane/internal/httpapi/placement_api_test.go`
  - Adds request normalization/defaulting, bounds validation, persistence, response projection, and HTTP round-trip
    coverage. `placement_api.go` already decodes and encodes the placement service DTOs directly, so no separate
    handler mapper required a code change.
- `services/control-plane/internal/executiontargets/kubernetes_warm_pool.go`
- `services/control-plane/internal/executiontargets/kubernetes_reconciler.go`
- `services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go`
  - Splits stable plans into guaranteed and best-effort slices, prioritizes guaranteed creation, exempts guaranteed
    slots from demand eviction, loads min-idle configuration, and tests split, truncation, and eviction behavior.
- `services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go`
- `services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go`
- `services/control-plane/internal/warmcapacity/service.go`
- `services/control-plane/internal/warmcapacity/service_test.go`
  - Carries, validates, inserts, and CAS-updates the min-idle authority snapshot.
- `services/control-plane/internal/observability/distributed_routing_billing_metrics.go`
- `services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go`
  - Exports bounded `desired_idle` and `min_idle` unit kinds and fresh-only per-row-clamped warm deficit sums.
- `deploy/kubernetes/monitoring/prometheus-rules.yaml`
  - Preserves the pre-existing Increment A rules and appends `SynaraWarmPoolMinIdleDeficit`.
- `docs/contracts/worker-pool-placement-v1.md`
  - Documents the guarantee, ceiling, eviction, deficit, and non-reservation/non-waiting boundaries.

## Frozen design decisions applied

- D1: `minIdleUnits` is a non-negative floor inside `desiredIdleUnits`; PostgreSQL authority scope and SQLite safety
  triggers require the authority snapshot to match its Pool.
- D2: slot indices `[0, minIdleUnits)` are guaranteed plans and are applied before the cold Pod loop. Remaining plans
  retain their prior post-cold position. Pod naming and hashing still use the same plan objects in both reconcile passes.
- D3: only plans with `slot >= minIdleUnits` enter the `warm-pool-demand-fallback` candidate set. Other recycling and
  identity-safety deletion rules remain unchanged.
- D4: the target-wide `maxActivePods` budget still caps all creation. Truncating guaranteed plans is not a reconcile
  error; the published min/ready authority exposes the shortfall.
- D5: `synara_worker_pool_warm_deficit{capacity_class}` sums
  `max(0, min_idle_units - ready_idle_units)` per fresh row after bounded class collapse. Existing units also expose the
  bounded `desired_idle` and `min_idle` kinds.
- D6: no launch-transaction wait or retry was added. Placement keeps its existing cold-fallback behavior.

## Verification

No Bun command was run.

`GOCACHE=/private/tmp/synara-increment-b-go-cache go build ./...` passed with no output.

The requested unfiltered Go test command was run with the writable `GOCACHE`. All non-listener packages passed, but the
managed sandbox prohibits `httptest.NewServer` from binding loopback ports. The exact output tail was:

```text
ok  	github.com/synara-ai/synara/services/control-plane/internal/placement	1.668s
FAIL	github.com/synara-ai/synara/services/control-plane/internal/executiontargets	6.691s
ok  	github.com/synara-ai/synara/services/control-plane/internal/warmcapacity	3.727s
ok  	github.com/synara-ai/synara/services/control-plane/internal/observability	1.954s
ok  	github.com/synara-ai/synara/services/control-plane/internal/persistence	2.642s
ok  	github.com/synara-ai/synara/services/control-plane/internal/database	5.532s
FAIL	github.com/synara-ai/synara/services/control-plane/internal/httpapi	5.527s
FAIL
```

Both failures were pre-existing socket-backed tests whose panic was
`httptest: failed to listen on a port: listen tcp6 [::1]:0: bind: operation not permitted`.

Fresh supplemental package runs skipped only those pre-existing listener-backed cases and passed:

```text
ok  	github.com/synara-ai/synara/services/control-plane/internal/executiontargets	6.554s
ok  	github.com/synara-ai/synara/services/control-plane/internal/httpapi	5.045s
```

The focused PostgreSQL migration test was discovered but skipped because the integration database is not configured:

```text
=== RUN   TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation
    worker_pool_warm_capacity_migration_integration_test.go:19: SYNARA_TEST_DATABASE_URL is not configured
--- SKIP: TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation (0.00s)
PASS
ok  	github.com/synara-ai/synara/services/control-plane/internal/database	1.675s
```

## Remaining risks and intentionally unimplemented edges

- Rerun the exact unfiltered Go test command in an environment that permits loopback test listeners; no code failure was
  observed before the sandbox bind panics.
- Run the PostgreSQL integration lane with `SYNARA_TEST_DATABASE_URL` to exercise migration `000086` and its deferred
  scope trigger against PostgreSQL, not only SQLite/model/service coverage.
- No live Kubernetes/OrbStack acceptance lane was run in this task; reconcile behavior is covered by the existing fake
  Kubernetes client tests.
- The out-of-transaction interactive wait/retry shape remains intentionally deferred per D6.

exec
/bin/zsh -lc "git diff --check
rg -n '[ \\t]+"'$'"' .codex-report-increment-b.md services/control-plane/migrations/000086_worker_pool_min_idle.sql services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go || true" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
exec
/bin/zsh -lc "git status --short --branch
if git status --short | rg 'docs/reports/'; then exit 1; fi" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
## fast-cloud-agents
 M deploy/kubernetes/monitoring/prometheus-rules.yaml
 M docs/contracts/worker-pool-placement-v1.md
 M services/control-plane/internal/database/store.go
 M services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
 M services/control-plane/internal/executiontargets/kubernetes_reconciler.go
 M services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
 M services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
 M services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
 M services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
 M services/control-plane/internal/httpapi/placement_api_test.go
 M services/control-plane/internal/observability/distributed_routing_billing_metrics.go
 M services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
 M services/control-plane/internal/persistence/placement_models.go
 M services/control-plane/internal/persistence/warm_capacity_models.go
 M services/control-plane/internal/placement/service.go
 M services/control-plane/internal/placement/service_test.go
 M services/control-plane/internal/warmcapacity/service.go
 M services/control-plane/internal/warmcapacity/service_test.go
?? .codex-brief-increment-b.md
?? .codex-log-increment-b.txt
?? .codex-report-increment-b.md
?? docs/plans/fast-cloud-agents-implementation.md
?? services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
?? services/control-plane/migrations/000086_worker_pool_min_idle.sql

diff --git a/.codex-report-increment-b.md b/.codex-report-increment-b.md
new file mode 100644
index 0000000000000000000000000000000000000000..d0bd133b85b11838edecf52d026b68d345a97d06
--- /dev/null
+++ b/.codex-report-increment-b.md
@@ -0,0 +1,109 @@
+# Increment B implementation report
+
+## Outcome
+
+Implemented guaranteed warm-pool `minIdleUnits` end to end in the Go control plane. Guaranteed slot plans are created
+before cold Execution Pods, remain subject to the target-wide Pod ceiling, are exempt from demand-fallback eviction,
+and project a fresh authority-derived deficit metric and warning alert. The worktree remains uncommitted.
+
+## Changed files
+
+- `services/control-plane/migrations/000086_worker_pool_min_idle.sql`
+  - Adds `worker_pools.min_idle_units`, the named `0 <= min <= desired` check, the authority mirror column, and the
+    `CREATE OR REPLACE` scope assertion.
+- `services/control-plane/internal/persistence/placement_models.go`
+- `services/control-plane/internal/persistence/warm_capacity_models.go`
+  - Adds the GORM model columns for PostgreSQL and SQLite AutoMigrate parity.
+- `services/control-plane/internal/database/store.go`
+- `services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go`
+- `services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go`
+  - Extends SQLite Worker Pool shape checks, adds insert/update authority-scope mirrors, and covers SQLite plus
+    PostgreSQL min-idle mismatch rejection.
+- `services/control-plane/internal/placement/service.go`
+- `services/control-plane/internal/placement/service_test.go`
+- `services/control-plane/internal/httpapi/placement_api_test.go`
+  - Adds request normalization/defaulting, bounds validation, persistence, response projection, and HTTP round-trip
+    coverage. `placement_api.go` already decodes and encodes the placement service DTOs directly, so no separate
+    handler mapper required a code change.
+- `services/control-plane/internal/executiontargets/kubernetes_warm_pool.go`
+- `services/control-plane/internal/executiontargets/kubernetes_reconciler.go`
+- `services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go`
+  - Splits stable plans into guaranteed and best-effort slices, prioritizes guaranteed creation, exempts guaranteed
+    slots from demand eviction, loads min-idle configuration, and tests split, truncation, and eviction behavior.
+- `services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go`
+- `services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go`
+- `services/control-plane/internal/warmcapacity/service.go`
+- `services/control-plane/internal/warmcapacity/service_test.go`
+  - Carries, validates, inserts, and CAS-updates the min-idle authority snapshot.
+- `services/control-plane/internal/observability/distributed_routing_billing_metrics.go`
+- `services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go`
+  - Exports bounded `desired_idle` and `min_idle` unit kinds and fresh-only per-row-clamped warm deficit sums.
+- `deploy/kubernetes/monitoring/prometheus-rules.yaml`
+  - Preserves the pre-existing Increment A rules and appends `SynaraWarmPoolMinIdleDeficit`.
+- `docs/contracts/worker-pool-placement-v1.md`
+  - Documents the guarantee, ceiling, eviction, deficit, and non-reservation/non-waiting boundaries.
+
+## Frozen design decisions applied
+
+- D1: `minIdleUnits` is a non-negative floor inside `desiredIdleUnits`; PostgreSQL authority scope and SQLite safety
+  triggers require the authority snapshot to match its Pool.
+- D2: slot indices `[0, minIdleUnits)` are guaranteed plans and are applied before the cold Pod loop. Remaining plans
+  retain their prior post-cold position. Pod naming and hashing still use the same plan objects in both reconcile passes.
+- D3: only plans with `slot >= minIdleUnits` enter the `warm-pool-demand-fallback` candidate set. Other recycling and
+  identity-safety deletion rules remain unchanged.
+- D4: the target-wide `maxActivePods` budget still caps all creation. Truncating guaranteed plans is not a reconcile
+  error; the published min/ready authority exposes the shortfall.
+- D5: `synara_worker_pool_warm_deficit{capacity_class}` sums
+  `max(0, min_idle_units - ready_idle_units)` per fresh row after bounded class collapse. Existing units also expose the
+  bounded `desired_idle` and `min_idle` kinds.
+- D6: no launch-transaction wait or retry was added. Placement keeps its existing cold-fallback behavior.
+
+## Verification
+
+No Bun command was run.
+
+`GOCACHE=/private/tmp/synara-increment-b-go-cache go build ./...` passed with no output.
+
+The requested unfiltered Go test command was run with the writable `GOCACHE`. All non-listener packages passed, but the
+managed sandbox prohibits `httptest.NewServer` from binding loopback ports. The exact output tail was:
+
+```text
+ok  	github.com/synara-ai/synara/services/control-plane/internal/placement	1.668s
+FAIL	github.com/synara-ai/synara/services/control-plane/internal/executiontargets	6.691s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/warmcapacity	3.727s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/observability	1.954s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/persistence	2.642s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/database	5.532s
+FAIL	github.com/synara-ai/synara/services/control-plane/internal/httpapi	5.527s
+FAIL
+```
+
+Both failures were pre-existing socket-backed tests whose panic was
+`httptest: failed to listen on a port: listen tcp6 [::1]:0: bind: operation not permitted`.
+
+Fresh supplemental package runs skipped only those pre-existing listener-backed cases and passed:
+
+```text
+ok  	github.com/synara-ai/synara/services/control-plane/internal/executiontargets	6.554s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/httpapi	5.045s
+```
+
+The focused PostgreSQL migration test was discovered but skipped because the integration database is not configured:
+
+```text
+=== RUN   TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation
+    worker_pool_warm_capacity_migration_integration_test.go:19: SYNARA_TEST_DATABASE_URL is not configured
+--- SKIP: TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation (0.00s)
+PASS
+ok  	github.com/synara-ai/synara/services/control-plane/internal/database	1.675s
+```
+
+## Remaining risks and intentionally unimplemented edges
+
+- Rerun the exact unfiltered Go test command in an environment that permits loopback test listeners; no code failure was
+  observed before the sandbox bind panics.
+- Run the PostgreSQL integration lane with `SYNARA_TEST_DATABASE_URL` to exercise migration `000086` and its deferred
+  scope trigger against PostgreSQL, not only SQLite/model/service coverage.
+- No live Kubernetes/OrbStack acceptance lane was run in this task; reconcile behavior is covered by the existing fake
+  Kubernetes client tests.
+- The out-of-transaction interactive wait/retry shape remains intentionally deferred per D6.
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

exec
/bin/zsh -lc "nl -ba /Users/huang/.codex/memories/MEMORY.md | sed -n '1256,1279p'" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
  1256	### keywords
  1257	
  1258	- 评估第三阶段资源优化, RecoveryBundle, MemoryReferenceResolver, ExecutionSuspendAttempt, WorkerLease, lifecyclepolicy, suspend_resume.go, AgentMemoryHead, WaitingKeepAliveSeconds, SuspendAfterIdleSeconds, WorkspaceRetentionDays
  1259	
  1260	## Task 12: Review `TODO.md` and Stage 4 documentation layering without edits or heavy checks, outcome success
  1261	
  1262	### rollout_summary_files
  1263	
  1264	- rollout_summaries/2026-07-25T00-52-45-kxlT-synara_todo_stage4_documentation_review.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/sessions/2026/07/25/rollout-2026-07-25T08-52-45-019f96c2-a295-7bd3-8e1e-3e573d100c81.jsonl, updated_at=2026-07-25T00:56:21+00:00, thread_id=019f96c2-a295-7bd3-8e1e-3e573d100c81, read-only review; no tests/heavy checks)
  1265	- extensions/chronicle/resources/2026-07-24T23-45-00-AbUj-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-24T23-45-00-AbUj-10min-memory-summary.md, updated_at=2026-07-24T23:45:00+00:00, thread_id=None, observed roadmap review [chronicle memory])
  1266	- extensions/chronicle/resources/2026-07-25T00-46-00-UtMc-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T00-46-00-UtMc-10min-memory-summary.md, updated_at=2026-07-25T00:46:00+00:00, thread_id=None, observed review findings [chronicle memory])
  1267	
  1268	### keywords
  1269	
  1270	- TODO.md, stage-4-distributed-execution-resource-lifecycle.md, DESIGNED, IMPLEMENTED, REPO VERIFIED, ENV ACCEPTED, RELEASED, Stage 4A, Stage 4B, Stage 4C, Stage 4D, Stage 4 Release Checklist
  1271	
  1272	## Task 13: Continue Stage 4 resource/billing/routing acceptance and review in local OrbStack, outcome observed in progress
  1273	
  1274	### rollout_summary_files
  1275	
  1276	- extensions/chronicle/resources/2026-07-25T13-31-00-wRtc-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T13-31-00-wRtc-10min-memory-summary.md, updated_at=2026-07-25T13:31:00+00:00, thread_id=None, billing ledger/PostgreSQL/routing review; not submitted [chronicle memory])
  1277	- extensions/chronicle/resources/2026-07-25T11-51-00-jJyF-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T11-51-00-jJyF-10min-memory-summary.md, updated_at=2026-07-25T11:51:00+00:00, thread_id=None, provider `503 auth_unavailable` blocked run [chronicle memory])
  1278	- extensions/chronicle/resources/2026-07-25T10-48-00-VCOo-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T10-48-00-VCOo-10min-memory-summary.md, updated_at=2026-07-25T10:48:00+00:00, thread_id=None, lease-validation false failure diagnosed [chronicle memory])
  1279	- extensions/chronicle/resources/2026-07-25T09-25-00-EVsv-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T09-25-00-EVsv-10min-memory-summary.md, updated_at=2026-07-25T09:25:00+00:00, thread_id=None, CPAMP quota/desktop context [chronicle memory])

diff --git a/.codex-report-increment-b.md b/.codex-report-increment-b.md
new file mode 100644
index 0000000000000000000000000000000000000000..d0bd133b85b11838edecf52d026b68d345a97d06
--- /dev/null
+++ b/.codex-report-increment-b.md
@@ -0,0 +1,109 @@
+# Increment B implementation report
+
+## Outcome
+
+Implemented guaranteed warm-pool `minIdleUnits` end to end in the Go control plane. Guaranteed slot plans are created
+before cold Execution Pods, remain subject to the target-wide Pod ceiling, are exempt from demand-fallback eviction,
+and project a fresh authority-derived deficit metric and warning alert. The worktree remains uncommitted.
+
+## Changed files
+
+- `services/control-plane/migrations/000086_worker_pool_min_idle.sql`
+  - Adds `worker_pools.min_idle_units`, the named `0 <= min <= desired` check, the authority mirror column, and the
+    `CREATE OR REPLACE` scope assertion.
+- `services/control-plane/internal/persistence/placement_models.go`
+- `services/control-plane/internal/persistence/warm_capacity_models.go`
+  - Adds the GORM model columns for PostgreSQL and SQLite AutoMigrate parity.
+- `services/control-plane/internal/database/store.go`
+- `services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go`
+- `services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go`
+  - Extends SQLite Worker Pool shape checks, adds insert/update authority-scope mirrors, and covers SQLite plus
+    PostgreSQL min-idle mismatch rejection.
+- `services/control-plane/internal/placement/service.go`
+- `services/control-plane/internal/placement/service_test.go`
+- `services/control-plane/internal/httpapi/placement_api_test.go`
+  - Adds request normalization/defaulting, bounds validation, persistence, response projection, and HTTP round-trip
+    coverage. `placement_api.go` already decodes and encodes the placement service DTOs directly, so no separate
+    handler mapper required a code change.
+- `services/control-plane/internal/executiontargets/kubernetes_warm_pool.go`
+- `services/control-plane/internal/executiontargets/kubernetes_reconciler.go`
+- `services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go`
+  - Splits stable plans into guaranteed and best-effort slices, prioritizes guaranteed creation, exempts guaranteed
+    slots from demand eviction, loads min-idle configuration, and tests split, truncation, and eviction behavior.
+- `services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go`
+- `services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go`
+- `services/control-plane/internal/warmcapacity/service.go`
+- `services/control-plane/internal/warmcapacity/service_test.go`
+  - Carries, validates, inserts, and CAS-updates the min-idle authority snapshot.
+- `services/control-plane/internal/observability/distributed_routing_billing_metrics.go`
+- `services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go`
+  - Exports bounded `desired_idle` and `min_idle` unit kinds and fresh-only per-row-clamped warm deficit sums.
+- `deploy/kubernetes/monitoring/prometheus-rules.yaml`
+  - Preserves the pre-existing Increment A rules and appends `SynaraWarmPoolMinIdleDeficit`.
+- `docs/contracts/worker-pool-placement-v1.md`
+  - Documents the guarantee, ceiling, eviction, deficit, and non-reservation/non-waiting boundaries.
+
+## Frozen design decisions applied
+
+- D1: `minIdleUnits` is a non-negative floor inside `desiredIdleUnits`; PostgreSQL authority scope and SQLite safety
+  triggers require the authority snapshot to match its Pool.
+- D2: slot indices `[0, minIdleUnits)` are guaranteed plans and are applied before the cold Pod loop. Remaining plans
+  retain their prior post-cold position. Pod naming and hashing still use the same plan objects in both reconcile passes.
+- D3: only plans with `slot >= minIdleUnits` enter the `warm-pool-demand-fallback` candidate set. Other recycling and
+  identity-safety deletion rules remain unchanged.
+- D4: the target-wide `maxActivePods` budget still caps all creation. Truncating guaranteed plans is not a reconcile
+  error; the published min/ready authority exposes the shortfall.
+- D5: `synara_worker_pool_warm_deficit{capacity_class}` sums
+  `max(0, min_idle_units - ready_idle_units)` per fresh row after bounded class collapse. Existing units also expose the
+  bounded `desired_idle` and `min_idle` kinds.
+- D6: no launch-transaction wait or retry was added. Placement keeps its existing cold-fallback behavior.
+
+## Verification
+
+No Bun command was run.
+
+`GOCACHE=/private/tmp/synara-increment-b-go-cache go build ./...` passed with no output.
+
+The requested unfiltered Go test command was run with the writable `GOCACHE`. All non-listener packages passed, but the
+managed sandbox prohibits `httptest.NewServer` from binding loopback ports. The exact output tail was:
+
+```text
+ok  	github.com/synara-ai/synara/services/control-plane/internal/placement	1.668s
+FAIL	github.com/synara-ai/synara/services/control-plane/internal/executiontargets	6.691s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/warmcapacity	3.727s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/observability	1.954s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/persistence	2.642s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/database	5.532s
+FAIL	github.com/synara-ai/synara/services/control-plane/internal/httpapi	5.527s
+FAIL
+```
+
+Both failures were pre-existing socket-backed tests whose panic was
+`httptest: failed to listen on a port: listen tcp6 [::1]:0: bind: operation not permitted`.
+
+Fresh supplemental package runs skipped only those pre-existing listener-backed cases and passed:
+
+```text
+ok  	github.com/synara-ai/synara/services/control-plane/internal/executiontargets	6.554s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/httpapi	5.045s
+```
+
+The focused PostgreSQL migration test was discovered but skipped because the integration database is not configured:
+
+```text
+=== RUN   TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation
+    worker_pool_warm_capacity_migration_integration_test.go:19: SYNARA_TEST_DATABASE_URL is not configured
+--- SKIP: TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation (0.00s)
+PASS
+ok  	github.com/synara-ai/synara/services/control-plane/internal/database	1.675s
+```
+
+## Remaining risks and intentionally unimplemented edges
+
+- Rerun the exact unfiltered Go test command in an environment that permits loopback test listeners; no code failure was
+  observed before the sandbox bind panics.
+- Run the PostgreSQL integration lane with `SYNARA_TEST_DATABASE_URL` to exercise migration `000086` and its deferred
+  scope trigger against PostgreSQL, not only SQLite/model/service coverage.
+- No live Kubernetes/OrbStack acceptance lane was run in this task; reconcile behavior is covered by the existing fake
+  Kubernetes client tests.
+- The out-of-transaction interactive wait/retry shape remains intentionally deferred per D6.
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

exec
/bin/zsh -lc "rg -n -C 2 'Task 15: Continue Stage 4 scheduling|warm capacity, execution_launch_target_test' /Users/huang/.codex/memories/MEMORY.md" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
1298-- Stage 3, automatic Worker Release rollback, migration 000042, auto_rollback.go, policy-version-scoped observation window, Audit, Outbox, e944a2e7, 6f808f35, 9aefbfc8, synara-stage3-worm-data, all-dot-synara-hyphen-entries-absent
1299-
1300:## Task 15: Continue Stage 4 scheduling-decision, claim-release, fair-share, and warm-capacity work in OrbStack, outcome observed in progress
1301-
1302-### rollout_summary_files
--
1320-### keywords
1321-
1322:- 评估第三阶段资源优化, 000076_execution_scheduling_decisions.sql, ReleaseFact, worker_claim_facts, claim fair-share, quota fairness, warm capacity, execution_launch_target_test.go, recovery_bundle_test.go, OrbStack PostgreSQL, OrbStack Kubernetes, immutable decision record, shared allocation
1323-
1324-## Task 16: Continue cloud-agent optimization, documentation audit, and `upstream/main` integration, outcome observed in progress

diff --git a/.codex-report-increment-b.md b/.codex-report-increment-b.md
new file mode 100644
index 0000000000000000000000000000000000000000..d0bd133b85b11838edecf52d026b68d345a97d06
--- /dev/null
+++ b/.codex-report-increment-b.md
@@ -0,0 +1,109 @@
+# Increment B implementation report
+
+## Outcome
+
+Implemented guaranteed warm-pool `minIdleUnits` end to end in the Go control plane. Guaranteed slot plans are created
+before cold Execution Pods, remain subject to the target-wide Pod ceiling, are exempt from demand-fallback eviction,
+and project a fresh authority-derived deficit metric and warning alert. The worktree remains uncommitted.
+
+## Changed files
+
+- `services/control-plane/migrations/000086_worker_pool_min_idle.sql`
+  - Adds `worker_pools.min_idle_units`, the named `0 <= min <= desired` check, the authority mirror column, and the
+    `CREATE OR REPLACE` scope assertion.
+- `services/control-plane/internal/persistence/placement_models.go`
+- `services/control-plane/internal/persistence/warm_capacity_models.go`
+  - Adds the GORM model columns for PostgreSQL and SQLite AutoMigrate parity.
+- `services/control-plane/internal/database/store.go`
+- `services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go`
+- `services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go`
+  - Extends SQLite Worker Pool shape checks, adds insert/update authority-scope mirrors, and covers SQLite plus
+    PostgreSQL min-idle mismatch rejection.
+- `services/control-plane/internal/placement/service.go`
+- `services/control-plane/internal/placement/service_test.go`
+- `services/control-plane/internal/httpapi/placement_api_test.go`
+  - Adds request normalization/defaulting, bounds validation, persistence, response projection, and HTTP round-trip
+    coverage. `placement_api.go` already decodes and encodes the placement service DTOs directly, so no separate
+    handler mapper required a code change.
+- `services/control-plane/internal/executiontargets/kubernetes_warm_pool.go`
+- `services/control-plane/internal/executiontargets/kubernetes_reconciler.go`
+- `services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go`
+  - Splits stable plans into guaranteed and best-effort slices, prioritizes guaranteed creation, exempts guaranteed
+    slots from demand eviction, loads min-idle configuration, and tests split, truncation, and eviction behavior.
+- `services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go`
+- `services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go`
+- `services/control-plane/internal/warmcapacity/service.go`
+- `services/control-plane/internal/warmcapacity/service_test.go`
+  - Carries, validates, inserts, and CAS-updates the min-idle authority snapshot.
+- `services/control-plane/internal/observability/distributed_routing_billing_metrics.go`
+- `services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go`
+  - Exports bounded `desired_idle` and `min_idle` unit kinds and fresh-only per-row-clamped warm deficit sums.
+- `deploy/kubernetes/monitoring/prometheus-rules.yaml`
+  - Preserves the pre-existing Increment A rules and appends `SynaraWarmPoolMinIdleDeficit`.
+- `docs/contracts/worker-pool-placement-v1.md`
+  - Documents the guarantee, ceiling, eviction, deficit, and non-reservation/non-waiting boundaries.
+
+## Frozen design decisions applied
+
+- D1: `minIdleUnits` is a non-negative floor inside `desiredIdleUnits`; PostgreSQL authority scope and SQLite safety
+  triggers require the authority snapshot to match its Pool.
+- D2: slot indices `[0, minIdleUnits)` are guaranteed plans and are applied before the cold Pod loop. Remaining plans
+  retain their prior post-cold position. Pod naming and hashing still use the same plan objects in both reconcile passes.
+- D3: only plans with `slot >= minIdleUnits` enter the `warm-pool-demand-fallback` candidate set. Other recycling and
+  identity-safety deletion rules remain unchanged.
+- D4: the target-wide `maxActivePods` budget still caps all creation. Truncating guaranteed plans is not a reconcile
+  error; the published min/ready authority exposes the shortfall.
+- D5: `synara_worker_pool_warm_deficit{capacity_class}` sums
+  `max(0, min_idle_units - ready_idle_units)` per fresh row after bounded class collapse. Existing units also expose the
+  bounded `desired_idle` and `min_idle` kinds.
+- D6: no launch-transaction wait or retry was added. Placement keeps its existing cold-fallback behavior.
+
+## Verification
+
+No Bun command was run.
+
+`GOCACHE=/private/tmp/synara-increment-b-go-cache go build ./...` passed with no output.
+
+The requested unfiltered Go test command was run with the writable `GOCACHE`. All non-listener packages passed, but the
+managed sandbox prohibits `httptest.NewServer` from binding loopback ports. The exact output tail was:
+
+```text
+ok  	github.com/synara-ai/synara/services/control-plane/internal/placement	1.668s
+FAIL	github.com/synara-ai/synara/services/control-plane/internal/executiontargets	6.691s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/warmcapacity	3.727s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/observability	1.954s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/persistence	2.642s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/database	5.532s
+FAIL	github.com/synara-ai/synara/services/control-plane/internal/httpapi	5.527s
+FAIL
+```
+
+Both failures were pre-existing socket-backed tests whose panic was
+`httptest: failed to listen on a port: listen tcp6 [::1]:0: bind: operation not permitted`.
+
+Fresh supplemental package runs skipped only those pre-existing listener-backed cases and passed:
+
+```text
+ok  	github.com/synara-ai/synara/services/control-plane/internal/executiontargets	6.554s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/httpapi	5.045s
+```
+
+The focused PostgreSQL migration test was discovered but skipped because the integration database is not configured:
+
+```text
+=== RUN   TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation
+    worker_pool_warm_capacity_migration_integration_test.go:19: SYNARA_TEST_DATABASE_URL is not configured
+--- SKIP: TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation (0.00s)
+PASS
+ok  	github.com/synara-ai/synara/services/control-plane/internal/database	1.675s
+```
+
+## Remaining risks and intentionally unimplemented edges
+
+- Rerun the exact unfiltered Go test command in an environment that permits loopback test listeners; no code failure was
+  observed before the sandbox bind panics.
+- Run the PostgreSQL integration lane with `SYNARA_TEST_DATABASE_URL` to exercise migration `000086` and its deferred
+  scope trigger against PostgreSQL, not only SQLite/model/service coverage.
+- No live Kubernetes/OrbStack acceptance lane was run in this task; reconcile behavior is covered by the existing fake
+  Kubernetes client tests.
+- The out-of-transaction interactive wait/retry shape remains intentionally deferred per D6.
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

exec
/bin/zsh -lc "nl -ba /Users/huang/.codex/memories/MEMORY.md | sed -n '1300,1322p'" in /Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents
 succeeded in 0ms:
  1300	## Task 15: Continue Stage 4 scheduling-decision, claim-release, fair-share, and warm-capacity work in OrbStack, outcome observed in progress
  1301	
  1302	### rollout_summary_files
  1303	
  1304	- extensions/chronicle/resources/2026-07-25T15-15-00-DdAd-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T15-15-00-DdAd-10min-memory-summary.md, updated_at=2026-07-25T15:15:00+00:00, thread_id=None, Kubernetes/ACK product and cost-research context [chronicle memory])
  1305	- extensions/chronicle/resources/2026-07-25T14-55-00-YhbE-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T14-55-00-YhbE-10min-memory-summary.md, updated_at=2026-07-25T14:55:00+00:00, thread_id=None, Phase-Three resource optimization, warm-capacity and merge context [chronicle memory])
  1306	- extensions/chronicle/resources/2026-07-25T09-26-00-Fkxv-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T09-26-00-Fkxv-10min-memory-summary.md, updated_at=2026-07-25T09:26:00+00:00, thread_id=None, merge/TODO/resource-optimization observations [chronicle memory])
  1307	- extensions/chronicle/resources/2026-07-25T08-54-00-tquZ-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T08-54-00-tquZ-10min-memory-summary.md, updated_at=2026-07-25T08:54:00+00:00, thread_id=None, Kubernetes-resilience review context [chronicle memory])
  1308	- extensions/chronicle/resources/2026-07-25T08-53-00-mQpu-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T08-53-00-mQpu-10min-memory-summary.md, updated_at=2026-07-25T08:53:00+00:00, thread_id=None, JetBrains diff and Codex checklist context [chronicle memory])
  1309	- extensions/chronicle/resources/2026-07-26T05-29-00-cKpz-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-26T05-29-00-cKpz-10min-memory-summary.md, updated_at=2026-07-26T05:29:00+00:00, thread_id=None, active scheduling/ledger/cost-allocation subthreads [chronicle memory])
  1310	- extensions/chronicle/resources/2026-07-26T05-05-00-KkQT-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-26T05-05-00-KkQT-10min-memory-summary.md, updated_at=2026-07-26T05:05:00+00:00, thread_id=None, Codex/JetBrains Stage 4 review context [chronicle memory])
  1311	- extensions/chronicle/resources/2026-07-26T04-28-00-YqoR-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-26T04-28-00-YqoR-10min-memory-summary.md, updated_at=2026-07-26T04:28:00+00:00, thread_id=None, Stage 4 resource-optimization subagent/review context [chronicle memory])
  1312	- extensions/chronicle/resources/2026-07-26T04-02-00-mdCV-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-26T04-02-00-mdCV-10min-memory-summary.md, updated_at=2026-07-26T04:02:00+00:00, thread_id=None, Agent Router/All API Hub configuration and terminal-debug context [chronicle memory])
  1313	- extensions/chronicle/resources/2026-07-26T03-52-00-icSp-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-26T03-52-00-icSp-10min-memory-summary.md, updated_at=2026-07-26T03:52:00+00:00, thread_id=None, Agent Router provider-management context; credential values omitted [chronicle memory])
  1314	- extensions/chronicle/resources/2026-07-26T03-18-00-qRvV-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-26T03-18-00-qRvV-10min-memory-summary.md, updated_at=2026-07-26T03:18:00+00:00, thread_id=None, local OrbStack PostgreSQL/Kubernetes review context [chronicle memory])
  1315	- extensions/chronicle/resources/2026-07-26T03-08-00-RMyP-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-26T03-08-00-RMyP-10min-memory-summary.md, updated_at=2026-07-26T03:08:00+00:00, thread_id=None, immutable scheduling-decision and claim-release observations [chronicle memory])
  1316	- extensions/chronicle/resources/2026-07-26T02-58-00-lYTB-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-26T02-58-00-lYTB-10min-memory-summary.md, updated_at=2026-07-26T02:58:00+00:00, thread_id=None, claim/fair-share and temporary PostgreSQL namespace context [chronicle memory])
  1317	- extensions/chronicle/resources/2026-07-25T15-25-00-NMDN-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T15-25-00-NMDN-10min-memory-summary.md, updated_at=2026-07-25T15:25:00+00:00, thread_id=None, Stage 4 task/evidence-gap context [chronicle memory])
  1318	- extensions/chronicle/resources/2026-07-25T15-05-00-xIiC-10min-memory-summary.md (cwd=/Users/huang/devel/project/huang/business/synara, rollout_path=/Users/huang/.codex/memories/extensions/chronicle/resources/2026-07-25T15-05-00-xIiC-10min-memory-summary.md, updated_at=2026-07-25T15:05:00+00:00, thread_id=None, warm-capacity work and parallel workbench context [chronicle memory])
  1319	
  1320	### keywords
  1321	
  1322	- 评估第三阶段资源优化, 000076_execution_scheduling_decisions.sql, ReleaseFact, worker_claim_facts, claim fair-share, quota fairness, warm capacity, execution_launch_target_test.go, recovery_bundle_test.go, OrbStack PostgreSQL, OrbStack Kubernetes, immutable decision record, shared allocation

diff --git a/.codex-report-increment-b.md b/.codex-report-increment-b.md
new file mode 100644
index 0000000000000000000000000000000000000000..d0bd133b85b11838edecf52d026b68d345a97d06
--- /dev/null
+++ b/.codex-report-increment-b.md
@@ -0,0 +1,109 @@
+# Increment B implementation report
+
+## Outcome
+
+Implemented guaranteed warm-pool `minIdleUnits` end to end in the Go control plane. Guaranteed slot plans are created
+before cold Execution Pods, remain subject to the target-wide Pod ceiling, are exempt from demand-fallback eviction,
+and project a fresh authority-derived deficit metric and warning alert. The worktree remains uncommitted.
+
+## Changed files
+
+- `services/control-plane/migrations/000086_worker_pool_min_idle.sql`
+  - Adds `worker_pools.min_idle_units`, the named `0 <= min <= desired` check, the authority mirror column, and the
+    `CREATE OR REPLACE` scope assertion.
+- `services/control-plane/internal/persistence/placement_models.go`
+- `services/control-plane/internal/persistence/warm_capacity_models.go`
+  - Adds the GORM model columns for PostgreSQL and SQLite AutoMigrate parity.
+- `services/control-plane/internal/database/store.go`
+- `services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go`
+- `services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go`
+  - Extends SQLite Worker Pool shape checks, adds insert/update authority-scope mirrors, and covers SQLite plus
+    PostgreSQL min-idle mismatch rejection.
+- `services/control-plane/internal/placement/service.go`
+- `services/control-plane/internal/placement/service_test.go`
+- `services/control-plane/internal/httpapi/placement_api_test.go`
+  - Adds request normalization/defaulting, bounds validation, persistence, response projection, and HTTP round-trip
+    coverage. `placement_api.go` already decodes and encodes the placement service DTOs directly, so no separate
+    handler mapper required a code change.
+- `services/control-plane/internal/executiontargets/kubernetes_warm_pool.go`
+- `services/control-plane/internal/executiontargets/kubernetes_reconciler.go`
+- `services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go`
+  - Splits stable plans into guaranteed and best-effort slices, prioritizes guaranteed creation, exempts guaranteed
+    slots from demand eviction, loads min-idle configuration, and tests split, truncation, and eviction behavior.
+- `services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go`
+- `services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go`
+- `services/control-plane/internal/warmcapacity/service.go`
+- `services/control-plane/internal/warmcapacity/service_test.go`
+  - Carries, validates, inserts, and CAS-updates the min-idle authority snapshot.
+- `services/control-plane/internal/observability/distributed_routing_billing_metrics.go`
+- `services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go`
+  - Exports bounded `desired_idle` and `min_idle` unit kinds and fresh-only per-row-clamped warm deficit sums.
+- `deploy/kubernetes/monitoring/prometheus-rules.yaml`
+  - Preserves the pre-existing Increment A rules and appends `SynaraWarmPoolMinIdleDeficit`.
+- `docs/contracts/worker-pool-placement-v1.md`
+  - Documents the guarantee, ceiling, eviction, deficit, and non-reservation/non-waiting boundaries.
+
+## Frozen design decisions applied
+
+- D1: `minIdleUnits` is a non-negative floor inside `desiredIdleUnits`; PostgreSQL authority scope and SQLite safety
+  triggers require the authority snapshot to match its Pool.
+- D2: slot indices `[0, minIdleUnits)` are guaranteed plans and are applied before the cold Pod loop. Remaining plans
+  retain their prior post-cold position. Pod naming and hashing still use the same plan objects in both reconcile passes.
+- D3: only plans with `slot >= minIdleUnits` enter the `warm-pool-demand-fallback` candidate set. Other recycling and
+  identity-safety deletion rules remain unchanged.
+- D4: the target-wide `maxActivePods` budget still caps all creation. Truncating guaranteed plans is not a reconcile
+  error; the published min/ready authority exposes the shortfall.
+- D5: `synara_worker_pool_warm_deficit{capacity_class}` sums
+  `max(0, min_idle_units - ready_idle_units)` per fresh row after bounded class collapse. Existing units also expose the
+  bounded `desired_idle` and `min_idle` kinds.
+- D6: no launch-transaction wait or retry was added. Placement keeps its existing cold-fallback behavior.
+
+## Verification
+
+No Bun command was run.
+
+`GOCACHE=/private/tmp/synara-increment-b-go-cache go build ./...` passed with no output.
+
+The requested unfiltered Go test command was run with the writable `GOCACHE`. All non-listener packages passed, but the
+managed sandbox prohibits `httptest.NewServer` from binding loopback ports. The exact output tail was:
+
+```text
+ok  	github.com/synara-ai/synara/services/control-plane/internal/placement	1.668s
+FAIL	github.com/synara-ai/synara/services/control-plane/internal/executiontargets	6.691s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/warmcapacity	3.727s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/observability	1.954s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/persistence	2.642s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/database	5.532s
+FAIL	github.com/synara-ai/synara/services/control-plane/internal/httpapi	5.527s
+FAIL
+```
+
+Both failures were pre-existing socket-backed tests whose panic was
+`httptest: failed to listen on a port: listen tcp6 [::1]:0: bind: operation not permitted`.
+
+Fresh supplemental package runs skipped only those pre-existing listener-backed cases and passed:
+
+```text
+ok  	github.com/synara-ai/synara/services/control-plane/internal/executiontargets	6.554s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/httpapi	5.045s
+```
+
+The focused PostgreSQL migration test was discovered but skipped because the integration database is not configured:
+
+```text
+=== RUN   TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation
+    worker_pool_warm_capacity_migration_integration_test.go:19: SYNARA_TEST_DATABASE_URL is not configured
+--- SKIP: TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation (0.00s)
+PASS
+ok  	github.com/synara-ai/synara/services/control-plane/internal/database	1.675s
+```
+
+## Remaining risks and intentionally unimplemented edges
+
+- Rerun the exact unfiltered Go test command in an environment that permits loopback test listeners; no code failure was
+  observed before the sandbox bind panics.
+- Run the PostgreSQL integration lane with `SYNARA_TEST_DATABASE_URL` to exercise migration `000086` and its deferred
+  scope trigger against PostgreSQL, not only SQLite/model/service coverage.
+- No live Kubernetes/OrbStack acceptance lane was run in this task; reconcile behavior is covered by the existing fake
+  Kubernetes client tests.
+- The out-of-transaction interactive wait/retry shape remains intentionally deferred per D6.
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

Increment B implementation, focused coverage, requested build/test attempts, supplemental sandbox-compatible verification, and the final worktree report are complete. The report records the loopback-listener and PostgreSQL integration-environment limits.
  ✓ Read frozen plan, relevant memory, Git state, and existing implementation/tests
  ✓ Implement migration, SQLite parity, models, placement API, reconciler, capacity writer, metrics, alert, and contract docs
  ✓ Add focused tests for validation, split/budget, eviction, metrics freshness, and SQLite scope parity
  ✓ Run requested Go build/tests, fix failures, and write .codex-report-increment-b.md
diff --git a/.codex-report-increment-b.md b/.codex-report-increment-b.md
new file mode 100644
index 0000000000000000000000000000000000000000..d0bd133b85b11838edecf52d026b68d345a97d06
--- /dev/null
+++ b/.codex-report-increment-b.md
@@ -0,0 +1,109 @@
+# Increment B implementation report
+
+## Outcome
+
+Implemented guaranteed warm-pool `minIdleUnits` end to end in the Go control plane. Guaranteed slot plans are created
+before cold Execution Pods, remain subject to the target-wide Pod ceiling, are exempt from demand-fallback eviction,
+and project a fresh authority-derived deficit metric and warning alert. The worktree remains uncommitted.
+
+## Changed files
+
+- `services/control-plane/migrations/000086_worker_pool_min_idle.sql`
+  - Adds `worker_pools.min_idle_units`, the named `0 <= min <= desired` check, the authority mirror column, and the
+    `CREATE OR REPLACE` scope assertion.
+- `services/control-plane/internal/persistence/placement_models.go`
+- `services/control-plane/internal/persistence/warm_capacity_models.go`
+  - Adds the GORM model columns for PostgreSQL and SQLite AutoMigrate parity.
+- `services/control-plane/internal/database/store.go`
+- `services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go`
+- `services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go`
+  - Extends SQLite Worker Pool shape checks, adds insert/update authority-scope mirrors, and covers SQLite plus
+    PostgreSQL min-idle mismatch rejection.
+- `services/control-plane/internal/placement/service.go`
+- `services/control-plane/internal/placement/service_test.go`
+- `services/control-plane/internal/httpapi/placement_api_test.go`
+  - Adds request normalization/defaulting, bounds validation, persistence, response projection, and HTTP round-trip
+    coverage. `placement_api.go` already decodes and encodes the placement service DTOs directly, so no separate
+    handler mapper required a code change.
+- `services/control-plane/internal/executiontargets/kubernetes_warm_pool.go`
+- `services/control-plane/internal/executiontargets/kubernetes_reconciler.go`
+- `services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go`
+  - Splits stable plans into guaranteed and best-effort slices, prioritizes guaranteed creation, exempts guaranteed
+    slots from demand eviction, loads min-idle configuration, and tests split, truncation, and eviction behavior.
+- `services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go`
+- `services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go`
+- `services/control-plane/internal/warmcapacity/service.go`
+- `services/control-plane/internal/warmcapacity/service_test.go`
+  - Carries, validates, inserts, and CAS-updates the min-idle authority snapshot.
+- `services/control-plane/internal/observability/distributed_routing_billing_metrics.go`
+- `services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go`
+  - Exports bounded `desired_idle` and `min_idle` unit kinds and fresh-only per-row-clamped warm deficit sums.
+- `deploy/kubernetes/monitoring/prometheus-rules.yaml`
+  - Preserves the pre-existing Increment A rules and appends `SynaraWarmPoolMinIdleDeficit`.
+- `docs/contracts/worker-pool-placement-v1.md`
+  - Documents the guarantee, ceiling, eviction, deficit, and non-reservation/non-waiting boundaries.
+
+## Frozen design decisions applied
+
+- D1: `minIdleUnits` is a non-negative floor inside `desiredIdleUnits`; PostgreSQL authority scope and SQLite safety
+  triggers require the authority snapshot to match its Pool.
+- D2: slot indices `[0, minIdleUnits)` are guaranteed plans and are applied before the cold Pod loop. Remaining plans
+  retain their prior post-cold position. Pod naming and hashing still use the same plan objects in both reconcile passes.
+- D3: only plans with `slot >= minIdleUnits` enter the `warm-pool-demand-fallback` candidate set. Other recycling and
+  identity-safety deletion rules remain unchanged.
+- D4: the target-wide `maxActivePods` budget still caps all creation. Truncating guaranteed plans is not a reconcile
+  error; the published min/ready authority exposes the shortfall.
+- D5: `synara_worker_pool_warm_deficit{capacity_class}` sums
+  `max(0, min_idle_units - ready_idle_units)` per fresh row after bounded class collapse. Existing units also expose the
+  bounded `desired_idle` and `min_idle` kinds.
+- D6: no launch-transaction wait or retry was added. Placement keeps its existing cold-fallback behavior.
+
+## Verification
+
+No Bun command was run.
+
+`GOCACHE=/private/tmp/synara-increment-b-go-cache go build ./...` passed with no output.
+
+The requested unfiltered Go test command was run with the writable `GOCACHE`. All non-listener packages passed, but the
+managed sandbox prohibits `httptest.NewServer` from binding loopback ports. The exact output tail was:
+
+```text
+ok  	github.com/synara-ai/synara/services/control-plane/internal/placement	1.668s
+FAIL	github.com/synara-ai/synara/services/control-plane/internal/executiontargets	6.691s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/warmcapacity	3.727s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/observability	1.954s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/persistence	2.642s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/database	5.532s
+FAIL	github.com/synara-ai/synara/services/control-plane/internal/httpapi	5.527s
+FAIL
+```
+
+Both failures were pre-existing socket-backed tests whose panic was
+`httptest: failed to listen on a port: listen tcp6 [::1]:0: bind: operation not permitted`.
+
+Fresh supplemental package runs skipped only those pre-existing listener-backed cases and passed:
+
+```text
+ok  	github.com/synara-ai/synara/services/control-plane/internal/executiontargets	6.554s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/httpapi	5.045s
+```
+
+The focused PostgreSQL migration test was discovered but skipped because the integration database is not configured:
+
+```text
+=== RUN   TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation
+    worker_pool_warm_capacity_migration_integration_test.go:19: SYNARA_TEST_DATABASE_URL is not configured
+--- SKIP: TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation (0.00s)
+PASS
+ok  	github.com/synara-ai/synara/services/control-plane/internal/database	1.675s
+```
+
+## Remaining risks and intentionally unimplemented edges
+
+- Rerun the exact unfiltered Go test command in an environment that permits loopback test listeners; no code failure was
+  observed before the sandbox bind panics.
+- Run the PostgreSQL integration lane with `SYNARA_TEST_DATABASE_URL` to exercise migration `000086` and its deferred
+  scope trigger against PostgreSQL, not only SQLite/model/service coverage.
+- No live Kubernetes/OrbStack acceptance lane was run in this task; reconcile behavior is covered by the existing fake
+  Kubernetes client tests.
+- The out-of-transaction interactive wait/retry shape remains intentionally deferred per D6.
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

codex
Implemented Increment B and left all changes uncommitted.

Key results:

- Added PostgreSQL migration `000086`, SQLite parity, models, API plumbing, reconciler priority/eviction behavior, authority propagation, deficit metrics, alert, documentation, and focused tests.
- `go build ./...` passed using a writable temporary `GOCACHE`.
- All feature and non-network tests passed.
- The full test command was attempted but two pre-existing `httptest.NewServer` suites cannot bind loopback ports in this sandbox. PostgreSQL integration was skipped because `SYNARA_TEST_DATABASE_URL` is unset.
- No Bun commands ran and nothing under `docs/reports/` changed.

Full file list, design decisions, exact test output, and remaining verification boundaries are in [.codex-report-increment-b.md](/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/.codex-report-increment-b.md).


diff --git a/.codex-report-increment-b.md b/.codex-report-increment-b.md
new file mode 100644
index 0000000000000000000000000000000000000000..d0bd133b85b11838edecf52d026b68d345a97d06
--- /dev/null
+++ b/.codex-report-increment-b.md
@@ -0,0 +1,109 @@
+# Increment B implementation report
+
+## Outcome
+
+Implemented guaranteed warm-pool `minIdleUnits` end to end in the Go control plane. Guaranteed slot plans are created
+before cold Execution Pods, remain subject to the target-wide Pod ceiling, are exempt from demand-fallback eviction,
+and project a fresh authority-derived deficit metric and warning alert. The worktree remains uncommitted.
+
+## Changed files
+
+- `services/control-plane/migrations/000086_worker_pool_min_idle.sql`
+  - Adds `worker_pools.min_idle_units`, the named `0 <= min <= desired` check, the authority mirror column, and the
+    `CREATE OR REPLACE` scope assertion.
+- `services/control-plane/internal/persistence/placement_models.go`
+- `services/control-plane/internal/persistence/warm_capacity_models.go`
+  - Adds the GORM model columns for PostgreSQL and SQLite AutoMigrate parity.
+- `services/control-plane/internal/database/store.go`
+- `services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go`
+- `services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go`
+  - Extends SQLite Worker Pool shape checks, adds insert/update authority-scope mirrors, and covers SQLite plus
+    PostgreSQL min-idle mismatch rejection.
+- `services/control-plane/internal/placement/service.go`
+- `services/control-plane/internal/placement/service_test.go`
+- `services/control-plane/internal/httpapi/placement_api_test.go`
+  - Adds request normalization/defaulting, bounds validation, persistence, response projection, and HTTP round-trip
+    coverage. `placement_api.go` already decodes and encodes the placement service DTOs directly, so no separate
+    handler mapper required a code change.
+- `services/control-plane/internal/executiontargets/kubernetes_warm_pool.go`
+- `services/control-plane/internal/executiontargets/kubernetes_reconciler.go`
+- `services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go`
+  - Splits stable plans into guaranteed and best-effort slices, prioritizes guaranteed creation, exempts guaranteed
+    slots from demand eviction, loads min-idle configuration, and tests split, truncation, and eviction behavior.
+- `services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go`
+- `services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go`
+- `services/control-plane/internal/warmcapacity/service.go`
+- `services/control-plane/internal/warmcapacity/service_test.go`
+  - Carries, validates, inserts, and CAS-updates the min-idle authority snapshot.
+- `services/control-plane/internal/observability/distributed_routing_billing_metrics.go`
+- `services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go`
+  - Exports bounded `desired_idle` and `min_idle` unit kinds and fresh-only per-row-clamped warm deficit sums.
+- `deploy/kubernetes/monitoring/prometheus-rules.yaml`
+  - Preserves the pre-existing Increment A rules and appends `SynaraWarmPoolMinIdleDeficit`.
+- `docs/contracts/worker-pool-placement-v1.md`
+  - Documents the guarantee, ceiling, eviction, deficit, and non-reservation/non-waiting boundaries.
+
+## Frozen design decisions applied
+
+- D1: `minIdleUnits` is a non-negative floor inside `desiredIdleUnits`; PostgreSQL authority scope and SQLite safety
+  triggers require the authority snapshot to match its Pool.
+- D2: slot indices `[0, minIdleUnits)` are guaranteed plans and are applied before the cold Pod loop. Remaining plans
+  retain their prior post-cold position. Pod naming and hashing still use the same plan objects in both reconcile passes.
+- D3: only plans with `slot >= minIdleUnits` enter the `warm-pool-demand-fallback` candidate set. Other recycling and
+  identity-safety deletion rules remain unchanged.
+- D4: the target-wide `maxActivePods` budget still caps all creation. Truncating guaranteed plans is not a reconcile
+  error; the published min/ready authority exposes the shortfall.
+- D5: `synara_worker_pool_warm_deficit{capacity_class}` sums
+  `max(0, min_idle_units - ready_idle_units)` per fresh row after bounded class collapse. Existing units also expose the
+  bounded `desired_idle` and `min_idle` kinds.
+- D6: no launch-transaction wait or retry was added. Placement keeps its existing cold-fallback behavior.
+
+## Verification
+
+No Bun command was run.
+
+`GOCACHE=/private/tmp/synara-increment-b-go-cache go build ./...` passed with no output.
+
+The requested unfiltered Go test command was run with the writable `GOCACHE`. All non-listener packages passed, but the
+managed sandbox prohibits `httptest.NewServer` from binding loopback ports. The exact output tail was:
+
+```text
+ok  	github.com/synara-ai/synara/services/control-plane/internal/placement	1.668s
+FAIL	github.com/synara-ai/synara/services/control-plane/internal/executiontargets	6.691s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/warmcapacity	3.727s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/observability	1.954s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/persistence	2.642s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/database	5.532s
+FAIL	github.com/synara-ai/synara/services/control-plane/internal/httpapi	5.527s
+FAIL
+```
+
+Both failures were pre-existing socket-backed tests whose panic was
+`httptest: failed to listen on a port: listen tcp6 [::1]:0: bind: operation not permitted`.
+
+Fresh supplemental package runs skipped only those pre-existing listener-backed cases and passed:
+
+```text
+ok  	github.com/synara-ai/synara/services/control-plane/internal/executiontargets	6.554s
+ok  	github.com/synara-ai/synara/services/control-plane/internal/httpapi	5.045s
+```
+
+The focused PostgreSQL migration test was discovered but skipped because the integration database is not configured:
+
+```text
+=== RUN   TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation
+    worker_pool_warm_capacity_migration_integration_test.go:19: SYNARA_TEST_DATABASE_URL is not configured
+--- SKIP: TestPostgresWorkerPoolWarmCapacityRejectsScopeAndMutation (0.00s)
+PASS
+ok  	github.com/synara-ai/synara/services/control-plane/internal/database	1.675s
+```
+
+## Remaining risks and intentionally unimplemented edges
+
+- Rerun the exact unfiltered Go test command in an environment that permits loopback test listeners; no code failure was
+  observed before the sandbox bind panics.
+- Run the PostgreSQL integration lane with `SYNARA_TEST_DATABASE_URL` to exercise migration `000086` and its deferred
+  scope trigger against PostgreSQL, not only SQLite/model/service coverage.
+- No live Kubernetes/OrbStack acceptance lane was run in this task; reconcile behavior is covered by the existing fake
+  Kubernetes client tests.
+- The out-of-transaction interactive wait/retry shape remains intentionally deferred per D6.
diff --git a/deploy/kubernetes/monitoring/prometheus-rules.yaml b/deploy/kubernetes/monitoring/prometheus-rules.yaml
index 28897ab525331db095367be87a646eed9a5ed492..cd10ea64ebd89359162329538740b99fdb29575e
--- a/deploy/kubernetes/monitoring/prometheus-rules.yaml
+++ b/deploy/kubernetes/monitoring/prometheus-rules.yaml
@@ -166,3 +166,11 @@
           annotations:
             summary: Warm Pool requests are frequently falling back to cold starts
             description: More than twenty percent of warm-requested Generations in the trailing 30-day window fell back to cold provisioning; warm capacity is undersized or unhealthy relative to interactive demand.
+        - alert: SynaraWarmPoolMinIdleDeficit
+          expr: sum by (capacity_class) (synara_worker_pool_warm_deficit) > 0
+          for: 10m
+          labels:
+            severity: warning
+          annotations:
+            summary: A guaranteed warm floor is unmet
+            description: The guaranteed warm floor has remained unmet for this capacity class for at least ten minutes; inspect target Pod budget, scheduling, and Worker readiness.
diff --git a/docs/contracts/worker-pool-placement-v1.md b/docs/contracts/worker-pool-placement-v1.md
index e3fe9579430379eeb8c446ac36cdfe38814bcd86..990c0206453b43f9fb1287b27284bac33a2684c2
--- a/docs/contracts/worker-pool-placement-v1.md
+++ b/docs/contracts/worker-pool-placement-v1.md
@@ -45,6 +45,14 @@
 storage, accelerators, and price data stay in the Pool's versioned scheduling template until a shared class registry is
 introduced.
 
+`desiredIdleUnits` is the Pool's total idle-warm target. `minIdleUnits` is a guaranteed floor inside that target:
+`0 <= minIdleUnits <= desiredIdleUnits`. For Kubernetes warm Pools, slots below the floor are replenished before cold
+Execution Pods and are exempt from demand-fallback eviction. Remaining desired slots are best-effort and retain the
+cold-first behavior. The guarantee does not override the Target's `maxActivePods` ceiling; budget truncation leaves the
+floor unmet and must be reported as a visible deficit rather than failing the reconcile pass.
+`minIdleUnits` guarantees reconcile priority and fail-visible capacity state, not a reservation for a specific launch or
+a bounded start time; placement does not wait inside its launch transaction when the floor is unavailable.
+
 `clusterId`, `region`, and `namespace` are target-local placement attributes in v1. For the first Kubernetes
 implementation, `clusterId` is the canonical local value `kubernetes` and Pool namespace must equal the Target's
 managed namespace. Cross-Target or global multi-cluster selection is deliberately outside this contract.
@@ -150,9 +158,9 @@
 Reconciler has proved reusable now.
 
 Each warm-capacity row is scoped to an exact tenant-owned Kubernetes Target, Pool ID/version, capacity class, and current
-release revision/channel. It freezes the Pool's desired/max configuration, observed claimed footprint, computed desired
-warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS version;
-missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
+release revision/channel. It freezes the Pool's desired/min/max configuration, observed claimed footprint, computed
+desired warm footprint, and ready-idle units. Observations have a bounded TTL, a monotonic observation time, and a CAS
+version; missing, stale, unsupported, or release-mismatched observations never advertise a warm hit.
 
 `readyIdleUnits` is published after the Reconciler validates exact Pod/Worker identity and subtracts ready Workers already
 needed by visible queued/recovering Executions. It is therefore a performance preference, not a reservation ledger or an
@@ -187,9 +195,12 @@
 labels.
 
 The live authority adds `synara_worker_pool_warm_capacity_authorities{capacity_class,freshness,warm_supported}` and
-`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_total`, `claimed`, and
-`ready_idle`; unit sums include only fresh observations. Unknown capacity classes collapse to `other`, and no
-Tenant/Target/Pool identifier is exposed as a label.
+`synara_worker_pool_warm_capacity_units{capacity_class,kind}`. `kind` is limited to `desired_idle`, `min_idle`,
+`desired_total`, `claimed`, and `ready_idle`; unit sums include only fresh observations. The live
+`synara_worker_pool_warm_deficit{capacity_class}` gauge sums `max(0, min_idle_units - ready_idle_units)` per fresh
+authority row. A non-zero deficit means the guaranteed floor is currently unmet, including when the Target-wide Pod
+budget truncated guaranteed-slot creation; it does not relax the ceiling or reserve a warm unit for a particular
+Execution. Unknown capacity classes collapse to `other`, and no Tenant/Target/Pool identifier is exposed as a label.
 
 Durable queue pressure adds `synara_execution_queue_depth{target_kind,capacity_class}` and
 `synara_execution_queue_oldest_age_seconds{target_kind,capacity_class}`. Both combine `queued | recovering`, bound
diff --git a/services/control-plane/internal/database/store.go b/services/control-plane/internal/database/store.go
index 44fe161b4f1d50d24dd2de86906d30c233811c4c..e04316b6a9f0ce19320d2a326170b97ddf3a51e0
--- a/services/control-plane/internal/database/store.go
+++ b/services/control-plane/internal/database/store.go
@@ -413,12 +413,12 @@
 		 WHERE status IN ('queued', 'recovering', 'leased', 'running', 'waiting-for-approval')`,
 		`INSERT INTO worker_pools (
 		   id, tenant_id, execution_target_id, name, mode, capacity_class,
-		   cluster_id, region, namespace, desired_idle_units, max_active_units,
+		   cluster_id, region, namespace, desired_idle_units, min_idle_units, max_active_units,
 		   scheduling_template, status, version, created_at, updated_at
 		 )
 		 SELECT target.id, target.tenant_id, target.id, 'default',
 		   CASE WHEN target.kind = 'kubernetes' THEN 'per-execution' ELSE 'resident' END,
-		   'standard', '', '', '', 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
+		   'standard', '', '', '', 0, 0, 1, '{}', 'active', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
 		 FROM execution_targets AS target
 		 WHERE NOT EXISTS (
 		   SELECT 1 FROM worker_pools AS pool
@@ -445,6 +445,8 @@
 		      OR NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <= 0
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -472,6 +474,8 @@
 		   WHERE NEW.status NOT IN ('active', 'draining', 'disabled')
 		      OR length(trim(NEW.name)) NOT BETWEEN 1 AND 160
 		      OR NEW.desired_idle_units < 0
+		      OR NEW.min_idle_units < 0
+		      OR NEW.min_idle_units > NEW.desired_idle_units
 		      OR NEW.max_active_units < NEW.desired_idle_units
 		      OR NEW.version <> OLD.version + 1
 		      OR json_valid(NEW.scheduling_template) = 0
@@ -489,6 +493,70 @@
 		 BEGIN
 		   SELECT RAISE(ABORT, 'Worker pools cannot be deleted');
 		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_insert`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_insert
+		 BEFORE INSERT ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
+		`DROP TRIGGER IF EXISTS trg_worker_pool_warm_capacity_scope_update`,
+		`CREATE TRIGGER trg_worker_pool_warm_capacity_scope_update
+		 BEFORE UPDATE ON worker_pool_warm_capacity
+		 BEGIN
+		   SELECT RAISE(ABORT, 'Worker pool warm capacity scope is invalid')
+		   WHERE NOT EXISTS (
+		     SELECT 1
+		     FROM worker_pools AS pool
+		     JOIN execution_targets AS target ON target.id = pool.execution_target_id
+		     WHERE pool.id = NEW.worker_pool_id
+		       AND pool.execution_target_id = NEW.execution_target_id
+		       AND pool.tenant_id IS NEW.tenant_id
+		       AND pool.version = NEW.worker_pool_version
+		       AND pool.mode = 'warm'
+		       AND pool.status <> 'disabled'
+		       AND pool.capacity_class = NEW.capacity_class
+		       AND pool.desired_idle_units = NEW.desired_idle_units
+		       AND pool.min_idle_units = NEW.min_idle_units
+		       AND pool.max_active_units = NEW.max_active_units
+		       AND target.tenant_id IS NOT NULL
+		       AND target.tenant_id IS NEW.tenant_id
+		       AND target.kind = 'kubernetes'
+		       AND target.status = 'active'
+		   )
+		   OR (NEW.worker_release_revision_id IS NOT NULL AND NOT EXISTS (
+		     SELECT 1
+		     FROM worker_release_revisions AS release
+		     WHERE release.id = NEW.worker_release_revision_id
+		       AND release.execution_target_id = NEW.execution_target_id
+		       AND release.tenant_id = NEW.tenant_id
+		   ));
+		 END`,
 		`DROP TRIGGER IF EXISTS trg_execution_placement_policy_insert`,
 		`CREATE TRIGGER trg_execution_placement_policy_insert
 		 BEFORE INSERT ON execution_placement_policies
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
index deabcf9dc79e775cf662b16f201cd5c8dfd45baf..c16cababfb35f86633c6f57e4230fb415ff1fdff
--- a/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_migration_integration_test.go
@@ -33,6 +33,7 @@
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &releaseChannel,
 		DesiredIdleUnits:        fixture.pool.DesiredIdleUnits,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		MaxActiveUnits:          fixture.pool.MaxActiveUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
@@ -66,6 +67,20 @@
 		db.Create(&invalidRelease).Error,
 		"Worker pool warm capacity scope is invalid",
 	)
+	assertWorkerPoolWarmCapacityConstraintRejected(
+		t,
+		db.Model(&persistence.WorkerPoolWarmCapacity{}).
+			Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
+				fixture.tenantID, fixture.target.ID, fixture.pool.ID, fixture.pool.Version).
+			Updates(map[string]any{
+				"min_idle_units": 0,
+				"version":        2,
+				"observed_at":    fixture.now.Add(time.Second),
+				"expires_at":     fixture.now.Add(2 * time.Minute),
+				"updated_at":     fixture.now.Add(time.Second),
+			}).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
 
 	if err := db.Model(&persistence.WorkerPoolWarmCapacity{}).
 		Where("tenant_id = ? AND execution_target_id = ? AND worker_pool_id = ? AND worker_pool_version = ?",
@@ -170,7 +185,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: target.ID,
 		Name: "warm-a", Mode: "warm", CapacityClass: "interactive",
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	otherPool := persistence.WorkerPool{
diff --git a/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..0a6825f92d0466eaa1a51880c5fd1fdd0e3c0bc5
--- /dev/null
+++ b/services/control-plane/internal/database/worker_pool_warm_capacity_sqlite_test.go
@@ -0,0 +1,60 @@
+package database
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/uuid"
+
+	"github.com/synara-ai/synara/services/control-plane/internal/persistence"
+)
+
+func TestSQLiteWorkerPoolMinIdleAndWarmCapacityScopeMatch(t *testing.T) {
+	fixture := openStage4SQLiteFixture(t)
+	now := time.Now().UTC().Truncate(time.Second)
+	target := persistence.ExecutionTarget{
+		ID: uuid.New(), TenantID: &fixture.tenantID, OrganizationID: &fixture.organizationID,
+		Kind: "kubernetes", Name: "SQLite guaranteed warm target", Status: "active",
+		ConfigurationEncrypted: []byte{}, Capabilities: map[string]any{}, CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&target).Error; err != nil {
+		t.Fatal(err)
+	}
+	pool := persistence.WorkerPool{
+		ID: uuid.New(), TenantID: &fixture.tenantID, ExecutionTargetID: target.ID,
+		Name: "guaranteed-warm", Mode: "warm", CapacityClass: "interactive",
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3,
+		SchedulingTemplate: map[string]any{}, Status: "active", Version: 1,
+		CreatedAt: now, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&pool).Error; err != nil {
+		t.Fatal(err)
+	}
+
+	invalidPool := pool
+	invalidPool.ID = uuid.New()
+	invalidPool.Name = "invalid-guaranteed-warm"
+	invalidPool.MinIdleUnits = invalidPool.DesiredIdleUnits + 1
+	assertSQLiteStage4Rejected(t, fixture.db.Create(&invalidPool).Error, "invalid Worker pool shape")
+
+	authority := persistence.WorkerPoolWarmCapacity{
+		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
+		TenantID: fixture.tenantID, ExecutionTargetID: target.ID,
+		CapacityClass: pool.CapacityClass, WarmSupported: true,
+		DesiredIdleUnits: pool.DesiredIdleUnits, MinIdleUnits: pool.MinIdleUnits, MaxActiveUnits: pool.MaxActiveUnits,
+		DesiredTotalUnits: 2, ReadyIdleUnits: 0, Source: "sqlite/min-idle-test",
+		ObservedAt: now, ExpiresAt: now.Add(time.Minute), Version: 1, UpdatedAt: now,
+	}
+	if err := fixture.db.Create(&authority).Error; err != nil {
+		t.Fatalf("SQLite rejected matching warm-capacity scope: %v", err)
+	}
+	assertSQLiteStage4Rejected(
+		t,
+		fixture.db.Exec(`
+			UPDATE worker_pool_warm_capacity
+			SET min_idle_units = 0
+			WHERE worker_pool_id = ? AND worker_pool_version = ?`, pool.ID, pool.Version,
+		).Error,
+		"Worker pool warm capacity scope is invalid",
+	)
+}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
index 916f94bb4a50e6c45c7736a4109187b6b5e2fc1b..35abfe12bfe1eb1d31b513ede6fdfad4fa30ee5b
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler.go
@@ -616,7 +616,7 @@
 		}
 		existing[pod.Name] = pod
 	}
-	_, validationWarmPlansByName, err := kubernetesWarmPodPlans(
+	_, _, validationWarmPlansByName, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -662,7 +662,7 @@
 		}
 		if observed.State == nil {
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -703,7 +703,7 @@
 				}
 			}
 			existing[observed.Pod.Name] = observed.Pod
-			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok {
+			if key, ok := kubernetesWarmCapacityKeyForPlan(plan); ok && !kubernetesWarmPodPlanGuaranteed(plan) {
 				warmDemandEvictionCandidates = append(warmDemandEvictionCandidates, kubernetesWarmDemandEvictionCandidate{
 					Observed: observed,
 					Key:      key,
@@ -805,7 +805,7 @@
 			}
 		}
 	}
-	desiredWarmPlans, _, err := kubernetesWarmPodPlans(
+	guaranteedWarmPlans, bestEffortWarmPlans, _, err := kubernetesWarmPodPlans(
 		warmPools, warmPoolsSupported, warmClaimedCounts, warmRelease, podBaseHash, configuration.Image,
 	)
 	if err != nil {
@@ -816,13 +816,43 @@
 	// capacity so reconciliation does not create a replacement that the API
 	// server must reject with an exceeded-quota error.
 	scheduled := len(existing) + deleted
+	var executionFailures []error
+	applyWarmPlans := func(plans []kubernetesWarmPodPlan) {
+		for _, plan := range plans {
+			if scheduled >= configuration.MaxActivePods {
+				break
+			}
+			name := kubernetesWarmPodName(plan)
+			if _, found := existing[name]; found {
+				continue
+			}
+			pod, err := r.warmPoolPod(target, configuration, plan, credential)
+			if err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
+				continue
+			}
+			path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
+			if err := client.Apply(ctx, path, pod); err != nil {
+				executionFailures = append(executionFailures, fmt.Errorf(
+					"warm pod %s: %w", name,
+					problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
+				))
+				continue
+			}
+			created++
+			scheduled++
+		}
+	}
+	// Guaranteed slots consume the target-wide budget before cold Execution
+	// Pods. Budget exhaustion truncates this slice without failing the pass;
+	// the published min/ready authority makes the resulting deficit visible.
+	applyWarmPlans(guaranteedWarmPlans)
 	// One Execution that cannot get a Pod must not starve every other queued
 	// Execution on this target for the cycle: a malformed pool scheduling
 	// template, a rejected Pod spec or a failed observation write is scoped to
 	// its own Execution. Failures are collected and reported after the sweep,
 	// so the pass is still reported as failed and warm capacity stays
 	// unpublished, but the remaining Executions are still placed.
-	var executionFailures []error
 	for _, execution := range executions {
 		if execution.Status != "queued" && execution.Status != "recovering" {
 			continue
@@ -897,30 +927,7 @@
 		created++
 		scheduled++
 	}
-	for _, plan := range desiredWarmPlans {
-		if scheduled >= configuration.MaxActivePods {
-			break
-		}
-		name := kubernetesWarmPodName(plan)
-		if _, found := existing[name]; found {
-			continue
-		}
-		pod, err := r.warmPoolPod(target, configuration, plan, credential)
-		if err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf("warm pod %s: %w", name, err))
-			continue
-		}
-		path := kubernetesNamespacedPath(configuration.Namespace, "pods", name)
-		if err := client.Apply(ctx, path, pod); err != nil {
-			executionFailures = append(executionFailures, fmt.Errorf(
-				"warm pod %s: %w", name,
-				problem.Wrap(502, "kubernetes_pod_apply_failed", "A Kubernetes Worker Pod could not be applied.", err),
-			))
-			continue
-		}
-		created++
-		scheduled++
-	}
+	applyWarmPlans(bestEffortWarmPlans)
 	if err := r.setKubernetesStatus(ctx, target, "active", foundationChanged, created+deleted > 0, created, deleted); err != nil {
 		return errors.Join(append(executionFailures, err)...)
 	}
@@ -1218,13 +1225,14 @@
 		ClusterID          string    `gorm:"column:cluster_id"`
 		Namespace          string    `gorm:"column:namespace"`
 		DesiredIdleUnits   int       `gorm:"column:desired_idle_units"`
+		MinIdleUnits       int       `gorm:"column:min_idle_units"`
 		MaxActiveUnits     int       `gorm:"column:max_active_units"`
 		SchedulingTemplate string    `gorm:"column:scheduling_template"`
 		Status             string    `gorm:"column:status"`
 	}
 	var rows []warmPoolRow
 	err := r.targets.db.WithContext(ctx).Table("worker_pools").
-		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, max_active_units, scheduling_template, status").
+		Select("id, version, capacity_class, cluster_id, namespace, desired_idle_units, min_idle_units, max_active_units, scheduling_template, status").
 		Where("execution_target_id = ? AND mode = ?", targetID, placement.PoolModeWarm).
 		Order("capacity_class, id").
 		Scan(&rows).Error
@@ -1242,7 +1250,7 @@
 		items = append(items, kubernetesWarmPool{
 			ID: row.ID, Version: row.Version, CapacityClass: row.CapacityClass,
 			ClusterID: row.ClusterID, Namespace: row.Namespace,
-			DesiredIdleUnits: row.DesiredIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
+			DesiredIdleUnits: row.DesiredIdleUnits, MinIdleUnits: row.MinIdleUnits, MaxActiveUnits: row.MaxActiveUnits,
 			SchedulingTemplate: template, Status: row.Status,
 		})
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
index f165b73d296b967ccd8e4c7ce4c979602083844f..09106d5aafad0b2a01622986e8cbf304e1dd438c
--- a/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_reconciler_test.go
@@ -754,6 +754,45 @@
 	}
 }
 
+func TestKubernetesReconcilerPrioritizesGuaranteedWarmPodsAndExposesBudgetTruncation(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 1
+	fixture.updateConfiguration(t, configuration)
+	pool := fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		2,
+		2,
+		placement.PoolStatusActive,
+	)
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+	var observations []ManagedKubernetesWarmCapacityObservation
+	fixture.reconciler.config.PublishWarmCapacity = func(_ context.Context, observation ManagedKubernetesWarmCapacityObservation) error {
+		observations = append(observations, observation)
+		return nil
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.pods) != 1 || client.kindCount("Pod") != 1 {
+		t.Fatalf("budget-truncated reconcile created pods=%d active=%#v", client.kindCount("Pod"), client.pods)
+	}
+	pod := onlyFakePod(t, client)
+	if pod.Labels[kubernetesWorkerModeLabel] != kubernetesWorkerModeWarmPool ||
+		pod.Labels[kubernetesWarmSlotLabel] != "0" ||
+		pod.Labels[kubernetesWorkerPoolIDLabel] != pool.ID.String() {
+		t.Fatalf("guaranteed warm Pod was not prioritized ahead of cold demand: %#v", pod.Labels)
+	}
+	if len(observations) != 1 || observations[0].MinIdleUnits != 2 ||
+		observations[0].DesiredTotalUnits != 2 || observations[0].ReadyIdleUnits != 0 {
+		t.Fatalf("budget-truncated warm authority = %#v", observations)
+	}
+}
+
 func TestKubernetesReconcilerReservesReadyWarmWorkerBeforeColdFallback(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	configuration := kubernetesTestConfiguration("")
@@ -1044,6 +1083,64 @@
 	}
 }
 
+func TestKubernetesReconcilerExemptsGuaranteedWarmSlotFromDemandFallbackEviction(t *testing.T) {
+	fixture := newKubernetesReconcileFixture(t, "")
+	configuration := kubernetesTestConfiguration("")
+	configuration["maxActivePods"] = 2
+	fixture.updateConfiguration(t, configuration)
+	fixture.createWarmPoolWithMinIdle(
+		t,
+		placement.CapacityClassInteractive,
+		2,
+		1,
+		2,
+		placement.PoolStatusActive,
+	)
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("execution_target_id = ?", fixture.targetID).
+		Update("status", "completed").Error; err != nil {
+		t.Fatal(err)
+	}
+	client := newFakeKubernetesClient()
+	fixture.reconciler.factory = &fakeKubernetesFactory{client: client}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	warmPodsBySlot := make(map[string]kubernetesPod)
+	for _, pod := range client.pods {
+		warmPodsBySlot[pod.Labels[kubernetesWarmSlotLabel]] = pod
+	}
+	if len(warmPodsBySlot) != 2 {
+		t.Fatalf("initial warm slots = %#v, want slots 0 and 1", warmPodsBySlot)
+	}
+	if err := fixture.db.Model(&persistence.AgentExecution{}).
+		Where("id = ?", fixture.executionIDs[0]).
+		Update("status", "queued").Error; err != nil {
+		t.Fatal(err)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if len(client.deletedPods) != 1 || client.deletedPods[0] != warmPodsBySlot["1"].Name {
+		t.Fatalf("demand fallback deleted %#v, want only best-effort slot 1", client.deletedPods)
+	}
+	if _, found := client.pods[warmPodsBySlot["0"].Name]; !found {
+		t.Fatalf("guaranteed slot 0 was evicted: active=%#v", client.pods)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); found {
+		t.Fatalf("cold Pod was created before deletion released quota: active=%#v", client.pods)
+	}
+
+	if err := fixture.reconciler.ReconcileOnce(context.Background()); err != nil {
+		t.Fatal(err)
+	}
+	if _, found := findExecutionPod(client, fixture.executionIDs[0]); !found {
+		t.Fatalf("cold demand did not use capacity released by best-effort slot: active=%#v", client.pods)
+	}
+}
+
 func TestKubernetesReconcilerRecyclesWarmWorkerWithStaleHeartbeat(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
 	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 1, placement.PoolStatusActive)
@@ -1617,10 +1714,10 @@
 func TestKubernetesWarmPodPlansBackfillIdleSlotAfterClaim(t *testing.T) {
 	pool := kubernetesWarmPool{
 		ID: uuid.New(), Version: 1, CapacityClass: placement.CapacityClassInteractive,
-		DesiredIdleUnits: 1, MaxActiveUnits: 2, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 3, SchedulingTemplate: map[string]any{},
 		Status: placement.PoolStatusActive,
 	}
-	plans, _, err := kubernetesWarmPodPlans(
+	guaranteed, bestEffort, plansByName, err := kubernetesWarmPodPlans(
 		[]kubernetesWarmPool{pool},
 		true,
 		map[uuid.UUID]int{pool.ID: 1},
@@ -1631,8 +1728,14 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if len(plans) != 2 || plans[0].Slot != 0 || plans[1].Slot != 1 {
-		t.Fatalf("warm plans after one claim = %#v, want occupied slot 0 plus idle backfill slot 1", plans)
+	if len(guaranteed) != 1 || guaranteed[0].Slot != 0 {
+		t.Fatalf("guaranteed warm plans = %#v, want slot 0", guaranteed)
+	}
+	if len(bestEffort) != 2 || bestEffort[0].Slot != 1 || bestEffort[1].Slot != 2 {
+		t.Fatalf("best-effort warm plans = %#v, want slots 1 and 2", bestEffort)
+	}
+	if len(plansByName) != 3 {
+		t.Fatalf("warm plan name index = %#v, want all three stable plans", plansByName)
 	}
 }
 
@@ -2165,13 +2268,22 @@
 	desiredIdleUnits, maxActiveUnits int,
 	status string,
 ) persistence.WorkerPool {
+	return f.createWarmPoolWithMinIdle(t, capacityClass, desiredIdleUnits, 0, maxActiveUnits, status)
+}
+
+func (f kubernetesReconcileFixture) createWarmPoolWithMinIdle(
+	t *testing.T,
+	capacityClass string,
+	desiredIdleUnits, minIdleUnits, maxActiveUnits int,
+	status string,
+) persistence.WorkerPool {
 	t.Helper()
 	now := time.Now().UTC()
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &f.tenantID, ExecutionTargetID: f.targetID,
 		Name: "warm-" + strings.ReplaceAll(uuid.NewString(), "-", "")[:8],
 		Mode: placement.PoolModeWarm, CapacityClass: capacityClass,
-		DesiredIdleUnits: desiredIdleUnits, MaxActiveUnits: maxActiveUnits,
+		DesiredIdleUnits: desiredIdleUnits, MinIdleUnits: minIdleUnits, MaxActiveUnits: maxActiveUnits,
 		SchedulingTemplate: map[string]any{}, Status: status, Version: 1,
 		CreatedAt: now, UpdatedAt: now,
 	}
diff --git a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
index 3c5b689fa2eac1ba652cbe5930cf3b1bad9fb596..e021224ecedda289da24290b792cd1b1d4114a7c
--- a/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
+++ b/services/control-plane/internal/executiontargets/kubernetes_warm_pool.go
@@ -23,6 +23,7 @@
 	ClusterID          string         `gorm:"column:cluster_id"`
 	Namespace          string         `gorm:"column:namespace"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template"`
 	Status             string         `gorm:"column:status"`
@@ -83,6 +84,10 @@
 	Key      kubernetesWarmCapacityKey
 }
 
+func kubernetesWarmPodPlanGuaranteed(plan kubernetesWarmPodPlan) bool {
+	return plan.Slot < plan.Pool.MinIdleUnits
+}
+
 func managedKubernetesWarmCapacityObservations(
 	tenantID uuid.UUID,
 	executionTargetID uuid.UUID,
@@ -107,6 +112,7 @@
 			WorkerPoolID:      pool.ID,
 			WorkerPoolVersion: pool.Version,
 			WarmSupported:     warmPoolsSupported,
+			MinIdleUnits:      pool.MinIdleUnits,
 			ClaimedUnits:      claimed,
 		}
 		if !warmPoolsSupported {
@@ -320,11 +326,12 @@
 	warmRelease kubernetesWarmReleaseSelection,
 	podBaseHash string,
 	baseImage string,
-) ([]kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
-	plans := make([]kubernetesWarmPodPlan, 0)
+) ([]kubernetesWarmPodPlan, []kubernetesWarmPodPlan, map[string]kubernetesWarmPodPlan, error) {
+	guaranteedPlans := make([]kubernetesWarmPodPlan, 0)
+	bestEffortPlans := make([]kubernetesWarmPodPlan, 0)
 	plansByName := make(map[string]kubernetesWarmPodPlan)
 	if !warmPoolsSupported {
-		return plans, plansByName, nil
+		return guaranteedPlans, bestEffortPlans, plansByName, nil
 	}
 	for _, pool := range warmPools {
 		if pool.Status != placement.PoolStatusActive {
@@ -342,14 +349,18 @@
 			plan := kubernetesWarmPodPlan{Pool: pool, Slot: slot, Release: warmRelease}
 			configHash, err := kubernetesWarmPoolPodHash(podBaseHash, baseImage, plan)
 			if err != nil {
-				return nil, nil, err
+				return nil, nil, nil, err
 			}
 			plan.ConfigHash = configHash
-			plans = append(plans, plan)
+			if slot < pool.MinIdleUnits {
+				guaranteedPlans = append(guaranteedPlans, plan)
+			} else {
+				bestEffortPlans = append(bestEffortPlans, plan)
+			}
 			plansByName[kubernetesWarmPodName(plan)] = plan
 		}
 	}
-	return plans, plansByName, nil
+	return guaranteedPlans, bestEffortPlans, plansByName, nil
 }
 
 func kubernetesWarmPodName(plan kubernetesWarmPodPlan) string {
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
index 031248d4ac73edad26757c55396750d3030a1946..d08c624ee564fdeb97f66fa53c6b35396c5fe516
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher.go
@@ -20,6 +20,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -73,6 +74,7 @@
 		WarmSupported:           observation.WarmSupported,
 		WorkerReleaseRevisionID: observation.WorkerReleaseRevisionID,
 		WorkerReleaseChannel:    observation.WorkerReleaseChannel,
+		MinIdleUnits:            observation.MinIdleUnits,
 		DesiredTotalUnits:       observation.DesiredTotalUnits,
 		ClaimedUnits:            observation.ClaimedUnits,
 		ReadyIdleUnits:          observation.ReadyIdleUnits,
diff --git a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
index d0463e111e781bcd5cc94feb39e15f44b6643c28..ceb32d9683f9b13d84eefbf71ce95ad347cbda25
--- a/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
+++ b/services/control-plane/internal/executiontargets/managed_kubernetes_warm_capacity_publisher_test.go
@@ -15,7 +15,7 @@
 
 func TestManagedKubernetesWarmCapacityPublisherWritesScopedObservation(t *testing.T) {
 	fixture := newKubernetesReconcileFixture(t, "")
-	pool := fixture.createWarmPool(t, placement.CapacityClassInteractive, 1, 2, placement.PoolStatusActive)
+	pool := fixture.createWarmPoolWithMinIdle(t, placement.CapacityClassInteractive, 1, 1, 2, placement.PoolStatusActive)
 	if err := fixture.db.Model(&persistence.ExecutionTarget{}).
 		Where("id = ?", fixture.targetID).
 		Update("status", "active").Error; err != nil {
@@ -31,7 +31,7 @@
 	if err := publisher.PublishReconcile(context.Background(), ManagedKubernetesWarmCapacityObservation{
 		TenantID: fixture.tenantID, ExecutionTargetID: fixture.targetID,
 		WorkerPoolID: pool.ID, WorkerPoolVersion: pool.Version,
-		WarmSupported: true, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
+		WarmSupported: true, MinIdleUnits: 1, DesiredTotalUnits: 1, ReadyIdleUnits: 1,
 	}); err != nil {
 		t.Fatal(err)
 	}
@@ -46,7 +46,7 @@
 		!capacity.ExpiresAt.Equal(observedAt.Add(10*time.Second)) {
 		t.Fatalf("warm capacity publisher metadata = %#v", capacity)
 	}
-	if !capacity.WarmSupported || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
+	if !capacity.WarmSupported || capacity.MinIdleUnits != 1 || capacity.DesiredTotalUnits != 1 || capacity.ClaimedUnits != 0 || capacity.ReadyIdleUnits != 1 {
 		t.Fatalf("warm capacity publisher counters = %#v", capacity)
 	}
 }
diff --git a/services/control-plane/internal/httpapi/placement_api_test.go b/services/control-plane/internal/httpapi/placement_api_test.go
index b28a498ffd5100902db043c2be37b8e6fd2f1f77..91aad6317d2335f3debe112d8a6d51d52e5389c6
--- a/services/control-plane/internal/httpapi/placement_api_test.go
+++ b/services/control-plane/internal/httpapi/placement_api_test.go
@@ -67,6 +67,7 @@
 		"region":             "",
 		"namespace":          "",
 		"desiredIdleUnits":   2,
+		"minIdleUnits":       1,
 		"maxActiveUnits":     6,
 		"schedulingTemplate": map[string]any{},
 		"status":             "active",
@@ -84,7 +85,7 @@
 	}
 	var created placement.Pool
 	decodePlacementHTTPResponse(t, createdResponse, &created)
-	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 {
+	if created.Name != "interactive-warm" || created.Mode != placement.PoolModeWarm || created.Version != 1 || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -98,6 +99,7 @@
 		"region":             "cn-east-1",
 		"namespace":          "synara-workers",
 		"desiredIdleUnits":   3,
+		"minIdleUnits":       2,
 		"maxActiveUnits":     8,
 		"schedulingTemplate": map[string]any{"priorityClassName": "interactive"},
 		"status":             "draining",
@@ -114,7 +116,7 @@
 	}
 	var updated placement.Pool
 	decodePlacementHTTPResponse(t, updatedResponse, &updated)
-	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining {
+	if updated.Name != "interactive-warm-primary" || updated.Version != 2 || updated.Status != placement.PoolStatusDraining || updated.MinIdleUnits != 2 {
 		t.Fatalf("updated pool = %#v", updated)
 	}
 
@@ -172,6 +174,7 @@
 			"region":             "cn-east-1",
 			"namespace":          "synara-workers",
 			"desiredIdleUnits":   1,
+			"minIdleUnits":       1,
 			"maxActiveUnits":     4,
 			"schedulingTemplate": map[string]any{},
 			"status":             "active",
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
index f1314eb622a5e6e47b6a15927a87672d0ef7ed46..26c9532d11bfe646f90efb696351e5588eb4238b
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics.go
@@ -34,9 +34,12 @@
 
 type warmCapacityUnitGroup struct {
 	CapacityClass     string `gorm:"column:capacity_class"`
+	DesiredIdleUnits  int64  `gorm:"column:desired_idle_units"`
+	MinIdleUnits      int64  `gorm:"column:min_idle_units"`
 	DesiredTotalUnits int64  `gorm:"column:desired_total_units"`
 	ClaimedUnits      int64  `gorm:"column:claimed_units"`
 	ReadyIdleUnits    int64  `gorm:"column:ready_idle_units"`
+	DeficitUnits      int64  `gorm:"column:deficit_units"`
 }
 
 type warmCapacityAuthorityMetricKey struct {
@@ -570,26 +573,38 @@
 	var unitRows []warmCapacityUnitGroup
 	if err := r.db.WithContext(ctx).Table("worker_pool_warm_capacity").
 		Select(`capacity_class,
+			SUM(desired_idle_units) AS desired_idle_units,
+			SUM(min_idle_units) AS min_idle_units,
 			SUM(desired_total_units) AS desired_total_units,
 			SUM(claimed_units) AS claimed_units,
-			SUM(ready_idle_units) AS ready_idle_units`).
+			SUM(ready_idle_units) AS ready_idle_units,
+			SUM(CASE
+				WHEN min_idle_units > ready_idle_units THEN min_idle_units - ready_idle_units
+				ELSE 0
+			END) AS deficit_units`).
 		Where("expires_at > ?", now).
 		Group("capacity_class").
 		Scan(&unitRows).Error; err != nil {
 		return fmt.Errorf("collect fresh Worker Pool warm capacity unit metrics: %w", err)
 	}
 	type unitTotals struct {
+		desiredIdle  int64
+		minIdle      int64
 		desiredTotal int64
 		claimed      int64
 		readyIdle    int64
+		deficit      int64
 	}
 	units := make(map[string]unitTotals)
 	for _, row := range unitRows {
 		capacityClass := boundedWarmCapacityClass(row.CapacityClass)
 		totals := units[capacityClass]
+		totals.desiredIdle += row.DesiredIdleUnits
+		totals.minIdle += row.MinIdleUnits
 		totals.desiredTotal += row.DesiredTotalUnits
 		totals.claimed += row.ClaimedUnits
 		totals.readyIdle += row.ReadyIdleUnits
+		totals.deficit += row.DeficitUnits
 		units[capacityClass] = totals
 	}
 	capacityClasses := make([]string, 0, len(units))
@@ -609,6 +624,8 @@
 			kind  string
 			value int64
 		}{
+			{kind: "desired_idle", value: totals.desiredIdle},
+			{kind: "min_idle", value: totals.minIdle},
 			{kind: "desired_total", value: totals.desiredTotal},
 			{kind: "claimed", value: totals.claimed},
 			{kind: "ready_idle", value: totals.readyIdle},
@@ -621,6 +638,20 @@
 			)
 		}
 	}
+	writeHelp(
+		output,
+		"synara_worker_pool_warm_deficit",
+		"Fresh authoritative guaranteed warm-floor deficit by bounded capacity class.",
+		"gauge",
+	)
+	for _, capacityClass := range capacityClasses {
+		fmt.Fprintf(
+			output,
+			"synara_worker_pool_warm_deficit%s %d\n",
+			labels(map[string]string{"capacity_class": capacityClass}),
+			units[capacityClass].deficit,
+		)
+	}
 	return nil
 }
 
diff --git a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
index cc450621eac4ed8449a55d133b143bc0e8c868b8..809489b338ae9410001faee1ab560292daa709b9
--- a/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
+++ b/services/control-plane/internal/observability/distributed_routing_billing_metrics_test.go
@@ -29,11 +29,11 @@
 	poolIDs := []uuid.UUID{uuid.New(), uuid.New(), uuid.New(), uuid.New(), uuid.New()}
 	unboundedClass := "tenant-" + uuid.NewString()
 	rows := []persistence.WorkerPoolWarmCapacity{
-		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 5, 2, 3, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 0, 4, 0, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 1, 1, now.Add(time.Minute)),
-		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 0, 7, 0, now),
-		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 4, 3, 2, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[0], tenantID, targetID, "standard", true, 4, 4, 5, 2, 3, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[1], tenantID, targetID, "standard", false, 2, 2, 0, 4, 0, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[2], tenantID, targetID, "interactive", true, 2, 2, 2, 1, 1, now.Add(time.Minute)),
+		warmCapacityMetricFixture(poolIDs[3], tenantID, targetID, "interactive", false, 7, 7, 0, 7, 0, now),
+		warmCapacityMetricFixture(poolIDs[4], tenantID, targetID, unboundedClass, true, 3, 3, 4, 3, 2, now.Add(time.Minute)),
 	}
 	if err := db.Create(&rows).Error; err != nil {
 		t.Fatal(err)
@@ -59,20 +59,29 @@
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="other",freshness="fresh",warm_supported="true"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="false"} 1`,
 		`synara_worker_pool_warm_capacity_authorities{capacity_class="standard",freshness="fresh",warm_supported="true"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_idle"} 2`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="min_idle"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="desired_total"} 2`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="claimed"} 1`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="interactive",kind="ready_idle"} 1`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_idle"} 3`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="min_idle"} 3`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="other",kind="desired_total"} 4`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_idle"} 6`,
+		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="min_idle"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="desired_total"} 5`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="claimed"} 6`,
 		`synara_worker_pool_warm_capacity_units{capacity_class="standard",kind="ready_idle"} 3`,
+		`synara_worker_pool_warm_deficit{capacity_class="interactive"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="other"} 1`,
+		`synara_worker_pool_warm_deficit{capacity_class="standard"} 3`,
 	} {
 		if !strings.Contains(metrics, expected) {
 			t.Fatalf("metrics omitted %q:\n%s", expected, metrics)
 		}
 	}
 	for _, capacityClass := range []string{"interactive", "other", "standard"} {
-		for _, kind := range []string{"desired_total", "claimed", "ready_idle"} {
+		for _, kind := range []string{"desired_idle", "min_idle", "desired_total", "claimed", "ready_idle"} {
 			sample := `synara_worker_pool_warm_capacity_units{capacity_class="` + capacityClass + `",kind="` + kind + `"}`
 			if count := strings.Count(metrics, sample); count != 1 {
 				t.Fatalf("warm-capacity sample %q appeared %d times, want exactly once:\n%s", sample, count, metrics)
@@ -82,6 +91,9 @@
 	if strings.Contains(metrics, `capacity_class="interactive",kind="claimed"} 8`) {
 		t.Fatalf("expired warm-capacity units contributed to fresh unit sums:\n%s", metrics)
 	}
+	if strings.Contains(metrics, `synara_worker_pool_warm_deficit{capacity_class="interactive"} 8`) {
+		t.Fatalf("expired warm-capacity deficit contributed to fresh deficit sums:\n%s", metrics)
+	}
 	for _, forbidden := range append([]string{tenantID.String(), targetID.String(), unboundedClass}, uuidStrings(poolIDs)...) {
 		if strings.Contains(metrics, forbidden) {
 			t.Fatalf("metrics leaked high-cardinality identifier %q:\n%s", forbidden, metrics)
@@ -93,6 +105,7 @@
 	poolID, tenantID, targetID uuid.UUID,
 	capacityClass string,
 	warmSupported bool,
+	desiredIdle, minIdle int,
 	desiredTotal, claimed, readyIdle int,
 	expiresAt time.Time,
 ) persistence.WorkerPoolWarmCapacity {
@@ -100,7 +113,7 @@
 	return persistence.WorkerPoolWarmCapacity{
 		WorkerPoolID: poolID, WorkerPoolVersion: 1, TenantID: tenantID, ExecutionTargetID: targetID,
 		CapacityClass: capacityClass, WarmSupported: warmSupported,
-		DesiredIdleUnits: 2, MaxActiveUnits: 10,
+		DesiredIdleUnits: desiredIdle, MinIdleUnits: minIdle, MaxActiveUnits: 10,
 		DesiredTotalUnits: desiredTotal, ClaimedUnits: claimed, ReadyIdleUnits: readyIdle,
 		Source: "test", ObservedAt: observedAt, ExpiresAt: expiresAt, Version: 1, UpdatedAt: observedAt,
 	}
diff --git a/services/control-plane/internal/persistence/placement_models.go b/services/control-plane/internal/persistence/placement_models.go
index 0c41de4f0493800a90cacee614afc21881a68f37..89833e353bd30bda8f0ccfeab2197c42e3fa2b71
--- a/services/control-plane/internal/persistence/placement_models.go
+++ b/services/control-plane/internal/persistence/placement_models.go
@@ -17,6 +17,7 @@
 	Region             string         `gorm:"column:region;not null;default:''"`
 	Namespace          string         `gorm:"column:namespace;not null;default:''"`
 	DesiredIdleUnits   int            `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits       int            `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits     int            `gorm:"column:max_active_units;not null;default:1"`
 	SchedulingTemplate map[string]any `gorm:"column:scheduling_template;serializer:json"`
 	Status             string         `gorm:"column:status;not null;default:active;index:idx_worker_pool_target_status,priority:2"`
diff --git a/services/control-plane/internal/persistence/warm_capacity_models.go b/services/control-plane/internal/persistence/warm_capacity_models.go
index 3bdb614abdb0223dfbeef87d3030dc0c70596d6f..db52018c1d473cd384c97c8cf1797217da10c7a0
--- a/services/control-plane/internal/persistence/warm_capacity_models.go
+++ b/services/control-plane/internal/persistence/warm_capacity_models.go
@@ -16,6 +16,7 @@
 	WorkerReleaseRevisionID *uuid.UUID `gorm:"column:worker_release_revision_id;type:uuid"`
 	WorkerReleaseChannel    *string    `gorm:"column:worker_release_channel"`
 	DesiredIdleUnits        int        `gorm:"column:desired_idle_units;not null;default:0"`
+	MinIdleUnits            int        `gorm:"column:min_idle_units;not null;default:0"`
 	MaxActiveUnits          int        `gorm:"column:max_active_units;not null;default:0"`
 	DesiredTotalUnits       int        `gorm:"column:desired_total_units;not null;default:0"`
 	ClaimedUnits            int        `gorm:"column:claimed_units;not null;default:0"`
diff --git a/services/control-plane/internal/placement/service.go b/services/control-plane/internal/placement/service.go
index ec23c0089efff69447ea29b27746174e4748dcfb..bf84c9f6317ee6c53f902e08d5ed7fea931c6b05
--- a/services/control-plane/internal/placement/service.go
+++ b/services/control-plane/internal/placement/service.go
@@ -47,6 +47,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       int            `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -86,6 +87,7 @@
 	Region             string         `json:"region"`
 	Namespace          string         `json:"namespace"`
 	DesiredIdleUnits   int            `json:"desiredIdleUnits"`
+	MinIdleUnits       *int           `json:"minIdleUnits"`
 	MaxActiveUnits     int            `json:"maxActiveUnits"`
 	SchedulingTemplate map[string]any `json:"schedulingTemplate"`
 	Status             string         `json:"status"`
@@ -273,6 +275,7 @@
 			Region:             normalized.Region,
 			Namespace:          normalized.Namespace,
 			DesiredIdleUnits:   normalized.DesiredIdleUnits,
+			MinIdleUnits:       *normalized.MinIdleUnits,
 			MaxActiveUnits:     normalized.MaxActiveUnits,
 			SchedulingTemplate: normalized.SchedulingTemplate,
 			Status:             normalized.Status,
@@ -364,6 +367,7 @@
 		current.Region = normalized.Region
 		current.Namespace = normalized.Namespace
 		current.DesiredIdleUnits = normalized.DesiredIdleUnits
+		current.MinIdleUnits = *normalized.MinIdleUnits
 		current.MaxActiveUnits = normalized.MaxActiveUnits
 		current.SchedulingTemplate = normalized.SchedulingTemplate
 		current.Status = normalized.Status
@@ -373,7 +377,7 @@
 			Where("id = ? AND version = ?", current.ID, expectedVersion).
 			Select(
 				"name", "mode", "capacity_class", "cluster_id", "region", "namespace",
-				"desired_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
+				"desired_idle_units", "min_idle_units", "max_active_units", "scheduling_template", "status", "version", "updated_at",
 			).
 			Updates(&current)
 		if result.Error != nil {
@@ -545,6 +549,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -660,6 +665,7 @@
 		Region:             "",
 		Namespace:          "",
 		DesiredIdleUnits:   0,
+		MinIdleUnits:       0,
 		MaxActiveUnits:     1,
 		SchedulingTemplate: map[string]any{},
 		Status:             PoolStatusActive,
@@ -744,6 +750,7 @@
 		Region:             model.Region,
 		Namespace:          model.Namespace,
 		DesiredIdleUnits:   model.DesiredIdleUnits,
+		MinIdleUnits:       model.MinIdleUnits,
 		MaxActiveUnits:     model.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             model.Status,
@@ -823,9 +830,13 @@
 	default:
 		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_status", "Worker pool status must be active, draining, or disabled.")
 	}
-	if input.DesiredIdleUnits < 0 || input.MaxActiveUnits < input.DesiredIdleUnits {
-		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool desiredIdleUnits and maxActiveUnits are invalid.")
+	minIdleUnits := 0
+	if input.MinIdleUnits != nil {
+		minIdleUnits = *input.MinIdleUnits
 	}
+	if input.DesiredIdleUnits < 0 || minIdleUnits < 0 || minIdleUnits > input.DesiredIdleUnits || input.MaxActiveUnits < input.DesiredIdleUnits {
+		return CreatePoolInput{}, problem.New(400, "invalid_worker_pool_capacity_bounds", "Worker pool capacity bounds require 0 <= minIdleUnits <= desiredIdleUnits <= maxActiveUnits.")
+	}
 	template := input.SchedulingTemplate
 	if template == nil {
 		template = map[string]any{}
@@ -838,6 +849,7 @@
 		Region:             strings.TrimSpace(input.Region),
 		Namespace:          strings.TrimSpace(input.Namespace),
 		DesiredIdleUnits:   input.DesiredIdleUnits,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     input.MaxActiveUnits,
 		SchedulingTemplate: template,
 		Status:             status,
diff --git a/services/control-plane/internal/placement/service_test.go b/services/control-plane/internal/placement/service_test.go
index 229fdc21dbd4722c55a5990ef0481705f873f4d6..b8b233e401646dd2eaee83097597a55538cd6212
--- a/services/control-plane/internal/placement/service_test.go
+++ b/services/control-plane/internal/placement/service_test.go
@@ -277,6 +277,7 @@
 	ctx := context.Background()
 	target := fixture.createTarget(t, "kubernetes", false)
 
+	minIdleUnits := 1
 	created, err := fixture.service.CreatePool(ctx, fixture.owner, fixture.tenantID, target.ID, CreatePoolInput{
 		Name:               "Warm Interactive",
 		Mode:               PoolModeWarm,
@@ -285,6 +286,7 @@
 		Region:             "cn-sha",
 		Namespace:          "workers",
 		DesiredIdleUnits:   1,
+		MinIdleUnits:       &minIdleUnits,
 		MaxActiveUnits:     3,
 		SchedulingTemplate: map[string]any{"priorityClassName": "interactive"},
 		Status:             PoolStatusActive,
@@ -292,7 +294,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive {
+	if created.Version != 1 || created.Mode != PoolModeWarm || created.CapacityClass != CapacityClassInteractive || created.MinIdleUnits != 1 {
 		t.Fatalf("created pool = %#v", created)
 	}
 
@@ -371,6 +373,42 @@
 	}
 }
 
+func TestNormalizePoolInputValidatesMinIdleUnits(t *testing.T) {
+	tests := []struct {
+		name        string
+		minIdle     *int
+		desiredIdle int
+		wantMin     int
+		wantCode    string
+	}{
+		{name: "omitted defaults to zero", desiredIdle: 2, wantMin: 0},
+		{name: "within desired idle accepted", minIdle: poolInputIntPointer(1), desiredIdle: 2, wantMin: 1},
+		{name: "negative rejected", minIdle: poolInputIntPointer(-1), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+		{name: "above desired idle rejected", minIdle: poolInputIntPointer(3), desiredIdle: 2, wantCode: "invalid_worker_pool_capacity_bounds"},
+	}
+	for _, test := range tests {
+		t.Run(test.name, func(t *testing.T) {
+			normalized, err := normalizePoolInput(CreatePoolInput{
+				Name: "warm", Mode: PoolModeWarm, CapacityClass: CapacityClassInteractive,
+				DesiredIdleUnits: test.desiredIdle, MinIdleUnits: test.minIdle, MaxActiveUnits: 4,
+				SchedulingTemplate: map[string]any{}, Status: PoolStatusActive,
+			})
+			if test.wantCode != "" {
+				if code := problemCode(err); code != test.wantCode {
+					t.Fatalf("problem code = %q, want %q (error: %v)", code, test.wantCode, err)
+				}
+				return
+			}
+			if err != nil {
+				t.Fatal(err)
+			}
+			if normalized.MinIdleUnits == nil || *normalized.MinIdleUnits != test.wantMin {
+				t.Fatalf("normalized minIdleUnits = %#v, want %d", normalized.MinIdleUnits, test.wantMin)
+			}
+		})
+	}
+}
+
 func TestUpdatePoolBlocksVersionAdvanceWhileNonterminalExecutionsReferenceSnapshot(t *testing.T) {
 	fixture := newPlacementFixture(t)
 	ctx := context.Background()
@@ -536,6 +574,8 @@
 	return pool
 }
 
+func poolInputIntPointer(value int) *int { return &value }
+
 func (f placementFixture) createWarmCapacity(
 	t *testing.T,
 	target persistence.ExecutionTarget,
diff --git a/services/control-plane/internal/warmcapacity/service.go b/services/control-plane/internal/warmcapacity/service.go
index 21ea5513f8eaa1f6a4a211fa01365bff6121e3b8..6c5d0f32c64b79db2344348996d190f4b07c2746
--- a/services/control-plane/internal/warmcapacity/service.go
+++ b/services/control-plane/internal/warmcapacity/service.go
@@ -35,6 +35,7 @@
 	WarmSupported           bool
 	WorkerReleaseRevisionID *uuid.UUID
 	WorkerReleaseChannel    *string
+	MinIdleUnits            int
 	DesiredTotalUnits       int
 	ClaimedUnits            int
 	ReadyIdleUnits          int
@@ -114,6 +115,12 @@
 		if pool.DesiredIdleUnits > pool.MaxActiveUnits {
 			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool desired idle units exceed max active units.")
 		}
+		if pool.MinIdleUnits < 0 || pool.MinIdleUnits > pool.DesiredIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Worker Pool min idle units are outside its desired idle units.")
+		}
+		if input.MinIdleUnits != pool.MinIdleUnits {
+			return problem.New(409, "warm_capacity_pool_shape_invalid", "Warm capacity min idle units do not match the Worker Pool.")
+		}
 		if input.DesiredTotalUnits > pool.MaxActiveUnits {
 			return problem.New(400, "invalid_warm_capacity_counters", "Warm desired total units must not exceed the Worker Pool max active units.")
 		}
@@ -146,6 +153,7 @@
 				WorkerReleaseRevisionID: releaseRevisionID,
 				WorkerReleaseChannel:    releaseChannel,
 				DesiredIdleUnits:        pool.DesiredIdleUnits,
+				MinIdleUnits:            input.MinIdleUnits,
 				MaxActiveUnits:          pool.MaxActiveUnits,
 				DesiredTotalUnits:       input.DesiredTotalUnits,
 				ClaimedUnits:            input.ClaimedUnits,
@@ -174,6 +182,7 @@
 		result.WorkerReleaseRevisionID = releaseRevisionID
 		result.WorkerReleaseChannel = releaseChannel
 		result.DesiredIdleUnits = pool.DesiredIdleUnits
+		result.MinIdleUnits = input.MinIdleUnits
 		result.MaxActiveUnits = pool.MaxActiveUnits
 		result.DesiredTotalUnits = input.DesiredTotalUnits
 		result.ClaimedUnits = input.ClaimedUnits
@@ -191,6 +200,7 @@
 			"worker_release_revision_id": result.WorkerReleaseRevisionID,
 			"worker_release_channel":     result.WorkerReleaseChannel,
 			"desired_idle_units":         result.DesiredIdleUnits,
+			"min_idle_units":             result.MinIdleUnits,
 			"max_active_units":           result.MaxActiveUnits,
 			"desired_total_units":        result.DesiredTotalUnits,
 			"claimed_units":              result.ClaimedUnits,
diff --git a/services/control-plane/internal/warmcapacity/service_test.go b/services/control-plane/internal/warmcapacity/service_test.go
index 93507408e568c183a17410c2faa72ce64b171ab2..afabf83ffb5a5224a1c6ee69cacfa1287d835c5a
--- a/services/control-plane/internal/warmcapacity/service_test.go
+++ b/services/control-plane/internal/warmcapacity/service_test.go
@@ -27,6 +27,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       3,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          2,
@@ -40,6 +41,7 @@
 	}
 	if observed.CapacityClass != fixture.pool.CapacityClass ||
 		observed.DesiredIdleUnits != fixture.pool.DesiredIdleUnits ||
+		observed.MinIdleUnits != fixture.pool.MinIdleUnits ||
 		observed.MaxActiveUnits != fixture.pool.MaxActiveUnits ||
 		observed.Version != 1 ||
 		observed.WorkerReleaseRevisionID == nil ||
@@ -74,6 +76,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       4,
 		ClaimedUnits:            2,
 		ReadyIdleUnits:          1,
@@ -113,6 +116,7 @@
 		WorkerPoolID:      fixture.pool.ID,
 		WorkerPoolVersion: fixture.pool.Version,
 		WarmSupported:     true,
+		MinIdleUnits:      fixture.pool.MinIdleUnits,
 		DesiredTotalUnits: 2,
 		ClaimedUnits:      1,
 		ReadyIdleUnits:    1,
@@ -150,6 +154,13 @@
 			code: "warm_capacity_pool_mode_invalid",
 		},
 		{
+			name: "min idle mismatch rejected",
+			mut: func(input *Observation) {
+				input.MinIdleUnits = fixture.pool.MinIdleUnits + 1
+			},
+			code: "warm_capacity_pool_shape_invalid",
+		},
+		{
 			name: "unsupported counters rejected",
 			mut: func(input *Observation) {
 				input.WarmSupported = false
@@ -206,6 +217,7 @@
 		WarmSupported:           true,
 		WorkerReleaseRevisionID: &fixture.release.ID,
 		WorkerReleaseChannel:    &channel,
+		MinIdleUnits:            fixture.pool.MinIdleUnits,
 		DesiredTotalUnits:       2,
 		ClaimedUnits:            1,
 		ReadyIdleUnits:          1,
@@ -317,7 +329,7 @@
 	pool := persistence.WorkerPool{
 		ID: uuid.New(), TenantID: &tenantID, ExecutionTargetID: targetID,
 		Name: "warm-default", Mode: poolModeWarm, CapacityClass: capacityClassInteractive,
-		DesiredIdleUnits: 2, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
+		DesiredIdleUnits: 2, MinIdleUnits: 1, MaxActiveUnits: 5, SchedulingTemplate: map[string]any{},
 		Status: "active", Version: 1, CreatedAt: now, UpdatedAt: now,
 	}
 	if err := db.Create(&pool).Error; err != nil {
diff --git a/services/control-plane/migrations/000086_worker_pool_min_idle.sql b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
new file mode 100644
index 0000000000000000000000000000000000000000..8e508f9b0a8fc7d7a2e250aef5331d0e60808ca1
--- /dev/null
+++ b/services/control-plane/migrations/000086_worker_pool_min_idle.sql
@@ -0,0 +1,55 @@
+ALTER TABLE worker_pools
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+ALTER TABLE worker_pools
+  ADD CONSTRAINT chk_worker_pools_min_idle_units CHECK (
+    min_idle_units >= 0 AND min_idle_units <= desired_idle_units
+  );
+
+ALTER TABLE worker_pool_warm_capacity
+  ADD COLUMN min_idle_units INTEGER NOT NULL DEFAULT 0;
+
+CREATE OR REPLACE FUNCTION assert_worker_pool_warm_capacity_scope()
+RETURNS TRIGGER
+LANGUAGE plpgsql
+AS $$
+DECLARE
+  pool worker_pools%ROWTYPE;
+  target execution_targets%ROWTYPE;
+  release worker_release_revisions%ROWTYPE;
+BEGIN
+  SELECT * INTO pool FROM worker_pools WHERE id = NEW.worker_pool_id;
+  IF NOT FOUND
+     OR pool.execution_target_id <> NEW.execution_target_id
+     OR pool.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR pool.version <> NEW.worker_pool_version
+     OR pool.mode <> 'warm'
+     OR pool.status = 'disabled'
+     OR pool.capacity_class <> NEW.capacity_class
+     OR pool.desired_idle_units <> NEW.desired_idle_units
+     OR pool.min_idle_units <> NEW.min_idle_units
+     OR pool.max_active_units <> NEW.max_active_units THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  SELECT * INTO target FROM execution_targets WHERE id = NEW.execution_target_id;
+  IF NOT FOUND
+     OR target.tenant_id IS NULL
+     OR target.tenant_id IS DISTINCT FROM NEW.tenant_id
+     OR target.kind <> 'kubernetes'
+     OR target.status <> 'active' THEN
+    RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+  END IF;
+
+  IF NEW.worker_release_revision_id IS NOT NULL THEN
+    SELECT * INTO release FROM worker_release_revisions WHERE id = NEW.worker_release_revision_id;
+    IF NOT FOUND
+       OR release.execution_target_id <> NEW.execution_target_id
+       OR release.tenant_id <> NEW.tenant_id THEN
+      RAISE EXCEPTION 'Worker pool warm capacity scope is invalid' USING ERRCODE = '23514';
+    END IF;
+  END IF;
+
+  RETURN NEW;
+END;
+$$;

tokens used
312,816
Implemented Increment B and left all changes uncommitted.

Key results:

- Added PostgreSQL migration `000086`, SQLite parity, models, API plumbing, reconciler priority/eviction behavior, authority propagation, deficit metrics, alert, documentation, and focused tests.
- `go build ./...` passed using a writable temporary `GOCACHE`.
- All feature and non-network tests passed.
- The full test command was attempted but two pre-existing `httptest.NewServer` suites cannot bind loopback ports in this sandbox. PostgreSQL integration was skipped because `SYNARA_TEST_DATABASE_URL` is unset.
- No Bun commands ran and nothing under `docs/reports/` changed.

Full file list, design decisions, exact test output, and remaining verification boundaries are in [.codex-report-increment-b.md](/Users/huang/devel/project/huang/business/synara/.claude/worktrees/fast-cloud-agents/.codex-report-increment-b.md).


codex exit: 0
