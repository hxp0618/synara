# ARCHIVED: repository scan snapshot (2026-04-16)

This directory is a point-in-time repository review from 2026-04-16. It predates the Go control
plane, the SaaS stages recorded in `TODO.md`, and the current contracts under `docs/contracts/`.

Do not use it as current guidance:

- the file links inside these documents are absolute paths from the original reviewer's machine and
  are all broken;
- the architecture, priorities, and issue lists reflect the April codebase, much of which has since
  been restructured or resolved.

It is retained only as historical context. If it stops being useful, deleting the whole directory is
safe; nothing outside this directory links into it.
