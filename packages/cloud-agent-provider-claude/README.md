# `@synara/cloud-agent-provider-claude`

Portable Claude provider implementation for `@synara/cloud-agent-runtime`. The public package is host-neutral; `claudeAgent` remains the compatible wire kind for the first release.
