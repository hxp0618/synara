# `@synara/cloud-agent-provider-codex`

Portable Codex provider implementation for `@synara/cloud-agent-runtime`. It is explicitly registered by a host or distribution and does not scan user directories or `node_modules`.
